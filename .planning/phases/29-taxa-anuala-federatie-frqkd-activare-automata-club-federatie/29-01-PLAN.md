---
phase: 29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-SCHEMA-AUDIT.md
  - sql/migrations/taxa_anuala_federatie_schema_260912.sql
  - sql/migrations/taxa_anuala_federatie_backfill_260912.sql
autonomous: true
requirements: [TAF-01, TAF-02, TAF-03]
must_haves:
  truths:
    - "Tabela deconturi_federatie are club_id si an_fiscal si nu poate contine doua deconturi pentru acelasi club in acelasi sezon"
    - "SUPER_ADMIN_FEDERATIE poate scrie pretul unui sezon in taxa_anuala_config; un ADMIN_CLUB nu poate"
    - "Orice utilizator autentificat poate citi pretul sezonului curent"
    - "Cei 37 de sportivi cu factura FRQKD 2025-2026 au viza activa pe an=2025 si apar intr-un decont FRQKD 2025 al clubului lor"
    - "Re-rularea migratiei de backfill nu creeaza randuri duplicate"
  artifacts:
    - path: ".planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-SCHEMA-AUDIT.md"
      provides: "Schema live verificata (coloane, constrangeri, RLS, FORCE RLS, CHECK status_viza, triggere existente) + verdict pe blocajele B1-B6"
      contains: "B1"
    - path: "sql/migrations/taxa_anuala_federatie_schema_260912.sql"
      provides: "ALTER deconturi_federatie + constrangeri unicitate decont_sportivi/vize_sportivi + CREATE taxa_anuala_config + RLS + seed 2026=170"
      contains: "taxa_anuala_config"
    - path: "sql/migrations/taxa_anuala_federatie_backfill_260912.sql"
      provides: "Backfill idempotent al celor 37 facturi FRQKD sezon 2025-2026 in deconturi_federatie / vize_sportivi / decont_sportivi"
      contains: "FRQKD Sezonul 2025-2026"
  key_links:
    - from: "sql/migrations/taxa_anuala_federatie_backfill_260912.sql"
      to: "public.deconturi_federatie(club_id, an_fiscal)"
      via: "ON CONFLICT pe indexul unic creat in migratia de schema"
      pattern: "ON CONFLICT"
    - from: "public.taxa_anuala_config"
      to: "public.utilizator_roluri_multicont"
      via: "politica RLS de INSERT/UPDATE restransa la SUPER_ADMIN_FEDERATIE"
      pattern: "SUPER_ADMIN_FEDERATIE"
---

<objective>
Repara schema orfana `deconturi_federatie` pe DB-ul live (proiect Supabase `wuhidifzsutwgdfkwhmd`), adauga constrangerile de unicitate care fac idempotenta posibila, creeaza tabela de configurare a pretului `taxa_anuala_config` cu RLS, si aduce cele 37 de facturi FRQKD istorice (sezon 2025-2026) in noul model de date.

Purpose: fara aceste coloane si constrangeri, functia de activare automata din planul 29-02 nu poate scrie nimic — `ON CONFLICT (club_id, an_fiscal)` si `ON CONFLICT (sportiv_id, an)` au nevoie de indecsi unici existenti, iar `taxa_anuala_config` este sursa pretului.
Output: un document de audit al schemei live + doua fisiere de migratie aplicate live si verificate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-CONTEXT.md
@docs/superpowers/specs/2026-09-12-taxa-anuala-federatie-design.md
@sql/migrations/create_missing_tables.sql
@sql/migrations/create_vize_sportivi.sql
@sql/migrations/add_decont_sportivi.sql
@sql/migrations/fix_deconturi_federatie_policy.sql
</context>

## Maparea deciziilor utilizatorului (D-NN)

`29-CONTEXT.md` nu numeroteaza deciziile. Aceasta tabela este referinta canonica pentru toate cele 4 planuri ale fazei 29; ID-urile D-NN citate in actiuni trimit aici.

| ID | Decizie (sectiunea din 29-CONTEXT.md) |
|----|----------------------------------------|
| D-01 | An fiscal federatie fix: `luna(CURRENT_DATE) >= 9 ? an_curent : an_curent - 1`, independent de tabela `sezoane` |
| D-02 | `deconturi_federatie` se ALTEREAZA (nu se recreeaza): pastreaza `dovada_transfer_url`, `confirmata_federatie`, `data_decont`; adauga `club_id`, `an_fiscal`, `tip_activitate` default `'FRQKD'`, `nr_participanti` default 0, `status_plata` default `'In asteptare'` check `In asteptare|Platit`, `metoda_plata` check `Cash|Transfer Bancar|Revolut`; `UNIQUE (club_id, an_fiscal)` |
| D-03 | `decont_sportivi` reutilizeaza coloana `an` ca an fiscal; se adauga `UNIQUE (sportiv_id, an)` |
| D-04 | `vize_sportivi` este gate-ul central de idempotenta, un rand per `(sportiv_id, an)`; se adauga `UNIQUE (sportiv_id, an)` daca lipseste |
| D-05 | `taxa_anuala_config` NOU: `id`, `an_fiscal int unique`, `suma numeric check (suma >= 0)`, `created_at`. RLS: INSERT/UPDATE doar `SUPER_ADMIN_FEDERATIE`, SELECT deschis oricui autentificat. Seed `an_fiscal=2026, suma=170` |
| D-06 | Functie `activeaza_taxa_anuala(p_sportiv_id uuid)` SECURITY DEFINER + 4 triggere AFTER INSERT FOR EACH ROW pe `inscrieri_examene.sportiv_id`, `stagii_cvd_participare.sportiv_id`, `participare_stagiu.practicant_id`, `inscrieri_competitie.sportiv_id` |
| D-07 | Logica idempotenta in 6 pasi: an fiscal → gate `vize_sportivi` ON CONFLICT DO NOTHING RETURNING → club_id sportiv → suma din config → INSERT `plati` tip `'FRQKD'` status `'Neachitat'` + legare `plata_id` → get-or-create `deconturi_federatie` + UPDATE incremental → INSERT `decont_sportivi` |
| D-08 | Esecul pe lipsa pret este comportament acceptat: `RAISE EXCEPTION` cu rollback complet al tranzactiei sursa, fara randuri orfane |
| D-09 | `FederationInvoices.tsx`: lista sportivilor vine din `decont_sportivi` (fara selectie manuala) + camp nou "Metoda Plata" (Cash / Transfer Bancar / Revolut) scris in `metoda_plata`, impreuna cu `status_plata='Platit'` si `confirmata_federatie=true` |
| D-10 | Ecran mic pentru `SUPER_ADMIN_FEDERATIE` (an_fiscal + suma) implementat ca tab intr-un ecran existent de administrare, nu modul separat |
| D-11 | Backfill istoric rulat o singura data, in aceeasi migratie care creeaza schema: an_fiscal=2025, `status_plata='In asteptare'` (nu exista dovada de virament catre federatie) |

## Artifacts this phase produces

Contributia acestui plan la inventarul fazei 29 (inventarul complet este repetat in fiecare plan):

| Tip | Simbol | Creat in |
|-----|--------|----------|
| Coloana | `deconturi_federatie.club_id` | 29-01 |
| Coloana | `deconturi_federatie.an_fiscal` | 29-01 |
| Coloana | `deconturi_federatie.tip_activitate` | 29-01 |
| Coloana | `deconturi_federatie.nr_participanti` | 29-01 |
| Coloana | `deconturi_federatie.status_plata` | 29-01 |
| Coloana | `deconturi_federatie.metoda_plata` | 29-01 |
| Coloana | `deconturi_federatie.data_generare` | 29-01 |
| Constrangere | `deconturi_federatie_club_an_fiscal_key` (UNIQUE club_id, an_fiscal) | 29-01 |
| Constrangere | `deconturi_federatie_status_plata_check` | 29-01 |
| Constrangere | `deconturi_federatie_metoda_plata_check` | 29-01 |
| Constrangere | `decont_sportivi_sportiv_an_key` (UNIQUE sportiv_id, an) | 29-01 |
| Constrangere | `vize_sportivi_sportiv_an_key` (UNIQUE sportiv_id, an) | 29-01 |
| Tabela | `public.taxa_anuala_config` | 29-01 |
| Politica RLS | `taxa_anuala_config_select` / `_insert` / `_update` | 29-01 |
| Functie SQL | `public.an_fiscal_federatie(date)` | 29-02 |
| Functie SQL | `public.activeaza_taxa_anuala(uuid)` | 29-02 |
| Functie SQL | `public.trg_activeaza_taxa_anuala()` | 29-02 |
| Trigger | `trg_taxa_anuala_inscrieri_examene` | 29-02 |
| Trigger | `trg_taxa_anuala_stagii_cvd` | 29-02 |
| Trigger | `trg_taxa_anuala_participare_stagiu` | 29-02 |
| Trigger | `trg_taxa_anuala_inscrieri_competitie` | 29-02 |
| Tip TS | `TaxaAnualaFederatieConfig` (types.ts) | 29-03 |
| Campuri TS | `DecontFederatie.an_fiscal`, `.metoda_plata`, `.data_decont`, `.dovada_transfer_url`, `.confirmata_federatie` | 29-03 |
| Util TS | `utils/anFiscal.ts` → `getAnFiscalFederatie`, `formatSezon` | 29-03 |
| Stare app | `useDataProvider.taxaAnualaFederatieConfig` + `setTaxaAnualaFederatieConfig` | 29-03 |
| Componenta UI | `PaymentConfirmationModal` cu lista read-only + select `metoda_plata` | 29-03 |
| Componenta UI | `TabTaxaFederatieFRQKD` (tab in TaxeAnuale.tsx) | 29-04 |

<tasks>

<task type="auto">
  <name>Task 1: Audit schema live si verdict pe blocaje inainte de orice ALTER</name>
  <files>.planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-SCHEMA-AUDIT.md</files>
  <read_first>
    - .planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-CONTEXT.md (deciziile blocate)
    - docs/superpowers/specs/2026-09-12-taxa-anuala-federatie-design.md (sectiunile "Schema DB" si "Migrare / backfill istoric")
    - sql/migrations/create_vize_sportivi.sql (CHECK-ul declarat pe status_viza: 'Activ' / 'Inactiv' / 'Suspendat')
    - sql/migrations/add_decont_sportivi.sql (UNIQUE(decont_id, sportiv_id) existent + ON DELETE RESTRICT pe sportiv_id)
    - sql/migrations/create_missing_tables.sql liniile 163-272 (varianta de deconturi_federatie din repo, NEaplicata live)
  </read_first>
  <action>
Ruleaza interogari read-only pe proiectul Supabase live `wuhidifzsutwgdfkwhmd` folosind unealta MCP Supabase `execute_sql` si scrie rezultatele in `29-SCHEMA-AUDIT.md`. Repo-ul are fisiere de migratie NEaplicate (`create_missing_tables.sql` descrie o tabela `deconturi_federatie` care nu exista in forma asta live), deci nimic din repo nu poate fi considerat adevar despre schema live — toate afirmatiile din documentul de audit trebuie sa vina din interogari rulate acum.

Interogari obligatorii (una per punct, rezultatul copiat integral in document):
1. `information_schema.columns` filtrat pe `table_schema='public'` si `table_name IN ('deconturi_federatie','decont_sportivi','vize_sportivi','plati','sportivi','taxe_anuale_config')`, coloanele `table_name, column_name, data_type, is_nullable, column_default`, ordonat dupa `table_name, ordinal_position`.
2. `pg_constraint` cu `pg_get_constraintdef(oid)` pentru `conrelid` in `deconturi_federatie`, `decont_sportivi`, `vize_sportivi`, `plati`.
3. `pg_indexes` pentru `tablename IN ('plati','vize_sportivi','decont_sportivi','deconturi_federatie')`.
4. `pg_policies` (`tablename, policyname, cmd, roles, qual, with_check`) pentru aceleasi 4 tabele plus `sportivi`.
5. `pg_class` coloanele `relname, relrowsecurity, relforcerowsecurity` pentru `plati`, `deconturi_federatie`, `decont_sportivi`, `vize_sportivi`, `sportivi`.
6. `pg_trigger` necombinat cu `tgisinternal`, cu `pg_get_triggerdef(oid)`, pentru `inscrieri_examene`, `stagii_cvd_participare`, `participare_stagiu`, `inscrieri_competitie` — plus confirmarea ca exista coloanele `sportiv_id` respectiv `practicant_id` pe fiecare.
7. Numaratori: `count(*)` pe `deconturi_federatie`, `decont_sportivi`, `vize_sportivi`, `taxe_anuale_config`.
8. Profilul celor 37 de facturi istorice: din `plati` cu `tip='FRQKD'` si `descriere='FRQKD Sezonul 2025-2026'`, grupat pe `club_id`, cu `count(*)`, `count(DISTINCT sportiv_id)`, `count(*) FILTER (WHERE sportiv_id IS NULL)`, `sum(suma)`, `min(data)`, `max(data)`.
9. Duplicate care ar bloca indecsii unici noi: din `decont_sportivi` grupat pe `(sportiv_id, an)` avand `count(*) > 1`; acelasi lucru pe `vize_sportivi` grupat pe `(sportiv_id, an)`.
10. Rolul proprietar al tabelelor tinta: `tableowner` din `pg_tables` pentru `plati`, `deconturi_federatie`, `decont_sportivi`, `vize_sportivi`.

In `29-SCHEMA-AUDIT.md`, dupa rezultatele brute, scrie o sectiune `## Verdict blocaje` cu exact aceste sase intrari, fiecare marcata `BLOCANT` sau `OK` si justificata prin numarul interogarii:
- B1 — exista index/constrangere UNIQUE pe `plati(sportiv_id, an)`? (`components/Plati/TaxeAnuale.tsx` linia 625 face upsert cu `onConflict: 'sportiv_id,an'`, deci constrangerea ar putea exista). Daca exista, INSERT-ul in `plati` din D-07 pasul 4 va esua pentru orice sportiv care are deja un rand `Abonament` sau `Taxa Anuala` pe acelasi `an` — marcheaza BLOCANT si opreste planificarea schemei pana se decide cu utilizatorul.
- B2 — `relforcerowsecurity` este `true` pe vreuna dintre `plati`, `deconturi_federatie`, `decont_sportivi`, `vize_sportivi`? FORCE ROW LEVEL SECURITY anuleaza bypass-ul RLS al unei functii SECURITY DEFINER chiar si cand ruleaza ca proprietar (vezi memoria proiectului `feedback_rls_force_security_definer_recursion`). Daca `true`, noteaza pentru planul 29-02 ca functia are nevoie de politici dedicate sau de un rol cu BYPASSRLS.
- B3 — valorile permise de CHECK-ul pe `vize_sportivi.status_viza`. Spec-ul de design scrie `'Activa'`, migratia din repo declara `'Activ'`. Consemneaza valoarea reala acceptata live; toate migratiile si functiile fazei 29 trebuie sa o foloseasca pe aceea.
- B4 — `vize_sportivi.data_platii` este NOT NULL? Daca da, atat backfill-ul cat si functia trigger trebuie sa furnizeze o valoare.
- B5 — `decont_sportivi.sportiv_id` are ON DELETE RESTRICT? Consemneaza impactul asupra `merge_sportivi` / deduplicare (fisierul `components/Sportivi/DeduplicareSportivi/index.tsx` liniile 139-140 listeaza deja `decont_sportivi` si `vize_sportivi` ca tabele de remapat).
- B6 — exista duplicate reale care blocheaza `UNIQUE (sportiv_id, an)` pe `decont_sportivi` sau pe `vize_sportivi` (interogarea 9)?

Nu aplica niciun DDL in acest task. Documentul este singurul output.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-SCHEMA-AUDIT.md &amp;&amp; for t in B1 B2 B3 B4 B5 B6; do grep -q "^- $t " .planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-SCHEMA-AUDIT.md || { echo "lipseste verdict $t"; exit 1; }; done &amp;&amp; grep -qE "relforcerowsecurity" .planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-SCHEMA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `29-SCHEMA-AUDIT.md` exista si contine rezultatele brute ale celor 10 interogari, fiecare sub un titlu numerotat.
    - Sectiunea `## Verdict blocaje` contine exact sase intrari `- B1 ` … `- B6 `, fiecare cu eticheta `BLOCANT` sau `OK`.
    - B3 consemneaza literalmente lista de valori din CHECK-ul live pe `vize_sportivi.status_viza`.
    - B1 consemneaza explicit `EXISTA` sau `NU EXISTA` pentru un index unic pe `plati(sportiv_id, an)`, cu numele indexului daca exista.
    - Documentul nu contine nicio afirmatie despre schema care sa nu fie sustinuta de output-ul unei interogari incluse in document.
    - Zero instructiuni DDL rulate pe DB in acest task (niciun `apply_migration`, niciun `ALTER`/`CREATE` prin `execute_sql`).
  </acceptance_criteria>
  <done>Documentul de audit exista, are verdicte pentru B1-B6, iar continuarea catre Task 2 este posibila doar daca B1 si B6 sunt `OK` (altfel se opreste si se raporteaza blocajul).</done>
</task>

<task type="auto">
  <name>Task 2: Migratie schema — ALTER deconturi_federatie, constrangeri unicitate, tabela taxa_anuala_config</name>
  <files>sql/migrations/taxa_anuala_federatie_schema_260912.sql</files>
  <read_first>
    - .planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-SCHEMA-AUDIT.md (verdictele B1-B6 produse in Task 1)
    - .planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-CONTEXT.md (D-02, D-03, D-04, D-05)
    - sql/migrations/fix_deconturi_federatie_policy.sql (politica SELECT curenta pe deconturi_federatie + pattern-ul `utilizator_roluri_multicont`)
    - sql/migrations/add_decont_sportivi.sql (pattern-ul de politici RLS folosit in acest repo pentru tabele federale)
    - sql/migrations/mfa_email_verificari_260907.sql (conventia de antet/denumire pentru migratiile recente)
  </read_first>
  <action>
Scrie `sql/migrations/taxa_anuala_federatie_schema_260912.sql` respectand conventia locala (antet comentat cu `-- Migration:`, `-- Subiect:`, `-- Rulare:`), apoi aplica-l live cu unealta MCP Supabase `apply_migration` pe proiectul `wuhidifzsutwgdfkwhmd`, cu numele migratiei `taxa_anuala_federatie_schema_260912`.

Continutul migratiei, in aceasta ordine:

Sectiunea 1 — ALTER `public.deconturi_federatie` (D-02), toate aditive, niciun DROP/RENAME pe coloanele existente `id`, `data_decont`, `suma_totala`, `dovada_transfer_url`, `confirmata_federatie`, `created_at`:
`ADD COLUMN IF NOT EXISTS club_id uuid REFERENCES public.cluburi(id) ON DELETE CASCADE`;
`ADD COLUMN IF NOT EXISTS an_fiscal integer`;
`ADD COLUMN IF NOT EXISTS tip_activitate text DEFAULT 'FRQKD'`;
`ADD COLUMN IF NOT EXISTS nr_participanti integer DEFAULT 0`;
`ADD COLUMN IF NOT EXISTS status_plata text DEFAULT 'In asteptare'`;
`ADD COLUMN IF NOT EXISTS metoda_plata text`;
`ADD COLUMN IF NOT EXISTS data_generare timestamptz DEFAULT now()`.
`data_generare` nu apare in D-02, dar `types.ts` (`DecontFederatie.data_generare`), `components/FederationInvoices.tsx` liniile 241-243 si 341, si `components/AdminMasterMap.tsx` il citesc deja; fara coloana, UI-ul fazei ramane rupt. Marcheaza-l in comentariu drept adaugare descoperita in planificare, nu decizie de utilizator, si populeaza-l retroactiv cu `coalesce(data_decont::timestamptz, created_at)` pentru randurile unde e NULL.

Constrangeri pe `deconturi_federatie`, adaugate idempotent (blocuri `DO $$ ... IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = ...) THEN ... END IF; END $$;`):
`deconturi_federatie_status_plata_check` — `status_plata IN ('In asteptare','Platit')`;
`deconturi_federatie_metoda_plata_check` — `metoda_plata IS NULL OR metoda_plata IN ('Cash','Transfer Bancar','Revolut')`;
`CREATE UNIQUE INDEX IF NOT EXISTS deconturi_federatie_club_an_fiscal_key ON public.deconturi_federatie (club_id, an_fiscal)` — obligatoriu inainte de orice `ON CONFLICT (club_id, an_fiscal)` din 29-02 si din migratia de backfill.
Indecsi de suport: `deconturi_federatie_club_id_idx` pe `club_id`, `deconturi_federatie_status_idx` pe `status_plata`, `deconturi_federatie_data_generare_idx` pe `data_generare DESC` — toate `IF NOT EXISTS`.
`club_id` si `an_fiscal` primesc `SET NOT NULL` doar daca Task 1 (interogarea 7) a raportat `count(*) = 0` pe `deconturi_federatie`; implementeaza asta ca bloc `DO` conditionat de `(SELECT count(*) FROM public.deconturi_federatie) = 0`, si comenteaza in fisier de ce.

Sectiunea 2 — constrangeri de idempotenta (D-03, D-04):
`CREATE UNIQUE INDEX IF NOT EXISTS decont_sportivi_sportiv_an_key ON public.decont_sportivi (sportiv_id, an)` — se adauga PE LANGA `UNIQUE(decont_id, sportiv_id)` existent, care ramane neatins pentru ca `components/FederationInvoices.tsx` linia 288 il foloseste ca `onConflict`.
`CREATE UNIQUE INDEX IF NOT EXISTS vize_sportivi_sportiv_an_key ON public.vize_sportivi (sportiv_id, an)` — se creeaza doar daca Task 1 nu a gasit deja o constrangere echivalenta; daca exista sub alt nume, noteaza numele real intr-un comentariu si sari peste creare.

Sectiunea 3 — `CREATE TABLE IF NOT EXISTS public.taxa_anuala_config` (D-05) cu exact patru coloane: `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `an_fiscal integer NOT NULL UNIQUE`, `suma numeric(10,2) NOT NULL CHECK (suma >= 0)`, `created_at timestamptz DEFAULT now()`. Nu adauga alte coloane — D-05 defineste exhaustiv forma tabelei.
`ALTER TABLE public.taxa_anuala_config ENABLE ROW LEVEL SECURITY`.
Politici, toate `DROP POLICY IF EXISTS` inainte de `CREATE POLICY`, folosind pattern-ul `utilizator_roluri_multicont` din `add_decont_sportivi.sql`:
`taxa_anuala_config_select` FOR SELECT TO authenticated USING (true) — SELECT deschis oricui autentificat, per D-05, pentru ca UI-ul de club sa poata afisa suma;
`taxa_anuala_config_insert` FOR INSERT TO authenticated WITH CHECK — `EXISTS (SELECT 1 FROM public.utilizator_roluri_multicont WHERE user_id = auth.uid() AND rol_denumire = 'SUPER_ADMIN_FEDERATIE')`;
`taxa_anuala_config_update` FOR UPDATE TO authenticated USING (aceeasi conditie) WITH CHECK (aceeasi conditie).
Nu crea politica de DELETE — absenta ei inseamna refuz implicit pentru toti (deny-by-default), ceea ce protejeaza pretul unui sezon deja facturat.

Sectiunea 4 — seed (D-05): `INSERT INTO public.taxa_anuala_config (an_fiscal, suma) VALUES (2026, 170) ON CONFLICT (an_fiscal) DO NOTHING;`

Sectiunea 5 — comentariu final cu interogarile de verificare post-aplicare (aceeasi conventie ca in `fix_deconturi_federatie_policy.sql`).

Dupa aplicare, ruleaza verificarile prin `execute_sql` si lipeste rezultatele in SUMMARY.
  </action>
  <verify>
    <automated>grep -v '^--' sql/migrations/taxa_anuala_federatie_schema_260912.sql | grep -c "deconturi_federatie_club_an_fiscal_key" | grep -qv '^0$' &amp;&amp; grep -v '^--' sql/migrations/taxa_anuala_federatie_schema_260912.sql | grep -c "taxa_anuala_config_insert" | grep -qv '^0$' &amp;&amp; ! grep -v '^--' sql/migrations/taxa_anuala_federatie_schema_260912.sql | grep -qiE "drop (table|column)|rename (table|column)"</automated>
  </verify>
  <acceptance_criteria>
    - Fisierul exista in `sql/migrations/` si nu contine niciun `DROP TABLE`, `DROP COLUMN`, `RENAME TABLE` sau `RENAME COLUMN` (verificat pe liniile fara comentariu).
    - Migratia a fost aplicata live prin `apply_migration` cu numele `taxa_anuala_federatie_schema_260912`, fara eroare.
    - `SELECT column_name FROM information_schema.columns WHERE table_name='deconturi_federatie'` returneaza toate cele 7 coloane noi plus cele 6 vechi (13 in total).
    - `SELECT indexname FROM pg_indexes WHERE tablename='deconturi_federatie'` include `deconturi_federatie_club_an_fiscal_key`.
    - `SELECT indexname FROM pg_indexes WHERE tablename IN ('decont_sportivi','vize_sportivi')` include un index unic pe `(sportiv_id, an)` pentru fiecare dintre cele doua tabele.
    - `SELECT an_fiscal, suma FROM public.taxa_anuala_config` returneaza exact un rand: `2026, 170.00`.
    - `SELECT policyname, cmd FROM pg_policies WHERE tablename='taxa_anuala_config'` returneaza exact 3 randuri (SELECT, INSERT, UPDATE) si niciunul pentru DELETE.
    - Re-aplicarea aceluiasi fisier (rulat a doua oara prin `execute_sql`) se termina fara eroare si fara sa modifice numarul de randuri din `taxa_anuala_config`.
  </acceptance_criteria>
  <done>Schema live accepta `ON CONFLICT (club_id, an_fiscal)` pe `deconturi_federatie` si `ON CONFLICT (sportiv_id, an)` pe `vize_sportivi`/`decont_sportivi`; pretul sezonului 2026 este 170 si doar SUPER_ADMIN_FEDERATIE il poate modifica.</done>
</task>

<task type="auto">
  <name>Task 3: Migratie backfill idempotent pentru cele 37 facturi FRQKD 2025-2026</name>
  <files>sql/migrations/taxa_anuala_federatie_backfill_260912.sql</files>
  <read_first>
    - .planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-SCHEMA-AUDIT.md (verdictele B3 pentru valoarea `status_viza` si B4 pentru `data_platii`, plus profilul celor 37 de randuri din interogarea 8)
    - sql/migrations/taxa_anuala_federatie_schema_260912.sql (indecsii unici pe care se sprijina clauzele ON CONFLICT)
    - docs/superpowers/specs/2026-09-12-taxa-anuala-federatie-design.md sectiunea "Migrare / backfill istoric"
  </read_first>
  <action>
Scrie `sql/migrations/taxa_anuala_federatie_backfill_260912.sql` si aplica-l live prin `apply_migration` cu numele `taxa_anuala_federatie_backfill_260912`. Este o migratie peste date financiare reale de productie: fiecare instructiune trebuie sa fie re-rulabila fara sa duplice nimic (D-11).

Filtrul comun al sursei, folosit identic in toate cele trei instructiuni: din `public.plati` unde `tip = 'FRQKD'` si `descriere = 'FRQKD Sezonul 2025-2026'`. Declara-l o singura data ca CTE `sursa` la inceputul fiecarei instructiuni, pentru a nu rata randuri intre pasi.

Pasul 1 — decont agregat per club: `INSERT INTO public.deconturi_federatie (club_id, an_fiscal, tip_activitate, suma_totala, nr_participanti, status_plata, data_generare)` selectand din `sursa` unde `club_id IS NOT NULL`, grupat pe `club_id`, cu `2025` ca `an_fiscal`, `'FRQKD'` ca `tip_activitate`, `sum(suma)` ca `suma_totala`, `count(DISTINCT sportiv_id)` ca `nr_participanti` (nu `count(*)` — daca un sportiv are doua facturi, decontul trebuie sa il numere o singura data, ca sa ramana coerent cu `decont_sportivi` care are acum `UNIQUE (sportiv_id, an)`), `'In asteptare'` ca `status_plata` per D-11, si `min(data)::timestamptz` ca `data_generare`. Termina cu `ON CONFLICT (club_id, an_fiscal) DO NOTHING`.

Pasul 2 — vize: `INSERT INTO public.vize_sportivi (sportiv_id, an, status_viza, plata_id, data_platii)` selectand `DISTINCT ON (sportiv_id)` din `sursa` unde `sportiv_id IS NOT NULL`, cu `2025` ca `an`, valoarea de `status_viza` confirmata de verdictul B3 din audit (foloseste valoarea reala din CHECK-ul live, nu `'Activa'` din spec — spec-ul contrazice migratia `create_vize_sportivi.sql`), `id` ca `plata_id` si `data` ca `data_platii`, ordonat `sportiv_id, data ASC, id ASC` ca sa fie determinist la re-rulare. Termina cu `ON CONFLICT (sportiv_id, an) DO NOTHING`.

Pasul 3 — legatura decont↔sportiv: `INSERT INTO public.decont_sportivi (decont_id, sportiv_id, an)` selectand `DISTINCT d.id, p.sportiv_id, 2025` din `sursa p` JOIN `public.deconturi_federatie d` pe `d.club_id = p.club_id AND d.an_fiscal = 2025`, unde `p.sportiv_id IS NOT NULL`. Foloseste `ON CONFLICT DO NOTHING` fara tinta explicita, ca sa fie acoperite ambele constrangeri unice de pe tabela (`decont_sportivi_sportiv_an_key` si `UNIQUE(decont_id, sportiv_id)`).

Nu modifica si nu sterge niciun rand din `plati` — cele 37 de facturi raman exact cum sunt, inclusiv cea cu `suma = 0` si statusurile mixte Achitat/Neachitat.

Adauga in coada fisierului, drept comentariu, interogarile de verificare, si ruleaza-le dupa aplicare prin `execute_sql`.
  </action>
  <verify>
    <automated>grep -v '^--' sql/migrations/taxa_anuala_federatie_backfill_260912.sql | grep -c "ON CONFLICT" | awk '$1>=3{exit 0} {exit 1}' &amp;&amp; ! grep -v '^--' sql/migrations/taxa_anuala_federatie_backfill_260912.sql | grep -qiE "update[[:space:]]+public\.plati|delete[[:space:]]+from[[:space:]]+public\.plati|update[[:space:]]+plati|delete[[:space:]]+from[[:space:]]+plati"</automated>
  </verify>
  <acceptance_criteria>
    - Fisierul contine cel putin 3 clauze `ON CONFLICT` (una per instructiune INSERT) pe liniile fara comentariu.
    - Fisierul nu contine niciun `UPDATE plati` sau `DELETE FROM plati`.
    - Dupa aplicare: `SELECT count(*) FROM public.deconturi_federatie WHERE an_fiscal = 2025` returneaza numarul de cluburi distincte raportat de interogarea 8 din audit (asteptat: 1).
    - `SELECT nr_participanti, suma_totala, status_plata FROM public.deconturi_federatie WHERE an_fiscal = 2025` are `nr_participanti` egal cu `count(DISTINCT sportiv_id)` din audit, `suma_totala` egala cu `sum(suma)` din audit, si `status_plata = 'In asteptare'`.
    - `SELECT count(*) FROM public.vize_sportivi WHERE an = 2025` este egal cu `count(DISTINCT sportiv_id)` din audit.
    - `SELECT count(*) FROM public.decont_sportivi WHERE an = 2025` este egal cu acelasi numar.
    - `SELECT count(*) FROM public.plati WHERE tip='FRQKD' AND descriere='FRQKD Sezonul 2025-2026'` este inca 37 dupa migratie (neatinse).
    - Rularea a doua oara a intregului continut al fisierului prin `execute_sql` nu schimba niciuna dintre numaratorile de mai sus (idempotenta dovedita, nu presupusa).
  </acceptance_criteria>
  <done>Cei 37 de sportivi FRQKD 2025-2026 au viza pe `an=2025`, apar in `decont_sportivi`, iar clubul lor are un decont FRQKD 2025 in asteptare; re-rularea migratiei nu produce randuri noi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client autenticat → PostgREST → `taxa_anuala_config` | orice utilizator logat poate emite INSERT/UPDATE pe tabela de preturi; singurul control este RLS |
| migratie rulata manual → date financiare live | DDL/DML aplicat pe productie cu 3500+ sportivi tinta si 7 cluburi active |
| `deconturi_federatie` intre cluburi | un ADMIN_CLUB nu trebuie sa vada sau sa modifice decontul altui club |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Elevation of Privilege | `taxa_anuala_config` INSERT/UPDATE | mitigate | politici `taxa_anuala_config_insert` si `_update` restrictionate la `EXISTS(... utilizator_roluri_multicont ... rol_denumire='SUPER_ADMIN_FEDERATIE')`, cu `WITH CHECK` identic cu `USING` pentru a bloca si mutarea unui rand in afara domeniului permis |
| T-29-02 | Tampering | stergerea pretului unui sezon deja facturat | mitigate | nicio politica DELETE pe `taxa_anuala_config` → refuz implicit pentru toti utilizatorii, inclusiv SUPER_ADMIN (stergerea ramane operatiune manuala de DBA) |
| T-29-03 | Information Disclosure | `taxa_anuala_config` SELECT deschis | accept | tabela contine doar `an_fiscal` si `suma` — pretul public al taxei federale, deja comunicat cluburilor; D-05 cere explicit SELECT deschis pentru afisarea in UI de club |
| T-29-04 | Tampering | ALTER pe tabela financiara live | mitigate | exclusiv operatii aditive (`ADD COLUMN IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`); `SET NOT NULL` doar conditionat de `count(*) = 0`; gate automat in verify care respinge orice `DROP`/`RENAME` de tabela sau coloana |
| T-29-05 | Tampering | backfill rulat de doua ori peste facturi reale | mitigate | fiecare INSERT are `ON CONFLICT DO NOTHING` sprijinit pe indecsi unici creati in Task 2; criteriu de acceptare dedicat cere dovada empirica a idempotentei prin re-rulare |
| T-29-06 | Repudiation | decont istoric marcat platit fara dovada | mitigate | backfill-ul seteaza `status_plata = 'In asteptare'` per D-11 — nu se afirma niciodata un virament pentru care nu exista dovada |
| T-29-07 | Information Disclosure | ADMIN_CLUB vede deconturile altor cluburi | accept | politica `deconturi_federatie_select` existenta (`fix_deconturi_federatie_policy.sql`) permite oricarui ADMIN_CLUB sa vada toate deconturile; filtrarea pe club se face azi in `FederationInvoices.tsx` linia 246. Este un gap RLS preexistent, in afara scope-ului fazei 29 — se consemneaza in SUMMARY ca todo si se coreleaza cu todo-ul deschis `2026-08-28-audit-politici-rls-fantoma-pe-tabele-in-afara-fazei-25` |
| T-29-SC | Tampering | instalari npm/pip/cargo | accept | faza 29 nu instaleaza niciun pachet nou (constrangere de proiect: fara librarii externe noi); nu se aplica poarta de legitimitate |
</threat_model>

<verification>
Rulate dupa Task 3, prin `execute_sql` pe `wuhidifzsutwgdfkwhmd`:
1. Structura: `deconturi_federatie` are 13 coloane; indexul `deconturi_federatie_club_an_fiscal_key` exista.
2. Idempotenta constrangerilor: un `INSERT ... ON CONFLICT (club_id, an_fiscal) DO NOTHING` repetat intr-o tranzactie cu ROLLBACK nu creeaza al doilea rand.
3. RLS: `SELECT policyname, cmd FROM pg_policies WHERE tablename='taxa_anuala_config'` → 3 politici, niciuna pentru DELETE.
4. Backfill: numaratorile din criteriile de acceptare ale Task 3 se potrivesc cu profilul din audit.
5. Datele istorice intacte: `SELECT count(*), sum(suma) FROM plati WHERE tip='FRQKD' AND descriere='FRQKD Sezonul 2025-2026'` identic cu valoarea din audit.
</verification>

<success_criteria>
- `29-SCHEMA-AUDIT.md` documenteaza schema live si verdictele B1-B6, toate sustinute de output de interogare.
- Ambele migratii exista in `sql/migrations/`, sunt aplicate live si sunt re-rulabile fara efect.
- `taxa_anuala_config` contine `(2026, 170)` si este scriabila doar de SUPER_ADMIN_FEDERATIE.
- Decontul FRQKD 2025 exista pentru clubul `cbb0b228-b3e0-4735-9658-70999eb256c6` cu `status_plata = 'In asteptare'`.
- Niciun rand din `plati` nu a fost modificat sau sters.
</success_criteria>

<output>
Create `.planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-01-SUMMARY.md` when done. Include in SUMMARY: verdictele B1-B6, numaratorile post-backfill, si orice abatere fata de D-02..D-05 impusa de schema live.
</output>
