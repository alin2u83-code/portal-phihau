---
phase: 31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa
plan: 05
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - components/Plati/JurnalIncasari.tsx
  - components/Plati/FacturiPersonale.tsx
  - components/Plati/FinancialDashboard.tsx
  - components/Plati/hub/TabIncasari.tsx
  - components/Plati/hub/TabRapoarte.tsx
autonomous: true
requirements: [D-02, D-03]
user_setup: []

must_haves:
  truths:
    - "Tab-ul Încasări randeaza JurnalIncasari sau IstoricPlati cu props identice celor din AppRouter (D-02)"
    - "JurnalIncasari montat in hub primeste facturile preselectate (platiInitiale) si un onBack care e invocat si automat la 1500 ms dupa o incasare reusita — fluxul F2/F3 ramane functional"
    - "Tab-ul Rapoarte randeaza RaportFinanciar sau FinancialDashboard cu props identice celor din AppRouter (D-03)"
    - "IstoricPlati montat standalone pentru rolul SPORTIV isi pastreaza butonul 'Înapoi la Portal'"
  artifacts:
    - path: "components/Plati/hub/TabIncasari.tsx"
      provides: "Tab-ul Încasări al hub-ului"
      exports: ["TabIncasari", "TabIncasariProps"]
      min_lines: 40
    - path: "components/Plati/hub/TabRapoarte.tsx"
      provides: "Tab-ul Rapoarte al hub-ului"
      exports: ["TabRapoarte"]
      min_lines: 30
    - path: "components/Plati/JurnalIncasari.tsx"
      provides: "Prop optional hideBackButton (onBack automat pastrat)"
      contains: "hideBackButton"
  key_links:
    - from: "components/Plati/hub/TabIncasari.tsx"
      to: "components/Plati/JurnalIncasari.tsx"
      via: "platiInitiale={platiPentruIncasare} onIncasareProcesata onBack={onJurnalBack}"
      pattern: "onBack=\\{onJurnalBack\\}"
    - from: "components/Plati/hub/TabRapoarte.tsx"
      to: "components/Plati/RaportFinanciar.tsx"
      via: "istoricPlatiDetaliat={filteredData.istoricPlatiDetaliat}"
      pattern: "istoricPlatiDetaliat=\\{filteredData\\.istoricPlatiDetaliat\\}"
---

<objective>
Tab-urile "Încasări" (D-02: inlocuieste `jurnal-incasari`, `istoric-plati`) si "Rapoarte" (D-03: inlocuieste `raport-financiar`, `financial-dashboard`) ale hub-ului, cu props identice hartii verificate in 31-01.

Punctul critic: fluxul de incasare multipla (F1-F4 din antetul `platiHubConfig.ts`). JurnalIncasari primeste facturile selectate in Facturi prin `platiInitiale`, iar dupa orice incasare reusita apeleaza `onIncasareProcesata()` si apoi AUTOMAT `onBack()` la 1500 ms (JurnalIncasari.tsx:493-494). De aceea TabIncasari NU transmite `onBack`-ul generic al hub-ului catre Jurnal, ci un callback dedicat `onJurnalBack` (implementat de PlatiHub in 31-08 ca oglinda a `handleJurnalBack` din AppRouter.tsx:78-85). Butonul "Înapoi" al Jurnalului e ascuns in hub (`hideBackButton`), dar apelul automat ramane neatins.

RaportFinanciar nu randeaza niciun buton de intoarcere (prop-ul `onBack` e declarat dar nefolosit — verificat la planificare: `grep -n onBack components/Plati/RaportFinanciar.tsx` arata doar liniile 30 si 53), deci nu primeste `hideBackButton` si nu se modifica in acest plan.

Output: `TabIncasari.tsx`, `TabRapoarte.tsx`; `hideBackButton` optional pe JurnalIncasari, IstoricPlati (FacturiPersonale.tsx), FinancialDashboard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-CONTEXT.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-01-SUMMARY.md
@CLAUDE.md
@components/Plati/hub/platiHubConfig.ts
</context>

<interfaces>
Contract consumat (31-01):
```ts
export interface PlatiHubTabProps {
  sectiune: SectiuneHub; currentUser: User; permissions: Permissions; activeRoleContext: any;
  onViewSportiv: (sportiv: Sportiv, tab?: TabProfilSportiv) => void; onBack: () => void;
}
```

Contracte create de acest plan (consumate de PlatiHub in 31-08):
```ts
// components/Plati/hub/TabIncasari.tsx
export interface TabIncasariProps extends PlatiHubTabProps {
  platiPentruIncasare: Plata[];   // state App.tsx:50, trecut prin AppRouter → PlatiHub
  onIncasareProcesata: () => void; // goleste selectia (AppRouter.tsx:87-89)
  onJurnalBack: () => void;        // oglinda handleJurnalBack (AppRouter.tsx:78-85)
}
export const TabIncasari: React.FC<TabIncasariProps>;

// components/Plati/hub/TabRapoarte.tsx
export const TabRapoarte: React.FC<PlatiHubTabProps>;
```

Props exacte (harta 31-01):
- JurnalIncasari: currentUser, permissions, plati=filteredData.plati, setPlati, sportivi=filteredData.sportivi, familii=filteredData.familii, preturiConfig (nefiltrat), tipuriAbonament=filteredData.tipuriAbonament, tipuriPlati (nefiltrat), setTipuriPlati, tranzactii=filteredData.tranzactii, setTranzactii, platiInitiale, onIncasareProcesata, onBack, reduceri (NEFILTRAT), onViewSportiv
- IstoricPlati: onBack, viewedUser=currentUser, plati=filteredData.plati, tranzactii=filteredData.tranzactii
- RaportFinanciar: onBack, istoricPlatiDetaliat=filteredData.istoricPlatiDetaliat, sportivi=filteredData.sportivi, familii=filteredData.familii, plati=filteredData.plati, setPlati, setTranzactii, onViewSportiv
- FinancialDashboard: onBack, plati=filteredData.plati, tranzactii=filteredData.tranzactii, sportivi=filteredData.sportivi, familii=filteredData.familii

Ancore (inainte de editare): JurnalIncasari.tsx:56-75 interfata, :175 semnatura, :494 `setTimeout(() => onBack(), 1500)` (NU SE ATINGE), :663 butonul "Înapoi"; FacturiPersonale.tsx:7-12 `IstoricPlatiProps`, :14 semnatura, :197 butonul "Înapoi la Portal"; FinancialDashboard.tsx:7-13 props, :43 semnatura, :132 butonul "Înapoi la Meniu".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: hideBackButton optional pe JurnalIncasari, IstoricPlati si FinancialDashboard</name>
  <read_first>
    - components/Plati/JurnalIncasari.tsx (liniile 56-75, 175-190, 485-500, 655-668)
    - components/Plati/FacturiPersonale.tsx (liniile 1-20, 190-200)
    - components/Plati/FinancialDashboard.tsx (liniile 1-50, 125-135)
  </read_first>
  <files>components/Plati/JurnalIncasari.tsx, components/Plati/FacturiPersonale.tsx, components/Plati/FinancialDashboard.tsx</files>
  <action>
JurnalIncasari.tsx: adauga `hideBackButton?: boolean;` in `JurnalIncasariProps` (comentariu: "Faza 31 — hub-ul ascunde butonul; onBack ramane apelat automat dupa incasare") si in destructurarea de la linia 175. Linia 663: `{!hideBackButton && (<Button onClick={onBack} variant="secondary"><ArrowLeftIcon className="w-5 h-5 mr-2" /> Înapoi</Button>)}`. NU modifica linia 494 (`setTimeout(() => onBack(), 1500)`) si nici logica de procesare.

FacturiPersonale.tsx (`IstoricPlati`): adauga `hideBackButton?: boolean;` in `IstoricPlatiProps` si in destructurarea de la linia 14; linia 197 → `{!hideBackButton && (...)}`. Montarea standalone pentru SPORTIV (fara prop) ramane identica.

FinancialDashboard.tsx: adauga `hideBackButton?: boolean;` in `FinancialDashboardProps` si destructurarea de la linia 43; linia 132 → `{!hideBackButton && (...)}`.

Nicio alta modificare.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && grep -c 'hideBackButton' components/Plati/JurnalIncasari.tsx components/Plati/FacturiPersonale.tsx components/Plati/FinancialDashboard.tsx && grep -c 'setTimeout(() => onBack(), 1500)' components/Plati/JurnalIncasari.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - Fiecare din cele 3 fisiere contine `hideBackButton?: boolean` si `!hideBackButton &&` (`grep -c hideBackButton` >= 3 per fisier).
    - `grep -c "setTimeout(() => onBack(), 1500)" components/Plati/JurnalIncasari.tsx` = 1 (apelul automat pastrat).
    - `git diff -- components/Plati/JurnalIncasari.tsx` atinge doar interfata, destructurarea si linia butonului.
  </acceptance_criteria>
  <done>Cele 3 componente pot fi montate in hub fara buton dublu, cu comportament standalone neschimbat.</done>
</task>

<task type="auto">
  <name>Task 2: Componentele TabIncasari si TabRapoarte</name>
  <read_first>
    - components/Plati/hub/platiHubConfig.ts (antetul "Harta props verificata" si fluxurile F1-F4)
    - components/AppRouter.tsx (liniile 72-89, 228-229, 238-241, 284-285)
    - components/LazyComponents.tsx (JurnalIncasari, IstoricPlati, RaportFinanciar, FinancialDashboard)
    - components/Plati/JurnalIncasari.tsx (liniile 56-75 — tipurile exacte ale props)
    - contexts/DataContext.tsx
  </read_first>
  <files>components/Plati/hub/TabIncasari.tsx, components/Plati/hub/TabRapoarte.tsx</files>
  <action>
Creeaza `components/Plati/hub/TabIncasari.tsx` (per D-02):
- Importuri: `React`; `import type { Plata } from '../../../types';`; `useData` din `'../../../contexts/DataContext'`; `import * as Lazy from '../../LazyComponents';`; `import type { PlatiHubTabProps } from './platiHubConfig';`.
- `export interface TabIncasariProps extends PlatiHubTabProps { platiPentruIncasare: Plata[]; onIncasareProcesata: () => void; onJurnalBack: () => void; }`.
- `const { filteredData, setPlati, setTranzactii, preturiConfig, tipuriPlati, setTipuriPlati, reduceri } = useData();` (preturiConfig, tipuriPlati, reduceri NEFILTRATE — exact ca AppRouter.tsx:239).
- `sectiune === 'jurnal-incasari'` → `<Lazy.JurnalIncasari currentUser={currentUser} permissions={permissions} plati={filteredData.plati} setPlati={setPlati} sportivi={filteredData.sportivi} familii={filteredData.familii} preturiConfig={preturiConfig} tipuriAbonament={filteredData.tipuriAbonament} tipuriPlati={tipuriPlati} setTipuriPlati={setTipuriPlati} tranzactii={filteredData.tranzactii} setTranzactii={setTranzactii} platiInitiale={platiPentruIncasare} onIncasareProcesata={onIncasareProcesata} onBack={onJurnalBack} reduceri={reduceri} onViewSportiv={onViewSportiv} hideBackButton />`.
- `sectiune === 'istoric-plati'` → `<Lazy.IstoricPlati onBack={onBack} viewedUser={currentUser} plati={filteredData.plati} tranzactii={filteredData.tranzactii} hideBackButton />`.
- Comentariu-antet: tab Încasări (D-02); ATENTIE — JurnalIncasari apeleaza `onBack` automat la 1500 ms dupa incasare (JurnalIncasari.tsx:494), de aceea primeste `onJurnalBack`, nu `onBack`.

Creeaza `components/Plati/hub/TabRapoarte.tsx` (per D-03):
- Importuri: `React`; `useData`; `import * as Lazy from '../../LazyComponents';`; `import type { PlatiHubTabProps } from './platiHubConfig';`.
- `export const TabRapoarte: React.FC<PlatiHubTabProps> = ({ sectiune, onViewSportiv, onBack }) => { ... }` cu `const { filteredData, setPlati, setTranzactii } = useData();`.
- `sectiune === 'raport-financiar'` → `<Lazy.RaportFinanciar onBack={onBack} istoricPlatiDetaliat={filteredData.istoricPlatiDetaliat} sportivi={filteredData.sportivi} familii={filteredData.familii} plati={filteredData.plati} setPlati={setPlati} setTranzactii={setTranzactii} onViewSportiv={onViewSportiv} />` (fara hideBackButton — RaportFinanciar nu are buton de intoarcere).
- `sectiune === 'financial-dashboard'` → `<Lazy.FinancialDashboard onBack={onBack} plati={filteredData.plati} tranzactii={filteredData.tranzactii} sportivi={filteredData.sportivi} familii={filteredData.familii} hideBackButton />`.

Niciun Suspense propriu (boundary in PlatiHub, 31-08); nicio interogare Supabase.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && grep -c 'onBack={onJurnalBack}' components/Plati/hub/TabIncasari.tsx && grep -c 'platiInitiale={platiPentruIncasare}' components/Plati/hub/TabIncasari.tsx && grep -c 'istoricPlatiDetaliat={filteredData.istoricPlatiDetaliat}' components/Plati/hub/TabRapoarte.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - `TabIncasari.tsx` exporta `TabIncasari` si `TabIncasariProps` (extinde `PlatiHubTabProps` cu `platiPentruIncasare`, `onIncasareProcesata`, `onJurnalBack`).
    - `grep -c "onBack={onJurnalBack}" components/Plati/hub/TabIncasari.tsx` = 1 si `grep -c "platiInitiale={platiPentruIncasare}"` = 1.
    - JurnalIncasari primeste `reduceri={reduceri}` si `preturiConfig={preturiConfig}` din `useData()` direct (nu din filteredData).
    - `TabRapoarte.tsx` exporta `TabRapoarte`; contine ramurile `sectiune === 'raport-financiar'` si `sectiune === 'financial-dashboard'`.
    - `grep -c "supabase" components/Plati/hub/TabIncasari.tsx components/Plati/hub/TabRapoarte.tsx` = 0 in ambele.
  </acceptance_criteria>
  <done>Tab-urile Încasări si Rapoarte exista, compileaza si monteaza componentele cu props identice celor de azi, cu fluxul de incasare multipla pregatit pentru shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selectie facturi (Facturi) → Jurnal Încasări (scriere tranzactii) | Lista `platiInitiale` traverseaza componente si trebuie golita la iesire, altfel o incasare ulterioara ar prelua facturi vechi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05-01 | Tampering | `platiPentruIncasare` ramas setat dupa parasirea Jurnalului → incasare pe facturi gresite | mitigate | TabIncasari transmite `onIncasareProcesata`/`onJurnalBack` (ambele golesc selectia in PlatiHub, 31-08); golirea la schimbarea sectiunii e implementata in 31-08 |
| T-31-05-02 | Information Disclosure | IstoricPlati afiseaza platile utilizatorului logat | accept | Comportament identic cu vederea veche `istoric-plati` (viewedUser=currentUser); date din filteredData |
| T-31-05-SC | Tampering | npm installs | accept | Zero pachete noi |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Inventarul complet al fazei e in `31-01-PLAN.md` (sectiunea omonima). Acest plan (31-05) produce:
- `components/Plati/hub/TabIncasari.tsx` → `TabIncasari`, `TabIncasariProps`.
- `components/Plati/hub/TabRapoarte.tsx` → `TabRapoarte`.
- Props optionale noi: `hideBackButton?: boolean` pe `JurnalIncasari`, `IstoricPlati` (FacturiPersonale.tsx), `FinancialDashboard`.
</artifacts_this_phase_produces>

<verification>
1. `npm run lint` — cod 0.
2. Gardele grep din acceptance (onJurnalBack, platiInitiale, istoricPlatiDetaliat, setTimeout pastrat).
</verification>

<success_criteria>
- D-02 si D-03: tab-urile Încasări si Rapoarte grupeaza ecranele cu props identice.
- Fluxul de incasare multipla (F1-F4) are toate conexiunile necesare pentru shell.
</success_criteria>

<output>
Creeaza `.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-05-SUMMARY.md`.
</output>
