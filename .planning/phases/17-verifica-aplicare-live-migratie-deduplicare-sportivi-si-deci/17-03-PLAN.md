---
phase: 17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci
plan: 03
type: execute
wave: 2
depends_on: [17-02]
files_modified:
  - scripts/audit-mfa-coverage.ts
autonomous: false
requirements: [MFA-03]

must_haves:
  truths:
    - "Exista un script server-side (service-role) care listeaza, pentru fiecare cont ADMIN_CLUB/SUPER_ADMIN_FEDERATIE, daca are un factor MFA verificat"
    - "Inainte de a considera faza incheiata, se confirma ca contul super-admin activ are (sau si-a inrolat) un factor MFA verificat, ca sa nu fie blocat afara"
    - "Un test end-to-end manual confirma ca enrollment TOTP functioneaza (scan QR -> cod -> aal2) si ca nu apare flash de continut protejat inainte de redirect"
  artifacts:
    - path: "scripts/audit-mfa-coverage.ts"
      provides: "Audit rollout: cine dintre admini are MFA verificat (service-role, server-side)"
      contains: "listFactors"
  key_links:
    - from: "scripts/audit-mfa-coverage.ts"
      to: "supabase.auth.admin.mfa.listFactors"
      via: "SUPABASE_SERVICE_ROLE_KEY server-side"
      pattern: "admin\\.mfa\\.listFactors"
---

<objective>
Preveni lockout-ul singurului cont SUPER_ADMIN_FEDERATIE cand enforcement-ul MFA (Plan 02) devine live. Per phase_scope_note si RESEARCH.md Pitfall 4: inainte de a "flip"-ui enforcement in productie trebuie sa stim cine dintre conturile privilegiate are deja un factor MFA verificat.

Doua livrabile:
1. Un script one-off server-side (service-role, in `scripts/` — NU un nou endpoint `api/`, deoarece proiectul e la 10/12 functii Vercel Hobby dupa consolidarea 260705-1js) care raporteaza per cont admin daca are MFA verificat (MFA-03).
2. Un checkpoint de verificare umana: ruleaza scriptul, confirma acoperirea contului admin activ, si testeaza end-to-end enrollment TOTP + absenta flash-ului de continut protejat.

Purpose: Enforcement-ul e blocare imediata (fara toggle de dezactivare per D-02) — deci un admin fara MFA la momentul deploy-ului se auto-blocheaza si nu mai poate accesa DB/dashboard pentru a repara. Acest plan e plasa de siguranta.
Output: Script de audit reutilizabil + confirmare umana ca rollout-ul e sigur.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci/17-CONTEXT.md
@.planning/phases/17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci/17-RESEARCH.md
@scripts/create-missing-users.js
@api/account.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creeaza scriptul de audit al acoperirii MFA (service-role, server-side)</name>
  <files>scripts/audit-mfa-coverage.ts</files>
  <read_first>
    - .planning/phases/17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci/17-RESEARCH.md (Code Examples: "Rollout safety check"; Anti-pattern: nu rula listFactors client-side)
    - api/account.ts (pattern existent de folosire SUPABASE_SERVICE_ROLE_KEY server-side)
    - scripts/create-missing-users.js (pattern existent de script one-off cu createClient service-role)
  </read_first>
  <action>
    Creeaza `scripts/audit-mfa-coverage.ts` (rulabil cu `npx tsx scripts/audit-mfa-coverage.ts`, urmand pattern-ul din scripturile existente):
    - `createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY)` — service-role, NICIODATA in cod client (per conventia proiectului: cheia service-role e backend-only).
    - Determina lista de user_id-uri privilegiate: interogheaza tabelele de roluri (ex. join pe `roluri_utilizatori`/echivalentul care leaga user -> `roluri.nume`) pentru toti userii care detin `ADMIN_CLUB` sau `SUPER_ADMIN_FEDERATIE`. Foloseste structura reala descoperita citind cum face `usePermissions`/`useRoleManager` join-ul (roluri.nume). Deduplica user_id-urile.
    - Pentru fiecare user_id: `await supabaseAdmin.auth.admin.mfa.listFactors({ userId })`; determina `hasVerified = data.factors.some(f => f.status === 'verified')`. Ignora factori non-TOTP la raportare doar informativ (dar orice factor verified conteaza).
    - Printeaza un tabel lizibil: user_id, email (din `auth.admin` sau join users), rol(uri), statut `OK - has MFA` / `RISK - no MFA factor`. La final printeaza un rezumat: N admini total, M cu MFA verificat, si evidentiaza explicit conturile SUPER_ADMIN_FEDERATIE fara MFA (risc de lockout maxim).
    - Exit code non-zero daca vreun SUPER_ADMIN_FEDERATIE nu are MFA verificat (semnal clar de "nu deploya enforcement inca"). Nu scrie nimic in DB — read-only.
    - Comentarii de domeniu in romana per CLAUDE.md; fara librarii noi (doar @supabase/supabase-js deja instalat).
  </action>
  <verify>
    <automated>npx tsx scripts/audit-mfa-coverage.ts; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `scripts/audit-mfa-coverage.ts` foloseste `SUPABASE_SERVICE_ROLE_KEY` si `auth.admin.mfa.listFactors`
    - Source: scriptul NU importa/foloseste cheia anon pentru operatiile de audit si nu contine scrieri DB
    - Behavior: rularea listeaza fiecare cont admin cu statut OK/RISK si un rezumat; exit non-zero daca un SUPER_ADMIN_FEDERATIE nu are MFA
    - CLI: output-ul contine cel putin o linie per cont privilegiat + un rezumat agregat
  </acceptance_criteria>
  <done>Scriptul ruleaza server-side, raporteaza acoperirea MFA per admin, si semnaleaza prin exit code conturile super-admin fara MFA.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint rollout — confirma acoperire MFA admin + test end-to-end enforcement</name>
  <files>scripts/audit-mfa-coverage.ts</files>
  <what-built>
    Enforcement MFA (Plan 02: useMFAGuard scoped la 2 roluri + render-gate App.tsx + enrollment TOTP in SetupMFAPage) + scriptul de audit rollout (Task 1 al acestui plan).
  </what-built>
  <action>
    Verificare umana (checkpoint blocant) inainte de a considera faza incheiata / enforcement sigur pentru deploy — parcurge pasii din how-to-verify si confirma acceptance_criteria.
  </action>
  <how-to-verify>
    1. Ruleaza `npx tsx scripts/audit-mfa-coverage.ts` si citeste raportul. Confirma daca contul tau SUPER_ADMIN_FEDERATIE (cel folosit acum) apare ca `OK - has MFA` sau `RISK - no MFA factor`.
    2. Daca apare RISK: NU deploya enforcement inca. Loghează-te cu contul admin, navigheaza voluntar la view-ul `setup-mfa` (fluxul TOTP nou), scaneaza codul QR cu Google Authenticator/Authy, introdu codul de 6 cifre. Confirma ca `verify()` reuseste si ajungi pe dashboard. Re-ruleaza scriptul si confirma ca acum apare `OK`.
    3. Test end-to-end enforcement (ideal pe un al doilea cont admin de test FARA MFA, sau prin unenroll temporar): loghează-te ca ADMIN_CLUB/SUPER_ADMIN_FEDERATIE fara MFA verificat. Confirma: (a) esti redirectionat/blocat la setup-mfa; (b) NU vezi niciun continut protejat (financiar/sportivi) inainte de blocare — verifica cu throttling de retea lent in DevTools ca nu apare flash de AppLayout (ecranul de loading/skeleton apare in loc).
    4. Test non-regresie: loghează-te cu un context activ INSTRUCTOR sau SPORTIV. Confirma ca accesul e normal, NU esti fortat la setup-mfa.
    5. Confirma ca nu exista niciun buton/setare de dezactivare a enforcement-ului nicaieri in UI (respins per D-02).
  </how-to-verify>
  <verify>
    <automated>MISSING — checkpoint de verificare umana (scan QR cu authenticator app, test flash cu throttling retea); nu poate fi automatizat fara device MFA real</automated>
  </verify>
  <acceptance_criteria>
    - Scriptul de audit a rulat si contul super-admin activ e confirmat `OK - has MFA` (direct sau dupa enrollment)
    - Enrollment TOTP end-to-end reuseste (QR -> cod -> aal2 -> dashboard)
    - Un rol privilegiat fara MFA este blocat la setup-mfa fara flash de continut protejat
    - INSTRUCTOR/SPORTIV nu sunt afectati
  </acceptance_criteria>
  <done>Contul super-admin activ are MFA verificat; enforcement testat end-to-end (blocare reala fara flash); non-regresie confirmata pentru roluri ne-privilegiate.</done>
  <resume-signal>Scrie "approved" daca toate verificarile trec, sau descrie ce a esuat (ex. flash de continut, enroll error, lockout).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script rollout → Supabase Admin API | Necesita SUPABASE_SERVICE_ROLE_KEY; ruleaza doar server-side/local, niciodata in browser |
| deploy enforcement → conturi admin existente | Riscul de a bloca afara singurul super-admin fara MFA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Denial of Service | lockout self al singurului SUPER_ADMIN_FEDERATIE la go-live enforcement | mitigate | Audit pre-flight (Task 1) + checkpoint blocant care cere confirmarea MFA a contului admin activ inainte de a considera faza incheiata |
| T-17-08 | Information Disclosure | cheia service-role expusa daca scriptul ruleaza client-side | mitigate | Scriptul e in scripts/ (server-side), foloseste process.env.SUPABASE_SERVICE_ROLE_KEY, conform conventiei backend-only verificata prin grep in api/ |
</threat_model>

<verification>
- `npx tsx scripts/audit-mfa-coverage.ts` ruleaza si raporteaza acoperirea; exit non-zero daca un super-admin nu are MFA
- Checkpoint uman: contul admin activ confirmat cu MFA verificat + test end-to-end TOTP + absenta flash + non-regresie INSTRUCTOR/SPORTIV
</verification>

<success_criteria>
- Exista un audit reutilizabil al acoperirii MFA pe conturile privilegiate (service-role, read-only)
- Contul super-admin activ are un factor MFA verificat inainte ca enforcement-ul sa fie considerat sigur pentru deploy
- Enforcement-ul e verificat end-to-end (blocare reala fara flash) si non-regresiv pentru rolurile ne-privilegiate
</success_criteria>

<output>
Create `.planning/phases/17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci/17-03-SUMMARY.md` when done
</output>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- New file `scripts/audit-mfa-coverage.ts` — one-off server-side audit; runnable via `npx tsx scripts/audit-mfa-coverage.ts`; uses `createClient(..., SUPABASE_SERVICE_ROLE_KEY)` + `supabase.auth.admin.mfa.listFactors({ userId })`; prints per-admin OK/RISK table + aggregate summary; non-zero exit code when any SUPER_ADMIN_FEDERATIE lacks a verified MFA factor.
- No new packages (uses installed @supabase/supabase-js); no new Vercel api/ function (kept off the 12-function Hobby limit).
</artifacts_produced>
