---
phase: 31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Plati/GestiuneFacturi.tsx
  - components/Plati/RaportFinanciar.tsx
  - components/UserProfile.tsx
autonomous: true
requirements: [D-07]
user_setup: []

must_haves:
  truths:
    - "Niciun apel .update() pe tabela plati din GestiuneFacturi, RaportFinanciar, JurnalIncasari, SMSIncasari si UserProfile nu mai trimite un obiect obtinut prin spread/rest dintr-un rand rbv_plati_club — doar payload literal cu chei explicite (D-07)"
    - "Editarea unei facturi din profilul sportivului (PlataEditModal) nu mai poate produce PGRST204 'column club_nume not found'"
    - "Dupa o editare sau o incasare rapida in Gestiune Facturi / Raport Financiar, randul din lista isi pastreaza numele platitorului si statusul real (nu devine 'Sportiv Șters' sau status undefined)"
    - "Fiecare site de scriere pe plati din cele 5 fisiere are un verdict documentat in SUMMARY (OK / REPARAT / observatie in afara scope-ului)"
  artifacts:
    - path: "components/UserProfile.tsx"
      provides: "handleSavePlataEdit cu payload whitelist {descriere, suma, data, status}"
      contains: "descriere: editedPlata.descriere"
    - path: "components/Plati/GestiuneFacturi.tsx"
      provides: "Merge de stare {...p, ...data} + refetch plata dupa RPC proceseaza_plata_factura"
      contains: "...p, ...data"
    - path: "components/Plati/RaportFinanciar.tsx"
      provides: "Merge de stare dupa handleSaveEditFactura"
      contains: "...p, ..."
  key_links:
    - from: "components/UserProfile.tsx handleSavePlataEdit"
      to: "public.plati"
      via: "supabase.from('plati').update(payload) cu chei explicite"
      pattern: "from\\('plati'\\)\\.update\\(payload\\)"
    - from: "components/Plati/GestiuneFacturi.tsx handleProcessPayment"
      to: "public.plati"
      via: "refetch select('*').eq('id') dupa RPC"
      pattern: "from\\('plati'\\)[\\s\\S]{0,40}select\\('\\*'\\)"
---

<objective>
Audit si reparatie a anti-pattern-ului "spread din stare derivata din view-ul `rbv_plati_club` in `.update()` pe tabela `plati`" (D-07) in toate componentele care vor fi montate in hub sau care editeaza facturi, plus aducerea starii locale in acord cu raspunsul DB dupa scriere.

Bug-ul de referinta (NU se re-implementeaza): `components/Plati/PlatiScadente.tsx` `handleSaveEdit` (linia ~485) a fost deja fixat in commit 6edee5a cu payload whitelist. Pattern-ul corect de urmat: `components/Plati/GestiuneFacturi.tsx:353-357` (update cu chei explicite) si `components/UserProfile.tsx:314` (destructurare care elimina campurile JOIN).

Inventar facut la planificare (grep pe toate cele 30+ site-uri `.from('plati')` din repo) — executorul il RE-VERIFICA, nu il accepta orbeste:

| Fisier:linie | Apel | Verdict planificare |
|---|---|---|
| GestiuneFacturi.tsx:353-357 | update({ status, suma_initiala, suma }) | OK payload (pattern de referinta) |
| GestiuneFacturi.tsx:364 | setPlati(prev => prev.map(p => p.id === data.id ? data : p)) | DE REPARAT: inlocuieste randul view (cu club_nume/sportiv_nume/sportiv_prenume) cu rand brut din tabela → pierde numele platitorului cross-club |
| GestiuneFacturi.tsx:441 | setPlati(... { ...p, status: result.status_nou }) | DE REPARAT: RPC `proceseaza_plata_factura` intoarce doar { success, tranzactie_id } (sql/refactor/REFACTOR_FINANCIAL.sql:118-121; confirmat de comentariul "Bug 1 Fix" din PlatiScadente.tsx:531-541) → status devine undefined in stare |
| GestiuneFacturi.tsx:243 | insert(newPlata) | De verificat ca newPlata e literal explicit (asteptat OK) |
| RaportFinanciar.tsx:274-277 | update({ status, suma }) | OK payload |
| RaportFinanciar.tsx:~281 | setPlati(prev => prev.map(p => p.id === data.id ? data as Plata : p)) | DE REPARAT: acelasi merge ca GestiuneFacturi:364 |
| JurnalIncasari.tsx:514 | update({ status: 'Neachitat' }).in('id', plata_ids) | OK payload literal |
| JurnalIncasari.tsx:422 | insert({ ...chei explicite }) | OK |
| SMSIncasari.tsx:335-336 | update({ status: 'Achitat' }) | OK payload literal (observatie de business in afara scope-ului — vezi Task 1) |
| SMSIncasari.tsx:347 | update({ status: 'Neachitat' }) | OK payload literal |
| UserProfile.tsx:451-452 | `const { id, ...updates } = editedPlata;` + `.update(updates)` | ANTI-PATTERN GASIT: editedPlata provine din filteredData.plati (rbv_plati_club, are club_nume) → PGRST204. Fisierul nu era in lista celor 4 din CONTEXT, dar e exact clasa de bug D-07 ("de urmat consecvent") |
| UserProfile.tsx:481-482 | update({ status }) | OK payload literal |

NU in scope (31-CONTEXT.md domain): logica de business a platilor, RLS, triggere DB. Observatiile de business gasite se documenteaza, nu se repara.

Output: 3 fisiere reparate, verdict per site in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-CONTEXT.md
@CLAUDE.md
@hooks/usePlati.ts
</context>

<interfaces>
Tipuri existente (types.ts:146-183):
- `interface Tranzactie { id; plata_ids: string[]; sportiv_id; familie_id; club_id?; suma; data_platii; metoda_plata }`
- `interface Plata { id; sportiv_id; familie_id; club_id?; suma_initiala?; reducere_id?; reducereDetalii? (coloana camelCase reala); suma; data; status: 'Achitat' | 'Neachitat' | 'Achitat Parțial' | 'Anulat'; descriere; tip; observatii; sesiune_id?; eveniment_id?; luna?; an?; sportiv_nume?; sportiv_prenume? }` — la runtime randurile din `rbv_plati_club` au in plus `club_nume` (inexistent ca si coloana in tabela `plati`).

Model de refetch dupa RPC (de copiat, PlatiScadente.tsx:531-541): dupa `result.success`, `supabase.from('plati').select('*').eq('id', <id>).single()` si inlocuirea randului in stare.

Campurile editate de `components/UserProfile/PlataEditModal.tsx` (liniile 78-108): `descriere`, `suma`, `data`, `status` — exact acestea patru, nimic altceva.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit + reparatie in fisierele hub-ului (GestiuneFacturi, RaportFinanciar) si verificare JurnalIncasari/SMSIncasari</name>
  <read_first>
    - components/Plati/PlatiScadente.tsx (liniile 485-555 — handleSaveEdit fixat si handleProcessPayment cu "Bug 1 Fix")
    - components/Plati/GestiuneFacturi.tsx (liniile 220-265, 333-368, 415-463)
    - components/Plati/RaportFinanciar.tsx (liniile 262-290)
    - components/Plati/JurnalIncasari.tsx (liniile 118-140, 318-372, 418-440, 505-530)
    - components/SMS/SMSIncasari.tsx (liniile 145-165, 325-370)
    - sql/refactor/REFACTOR_FINANCIAL.sql (liniile 76-123 — ce intoarce RPC-ul proceseaza_plata_factura)
    - hooks/usePlati.ts (sursa rbv_plati_club)
  </read_first>
  <files>components/Plati/GestiuneFacturi.tsx, components/Plati/RaportFinanciar.tsx</files>
  <action>
Pas 1 — reverifica inventarul: ruleaza `grep -n "update(\|insert(\|upsert(" components/Plati/GestiuneFacturi.tsx components/Plati/RaportFinanciar.tsx components/Plati/JurnalIncasari.tsx components/SMS/SMSIncasari.tsx` si pentru FIECARE rezultat care scrie in `plati` sau `tranzactii` deschide constructia argumentului: verdict OK daca argumentul e un obiect literal cu chei explicite sau o variabila construita integral din chei explicite in aceeasi functie; verdict ANTI-PATTERN daca argumentul provine din spread/rest (`...x`, `const { id, ...rest } = x`) al unui obiect care vine din `plati`/`filteredData.plati`/props `plati`/stare de editare initializata dintr-un astfel de rand. Daca gasesti un ANTI-PATTERN nelistat in tabelul din obiectiv, repara-l cu payload literal cu exact campurile pe care UI-ul respectiv le editeaza (per D-07) si adauga fisierul in SUMMARY.

Pas 2 — GestiuneFacturi.tsx `handleSaveEdit` (linia ~364): inlocuieste `p.id === data.id ? data : p` cu `p.id === data.id ? { ...p, ...data } : p` (pastreaza campurile JOIN ale view-ului — club_nume, sportiv_nume, sportiv_prenume — pe care tabela nu le intoarce). Payload-ul de la 353-357 ramane neschimbat.

Pas 3 — GestiuneFacturi.tsx `handleProcessPayment` (liniile ~437-450): elimina linia `setPlati(prev => prev.map(p => p.id === plataForPayment.id ? { ...p, status: result.status_nou } : p));`. In locul ei, retine `const plataId = plataForPayment.id;`, apoi `const { data: plataActualizata } = await supabase.from('plati').select('*').eq('id', plataId).maybeSingle();` si, daca exista, `setPlati(prev => prev.map(p => p.id === plataId ? { ...p, ...plataActualizata } : p));` — identic ca intentie cu PlatiScadente.tsx:531-541, dar cu merge (nu inlocuire). Pastreaza neschimbat blocul existent care aduce tranzactia noua prin `result.tranzactie_id` si `setTranzactii`.

Pas 4 — RaportFinanciar.tsx `handleSaveEditFactura` (linia ~281): inlocuieste `p.id === data.id ? data as Plata : p` cu `p.id === data.id ? { ...p, ...(data as Plata) } : p`. Payload-ul `{ status: editFacturaStatus, suma: sumaRamasa }` ramane neschimbat.

Pas 5 — JurnalIncasari.tsx si SMSIncasari.tsx: asteptat ZERO modificari (payload-uri literale). Documenteaza in SUMMARY, sectiunea "Observatii in afara scope-ului (business logic — nu s-au reparat)": (a) SMSIncasari.tsx:335-336 marcheaza factura 'Achitat' fara sa creeze tranzactie si fara sa aduca `suma` (restul) la 0 — ecranul de detaliu din 31-03 va semnala aceasta inconsistenta; (b) JurnalIncasari.tsx:514 seteaza 'Neachitat' pe toate facturile tranzactiei sterse inainte de DELETE, chiar daca exista alte tranzactii pe aceleasi facturi (trigger-ul de recalculare le corecteaza daca e prezent live).

NU schimba semantica niciunui payload, NU schimba numele coloanelor, NU atinge alte fisiere.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && grep -c 'status_nou' components/Plati/GestiuneFacturi.tsx; grep -n '\.\.\.p, \.\.\.data' components/Plati/GestiuneFacturi.tsx; grep -n '\.\.\.p, \.\.\.(data as Plata)' components/Plati/RaportFinanciar.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - `grep -c "status_nou" components/Plati/GestiuneFacturi.tsx` = 0.
    - `grep -c "...p, ...data" components/Plati/GestiuneFacturi.tsx` >= 1 si GestiuneFacturi contine `...p, ...plataActualizata`.
    - `grep -c "...p, ...(data as Plata)" components/Plati/RaportFinanciar.tsx` = 1.
    - Payload-urile de la GestiuneFacturi.tsx `update({ status: editStatus, suma_initiala: sumaTotal, suma: sumaRamasa })` si RaportFinanciar.tsx `update({ status: editFacturaStatus, suma: sumaRamasa })` sunt neschimbate (diff git fara linii modificate in aceste obiecte).
    - `git diff --stat -- components/Plati/JurnalIncasari.tsx components/SMS/SMSIncasari.tsx` nu arata modificari (daca executorul a gasit un anti-pattern nou, SUMMARY il justifica explicit).
    - SUMMARY contine un tabel cu verdict per site de scriere pe `plati`/`tranzactii` pentru cele 4 fisiere si sectiunea "Observatii in afara scope-ului".
  </acceptance_criteria>
  <done>Toate scrierile pe plati din fisierele hub-ului folosesc payload explicit; starea locala dupa editare/incasare rapida pastreaza campurile view-ului si statusul real.</done>
</task>

<task type="auto">
  <name>Task 2: Reparatia anti-pattern-ului in UserProfile.handleSavePlataEdit + garda repo-wide</name>
  <read_first>
    - components/UserProfile.tsx (liniile 305-320 — pattern-ul corect de destructurare; liniile 440-500 — handleSavePlataEdit si handleMutaPlata)
    - components/UserProfile/PlataEditModal.tsx (integral — ce campuri editeaza efectiv)
    - components/Plati/PlatiScadente.tsx (liniile 485-506 — fix-ul de referinta)
  </read_first>
  <files>components/UserProfile.tsx</files>
  <action>
In `components/UserProfile.tsx` `handleSavePlataEdit` (liniile ~448-467), per D-07:

- Sterge `const { id, ...updates } = editedPlata;`.
- Construieste `const payload = { descriere: editedPlata.descriere, suma: editedPlata.suma, data: editedPlata.data, status: editedPlata.status };` — exact cele 4 campuri editate de `PlataEditModal` (Descriere, Sumă, Data emitere, Status). Adauga deasupra un comentariu de 2-3 randuri in romana: editedPlata vine din filteredData.plati (view rbv_plati_club, are club_nume); spread-ul trimitea coloane inexistente → PGRST204; whitelist explicit ca in PlatiScadente.handleSaveEdit si GestiuneFacturi.handleSaveEdit.
- Apelul devine `supabase.from('plati').update(payload).eq('id', editedPlata.id).select().maybeSingle()`.
- In ramura de succes inlocuieste `setPlati(prev => prev.map(p => p.id === id ? data : p))` cu `setPlati(prev => prev.map(p => p.id === editedPlata.id ? { ...p, ...data } : p))` si foloseste `editedPlata.id` in loc de `id` in patch-ul existent `setVizualizarePlati(...)` (restul patch-ului neschimbat).
- Restul functiei (mesajele de eroare, "Verificați permisiunile", showSuccess) ramane identic.

NU modifica `PlataEditModal.tsx` (etichetele sale raman cum sunt — ecranul nou de detaliu din 31-03 ofera campurile separate cerute de D-06). NU modifica `handleMutaPlata` (payload-uri literale, OK).

Garda repo-wide dupa modificare: ruleaza `grep -rnE "from\('plati'\)\.update\(updates\)" components services hooks utils` si `grep -rn "\.\.\.updates } = editedPlata" components` — ambele trebuie sa intoarca zero rezultate. Noteaza iesirea in SUMMARY.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && ! grep -rnE \"from\\('plati'\\)\\.update\\(updates\\)\" components services hooks utils && ! grep -rn '\.\.\.updates } = editedPlata' components && grep -n 'descriere: editedPlata.descriere' components/UserProfile.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - `grep -rnE "from\('plati'\)\.update\(updates\)" components services hooks utils` → 0 rezultate.
    - `grep -rn "...updates } = editedPlata" components` → 0 rezultate.
    - `components/UserProfile.tsx` contine `descriere: editedPlata.descriere`, `suma: editedPlata.suma`, `data: editedPlata.data`, `status: editedPlata.status` si `update(payload)`.
    - Obiectul `payload` din handleSavePlataEdit are exact 4 chei (verificabil prin citire: nicio alta cheie intre acolade).
    - `git diff --stat -- components/UserProfile/PlataEditModal.tsx` → nicio modificare.
  </acceptance_criteria>
  <done>Editarea facturii din profilul sportivului trimite doar coloane reale ale tabelei plati; nu mai exista in repo niciun .update() pe plati cu spread dintr-un rand de view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stare client (randuri rbv_plati_club + campuri calculate) → `public.plati` UPDATE | Orice cheie trimisa ajunge la PostgREST; chei necunoscute produc PGRST204, chei valide nedorite ar putea suprascrie coloane (ex. club_id, sportiv_id) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | Tampering | UserProfile.handleSavePlataEdit trimitea tot obiectul Plata (inclusiv club_id, sportiv_id, familie_id, tip) | mitigate | Payload whitelist cu 4 chei (descriere, suma, data, status) — nu se mai pot rescrie accidental coloanele de proprietate/club din UI |
| T-31-02-02 | Tampering | Stare locala inconsistenta dupa RPC (status undefined in GestiuneFacturi) → decizii gresite de incasare | mitigate | Refetch plata din DB dupa RPC si merge in stare |
| T-31-02-03 | Repudiation | SMSIncasari marcheaza Achitat fara tranzactie | accept | Logica de business in afara scope-ului fazei (31-CONTEXT domain); documentat in SUMMARY si semnalat vizual de ecranul de detaliu 31-03 |
| T-31-02-SC | Tampering | npm installs | accept | Zero pachete noi |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Inventarul complet al fazei e in `31-01-PLAN.md` (sectiunea omonima). Acest plan (31-02) nu creeaza simboluri noi; modifica doar implementari existente:
- `components/UserProfile.tsx` → `handleSavePlataEdit` cu `payload` whitelist (4 chei).
- `components/Plati/GestiuneFacturi.tsx` → merge `{ ...p, ...data }` in `handleSaveEdit`; refetch + merge in `handleProcessPayment` (fara `status_nou`).
- `components/Plati/RaportFinanciar.tsx` → merge `{ ...p, ...(data as Plata) }` in `handleSaveEditFactura`.
</artifacts_this_phase_produces>

<verification>
1. `npm run lint` — cod 0.
2. Gardele grep repo-wide intorc 0 rezultate.
3. SUMMARY contine tabelul de verdict per site de scriere si observatiile de business.
</verification>

<success_criteria>
- Clasa de bug D-07 (spread din view in update pe plati) e eliminata din toate componentele care editeaza facturi.
- Nicio semantica de payload nu s-a schimbat; doar forma (whitelist) si sincronizarea starii locale.
</success_criteria>

<output>
Creeaza `.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-02-SUMMARY.md` cu tabelul de verdict per site si sectiunea "Observatii in afara scope-ului (business logic — nu s-au reparat)".
</output>
