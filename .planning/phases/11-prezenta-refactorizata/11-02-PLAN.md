---
phase: 11-prezenta-refactorizata
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - components/Prezenta/index.tsx
  - components/Prezenta/ListaPrezentaAntrenament.tsx
autonomous: true
requirements: [PRZ-02, PRZ-03, PRZ-04]
must_haves:
  truths:
    - "Tab 'Grupe' are un shortcut 'Calendar Toate Grupele' care deschide direct calendarul multi-grupă"
    - "Tab 'Grupe' are un shortcut 'Generator Program' care deschide GeneratorProgramMasiv (PRZ-04)"
    - "Click pe o zi în calendarul multi-grupă deschide direct form de marcare prezență, fără pași Orar->Calendar (PRZ-02)"
    - "Dacă 2+ grupe au antrenament la același interval, sportivii din toate grupele sunt vizibili împreună și se salvează într-o singură acțiune (PRZ-03)"
    - "DECIZIE PRZ-03: un sportiv prezent în 2 grupe simultane are prezența salvată DOAR în antrenamentul grupei lui PRINCIPALE (grupa_id de pe sportiv), nu în ambele; UI marchează clar grupa de apartenență a fiecărui sportiv"
  artifacts:
    - path: "components/Prezenta/ListaPrezentaAntrenament.tsx"
      provides: "FormularPrezentaMultiGrupa export pentru grupe simultane"
      contains: "FormularPrezentaMultiGrupa"
    - path: "components/Prezenta/index.tsx"
      provides: "case 'calendar-all', case 'prezenta-multi', shortcuts Generator + Calendar Toate Grupele"
      contains: "calendar-all"
  key_links:
    - from: "components/Prezenta/index.tsx"
      to: "components/Prezenta/CalendarActivitatiMultiGrupa.tsx"
      via: "render case 'calendar-all'"
      pattern: "CalendarActivitatiMultiGrupa"
    - from: "components/Prezenta/index.tsx"
      to: "components/Prezenta/ListaPrezentaAntrenament.tsx"
      via: "FormularPrezentaMultiGrupa render case 'prezenta-multi'"
      pattern: "FormularPrezentaMultiGrupa"
    - from: "components/Prezenta/ListaPrezentaAntrenament.tsx"
      to: "saveAttendance"
      via: "apel iterativ per antrenament_id"
      pattern: "saveAttendance\\("
---

<objective>
Cablează calendarul multi-grupă în navigarea modulului Prezenta și implementează marcarea prezenței pentru grupe simultane. Acoperă PRZ-02 (click-direct la prezență), PRZ-03 (grupe simultane într-un form unificat) și PRZ-04 (Generator accesibil din tab Grupe).

Depinde de Plan 01 (CalendarActivitatiMultiGrupa + useMultiCalendarView trebuie să existe). Acest plan deține index.tsx exclusiv în fază — toate shortcut-urile noi (inclusiv cel către raportul per interval examen din Plan 04) se adaugă aici pentru a evita conflicte de fișier.

DECIZIE ARHITECTURALĂ LOCKED (PRZ-03): Dacă un sportiv e în 2 grupe cu antrenament la aceeași oră, prezența se salvează DOAR în grupa lui principală (grupa_id de pe sportiv), NU în ambele. Această decizie a utilizatorului are prioritate față de asumarea A2 din RESEARCH.md (care sugera salvarea în ambele). UI-ul trebuie să marcheze clar cărei grupe aparține fiecare sportiv.

Purpose: Reduce drastic numărul de click-uri pentru marcarea prezenței și gestionează grupele cu antrenament simultan fără dublarea prezenței.
Output: index.tsx modificat (navigare + shortcuts), ListaPrezentaAntrenament.tsx extins cu FormularPrezentaMultiGrupa.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-prezenta-refactorizata/11-RESEARCH.md
@CLAUDE.md
@components/Prezenta/index.tsx
@components/Prezenta/ListaPrezentaAntrenament.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Creează FormularPrezentaMultiGrupa în ListaPrezentaAntrenament.tsx</name>
  <files>components/Prezenta/ListaPrezentaAntrenament.tsx</files>
  <behavior>
    - Primește antrenamente: array de Antrenament cu grupe.sportivi (toate cu același interval orar)
    - Lista de sportivi afișată = unirea sportivilor din toate grupele, deduplicat pe sportiv.id
    - Fiecare sportiv arată clar cărei grupe aparține (badge cu denumirea grupei) — PRZ-03 cerință UI
    - DECIZIE LOCKED PRZ-03: la salvare, prezența unui sportiv se scrie DOAR în antrenamentul grupei lui PRINCIPALE (grupa unde sportiv.grupa_id === antrenament.grupa_id). NU în ambele antrenamente.
    - Pentru fiecare antrenament din array, apelează saveAttendance(antrenament.id, recorduriPentruSportiviiAlCarorGrupaPrincipalaEsteAcestAntrenament, allIdsAcelaiGrup) — secvențial (for...of await), nu Promise.all
  </behavior>
  <read_first>
    - components/Prezenta/ListaPrezentaAntrenament.tsx (FormularPrezenta existent — liniile 110-465, reutilizează pattern checkbox + save; SportivCuTip tip linia 17)
    - hooks/useAttendance.ts (semnătura saveAttendance: id, records, allSportivIds — delete+insert per antrenament_id)
    - .planning/phases/11-prezenta-refactorizata/11-RESEARCH.md (Research Focus Area 5; NOTĂ: A2 din research e suprascrisă de decizia LOCKED de mai jos)
  </read_first>
  <action>
    În components/Prezenta/ListaPrezentaAntrenament.tsx adaugă un nou export FormularPrezentaMultiGrupa. NU modifica FormularPrezenta existent (backward-compat — folosit de fluxul single din case 'prezenta' și de ListaPrezentaAntrenament).
    Props: `{ antrenamente: (Antrenament & { grupe: Grupa & { sportivi: Sportiv[] }})[]; onBack: () => void; onViewSportiv?: (s: Sportiv) => void; saveAttendance: (id: string, records: { sportiv_id: string; status_id: string; is_invitat?: boolean; grupa_origine_id?: string }[], allSportivIds?: string[]) => Promise<boolean> }`. Aceeași semnătură saveAttendance ca FormularPrezenta.
    Folosește useStatusePrezenta() pentru prezentId.
    Construiește lista AFIȘATĂ de sportivi (sportiviAfisati): pentru fiecare antrenament ia antrenament.grupe?.sportivi filtrat pe status === 'Activ'; adună într-un array deduplicat pe sportiv.id (un sportiv apare o singură dată în UI). Pentru fiecare sportiv afișat reține TOATE grupele în care apare ca etichetă (badge-uri denumire grupă).
    DECIZIE LOCKED PRZ-03 — maparea de salvare: construiește grupaPrincipalaPerAntrenament determinând, pentru fiecare antrenament, care sportivi au acel antrenament ca grupă PRINCIPALĂ, adică `sportiv.grupa_id === antrenament.grupe?.id` (sau antrenament.grupa_id). Astfel fiecare sportiv "aparține" exact unui antrenament pentru salvare = cel al grupei lui principale.
      Caz limită: dacă un sportiv este în mai multe dintre grupele simultane DAR niciuna nu e grupa lui principală (e secundar în toate), atribuie-l primului antrenament din array (fallback determinist) și documentează cu comentariu. Dacă grupa lui principală nu e printre antrenamentele simultane afișate dar el apare ca secundar, la fel: atribuie primului antrenament în care apare.
    Stare prezență: presentIds: Set<string> inițializat din antrenamente.flatMap(a => a.prezenta || []).filter(p => p.status?.este_prezent).map(p => p.sportiv_id) (dedup în Set). Checkbox toggle identic cu FormularPrezenta.
    UI listă: fiecare rând afișează numele sportivului + badge(uri) cu denumirea grupei/grupelor lui; marchează vizual grupa PRINCIPALĂ distinct de secundară (ex. badge indigo "principală" vs purple "secundară", reutilizează stilul SECUNDAR din FormularPrezenta linia 357). Header arată data + intervalul orar comun + denumirile grupelor implicate.
    SALVARE (handleSaveMulti): iterează `for (const ant of antrenamente)`: determină idsSportiviGrupaPrincipala = sportiviAfisati.filter(s => s este atribuit acestui ant conform mapării de mai sus).map(s => s.id). allIdsGrupa = ant.grupe.sportivi.filter(s => s.status==='Activ').map(s => s.id). records = idsSportiviGrupaPrincipala.filter(id => presentIds.has(id)).map(id => ({ sportiv_id: id, status_id: prezentId, is_invitat: false })). Apoi `await saveAttendance(ant.id, records, allIdsGrupa)`. Secvențial (evită race condition — pitfall 3). Dacă vreun apel returnează false, oprește și nu afișa succesul. La final, dacă toate au reușit, banner succes (pattern saved din FormularPrezenta) și onBack() după timeout.
    Documentează deasupra handleSaveMulti cu comentariu: "PRZ-03 decizie LOCKED: prezența se salvează DOAR în antrenamentul grupei principale a sportivului, nu duplicat în toate grupele simultane."
    Respectă design system: Card, Button din ../ui, clase Tailwind dark. NU Shadcn/MUI.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "ListaPrezentaAntrenament" | grep -v "^#"; test $? -ne 0 && echo "NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - ListaPrezentaAntrenament.tsx exportă FormularPrezentaMultiGrupa (grep)
    - FormularPrezenta original NU este modificat structural (aceeași semnătură props)
    - handleSaveMulti apelează saveAttendance într-un for...of cu await (secvențial; grep "for (const" + "await saveAttendance")
    - Maparea de salvare folosește sportiv.grupa_id === antrenament grupa pentru atribuirea grupei principale (grep "grupa_id" în logica de salvare) — un sportiv NU este inclus în records-urile a două antrenamente diferite
    - Fiecare rând de sportiv afișează denumirea grupei (grep denumire/badge în randare)
    - npx tsc --noEmit fără erori noi în fișier
  </acceptance_criteria>
  <done>Form unificat pentru grupe simultane care merge sportivii deduplicat în UI dar salvează prezența DOAR în antrenamentul grupei principale a fiecărui sportiv (decizie LOCKED PRZ-03), secvențial per antrenament_id.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cablează navigarea calendar-all, prezenta-multi și shortcut-urile în index.tsx</name>
  <files>components/Prezenta/index.tsx</files>
  <read_first>
    - components/Prezenta/index.tsx (toată — type View linia 162, navigateTo linia 225, handleSelectAntrenament linia 235, case 'grupe' linia 282, ShortcutBar, importuri icons linia 4)
    - components/Prezenta/CalendarActivitatiMultiGrupa.tsx (props: grupe, onSelect, onSelectMultiple, onBack — din Plan 01)
    - components/Prezenta/ListaPrezentaAntrenament.tsx (FormularPrezentaMultiGrupa — din Task 1)
  </read_first>
  <action>
    În components/Prezenta/index.tsx:
    1. Extinde tipul View (linia 162) adăugând 'calendar-all' și 'prezenta-multi': `type View = 'azi' | 'rapid' | 'grupe' | 'orar' | 'calendar' | 'calendar-all' | 'prezenta' | 'prezenta-multi' | 'prezenta-grupe' | 'istoric' | 'generator';` (evită pitfall 5 — TS compile error).
    2. Importă CalendarActivitatiMultiGrupa din './CalendarActivitatiMultiGrupa' și FormularPrezentaMultiGrupa din './ListaPrezentaAntrenament' (adaugă la importul existent din ListaPrezentaAntrenament linia 7).
    3. Adaugă state `const [antrenamenteMulti, setAntrenamenteMulti] = useState<(Antrenament & { grupe: Grupa & { sportivi: Sportiv[] }})[]>([])`.
    4. Adaugă handler handleSelectMultipleAntrenamente = async (ids: string[]): dacă ids.length === 1, deleagă către handleSelectAntrenament(ids[0]). Altfel, fetch toate: `supabase.from('program_antrenamente').select('*, grupe(*, sportivi!grupa_id(id, nume, prenume, status, grad_actual_id, grupa_id)), prezenta:prezenta_antrenament(sportiv_id, status_id)').in('id', ids)`. Pe fiecare rând îmbogățește prezenta cu status din statusById (pattern handleSelectAntrenament liniile 242-248). setAntrenamenteMulti(enriched) și navigateTo('prezenta-multi', null). Gestionează erori cu showError. (NOTĂ: include grupa_id în select-ul sportivilor — necesar pentru decizia PRZ-03 din Task 1.)
    5. În case 'grupe' (ShortcutBar liniile 287-290) ADAUGĂ trei shortcut-uri noi, păstrând cele două existente (Raport Prezențe, Raport Lunar):
       - `{ label: 'Calendar Toate Grupele', icon: <CalendarDaysIcon className="w-3.5 h-3.5" />, onClick: () => navigateTo('calendar-all', null) }`
       - `{ label: 'Generator Program', icon: <CalendarDaysIcon className="w-3.5 h-3.5" />, onClick: () => navigateTo('generator', null) }` (PRZ-04)
       - `{ label: 'Raport Interval Examen', icon: <ClockIcon className="w-3.5 h-3.5" />, onClick: () => navTo('raport-interval-examen') }` (ruta globală se înregistrează în Plan 04)
    6. Adaugă case 'calendar-all': în renderContent() switch: `return <CalendarActivitatiMultiGrupa grupe={grupe} onSelect={handleSelectAntrenament} onSelectMultiple={handleSelectMultipleAntrenamente} onBack={navigateBack} />`. (PRZ-02: onSelect reutilizează handleSelectAntrenament care fetch + navighează la 'prezenta' — click-direct, fără Orar->Calendar.)
    7. Adaugă case 'prezenta-multi': în switch: `return antrenamenteMulti.length > 0 ? <FormularPrezentaMultiGrupa antrenamente={antrenamenteMulti} onBack={navigateBack} saveAttendance={saveAttendance} onViewSportiv={onViewSportiv} /> : <p className="text-slate-400 p-8">Niciun antrenament selectat.</p>`.
    NU modifica fluxul existent case 'orar' / case 'calendar' (rămâne pentru configurare detaliată per grupă). NU modifica FormularPrezenta single din case 'prezenta'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "Prezenta/index|calendar-all|prezenta-multi" | grep -v "^#"; test $? -ne 0 && echo "NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - type View conține 'calendar-all' și 'prezenta-multi' (grep)
    - renderContent are case 'calendar-all': randând CalendarActivitatiMultiGrupa (grep)
    - renderContent are case 'prezenta-multi': randând FormularPrezentaMultiGrupa (grep)
    - case 'grupe' ShortcutBar conține navigateTo('calendar-all', null), navigateTo('generator', null) și navTo('raport-interval-examen') (grep toate trei)
    - handleSelectMultipleAntrenamente folosește .in('id', ids), include grupa_id în select-ul sportivilor, și deleagă către single când ids.length === 1 (grep)
    - npx tsc --noEmit fără erori noi
  </acceptance_criteria>
  <done>Navigarea calendarului multi-grupă, marcarea click-direct, ruta grupe-simultane și cele 3 shortcut-uri noi sunt cablate; fluxurile existente intacte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase | id-uri de antrenamente și recorduri de prezență traversează către DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | saveAttendance multi-grupă | mitigate | saveAttendance (nemodificat) face delete+insert per antrenament_id; RLS pe prezenta_antrenament interzice insert dacă antrenamentul nu aparține clubului activ |
| T-11-04 | Tampering/duplicat | apeluri saveAttendance paralele | mitigate | Apeluri secvențiale for...of await (nu Promise.all); plus decizia PRZ-03 atribuie fiecare sportiv unui singur antrenament, eliminând complet duplicarea (pitfall 3) |
| T-11-05 | Spoofing/IDOR | grupe afișate în calendar-all | mitigate | grupe provin din state-ul filtrat per club/rol în index.tsx (query .eq('club_id', clubId) liniile 208-211); RLS ca backstop |
</threat_model>

<verification>
- npx tsc --noEmit fără erori — tipul View extins corect, props compatibile
- Shortcut-urile noi apar în tab Grupe (verificare vizuală în checkpoint-ul de fază)
- Salvarea multi-grupă scrie prezența DOAR în grupa principală, fără eroare 23505 și fără duplicare
</verification>

<success_criteria>
- PRZ-02: click pe zi în calendar-all duce direct la form prezență
- PRZ-03: grupe simultane afișate împreună, salvate într-o acțiune, prezență doar în grupa principală
- PRZ-04: Generator accesibil din tab Grupe
</success_criteria>

<output>
Create `.planning/phases/11-prezenta-refactorizata/11-02-SUMMARY.md` when done
</output>
