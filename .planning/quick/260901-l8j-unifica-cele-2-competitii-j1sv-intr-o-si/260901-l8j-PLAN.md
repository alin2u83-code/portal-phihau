---
phase: quick-260901-l8j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/j1sv_unificare_inspect.ts
  - scripts/j1sv_unificare_migrate.ts
  - scripts/j1sv_unificare_finalize.ts
  - scripts/j1sv_unificare_verify.ts
  - components/Competitii/GenerareSabloaneModal.tsx
autonomous: true
requirements: [S1-garda, S2-mutare, S3-curatenie, S4-redenumire, S5-taxe, S6-cod, S7-verificare, S8-build]

must_haves:
  truths:
    - "Executia se opreste fara nicio mutatie daca exista macar o inscriere sau echipa pe oricare din cele 2 competitii"
    - "Exista o singura competitie J1SV in tabelul `competitii`, cu id `3d2187dc-ffa8-464d-9bf9-b585b58004c7`"
    - "Competitia unificata are 74 categorii (58 tehnica + 16 giao_dau) si 4 probe distincte"
    - "Numerele de categorie sunt unice in competitia unificata: 1-58 (tehnica, nemodificate) si 59-74 (giao_dau, renumerotate pastrand ordinea)"
    - "Denumirea competitiei este exact 'Campionatul Național de Qwan Ki Do — Juniori 1, Seniori, Veterani' si contine in continuare marker-ul MARKER_J1SV"
    - "Taxa la inscriere este 100 lei individual si 150 lei pe echipa/pereche, uniform, pe toate probele"
    - "GenerareSabloaneModal ofera ambele seturi de sabloane J1SV (tehnica + giao_dau) indiferent de competitie.tip"
    - "npm run lint si npm run build trec"
  artifacts:
    - path: "scripts/j1sv_unificare_inspect.ts"
      provides: "Garda de siguranta read-only + inspectie schema/numerotare inainte de orice mutatie"
    - path: "scripts/j1sv_unificare_migrate.ts"
      provides: "Mutare probe_competitie + categorii_competitie sub competitia tehnica, cu renumerotare 59-74"
    - path: "scripts/j1sv_unificare_finalize.ts"
      provides: "Stergere competitie giao_dau ramasa fara copii + redenumire + config_taxe"
    - path: "scripts/j1sv_unificare_verify.ts"
      provides: "Asertiuni finale (1 competitie, 74 categorii, 4 probe, numerotare unica), exit 1 la esec"
    - path: "components/Competitii/GenerareSabloaneModal.tsx"
      provides: "Ramura isJ1SV returneaza ambele seturi de sabloane, renumerotate secvential"
  key_links:
    - from: "scripts/j1sv_unificare_migrate.ts"
      to: "categorii_competitie.competitie_id"
      via: "update per rand cu service role key"
      pattern: "from\\('categorii_competitie'\\)[\\s\\S]*\\.update"
    - from: "components/Competitii/GenerareSabloaneModal.tsx"
      to: "utils/competitiiTemplates.ts"
      via: "generateTemplateTehnnicaJ1SV + generateTemplateGiaoDauJ1SV concatenate"
      pattern: "generateTemplateTehnnicaJ1SV\\(\\)[\\s\\S]{0,120}generateTemplateGiaoDauJ1SV\\(\\)"
    - from: "competitii.config_taxe"
      to: "utils/taxeCompetitie.ts"
      via: "calculeazaTaxaIndividuala / calculeazaTaxaEchipa consumate in Pas4Sumar.tsx"
      pattern: "config_taxe"
---

<objective>
Unifica cele 2 competitii create din greseala pentru Campionatul National J1SV intr-o singura competitie: muta probele si categoriile de sub competitia `giao_dau` sub competitia `tehnica`, sterge randul orfan, redenumeste competitia ramasa, seteaza taxele uniform si repara logica de generare sabloane ca sa ofere ambele seturi.

Purpose: Utilizatorul voia UNA SINGURA competitie. Doua randuri separate inseamna doua fluxuri de inscriere paralele pentru acelasi eveniment real (31.10-01.11.2026), doua borderouri financiare si doua liste de categorii pentru acelasi club.
Output: O singura competitie `3d2187dc-ffa8-464d-9bf9-b585b58004c7` cu 74 categorii, 4 probe, taxe 100/150, plus 4 scripturi one-off si un fix in GenerareSabloaneModal.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Scriptul care a creat cele 2 competitii (sursa de adevar pentru schema payload-urilor)
@scripts/insert_cn_j1sv.ts

# Componenta de modificat la pasul 6
@components/Competitii/GenerareSabloaneModal.tsx
</context>

<facts_verificate_la_planificare>
Verificate prin citirea codului inainte de scrierea acestui plan — executorul NU trebuie sa le re-investigheze:

1. **`SUPABASE_SERVICE_ROLE_KEY` EXISTA in `.env`** (alaturi de `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`). `.env` si `.env.*` sunt in `.gitignore` (liniile 11-12). Deci mutatiile se fac cu service role key (bypass RLS), exact ca in `scripts/insert_cn_j1sv.ts:25-31` si `scripts/audit_rls_faza25.ts:14-23`. NU e nevoie de alt mecanism de autentificare.

2. **Dependinte disponibile:** `dotenv@^17.3.1`, `tsx@^4.21.0`, `@supabase/supabase-js@^2.98.0`. Zero pachete noi de instalat.

3. **Nu exista RPC generic de SQL brut** (`exec_sql` sau echivalent) — grep pe `scripts/` da doar `get_table_columns` si `delete_knowledge_by_category`. Deci `information_schema` / `pg_constraint` NU sunt interogabile prin PostgREST. **Consecinta pentru pasul 2 din cerinta:** constrangerea UNIQUE pe `numar_categorie` nu poate fi citita din catalog. Solutie adoptata: **renumerotam neconditionat 59-74**, ceea ce e corect in ambele cazuri (cu sau fara constrangere) si evita complet coliziunea. Un fapt derivabil se raporteaza totusi: ambele competitii au azi `numar_categorie` 1..58 respectiv 1..16, deci **nu exista constrangere UNIQUE globala** pe coloana — daca ar exista, insertul initial ar fi esuat.

4. **`generateTemplateTehnnicaJ1SV()` si `generateTemplateGiaoDauJ1SV()` numeroteaza AMBELE de la 1** (`utils/competitiiTemplates.ts:339` si `:463` — `let nr = 1`). Deci categoriile giao_dau au azi numerele 1-16, care se ciocnesc cu 1-16 din setul tehnica. Renumerotarea la 59-74 e obligatorie pentru claritate in UI, nu doar pentru o eventuala constrangere.

5. **PASUL 5 (taxe) — `config_taxe` ESTE citit efectiv in calculul taxei la inscriere.** Lant confirmat:
   - `utils/taxeCompetitie.ts:4` `calculeazaTaxaIndividuala` -> `competitie.config_taxe`, fallback `competitie.taxa_individual ?? 80`
   - `utils/taxeCompetitie.ts:12` `calculeazaTaxaEchipa` -> `config_taxe.echipa_juniori` (varsta_max <= 17) / `echipa_seniori`, fallback `competitie.taxa_echipa`
   - consumate in fluxul REAL de inscriere: `components/Competitii/InscriereClubWizard/Pas4Sumar.tsx:96` (per sportiv individual) si `:105` (per echipa)
   - si in `components/Competitii/FinanciarView.tsx:72`
   - scrise de `components/Competitii/CompetitieForm.tsx:75-90` (orice editare din UI suprascrie `config_taxe` SI `taxa_individual`/`taxa_echipa`)
   **Deci config_taxe SE completeaza** (pasul 5 e mutatie, nu doar raport). Tip `ConfigTaxe` (`types.ts:646-653`): `individual_tehnica`, `individual_cvd`, `echipa_seniori`, `echipa_juniori`, `cvd_echipa`, `currency`.

6. **Categoriile giao_dau J1SV au `tip_participare: 'pereche'`** (`utils/competitiiTemplates.ts:475`), iar in `Pas4Sumar.tsx` orice participare non-individuala trece prin `echipeFormate` -> `calculeazaTaxaEchipa`. Confirma cerinta utilizatorului: giao_dau se taxeaza pe echipa (150), nu individual.

7. **`MARKER_J1SV = 'Juniori 1, Seniori, Veterani'`** (`GenerareSabloaneModal.tsx:13`) — detectia J1SV se face pe `competitie.denumire`, NU pe `tip`. Denumirea ceruta la pasul 4 contine acest marker, deci detectia ramane functionala dupa redenumire. **Critic: nu modifica denumirea in ceva care nu contine acest substring exact.**

8. **Coloanele de verificat pentru garda:** `inscrieri_competitie.competitie_id` (`types.ts:726`) si `echipe_competitie.competitie_id` (`types.ts:745`).
</facts_verificate_la_planificare>

<constante>
Folosite in toate cele 4 scripturi (declara-le ca `const` la inceputul fiecarui script, nu le tasta inline):

```
COMP_TEHNICA (destinatie, supravietuieste) = 3d2187dc-ffa8-464d-9bf9-b585b58004c7
COMP_GIAO_DAU (sursa, se sterge)           = 32281416-56e2-4cff-8662-709c47ba4e07
DENUMIRE_FINALA = Campionatul Național de Qwan Ki Do — Juniori 1, Seniori, Veterani
```

`DENUMIRE_FINALA` contine em-dash `—` (U+2014) si diacritice romanesti — scrie fisierele in UTF-8 fara BOM, exact ca `scripts/insert_cn_j1sv.ts`.
</constante>

<tasks>

<task type="auto">
  <name>Task 1: Garda de siguranta + inspectie stare initiala (READ-ONLY)</name>
  <files>scripts/j1sv_unificare_inspect.ts</files>
  <action>
Creeaza `scripts/j1sv_unificare_inspect.ts`, script strict READ-ONLY (zero insert/update/delete/upsert), dupa modelul `scripts/audit_rls_faza25.ts:10-23`: import `createClient` din `@supabase/supabase-js`, `dotenv.config()`, citeste `VITE_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`, `process.exit(1)` cu mesaj clar daca lipsesc.

Declara constantele din sectiunea `<constante>`. Scriptul raporteaza, pentru fiecare din cele doua id-uri:

1. **Garda (cerinta pasul 1):** `count` exact pe `inscrieri_competitie` si pe `echipe_competitie` filtrat pe `competitie_id` (foloseste `.select('id', { count: 'exact', head: true })`).
2. Randul din `competitii`: `id, denumire, tip, taxa_individual, taxa_echipa, config_taxe, status`.
3. Probele din `probe_competitie`: `id, tip_proba, denumire, ordine_afisare`.
4. Categoriile din `categorii_competitie`: `count` total, plus `min`/`max` pe `numar_categorie` si pe `ordine_afisare` (calculate in JS pe randurile aduse cu `.select('id, numar_categorie, ordine_afisare, proba_id').order('numar_categorie')`).
5. **Constatare despre unicitate:** verifica in JS daca exista suprapunere de `numar_categorie` intre cele doua seturi si tipareste explicit: `Suprapunere numar_categorie intre competitii: N valori -> NU exista constrangere UNIQUE globala pe categorii_competitie.numar_categorie`. (Vezi `<facts_verificate_la_planificare>` punctul 3 — catalogul Postgres nu e accesibil prin PostgREST, deci aceasta e singura inferenta posibila si e suficienta, pentru ca oricum renumerotam la Task 2.)

La final, decizia de garda tiparita pe o singura linie, neambigua:
- daca total inscrieri == 0 SI total echipe == 0 pentru AMBELE competitii -> tipareste `GARDA OK - 0 inscrieri, 0 echipe - se poate continua` si `process.exit(0)`
- altfel -> tipareste `GARDA ESUATA - exista inscrieri/echipe, NU continua` cu detaliul numeric per competitie si `process.exit(1)`

**STOP CONDITION (obligatoriu):** daca acest script iese cu cod != 0, NU executa Task 2, Task 3 sau Task 4. Raporteaza imediat utilizatorului numarul de inscrieri/echipe gasite si opreste executia planului. Task 5 si Task 6 (modificarea de cod) raman valide si pot fi executate independent, dar mentioneaza explicit in SUMMARY ca partea de DB a fost abandonata din cauza garzii.
  </action>
  <verify>
    <automated>npx tsx scripts/j1sv_unificare_inspect.ts</automated>
  </verify>
  <done>Scriptul ruleaza, tipareste starea ambelor competitii si iese cu 0 afisand `GARDA OK`. Daca iese cu 1, planul se opreste conform STOP CONDITION de mai sus.</done>
</task>

<task type="auto">
  <name>Task 2: Muta probele si categoriile sub competitia tehnica, cu renumerotare 59-74</name>
  <files>scripts/j1sv_unificare_migrate.ts</files>
  <action>
Creeaza `scripts/j1sv_unificare_migrate.ts` (mutatii, service role key, aceeasi initializare ca Task 1).

**Pas A — garda repetata inline (fail-closed):** re-executa exact aceleasi patru `count` din Task 1 pe `inscrieri_competitie` si `echipe_competitie` pentru ambele id-uri. Daca oricare > 0, tipareste `ABORT - garda a esuat la migrate` si `process.exit(1)` INAINTE de orice scriere. Nu presupune ca Task 1 a rulat.

**Pas B — muta probele:** citeste `probe_competitie` unde `competitie_id = COMP_GIAO_DAU` ordonat dupa `ordine_afisare`. Citeste si `max(ordine_afisare)` existent pe `COMP_TEHNICA` (adu randurile si calculeaza in JS). Pentru fiecare proba sursa, `update` cu `{ competitie_id: COMP_TEHNICA, ordine_afisare: maxOrdineTehnica + i + 1 }` filtrat pe `.eq('id', proba.id)`. Pastreaza maparea `probaVecheId -> ramane acelasi id` (proba isi pastreaza `id`-ul, deci `categorii_competitie.proba_id` ramane valid automat — NU rescrie `proba_id`). Tipareste fiecare mutare.

**Pas C — muta si renumeroteaza categoriile:** citeste `categorii_competitie` unde `competitie_id = COMP_GIAO_DAU`, `.select('id, numar_categorie, denumire, ordine_afisare').order('numar_categorie', { ascending: true })`. Calculeaza `maxNrTehnica` = maximul `numar_categorie` de pe `COMP_TEHNICA` (asteptat 58, dar deriva-l din date, nu-l hardcoda). Pentru fiecare categorie sursa la indexul `i` (in ordinea crescatoare a `numar_categorie` original — asta pastreaza ordinea relativa ceruta), `update` cu:
`{ competitie_id: COMP_TEHNICA, numar_categorie: maxNrTehnica + i + 1, ordine_afisare: maxNrTehnica + i + 1 }`.
Tipareste maparea `vechi -> nou` pentru fiecare rand (ex. `3 -> 61 | Giao Dau Masculin 18-20 ani`).

Renumerotarea e neconditionata — vezi `<facts_verificate_la_planificare>` punctele 3 si 4: ambele seturi de sabloane numeroteaza de la 1, deci fara renumerotare competitia unificata ar avea 16 numere duplicate, indiferent daca DB accepta sau nu duplicatele. Daca vreun `update` returneaza eroare `23505` (unique violation), opreste-te si raporteaza — inseamna ca ipoteza de numerotare a fost gresita.

**Pas D — raport:** la final tipareste `count` categorii si probe ramase pe `COMP_GIAO_DAU` (asteptat 0 si 0) si `count` pe `COMP_TEHNICA` (asteptat 74 categorii, 4 probe). Iesi cu 1 daca `COMP_GIAO_DAU` mai are copii.

Nu sterge nimic in acest task — stergerea e Task 3.
  </action>
  <verify>
    <automated>npx tsx scripts/j1sv_unificare_migrate.ts</automated>
  </verify>
  <done>Script exit 0. Output arata 16 categorii mutate si renumerotate 59-74 (ordine relativa pastrata), 1 proba (giao_dau) mutata, 0 copii ramasi pe competitia sursa, 74 categorii / 4 probe pe competitia destinatie.</done>
</task>

<task type="auto">
  <name>Task 3: Sterge competitia orfana, redenumeste si seteaza taxele</name>
  <files>scripts/j1sv_unificare_finalize.ts</files>
  <action>
Creeaza `scripts/j1sv_unificare_finalize.ts` (mutatii, service role key, aceeasi initializare).

**Pas A — garda pre-stergere:** verifica `count` pe `probe_competitie`, `categorii_competitie`, `inscrieri_competitie`, `echipe_competitie` pentru `COMP_GIAO_DAU`. TOATE trebuie sa fie 0. Daca vreuna != 0, tipareste `ABORT - competitia giao_dau inca are copii, nu se sterge` si `process.exit(1)`.

**Pas B — stergere (cerinta pasul 3):** `delete` pe `competitii` cu `.eq('id', COMP_GIAO_DAU)`. Verifica lipsa erorii si re-citeste randul (`.select().eq('id', COMP_GIAO_DAU).maybeSingle()`) — trebuie sa fie `null`. Tipareste confirmarea cu id-ul sters.

**Pas C — redenumire + taxe (cerintele pasurile 4 si 5), un singur `update` pe `COMP_TEHNICA`:**
```
denumire: DENUMIRE_FINALA          // fara sufix "(Tehnica)"
tip: ramane 'tehnica'              // NU il include in payload, nu-l atinge
taxa_individual: 100
taxa_echipa: 150
config_taxe: {
  individual_tehnica: 100,
  individual_cvd: 100,
  echipa_seniori: 150,
  echipa_juniori: 150,
  cvd_echipa: 150,
  currency: 'RON'
}
```
Motivatia valorilor (nu o schimba): `calculeazaTaxaIndividuala` alege `individual_cvd` doar cand `cat.arma` e setat si `individual_tehnica` altfel — ambele 100 => 100 lei individual uniform. `calculeazaTaxaEchipa` alege `echipa_juniori` cand `varsta_max <= 17` si `echipa_seniori` altfel — ambele 150 => 150 lei pe echipa/pereche uniform, deci si pentru giao_dau (`tip_participare: 'pereche'`), sincron si song_luyen. `taxa_individual`/`taxa_echipa` raman sincronizate cu `config_taxe` exact cum face `CompetitieForm.tsx:88-89`, ca sa nu divergheze la o editare ulterioara din UI.

**Pas D — asertiune post-scriere:** re-citeste randul `COMP_TEHNICA` si tipareste `denumire`, `tip`, `taxa_individual`, `taxa_echipa`, `config_taxe`. Verifica in cod ca `denumire` contine substring-ul exact `Juniori 1, Seniori, Veterani` (marker-ul `MARKER_J1SV` din `GenerareSabloaneModal.tsx:13`) si ca `tip === 'tehnica'`; daca nu, `process.exit(1)`.
  </action>
  <verify>
    <automated>npx tsx scripts/j1sv_unificare_finalize.ts</automated>
  </verify>
  <done>Script exit 0. Competitia `32281416-...` nu mai exista in `competitii`. Competitia `3d2187dc-...` are denumirea finala (fara "(Tehnica)"), `tip='tehnica'`, `taxa_individual=100`, `taxa_echipa=150` si `config_taxe` cu cele 6 chei setate.</done>
</task>

<task type="auto">
  <name>Task 4: Verificare finala cu asertiuni (cerinta pasul 7)</name>
  <files>scripts/j1sv_unificare_verify.ts</files>
  <action>
Creeaza `scripts/j1sv_unificare_verify.ts` — READ-ONLY, cu asertiuni care determina codul de iesire. Colecteaza esecurile intr-un array si iesi cu 1 daca array-ul e nevid (nu opri la prima asertiune, ca sa vezi toata imaginea).

Asertiuni:
1. `competitii` filtrat pe `denumire ilike '%Juniori 1, Seniori, Veterani%'` returneaza **exact 1 rand**, si `id === COMP_TEHNICA`.
2. `competitii` cu `.eq('id', COMP_GIAO_DAU)` returneaza **0 randuri**.
3. `categorii_competitie` cu `.eq('competitie_id', COMP_TEHNICA)` are **count exact 74**.
4. `probe_competitie` cu `.eq('competitie_id', COMP_TEHNICA)`: **4 randuri**, iar `Set(tip_proba)` este exact `{thao_quyen_individual, sincron, song_luyen, giao_dau}` (compara ca multimi, nu ca liste ordonate; tipareste valorile reale gasite).
5. Fiecare categorie are `proba_id` non-null si acel `proba_id` apartine multimii de id-uri din probele competitiei (verificare integritate dupa mutare).
6. `numar_categorie` este unic pe cele 74 de randuri, iar multimea valorilor este exact `1..74` (tipareste eventualele lipsuri/duplicate).
7. `taxa_individual === 100`, `taxa_echipa === 150`, `config_taxe.individual_tehnica === 100`, `config_taxe.echipa_seniori === 150`, `config_taxe.echipa_juniori === 150`.
8. `inscrieri_competitie` si `echipe_competitie` pe `COMP_TEHNICA`: count 0 (nimic nu a fost creat accidental).

Tipareste la final `VERIFICARE FINALA OK - competitie unificata: <id>` sau lista esecurilor.
  </action>
  <verify>
    <automated>npx tsx scripts/j1sv_unificare_verify.ts</automated>
  </verify>
  <done>Script exit 0 cu `VERIFICARE FINALA OK`: 1 competitie, 74 categorii numerotate 1-74 unic, 4 probe distincte (thao_quyen_individual, sincron, song_luyen, giao_dau), taxe 100/150, toate `proba_id` valide.</done>
</task>

<task type="auto">
  <name>Task 5: GenerareSabloaneModal — ambele seturi de sabloane pentru J1SV</name>
  <files>components/Competitii/GenerareSabloaneModal.tsx</files>
  <action>
Modifica DOAR `useMemo`-ul `templates` de la liniile 40-46. Forma actuala filtreaza sabloanele dupa `competitie.tip`, deci pe competitia unificata (`tip='tehnica'`) o regenerare ar oferi doar cele 58 categorii tehnice, pierzand cele 16 de giao_dau.

Noua logica: cand `isJ1SV` e adevarat, returneaza concatenarea ambelor seturi **inainte** de orice ramificare pe `competitie.tip`, si **renumeroteaza secvential** rezultatul:

- `isJ1SV` ramane calculat identic: `(competitie.denumire || '').includes(MARKER_J1SV)`
- daca `isJ1SV`: `[...generateTemplateTehnnicaJ1SV(), ...generateTemplateGiaoDauJ1SV()]` mapat cu `(cat, i) => ({ ...cat, numar_categorie: i + 1 })`
- restul ramurilor raman neschimbate: `tehnica -> generateTemplateTehnnica()`, `giao_dau -> generateTemplateGiaoDau()`, `cvd -> generateTemplateCVD()`, default `[]`
- `useMemo` pastreaza dependintele existente `[competitie.tip, competitie.denumire]`

Renumerotarea din `.map` NU e optionala: ambele functii J1SV numeroteaza de la 1 (`utils/competitiiTemplates.ts:339` si `:463`), iar `numar_categorie` din sablon este scris direct in `ordine_afisare` la `handleGenerate` (linia 138: `ordine_afisare: cat.numar_categorie ?? (nextNr + i + 1)`). Fara renumerotare, lista din modal ar afisa 16 numere duplicate si ar insera 16 valori duplicate de `ordine_afisare`.

Adauga un comentariu scurt deasupra ramurii `isJ1SV` care explica de ce se ignora `competitie.tip` (competitie unica unificata, ambele tipuri de probe sub un singur rand).

NU modifica `handleGenerate` — el deja calculeaza `nextNr` din `categoriiExistente` si atribuie `numar_categorie` continuand dupa maximul existent, deci regenerarea peste cele 74 de categorii existente nu va produce coliziuni.
NU modifica mesajul de empty-state de la linia 183 si nici imports (ambele functii J1SV sunt deja importate la liniile 6-9).
  </action>
  <verify>
    <automated>grep -v '^\s*//' components/Competitii/GenerareSabloaneModal.tsx | grep -c "generateTemplateGiaoDauJ1SV()"</automated>
  </verify>
  <done>`templates` returneaza 74 de sabloane pentru orice competitie a carei denumire contine `Juniori 1, Seniori, Veterani`, indiferent de `tip`, cu `numar_categorie` 1..74 unic. Comportamentul pentru competitiile non-J1SV este neschimbat.</done>
</task>

<task type="auto">
  <name>Task 6: Lint + build (cerinta pasul 8)</name>
  <files>—</files>
  <action>
Ruleaza `npm run lint` (adica `tsc --noEmit`) si apoi `npm run build`. Ambele trebuie sa treaca fara erori.

Atentie: `npm run lint` face type-check pe intreg proiectul, inclusiv pe cele 4 scripturi noi din `scripts/`. Daca `tsc` raporteaza erori in scripturile create la Task 1-4 (ex. tipuri implicite `any` pe randurile Supabase, acces la proprietati pe rezultate posibil `null`), repara-le in scripturi — tipeaza defensiv randurile ca `any[]` sau adauga garzi de null, la fel ca `scripts/audit_rls_faza25.ts` (care foloseste `(r: any)`).

Daca `tsc` raporteaza erori preexistente, fara legatura cu fisierele atinse de acest plan, NU le repara — noteaza-le in SUMMARY si mergi mai departe.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>Ambele comenzi ies cu cod 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script one-off -> Supabase (service role) | Scripturile ruleaza cu `SUPABASE_SERVICE_ROLE_KEY`, bypass complet de RLS — orice greseala de filtru scrie/sterge date reale de productie |
| `.env` -> repo git | Cheia de service role e un secret de productie; nu are voie sa ajunga in git sau in output-ul consolei |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l8j-01 | Denial (pierdere date) | `DELETE FROM competitii` in Task 3 | mitigate | Id-uri hardcodate ca `const` la varful scriptului (zero id-uri construite dinamic); garda pre-stergere care cere 0 copii pe 4 tabele; re-citire post-delete care confirma exact randul tinta |
| T-l8j-02 | Tampering (pierdere inscrieri) | UPDATE pe `probe_competitie` / `categorii_competitie` | mitigate | Garda `inscrieri_competitie` + `echipe_competitie` == 0 rulata de doua ori (Task 1 read-only si din nou inline in Task 2 inainte de prima scriere), fail-closed cu `process.exit(1)` |
| T-l8j-03 | Tampering (coliziune numerotare) | `categorii_competitie.numar_categorie` | mitigate | Renumerotare neconditionata 59-74 in loc de pastrarea numerotarii sursa; asertiune la Task 4 ca multimea finala e exact 1..74 fara duplicate |
| T-l8j-04 | Information disclosure | `SUPABASE_SERVICE_ROLE_KEY` | mitigate | Citita exclusiv din `.env` prin `dotenv`, niciodata hardcodata, niciodata tiparita in consola; `.env` deja in `.gitignore:11-12` |
| T-l8j-05 | Elevation of privilege | executie viitoare accidentala a scripturilor | accept | Scripturi one-off, idempotent-safe prin garzi (rularea a doua oara iese cu 1 pentru ca sursa nu mai exista); risc rezidual acceptat pentru un task de reparatie de date |
| T-l8j-SC | Tampering | instalari npm/pip/cargo | n/a | Zero pachete noi — `dotenv`, `tsx`, `@supabase/supabase-js` sunt deja in `package.json` |
</threat_model>

<verification>
1. `npx tsx scripts/j1sv_unificare_verify.ts` iese cu 0 si tipareste `VERIFICARE FINALA OK`.
2. `npm run lint && npm run build` ies cu 0.
3. Verificare manuala optionala in UI (nu blocheaza planul): modulul Competitii afiseaza un singur card pentru Campionatul National J1SV; deschiderea lui arata 74 de categorii pe 4 probe.
</verification>

<success_criteria>
- Garda de siguranta a confirmat 0 inscrieri si 0 echipe inainte de orice mutatie (sau executia DB s-a oprit si a raportat).
- `competitii` contine un singur rand pentru J1SV: `3d2187dc-ffa8-464d-9bf9-b585b58004c7`, `tip='tehnica'`, denumire `Campionatul Național de Qwan Ki Do — Juniori 1, Seniori, Veterani`.
- 74 categorii legate de el, numerotate unic 1..74 (58 originale nemodificate + 16 renumerotate 59..74 cu ordinea relativa pastrata), toate cu `proba_id` valid.
- 4 probe distincte: thao_quyen_individual, sincron, song_luyen, giao_dau.
- `taxa_individual=100`, `taxa_echipa=150`, `config_taxe` completat (100/100/150/150/150 RON).
- `GenerareSabloaneModal` ofera ambele seturi J1SV renumerotate 1..74, indiferent de `competitie.tip`.
- `npm run lint` si `npm run build` trec.
</success_criteria>

<output>
Creeaza `.planning/quick/260901-l8j-unifica-cele-2-competitii-j1sv-intr-o-si/260901-l8j-SUMMARY.md` la final.

**Continut obligatoriu in SUMMARY (cerut explicit de utilizator):**
1. Rezultatul garzii de siguranta (numarul de inscrieri/echipe gasite pe fiecare competitie).
2. Ce s-a mutat: cate probe, cate categorii, si tabelul de renumerotare `numar_categorie vechi -> nou` pentru cele 16 categorii giao_dau.
3. **ID-ul final al competitiei unificate.**
4. Rezultatul verificarii finale (Task 4), cu toate cele 8 asertiuni si valorile reale.
5. **Constatarea pasul 5 (taxe):** `config_taxe` ESTE citit efectiv (lantul `utils/taxeCompetitie.ts` -> `Pas4Sumar.tsx:96,105` + `FinanciarView.tsx:72`, scris de `CompetitieForm.tsx:75-90`), deci a fost completat cu 100/150; mentioneaza si ca inainte era `null` si taxele se rezolvau prin fallback pe coloanele plate.
6. **Constatarea despre constrangerea UNIQUE:** catalogul Postgres nu e interogabil prin PostgREST (nu exista RPC de SQL brut); s-a inferat din date ca nu exista constrangere UNIQUE globala pe `numar_categorie`; renumerotarea 59-74 a fost aplicata oricum, pentru ca ambele seturi de sabloane numeroteaza de la 1.
7. Orice eroare intalnita si cum a fost rezolvata.
</output>
