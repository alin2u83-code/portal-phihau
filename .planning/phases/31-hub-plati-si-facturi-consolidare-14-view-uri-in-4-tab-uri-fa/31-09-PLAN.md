---
phase: 31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa
plan: 09
type: execute
wave: 4
depends_on: [31-08]
files_modified:
  - components/AdminMasterMap.tsx
  - components/menuConfig.ts
  - components/UnifiedDashboard.tsx
  - components/ReportsDashboard.tsx
  - components/Header.tsx
  - components/NavMenu.tsx
  - services/agents/orchestrator.ts
  - services/agents/financiarAgent.ts
  - services/claudeService.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
user_setup: []

must_haves:
  truths:
    - "Pe dashboard-ul adminului (AdminMasterMap, sectiunea 'Financiar & Plăți') exista un singur card 'Plăți & Facturi' catre hub, plus 'Deconturi Federație' separat (D-05) — in locul celor 11 carduri fragmentate"
    - "In meniul lateral (admin federatie si admin club), submeniul 'Financiar & Plăți' are 'Plăți & Facturi' (hub), 'Vacanțe Antrenamente' si 'Deconturi Federație'; Familii ramane in 'Gestiune Membri' (D-05)"
    - "Cardul 'Raport Financiar' din ecranul Rapoarte deschide hub-ul direct pe tab-ul Rapoarte → Raport Financiar"
    - "Header-ul afiseaza titlul 'Plăți & Facturi' pe vederea hub, iar asistentul AI rutează vederea hub catre agentul financiar"
    - "Favoritele/topViews salvate cu literale vechi continua sa afiseze eticheta si sa deschida hub-ul (alias-urile din 31-08)"
  artifacts:
    - path: "components/AdminMasterMap.tsx"
      provides: "Card unic 'Plăți & Facturi' + label 'plati-hub'"
      contains: "view=\"plati-hub\""
    - path: "components/menuConfig.ts"
      provides: "Intrare unica de meniu catre hub"
      contains: "'plati-hub'"
    - path: "components/ReportsDashboard.tsx"
      provides: "Deep-link catre tab-ul Rapoarte al hub-ului"
      contains: "sectiune: 'raport-financiar'"
    - path: "components/Header.tsx"
      provides: "Titlu vedere hub"
      contains: "'plati-hub'"
  key_links:
    - from: "components/ReportsDashboard.tsx"
      to: "contexts/NavigationContext.tsx"
      via: "navigateTo('plati-hub', { tab: 'rapoarte', sectiune: 'raport-financiar' })"
      pattern: "navigateTo\\("
    - from: "components/menuConfig.ts"
      to: "components/AppRouter.tsx case 'plati-hub'"
      via: "{ label: 'Plăți & Facturi', view: 'plati-hub' }"
      pattern: "view: 'plati-hub'"
---

<objective>
Punctul unic de intrare in hub (obiectivul fazei: "consolideaza cele 14 view-uri intr-un singur hub navigabil dintr-un punct unic de intrare in dashboard") — actualizarea tuturor intrarilor fragmentate catre `plati-hub`, plus etichetele si hartile auxiliare (Header, NavMenu, asistent AI), apoi verificarea de final de faza.

Inventarul intrarilor (grep complet facut la planificare pe literalele celor 14 vederi):
- `components/AdminMasterMap.tsx` — dashboard-ul real al adminilor (randat de AppRouter pe `dashboard`): 11 ItemCard-uri financiare (liniile 211-221) + 'deconturi-federatie' (222) + labelMap (91-113). Acesta e punctul principal de intrare — CONTEXT il omitea; e inclus aici.
- `components/menuConfig.ts` — submeniurile 'Financiar & Plăți' din `adminMenu` (56-71) si `adminClubMenu` (145-162); 'Nomenclatoare' din 'Setări & Admin' (79 si 167).
- `components/UnifiedDashboard.tsx:96-98` — carduri Financiar. Nota: componenta NU e montata nicaieri in prezent (grep: niciun import) — actualizata pentru consecventa, cum cere CONTEXT.
- `components/ReportsDashboard.tsx:14` — 'raport-financiar' → deep-link in tab-ul Rapoarte.
- `components/AdminDashboard.tsx:96-131` — contine doar 'familii' (D-05) → NESCHIMBAT (conform CONTEXT: "leave it").
- Link-uri adanci care raman INTENTIONAT pe literale vechi (functioneaza prin alias-urile din 31-08, nu se rescriu ca sa nu atingem module neinrudite): `GestiuneExamene/index.tsx:273` ('gestiune-facturi'), `Grupe/GrupaDetailView.tsx:657` ('gestiune-facturi' + sportivId), `UserProfile.tsx:804` ('plati-scadente'), `SportivDashboard/index.tsx:588,700` si `menuConfig.ts:220` ('istoric-plati', rol SPORTIV — ramura standalone).

Output: 9 fisiere cu modificari mici de configurare/etichete; raport de verificare finala.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-CONTEXT.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-08-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Contracte consumate:
- View literal `'plati-hub'` (types.ts, 31-01); ruta `case 'plati-hub'` + alias-uri (AppRouter.tsx, 31-08).
- `rezolvaPozitieHub` accepta `viewParams` `{ tab: 'rapoarte', sectiune: 'raport-financiar' }` (31-01).
- `useNavigation().navigateTo(view: View, params?: any)` (contexts/NavigationContext.tsx:56-60).
- `components/menuConfig.ts` → `SubMenuItem { label: string; view: View }`.
- `components/AdminMasterMap.tsx` → `ItemCard` props: `title`, `view`, `icon`, `onNavigate`, `badge?`, `isFavorite`, `onToggleFavorite`; `nav`, `favorites`, `toggleFavorite`, `pendingDeconturi` deja in scope.
- `services/agents/orchestrator.ts:42-46` — harta vedere → agent (`'plati-scadente': 'financiar'` etc.); `services/agents/financiarAgent.ts:72` — `views: [...]`; `services/claudeService.ts:21-40` — `VIEW_DESCRIPTIONS`.
- `components/NavMenu.tsx:16-23` — `VIEW_TO_NOTIF_TYPE`; `:92` — `data-tutorial="nav-plati"` (tinta pasului `nav-plati` din `components/Tutorial/tutorialSteps.ts:47-48`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard-uri si meniu — intrare unica 'Plăți & Facturi'</name>
  <read_first>
    - components/AdminMasterMap.tsx (liniile 1-60, 85-140, 205-225)
    - components/menuConfig.ts (integral)
    - components/UnifiedDashboard.tsx (liniile 1-12 si 90-101)
    - components/ReportsDashboard.tsx (integral)
    - contexts/NavigationContext.tsx (liniile 10-26 si 56-60)
    - components/AdminDashboard.tsx (liniile 96-131 — doar confirmare ca nu se atinge)
  </read_first>
  <files>components/AdminMasterMap.tsx, components/menuConfig.ts, components/UnifiedDashboard.tsx, components/ReportsDashboard.tsx</files>
  <action>
AdminMasterMap.tsx:
- In `AccordionItem id="financiar"` inlocuieste cele 11 `ItemCard` cu view-urile financial-dashboard, plati-scadente, gestiune-facturi, facturi-fara-prezenta, jurnal-incasari, raport-financiar, taxe-anuale, reduceri, tipuri-abonament, configurare-preturi, nomenclatoare cu UN singur `<ItemCard title="Plăți & Facturi" view="plati-hub" icon={WalletIcon} onNavigate={nav} isFavorite={favorites.includes('plati-hub')} onToggleFavorite={toggleFavorite} />`. Pastreaza neschimbat `ItemCard`-ul 'Deconturi Federație' cu `badge={pendingDeconturi}` (per D-05).
- In `labelMap` adauga `'plati-hub': 'Plăți & Facturi'`. PASTREAZA toate etichetele vechi (favoritele/topViews salvate in localStorage le folosesc; navigarea lor merge prin alias-uri).
- Elimina un import de iconita doar daca, dupa editare, `grep -c "<NumeIcon" components/AdminMasterMap.tsx` = 0 pentru acea iconita; altfel lasa importul.

menuConfig.ts:
- In `adminMenu` si in `adminClubMenu`, submeniul 'Financiar & Plăți' devine exact: `{ label: 'Plăți & Facturi', view: 'plati-hub' }`, `{ label: 'Vacanțe Antrenamente', view: 'perioade-vacanta' }`, `{ label: 'Deconturi Federație', view: 'deconturi-federatie' }` (per D-05 — Deconturi ramane separat; Vacanțe nu face parte din cele 14 vederi si ramane).
- In ambele meniuri, elimina `{ label: 'Nomenclatoare', view: 'nomenclatoare' }` din 'Setări & Admin' (nomenclatorul tipurilor de plata e acum in hub → Configurare, per D-04).
- NU modifica 'Familii' din 'Gestiune Membri', `instructorMenu` sau `sportivMenu` (inclusiv 'Istoric Plăți' → 'istoric-plati' pentru SPORTIV).

UnifiedDashboard.tsx:
- In `navItems` inlocuieste cele doua intrari 'Dashboard Financiar' (financial-dashboard) si 'Facturi & Plăți' (plati-scadente) cu una singura: `{ title: 'Plăți & Facturi', description: 'Facturi, încasări, rapoarte și configurare financiară.', icon: WalletIcon, view: 'plati-hub' as View, category: 'Financiar', roles: ['ADMIN', 'ADMIN_CLUB', 'SUPER_ADMIN_FEDERATIE'], iconColor: 'text-green-400', iconBg: 'bg-green-400/10' }`. Pastreaza 'Deconturi Federație'.

ReportsDashboard.tsx:
- Importa `useNavigation` din `'../contexts/NavigationContext'` si ia `const { navigateTo } = useNavigation();`.
- Intrarea 'Raport Financiar' devine `{ label: 'Raport Financiar', icon: ChartBarIcon, view: 'plati-hub', params: { tab: 'rapoarte', sectiune: 'raport-financiar' } }`; celelalte intrari raman fara `params`.
- `onClick` al cardului: `report.params ? navigateTo(report.view as View, report.params) : onNavigate(report.view as View)`. Semnatura `ReportsDashboardProps` ramane neschimbata.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && grep -c 'view=\"plati-hub\"' components/AdminMasterMap.tsx && grep -c \"view: 'plati-hub'\" components/menuConfig.ts && grep -c \"sectiune: 'raport-financiar'\" components/ReportsDashboard.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - `components/AdminMasterMap.tsx`: `grep -c 'view="plati-hub"'` = 1; `grep -c 'view="deconturi-federatie"'` = 1; `grep -cE 'view="(financial-dashboard|plati-scadente|gestiune-facturi|facturi-fara-prezenta|jurnal-incasari|raport-financiar|taxe-anuale|reduceri|tipuri-abonament|configurare-preturi|nomenclatoare)"'` = 0; `grep -c "'plati-hub': 'Plăți & Facturi'"` = 1; eticheta veche `'plati-scadente': 'Facturi & Plăți'` inca prezenta in labelMap.
    - `components/menuConfig.ts`: `grep -c "view: 'plati-hub'"` = 2; `grep -cE "view: '(financial-dashboard|plati-scadente|gestiune-facturi|facturi-fara-prezenta|jurnal-incasari|raport-financiar|taxe-anuale|tipuri-abonament|configurare-preturi|reduceri|nomenclatoare)'"` = 0; `grep -c "view: 'deconturi-federatie'"` = 2; `grep -c "view: 'familii'"` = 2; `grep -c "view: 'istoric-plati'"` = 1; `grep -c "view: 'perioade-vacanta'"` = 2.
    - `components/UnifiedDashboard.tsx`: contine `'plati-hub' as View` si nu mai contine `'financial-dashboard' as View` sau `'plati-scadente' as View`.
    - `components/ReportsDashboard.tsx`: contine `navigateTo(` si `sectiune: 'raport-financiar'`; `ReportsDashboardProps` neschimbat.
    - `git diff --stat -- components/AdminDashboard.tsx` → nicio modificare.
  </acceptance_criteria>
  <done>Dashboard-ul adminului, meniul lateral, UnifiedDashboard si ecranul Rapoarte duc toate in hub printr-o singura intrare; Familii si Deconturi raman separate.</done>
</task>

<task type="auto">
  <name>Task 2: Etichete si harti auxiliare (Header, NavMenu, asistent AI) + verificarea finala a fazei</name>
  <read_first>
    - components/Header.tsx (liniile 20-65)
    - components/NavMenu.tsx (liniile 10-25 si 85-100)
    - components/Tutorial/tutorialSteps.ts (liniile 40-55)
    - services/agents/orchestrator.ts (liniile 30-55)
    - services/agents/financiarAgent.ts (liniile 60-75)
    - services/claudeService.ts (liniile 15-45)
  </read_first>
  <files>components/Header.tsx, components/NavMenu.tsx, services/agents/orchestrator.ts, services/agents/financiarAgent.ts, services/claudeService.ts</files>
  <action>
- `components/Header.tsx` `VIEW_TITLES`: adauga `'plati-hub': 'Plăți & Facturi',` (pastreaza titlurile vechi — se afiseaza cand hub-ul e deschis printr-un alias).
- `components/NavMenu.tsx`: in `VIEW_TO_NOTIF_TYPE` adauga `'plati-hub': 'plata'`; la linia 92 conditia devine `item.view === 'plati-scadente' || item.view === 'plati-hub' ? 'nav-plati'` (pasul de tutorial `nav-plati` continua sa aiba tinta).
- `services/agents/orchestrator.ts`: in harta vedere → agent adauga `'plati-hub': 'financiar',`.
- `services/agents/financiarAgent.ts`: adauga `'plati-hub'` in array-ul `views`.
- `services/claudeService.ts` `VIEW_DESCRIPTIONS`: adauga `'plati-hub': 'Hub Plăți & Facturi (facturi, încasări, rapoarte, configurare financiară)',`.

Verificare finala a fazei (ruleaza si consemneaza iesirile in SUMMARY):
1. `npm run lint` si `npm run build`.
2. `npx tsx components/Plati/hub/platiHubConfig.test.ts` si `npx tsx utils/facturaDetaliu.test.ts`.
3. Garda D-07 repo-wide: `grep -rnE "from\('plati'\)\.update\(updates\)" components services hooks utils` → 0 rezultate.
4. Garda de rutare: pentru fiecare literal din (plati-scadente, gestiune-facturi, facturi-fara-prezenta, jurnal-incasari, raport-financiar, financial-dashboard, tipuri-abonament, configurare-preturi, reduceri, taxe-anuale, nomenclatoare, istoric-plati, plati-hub) exista exact un `case '<literal>':` in `components/AppRouter.tsx`.
5. Consemneaza in SUMMARY rezultatul "Verificare live D-09" din 31-07-SUMMARY.md (DA/NU/NEVERIFICAT) — daca e NEVERIFICAT sau NU, include-l explicit in lista de verificare umana de mai jos.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && npm run build && npx tsx components/Plati/hub/platiHubConfig.test.ts && npx tsx utils/facturaDetaliu.test.ts && ! grep -rnE \"from\\('plati'\\)\\.update\\(updates\\)\" components services hooks utils && grep -c \"'plati-hub'\" components/Header.tsx components/NavMenu.tsx services/agents/orchestrator.ts services/agents/financiarAgent.ts services/claudeService.ts"</automated>
    <human-check>
Verificare end-of-phase (workflow.human_verify_mode = end-of-phase). Se poate automatiza cu skill-ul proiectului playwright-portal-test; altfel manual, pe un club de test:
1. Login ADMIN_CLUB → Dashboard → sectiunea "Financiar & Plăți" arata DOAR "Plăți & Facturi" si "Deconturi Federație". Click "Plăți & Facturi" → titlu "Plăți & Facturi", 4 tab-uri: Facturi, Încasări, Rapoarte, Configurare; un singur buton "Meniu".
2. Tab Facturi: pastilele "Facturi & Plăți", "Gestiune Facturi", "Facturi fără Prezență" comuta ecranele; niciun al doilea buton "Meniu".
3. In "Facturi & Plăți" bifeaza 2 facturi → "Încasează 2 facturi selectate" → se deschide tab Încasări / Jurnal Încasări cu "Încasare Colectivă (2 facturi)". Salveaza → dupa ~1.5 s revii pe Facturi, iar Jurnalul deschis ulterior din tab e gol (fara facturile vechi).
4. Pe un rand de factura click pe butonul "Detalii" (iconita lista) → modal cu Context (plătitor, club, tip, perioadă), Sume separate (facturată / încasată / rest), Istoric tranzacții, "Marchează Achitat cu X RON". Pe o factura Neachitat de test: introdu o suma mai mica decat restul → apare avertismentul de corectie a sumei facturate → confirma → status Achitat, rest 0, tranzactia apare in istoric.
5. "Corectează manual": modifica doar "Sumă facturată" → Salvează → fara eroare PGRST204; valoarea noua apare in lista.
6. Click pe numele unui sportiv din Facturi → profil → inapoi (buton Header / gest) → revii pe acelasi tab si aceeasi pastila.
7. Tab Configurare: pastila "Taxe Anuale" vizibila pentru ADMIN_CLUB; "Nomenclatoare (Tipuri Plăți)" functioneaza. Meniul lateral nu mai are "Nomenclatoare" la Setări.
8. Ecranul Rapoarte → "Raport Financiar" → hub pe tab Rapoarte / Raport Financiar.
9. O favorita veche (ex. "Jurnal Încasări") din pastilele QuickAccess, daca exista, deschide hub-ul pe Încasări / Jurnal.
10. Login INSTRUCTOR → profil sportiv → "Istoric Financiar" → "Detalii" pe o factura → modalul se deschide cu actiunea rapida; o corectie de test fie reuseste, fie afiseaza "Verificați permisiunile." (confrunta cu rezultatul (a) din 31-07-SUMMARY).
11. Login SPORTIV → meniul "Istoric Plăți" afiseaza ecranul personal vechi (cu "Înapoi la Portal"), nu hub-ul.
12. "Familii" (Gestiune Membri) si "Deconturi Federație" se deschid ca ecrane separate, neschimbate.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm run build`, ambele teste tsx ies cu cod 0.
    - `grep -c "'plati-hub'"` >= 1 in fiecare din: components/Header.tsx, components/NavMenu.tsx (>= 2), services/agents/orchestrator.ts, services/agents/financiarAgent.ts, services/claudeService.ts.
    - `grep -c "item.view === 'plati-hub'" components/NavMenu.tsx` = 1.
    - Garda D-07 repo-wide intoarce 0 rezultate.
    - SUMMARY contine iesirile celor 5 verificari si starea "Verificare live D-09" preluata din 31-07.
  </acceptance_criteria>
  <done>Toate etichetele si hartile auxiliare cunosc vederea hub; faza trece toate gardele automate si are lista de verificare umana end-of-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meniu/dashboard (UX) → rute protejate | Ascunderea/afisarea intrarilor e doar UX; guard-ul real e in AppRouter + RLS |
| activeView → asistent AI (context de prompt) | Descrierea vederii ajunge in promptul trimis LLM-ului |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09-01 | Elevation of Privilege | Card/meniu 'plati-hub' vizibil unui rol fara drepturi | accept | Guard-ul `canManageFinances` din AppRouter (31-08) si RLS raman poarta; meniurile modificate sunt doar adminMenu/adminClubMenu (instructorMenu/sportivMenu neatinse) |
| T-31-09-02 | Information Disclosure | Descrierea vederii 'plati-hub' in promptul AI | accept | Text static, fara date personale; aceeasi clasa de continut ca descrierile existente |
| T-31-09-03 | Denial of Service | Favorite/topViews cu literale vechi | mitigate | labelMap pastreaza etichetele vechi; AppRouter pastreaza alias-urile (31-08); verificat in human-check pasul 9 |
| T-31-09-SC | Tampering | npm installs | accept | Zero pachete noi |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Inventarul complet al fazei e in `31-01-PLAN.md` (sectiunea omonima). Acest plan (31-09) nu creeaza simboluri noi; adauga intrarea `'plati-hub'` in:
- `components/AdminMasterMap.tsx` (ItemCard unic + labelMap), `components/menuConfig.ts` (adminMenu, adminClubMenu), `components/UnifiedDashboard.tsx` (navItems), `components/ReportsDashboard.tsx` (deep-link cu params `{ tab: 'rapoarte', sectiune: 'raport-financiar' }`).
- `components/Header.tsx` (`VIEW_TITLES`), `components/NavMenu.tsx` (`VIEW_TO_NOTIF_TYPE`, tinta tutorial `nav-plati`), `services/agents/orchestrator.ts`, `services/agents/financiarAgent.ts`, `services/claudeService.ts` (`VIEW_DESCRIPTIONS`).
</artifacts_this_phase_produces>

<verification>
1. `npm run lint` + `npm run build` — cod 0.
2. Ambele teste tsx ale fazei — cod 0.
3. Gardele grep (intrari unice, D-05, D-07 repo-wide, case-uri AppRouter).
4. Lista human-check end-of-phase (12 pasi).
</verification>

<success_criteria>
- Hub-ul "Plăți & Facturi" e accesibil dintr-un singur punct de intrare pe dashboard si din meniu.
- Nicio intrare veche nu duce intr-un ecran rupt; SPORTIV, Familii si Deconturi Federatie sunt neschimbate.
- Toate gardele automate ale fazei trec.
</success_criteria>

<output>
Creeaza `.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-09-SUMMARY.md` cu iesirile verificarii finale si lista human-check.
</output>
