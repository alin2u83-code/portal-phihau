---
phase: 08-button-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui.tsx
autonomous: true
requirements:
  - BTN-01
  - BTN-02

must_haves:
  truths:
    - "Button accepta pill, ghost, outline, leftIcon, rightIcon, size xs/lg fara erori TypeScript"
    - "Toate cele ~449 call-site-uri existente raman vizual identice — nicio regresia de padding sau culoare"
    - "ConfirmButton exportat din ui.tsx: click inicial afiseaza confirmare inline, Da cheama onConfirm, Nu revine, timeout 3s revine automat"
    - "Ghost/outline variants urmeaza tema activa prin CSS vars inline — nu Tailwind hardcodat"
  artifacts:
    - path: "components/ui.tsx"
      provides: "Button extins + ConfirmButton exportat"
      contains: "pill?: boolean"
    - path: "components/ui.tsx"
      provides: "sizeClasses cu xs si lg adaugate"
      contains: "xs: 'px-3 py-1 text-xs'"
    - path: "components/ui.tsx"
      provides: "variantStyles cu ghost_primary, ghost_secondary, ghost_danger, ghost_success, ghost_info, ghost_warning si echivalentele outline_*"
      contains: "ghost_primary"
    - path: "components/ui.tsx"
      provides: "ConfirmButton cu useRef timer si useEffect cleanup"
      contains: "export const ConfirmButton"
  key_links:
    - from: "Button finalClassName"
      to: "className prop"
      via: "concatenare string — className TREBUIE sa ramana ultimul element"
      pattern: "\\$\\{className\\}.*touch-manipulation"
    - from: "ConfirmButton"
      to: "useEffect cleanup"
      via: "timerRef.current clearTimeout la unmount"
      pattern: "clearTimeout\\(timerRef\\.current\\)"
---

<objective>
Extindem backward-compatible componenta Button din components/ui.tsx cu: variante vizuale ghost si outline (inline styles cu CSS vars pentru theme-following), prop boolean pill (rounded-full), props leftIcon/rightIcon (ReactNode cu gap-2), dimensiuni xs si lg. Adaugam componenta ConfirmButton exportata — pattern confirmare inline cu auto-reset 3s via useRef.

Purpose: Furnizeaza sistemul de variante Button complet pentru designul PhiHau — zero breaking changes pe ~449 call-site-uri existente (D-01/BTN-01).
Output: components/ui.tsx modificat cu noile exporturi Button si ConfirmButton.
</objective>

<execution_context>
@C:/Users/lungu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lungu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/Users/lungu/portal-phihau/.planning/PROJECT.md
@C:/Users/lungu/portal-phihau/.planning/ROADMAP.md
@C:/Users/lungu/portal-phihau/.planning/phases/08-button-design-system/08-CONTEXT.md
@C:/Users/lungu/portal-phihau/.planning/phases/08-button-design-system/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extindere ButtonProps si sizeClasses — additive, zero breaking changes (BTN-01)</name>
  <files>components/ui.tsx</files>

  <read_first>
  Citeste components/ui.tsx liniile 1-107 complet inainte de orice modificare.
  Extrage exact:
  - Signatura actuala a interfetei ButtonProps (liniile 6-10)
  - Structura actuala a sizeClasses (liniile 27-30)
  - Structura actuala a variantStyles (liniile 41-66)
  - Linia 68: finalClassName — ordinea exacta de concatenare (CRITICA)
  - Linia 70-78: content (isLoading spinner)
  - Liniile 80-107: ramurile label si button
  </read_first>

  <action>
  Modifica components/ui.tsx — NUMAI modificari additive pe componenta Button.

  **1. Extinde interfata ButtonProps** (linia 6-10, pastreaza toate campurile existente neschimbate):

  Adauga dupa `isLoading?: boolean;`:
  - `pill?: boolean;`
  - `ghost?: boolean;`
  - `outline?: boolean;`
  - `leftIcon?: ReactNode;`
  - `rightIcon?: ReactNode;`

  Schimba `size?: 'sm' | 'md';` in `size?: 'xs' | 'sm' | 'md' | 'lg';`

  **2. Extinde destructuring-ul in export const Button** (linia 12):

  Adauga in destructuring dupa `isLoading,`: `pill, ghost, outline, leftIcon, rightIcon,`

  **3. Inlocuieste baseClasses static cu expresie dinamica** (linia 25):

  Inlocuieste:
  `const baseClasses = "inline-flex items-center justify-center rounded-xl font-medium ...";`

  Cu:
  ```
  const roundedClass = pill ? 'rounded-full' : 'rounded-xl';
  const baseClasses = `inline-flex items-center justify-center ${roundedClass} font-medium transition-all focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-900 disabled:opacity-50 disabled:cursor-not-allowed text-sm whitespace-nowrap`;
  ```

  **4. Extinde sizeClasses** (liniile 27-30) — tipul Record trebuie sa includa xs si lg:

  Inlocuieste `const sizeClasses = {` cu:
  ```
  const sizeClasses: Record<'xs' | 'sm' | 'md' | 'lg', string> = {
    xs: 'px-3 py-1 text-xs',
    sm: 'px-4 py-2 text-sm',
    md: 'px-6 py-3 text-base',
    lg: 'px-8 py-4 text-lg',
  };
  ```

  **5. Extinde variantStyles cu ghost si outline** (dupa linia 66, inainte de `}`):

  Adauga intrari noi in obiectul variantStyles (tip `Partial<Record<string, React.CSSProperties>>`):

  Chei ghost (background transparent, text si border din CSS var):
  - `ghost_primary`: backgroundColor transparent (hover: color-mix(in srgb, var(--t-primary) 10%, transparent)), color var(--t-primary), border 1px solid var(--t-primary)
  - `ghost_secondary`: backgroundColor transparent (hover: var(--t-surface-2)), color var(--t-secondary-fg), border 1px solid var(--t-border)
  - `ghost_danger`: backgroundColor transparent (hover: color-mix(in srgb, var(--t-status-danger) 10%, transparent)), color var(--t-status-danger), border 1px solid var(--t-status-danger)
  - `ghost_success`: la fel cu var(--t-status-success)
  - `ghost_info`: la fel cu var(--t-status-info)
  - `ghost_warning`: la fel cu var(--t-status-warning)

  Chei outline (identic ghost dar border 2px solid):
  - `outline_primary`, `outline_secondary`, `outline_danger`, `outline_success`, `outline_info`, `outline_warning`
  - Aceleasi culori ca ghost, dar `border: '2px solid var(--t-*)'`

  Nota: variantStyles este deja `Partial<Record<string, React.CSSProperties>>` — adaugarea de chei noi nu necesita modificarea tipului.

  IMPORTANT — hover conditionat: valoarea isHovered se calculeaza la randare, deci fiecare cheie din variantStyles trebuie sa respecte starea curenta isHovered. Calculul cheii se face in JSX (pasul 7).

  **6. Calculeaza cheia de stil** — adauga dupa declararea variantStyles (si dupa isHovered state):

  ```
  const stylePrefix = ghost ? 'ghost_' : outline ? 'outline_' : '';
  const styleKey = `${stylePrefix}${variant}${isHovered ? '_hover' : ''}`;
  const activeStyle = variantStyles[styleKey] ?? variantStyles[variant] ?? {};
  ```

  Nota: variantStyles nu va mai fi indexat direct cu `variant` in JSX — va fi folosit prin `activeStyle`.

  **7. Extinde content pentru icoane** (linia 70-78):

  Inlocuieste:
  ```
  const content = isLoading ? (
    <span ...>spinner... Procesare...</span>
  ) : children;
  ```

  Cu:
  ```
  const content = isLoading ? (
    <span className="flex items-center gap-2">
      <svg className="animate-spin h-4 w-4 text-white" ...>...</svg>
      Procesare...
    </span>
  ) : (leftIcon || rightIcon) ? (
    <span className="flex items-center gap-2">
      {leftIcon}
      {children}
      {rightIcon}
    </span>
  ) : children;
  ```

  **8. Actualizeaza finalClassName** — ordinea TREBUIE sa ramana identica, ${className} trebuie sa ramana ultimul inainte de touch-manipulation:

  Linia 68 ramane structural identica:
  `const finalClassName = \`${baseClasses} ${sizeClasses[size]} ${variantClasses[variant]} ${className} touch-manipulation\`;`

  (className poate fi undefined pe unele call-site-uri — adauga `?? ''` daca TypeScript raporteaza eroare, dar verifica ca ordinea nu se schimba.)

  **9. Actualizeaza referintele la variantStyles in JSX** (liniile 85 si 98):

  Inlocuieste `style={variantStyles[variant]}` cu `style={activeStyle}` in ambele ramuri (label si button).

  ATENTIE: Nu modifica nicio alta logica (disabled, isLoading, ...props spread, onMouseEnter/Leave).
  </action>

  <verify>
  `cd "C:\Users\lungu\portal-phihau" && npx tsc --noEmit 2>&1 | head -30`

  Rezultat asteptat: zero erori TypeScript referitoare la ButtonProps, sizeClasses sau variantStyles.
  </verify>

  <acceptance_criteria>
  - TypeScript: `npx tsc --noEmit` trece fara erori in ui.tsx
  - Prop `pill?: boolean` prezent in interfata ButtonProps
  - Prop `ghost?: boolean` si `outline?: boolean` prezente in interfata ButtonProps
  - Props `leftIcon?: ReactNode` si `rightIcon?: ReactNode` prezente
  - Tipul lui `size` este `'xs' | 'sm' | 'md' | 'lg'` (nu mai e `'sm' | 'md'`)
  - sizeClasses are chei `xs` si `lg` cu valorile `px-3 py-1 text-xs` si `px-8 py-4 text-lg`
  - variantStyles contine cheia `ghost_primary` si `outline_primary`
  - finalClassName pastreaza `${className}` ca ultimul element inainte de `touch-manipulation`
  - variantStyles[variant] este inlocuit cu activeStyle in ambele ramuri JSX (label si button)
  - Valoarea default a lui size ramane `'md'` in destructuring
  - Valoarea default a lui variant ramane `'primary'` in destructuring
  </acceptance_criteria>

  <done>Button accepta toate prop-urile noi fara erori TypeScript; call-site-urile existente care nu trimit noile props raman functional identice.</done>
</task>

<task type="auto">
  <name>Task 2: Adauga ConfirmButton — componenta exportata cu state machine idle/confirming si auto-reset 3s (BTN-02)</name>
  <files>components/ui.tsx</files>

  <read_first>
  Citeste components/ui.tsx liniile 1-5 (imports) pentru a confirma ca useState, useRef, useEffect sunt deja importate (sunt pe linia 1).
  Citeste sfarsitul fisierului (dupa linia 107) pentru a stabili exact unde inserezi ConfirmButton — imediat dupa inchiderea componentei Button (linia 107: `};`).
  </read_first>

  <action>
  Adauga imediat dupa `}; // inchidere Button` (linia 107) urmatoarea componenta:

  **Interfata ConfirmButtonProps:**

  ```
  export interface ConfirmButtonProps extends Omit<ButtonProps & { as?: 'label'; htmlFor?: string }, 'onClick' | 'as'> {
    onConfirm: () => void;
    confirmText?: string;
    confirmLabel?: string;
    cancelLabel?: string;
  }
  ```

  Motivatie Omit: ConfirmButton intercepteaza onClick (nu-l expune), si nu suporta `as='label'` (nu exista semantic use-case pentru confirmare label).

  **Componenta ConfirmButton:**

  ```
  export const ConfirmButton: React.FC<ConfirmButtonProps> = ({
    onConfirm,
    confirmText = 'Ești sigur?',
    confirmLabel = 'Da',
    cancelLabel = 'Nu',
    children,
    variant = 'danger',
    ...buttonProps
  }) => {
    const [confirming, setConfirming] = useState(false);
    const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null);

    const startConfirming = () => {
      setConfirming(true);
      timerRef.current = setTimeout(() => setConfirming(false), 3000);
    };

    const handleConfirm = () => {
      if (timerRef.current) clearTimeout(timerRef.current);
      setConfirming(false);
      onConfirm();
    };

    const handleCancel = () => {
      if (timerRef.current) clearTimeout(timerRef.current);
      setConfirming(false);
    };

    useEffect(() => {
      return () => {
        if (timerRef.current) clearTimeout(timerRef.current);
      };
    }, []);

    if (confirming) {
      return (
        <span className="inline-flex items-center gap-2">
          <span className="text-sm text-slate-400">{confirmText}</span>
          <Button size="sm" variant={variant} onClick={handleConfirm}>{confirmLabel}</Button>
          <Button size="sm" variant="secondary" onClick={handleCancel}>{cancelLabel}</Button>
        </span>
      );
    }

    return (
      <Button variant={variant} onClick={startConfirming} {...buttonProps}>
        {children}
      </Button>
    );
  };
  ```

  Note de implementare:
  - timerRef foloseste useRef (nu useState) — ID-ul timerului nu conduce randarea, e doar infrastructura de cleanup. Evita re-randare inutila si ref-ul e mereu curent in callback.
  - useEffect cleanup garanteaza clearTimeout la unmount — previne warning "Can't perform a React state update on an unmounted component".
  - In starea confirming, cele doua butoane Button size="sm" sunt focusable si primesc keyboard events nativ.
  - ConfirmButton nu trece `as` spre Button intern (deja exclus prin Omit).
  </action>

  <verify>
  `cd "C:\Users\lungu\portal-phihau" && npx tsc --noEmit 2>&1 | head -30`

  Verificare manuala: importa ConfirmButton din components/ui.tsx intr-un fisier test temporar si verifica ca TypeScript accepta `onConfirm`, `confirmText`, `variant`, `size`, `disabled` fara erori.
  </verify>

  <acceptance_criteria>
  - `export const ConfirmButton` prezent in components/ui.tsx
  - `export interface ConfirmButtonProps` prezent si exportat
  - ConfirmButtonProps extinde Omit-ul corect (fara onClick si fara as)
  - `onConfirm: () => void` este required (nu optional)
  - `confirmText`, `confirmLabel`, `cancelLabel` sunt optional cu default-uri romanesti
  - `variant` default este `'danger'`
  - `timerRef` este `useRef<ReturnType<typeof setTimeout> | null>(null)` — nu useState
  - `useEffect` cu cleanup `clearTimeout` prezent
  - `npx tsc --noEmit` trece zero erori dupa adaugarea ConfirmButton
  - Starea confirming randeaza `<span>` cu textul confirmText + Button Da + Button Nu
  - Starea idle randeaza un Button normal cu onClick=startConfirming
  </acceptance_criteria>

  <done>ConfirmButton este exportat din ui.tsx, accepta onConfirm + optionale de text, varianta default danger, auto-reset 3s via useRef, cleanup la unmount; TypeScript fara erori.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → Button render | Props vin din componente parinte — toate sunt tipate TypeScript, zero input utilizator nevalidat |
| ConfirmButton → onConfirm callback | onConfirm este furnizat de parinte; ConfirmButton nu valideaza continutul — responsabilitatea parintelui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | ConfirmButton timer | mitigate | useRef + useEffect cleanup previne timer leak si state update pe component desmontat |
| T-08-02 | Elevation of Privilege | Button as="label" | accept | ConfirmButton exclude as="label" prin Omit — fara vector de exploit |
| T-08-SC | Tampering | npm/pip install | accept | Aceasta faza nu instaleaza niciun pachet nou — sectiunea nu se aplica |
</threat_model>

<verification>
1. `cd "C:\Users\lungu\portal-phihau" && npx tsc --noEmit` — zero erori
2. Grep confirm: `grep -n "pill\|ghost\|outline\|leftIcon\|rightIcon\|ConfirmButton\|xs.*px-3\|lg.*px-8" components/ui.tsx` — toate prezente
3. Grep ordine className: `grep -n 'className.*touch-manipulation' components/ui.tsx` — `${className}` trebuie sa fie inainte de `touch-manipulation` pe linia finalClassName
4. Grep ghost_primary in variantStyles: `grep -n "ghost_primary" components/ui.tsx` — prezent
5. Dev server pornit: `npm run dev` — fara erori de compilare in consola
</verification>

<success_criteria>
- Button API existent 100% neschimbat — zero breaking changes
- Variante ghost si outline disponibile cu inline styles CSS vars — urmeaza tema la runtime
- Dimensiunile xs si lg adaugate (xs: px-3 py-1 text-xs, lg: px-8 py-4 text-lg)
- pill=true aplica rounded-full in loc de rounded-xl
- leftIcon/rightIcon randati cu gap-2 in flex container
- ConfirmButton exportat cu behavior complet confirmare inline
- `npx tsc --noEmit` trece fara erori
</success_criteria>

<output>
Creeaza `.planning/phases/08-button-design-system/08-01-SUMMARY.md` cand e gata.

## Artifacts This Phase Produces

| Artifact | Tip | Descriere |
|----------|-----|-----------|
| `components/ui.tsx` | MODIFICAT | Button extins cu pill/ghost/outline/leftIcon/rightIcon/xs/lg + ConfirmButton exportat |
</output>
