---
phase: 30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament
plan: 05
type: execute
wave: 3
depends_on: ["30-02", "30-04"]
files_modified:
  - components/Plati/PlatiScadente.tsx
autonomous: true
requirements: [ABX-04, ABX-05, ABX-06]
user_setup: []

must_haves:
  truths:
    - "La generarea abonamentelor, un sportiv cu pauza mai lunga decat pragul clubului primeste data_start_facturare resetata la luna curenta, deci nu mai apare cu luni lipsa retroactive"
    - "La generarea abonamentelor, un sportiv sub pragul de gratie isi pastreaza data_start_facturare, deci lunile lipsa raman datorate"
    - "Factura generata pentru o entitate care indeplineste o politica de loialitate are suma redusa efectiv, nu doar un mesaj informativ"
    - "Fiecare reducere de loialitate aplicata e trasabila: suma_initiala, reducereDetalii si un rand in aplicare_reduceri"
    - "Cele doua comentarii 'Bug 4 TODO' din PlatiScadente.tsx nu mai exista"
  artifacts:
    - path: "components/Plati/PlatiScadente.tsx"
      provides: "handleGenerateSubscriptions cu gratie + loialitate aplicate efectiv"
      contains: "decideGratieReinnoire"
  key_links:
    - from: "components/Plati/PlatiScadente.tsx"
      to: "utils/perioadaGratie.ts"
      via: "import decideGratieReinnoire, primaZiLunaCurenta"
      pattern: "from '\\.\\./\\.\\./utils/perioadaGratie'"
    - from: "components/Plati/PlatiScadente.tsx"
      to: "utils/loialitateReinnoiri.ts"
      via: "import numaraReinnoiriConsecutive, calculeazaBonusLoialitate"
      pattern: "from '\\.\\./\\.\\./utils/loialitateReinnoiri'"
    - from: "components/Plati/PlatiScadente.tsx"
      to: "public.sportivi.data_start_facturare"
      via: "update batch pe id-urile care depasesc pragul de gratie"
      pattern: "data_start_facturare"
    - from: "components/Plati/PlatiScadente.tsx"
      to: "public.aplicare_reduceri"
      via: "insert jurnal dupa inserarea facturilor"
      pattern: "from\\('aplicare_reduceri'\\)"
---

<objective>
Punctul de cuplare al fazei 30 cu banii reali. 30-RESEARCH.md § Pitfall 5 e categoric: `handleGenerateSubscriptions` din `components/Plati/PlatiScadente.tsx` e SINGURUL loc care creeaza facturile lunare automate; orice logica de gratie sau de loialitate care nu se cupleaza aici ramane cosmetica (configurare salvata, facturi identice).

Acest plan e proprietarul unic al fisierului `components/Plati/PlatiScadente.tsx` in faza 30 — de aceea contine ambele integrari (gratie si loialitate) intr-un singur plan, in loc sa produca doua planuri care se lupta pe acelasi fisier.

Purpose: Perioada de gratie si bonusul de loialitate devin efecte reale asupra facturilor emise, cu urma de audit, si se inchide TODO-ul "Bug 4" lasat deschis in cod.
Output: `handleGenerateSubscriptions` extins cu doua etape noi, fara a schimba semnatura sau comportamentul existent de credit/duplicate.

Ce NU se schimba (garda de regresie — toate sunt comportamente existente confirmate in cod):
- Blocarea adminilor de federatie (aruncarea de la inceputul functiei).
- `setIsGenerating(true)` inainte de orice `await` (fix-ul Bug 3 impotriva dublu-click).
- Refetch-ul proaspat de `tranzactii` si `plati` din DB inainte de calculul soldului (fix-ul Bug 5).
- Excluderea sportivilor din grupe cu "retras" in denumire.
- Verificarea explicita a facturilor deja existente pe `(tip='Abonament', luna, an)` inainte de insert.
- Logica de stingere din credit (`creditSportiv >= sumaDeFacturat` → `status='Achitat'`; altfel scadere partiala).
- Colectarea sportivilor ignorati / cu tip de abonament din sezon arhivat.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-CONTEXT.md
@.planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-RESEARCH.md
@.planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-02-SUMMARY.md
@.planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-04-SUMMARY.md
@components/Plati/PlatiScadente.tsx
@utils/luniLipsa.ts
@hooks/useDataStartFacturare.ts
@CLAUDE.md
</context>

<interfaces>
Contracte de consumat (create in planurile 30-02 si 30-04, nu le redefini):
```
// utils/perioadaGratie.ts
type DecizieGratie = 'pastreaza' | 'reseteaza';
function decideGratieReinnoire(ultimaLunaFacturata: {luna:number;an:number} | null, azi: Date, pragZile: number): DecizieGratie;
function primaZiLunaCurenta(azi: Date): string; // 'YYYY-MM-DD'

// utils/loialitateReinnoiri.ts
function numaraReinnoiriConsecutive(platiEntitate: Plata[], azi: Date): number;
function calculeazaBonusLoialitate(politici: PoliticaReducere[], reinnoiriConsecutive: number, pretLista: number): { sumaBonus: number; politica: PoliticaReducere | null; detalii: string | null };

// services/loialitateService.ts
async function getPoliticiLoialitate(clubId: string): Promise<{ data: PoliticaReducere[] | null; error: Error | null }>;
```

Ancore existente in `components/Plati/PlatiScadente.tsx` (numere de linie valabile inainte de editare):
- 136 `const handleGenerateSubscriptions = async () => {`
- 154-159 refetch `tranzactiiProaspete` / `platiProaspete`
- 181-202 `sportiviActiviBrut` (`select('*')` — deci poarta si `data_start_facturare`) si filtrarea grupelor "retras"
- 204-223 `today`, `dataCurenta`, `lunaText`, `anulCurent`, `lunaCurenta`, `tipuriSezon`
- 225-257 bucla pe familii (`abonamentConfig`, `creditFamilie`, linia 242 `let sumaDeFacturat = abonamentConfig.pret;`)
- 239-241 si 270-271 comentariile `Bug 4 TODO` — de ELIMINAT
- 259-289 bucla pe sportivi individuali (linia 272 `let sumaDeFacturat = abonamentConfig.pret;`)
- 291-330+ verificarea duplicatelor si insert-urile `platiSportivNoi` / `platiFamilieNoi` cu `.select()`

Contracte externe folosite:
- `types.ts` → `Plata` (atentie: coloana `reducereDetalii` e camelCase in DB), `PoliticaReducere`, `Club.perioada_gratie_zile`
- `hooks/useDataStartFacturare.ts` → cheile React Query `['data-start-facturare', sportivId]` si `['data-start-facturare-all']`
- `useData()` → `clubs` e deja in scope in componenta (linia 79)
- `queryClient` din `useQueryClient()` e deja in scope (linia 88)

Proiect Supabase live: `wuhidifzsutwgdfkwhmd`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Aplica perioada de gratie in handleGenerateSubscriptions</name>
  <read_first>
    - components/Plati/PlatiScadente.tsx (liniile 136-300 — intreaga functie `handleGenerateSubscriptions`)
    - utils/perioadaGratie.ts (contractul exact creat in planul 30-02)
    - utils/luniLipsa.ts (ca sa intelegi ce inseamna concret `data_start_facturare` pentru raportul Luni Lipsa)
    - hooks/useDataStartFacturare.ts (cheile de cache care trebuie invalidate)
    - .planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-02-SUMMARY.md
  </read_first>
  <files>components/Plati/PlatiScadente.tsx</files>
  <action>
Adauga importul `import { decideGratieReinnoire, primaZiLunaCurenta } from '../../utils/perioadaGratie';` langa importul existent al lui `formatLuna` (linia 17).

Insereaza etapa de gratie DUPA calcularea lui `sportiviActivi` si a variabilelor de data (dupa linia ~223, inainte de bucla pe familii de la linia 225):

1. Citeste pragul clubului: `const pragGratie = (clubs || []).find(c => c.id === clubId)?.perioada_gratie_zile ?? 30;`
   — `clubs` e deja in scope din `useData()`. Fallback-ul 30 acopera cazul in care cache-ul nu poarta inca noua coloana.
2. Construieste, din `platiProaspete` (deja refetchuit din DB la liniile 154-159), o harta `ultimaLunaPerSportiv: Map<string, {luna:number; an:number}>` — pentru fiecare `sportiv_id`, cea mai recenta pereche `(an, luna)` dintre randurile cu `tip === 'Abonament'`, `status !== 'Anulat'`, `luna != null`, `an != null`. Se foloseste ultima luna FACTURATA (nu doar achitata), pentru ca `data_start_facturare` descrie continuitatea facturarii, nu a incasarii — noteaza asta intr-un comentariu de doua randuri in cod.
3. Pentru fiecare sportiv din `sportiviActivi`:
   - citeste `dataStartCurenta = (sportiv as any).data_start_facturare` (vine din `select('*')`, nu e pe interfata `Sportiv` — pastreaza precedentul fazei 14: NU adauga campul in `types.ts`);
   - sari peste sportivii cu `dataStartCurenta` null/undefined (nu au mecanismul de luni lipsa activat — nimic de resetat);
   - `const decizie = decideGratieReinnoire(ultimaLunaPerSportiv.get(sportiv.id) ?? null, today, pragGratie);`
   - daca `decizie === 'reseteaza'` SI `dataStartCurenta < primaZiLunaCurenta(today)` (comparatie de string-uri ISO, sigura pentru formatul `YYYY-MM-DD`), adauga `sportiv.id` intr-un array `idsDeResetat` si marcheaza-l intr-un `Set<string> sportiviCuGratie`.
4. Daca `idsDeResetat.length > 0`, executa o singura actualizare: `await supabase.from('sportivi').update({ data_start_facturare: primaZiLunaCurenta(today) } as any).in('id', idsDeResetat);` — trateaza eroarea prin `throw` catre `catch`-ul existent al functiei (comportament identic cu celelalte erori din functie).
5. Dupa un update reusit, invalideaza cache-urile: `queryClient.invalidateQueries({ queryKey: ['data-start-facturare'] });` si `queryClient.invalidateQueries({ queryKey: ['data-start-facturare-all'] });` — fara asta, raportul Luni Lipsa continua sa arate date vechi pana la expirarea `staleTime` de 5 minute.
6. In bucla pe sportivi individuali, pentru fiecare sportiv din `sportiviCuGratie`, adauga in `observatii` textul: ` Perioadă de grație aplicată (pauză peste ${pragGratie} zile) — lunile anterioare nu mai sunt datorate.`
7. Include numarul de sportivi resetati in mesajul de succes existent afisat la finalul generarii (nu crea un toast nou).

Nu modifica bucla pe familii pentru gratie: `data_start_facturare` e per sportiv, iar resetarea de la pasul 4 acopera si membrii de familie, pentru ca itereaza peste `sportiviActivi` integral.
  </action>
  <acceptance_criteria>
    - `components/Plati/PlatiScadente.tsx` importa `decideGratieReinnoire` si `primaZiLunaCurenta` din `../../utils/perioadaGratie`.
    - Fisierul contine exact un apel `.from('sportivi').update(` cu `data_start_facturare` si `.in('id',` (un singur update batch, nu unul per sportiv intr-o bucla).
    - Fisierul contine ambele invalidari de cache: `['data-start-facturare']` si `['data-start-facturare-all']`.
    - `npm run lint` trece cu 0 erori.
    - Test live pe un sportiv de proba: cu `data_start_facturare` setat la o data veche (ex. `2026-01-01`), fara facturi Abonament din ultimele 4 luni si cu `perioada_gratie_zile = 30`, rularea generarii muta `data_start_facturare` la prima zi a lunii curente; cu aceleasi date dar `perioada_gratie_zile = 365`, valoarea ramane `2026-01-01`. Starea de test se restaureaza dupa verificare.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "npm run lint &amp;&amp; grep -c 'decideGratieReinnoire' components/Plati/PlatiScadente.tsx &amp;&amp; grep -c 'data-start-facturare-all' components/Plati/PlatiScadente.tsx &amp;&amp; grep -c \"\\.in('id'\" components/Plati/PlatiScadente.tsx"</automated>
    <human-check>
      În Plăți → Plăți Scadente, ca ADMIN_CLUB, apasă "Generează Abonamente". Mesajul de succes menționează câți sportivi au primit resetarea de grație. Deschide profilul unui sportiv cu pauză lungă: data de start a facturării este prima zi a lunii curente, iar badge-ul de luni lipsă nu mai arată lunile vechi.
    </human-check>
  </verify>
  <done>Gratia modifica efectiv `data_start_facturare`, intr-un singur update batch, cu invalidare de cache si urma in observatii.</done>
</task>

<task type="auto">
  <name>Task 2: Aplica bonusul de loialitate si inchide Bug 4 TODO</name>
  <read_first>
    - components/Plati/PlatiScadente.tsx (liniile 225-340 — buclele pe familii si pe sportivi, plus blocul de insert)
    - utils/loialitateReinnoiri.ts si services/loialitateService.ts (contractele create in planul 30-04)
    - .planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-04-SUMMARY.md (schema live confirmata: coloanele reale ale `politici_reducere` si `aplicare_reduceri`, si tinta FK a lui `plati.reducere_id`)
    - types.ts (liniile 155-180 — `Plata`, in special nota despre `reducereDetalii` camelCase)
  </read_first>
  <files>components/Plati/PlatiScadente.tsx</files>
  <action>
1. Importuri noi: `import { numaraReinnoiriConsecutive, calculeazaBonusLoialitate } from '../../utils/loialitateReinnoiri';` si `import { getPoliticiLoialitate } from '../../services/loialitateService';`.

2. Dupa refetch-ul de `platiProaspete` (linia ~159), incarca politicile o singura data:
   `const { data: politiciLoialitate, error: errPolitici } = await getPoliticiLoialitate(clubId);`
   Daca `errPolitici` nu e null, NU opri generarea — logheaza in consola si continua cu `[]`. Motivatia (scrie-o ca si comentariu): o problema a modulului de loialitate nu are voie sa blocheze facturarea lunara, care e functia critica a ecranului.
   Declara `const politici = politiciLoialitate ?? [];`

3. In bucla pe familii, exact acolo unde se afla comentariul `Bug 4 TODO` (liniile 239-241), inainte de `let sumaDeFacturat = abonamentConfig.pret;`:
   - `const platiFamilie = (platiProaspete || []).filter(p => p.familie_id === familie.id);`
   - `const reinnoiri = numaraReinnoiriConsecutive(platiFamilie as Plata[], today);`
   - `const bonus = calculeazaBonusLoialitate(politici, reinnoiri, abonamentConfig.pret);`
   - `let sumaDeFacturat = abonamentConfig.pret - bonus.sumaBonus;` (bonusul e deja plafonat in `[0, pret]` de functia pura)
   - daca `bonus.sumaBonus > 0`: adauga la `observatii` textul ` ${bonus.detalii}.`
   - Logica de credit existenta ramane NESCHIMBATA ca ordine si ca forma — se aplica DUPA reducere, pe `sumaDeFacturat` deja redusa.
   - La `platiToInsert.push({...})` adauga campurile `suma_initiala: abonamentConfig.pret` si, cand `bonus.sumaBonus > 0`, `reducereDetalii: bonus.detalii`. NU seta `reducere_id` decat daca 30-04-SUMMARY confirma ca `plati.reducere_id` are FK catre `politici_reducere`; daca FK-ul e catre `reduceri` (cazul asteptat), lasa `reducere_id` neatins — altfel insertul esueaza pe violare de cheie straina.
   - Sterge complet comentariul `Bug 4 TODO` de aici.
4. Repeta acelasi tratament in bucla pe sportivi individuali (comentariul `Bug 4 TODO` de la liniile 270-271), cu `const platiSportivIstoric = (platiProaspete || []).filter(p => p.sportiv_id === sportiv.id);`. Sterge si acest comentariu.
5. Retine reducerile aplicate intr-un `Map` cheie → `{ politicaId, sumaBonus }`, unde cheia e `sportiv:${id}` sau `familie:${id}`, ca sa poti scrie jurnalul dupa insert.
6. Dupa insert-urile existente (`platiSportivNoi` si `platiFamilieNoi`, ambele cu `.select()` care returneaza randurile inserate cu `id`), construieste randurile de jurnal pentru facturile care au primit bonus si insereaza-le intr-un singur apel in `aplicare_reduceri`, folosind EXACT coloanele confirmate in 30-04-SUMMARY (asteptat: `politica_id`, `sportiv_id` sau `familie_id`, `plata_id`, `suma_redusa`). Daca insertul in jurnal esueaza, NU arunca: logheaza si arata un avertisment neblocant prin `showError` cu titlu explicit ("Facturi generate, jurnal reduceri incomplet") — facturile sunt deja emise, iar rollback-ul lor ar fi mai periculos decat un jurnal incomplet.
7. Include in mesajul de succes existent numarul de facturi care au primit bonus de loialitate si suma totala redusa.
  </action>
  <acceptance_criteria>
    - `grep -c "Bug 4 TODO" components/Plati/PlatiScadente.tsx` returneaza 0.
    - Fisierul contine `numaraReinnoiriConsecutive`, `calculeazaBonusLoialitate`, `getPoliticiLoialitate` si `from('aplicare_reduceri')`.
    - Fisierul contine `suma_initiala` in obiectele `platiToInsert.push(...)` din ambele bucle.
    - `npm run lint` trece cu 0 erori.
    - Test live pe date de proba: cu o politica `{activ: true, reinnoiri_necesare: 3, tip_bonus: 'discount', procentaj: 10}` pe clubul de test si un sportiv cu 3 luni consecutive achitate, factura generata are `suma = 0.9 * pret`, `suma_initiala = pret`, `reducereDetalii` nenul si exact un rand corespunzator in `aplicare_reduceri`. Datele de test se sterg dupa verificare.
    - Cu aceeasi politica dar un sportiv cu doar 2 luni consecutive achitate, factura are `suma = pret` si niciun rand in `aplicare_reduceri`.
    - O a doua rulare a generarii pentru aceeasi luna nu creeaza facturi duplicate si nu adauga randuri suplimentare in `aplicare_reduceri` (indexul unic `uq_aplicare_reduceri_plata` nu e violat).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "npm run lint; echo todo=\$(grep -c 'Bug 4 TODO' components/Plati/PlatiScadente.tsx) loial=\$(grep -c 'calculeazaBonusLoialitate' components/Plati/PlatiScadente.tsx) jurnal=\$(grep -c \"from('aplicare_reduceri')\" components/Plati/PlatiScadente.tsx) init=\$(grep -c 'suma_initiala' components/Plati/PlatiScadente.tsx)"</automated>
    <automated>mcp__plugin_supabase_supabase__execute_sql (project_id: wuhidifzsutwgdfkwhmd) cu: SELECT p.id, p.suma, p.suma_initiala, p."reducereDetalii", a.suma_redusa FROM public.plati p LEFT JOIN public.aplicare_reduceri a ON a.plata_id = p.id WHERE p.tip='Abonament' AND p.an = EXTRACT(YEAR FROM CURRENT_DATE)::int AND p.luna = EXTRACT(MONTH FROM CURRENT_DATE)::int AND p.suma_initiala IS NOT NULL AND p.suma_initiala &gt; p.suma ORDER BY p.id LIMIT 20; — asteptat: fiecare rand cu reducere are un rand corespondent in aplicare_reduceri.</automated>
    <human-check>
      În Plăți → Plăți Scadente apasă "Generează Abonamente" pe clubul de test. Mesajul de succes menționează câte facturi au primit bonus de loialitate și suma totală redusă. În lista de facturi, o factură cu reducere afișează suma redusă, iar detaliul reducerii ("Loialitate: ...") este vizibil la expandarea rândului.
    </human-check>
  </verify>
  <done>Reducerea de loialitate schimba efectiv suma facturata, e trasabila in `plati` si `aplicare_reduceri`, si ambele comentarii `Bug 4 TODO` au disparut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (ADMIN_CLUB) → `plati` INSERT / `sportivi` UPDATE | Generarea facturilor ruleaza integral client-side; sumele si `data_start_facturare` sunt calculate in browser |
| configurari (`cluburi.perioada_gratie_zile`, `politici_reducere`) → sume facturate | Doua setari de configurare controleaza direct cati bani se cer |
| `politici_reducere` al unui club → sportivii altui club | Riscul de a aplica politica gresita daca filtrarea pe club lipseste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05-01 | Tampering | Calculul sumei se face client-side, deci o sesiune compromisa poate insera facturi cu suma arbitrara | accept | Risc preexistent al arhitecturii (toata logica de facturare e client-side, 30-RESEARCH.md § Architectural Responsibility Map); RLS pe `plati` limiteaza scrierea la clubul propriu. Faza 30 nu muta facturarea pe server — ar fi o rescriere de arhitectura, nu o imbunatatire inspirata din Abonix |
| T-30-05-02 | Elevation of Privilege | Aplicarea unei politici a altui club pe sportivii clubului curent | mitigate | `getPoliticiLoialitate(clubId)` filtreaza `.eq('club_id', clubId)` cu acelasi `clubId` folosit pentru `sportivi` si pentru insert-urile de `plati`; RLS `has_access_to_club` pe `politici_reducere` blocheaza si la nivel de DB |
| T-30-05-03 | Tampering | Bonus aplicat de doua ori la rulari repetate ale generarii | mitigate | Verificarea de duplicate existenta pe `(tip='Abonament', luna, an)` impiedica a doua factura; indexul unic `uq_aplicare_reduceri_plata` (planul 30-04) impiedica un al doilea rand de jurnal pe aceeasi factura; testat explicit in acceptance criteria |
| T-30-05-04 | Repudiation | Sume reduse fara explicatie in evidenta contabila | mitigate | `plati.suma_initiala` pastreaza pretul de lista, `plati.reducereDetalii` pastreaza motivul, `aplicare_reduceri` pastreaza politica si suma; `observatii` primeste si mentiunea de gratie |
| T-30-05-05 | Denial of Service | Update `data_start_facturare` per sportiv intr-o bucla ar produce sute de request-uri | mitigate | Un singur `update(...).in('id', idsDeResetat)`, cerut explicit si verificat prin grep in acceptance criteria |
| T-30-05-06 | Information Disclosure | Refetch-ul `plati`/`tranzactii` deja filtrat pe `club_id` | accept | Comportament existent neschimbat (`.eq('club_id', clubId)` la liniile 154-159) |
| T-30-05-SC | Tampering | npm/pip/cargo installs | accept | Zero pachete noi in aceasta faza |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (planul 30-05)

**Fisiere modificate:**
- `components/Plati/PlatiScadente.tsx` — singurul fisier atins de acest plan

**Simboluri/variabile noi introduse in `handleGenerateSubscriptions`:**
- `pragGratie: number` — pragul de gratie al clubului activ
- `ultimaLunaPerSportiv: Map<string, { luna: number; an: number }>`
- `idsDeResetat: string[]` si `sportiviCuGratie: Set<string>`
- `politici: PoliticaReducere[]` (incarcate prin `getPoliticiLoialitate`)
- `reinnoiri: number` si `bonus: { sumaBonus, politica, detalii }` — per entitate facturata
- harta de reduceri aplicate, pentru jurnalul post-insert

**Efecte noi de scriere in DB (fara coloane noi):**
- `UPDATE public.sportivi SET data_start_facturare = <prima zi a lunii curente> WHERE id IN (...)`
- `INSERT INTO public.aplicare_reduceri (politica_id, sportiv_id|familie_id, plata_id, suma_redusa)`
- `public.plati` primeste, pentru facturile cu bonus, `suma_initiala` si `reducereDetalii` completate

**Eliminari:**
- cele doua comentarii `Bug 4 TODO` (fostele linii ~239-241 si ~270-271)

**Fara pachete npm noi, fara tipuri noi in `types.ts`.**
</artifacts_this_phase_produces>

<verification>
1. `npm run lint` — 0 erori.
2. `grep -c "Bug 4 TODO" components/Plati/PlatiScadente.tsx` → 0.
3. Fisierul importa din `utils/perioadaGratie`, `utils/loialitateReinnoiri` si `services/loialitateService`.
4. Exact un singur `update` batch pe `sportivi` cu `.in('id', ...)` pentru `data_start_facturare`.
5. Live: facturile lunii curente cu `suma_initiala > suma` au un rand corespondent in `aplicare_reduceri`.
6. A doua rulare a generarii pentru aceeasi luna nu creeaza facturi sau randuri de jurnal suplimentare.
</verification>

<success_criteria>
- Gratia si loialitatea produc efecte masurabile pe facturile reale, nu doar configurari salvate (Pitfall 5 evitat).
- Fiecare reducere e explicabila retroactiv din date (suma_initiala, reducereDetalii, aplicare_reduceri).
- Toate comportamentele de siguranta existente ale `handleGenerateSubscriptions` (anti dublu-click, solduri proaspete, anti-duplicat, excludere retrasi) raman intacte.
- TODO-ul "Bug 4" e inchis in cod, nu doar comentat diferit.
</success_criteria>

<output>
Creeaza `.planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-05-SUMMARY.md`, cu rezultatele testelor live pe date de proba (gratie: reset vs pastrare; loialitate: cu si fara prag atins) si cu datele de test folosite/sterse.
</output>
