---
phase: 08-button-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - components/ButtonCatalog.tsx
  - types.ts
  - components/LazyComponents.tsx
  - components/AppRouter.tsx
  - components/NavMenu.tsx
autonomous: true
requirements:
  - BTN-03
  - BTN-04

must_haves:
  truths:
    - "ButtonCatalog.tsx exista ca fisier nou si afiseaza grid variante × size × stari"
    - "'button-catalog' este in type View din types.ts"
    - "ButtonCatalog este lazy-loadat prin LazyComponents.tsx — nu in bundle principal"
    - "AppRouter.tsx are case 'button-catalog' cu renderProtected(..., isFederationAdmin)"
    - "NavMenu.tsx afiseaza link 'Button Catalog' DOAR cand permissions.isFederationAdmin este true"
    - "Un user non-SUPER_ADMIN nu vede si nu poate accesa view-ul button-catalog"
  artifacts:
    - path: "components/ButtonCatalog.tsx"
      provides: "Pagina catalog vizual — grid variante × size × stari + ConfirmButton demo"
      contains: "export const ButtonCatalog"
    - path: "types.ts"
      provides: "View union extinsa cu 'button-catalog'"
      contains: "button-catalog"
    - path: "components/LazyComponents.tsx"
      provides: "Export lazy ButtonCatalog"
      contains: "ButtonCatalog"
    - path: "components/AppRouter.tsx"
      provides: "Case 'button-catalog' cu renderProtected"
      contains: "case 'button-catalog'"
    - path: "components/NavMenu.tsx"
      provides: "NavItem pentru catalog DOAR in blocul isFederationAdmin"
      contains: "button-catalog"
  key_links:
    - from: "components/NavMenu.tsx"
      to: "activeView='button-catalog'"
      via: "onNavigate('button-catalog') in NavItem isFederationAdmin block"
      pattern: "button-catalog"
    - from: "components/AppRouter.tsx"
      to: "Lazy.ButtonCatalog"
      via: "case 'button-catalog': renderProtected"
      pattern: "renderProtected.*ButtonCatalog"
    - from: "components/LazyComponents.tsx"
      to: "components/ButtonCatalog.tsx"
      via: "lazy(() => import('./ButtonCatalog'))"
      pattern: "import\\('./ButtonCatalog'\\)"
---

<objective>
Cream pagina catalog vizual ButtonCatalog si o wiram complet in SPA: tip View adaugat, lazy export, case in AppRouter cu protectie isFederationAdmin, NavItem vizibil numai SUPER_ADMIN_FEDERATIE.

Purpose: Furnizeaza catalogul vizual interactiv al sistemului Button pentru SUPER_ADMIN_FEDERATIE — documentatie vie in-app care demonstreaza toate variantele, dimensiunile si starile (BTN-03, BTN-04).
Output: ButtonCatalog.tsx nou + 4 fisiere existente modificate additiv.
</objective>

<execution_context>
@C:/Users/lungu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lungu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/Users/lungu/portal-phihau/.planning/PROJECT.md
@C:/Users/lungu/portal-phihau/.planning/ROADMAP.md
@C:/Users/lungu/portal-phihau/.planning/phases/08-button-design-system/08-CONTEXT.md
@C:/Users/lungu/portal-phihau/.planning/phases/08-button-design-system/08-RESEARCH.md
@C:/Users/lungu/portal-phihau/.planning/phases/08-button-design-system/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creeaza ButtonCatalog.tsx — grid variante × size × stari + demo ConfirmButton (BTN-03)</name>
  <files>components/ButtonCatalog.tsx</files>

  <read_first>
  Citeste components/ui.tsx liniile 1-15 pentru a confirma exact ce exporta: `Button`, `ConfirmButton`, si alte componente necesare (Card, etc.) — copiaza import-urile corecte.
  Citeste components/LazyComponents.tsx liniile 1-60 pentru a vedea patternul de export si a confirma ca ButtonCatalog nu e deja acolo.
  </read_first>

  <action>
  Creeaza fisierul nou `components/ButtonCatalog.tsx`.

  **Import-uri necesare:**
  ```
  import React, { useState } from 'react';
  import { Button, ConfirmButton } from './ui';
  ```
  Nu importa din exterior — doar din ui.tsx intern. Nu importa iconite din Lucide direct daca nu exista deja in proiect.

  **Structura paginii:**

  Componenta: `export const ButtonCatalog: React.FC = () => { ... }`

  Layout: pagina cu scroll vertical, fundal `bg-slate-900 text-slate-100 p-8 min-h-screen`

  **Sectiune 1 — Header:**
  Titlu `Button Catalog — Design System PhiHau` (h1, text-2xl font-bold)
  Subtitlu: `Toate variantele × dimensiunile × starile disponibile`

  **Sectiune 2 — Tabela variante × size:**

  Afiseaza un grid cu:
  - Coloane (sizes): xs, sm, md, lg
  - Randuri (variants): primary, secondary, danger, success, info, warning

  Pentru fiecare celula: randeaza `<Button variant={v} size={s}>{v} {s}</Button>`

  **Sectiune 3 — Ghost variants:**

  Titlu sectiune: `Ghost Variants`
  Randeaza cate un rand per variant (primary, secondary, danger, success, info, warning):
  `<Button variant={v} ghost>ghost {v}</Button>`
  Adauga versiunile cu pill: `<Button variant={v} ghost pill>ghost pill {v}</Button>`

  **Sectiune 4 — Outline variants:**

  Titlu sectiune: `Outline Variants`
  Aceeasi structura ca ghost dar cu prop `outline` in loc de `ghost`.

  **Sectiune 5 — Cu icoane:**

  Titlu sectiune: `Cu Icoane`
  Cateva exemple reprezentative:
  - `<Button leftIcon={<span>+</span>}>Adauga</Button>`
  - `<Button variant="secondary" rightIcon={<span>→</span>}>Continua</Button>`
  - `<Button variant="danger" leftIcon={<span>×</span>} ghost>Sterge</Button>`

  (Foloseste span cu text simplu ca placeholder de iconita — nu necesita Lucide import suplimentar. Nota: in practica utilizatorii vor trece icon-uri reale Lucide ca `leftIcon`.)

  **Sectiune 6 — Stari speciale:**

  Titlu sectiune: `Stari`
  - `<Button isLoading>Loading...</Button>`
  - `<Button disabled>Disabled</Button>`
  - `<Button isLoading variant="success">Success loading</Button>`
  - `<Button disabled variant="danger">Danger disabled</Button>`

  **Sectiune 7 — Pill:**

  Titlu sectiune: `Pill (rounded-full)`
  - `<Button pill>Primary pill</Button>`
  - `<Button variant="secondary" pill>Secondary pill</Button>`
  - `<Button variant="danger" pill size="sm">Danger sm pill</Button>`

  **Sectiune 8 — ConfirmButton demo interactiv:**

  Titlu sectiune: `ConfirmButton — Confirmare inline`

  State local: `const [lastAction, setLastAction] = useState<string>('—');`

  Afiseaza:
  - `<ConfirmButton onConfirm={() => setLastAction('Sterge confirmat!')}>Sterge</ConfirmButton>`
  - `<ConfirmButton variant="warning" confirmText="Sigur vrei sa resetezi?" confirmLabel="Da, reseteaza" cancelLabel="Renunta" onConfirm={() => setLastAction('Reset confirmat!')}>Reset</ConfirmButton>`
  - Text: `Ultima actiune: {lastAction}`

  **Layout sectiuni:** Fiecare sectiune are un titlu h2 (text-lg font-semibold text-slate-300 mb-3 mt-8) si un div flex flex-wrap gap-3 cu butoanele.

  Intre sectiuni adauga un `<hr className="border-slate-700 my-6" />`.

  Nota: ButtonCatalog este o pagina statica de demo — fara React Query, fara context, fara data fetching. Zero dependinte externe in afara de ui.tsx.
  </action>

  <verify>
  `cd "C:\Users\lungu\portal-phihau" && npx tsc --noEmit 2>&1 | grep ButtonCatalog`

  Rezultat asteptat: nicio eroare care mentioneaza ButtonCatalog.tsx.

  Verificare structura:
  `grep -n "export const ButtonCatalog\|ConfirmButton\|ghost\|outline\|pill\|isLoading" "components/ButtonCatalog.tsx"`
  </verify>

  <acceptance_criteria>
  - `export const ButtonCatalog: React.FC` prezent in fisier
  - Importa Button si ConfirmButton din './ui' (nu import default)
  - Afiseaza butoane cu toate cele 6 variante (primary/secondary/danger/success/info/warning)
  - Afiseaza toate cele 4 dimensiuni (xs/sm/md/lg)
  - Afiseaza butoane cu `ghost` prop
  - Afiseaza butoane cu `outline` prop
  - Afiseaza butoane cu `pill` prop
  - Afiseaza butoane cu `leftIcon` si/sau `rightIcon`
  - Afiseaza butoane cu `isLoading` si `disabled`
  - Afiseaza sectiunea ConfirmButton cu state local lastAction
  - Zero import-uri din librarii externe noi
  - `npx tsc --noEmit` fara erori referitoare la ButtonCatalog.tsx
  </acceptance_criteria>

  <done>components/ButtonCatalog.tsx exista, exporta ButtonCatalog, afiseaza grid complet variante × size × stari + ConfirmButton demo interactiv, zero erori TypeScript.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring complet — types.ts + LazyComponents.tsx + AppRouter.tsx + NavMenu.tsx (BTN-04)</name>
  <files>types.ts, components/LazyComponents.tsx, components/AppRouter.tsx, components/NavMenu.tsx</files>

  <read_first>
  Citeste types.ts linia 517 — View union completa (confirma ca 'button-catalog' nu e deja acolo; ultimul element este 'setup-mfa').
  Citeste components/LazyComponents.tsx liniile 58-60 — ultimele exporturi (AdminSMS e ultimul) pentru a sti unde adaugi.
  Citeste components/AppRouter.tsx liniile 258-262 — case 'setup-mfa' si 'default:' pentru a sti unde inserezi noul case.
  Citeste components/NavMenu.tsx liniile 188-202 — blocul isFederationAdmin existent cu NavItem Admin Dashboard.
  </read_first>

  <action>
  Modifica 4 fisiere in ordine — toate modificarile sunt additive, zero cod existent sters:

  **1. types.ts — adauga 'button-catalog' la View union (linia 517):**

  Gaseste linia care contine `| 'setup-mfa';` (sfarsitul View union).
  Inlocuieste `| 'setup-mfa';` cu `| 'setup-mfa' | 'button-catalog';`

  Aceasta este singura modificare in types.ts.

  **2. components/LazyComponents.tsx — adauga export lazy ButtonCatalog:**

  Adauga la sfarsitul fisierului (dupa linia cu AdminSMS):
  ```
  export const ButtonCatalog = lazy(() =>
    import('./ButtonCatalog').then(m => ({ default: m.ButtonCatalog }))
  );
  ```

  Pattern exact: `.then(m => ({ default: m.ButtonCatalog }))` — consistent cu toate celelalte exporturi named din fisier.

  **3. components/AppRouter.tsx — adauga case 'button-catalog':**

  Gaseste linia `case 'setup-mfa':` (linia 258 aproximativ).
  Insereaza INAINTE de `case 'setup-mfa':` urmatorul bloc:

  ```
  case 'button-catalog':
    return renderProtected(<Lazy.ButtonCatalog />, isFederationAdmin);
  ```

  unde `Lazy` este alias-ul pentru LazyComponents (verifica importul existent la top-ul AppRouter.tsx — probabil `import * as Lazy from './LazyComponents'` sau similar).

  ATENTIE: ButtonCatalog nu primeste niciun prop — componenta e statica. Nu trece `onBack`, `permissions`, sau alt prop.

  **4. components/NavMenu.tsx — adauga NavItem in blocul isFederationAdmin:**

  Gaseste blocul (liniile 192-201):
  ```
  {permissions?.isFederationAdmin && (
    <NavItem
        item={{ label: 'Admin Dashboard', view: 'admin-dashboard', icon: ShieldCheckIcon }}
        isExpanded={isExpanded}
        isActive={'admin-dashboard' === activeView}
        onNavigate={onNavigate}
        activeView={activeView}
        openSubmenu={openSubmenu}
        setOpenSubmenu={setOpenSubmenu}
    />
  )}
  ```

  Inlocuieste cu un fragment care contine AMBELE NavItem-uri:
  ```
  {permissions?.isFederationAdmin && (
    <>
      <NavItem
          item={{ label: 'Admin Dashboard', view: 'admin-dashboard', icon: ShieldCheckIcon }}
          isExpanded={isExpanded}
          isActive={'admin-dashboard' === activeView}
          onNavigate={onNavigate}
          activeView={activeView}
          openSubmenu={openSubmenu}
          setOpenSubmenu={setOpenSubmenu}
      />
      <NavItem
          item={{ label: 'Button Catalog', view: 'button-catalog', icon: ShieldCheckIcon }}
          isExpanded={isExpanded}
          isActive={'button-catalog' === activeView}
          onNavigate={onNavigate}
          activeView={activeView}
          openSubmenu={openSubmenu}
          setOpenSubmenu={setOpenSubmenu}
      />
    </>
  )}
  ```

  Foloseste `ShieldCheckIcon` pentru iconita — deja importat in NavMenu.tsx (linia 5: `import { ShieldCheckIcon, ChevronDownIcon } from './icons';`). Nu adauga import-uri noi in NavMenu.tsx.

  IMPORTANT: Blocul isFederationAdmin devine un Fragment React (`<>...</>`) cu doua NavItem-uri. Aceasta e singura schimbare structurala — restul props-urilor NavItem sunt identice cu Admin Dashboard.

  Nu modifica menuConfig.ts — catalogul NU trebuie sa apara in adminMenu (ar deveni vizibil pentru ADMIN_CLUB). Wiring-ul se face NUMAI prin NavMenu.tsx isFederationAdmin block.
  </action>

  <verify>
  `cd "C:\Users\lungu\portal-phihau" && npx tsc --noEmit 2>&1 | head -30`

  Verificari specifice:
  - `grep -n "button-catalog" types.ts` — prezent pe linia View union
  - `grep -n "ButtonCatalog" components/LazyComponents.tsx` — prezent
  - `grep -n "button-catalog" components/AppRouter.tsx` — prezent in case statement
  - `grep -n "button-catalog" components/NavMenu.tsx` — prezent in isFederationAdmin block
  - `grep -n "isFederationAdmin" components/NavMenu.tsx` — blocul trebuie sa contina AMBELE view-uri (admin-dashboard SI button-catalog)
  </verify>

  <acceptance_criteria>
  - types.ts: `'button-catalog'` prezent in View union, imediat dupa `'setup-mfa'`
  - LazyComponents.tsx: export lazy ButtonCatalog prezent cu pattern `.then(m => ({ default: m.ButtonCatalog }))`
  - AppRouter.tsx: `case 'button-catalog': return renderProtected(<Lazy.ButtonCatalog />, isFederationAdmin);` prezent
  - AppRouter.tsx: ButtonCatalog NU primeste props (componenta statica)
  - NavMenu.tsx: blocul `{permissions?.isFederationAdmin && (...)}` contine Fragment cu doua NavItem-uri
  - NavMenu.tsx: al doilea NavItem are `view: 'button-catalog'` si `icon: ShieldCheckIcon`
  - NavMenu.tsx: nu a fost adaugat niciun import nou
  - menuConfig.ts: NEMODIFICAT (nu contine 'button-catalog')
  - `npx tsc --noEmit` zero erori dupa toate cele 4 modificari
  </acceptance_criteria>

  <done>View 'button-catalog' este complet wired: tip in types.ts, lazy export, case protejat in AppRouter, NavItem vizibil NUMAI pentru SUPER_ADMIN_FEDERATIE in NavMenu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| activeView state → AppRouter | Valoarea 'button-catalog' vine din NavigationContext in-memory — nu din URL, nu din input utilizator |
| isFederationAdmin → renderProtected | Flag calculat de usePermissions din activeRoleContext — trimis ca header la Supabase; RLS e gatekeeper real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Elevation of Privilege | ButtonCatalog view | mitigate | `renderProtected(<Lazy.ButtonCatalog />, isFederationAdmin)` — returneaza AccessDenied daca isFederationAdmin=false, consistent cu toate celelalte view-uri protejate |
| T-08-04 | Elevation of Privilege | NavMenu link | accept | Link-ul e in blocul `{permissions?.isFederationAdmin && ...}` — nu se randeaza in DOM pentru non-admins; RLS nu e relevant (pagina e read-only, fara data fetching) |
| T-08-05 | Information Disclosure | ButtonCatalog content | accept | Pagina afiseaza doar componente UI interne — zero date utilizator, zero informatii sensibile; riscul de divulgare e nul |
| T-08-SC | Tampering | npm/pip install | accept | Aceasta faza nu instaleaza niciun pachet nou — sectiunea nu se aplica |
</threat_model>

<verification>
1. `cd "C:\Users\lungu\portal-phihau" && npx tsc --noEmit` — zero erori in toate cele 5 fisiere modificate
2. `grep -c "button-catalog" types.ts` — 1
3. `grep -c "ButtonCatalog" components/LazyComponents.tsx` — 1
4. `grep -c "button-catalog" components/AppRouter.tsx` — 1
5. `grep -c "button-catalog" components/NavMenu.tsx` — 1
6. `grep -c "button-catalog" components/menuConfig.ts` — 0 (NEMODIFICAT)
7. Dev server: `npm run dev` — fara erori de compilare
8. Verificare checkpoint vizual: login ca SUPER_ADMIN_FEDERATIE → sidebar afiseaza "Button Catalog" → click → se incarca ButtonCatalog cu grid variante
9. Verificare acces restrictionat: login ca ADMIN_CLUB → "Button Catalog" nu apare in sidebar
</verification>

<success_criteria>
- ButtonCatalog.tsx exista si afiseaza catalog complet variante × size × stari
- View 'button-catalog' inregistrat in types.ts, LazyComponents.tsx, AppRouter.tsx
- Vizibil in Sidebar NUMAI pentru SUPER_ADMIN_FEDERATIE (isFederationAdmin=true)
- Non-admin primeste AccessDenied daca incearca sa acceseze prin state manipulation
- `npx tsc --noEmit` zero erori dupa toate modificarile
- Lazy loading confirmat — ButtonCatalog nu e in bundle principal
</success_criteria>

<output>
Creeaza `.planning/phases/08-button-design-system/08-02-SUMMARY.md` cand e gata.

## Artifacts This Phase Produces

| Artifact | Tip | Descriere |
|----------|-----|-----------|
| `components/ButtonCatalog.tsx` | NOU | Pagina catalog vizual — grid variante × size × stari + ConfirmButton demo |
| `types.ts` | MODIFICAT | 'button-catalog' adaugat la View union |
| `components/LazyComponents.tsx` | MODIFICAT | Export lazy ButtonCatalog adaugat |
| `components/AppRouter.tsx` | MODIFICAT | Case 'button-catalog' cu renderProtected(isFederationAdmin) |
| `components/NavMenu.tsx` | MODIFICAT | NavItem 'Button Catalog' in blocul isFederationAdmin |
</output>
