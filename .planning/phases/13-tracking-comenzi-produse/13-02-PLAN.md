---
phase: 13-tracking-comenzi-produse
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - services/comenziService.ts
  - components/Produse/ProdusFormModal.tsx
  - components/SportivDashboard/index.tsx
autonomous: true
requirements: [CMD-02, CMD-09]
user_setup: []
must_haves:
  truths:
    - "services/comenziService.ts exportă fetchCereriClub, fetchCereriSportiv, createCerere și apelează sendNotification către adminii clubului (per CMD-02)"
    - "Sportivul vede secțiunea Comenzile mele în tab Echipamente cu lista cererilor proprii (produs, variantă, stare, dată) (per CMD-02)"
    - "Sportivul poate plasa o cerere de produs din tab Echipamente printr-un form (selectează produs + variantă + cantitate) (per CMD-02)"
    - "La plasarea cererii, adminii clubului primesc notificare in-app (badge) (per CMD-02)"
    - "ProdusFormModal afișează un selector tip_produs (per_sportiv | per_club) salvat pe produs (per CMD-09)"
  artifacts:
    - path: "services/comenziService.ts"
      provides: "CRUD cereri + notificare admin"
      exports: ["fetchCereriClub", "fetchCereriSportiv", "createCerere"]
    - path: "components/SportivDashboard/index.tsx"
      provides: "Secțiune Comenzile mele + form plasare cerere în tab Echipamente"
      contains: "fetchCereriSportiv"
    - path: "components/Produse/ProdusFormModal.tsx"
      provides: "Selector tip_produs în formularul de produs"
      contains: "tip_produs"
  key_links:
    - from: "components/SportivDashboard/index.tsx"
      to: "services/comenziService.ts"
      via: "import createCerere + fetchCereriSportiv"
      pattern: "from.*comenziService"
    - from: "services/comenziService.ts"
      to: "utils/notifications.ts"
      via: "import sendBulkNotifications"
      pattern: "from.*notifications"
---

<objective>
Construiește service-ul de comenzi (comenziService.ts) cu CRUD pentru cereri și notificări către admini, extinde tab-ul Echipamente al sportivului cu secțiunea "Comenzile mele" și un form de plasare cerere, și adaugă selectorul tip_produs în ProdusFormModal.

Purpose: Permite primul flux end-to-end vizibil — sportivul plasează o cerere care apare ca notificare la admin. Service-ul devine fundația pentru planurile de management comenzi (13-03) și fluxuri federație (13-04).
Output: Service comenzi + UI cerere sportiv + câmp tip_produs în catalog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tracking-comenzi-produse/13-RESEARCH.md
@.planning/phases/13-tracking-comenzi-produse/13-PATTERNS.md
@services/produseService.ts
@utils/notifications.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service comenziService.ts — fetch + createCerere + notificare</name>
  <read_first>
    - services/produseService.ts (pattern import supabase, fetch cu nested select, error handling throw new Error cu prefix funcție, createVanzare)
    - utils/notifications.ts (semnătura sendNotification și sendBulkNotifications, câmpurile acceptate)
    - types.ts (CerereProdusBD, CerereProdusFull, ComandaProduseBD, ComandaProduseiFull — create în 13-01)
  </read_first>
  <files>services/comenziService.ts</files>
  <action>
    Creează `services/comenziService.ts`. Import supabase din '../supabaseClient', tipurile din '../types', și sendNotification/sendBulkNotifications din '../utils/notifications' (pattern PATTERNS.md liniile 208-219).

    Funcții (pattern exact PATTERNS.md liniile 223-284, error handling uniform `throw new Error(\`numeFunctie: ${error.message}\`)`):
    - `fetchCereriClub(clubId: string): Promise<CerereProdusFull[]>` — select cu nested: `*, varianta:produse_variante(*, produs:produse(denumire, tip_produs)), sportiv:sportivi(nume_complet, user_id)`, eq club_id, order created_at desc. Mapează sportiv.nume_complet în sportiv_nume dacă e nevoie.
    - `fetchCereriSportiv(sportivId: string): Promise<CerereProdusFull[]>` — select `*, varianta:produse_variante(*, produs:produse(denumire, tip_produs))`, eq sportiv_id, order created_at desc.
    - `createCerere(input: { club_id, sportiv_id, varianta_id, cantitate, observatii?, adminClubUserIds: string[] }): Promise<CerereProdusBD>` — INSERT în cereri_produse cu stare_cerere 'SOLICITATA', apoi sendBulkNotifications către adminClubUserIds (per CMD-02 — admin primește badge). Pitfall 2: filtrează adminClubUserIds pentru a elimina null/undefined înainte de notificare. Title 'Cerere echipament nouă', body cu numele sportivului dacă disponibil, sender_sportiv_id = input.sportiv_id.
    - `fetchAdminClubUserIds(clubId: string): Promise<string[]>` — query pe utilizator_roluri_multicont WHERE club_id = clubId AND rol_denumire = 'ADMIN_CLUB', returnează lista user_id (Open Question 3 din RESEARCH.md — verifică numele exact al tabelei și coloanelor citind un query existent dacă necesar; dacă tabela diferă, ajustează).

    Adaugă și stub-uri pentru funcțiile consumate de 13-03/13-04 ca să existe contractul (semnături exportate, implementare minimă cu throw 'not implemented' DOAR dacă nu pot fi implementate acum — preferabil implementează-le complet în planurile respective; aici NU le include dacă nu sunt necesare pentru 13-02).

    NU adăuga comenzile în DataContext (anti-pattern RESEARCH.md) — datele sunt locale modulului.
  </action>
  <acceptance_criteria>
    - services/comenziService.ts exportă fetchCereriClub, fetchCereriSportiv, createCerere, fetchAdminClubUserIds.
    - createCerere apelează sendBulkNotifications.
    - Toate funcțiile folosesc throw new Error cu prefix funcție (nu return { data, error }).
    - npx tsc --noEmit trece.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export async function fetchCereriClub" services/comenziService.ts && grep -q "export async function createCerere" services/comenziService.ts && grep -q "sendBulkNotifications" services/comenziService.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Service-ul exportă funcțiile de fetch și createCerere; createCerere trimite notificări la admini; tsc trece.</done>
</task>

<task type="auto">
  <name>Task 2: Selector tip_produs în ProdusFormModal</name>
  <read_first>
    - components/Produse/ProdusFormModal.tsx (structura form-ului, state-ul produsului, câmpurile existente, componenta Select din ui.tsx)
    - types.ts (câmpul tip_produs pe interfața produs — adăugat în 13-01)
  </read_first>
  <files>components/Produse/ProdusFormModal.tsx</files>
  <action>
    Adaugă în formularul de produs un selector `tip_produs` (per CMD-09) cu 2 opțiuni: 'per_sportiv' (label "Per sportiv — se distribuie individual") și 'per_club' (label "Per club — rămâne la club"). Folosește componenta Select din components/ui.tsx (pattern existent în acest fișier pentru alte select-uri).

    Adaugă tip_produs în state-ul formularului cu default 'per_sportiv' (preluat din produsul editat dacă există, altfel 'per_sportiv'). Include tip_produs în payload-ul de salvare (INSERT/UPDATE produs) — verifică funcția de salvare din produseService folosită de modal și asigură-te că tip_produs ajunge în coloana DB.

    Plasează selectorul lângă câmpurile existente de categorie/denumire, vizibil pentru rolul care editează produsul (admin club pentru produse club, super admin pentru produse globale — nu adăuga restricție de rol nouă dacă modalul nu are deja una; respectă comportamentul existent).
  </action>
  <acceptance_criteria>
    - ProdusFormModal.tsx conține un Select pentru tip_produs cu opțiunile per_sportiv și per_club.
    - tip_produs este inclus în state-ul formularului și în payload-ul de salvare.
    - npx tsc --noEmit trece.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "tip_produs" components/Produse/ProdusFormModal.tsx && grep -q "per_club" components/Produse/ProdusFormModal.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Formularul de produs are selector tip_produs care salvează valoarea pe produs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Tab Echipamente sportiv — Comenzile mele + form cerere</name>
  <read_first>
    - components/SportivDashboard/index.tsx (liniile 65-101 — activeTab, fetch echipamente existent fetchProduse/fetchVanzariSportiv; liniile 200-300 — render tab echipamente)
    - services/comenziService.ts (fetchCereriSportiv, createCerere, fetchAdminClubUserIds — create în Task 1)
    - components/ui.tsx (Card, Button, Modal, Select, Badge, Input)
  </read_first>
  <files>components/SportivDashboard/index.tsx</files>
  <action>
    Extinde tab-ul Echipamente existent (nu modifica structura de tabs dashboard/echipamente — RESEARCH.md linia 620). Adaugă:

    1. State nou: `cereriMele: CerereProdusFull[]` (useState []), plus state pentru modal/form cerere (cerereModalOpen boolean, selectedVarianta, cantitate).
    2. useEffect lazy de fetch (pattern liniile 89-101): `fetchCereriSportiv(viewedUser.id).then(setCereriMele).catch(() => {})` — silent catch (tab secundar). Refetch după plasarea unei cereri.
    3. Secțiune nouă "Comenzile mele" sub secțiunile existente din tab echipamente: listă carduri cu denumire produs, variantă (culoare/mărime), cantitate, Badge cu stare_cerere, data created_at formatată (date-fns sau toLocaleDateString ro-RO). Empty state dacă lista e goală.
    4. Buton "Solicită produs" care deschide un form simplu (inline sau Modal): selectează un produs din produseCatalog existent → variantă → cantitate. La submit apelează createCerere cu club_id-ul sportivului, sportiv_id = viewedUser.id, varianta_id, cantitate, și adminClubUserIds din fetchAdminClubUserIds(clubId). Afișează toast de succes (react-hot-toast) și refetch cereriMele.

    Guard (Pitfall din RESEARCH.md): plasare cerere doar dacă isViewingOwnProfile (sportivul cere pentru sine din dashboard propriu). Determină clubId din viewedUser (verifică câmpul existent: viewedUser.club_id sau echivalent).
  </action>
  <acceptance_criteria>
    - SportivDashboard/index.tsx importă fetchCereriSportiv și createCerere din comenziService.
    - Există state cereriMele și un useEffect care îl populează din fetchCereriSportiv.
    - Tab-ul echipamente conține secțiunea "Comenzile mele" și un buton de plasare cerere.
    - npx tsc --noEmit trece.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "fetchCereriSportiv" components/SportivDashboard/index.tsx && grep -q "createCerere" components/SportivDashboard/index.tsx && grep -q "cereriMele" components/SportivDashboard/index.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Sportivul vede comenzile proprii și poate plasa o cerere nouă care notifică adminii; tsc trece.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sportiv→Supabase | Sportivul inserează cereri pentru sine; RLS validează sportiv_id |
| service→notificari | Notificările trebuie să ajungă doar la adminii clubului corect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Spoofing | createCerere cu sportiv_id fals | mitigate | RLS INSERT policy: sportiv_id IN (SELECT id FROM sportivi WHERE user_id = auth.uid()) |
| T-13-06 | Information Disclosure | notificare la admin greșit | mitigate | fetchAdminClubUserIds filtrat pe club_id exact al cererii |
| T-13-07 | Error | sportiv fără user_id primește notificare | accept | guard filtrare null în adminClubUserIds (admini au mereu user_id) |
</threat_model>

<verification>
- Sportivul vede "Comenzile mele" și poate plasa o cerere din tab Echipamente.
- La plasare, adminii clubului primesc notificare badge.
- ProdusFormModal salvează tip_produs.
- npx tsc --noEmit trece.
</verification>

<success_criteria>
- comenziService.ts expune CRUD cereri + notificare.
- Sportivul plasează cereri end-to-end cu notificare la admin (CMD-02).
- tip_produs editabil în catalog (CMD-09).
</success_criteria>

<output>
Create `.planning/phases/13-tracking-comenzi-produse/13-02-SUMMARY.md` when done
</output>
