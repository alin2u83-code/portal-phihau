---
phase: 26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - api/_permisiuniCont.ts
  - api/_permisiuniCont.test.ts
  - api/creare-cont.ts
  - components/AppRouter.tsx
  - .planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-UAT.md
autonomous: true
gap_closure: true
requirements: [D-02, D-04, D-07]
user_setup: []

must_haves:
  truths:
    - "Un apelant care e ADMIN_CLUB în Club A și doar SPORTIV în Club B primește 403 când cere crearea unui cont ADMIN_CLUB în Club B (escaladarea cross-club din CR-01 e închisă)"
    - "Un apelant SUPER_ADMIN_FEDERATIE poate crea în continuare conturi cu orice rol în orice club (fără regresie pe fluxul wizard D-02)"
    - "Un ADMIN_CLUB poate crea în continuare conturi SPORTIV/INSTRUCTOR în clubul propriu (fără regresie pe UserManagement)"
    - "Dacă RPC-ul refactor_create_user_account eșuează după ce user-ul a fost creat în auth.users în aceeași cerere, rândul din auth.users este șters, deci reîncercarea D-07 pentru același email pornește curat și poate reuși"
    - "Rândul auth.users NU este șters când userId provine din ramura 'email deja înregistrat' (nu a fost creat de această cerere)"
    - "components/AppRouter.tsx declară toate hook-urile înainte de orice return condițional — nicio cale de render nu mai poate produce 'Rendered fewer hooks than expected'"
    - "Verificarea umană amânată din 26-01 și 26-02 există persistată și executabilă ca 26-UAT.md, extinsă cu testele pentru CR-01 și CR-02"
  artifacts:
    - path: "api/_permisiuniCont.ts"
      provides: "Gardă de autorizare pură și testabilă pentru creare cont: ROLE_WEIGHTS, greutatePerClub, verificaPermisiuneCreareCont"
      exports: ["ROLE_WEIGHTS", "greutateMaximaGlobala", "greutatePerClub", "verificaPermisiuneCreareCont"]
      min_lines: 55
    - path: "api/_permisiuniCont.test.ts"
      provides: "Test colocat pentru garda de autorizare, inclusiv scenariul de escaladare cross-club din CR-01"
      exports: ["ruleazaTeste"]
      min_lines: 70
    - path: "api/creare-cont.ts"
      provides: "Endpoint care delegă autorizarea către verificaPermisiuneCreareCont și face rollback pe auth.users la eșec de RPC"
      contains: "verificaPermisiuneCreareCont"
    - path: "components/AppRouter.tsx"
      provides: "Hook-uri declarate necondiționat, înaintea return-urilor timpurii"
      contains: "sportivIdPentruRaport"
    - path: ".planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-UAT.md"
      provides: "Checklist consolidat de verificare umană end-of-phase (26-01 + 26-02 + CR-01 + CR-02 + CR-03)"
      contains: "status: testing"
      min_lines: 60
  key_links:
    - from: "api/creare-cont.ts"
      to: "api/_permisiuniCont.ts"
      via: "import verificaPermisiuneCreareCont"
      pattern: "from '\\./_permisiuniCont'"
    - from: "api/creare-cont.ts"
      to: "supabaseAdmin.auth.admin.deleteUser"
      via: "rollback în ramura if (rpcError)"
      pattern: "auth\\.admin\\.deleteUser"
    - from: "api/_permisiuniCont.test.ts"
      to: "api/_permisiuniCont.ts"
      via: "import verificaPermisiuneCreareCont"
      pattern: "from '\\./_permisiuniCont'"
---

<objective>
Închide cele 2 gap-uri blocante din `26-VERIFICATION.md` (CR-01 escaladare de privilegii cross-club, CR-02 lipsa rollback-ului pe `auth.users` care face retry-ul D-07 imposibil), repară violarea Rules-of-Hooks din `components/AppRouter.tsx` (CR-03) și persistă verificarea umană amânată de 26-01 și 26-02 într-un `26-UAT.md` consolidat.

Purpose: Faza 26 e blocată la `gaps_found` (8/14 must-haves). Cele două defecte sunt reale, confirmate prin citire directă a codului, și se anulează reciproc cu propriile lor mitigări declarate: garda anti-escaladare (T-26-01/T-26-02) nu apără de fapt clubul țintă, iar retry-ul construit special pentru D-07 nu poate reuși niciodată în scenariul realist „auth user creat, RPC eșuat". Fără aceste fix-uri, portalul acordă `ADMIN_CLUB` unui apelant care e doar `SPORTIV` în clubul respectiv, iar orice eșec parțial lasă un email otrăvit permanent, recuperabil doar prin intervenție manuală în DB.

Output: `api/_permisiuniCont.ts` + test colocat (gardă pură, testabilă automat), `api/creare-cont.ts` rescris pe garda per-club + rollback `deleteUser`, `components/AppRouter.tsx` cu hook-uri necondiționate, `26-UAT.md` cu 12 teste de verificat de om.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-CONTEXT.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-VERIFICATION.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-REVIEW.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-01-SUMMARY.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Simboluri **noi** create de acest plan (nu există astăzi în cod — nu le trata ca drift):

| Simbol | Fișier | Tip |
|---|---|---|
| `ROLE_WEIGHTS` | `api/_permisiuniCont.ts` (fișier nou) | constantă exportată (mutată din `api/creare-cont.ts`) |
| `GREUTATE_MINIMA_CREARE_CONT` | `api/_permisiuniCont.ts` | constantă exportată (`2`) |
| `GREUTATE_FEDERATIE` | `api/_permisiuniCont.ts` | constantă exportată (`5`) |
| `RolApelant` | `api/_permisiuniCont.ts` | interfață exportată (`{ rol_denumire: string; club_id: string \| null }`) |
| `RezultatPermisiune` | `api/_permisiuniCont.ts` | tip exportat (union discriminat pe `permis`) |
| `greutateMaximaGlobala(callerRoles)` | `api/_permisiuniCont.ts` | funcție exportată |
| `greutatePerClub(callerRoles)` | `api/_permisiuniCont.ts` | funcție exportată (`Map<string, number>`) |
| `verificaPermisiuneCreareCont({ callerRoles, roles, clubTinta })` | `api/_permisiuniCont.ts` | funcție exportată |
| `ruleazaTeste()` | `api/_permisiuniCont.test.ts` (fișier nou) | funcție exportată |
| `userNouCreat` | `api/creare-cont.ts` | variabilă locală nouă (flag de rollback) |
| `26-UAT.md` | `.planning/phases/26-.../` (fișier nou) | artefact de verificare umană |

Simboluri **eliminate** de acest plan (nu le trata ca regresie): `ROLE_WEIGHTS` local din `api/creare-cont.ts`, variabila `callerMaxWeight` și setul `cluburiApelant` din `api/creare-cont.ts` (înlocuite de garda unică `verificaPermisiuneCreareCont`).

## Scope explicit — ce NU se face aici

- **WR-01** (validare server-side a lungimii parolei) — rămâne deschis; ar cere fie import cross-director `api/ -> utils/parola`, fie o nouă constantă duplicată. Se planifică separat.
- **WR-03** (ROLE_WEIGHTS duplicat client/server) — se reduce parțial ca efect secundar (server-side devine un singur modul `api/_permisiuniCont.ts`), dar copia din `hooks/useRoleAssignment.ts` rămâne. NU modifica `hooks/useRoleAssignment.ts` în acest plan.
- **IN-01, IN-02** — rămân deschise.
- Nicio modificare în `26-01-PLAN.md` sau `26-02-PLAN.md`.
- Niciun `npm install` — zero dependențe noi.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gardă de autorizare per club (CR-01) — extrage în api/_permisiuniCont.ts, testează, rewire api/creare-cont.ts</name>
  <files>api/_permisiuniCont.ts, api/_permisiuniCont.test.ts, api/creare-cont.ts</files>
  <read_first>
    - `api/creare-cont.ts` — fișierul integral, în special: `ROLE_WEIGHTS` (liniile 5-13), citirea rolurilor apelantului (54-64), garda de rol minim (66-68), destructurarea `req.body` (70), garda anti-escaladare (72-83) și garda de scoping pe club (85-91). Acestea sunt exact liniile pe care le înlocuiești.
    - `api/_rateLimit.ts` — convenția de helper colocat în `api/` cu prefix `_` (nu e rută Vercel), importat ca `'./_rateLimit'`; noul modul trebuie să respecte exact acest tipar
    - `utils/parola.test.ts` și `utils/luniLipsa.test.ts` — tiparul de test colocat folosit în proiect: funcție locală `assert(condition, message)` care aruncă la fals și loghează `PASS:`, funcție exportată care rulează toate asserțiunile, apel automat la finalul fișierului, rulare cu `node --import tsx <fisier>`. Proiectul NU are vitest/jest.
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-REVIEW.md` secțiunea CR-01 (liniile 38-70) — descrierea exactă a bug-ului și fix-ul propus
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-VERIFICATION.md` gap-ul 1 (liniile 8-15) — formularea `missing` care definește criteriul de închidere
    - `docs/roluri-permisiuni.md` — confirmă ierarhia `SUPER_ADMIN_FEDERATIE > ADMIN_CLUB > INSTRUCTOR > SPORTIV` și că un utilizator poate avea roluri la cluburi diferite
  </read_first>
  <behavior>
Comportamentul funcției `verificaPermisiuneCreareCont({ callerRoles, roles, clubTinta })`, verificat de `api/_permisiuniCont.test.ts`:

    - **Escaladare cross-club blocată (CR-01, cazul central):** `callerRoles = [{ rol_denumire: 'ADMIN_CLUB', club_id: 'club-A' }, { rol_denumire: 'SPORTIV', club_id: 'club-B' }]`, `roles = ['ADMIN_CLUB']`, `clubTinta = 'club-B'` → `{ permis: false, status: 403 }`, mesaj conținând `în acest club`
    - **Același apelant, clubul propriu:** aceleași `callerRoles`, `roles = ['ADMIN_CLUB']`, `clubTinta = 'club-A'` → `{ permis: true }`
    - **Același apelant, rol permis în clubul secundar:** aceleași `callerRoles`, `roles = ['SPORTIV']`, `clubTinta = 'club-B'` → `{ permis: true }` (greutate 1 <= 1)
    - **INSTRUCTOR în clubul propriu:** `[{ 'INSTRUCTOR', 'club-B' }]`, `roles = ['INSTRUCTOR']`, `clubTinta = 'club-B'` → `{ permis: true }`
    - **ADMIN_CLUB creează SPORTIV în clubul propriu (regresie UserManagement):** `[{ 'ADMIN_CLUB', 'club-A' }]`, `roles = ['SPORTIV']`, `clubTinta = 'club-A'` → `{ permis: true }`
    - **SUPER_ADMIN_FEDERATIE fără club_id creează în orice club (regresie wizard D-02):** `[{ 'SUPER_ADMIN_FEDERATIE', null }]`, `roles = ['ADMIN_CLUB']`, `clubTinta = 'club-nou-999'` → `{ permis: true }`
    - **Escaladare verticală în clubul propriu:** `[{ 'ADMIN_CLUB', 'club-A' }]`, `roles = ['SUPER_ADMIN_FEDERATIE']`, `clubTinta = 'club-A'` → `{ permis: false, status: 403 }`
    - **Apelant fără drept de creare:** `[{ 'SPORTIV', 'club-A' }]`, `roles = ['SPORTIV']`, `clubTinta = 'club-A'` → `{ permis: false, status: 403 }`, mesaj `Nu aveți permisiunea de a crea conturi.`
    - **Apelant fără niciun rol:** `callerRoles = []` → `{ permis: false, status: 403 }`
    - **Rol necunoscut:** `[{ 'ADMIN_CLUB', 'club-A' }]`, `roles = ['ROL_INVENTAT']` → `{ permis: false, status: 400 }`, mesaj conținând `Rol necunoscut`
    - **Listă de roluri goală/invalidă:** `roles = []` și `roles = undefined` → `{ permis: false, status: 400 }`, mesaj `Lista de roluri este invalidă.`
    - **Club țintă lipsă pentru apelant non-federație:** `[{ 'ADMIN_CLUB', 'club-A' }]`, `roles = ['SPORTIV']`, `clubTinta = null` → `{ permis: false, status: 403 }`, mesaj `Nu puteți crea conturi în alt club.`
    - **Club țintă în care apelantul nu are niciun rol:** `[{ 'ADMIN_CLUB', 'club-A' }]`, `roles = ['SPORTIV']`, `clubTinta = 'club-Z'` → `{ permis: false, status: 403 }`, mesaj `Nu puteți crea conturi în alt club.`
  </behavior>
  <action>
**A. Creează `api/_permisiuniCont.ts`** (fișier nou, prefix `_` → Vercel nu îl expune ca rută, exact ca `api/_rateLimit.ts`). Comentariu de antet în română care spune: gardă pură, fără I/O, testabilă independent; închide CR-01 (escaladare cross-club) din `26-REVIEW.md`.

Exportă:

1. `export const ROLE_WEIGHTS: Record<string, number>` cu exact aceleași valori mutate din `api/creare-cont.ts`: `SUPER_ADMIN_FEDERATIE: 5`, `ADMIN: 4`, `ADMIN_CLUB: 3`, `INSTRUCTOR: 2`, `SPORTIV: 1`.
2. `export const GREUTATE_MINIMA_CREARE_CONT = 2;` (INSTRUCTOR+ — pragul stabilit în 26-01, nu-l schimba).
3. `export const GREUTATE_FEDERATIE = 5;` (pragul peste care scoping-ul pe club nu se aplică — echivalentul lui `callerMaxWeight < 5` de azi).
4. `export interface RolApelant { rol_denumire: string; club_id: string | null; }`
5. `export type RezultatPermisiune = { permis: true } | { permis: false; status: 400 | 403; error: string };`
6. `export function greutateMaximaGlobala(callerRoles: RolApelant[]): number` — `Math.max(0, ...callerRoles.map(r => ROLE_WEIGHTS[r.rol_denumire] || 0))`; returnează `0` pe listă goală (atenție: `Math.max(0)` = `0`, corect).
7. `export function greutatePerClub(callerRoles: RolApelant[]): Map<string, number>` — pentru fiecare rând cu `club_id` non-null, păstrează maximul greutății pe acel `club_id`. Rândurile cu `club_id` null (tipic `SUPER_ADMIN_FEDERATIE`) sunt **ignorate** aici — ele sunt tratate de short-circuit-ul de la punctul 8.4.
8. `export function verificaPermisiuneCreareCont(params: { callerRoles: RolApelant[]; roles: unknown; clubTinta: string | null }): RezultatPermisiune` — pașii **în această ordine exactă** (ordinea reproduce mesajele/status-urile de azi, ca fluxurile existente să nu vadă alt cod de eroare):
   1. `const globala = greutateMaximaGlobala(callerRoles);` dacă `globala < GREUTATE_MINIMA_CREARE_CONT` → `{ permis: false, status: 403, error: 'Nu aveți permisiunea de a crea conturi.' }`
   2. dacă `!Array.isArray(roles) || roles.length === 0` → `{ permis: false, status: 400, error: 'Lista de roluri este invalidă.' }`
   3. pentru fiecare `roleName` din `roles`: dacă `!(roleName in ROLE_WEIGHTS)` → `{ permis: false, status: 400, error: \`Rol necunoscut: ${roleName}.\` }`
   4. dacă `globala >= GREUTATE_FEDERATIE` → `{ permis: true }` (federația poate crea în orice club — comportament păstrat din 26-01)
   5. `const gClub = clubTinta ? (greutatePerClub(callerRoles).get(clubTinta) ?? 0) : 0;` dacă `!clubTinta || gClub === 0` → `{ permis: false, status: 403, error: 'Nu puteți crea conturi în alt club.' }`
   6. pentru fiecare `roleName`: dacă `ROLE_WEIGHTS[roleName] > gClub` → `{ permis: false, status: 403, error: 'Nu puteți acorda un rol cu privilegii mai mari decât rolul dumneavoastră în acest club.' }`
   7. `{ permis: true }`

**Esența fix-ului CR-01:** greutatea comparată la pasul 6 este `gClub` (greutatea apelantului **în clubul țintă**), nu maximul global. Nu introduce nicio variabilă `callerMaxWeight` folosită pentru comparația de la pasul 6.

**B. Creează `api/_permisiuniCont.test.ts`** (fișier nou; prefixul `_` îl scoate din rutele Vercel). Copiază structura din `utils/parola.test.ts`: comentariu de antet care documentează rularea `node --import tsx api/_permisiuniCont.test.ts`, funcție locală `assert(condition, message)`, funcție exportată `ruleazaTeste()` care rulează toate cazurile din blocul `<behavior>` de mai sus, afișează la final `Rezultat: N PASS, M FAIL` și apel automat la `ruleazaTeste()` pe ultima linie a fișierului.

Cazul „Escaladare cross-club blocată" trebuie să conțină literal cuvântul `escaladare` în mesajul de assert, ca să fie grep-abil. Pentru cazurile care așteaptă `permis: false`, verifică ȘI `status`, ȘI că `error` conține substringul indicat în `<behavior>`.

**C. Rewire `api/creare-cont.ts`.**

1. Șterge blocul local `ROLE_WEIGHTS` (liniile 5-13) și comentariul lui; adaugă `import { verificaPermisiuneCreareCont } from './_permisiuniCont';` lângă importul existent `./_rateLimit`.
2. Păstrează neschimbate: garda de metodă, rate limit-ul, validarea variabilelor de mediu, crearea `supabaseAdmin`, garda de autentificare Bearer + `auth.getUser` (liniile 15-51) și citirea rolurilor din `utilizator_roluri_multicont` (54-61).
3. Înlocuiește tot blocul de la `const callerRoles = ...` (linia 63) până la finalul gărzii de scoping pe club (linia 91) cu:
   - `const callerRoles = (callerRoleRows || []) as any[];`
   - `const { email, password, userData, roles } = req.body;` (destructurarea se mută mai sus, înaintea gărzii)
   - **(WR-02, bundle trivial adiacent)** gardă de payload imediat după destructurare: dacă `!email || typeof email !== 'string' || !userData?.nume || !userData?.prenume` → `return res.status(400).json({ error: 'Date lipsă sau invalide pentru crearea contului.' });`
   - garda unică: `const permisiune = verificaPermisiuneCreareCont({ callerRoles, roles, clubTinta: userData?.club_id ?? null });` urmată de `if (!permisiune.permis) { return res.status(permisiune.status).json({ error: permisiune.error }); }`
4. Nu rămâne nicio referință la `callerMaxWeight` sau `cluburiApelant` în fișier.
5. Renumerotează comentariile de gardă coerent (1. rate limit, 2. autentificare, 3. roluri apelant, 4. validare payload, 5. autorizare per club) și actualizează referințele la threat-uri: garda unică acoperă acum T-26-01 **și** T-26-02.
6. NU atinge blocul `try { ... }` (liniile 93-181) în acest task — e scopul Task 2.
  </action>
  <verify>
    <automated>node --import tsx api/_permisiuniCont.test.ts && grep -cE "^export (const ROLE_WEIGHTS|const GREUTATE_MINIMA_CREARE_CONT|const GREUTATE_FEDERATIE|function greutateMaximaGlobala|function greutatePerClub|function verificaPermisiuneCreareCont)" api/_permisiuniCont.ts && grep -v '^\s*//' api/creare-cont.ts | grep -c "verificaPermisiuneCreareCont"</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx api/_permisiuniCont.test.ts` rulează cu exit code 0 și afișează `0 FAIL`
    - `grep -cE "^export (const ROLE_WEIGHTS|const GREUTATE_MINIMA_CREARE_CONT|const GREUTATE_FEDERATIE|function greutateMaximaGlobala|function greutatePerClub|function verificaPermisiuneCreareCont)" api/_permisiuniCont.ts` este `6`
    - `grep -c "escaladare" api/_permisiuniCont.test.ts` este `>= 1` (cazul central CR-01 e testat explicit)
    - `grep -c "club-B" api/_permisiuniCont.test.ts` este `>= 3` (scenariul multi-club e exercitat pe mai multe direcții)
    - `grep -v '^\s*//' api/creare-cont.ts | grep -c "verificaPermisiuneCreareCont"` este `>= 2` (import + apel)
    - `grep -v '^\s*//' api/creare-cont.ts | grep -c "from './_permisiuniCont'"` este `1`
    - `grep -c "callerMaxWeight" api/creare-cont.ts` este `0` (variabila globală care cauza CR-01 nu mai există)
    - `grep -c "cluburiApelant" api/creare-cont.ts` este `0` (setul de cluburi verificat independent nu mai există)
    - `grep -c "const ROLE_WEIGHTS" api/creare-cont.ts` este `0` (mutat în modulul partajat)
    - `grep -v '^\s*//' api/creare-cont.ts | grep -c "Date lipsă sau invalide pentru crearea contului"` este `1` (WR-02 bundled)
    - `grep -c "în acest club" api/_permisiuniCont.ts` este `>= 1` (mesajul 403 distinge explicit clubul țintă)
    - Fișierele noi respectă convenția Vercel: `ls api/_permisiuniCont.ts api/_permisiuniCont.test.ts` reușește și ambele nume încep cu `_`
  </acceptance_criteria>
  <done>Un apelant ADMIN_CLUB în Club A și SPORTIV în Club B primește 403 la cererea de a crea un cont ADMIN_CLUB în Club B, în timp ce SUPER_ADMIN_FEDERATIE și ADMIN_CLUB-ul în clubul propriu funcționează neschimbat — dovedit de suita de teste colocată care rulează cu 0 FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: Rollback auth.users la eșec de RPC (CR-02) + hook-uri necondiționate în AppRouter (CR-03)</name>
  <files>api/creare-cont.ts, components/AppRouter.tsx</files>
  <read_first>
    - `api/creare-cont.ts` — starea după Task 1, în special blocul `try` (fostele linii 93-181): crearea user-ului (`auth.admin.createUser`), ramura `isAlreadyRegistered` care preia `userId` din `sportivi`, apelul RPC `refactor_create_user_account` și `if (rpcError) throw rpcError;`
    - `api/genereaza-magic-link.ts` liniile 66-100 — tiparul de referință care TREBUIE oglindit: `const userId = authData.user.id;` urmat de `if (rpcError) { await supabaseAdmin.auth.admin.deleteUser(userId); throw rpcError; }`
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-REVIEW.md` secțiunea CR-02 (liniile 74-113) și CR-03 (liniile 117-149) — fix-urile exacte propuse
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-VERIFICATION.md` gap-ul 2 (liniile 16-25) — criteriul `missing`: ștergere DOAR când user-ul a fost creat în această cerere
    - `components/CluburiManagement.tsx` liniile 160-234 — `creeazaAdminClub` / `handleRetryAdmin` / `handleCloseModal`: consumatorul care depinde de endpoint pentru ca retry-ul D-07 să poată reuși. NU modifica acest fișier.
    - `components/AppRouter.tsx` liniile 85-115 — cele două `return`-uri timpurii (`@frqkd.ro` la linia 91, `trebuie_schimbata_parola` la linia 95) și cele două `useState` de mutat (liniile 103-104: `sportivIdPentruRaport`, `sportivProfilTab`)
  </read_first>
  <action>
**A. `api/creare-cont.ts` — rollback pe `auth.users` (CR-02).**

1. Lângă `let userId: string;` adaugă `let userNouCreat = false;` — flag care distinge user-ul creat **în această cerere** de cel preluat din ramura „email deja înregistrat".
2. În ramura `else` (cazul în care `auth.admin.createUser` a reușit): păstrează `userId = authData.user.id;` și adaugă imediat `userNouCreat = true;`.
3. În ramura `isAlreadyRegistered` (unde `userId = existingSportiv.user_id;`) **nu** seta flag-ul — user-ul aparținea deja sistemului și ștergerea lui ar distruge un cont legitim.
4. Înlocuiește `if (rpcError) throw rpcError;` cu:
   - dacă `rpcError` este setat ȘI `userNouCreat` este `true`: `await supabaseAdmin.auth.admin.deleteUser(userId).catch(() => {});` (înghițim eroarea de cleanup ca să nu mascheze cauza reală), apoi `throw rpcError;`
   - dacă `rpcError` este setat și `userNouCreat` este `false`: doar `throw rpcError;`
5. Adaugă deasupra un comentariu în română care explică de ce: fără cleanup, rândul orfan din `auth.users` (fără rând corespunzător în `sportivi`) face ca orice reîncercare pentru același email să cadă pe ramura `isAlreadyRegistered` → căutare în `sportivi` → eroarea „există în autentificare dar nu are un profil sportiv asociat", ceea ce face butonul „Reîncearcă Crearea Contului Admin" (D-07) permanent inutilizabil. Referință: tiparul identic din `api/genereaza-magic-link.ts`.
6. NU adăuga `deleteUser` pe alte căi de eroare (`sportivFetchError`, `forceParolaError`, blocul `catch` general) — acolo RPC-ul a reușit deja și rândul din `sportivi` există; ștergerea ar produce exact starea inconsistentă pe care o evităm.
7. NU modifica garda de autorizare construită în Task 1.

**B. `components/AppRouter.tsx` — hook-uri necondiționate (CR-03).**

Mută cele două declarații `useState` de la liniile 103-104 (`const [sportivIdPentruRaport, setSportivIdPentruRaport] = useState<string | null>(null);` și `const [sportivProfilTab, setSportivProfilTab] = useState<'profil' | 'contact' | 'grade' | 'financiar' | 'familie' | 'grupe-istoric' | undefined>(undefined);`) **deasupra** primului `return` timpuriu — concret, imediat după `handleIncasareProcesata` (linia 89) și înaintea lui `if (currentUser && currentUser.email?.endsWith('@frqkd.ro'))` (linia 91).

Păstrează neschimbate: tipurile generice, valorile inițiale, numele setterelor, ordinea relativă a celor două `useState` între ele, cele două `return`-uri timpurii și `renderProtected` (nu e hook, poate rămâne unde e). Zero schimbare de comportament — doar poziția declarațiilor.

Adaugă un comentariu scurt în română deasupra celor două `useState`: hook-urile trebuie declarate necondiționat, înaintea oricărui `return` timpuriu (Rules of Hooks) — altfel o schimbare a `trebuie_schimbata_parola` fără remount produce „Rendered fewer hooks than during the previous render".
  </action>
  <verify>
    <automated>npm run lint && node --import tsx api/_permisiuniCont.test.ts && grep -v '^\s*//' api/creare-cont.ts | grep -c "userNouCreat" && HOOK=$(grep -n "const \[sportivIdPentruRaport" components/AppRouter.tsx | head -1 | cut -d: -f1) && RET=$(grep -n "@frqkd.ro" components/AppRouter.tsx | head -1 | cut -d: -f1) && test "$HOOK" -lt "$RET" && echo "HOOKS-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` (tsc --noEmit) trece fără erori
    - `node --import tsx api/_permisiuniCont.test.ts` continuă să ruleze cu `0 FAIL` (Task 1 nu a regresat)
    - `grep -v '^\s*//' api/creare-cont.ts | grep -c "userNouCreat"` este `>= 3` (declarare + setare în ramura de creare + gardă la rollback)
    - `grep -v '^\s*//' api/creare-cont.ts | grep -c "auth.admin.deleteUser"` este exact `1` (un singur punct de rollback)
    - Rollback-ul este în ramura de eșec RPC: `grep -A 6 "if (rpcError)" api/creare-cont.ts | grep -c "deleteUser"` este `>= 1`
    - Rollback-ul NU se aplică user-ului preexistent: în fișier, `userNouCreat = true` apare exact o dată și **după** `userId = authData.user.id` — verificabil cu `T=$(grep -n "userNouCreat = true" api/creare-cont.ts | wc -l)` egal cu `1`, și `grep -n "userId = existingSportiv.user_id" api/creare-cont.ts` NU e urmat de `userNouCreat = true` în următoarele 2 linii (`grep -A 2 "userId = existingSportiv.user_id" api/creare-cont.ts | grep -c "userNouCreat" ` este `0`)
    - Hook înaintea return-ului timpuriu: `HOOK=$(grep -n "const \[sportivIdPentruRaport" components/AppRouter.tsx | head -1 | cut -d: -f1); RET=$(grep -n "@frqkd.ro" components/AppRouter.tsx | head -1 | cut -d: -f1); test "$HOOK" -lt "$RET"` reușește
    - Al doilea hook e tot deasupra return-ului: `HOOK2=$(grep -n "const \[sportivProfilTab" components/AppRouter.tsx | head -1 | cut -d: -f1); test "$HOOK2" -lt "$RET"` reușește
    - Numărul total de `useState` din `components/AppRouter.tsx` e neschimbat față de HEAD anterior: `grep -c "useState" components/AppRouter.tsx` produce aceeași valoare ca `git show HEAD:components/AppRouter.tsx | grep -c "useState"`
    - `components/CluburiManagement.tsx` NU e modificat: `git diff --name-only` nu conține `components/CluburiManagement.tsx`
  </acceptance_criteria>
  <done>Un eșec al RPC-ului după crearea user-ului șterge rândul din `auth.users`, deci reîncercarea D-07 pentru același email pornește curat; `AppRouter` declară toate hook-urile necondiționat, fără schimbare de comportament vizibil.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidează verificarea umană amânată în 26-UAT.md (26-01 + 26-02 + CR-01 + CR-02 + CR-03)</name>
  <files>.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-UAT.md</files>
  <read_first>
    - `.planning/phases/07-aplicare-filtre-pe-tab-uri/07-UAT.md` — formatul canonic de UAT din acest proiect: frontmatter (`status: testing`, `phase`, `source`, `started`, `updated`), secțiunea `## Current Test` (`number`/`name`/`expected`/`awaiting: user response`) și secțiunea `## Tests` cu `### N. Titlu`, `expected:` pe un rând, `result: [pending]`
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-01-SUMMARY.md` secțiunea „Human Verification Pending (end-of-phase)" (liniile 110-117) — cei 4 pași amânați din 26-01
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-02-PLAN.md` Task 2, blocul `<human-check>` (liniile 232-241) — cei 6 pași amânați din 26-02, cu copy-ul exact al elementelor de UI
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-VERIFICATION.md` secțiunea `human_verification` (liniile 26-38) — cele 4 itemi identificați de verificator ca neexecutați
  </read_first>
  <action>
Creează `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-UAT.md`, respectând **exact** formatul din `07-UAT.md`.

**Frontmatter:**
```
status: testing
phase: 26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a
source: [26-01-SUMMARY.md, 26-02-SUMMARY.md, 26-03-SUMMARY.md]
started: <data curentă ISO 8601 UTC>
updated: <aceeași dată>
```

**`## Current Test`** = testul 1 (număr, nume, `expected` multiline cu `|`, `awaiting: user response`).

**`## Tests`** — exact 12 teste, fiecare cu `### N. Titlu`, un singur rând `expected:` și `result: [pending]`:

1. **Creare cont staff din User Management (26-01)** — User Management → „Adaugă Membru Staff" → completează datele → Salvează. Așteptat: contul e creat, apare `CredentialeContModal`, fără eroare 401.
2. **Creare cont pentru sportiv existent (26-01)** — User Management → un sportiv fără cont → „Creează Cont" → Email + Parolă → „Generează și Asociază". Așteptat: contul e creat fără eroare.
3. **Schimbare obligatorie de parolă la primul login (26-01)** — delogare, apoi login cu contul creat la testul 1. Așteptat: ecranul `MandatoryPasswordChange` apare imediat (dovedește `trebuie_schimbata_parola = true`).
4. **Token de sesiune trimis, parola nu se întoarce (26-01)** — DevTools → Network → request-ul `/api/creare-cont` de la testul 1. Așteptat: header `Authorization: Bearer ...` prezent, iar corpul răspunsului NU conține parola.
5. **Formular unificat club + admin (26-02)** — Gestiune Cluburi → „Adaugă Club". Așteptat: două secțiuni, „Date Club" (icon clădire) și „Date Prim Administrator" (icon user+), textul „Parola va fi generată automat și afișată după creare...", buton „Creează Club și Admin"; fără câmp de parolă și fără selector de rol.
6. **Validare inline blochează scrierea în DB (26-02)** — în formularul de la testul 5, apasă Salvează cu cele trei câmpuri admin goale. Așteptat: erori inline sub fiecare câmp; clubul NU apare în listă.
7. **Un singur submit creează club + cont (26-02)** — completează club + admin (email de test, ex. `test.admin+26@exemplu.ro`) și trimite. Așteptat: clubul apare în tabel ȘI se deschide „Cont creat cu succes" cu email + parolă, fiecare cu buton „Copiază"; parola arată aleatoare, nu derivată din nume.
8. **Secțiunea admin lipsește la editare (26-02)** — „Editează" pe un club existent. Așteptat: secțiunea „Date Prim Administrator" NU apare, butonul zice „Salvează".
9. **Noul admin se poate autentifica în clubul nou (26-02)** — delogare, login cu credențialele de la testul 7. Așteptat: ecran de schimbare obligatorie a parolei; după schimbare, utilizatorul intră în contextul clubului NOU (nu Phi Hau Iași), cu meniu de ADMIN_CLUB.
10. **Retry D-07 reușește după eșec de RPC (CR-02 — testul care a eșuat static)** — pe un mediu de preview Vercel sau `vercel dev` (NU pe producție): redenumește temporar în `api/creare-cont.ts` apelul RPC în `refactor_create_user_account_INEXISTENT`, rulează wizard-ul de la testul 7 cu un email nou. Așteptat: clubul rămâne creat, modalul rămâne deschis cu banner roșu + buton „Reîncearcă Crearea Contului Admin"; în Supabase → Authentication → Users NU există niciun rând pentru emailul folosit (rollback-ul a rulat). Revino la numele corect al RPC-ului, redeploy, apasă „Reîncearcă Crearea Contului Admin". Așteptat: contul se creează cu succes, apare ecranul de credențiale, iar în listă există un singur club cu acel nume.
11. **Escaladare cross-club blocată (CR-01)** — autentifică-te cu un utilizator care are `ADMIN_CLUB` în Club A și orice rol inferior (`SPORTIV`/`INSTRUCTOR`) în Club B; din DevTools Console rulează `fetch('/api/creare-cont', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer <access_token>' }, body: JSON.stringify({ email: 'escalare.test@exemplu.ro', password: 'Parola-Test-123456', userData: { nume: 'Test', prenume: 'Escalare', club_id: '<ID_CLUB_B>' }, roles: ['ADMIN_CLUB'] }) }).then(r => r.json().then(j => console.log(r.status, j)))`. Așteptat: status `403` cu mesajul „Nu puteți acorda un rol cu privilegii mai mari decât rolul dumneavoastră în acest club."; în Supabase → Authentication → Users NU apare `escalare.test@exemplu.ro`. Repetă cu `club_id` = ID Club A. Așteptat: `200`, contul se creează (fluxul legitim nu e blocat).
12. **AppRouter nu crapă după schimbarea parolei (CR-03)** — repetă testul 3 sau 9 și, după schimbarea parolei, navighează prin minimum 3 view-uri diferite din meniu. Așteptat: nicio eroare `Rendered fewer hooks than during the previous render` în consolă, nicio pagină albă.

**Reguli de redactare:** `expected:` pe un singur rând per test (ca în `07-UAT.md`); textele UI în română, exact ca în cod; nu marca niciun test ca trecut — toate rămân `result: [pending]`, ele se completează la rularea UAT-ului de către dezvoltator.

Adaugă la finalul fișierului o secțiune `## Note` care spune: testele 1-9 sunt verificările amânate explicit de `26-01-SUMMARY.md` și `26-02-SUMMARY.md` sub `workflow.human_verify_mode: "end-of-phase"`; testele 10-12 verifică fix-urile aduse de planul 26-03 pentru CR-01, CR-02 și CR-03; testul 10 modifică temporar codul și NU se rulează pe producție.
  </action>
  <verify>
    <automated>UAT=.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-UAT.md; test -f "$UAT" && grep -c "^### " "$UAT" && grep -c "result: \[pending\]" "$UAT" && grep -c "^status: testing" "$UAT"</automated>
  </verify>
  <acceptance_criteria>
    - Fișierul `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-UAT.md` există
    - `grep -c "^status: testing" 26-UAT.md` este `1`
    - `grep -c "^### " 26-UAT.md` este exact `12`
    - `grep -c "result: \[pending\]" 26-UAT.md` este exact `12`
    - `grep -c "awaiting: user response" 26-UAT.md` este `1`
    - `grep -c "26-01-SUMMARY.md" 26-UAT.md` este `>= 1` și `grep -c "26-02-SUMMARY.md" 26-UAT.md` este `>= 1` (trasabilitate către verificările amânate)
    - `grep -c "MandatoryPasswordChange" 26-UAT.md` este `>= 1` (testul 3 din 26-01 e prezent)
    - `grep -c "Creează Club și Admin" 26-UAT.md` este `>= 1` (testul 5 din 26-02 e prezent)
    - `grep -c "Reîncearcă Crearea Contului Admin" 26-UAT.md` este `>= 1` (testul CR-02 e prezent)
    - `grep -c "privilegii mai mari decât rolul dumneavoastră în acest club" 26-UAT.md` este `>= 1` (testul CR-01 verifică mesajul exact produs de Task 1)
    - `grep -c "Rendered fewer hooks" 26-UAT.md` este `>= 1` (testul CR-03 e prezent)
    - `grep -c "NU se rulează pe producție" 26-UAT.md` este `>= 1` (avertismentul pentru testul 10)
  </acceptance_criteria>
  <done>Verificarea umană amânată de 26-01 și 26-02 nu mai există doar ca text în SUMMARY-uri, ci ca un `26-UAT.md` executabil cu 12 teste `[pending]`, extins cu probele pentru CR-01, CR-02 și CR-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orice client autentificat -> `POST /api/creare-cont` | Emitere de identitate privilegiată cu `SUPABASE_SERVICE_ROLE_KEY`; singura apărare este garda din handler, RLS nu se aplică pe service role |
| `api/creare-cont.ts` -> `auth.users` + `sportivi` (două scrieri, fără tranzacție) | Eșec parțial lasă stare inconsistentă între cele două sisteme |
| `components/AppRouter.tsx` -> arborele de render al aplicației | O violare Rules-of-Hooks scoate întreaga aplicație din funcțiune (disponibilitate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Elevation of Privilege | `api/creare-cont.ts` — acordarea unui rol mai privilegiat decât al apelantului | mitigate (**re-mitigat în acest plan** — mitigarea din 26-01 era ineficace, vezi CR-01) | Task 1: comparația de greutăți se face acum împotriva `gClub` = greutatea apelantului **în clubul țintă**, nu împotriva maximului global; probă automată: `api/_permisiuniCont.test.ts` cazurile „Escaladare verticală în clubul propriu" și „Apelant fără drept de creare", plus criteriul `grep -c "callerMaxWeight" api/creare-cont.ts` = 0 |
| T-26-02 | Elevation of Privilege | `api/creare-cont.ts` — creare de cont într-un club în care apelantul nu are autoritate | mitigate (**re-mitigat în acest plan** — verificarea de apartenență rula independent de greutate) | Task 1: garda unică `verificaPermisiuneCreareCont` compune apartenența și greutatea în același pas (pașii 5-6); probă automată: cazul „Escaladare cross-club blocată" (ADMIN_CLUB club-A + SPORTIV club-B cere ADMIN_CLUB în club-B → 403) și cazul „Club țintă în care apelantul nu are niciun rol" |
| T-26-17 | Denial of Service | `auth.users` orfan după eșec de RPC — emailul devine permanent inutilizabil, retry-ul D-07 nu poate reuși | mitigate | Task 2: `auth.admin.deleteUser(userId)` în ramura `if (rpcError)`, gardat de `userNouCreat` (oglindește `api/genereaza-magic-link.ts:97-100`); probă: `grep -A 6 "if (rpcError)" api/creare-cont.ts \| grep -c "deleteUser"` >= 1 |
| T-26-18 | Tampering | Rollback prea agresiv — ștergerea unui `auth.users` legitim preluat din ramura „email deja înregistrat" | mitigate | Task 2 pașii 2-3 și 6: flag-ul `userNouCreat` se setează exclusiv după `userId = authData.user.id`; criteriu de acceptare explicit că `grep -A 2 "userId = existingSportiv.user_id" \| grep -c "userNouCreat"` = 0; niciun `deleteUser` pe alte căi de eroare |
| T-26-19 | Denial of Service | Violare Rules-of-Hooks în `AppRouter` → crash al întregii aplicații la o tranziție de stare fără remount | mitigate | Task 2 punctul B: cele două `useState` mutate deasupra return-urilor timpurii; probă automată prin comparație de numere de linie (hook < primul `return`) + `npm run lint` |
| T-26-20 | Information Disclosure | Mesajele de eroare ale gărzii dezvăluie topologia rolurilor apelantului | accept | Mesajele rămân generice („Nu puteți crea conturi în alt club.", „...mai mari decât rolul dumneavoastră în acest club.") — nu enumeră cluburi, roluri sau ID-uri; apelantul e deja autentificat și cunoaște propriile roluri |
| T-26-21 | Spoofing | Parolă slabă acceptată de server (WR-01 rămâne deschis) | accept (documentat, out of scope) | Riscul rămâne limitat: doar apelanți INSTRUCTOR+ autentificați ating endpoint-ul, iar `trebuie_schimbata_parola = true` invalidează parola la primul login; se planifică separat, vezi „Scope explicit" |
| T-26-SC | Tampering | Instalări npm/pip/cargo | mitigate | Zero dependențe noi — se folosesc doar `crypto`/`tsx` deja prezente și module interne. Nu se rulează `npm install`; nu e necesară verificare de legitimitate a pachetelor. |
</threat_model>

<verification>
1. `node --import tsx api/_permisiuniCont.test.ts` — 0 FAIL (probă comportamentală pentru CR-01)
2. `npm run lint` — zero erori TypeScript (acoperă `components/AppRouter.tsx`; atenție: `tsconfig.json` NU include `api/`, de aceea garda are test propriu)
3. Asserțiuni de sursă (grep) din `<acceptance_criteria>` fiecărui task
4. `26-UAT.md` există cu 12 teste `[pending]` și acoperă explicit CR-01 (test 11), CR-02 (test 10) și CR-03 (test 12)
5. După execuție: dezvoltatorul rulează UAT-ul; abia atunci truths #6, #10, #12 și #14 din `26-VERIFICATION.md` pot trece de la UNCERTAIN/PARTIAL la VERIFIED
</verification>

<success_criteria>
- Escaladarea cross-club din CR-01 e imposibilă: greutatea comparată e cea a apelantului în clubul țintă, dovedită de test automat cu scenariul exact din `26-VERIFICATION.md`
- Fluxurile legitime nu regresează: SUPER_ADMIN_FEDERATIE în orice club, ADMIN_CLUB/INSTRUCTOR în clubul propriu — toate acoperite de cazuri de test dedicate
- CR-02 închis: eșecul RPC-ului după crearea user-ului șterge rândul din `auth.users`, deci retry-ul D-07 poate efectiv reuși, nu doar „nu duplică"
- Rollback-ul nu atinge niciodată un user preexistent (ramura „email deja înregistrat")
- CR-03 închis: toate hook-urile din `AppRouter` sunt declarate necondiționat, fără schimbare de comportament
- Verificarea umană amânată de 26-01 și 26-02 e persistată și executabilă ca `26-UAT.md`, extinsă cu probele pentru CR-01, CR-02 și CR-03
- `26-01-PLAN.md`, `26-02-PLAN.md` și `components/CluburiManagement.tsx` rămân neatinse
</success_criteria>

<output>
Create `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-03-SUMMARY.md` when done
</output>
