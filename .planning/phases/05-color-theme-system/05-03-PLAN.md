---
phase: 05-color-theme-system
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - components/ThemeEditor.tsx
  - index.tsx
  - components/Sidebar.tsx
  - components/AppLayout.tsx
autonomous: false
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - THEME-04

must_haves:
  truths:
    - "ThemeProvider este în provider stack în index.tsx ca descendent al DataProvider"
    - "ThemeEditor modal se deschide din Sidebar footer prin click pe icon paletă"
    - "ThemeEditor are 3 tab-uri: Teme predefinite (grid 4x2 swatches), Personalizat (4 color pickers), Temele mele (reserved)"
    - "Click pe swatch din grid aplică preview instant pe butonul din modal via setTheme()"
    - "Buton 'Aplică doar mie' apelează saveTheme(theme, 'user'); Buton 'Aplică la tot clubul' vizibil doar ADMIN_CLUB+ și apelează saveTheme(theme, 'club')"
    - "Schimbarea temei se vede vizibil în aplicație fără reload"
  artifacts:
    - path: "components/ThemeEditor.tsx"
      provides: "Modal cu tab-uri teme predefinite, editor custom, save per scope"
      exports: ["ThemeEditor"]
    - path: "index.tsx"
      provides: "ThemeProvider în provider stack după DataProvider"
      contains: "ThemeProvider"
    - path: "components/Sidebar.tsx"
      provides: "Buton paletă în footer care deschide ThemeEditor"
      contains: "PaletteIcon"
    - path: "components/AppLayout.tsx"
      provides: "isThemeEditorOpen state + ThemeEditor render"
      contains: "isThemeEditorOpen"
  key_links:
    - from: "components/ThemeEditor.tsx"
      to: "contexts/ThemeContext.tsx"
      via: "useTheme()"
      pattern: "useTheme"
    - from: "components/Sidebar.tsx"
      to: "components/AppLayout.tsx"
      via: "onOpenThemeEditor prop"
      pattern: "onOpenThemeEditor"
    - from: "index.tsx"
      to: "contexts/ThemeContext.tsx"
      via: "ThemeProvider wraps App"
      pattern: "ThemeProvider"
---

<objective>
ThemeEditor component + wiring complet: provider în stack, buton Sidebar, state în AppLayout.

Purpose: Fără acest plan, sistemul de teme e funcțional tehnic dar inaccesibil utilizatorului. ThemeEditor e interfața prin care userul alege și salvează tema.

Output: components/ThemeEditor.tsx (nou), index.tsx (modificat — ThemeProvider adăugat), components/Sidebar.tsx (modificat — buton paletă + prop onOpenThemeEditor), components/AppLayout.tsx (modificat — isThemeEditorOpen state + render ThemeEditor).
</objective>

<execution_context>
@C:/Users/lungu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lungu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-color-theme-system/05-CONTEXT.md
@.planning/phases/05-color-theme-system/05-RESEARCH.md
@.planning/phases/05-color-theme-system/05-01-SUMMARY.md
@.planning/phases/05-color-theme-system/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ThemeEditor.tsx — modal cu 3 taburi + ThemeProvider în index.tsx</name>
  <files>components/ThemeEditor.tsx, index.tsx</files>

  <read_first>
    - components/ui.tsx (liniile 131-165): structura Modal component — props isOpen/onClose/title, cum funcționează
    - contexts/ThemeContext.tsx (complet — creat în Plan 02): ThemeContextValue interface, useTheme, saveTheme signature
    - lib/themes.ts (complet — creat în Plan 01): ThemeConfig, PREDEFINED_THEMES cu toate câmpurile
    - hooks/usePermissions.ts sau hooks/useDataProvider.ts: cum se obține permissions.isAdminClub
    - index.tsx (complet): ordinea exactă a provider-ilor — unde se inserează ThemeProvider
  </read_first>

  <action>
  Pasul 1 — Creează components/ThemeEditor.tsx:

  Importuri necesare:
  - React, useState din react
  - Modal din ./ui
  - useTheme din ../contexts/ThemeContext
  - useData din ../contexts/DataContext
  - usePermissions din ../hooks/usePermissions (verifică path exact)
  - ThemeConfig din ../types
  - PaletteIcon (opțional — dacă se dorește în header modal)

  Props: `{ isOpen: boolean; onClose: () => void }`

  State intern:
  - activeTab: 'predefined' | 'custom' | 'saved' — inițial 'predefined'
  - selectedTheme: ThemeConfig | null — tema selectată pentru preview (nu salvată încă)
  - customTheme state pentru tab-ul Custom: { name, primary, bg, surface, border } — 4 câmpuri editabile
  - isSaving: boolean — pentru loading state pe butoane Save

  Din contexte:
  - const { currentTheme, setTheme, saveTheme, predefinedThemes } = useTheme()
  - const { activeRoleContext, currentUser } = useData()
  - const permissions = usePermissions(activeRoleContext)

  Structura JSX — returnează Modal cu title="Personalizare Temă":

  Tab bar: 3 butoane inline (Teme Predefinite | Personalizat | Temele Mele), stilizate cu border-bottom activ.

  Tab Teme Predefinite (activeTab === 'predefined'):
  - Grid 4 coloane (grid-cols-2 pe mobile, grid-cols-4 pe md)
  - Mapare peste predefinedThemes — pentru fiecare temă un swatch:
    - Div cu background = theme.primary (inline style), borderat dacă e selected/current
    - Click → setTheme(theme) + setSelectedTheme(theme) (preview instant)
    - Afișează theme.name sub swatch
  - Preview live: afișează un Button primary + Button secondary cu textul "Exemplu" în zona de preview (sub grid)
    - Butoanele vor urmă automat --t-* vars deoarece sunt componentele Button migrate
  - Butoane de acțiune (jos modal):
    - "Aplică doar mie" (variant primary): onClick → await saveTheme(selectedTheme ?? currentTheme, 'user'); onClose()
    - "Aplică la tot clubul" (variant secondary): VIZIBIL DOAR DACĂ permissions.isAdminClub sau permissions.isFederationAdmin; onClick → await saveTheme(selectedTheme ?? currentTheme, 'club'); onClose()
    - Ambele butoane: disabled={isSaving}, isLoading={isSaving}

  Tab Personalizat (activeTab === 'custom'):
  - Input text pentru Nume temă (customTheme.name)
  - 4 color pickers `<input type="color">` pentru: Culoare primară (primary), Fundal (bg), Suprafețe (surface), Borduri (border)
  - La change pe orice color picker: actualizează customTheme state + construiește un ThemeConfig complet cu valorile custom + apelează setTheme() pentru preview live
    - Valorile derivate pentru câmpurile neconfigurate (primaryHover, primaryFg, secondary etc.) = ia din DEFAULT_THEME, înlocuind doar câmpurile editate
  - Buton "Previzualizează" → setTheme(construiteDinCustomTheme)
  - Butoanele de acțiune (aceleași ca la tab 1 dar salvează customTheme construit)

  Tab Temele Mele (activeTab === 'saved'):
  - Placeholder simplu: text "Temele salvate vor apărea aici." + un Button secondary "Închide" → onClose()
  - Notă: funcționalitatea completă (lista cu delete) e de implementat în Sprint 2

  IMPORTANT: Componenta ThemeEditor NU face fetch din DB — citește din useTheme() și useData(). Singurele operații Supabase sunt via saveTheme() din ThemeContext.

  Pasul 2 — Modifică index.tsx:

  Adaugă import: `import { ThemeProvider } from './contexts/ThemeContext';`

  Inserează ThemeProvider DUPĂ NavigationProvider și ÎNAINTE de BrowserRouter:

  ÎNAINTE (liniile 47-50):
  ```
  <NavigationProvider>
    <BrowserRouter future={...}>
      <App />
    </BrowserRouter>
  </NavigationProvider>
  ```

  DUPĂ:
  ```
  <NavigationProvider>
    <ThemeProvider>
      <BrowserRouter future={...}>
        <App />
      </BrowserRouter>
    </ThemeProvider>
  </NavigationProvider>
  ```

  ThemeProvider trebuie să fie descendent al DataProvider (care e la linia 46) și NavigationProvider (pentru că ThemeContext apelează useData() — DataProvider e deja ancestor, NavigationProvider nu e necesar ca ancestor).
  </action>

  <verify>
    <automated>cd /mnt/c/Users/lungu/portal-phihau && npx tsc --noEmit 2>&1 | grep -E "ThemeEditor|ThemeProvider" | head -20</automated>
  </verify>

  <acceptance_criteria>
  - components/ThemeEditor.tsx exportă ThemeEditor ca named export
  - ThemeEditor acceptă props isOpen: boolean și onClose: () => void
  - ThemeEditor importă useTheme din contexts/ThemeContext (nu alt path)
  - index.tsx conține ThemeProvider (grep -c "ThemeProvider" index.tsx >= 2 — import + usage)
  - ThemeProvider în index.tsx este DUPĂ DataProvider (DataProvider e linia anterioară în JSX)
  - tsc --noEmit fără erori în ThemeEditor.tsx și index.tsx
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sidebar footer buton paletă + AppLayout wiring</name>
  <files>components/Sidebar.tsx, components/AppLayout.tsx</files>

  <read_first>
    - components/Sidebar.tsx (complet): SidebarProps interface (liniile 12-27), buildSidebarContent funcție, blocul footer (liniile 175-207) cu structura exactă de HTML
    - components/AppLayout.tsx (complet): AppLayoutProps interface, cum e instanțiat Sidebar (liniile 48-63), useState existent (isMobileSidebarOpen), cum e structurat JSX-ul după Sidebar
  </read_first>

  <action>
  Pasul 1 — Modifică components/Sidebar.tsx:

  1a. Adaugă PaletteIcon la importul din ./icons:
  ÎNAINTE: `import { ArrowRightOnRectangleIcon, ChevronDownIcon, ShieldCheckIcon, UserCircleIcon } from './icons';`
  DUPĂ: `import { ArrowRightOnRectangleIcon, ChevronDownIcon, ShieldCheckIcon, UserCircleIcon, PaletteIcon } from './icons';`

  1b. Adaugă prop `onOpenThemeEditor: () => void` la interfața SidebarProps (după câmpul `isMobileOpen` și `setIsMobileOpen` — la finalul interfeței).

  1c. Adaugă onOpenThemeEditor la destructurarea props în funcția Sidebar (după setIsMobileOpen).

  1d. Adaugă butonul paletă în footer — DEASUPRA blocului `<div className="h-px bg-gradient-to-r...separator...">`. Inserează imediat după `{/* User profile + logout at bottom */}` și ÎNAINTE de `<div className="h-px...">`:

  ```tsx
  {/* ThemeEditor trigger */}
  <div className="px-2 mb-1">
    <button
      onClick={onOpenThemeEditor}
      title="Personalizează tema"
      className={`w-full flex items-center ${effectiveExpanded ? 'gap-2 px-3 py-2' : 'justify-center p-2.5'} rounded-xl text-slate-500 hover:text-sky-400 hover:bg-sky-500/10 transition-all border border-slate-700/40`}
    >
      <PaletteIcon className="h-4 w-4 shrink-0" />
      {effectiveExpanded && <span className="text-xs font-medium">Temă</span>}
    </button>
  </div>
  ```

  Pasul 2 — Modifică components/AppLayout.tsx:

  2a. Adaugă import ThemeEditor:
  `import { ThemeEditor } from './ThemeEditor';`

  2b. Adaugă state isThemeEditorOpen LÂNGĂ celelalte useState-uri (după isMobileSidebarOpen):
  `const [isThemeEditorOpen, setIsThemeEditorOpen] = useState(false);`

  2c. La instanțierea Sidebar (liniile 48-63), adaugă prop:
  `onOpenThemeEditor={() => setIsThemeEditorOpen(true)}`

  2d. Adaugă render ThemeEditor DUPĂ componenta Sidebar (sau în orice loc din return, înainte de closing </div> al wrapper principal):
  ```tsx
  <ThemeEditor
    isOpen={isThemeEditorOpen}
    onClose={() => setIsThemeEditorOpen(false)}
  />
  ```
  </action>

  <verify>
    <automated>cd /mnt/c/Users/lungu/portal-phihau && npx tsc --noEmit 2>&1 | grep -E "Sidebar|AppLayout" | head -20</automated>
  </verify>

  <acceptance_criteria>
  - components/Sidebar.tsx conține PaletteIcon în import și în JSX
  - SidebarProps conține `onOpenThemeEditor: () => void`
  - components/AppLayout.tsx conține isThemeEditorOpen state și ThemeEditor render
  - AppLayout.tsx trece onOpenThemeEditor={() => setIsThemeEditorOpen(true)} ca prop la Sidebar
  - tsc --noEmit fără erori noi în Sidebar.tsx și AppLayout.tsx
  - Verificare vizuală (checkpoint următor): icon paletă vizibil în sidebar footer
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Plan 03 a construit:
  - ThemeEditor modal cu 3 taburi (Teme Predefinite cu grid 4x2 swatches, Personalizat cu 4 color pickers, Temele Mele placeholder)
  - ThemeProvider inserat în stack după DataProvider
  - Buton paletă în Sidebar footer
  - AppLayout gestionează isThemeEditorOpen state

  Sistemul complet de teme este acum live: ThemeContext injectează CSS vars la mount, Button primary/secondary urmează tema, ThemeEditor permite schimbarea temei.
  </what-built>

  <how-to-verify>
  1. Pornește dev server: `npm run dev`

  2. Verificare ThemeContext + CSS vars default:
     - Deschide Chrome DevTools → Console → rulează: `document.documentElement.style.getPropertyValue('--t-primary')`
     - Dacă returnează '' (string gol) verifică computed styles: DevTools → Elements → `<html>` → Computed → caută "--t-primary" — trebuie să fie `#3b82f6` (din index.css fallback)

  3. Verificare Button migrare:
     - Navighează la orice pagină cu butoane (ex: Sportivi)
     - Butoanele primary trebuie să fie albastre (#3b82f6) — dacă sunt albastru-indigo e OK (same culoare), dacă sunt un alt layout e o problemă
     - Hover pe buton: culoarea trebuie să se schimbe (mai închisă: #2563eb)

  4. Verificare buton paletă în Sidebar:
     - Sidebar footer: trebuie să apară un icon paletă mic deasupra separatorului și a profilului user
     - Sidebar collapsed: icon centrat; Sidebar expanded: icon + text "Temă"
     - Click pe icon: ThemeEditor modal se deschide

  5. Verificare ThemeEditor funcționare:
     - Tab "Teme Predefinite": grid cu 8 swatches colorate (QwanKiDo Blue, Midnight Navy, Forest, Crimson, Violet, Amber, Ocean, Graphite)
     - Click pe "Forest" (verde): butoanele din preview și din modal trebuie să devină VERZI instant
     - Click pe "QwanKiDo Blue": revine la albastru
     - Buton "Aplică doar mie": modal se închide, culoare selectată persistă pe pagină

  6. Verificare save (opțional — necesită DB):
     - Aplică tema "Forest" și apasă "Aplică doar mie"
     - Reload pagina (F5): tema Forest trebuie să fie re-aplicată din utilizatori.tema_config

  7. Verificare TypeScript:
     - `npm run lint` sau `npx tsc --noEmit` — 0 erori noi
  </how-to-verify>

  <resume-signal>Scrie "aprobat" sau descrie problemele găsite (cu screenshot dacă e posibil)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThemeEditor UI → saveTheme() | Input vine de la user via color picker sau swatch click; saveTheme validează scope via RLS |
| AppLayout → ThemeEditor | isThemeEditorOpen e state local, nu vine din URL sau surse externe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Elevation of Privilege | "Aplică la tot clubul" buton | mitigate | Butonul este randat NUMAI dacă permissions.isAdminClub sau permissions.isFederationAdmin este true; saveTheme('club') e blocată și la nivel RLS (Plan 01) |
| T-05-09 | Tampering | customTheme color values din input type=color | accept | Browser garantează format hex valid (#rrggbb) pentru input type=color; CSS setProperty cu valori hex nu execută cod |
| T-05-10 | Spoofing | ThemeEditor se deschide din Sidebar footer | accept | ThemeEditor nu are acțiuni privilegiate fără autentificare — useData() necesită sesiune activă |
| T-05-SC | Tampering | npm/pip installs | accept | Faza nu instalează pachete noi |
</threat_model>

<verification>
Verificare completă fază 05:

Automată:
- `npx tsc --noEmit` — 0 erori noi
- `grep -c "ThemeProvider" index.tsx` — >= 2 (import + usage)
- `grep -c "PaletteIcon" components/Sidebar.tsx` — >= 2 (import + usage)
- `grep -c "isThemeEditorOpen" components/AppLayout.tsx` — >= 2 (useState + ThemeEditor prop)
- `grep -c "bg-indigo-600" components/ui.tsx` — 0
- `grep -c "t-primary" components/ui.tsx` — >= 2

Verificare Supabase (SQL):
```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name IN ('cluburi', 'utilizatori')
  AND column_name = 'tema_config';
```
Trebuie să returneze 2 rânduri (cluburi + utilizatori).

Vizuală (checkpoint):
- Icon paletă în sidebar footer ✓
- ThemeEditor se deschide ✓
- Grid 8 swatches ✓
- Click pe swatch schimbă culoarea butoanelor în modal ✓
- "Aplică la tot clubul" vizibil doar pentru ADMIN_CLUB+ ✓
</verification>

<success_criteria>
THEME-01: ThemeContext injectează 12 variabile CSS --t-* în :root la mount, cu prioritatea user > club > default. Verificat în DevTools.
THEME-02: Button primary/secondary se schimbă vizibil la schimbarea temei din ThemeEditor. Verificat manual.
THEME-03: ThemeEditor modal accesibil din Sidebar footer, 3 taburi funcționale, save fără erori în consolă.
THEME-04: cluburi.tema_config și utilizatori.tema_config există în Supabase (confirmat SQL).
</success_criteria>

<output>
Creează `.planning/phases/05-color-theme-system/05-03-SUMMARY.md` când termini.
</output>
