---
phase: 13-tracking-comenzi-produse
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - services/comenziService.ts
  - components/Produse/index.tsx
  - components/Produse/ComenziProduse/index.tsx
  - components/Produse/ComenziProduse/ComandaCard.tsx
  - components/Produse/ComenziProduse/PredareModal.tsx
autonomous: false
requirements: [CMD-01, CMD-03, CMD-06]
user_setup: []
must_haves:
  truths:
    - "ProduseManagement are tab nou Comenzi între Vânzări și Raport care încarcă lazy cererile și comenzile clubului (per CMD-03)"
    - "Adminul vede sumar agregat per produs (cantitate totală) cu detaliu expandabil listând sportivii aferenți și starea individuală (per CMD-03)"
    - "Adminul poate grupa cereri SOLICITATA într-o comandă activă, adăuga cereri noi la comanda activă (dacă stare <= PLASATA) sau marca cereri pentru batch următor (per CMD-03)"
    - "Adminul avansează manual stările cererilor: SOLICITATA→CONFIRMATA→PLASATA→SOSITA→PREDATA, plus ANULATA și PLATITA (per CMD-01)"
    - "La predare se generează automat o Plata (status Neachitat) în portofelul sportivului, se scade stocul variantei pentru produse per_sportiv, și o notificare; sportivii cu plată restantă pot primi notificare reminder dintr-un buton vizibil în UI (per CMD-06)"
    - "La marcarea SOSITA sau CONFIRMATA, sportivul primește notificare in-app (per CMD-06)"
  artifacts:
    - path: "components/Produse/ComenziProduse/index.tsx"
      provides: "Tab management comenzi admin — sumar agregat + listă cereri"
      min_lines: 60
    - path: "components/Produse/ComenziProduse/ComandaCard.tsx"
      provides: "Card comandă cu avansare stări + buton Reminder plată"
      contains: "trimiteReminderPlata"
    - path: "components/Produse/ComenziProduse/PredareModal.tsx"
      provides: "Modal predare → factură automată"
      contains: "marcheazaPredare"
    - path: "services/comenziService.ts"
      provides: "grupeazaInComanda, marcheazaStareCerere, marcheazaPredare, trimiteReminderPlata, fetchComenziClub"
      exports: ["marcheazaPredare", "marcheazaStareCerere", "grupeazaInComanda", "trimiteReminderPlata", "fetchComenziClub"]
  key_links:
    - from: "components/Produse/index.tsx"
      to: "components/Produse/ComenziProduse/index.tsx"
      via: "render tab comenzi"
      pattern: "ComenziProduse"
    - from: "services/comenziService.ts"
      to: "plati"
      via: "INSERT la predare"
      pattern: "from\\('plati'\\)"
    - from: "components/Produse/ComenziProduse/ComandaCard.tsx"
      to: "services/comenziService.ts"
      via: "buton Reminder plată"
      pattern: "trimiteReminderPlata"
---

<objective>
Adaugă tab-ul "Comenzi" în ProduseManagement cu UI complet de management: vizualizare sumar agregat per produs + detaliu expandabil cu sportivii, grupare cereri în comandă, avansare mașină de stări, și predare cu generare automată de factură + scădere stoc + notificare, plus buton reminder plată restantă. Extinde comenziService cu funcțiile de agregare, avansare stări și predare.

Purpose: Acesta este nucleul Fluxului A (sportiv→club→furnizor). Adminul transformă cererile sportivilor în comenzi gestionabile și închide ciclul cu predare + plată automată (integrare Plăți).
Output: Tab Comenzi funcțional + service complet pentru Flux A.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tracking-comenzi-produse/13-RESEARCH.md
@.planning/phases/13-tracking-comenzi-produse/13-PATTERNS.md
@components/Produse/VanzareModal.tsx
@services/produseService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extindere comenziService — agregare, stări, predare</name>
  <read_first>
    - services/comenziService.ts (funcțiile existente din 13-02; pattern import + error handling)
    - services/produseService.ts (createVanzare liniile ~215-273 — pattern INSERT plati cu status Neachitat, tip_plata_id, ȘI scădere stoc per variantă liniile ~260-271)
    - utils/notifications.ts (sendNotification — pentru notificări sportiv)
    - components/Produse/index.tsx (liniile 90-91 — pattern extragere tipPlataEchipamente din tipuriPlati)
  </read_first>
  <files>services/comenziService.ts</files>
  <action>
    Adaugă în comenziService.ts (pattern PATTERNS.md liniile 287-333):

    - `fetchComenziClub(clubId: string): Promise<ComandaProduseiFull[]>` — select comenzi_produse cu nested `cereri:cereri_produse(*, varianta:produse_variante(*, produs:produse(denumire, tip_produs)), sportiv:sportivi(nume_complet, user_id)), iteme:comenzi_produse_iteme(*, varianta:produse_variante(*, produs:produse(denumire)))`, eq club_id, order created_at desc. (Pitfall 6 — un singur query nested, agregare client-side.)
    - `grupeazaInComanda(clubId, cerereIds: string[], tip_comanda?): Promise<ComandaProduseBD>` — găsește comanda activă tip 'club_furnizor' (stare DESCHISA) sau creează una nouă; UPDATE cereri_produse SET comanda_id pentru cerereIds; opțional rebuild comenzi_produse_iteme agregat. Validare: nu adăuga cereri la comandă dacă stare comandă > PLASATA (Recomandare RESEARCH.md linia 324). **GUARD comandă duplicată (RESEARCH.md Open Question 2 — REZOLVAT):** la crearea unei comenzi noi, verifică mai întâi că NU există deja o comandă cu `tip_comanda = X AND stare NOT IN ('FINALIZATA','ANULATA')` pentru `club_id`. Dacă există deja una activă de același tip, refolosește-o (nu crea a doua) în loc să arunci eroare — un club poate avea simultan o singură comandă deschisă per tip (`club_furnizor` și `club_federatie` pot coexista, dar nu două de același tip). Extrage acest guard într-un helper intern `gasesteSauCreeazaComandaActiva(clubId, tip_comanda)` reutilizat și de fluxurile viitoare.
    - `marcheazaBatchUrmatoarea(cerereId, valoare: boolean): Promise<void>` — UPDATE batch_urmatoarea (per CMD-03 — "amână pentru următoarea").
    - `marcheazaStareCerere(cerereId, stareNoua: StareCerereProdusTip, opts?: { sportiv_user_id?: string | null }): Promise<void>` — UPDATE stare_cerere + updated_at. La CONFIRMATA → notifică sportivul "Comanda ta a fost confirmată"; la SOSITA → notifică "Echipamentul a sosit!" (per CMD-06). Guard Pitfall 2: notifică doar dacă sportiv_user_id non-null. Validare ordine stări: nu permite PREDATA dacă stare curentă != SOSITA (T-13 threat — aruncă eroare descriptivă).
    - `marcheazaPredare(cerereId, input: { sportiv_id, club_id, varianta_id, tip_plata_id, suma, cantitate, tip_produs, denumire_varianta, sportiv_user_id }): Promise<void>` — pattern exact PATTERNS.md liniile 289-333 + createVanzare: (1) validează stare curentă = SOSITA → (2) INSERT plati (status 'Neachitat', tip_plata_id, suma, data today, descriere `Echipament: ...`) → (3) **dacă `tip_produs === 'per_sportiv'`: scade stocul variantei (identic cu createVanzare liniile ~260-271 — SELECT stoc_curent pe produse_variante, UPDATE stoc_curent = Math.max(0, stoc - cantitate), updated_at). Pentru `per_club`: NU scade stoc (RESEARCH.md Open Question 1 — REZOLVAT: stocul per_club se gestionează prin IntrareMarfaModal separat).** → (4) UPDATE cerere stare='PREDATA' + plata_id → (5) notifică sportivul (guard user_id). Pitfall 4: dacă tip_plata_id este gol/lipsă, aruncă eroare descriptivă "Tip plată Echipamente lipsește".
    - `marcheazaPlatita(cerereId): Promise<void>` — UPDATE stare_cerere='PLATITA' (per CMD-01 — stare separată).
    - `trimiteReminderPlata(cerereId, sportiv_user_id): Promise<void>` — sendNotification "Plată restantă — echipament" (per CMD-06, guard user_id).

    Error handling uniform throw new Error cu prefix funcție.
  </action>
  <acceptance_criteria>
    - comenziService.ts exportă fetchComenziClub, grupeazaInComanda, marcheazaStareCerere, marcheazaPredare, marcheazaPlatita, marcheazaBatchUrmatoarea, trimiteReminderPlata.
    - grupeazaInComanda nu creează a doua comandă activă de același tip pentru același club (guard duplicat).
    - marcheazaPredare face INSERT în 'plati' cu status 'Neachitat'.
    - marcheazaPredare scade stocul variantei DOAR pentru tip_produs === 'per_sportiv'.
    - marcheazaPredare validează stare SOSITA înainte de a genera factura.
    - marcheazaStareCerere trimite notificare la CONFIRMATA și SOSITA.
    - npx tsc --noEmit trece.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export async function marcheazaPredare" services/comenziService.ts && grep -q "export async function grupeazaInComanda" services/comenziService.ts && grep -q "export async function trimiteReminderPlata" services/comenziService.ts && grep -q "from('plati')" services/comenziService.ts && grep -q "per_sportiv" services/comenziService.ts && grep -q "stoc_curent" services/comenziService.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Service-ul expune agregare cu guard comandă duplicată, avansare stări cu notificări, și predare cu factură automată + scădere stoc per_sportiv; tsc trece.</done>
</task>

<task type="auto">
  <name>Task 2: Tab Comenzi în ProduseManagement + ComandaCard + PredareModal</name>
  <read_first>
    - components/Produse/index.tsx (liniile 24, 52-57, 64, 73, 90-91, 193-209, 458-530 — ActiveTab, TAB_LABELS, useData, activeTab, tipPlataEchipamente, render tab-uri)
    - components/Produse/VanzareModal.tsx (pattern modal cu props + state saving/error + handleConfirm)
    - services/comenziService.ts (toate funcțiile — Task 1)
    - components/ui.tsx (Card, Button, Badge, Modal, Select)
  </read_first>
  <files>components/Produse/index.tsx, components/Produse/ComenziProduse/index.tsx, components/Produse/ComenziProduse/ComandaCard.tsx, components/Produse/ComenziProduse/PredareModal.tsx</files>
  <action>
    1. components/Produse/index.tsx (pattern PATTERNS.md liniile 350-391):
    - Extinde `ActiveTab` (linia 24) cu 'comenzi': `'catalog' | 'intrari' | 'vanzari' | 'comenzi' | 'raport'`.
    - Adaugă în TAB_LABELS (liniile 52-57) `{ id: 'comenzi', label: 'Comenzi' }` între 'vanzari' și 'raport'.
    - Adaugă state lazy: cereri, comenzi, loadingComenzi. useEffect care fetch DOAR când activeTab === 'comenzi' (Pitfall 5 — lazy fetch), apelând Promise.all([fetchCereriClub(clubId), fetchComenziClub(clubId)]) cu cancelled guard.
    - Render `{activeTab === 'comenzi' && <ComenziProduse cereri={cereri} comenzi={comenzi} loading={loadingComenzi} clubId={clubId} permissions={permissions} tipPlataEchipamenteId={tipPlataEchipamente} onRefetch={refetchComenzi} clubNume={...} />}`.

    2. components/Produse/ComenziProduse/index.tsx (analog RaportProduse.tsx structura + reduce pattern PATTERNS.md liniile 413-431):
    - Props: cereri, comenzi, loading, clubId, permissions, tipPlataEchipamenteId, onRefetch, clubNume.
    - Secțiunea "Cereri noi" — cereri cu comanda_id null și stare SOLICITATA: checkbox-uri + butoane "Adaugă la comanda curentă" (grupeazaInComanda) / "Amână pentru următoarea" (marcheazaBatchUrmatoarea) — per CMD-03 (butoane explicite).
    - Secțiunea "Comenzi active" — pentru fiecare comandă, render ComandaCard.
    - Calcul sumar agregat per varianta cu useMemo + reduce (cantitate totală per produs).

    3. components/Produse/ComenziProduse/ComandaCard.tsx:
    - Props: comanda (ComandaProduseiFull), tipPlataEchipamenteId, onRefetch, clubNume.
    - Header card: tip comandă + stare (Badge) + furnizor.
    - Sumar agregat: listă "Denumire variantă ×N" (cantitate totală per produs) — per CMD-03.
    - Detaliu expandabil (useState expanded): la expand, listă sportivi per produs cu stare individuală (Badge) + butoane avansare stare (marcheazaStareCerere) per cerere, buton "Predă" care deschide PredareModal, buton "Anulează" (ANULATA), buton "Marchează plătit" (marcheazaPlatita) pentru cereri PREDATA — per CMD-01 (avansare manuală toate stările).
    - **Buton "Reminder plată" (per CMD-06):** pe fiecare cerere afișează un buton "Reminder plată" VIZIBIL DOAR când `cerere.stare_cerere === 'PREDATA'` (predată dar neachitată — încă nu e PLATITA). Butonul apelează `trimiteReminderPlata(cerere.id, cerere.sportiv?.user_id)`; dezactivat (sau ascuns) dacă `cerere.sportiv?.user_id` lipsește (Pitfall 2). Wrap în try/catch cu toast succes "Reminder trimis" / eroare. NU afișa butonul pentru cereri PLATITA, ANULATA sau stări sub PREDATA.
  </action>
  <acceptance_criteria>
    - components/Produse/index.tsx conține 'comenzi' în ActiveTab și în TAB_LABELS.
    - ComenziProduse/index.tsx, ComandaCard.tsx, PredareModal.tsx există.
    - ComandaCard afișează sumar agregat + detaliu expandabil cu sportivii.
    - ComandaCard conține buton "Reminder plată" care apelează trimiteReminderPlata, condiționat pe stare_cerere === 'PREDATA' și user_id prezent.
    - PredareModal apelează marcheazaPredare.
    - Butoanele "Adaugă la comanda curentă" și "Amână pentru următoarea" există.
    - npx tsc --noEmit trece.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "'comenzi'" components/Produse/index.tsx && grep -q "ComenziProduse" components/Produse/index.tsx && grep -q "marcheazaPredare" components/Produse/ComenziProduse/PredareModal.tsx && grep -q "trimiteReminderPlata" components/Produse/ComenziProduse/ComandaCard.tsx && grep -q "PREDATA" components/Produse/ComenziProduse/ComandaCard.tsx && grep -q "expand" components/Produse/ComenziProduse/ComandaCard.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Tab-ul Comenzi e funcțional: agregare, avansare stări, predare cu factură, buton reminder plată pentru cereri PREDATA neachitate; tsc trece.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificare flux complet Flux A</name>
  <action>Pas de verificare manuală — developerul testează în aplicație fluxul A complet (cerere → grupare → avansare stări → predare → factură → reminder) folosind pașii de mai jos. Nu se modifică cod; se confirmă comportamentul real înainte de a continua la fluxurile federație (13-04).</action>
  <what-built>Tab Comenzi în ProduseManagement: grupare cereri, avansare stări SOLICITATA→PREDATA, predare cu generare automată factură Neachitat + scădere stoc per_sportiv, buton reminder plată, notificări.</what-built>
  <how-to-verify>
    1. Login ca ADMIN_CLUB, deschide Echipamente → tab Comenzi.
    2. Confirmă că apar cererile plasate de sportivi (din 13-02) în "Cereri noi".
    3. Grupează una sau mai multe cereri într-o comandă ("Adaugă la comanda curentă"). Încearcă să creezi o a doua comandă club_furnizor — confirmă că se refolosește comanda activă existentă (guard duplicat).
    4. Avansează starea comenzii/cererii: CONFIRMATA → PLASATA → SOSITA. Verifică că sportivul primește notificări la CONFIRMATA și SOSITA (verifică NotificationBell ca sportiv).
    5. La SOSITA, apasă "Predă" → confirmă PredareModal. Verifică în modulul Plăți că s-a creat o factură Neachitat pentru sportiv cu suma corectă. Pentru un produs per_sportiv, verifică în Catalog că stocul variantei a scăzut.
    6. Pe o cerere PREDATA neachitată, apasă "Reminder plată" → verifică că sportivul primește notificarea "Plată restantă — echipament".
    7. Verifică sumar agregat ("Produs ×N") și detaliul expandabil cu sportivii.
  </how-to-verify>
  <resume-signal>Scrie "ok" dacă fluxul funcționează, sau descrie problemele.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→plati | Predarea generează factură reală în portofelul sportivului — trebuie validată starea |
| admin→cereri cross-club | Adminul nu trebuie să gestioneze cereri ale altui club |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Elevation of Privilege | factură generată fără produs predat real | mitigate | marcheazaPredare validează stare curentă = SOSITA înainte de INSERT plati |
| T-13-09 | Tampering | stare avansată în afara ordinii (PREDATA fără SOSITA) | mitigate | validare în service + enum DB |
| T-13-10 | Tampering | admin modifică comenzi alt club | mitigate | RLS club_id = get_active_club_id() pe cereri_produse și comenzi_produse |
| T-13-11 | Information Disclosure | notificare predare/reminder la sportiv greșit | mitigate | sportiv_user_id derivat din cerere.sportiv, validat de RLS notificari |
| T-13-17 | Tampering | stoc scăzut incorect (per_club tratat ca per_sportiv) | mitigate | scădere stoc condiționată strict pe tip_produs === 'per_sportiv' |
</threat_model>

<verification>
- Flux A complet: cerere → grupare → avansare stări → predare → factură → reminder (Task 3).
- Scădere stoc la predare doar pentru per_sportiv.
- Sumar agregat + detaliu expandabil vizibile.
- Notificări la CONFIRMATA, SOSITA și reminder plată.
- npx tsc --noEmit trece.
</verification>

<success_criteria>
- Tab Comenzi cu agregare și mașină de stări completă (CMD-01, CMD-03).
- Predare generează factură automată + scădere stoc per_sportiv + notificare; reminder plată cablat în UI (CMD-06).
</success_criteria>

<output>
Create `.planning/phases/13-tracking-comenzi-produse/13-03-SUMMARY.md` when done
</output>
