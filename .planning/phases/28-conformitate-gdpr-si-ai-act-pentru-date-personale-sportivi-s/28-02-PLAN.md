---
phase: 28-conformitate-gdpr-si-ai-act-pentru-date-personale-sportivi-s
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/agents/types.ts
  - services/agents/adminAgent.ts
  - services/agents/exameneAgent.ts
  - services/agents/financiarAgent.ts
  - services/agents/generalAgent.ts
  - services/agents/grupeAgent.ts
  - services/agents/legitimatiiAgent.ts
  - services/agents/prezentaAgent.ts
  - services/agents/rapoarteAgent.ts
  - services/agents/sportiviAgent.ts
  - services/claudeService.ts
  - contexts/AIAssistantContext.tsx
  - docs/gdpr/DPIA-AI-ASSISTANT.md
  - docs/gdpr/SUBPROCESATORI.md
autonomous: true
requirements: [REQ-2, REQ-3, REQ-6]
tags: [gdpr, ai-act, dpia, minimizare-date, llm]

must_haves:
  truths:
    - "Numele utilizatorului nu mai paraseste browserul catre furnizorul LLM la niciun mesaj din AI Assistant (REQ-6)"
    - "AI Assistant raspunde in continuare coerent dupa eliminarea userName"
    - "docs/gdpr/DPIA-AI-ASSISTANT.md descrie fluxul REAL (services/agents/* + api/llm-proxy.ts), nu fisierul mort services/claudeService.ts"
    - "docs/gdpr/SUBPROCESATORI.md listeaza Groq ca furnizor de chat efectiv folosit in productie"
  artifacts:
    - path: "docs/gdpr/DPIA-AI-ASSISTANT.md"
      provides: "Evaluare impact GDPR art. 35 + transparenta AI Act pentru modulul AI Assistant (REQ-2)"
      contains: "services/agents/orchestrator.ts"
      min_lines: 70
    - path: "docs/gdpr/SUBPROCESATORI.md"
      provides: "Lista subprocesatori externi cu status DPA si transfer extra-UE (REQ-3)"
      contains: "Groq"
      min_lines: 40
  key_links:
    - from: "contexts/AIAssistantContext.tsx"
      to: "services/agents/orchestrator.ts"
      via: "orchestrate(apiMessages, ctx) fara camp userName"
      pattern: "orchestrate\\("
    - from: "docs/gdpr/DPIA-AI-ASSISTANT.md"
      to: "api/llm-proxy.ts"
      via: "citare explicita a boundary-ului catre furnizorul extern"
      pattern: "llm-proxy"
---

<objective>
Implementeaza minimizarea reala a datelor personale trimise catre furnizorul LLM extern (REQ-6) si redacteaza cele doua documente de conformitate care depind de fluxul AI: DPIA-ul modulului AI Assistant (REQ-2) si lista de subprocesatori (REQ-3).

Purpose: Azi, la FIECARE mesaj din AI Assistant, numele complet al utilizatorului pleaca in system prompt catre un procesator extern (Groq, SUA), fara baza legala documentata. Acesta este singurul flux de date personale catre un tert identificat prin cod in aceasta aplicatie.
Output: `userName` eliminat din intreg lantul agentilor + `docs/gdpr/DPIA-AI-ASSISTANT.md` + `docs/gdpr/SUBPROCESATORI.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-conformitate-gdpr-si-ai-act-pentru-date-personale-sportivi-s/28-SPEC.md
@.planning/phases/28-conformitate-gdpr-si-ai-act-pentru-date-personale-sportivi-s/28-CONTEXT.md
@.planning/phases/28-conformitate-gdpr-si-ai-act-pentru-date-personale-sportivi-s/28-RESEARCH.md
@.planning/phases/28-conformitate-gdpr-si-ai-act-pentru-date-personale-sportivi-s/28-PATTERNS.md
@services/agents/types.ts
@services/agents/sportiviAgent.ts
@contexts/AIAssistantContext.tsx
@api/llm-proxy.ts
@services/ragService.ts
@docs/RAG_IMPLEMENTARE.md
</context>

<gotchas>
1. **`services/claudeService.ts` este cod MORT** (zero call-site-uri, confirmat prin grep in RESEARCH.md Critical Finding #1). Textul literal al REQ-6 din SPEC.md tinteste acest fisier. A repara DOAR acest fisier trece criteriul literal de acceptanta si livreaza ZERO minimizare reala. Acest plan repara si fluxul real (`services/agents/*`) SI fisierul mort (ca sa nu ramana o mostra de pattern gresit care va fi copiata), si documenteaza statutul lui in DPIA.
2. **Furnizorul LLM real este Groq, nu Anthropic.** `api/llm-proxy.ts` are `provider = 'groq'` ca default si `services/agents/orchestrator.ts` apeleaza explicit `/api/llm-proxy?provider=groq`. SPEC.md a fost scris cu presupunerea Anthropic. Documentele nu au voie sa descrie un flux care nu exista.
3. **Ordinea conteaza:** criteriul de acceptanta REQ-2 cere ca DPIA sa listeze campurile trimise DUPA implementarea REQ-6. De aceea Task 1 (codul) precede Task 2 (DPIA) in acelasi plan.
</gotchas>

<tasks>

<task type="auto">
  <name>Task 1: Elimina userName din intreg lantul AI Assistant (REQ-6)</name>
  <files>services/agents/types.ts, services/agents/adminAgent.ts, services/agents/exameneAgent.ts, services/agents/financiarAgent.ts, services/agents/generalAgent.ts, services/agents/grupeAgent.ts, services/agents/legitimatiiAgent.ts, services/agents/prezentaAgent.ts, services/agents/rapoarteAgent.ts, services/agents/sportiviAgent.ts, services/claudeService.ts, contexts/AIAssistantContext.tsx</files>
  <read_first>
    - services/agents/types.ts (interfata AgentContext, liniile 12-17)
    - services/agents/sportiviAgent.ts (forma canonica a buildSystemPrompt, linia 5 — identica in toate cele 9 fisiere)
    - contexts/AIAssistantContext.tsx (liniile 84-100 — apelul orchestrate())
    - services/claudeService.ts (liniile 1-60 — ClaudeRequestContext + buildSystemPrompt, cod mort)
    - .planning/phases/28-conformitate-gdpr-si-ai-act-pentru-date-personale-sportivi-s/28-PATTERNS.md (sectiunea "services/agents/types.ts + all 9 *Agent.ts")
  </read_first>
  <action>
Sterge campul `userName` din intregul lant care construieste promptul de sistem trimis catre furnizorul LLM extern (REQ-6). Exista exact 13 aparitii ale sirului `userName` in `services/` + `contexts/` + `components/` — toate trebuie sa dispara.

1. `services/agents/types.ts`: sterge linia `userName: string;` din interfata `AgentContext` (linia 15). Interfata ramane cu `activeView`, `userRole`, `clubName?`.

2. In fiecare din cele 9 fisiere `services/agents/adminAgent.ts`, `exameneAgent.ts`, `financiarAgent.ts`, `generalAgent.ts`, `grupeAgent.ts`, `legitimatiiAgent.ts`, `prezentaAgent.ts`, `rapoarteAgent.ts`, `sportiviAgent.ts`: linia 5 din template literal-ul `buildSystemPrompt` incepe azi cu `Utilizator: ${ctx.userName} | Rol: ${ctx.userRole}`. Inlocuieste cu `Rol: ${ctx.userRole}` — pastreaza neatins restul liniei, inclusiv sufixul conditional pentru `ctx.clubName`. Nu atinge nicio alta linie din prompturi; nu "imbunatati" formularile.

3. `contexts/AIAssistantContext.tsx` (liniile ~95-98): sterge intreaga proprietate `userName:` din obiectul de context pasat lui `orchestrate()`, inclusiv expresia ternara care concateneaza `currentUser.nume` si `currentUser.prenume` si fallback-ul `'Utilizator'`. Daca `currentUser` ramane nefolosit in acest scope dupa stergere, verifica intai daca mai e folosit pentru `clubName` (linia ~85 il foloseste pentru `currentUser?.club_id`) — daca da, nu sterge importul/variabila.

4. `services/claudeService.ts`: desi e cod mort (zero call-site-uri), sterge si de aici `userName: string;` din `ClaudeRequestContext` (linia 9) si linia `Utilizator: ${ctx.userName}` din `buildSystemPrompt` (linia 56). Motiv dublu: (a) satisface literal criteriul de acceptanta REQ-6 din SPEC.md ("Grep pe services/claudeService.ts nu mai gaseste userName"), (b) elimina o mostra de pattern non-conform care ar fi copiata la un viitor refactor. NU sterge fisierul — decizia de stergere ramane in afara scope-ului acestei faze (RESEARCH.md Open Question 2), iar DPIA-ul il va documenta explicit ca inactiv.

Ruleaza `npm run lint` (tsc --noEmit) dupa modificari — orice referinta ramasa la `ctx.userName` va da eroare de compilare, ceea ce este mecanismul de verificare dorit.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>test "$(grep -rn 'userName' services/ contexts/ components/ | wc -l)" -eq 0</automated>
    <automated>test "$(grep -rn 'Rol: \${ctx.userRole}' services/agents/ | wc -l)" -eq 9</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fara erori.
    - `grep -rn "userName" services/ contexts/ components/` returneaza zero linii.
    - Toate cele 9 fisiere `services/agents/*Agent.ts` contin `Rol: ${ctx.userRole}` la inceputul liniei de context a promptului (9 potriviri).
    - `services/agents/types.ts` `AgentContext` are exact 3 campuri: `activeView`, `userRole`, `clubName?`.
    - `services/claudeService.ts` inca exista ca fisier (nu e sters), dar fara `userName`.
  </acceptance_criteria>
  <done>Zero date de identificare personala (nume utilizator) mai sunt incluse in payload-ul trimis catre `/api/llm-proxy` si mai departe catre furnizorul LLM extern; build-ul TypeScript trece.</done>
</task>

<task type="auto">
  <name>Task 2: Redacteaza docs/gdpr/DPIA-AI-ASSISTANT.md (REQ-2)</name>
  <files>docs/gdpr/DPIA-AI-ASSISTANT.md</files>
  <read_first>
    - services/agents/orchestrator.ts (selectAgent + apelul fetch catre /api/llm-proxy?provider=groq)
    - api/llm-proxy.ts (cei 3 furnizori suportati, default groq, variabilele de mediu per furnizor)
    - services/ragService.ts (confirma ca trimite doar textul interogarii + cauta in knowledge_base, fara date personale)
    - docs/RAG_IMPLEMENTARE.md (arhitectura embeddings Gemini + pgvector)
    - contexts/AIAssistantContext.tsx (starea POST-Task 1 — sursa campurilor efectiv trimise)
    - .planning/phases/28-conformitate-gdpr-si-ai-act-pentru-date-personale-sportivi-s/28-SPEC.md (Requirement 2, criteriu de acceptanta)
  </read_first>
  <action>
Creeaza directorul `docs/gdpr/` si scrie `docs/gdpr/DPIA-AI-ASSISTANT.md` in limba romana, ca evaluare de impact conform art. 35 GDPR plus obligatiile de transparenta din AI Act (Reg. UE 2024/1689).

Structura obligatorie (sectiuni cu titluri H2):
- **Identificare document**: data, versiunea, faza GSD 28, autor, mentiunea ca documentul necesita validare de catre un consilier juridic/DPO inainte de a fi publicat ca autoritate (numirea DPO e explicit out of scope in SPEC.md).
- **Descrierea sistemului**: modulul AI Assistant — widget de ajutor contextual, RAG peste `knowledge_base` (continut static de ajutor al aplicatiei) + chat completion. Clasificare AI Act: sistem cu obligatii de transparenta (utilizatorul trebuie sa stie ca interactioneaza cu un sistem AI), NU sistem cu risc ridicat conform Anexei III.
- **Fluxul de date analizat — fisierele reale**: tabel cu lantul `contexts/AIAssistantContext.tsx` → `services/agents/orchestrator.ts` (`orchestrate()`, `selectAgent()`) → unul din cei 9 agenti `services/agents/*Agent.ts` (`buildSystemPrompt()`) → `api/llm-proxy.ts` → API extern Groq. Separat, lantul RAG: `services/ragService.ts` → `/api/rag-search` → embeddings Google Gemini → cautare pgvector in `knowledge_base`. Citeaza explicit `services/ragService.ts`; criteriul de acceptanta SPEC cere citarea lui.
- **Campuri efectiv trimise catre fiecare API (stare POST-REQ-6)**: tabel cu o linie per camp. Catre Groq via `/api/llm-proxy`: `activeView` (string, nume de ecran, nepersonal), `userRole` (string, rol, nepersonal), `clubName` (string, denumire club — date de organizatie, nu persoana fizica), plus continutul mesajelor scrise de utilizator (poate contine date personale introduse VOLUNTAR de utilizator in text liber — risc rezidual documentat). Catre Google Gemini: doar textul interogarii pentru embedding. Marcheaza explicit ca `userName` a fost ELIMINAT in Faza 28 si indica commit-ul/plan-ul.
- **Cod inactiv**: sectiune scurta care documenteaza ca `services/claudeService.ts` exista in repo dar nu are niciun call-site (verificat prin grep, RESEARCH.md Critical Finding #1) si NU face parte din fluxul live de date; e mentionat aici pentru a evita concluzii gresite la un audit viitor.
- **Riscuri identificate**: tabel cu coloanele Risc / Probabilitate / Impact / Masura aplicata. Minim: (1) transmiterea numelui utilizatorului catre procesator extern — REDUS LA ZERO prin REQ-6; (2) date personale introduse liber de utilizator in textul chatului — risc rezidual, masura: nota de transparenta in UI si politica de retentie; (3) transfer de date in afara UE catre Groq Inc. (SUA) — necesita SCC/DPA, trimitere la `SUBPROCESATORI.md`; (4) lipsa de transparenta AI Act (utilizatorul nu stie ca vorbeste cu un AI) — masura: eticheta vizibila in widget; (5) RLS sparta ar expune date prin alte fluxuri — trimitere la Fazele 15/16/25 deja executate.
- **Concluzie si masuri restante**: lista cu ce ramane de facut dupa aceasta faza (ex. validare juridica, confirmare chei API configurate in Vercel, decizie privind pastrarea istoricului de chat).

Nu inventa afirmatii despre termenii contractuali ai furnizorilor — pentru acestea trimite la `SUBPROCESATORI.md` (Task 3). Nu descrie Anthropic/Claude drept furnizorul de chat activ (Gotcha 2).
  </action>
  <verify>
    <automated>test -f docs/gdpr/DPIA-AI-ASSISTANT.md &amp;&amp; test "$(grep -v '^#' docs/gdpr/DPIA-AI-ASSISTANT.md | grep -c 'services/agents/orchestrator.ts')" -ge 1</automated>
    <automated>test "$(grep -v '^#' docs/gdpr/DPIA-AI-ASSISTANT.md | grep -c 'services/ragService.ts')" -ge 1 &amp;&amp; test "$(grep -v '^#' docs/gdpr/DPIA-AI-ASSISTANT.md | grep -c 'Groq')" -ge 1</automated>
    <automated>test "$(grep -v '^#' docs/gdpr/DPIA-AI-ASSISTANT.md | grep -ci 'TBD\|de stabilit ulterior')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `docs/gdpr/DPIA-AI-ASSISTANT.md` exista si are minim 70 de linii.
    - Contine citari explicite ale fisierelor: `services/agents/orchestrator.ts`, `services/ragService.ts`, `api/llm-proxy.ts`, `contexts/AIAssistantContext.tsx`.
    - Contine sirul `Groq` si NU descrie Anthropic drept furnizorul activ de chat.
    - Sectiunea "Campuri efectiv trimise" listeaza `activeView`, `userRole`, `clubName` si mentioneaza explicit eliminarea `userName`.
    - Contine sectiunea despre `services/claudeService.ts` ca fisier inactiv.
    - Zero aparitii de "TBD" sau "de stabilit ulterior".
  </acceptance_criteria>
  <done>DPIA descrie fluxul real de date post-REQ-6, cu fisierele exacte, riscurile si masurile; niciun camp lasat nedefinit.</done>
</task>

<task type="auto">
  <name>Task 3: Redacteaza docs/gdpr/SUBPROCESATORI.md (REQ-3)</name>
  <files>docs/gdpr/SUBPROCESATORI.md</files>
  <read_first>
    - api/llm-proxy.ts (variabilele de mediu si furnizorii suportati: groq/gemini/claude)
    - .env.example (SMS_PROVIDER cu cele 4 optiuni si valoarea default android_gateway; variabilele Supabase)
    - vercel.json (configuratia de deploy — gazduire)
    - .planning/phases/28-conformitate-gdpr-si-ai-act-pentru-date-personale-sportivi-s/28-RESEARCH.md (Critical Finding #2, Assumptions A2/A3)
    - CLAUDE.md (sectiunea Configuration — lista completa de variabile de mediu)
  </read_first>
  <action>
Scrie `docs/gdpr/SUBPROCESATORI.md` in limba romana — registrul subprocesatorilor externi carora li se transmit date in cadrul portalului.

Tabel principal cu coloanele: **Subprocesator** | **Rol/Serviciu** | **Categorii de date primite** | **Locatie prelucrare** | **Transfer extra-UE (SCC necesar?)** | **Status utilizare in productie** | **Link DPA public / actiune necesara**.

Randuri obligatorii (minim 5, acoperind toti furnizorii identificati in cod, nu doar cei 4 din textul SPEC):
1. **Supabase** — baza de date PostgreSQL, autentificare, storage. Primeste practic toate datele personale ale sportivilor. Variabile: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`. Noteaza ca regiunea proiectului trebuie confirmata de operator (migrarea infrastructurii e explicit out of scope in SPEC.md — se documenteaza unde sunt datele azi, nu se muta).
2. **Groq Inc.** — chat completion pentru AI Assistant. ACESTA este furnizorul activ: `api/llm-proxy.ts` are `provider = 'groq'` ca default si `services/agents/orchestrator.ts` il apeleaza explicit. Variabila: `GROQ_API_KEY`. Primeste: promptul de sistem (`activeView`, `userRole`, `clubName` — post-REQ-6, fara nume) + textul mesajelor scrise de utilizator. SUA → transfer extra-UE, necesita SCC/DPA.
3. **Google (Gemini)** — generare de embeddings pentru RAG. Variabila: `GEMINI_API_KEY`. Primeste: textul interogarii pentru embedding (confirmat prin `services/ragService.ts` ca nu contine date personale ale sportivilor).
4. **Anthropic (Claude)** — cale de cod prezenta in `api/llm-proxy.ts` (`provider=claude`, `CLAUDE_API_KEY`) dar NEAPELATA de interfata live. Marcheaza status: "cod prezent, neinvocat de UI; a se confirma daca `CLAUDE_API_KEY` este setata in mediul Vercel de productie". Nu afirma ca este sau nu configurata — RESEARCH.md Open Question 3 marcheaza acest lucru ca neverificat.
5. **Furnizor SMS** — configurabil prin `SMS_PROVIDER` cu 4 optiuni (`android_gateway` default, `smslink`, `twilio`, `vonage`). Documenteaza explicit ca `android_gateway` este infrastructura auto-gazduita (nu vendor SaaS tert, deci fara DPA separat), dar ca orice club care ruleaza `smslink`/`twilio`/`vonage` are un subprocesator tert real care necesita DPA propriu (RESEARCH.md Assumption A3). Categorii de date: numar de telefon + continutul mesajului.
6. **Vercel** — gazduire aplicatie si functii serverless (`vercel.json`). Primeste: trafic, IP-uri, log-uri de request.

Adauga dupa tabel:
- Sectiune **"Actiuni restante"** cu bife pentru: obtinere/arhivare DPA semnat per furnizor, confirmare regiune Supabase, confirmare chei API active in Vercel, verificarea termenilor Groq privind retentia si folosirea datelor pentru antrenare de modele (RESEARCH.md Assumption A2 marcheaza acest punct ca neverificat).
- Sectiune **"Metodologie"** care explica de unde a fost derivata lista (cod: `api/llm-proxy.ts`, `.env.example`, `vercel.json`) si ca lista trebuie re-verificata la orice integrare noua.

Fiecare rand trebuie sa aiba TOATE coloanele completate — unde informatia nu e verificabila din cod, scrie "de confirmat de operator" cu actiunea concreta, nu "TBD".
  </action>
  <verify>
    <automated>test -f docs/gdpr/SUBPROCESATORI.md &amp;&amp; test "$(grep -v '^#' docs/gdpr/SUBPROCESATORI.md | grep -c 'Groq')" -ge 1</automated>
    <automated>for v in Supabase Gemini Anthropic SMS_PROVIDER Vercel; do test "$(grep -v '^#' docs/gdpr/SUBPROCESATORI.md | grep -c "$v")" -ge 1 || exit 1; done</automated>
    <automated>test "$(grep -v '^#' docs/gdpr/SUBPROCESATORI.md | grep -c 'TBD')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `docs/gdpr/SUBPROCESATORI.md` exista cu minim 40 de linii.
    - Tabelul contine cel putin 6 randuri: Supabase, Groq, Google/Gemini, Anthropic, furnizor SMS, Vercel.
    - Groq este marcat explicit ca furnizorul de chat ACTIV in productie.
    - Anthropic este marcat ca "cod prezent, neinvocat de UI", fara afirmatii despre configurarea cheii.
    - Furnizorul SMS documenteaza `android_gateway` ca auto-gazduit si celelalte 3 optiuni ca subprocesatori terti.
    - Zero celule cu "TBD"; unde lipseste informatia, apare "de confirmat de operator" cu actiune concreta.
  </acceptance_criteria>
  <done>Registrul de subprocesatori reflecta furnizorii reali derivati din cod si configuratie, cu status de transfer extra-UE si actiuni restante explicite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/api/llm-proxy` | Payload construit client-side; tot ce intra aici pleaca mai departe catre un tert |
| `/api/llm-proxy` → Groq API (SUA) | Iesire din perimetrul UE si din controlul operatorului |
| browser → `/api/rag-search` → Google Gemini | Al doilea boundary extern, pentru embeddings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | Information Disclosure | `buildSystemPrompt()` in cei 9 agenti trimite numele complet al utilizatorului catre Groq | mitigate | Task 1: `userName` sters din `AgentContext`, din toate cele 9 template-uri si din `contexts/AIAssistantContext.tsx`; verificat prin `npm run lint` + grep cu zero potriviri |
| T-28-08 | Information Disclosure | Utilizatorul introduce voluntar date personale in textul chatului, care ajung la Groq | accept | Risc rezidual documentat in DPIA; nu poate fi eliminat tehnic fara filtrare de continut (in afara scope-ului SPEC); masura compensatorie: transparenta + politica de retentie |
| T-28-09 | Repudiation | Documentele de conformitate descriu un flux care nu exista (Anthropic in loc de Groq), invalidand auditul | mitigate | Task 2/3 impun citarea fisierelor reale si verificare automata prin grep ca `Groq` apare in ambele documente |
| T-28-10 | Tampering | Un refactor viitor copiaza pattern-ul cu `userName` din `services/claudeService.ts` (cod mort) | mitigate | Task 1 elimina `userName` si din fisierul mort; DPIA il documenteaza explicit ca inactiv |
| T-28-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero pachete instalate in aceasta faza (RESEARCH.md "Package Legitimacy Audit"); fara suprafata de supply chain |
</threat_model>

## Artifacts this phase produces

**Fisiere noi:**
- `docs/gdpr/DPIA-AI-ASSISTANT.md`
- `docs/gdpr/SUBPROCESATORI.md`
- directorul `docs/gdpr/`

**Simboluri modificate:**
- `AgentContext` (`services/agents/types.ts`) — campul `userName: string` ELIMINAT; interfata ramane `{ activeView, userRole, clubName? }`
- `buildSystemPrompt(ctx: AgentContext)` in 9 fisiere (`adminAgent.ts`, `exameneAgent.ts`, `financiarAgent.ts`, `generalAgent.ts`, `grupeAgent.ts`, `legitimatiiAgent.ts`, `prezentaAgent.ts`, `rapoarteAgent.ts`, `sportiviAgent.ts`) — prefixul `Utilizator: ${ctx.userName} | ` ELIMINAT
- `ClaudeRequestContext` (`services/claudeService.ts`) — campul `userName: string` ELIMINAT (fisier inactiv, pastrat)
- apelul `orchestrate()` din `contexts/AIAssistantContext.tsx` — proprietatea `userName` ELIMINATA din obiectul de context

<verification>
1. `npm run lint` trece.
2. `grep -rn "userName" services/ contexts/ components/` returneaza zero rezultate.
3. `docs/gdpr/DPIA-AI-ASSISTANT.md` si `docs/gdpr/SUBPROCESATORI.md` exista, contin `Groq`, si citeaza fisierele reale de cod.
4. Verificare functionala manuala (end-of-phase): AI Assistant deschis in aplicatie raspunde coerent la o intrebare simpla si nu afirma eronat identitatea utilizatorului.
</verification>

<success_criteria>
- Zero date de identificare a persoanei fizice pleaca din browser catre furnizorul LLM prin promptul de sistem (REQ-6 rezolvat pe calea REALA, nu doar pe fisierul mort din SPEC).
- DPIA-ul descrie fluxul post-fix, cu fisierele exacte si riscurile reziduale (REQ-2).
- Registrul de subprocesatori reflecta furnizorii efectiv folositi, inclusiv Groq (REQ-3).
</success_criteria>

<output>
Create `.planning/phases/28-conformitate-gdpr-si-ai-act-pentru-date-personale-sportivi-s/28-02-SUMMARY.md` when done
</output>
