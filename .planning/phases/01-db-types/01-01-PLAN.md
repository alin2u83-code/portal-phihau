---
phase: 01-db-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migrations/add_status_motiv_antrenamente.sql
  - sql/migrations/add_pret_tipuri_stagii.sql
  - types.ts
autonomous: false
requirements: [DB-01, DB-02, DB-03]

must_haves:
  truths:
    - "program_antrenamente are coloana status (CHECK: planificat/anulat/efectuat, DEFAULT planificat) aplicata in Supabase"
    - "program_antrenamente are coloana motiv_anulare (TEXT nullable) aplicata in Supabase"
    - "tipuri_stagii are coloana pret (NUMERIC(10,2) nullable) aplicata in Supabase"
    - "Antrenament interface in types.ts expune status si motiv_anulare cu tipuri corecte"
    - "Tipul de stagiu in types.ts expune pret optional"
  artifacts:
    - path: "sql/migrations/add_status_motiv_antrenamente.sql"
      provides: "Migratie status + motiv_anulare pe program_antrenamente (D-01..D-05)"
      contains: "program_antrenamente"
    - path: "sql/migrations/add_pret_tipuri_stagii.sql"
      provides: "Migratie pret pe tipuri_stagii (D-06, D-07)"
      contains: "tipuri_stagii"
    - path: "types.ts"
      provides: "Antrenament.status, Antrenament.motiv_anulare, tip stagiu cu pret (D-08..D-10)"
      contains: "motiv_anulare"
  key_links:
    - from: "components/Grupe/GenerareAntrenamenteModal.tsx"
      to: "program_antrenamente.status"
      via: "insert cu status: 'planificat'"
      pattern: "status:\\s*'planificat'"
---

<objective>
Livreaza schema DB completa pentru noile feature-uri Grupe & Stagii: doua migratii SQL plain (status + motiv_anulare pe program_antrenamente; pret pe tipuri_stagii) si actualizarea tipurilor TypeScript din types.ts. Zero logica UI sau business — doar fundatia de date si tipuri.

Purpose: Eliminam erorile de runtime cauzate de coloane lipsa cand Phase 3 (anulare antrenament) si Phase 4 (pret dinamic stagiu) vor scrie/citi aceste campuri. Tipurile TS sincronizate cu schema permit autocomplete corect si compilare fara erori in fazele urmatoare.

Output: 2 fisiere .sql noi in sql/migrations/ + types.ts actualizat. Migratiile sunt aplicate MANUAL in Supabase (proiectul nu are CLI push automat) — task checkpoint blocheaza pana la confirmarea aplicarii.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-types/01-CONTEXT.md
@sql/migrations/add_tipuri_stagii.sql
@types.ts
@components/Grupe/GenerareAntrenamenteModal.tsx
@components/Competitii/StagiiCompetitii.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migratie status + motiv_anulare pe program_antrenamente</name>
  <files>sql/migrations/add_status_motiv_antrenamente.sql</files>
  <read_first>
    - sql/migrations/add_tipuri_stagii.sql (pattern migratie plain: ADD COLUMN IF NOT EXISTS, comentariu header cu data)
    - components/Grupe/GenerareAntrenamenteModal.tsx liniile 184-192 (insert curent cu status: 'planificat', is_recurent: true)
    - .planning/phases/01-db-types/01-CONTEXT.md sectiunea decisions D-01..D-05
  </read_first>
  <action>
    Creeaza fisierul nou sql/migrations/add_status_motiv_antrenamente.sql cu un header comentat (titlu migratie + data 2026-06-04) urmand stilul din add_tipuri_stagii.sql.

    Coloana status (D-01, D-02, D-03): foloseste ADD COLUMN IF NOT EXISTS status text DEFAULT 'planificat' pe tabela program_antrenamente, astfel incat sa fie idempotent indiferent daca coloana exista deja. GenerareAntrenamenteModal scrie deja string-ul 'planificat' (linia 188) fara constraint la nivel DB, deci coloana poate exista deja ca TEXT.

    Pentru constraint-ul CHECK cu cele trei valori 'planificat' | 'anulat' | 'efectuat': adauga-l intr-un bloc DO/plpgsql idempotent care verifica in pg_constraint daca constraint-ul (nume explicit, ex. program_antrenamente_status_check) exista deja inainte de ALTER TABLE ... ADD CONSTRAINT. Aceasta acopera D-03 (daca status exista ca TEXT fara constraint, doar adauga constraint-ul). Numele constraint-ului trebuie explicit, nu auto-generat, ca sa fie verificabil.

    Coloana motiv_anulare (D-04, D-05): ADD COLUMN IF NOT EXISTS motiv_anulare text — nullable, fara DEFAULT (NULL implicit). Nu adauga DEFAULT explicit.

    NU rula migratia — proiectul aplica SQL-ul manual in Supabase Studio. Task 3 (checkpoint) gestioneaza aplicarea.
  </action>
  <acceptance_criteria>
    - Fisierul sql/migrations/add_status_motiv_antrenamente.sql exista.
    - Contine ADD COLUMN IF NOT EXISTS pentru atat status cat si motiv_anulare pe program_antrenamente.
    - Contine un CHECK constraint cu exact valorile 'planificat', 'anulat', 'efectuat' (verificabil: grep gaseste toate trei stringuri).
    - status are DEFAULT 'planificat'; motiv_anulare NU are clauza DEFAULT.
    - Adaugarea constraint-ului este protejata idempotent (bloc DO cu verificare pg_constraint sau IF NOT EXISTS echivalent), astfel incat rularea repetata nu da eroare.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "planificat" sql/migrations/add_status_motiv_antrenamente.sql && grep -q "anulat" sql/migrations/add_status_motiv_antrenamente.sql && grep -q "efectuat" sql/migrations/add_status_motiv_antrenamente.sql && grep -q "motiv_anulare" sql/migrations/add_status_motiv_antrenamente.sql && grep -q "program_antrenamente" sql/migrations/add_status_motiv_antrenamente.sql</automated>
  </verify>
  <done>Fisierul de migratie exista cu coloanele status (DEFAULT planificat + CHECK 3 valori, idempotent) si motiv_anulare (TEXT nullable, fara DEFAULT) pe program_antrenamente, conform D-01..D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Migratie pret pe tipuri_stagii + update types.ts</name>
  <files>sql/migrations/add_pret_tipuri_stagii.sql, types.ts</files>
  <read_first>
    - sql/migrations/add_tipuri_stagii.sql (schema curenta tipuri_stagii: id, cod, denumire, activ, ordine — NO pret)
    - types.ts liniile 335-355 (interface Antrenament curenta, fara status/motiv_anulare)
    - components/Competitii/StagiiCompetitii.tsx liniile 16, 22-37 (TipStagiuOpt local: { cod, denumire }; select din tipuri_stagii)
    - .planning/phases/01-db-types/01-CONTEXT.md decisions D-06..D-10
  </read_first>
  <action>
    Parte SQL — creeaza sql/migrations/add_pret_tipuri_stagii.sql cu header comentat (titlu + data 2026-06-04). Foloseste ALTER TABLE tipuri_stagii ADD COLUMN IF NOT EXISTS pret numeric(10,2) — nullable, fara DEFAULT (D-06). NULL semnifica tip fara pret configurat, fallback la taxa globala din preturiConfig (D-07, comportament existent nemodificat). Nu adauga RLS suplimentar — politica de scriere restrictionata la SUPER_ADMIN_FEDERATIE exista deja pe tabela din add_tipuri_stagii.sql.

    Parte types.ts — fa doua modificari:
    1. Antrenament interface (linia ~335): adauga doua campuri optionale (D-08): status cu uniune literala 'planificat' | 'anulat' | 'efectuat' optional, si motiv_anulare optional de tip string | null. Plaseaza-le langa campurile existente ale interfetei, respectand stilul existent (campuri optionale cu ?:).
    2. Tip pentru stagiu cu pret (D-09): NU exista interfata TipStagiu in types.ts; TipStagiuOpt e definit local in StagiiCompetitii.tsx (linia 16) ca { cod; denumire }. Creeaza in types.ts o interfata exportata noua numita TipStagiu cu campurile cod: string, denumire: string si pret?: number | null. Aceasta devine fisierul unic de referinta (D-10) — nu se creeaza fisiere de tipuri separate. NU modifica inca StagiiCompetitii.tsx sa importe acest tip (Phase 4 face cablarea); aici doar definim contractul de tip in types.ts.
  </action>
  <acceptance_criteria>
    - Fisierul sql/migrations/add_pret_tipuri_stagii.sql exista si contine ADD COLUMN IF NOT EXISTS pret numeric(10,2) pe tipuri_stagii, fara DEFAULT.
    - types.ts interface Antrenament contine status?: ('planificat' | 'anulat' | 'efectuat') si motiv_anulare?: string | null.
    - types.ts contine o interfata exportata TipStagiu cu pret?: number | null.
    - `npm run lint` (tsc --noEmit) trece fara erori noi de compilare.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "pret" sql/migrations/add_pret_tipuri_stagii.sql && grep -q "tipuri_stagii" sql/migrations/add_pret_tipuri_stagii.sql && grep -q "motiv_anulare" types.ts && grep -q "TipStagiu" types.ts && npm run lint</automated>
  </verify>
  <done>Migratia pret pe tipuri_stagii exista (D-06, D-07); types.ts are Antrenament cu status + motiv_anulare (D-08) si interfata TipStagiu cu pret optional (D-09, D-10); tsc --noEmit trece.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Aplicare manuala migratii in Supabase</name>
  <files>sql/migrations/add_status_motiv_antrenamente.sql, sql/migrations/add_pret_tipuri_stagii.sql</files>
  <action>
    Checkpoint blocant — Claude nu poate aplica DDL in Supabase (proiectul nu are comanda CLI push automata). Prezinta utilizatorului pasii din how-to-verify, asteapta semnalul de reluare ("aplicat") inainte de a marca faza completa. Nu se scrie cod in acest task; rolul lui este doar verificarea umana a aplicarii migratiilor.
  </action>
  <what-built>
    Doua fisiere de migratie SQL idempotente in sql/migrations/:
    - add_status_motiv_antrenamente.sql (status + motiv_anulare pe program_antrenamente)
    - add_pret_tipuri_stagii.sql (pret pe tipuri_stagii)
  </what-built>
  <how-to-verify>
    Proiectul aplica migratiile MANUAL — nu exista comanda CLI push automata.
    1. Deschide Supabase Studio → proiectul Portal PhiHau → SQL Editor.
    2. Copiaza continutul din sql/migrations/add_status_motiv_antrenamente.sql, ruleaza-l. Asteptat: success, fara erori (migratia e idempotenta — sigura chiar daca status exista deja).
    3. Copiaza continutul din sql/migrations/add_pret_tipuri_stagii.sql, ruleaza-l. Asteptat: success.
    4. Verifica in Table Editor: program_antrenamente are coloanele status (cu default 'planificat') si motiv_anulare (nullable); tipuri_stagii are coloana pret (numeric, nullable).
    5. Optional: ruleaza din nou ambele scripturi — trebuie sa treaca fara eroare (confirma idempotenta).
  </how-to-verify>
  <verify>
    <human-check>Ambele migratii rulate cu succes in Supabase SQL Editor; coloanele status, motiv_anulare (pe program_antrenamente) si pret (pe tipuri_stagii) vizibile in Table Editor cu tipurile corecte.</human-check>
  </verify>
  <acceptance_criteria>
    - Ambele migratii rulate cu succes in Supabase SQL Editor.
    - program_antrenamente.status, program_antrenamente.motiv_anulare si tipuri_stagii.pret vizibile in Table Editor cu tipurile corecte.
  </acceptance_criteria>
  <done>Ambele migratii aplicate in Supabase; cele trei coloane noi vizibile in Table Editor cu tipurile corecte; utilizatorul a confirmat cu "aplicat".</done>
  <resume-signal>Scrie "aplicat" dupa ce ambele migratii au rulat cu succes in Supabase, sau descrie erorile intalnite.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL Editor → DB | SQL DDL aplicat manual de admin in Supabase Studio (trusted operator) |
| client → tipuri_stagii write | scriere pret restrictionata la SUPER_ADMIN_FEDERATIE prin RLS existent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | migratie status pe rows existente | mitigate | DEFAULT 'planificat' + CHECK constraint cu 3 valori previne valori invalide; ADD COLUMN IF NOT EXISTS si bloc DO idempotent previn coruperea schemei la rulare repetata (D-01, D-02, D-03) |
| T-01-02 | Elevation | scriere pret tipuri_stagii | accept | RLS existent din add_tipuri_stagii.sql restrictioneaza ALL la SUPER_ADMIN_FEDERATIE; nu se adauga politica noua, deci suprafata neschimbata |
| T-01-03 | Information | motiv_anulare TEXT | accept | camp text liber nullable, fara PII structurat; risc redus (D-04) |
</threat_model>

<verification>
- Ambele fisiere .sql exista in sql/migrations/ si sunt idempotente (ADD COLUMN IF NOT EXISTS; constraint protejat in bloc DO).
- types.ts: Antrenament expune status + motiv_anulare; interfata TipStagiu exportata cu pret optional.
- `npm run lint` (tsc --noEmit) trece fara erori noi.
- Checkpoint blocant confirma aplicarea manuala in Supabase (coloanele vizibile in Table Editor).
</verification>

<success_criteria>
1. program_antrenamente are status (enum planificat/anulat/efectuat, DEFAULT planificat) si motiv_anulare (text nullable) — migratie aplicata in Supabase (DB-01).
2. Antrenament in types.ts are status si motiv_anulare cu tipuri corecte (DB-02).
3. tipuri_stagii are coloana pret (numeric nullable) — permite preț per tip stagiu, fallback NULL la taxa globala; interfata TipStagiu in types.ts (DB-03).
</success_criteria>

<artifacts>
## Artifacts this phase produces

- sql/migrations/add_status_motiv_antrenamente.sql — migratie status (CHECK 3 valori, DEFAULT planificat) + motiv_anulare (nullable) pe program_antrenamente
- sql/migrations/add_pret_tipuri_stagii.sql — migratie pret numeric(10,2) nullable pe tipuri_stagii
- types.ts (modificat) — Antrenament.status, Antrenament.motiv_anulare, interfata exportata TipStagiu cu pret?: number | null
</artifacts>

<output>
Create `.planning/phases/01-db-types/01-01-SUMMARY.md` when done
</output>
