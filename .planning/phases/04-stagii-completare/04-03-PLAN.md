---
phase: "04-stagii-completare"
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - components/Competitii/StagiiCompetitii.tsx
autonomous: false
requirements: [STG-04]

must_haves:
  truths:
    - "EvenimentDetail afișează o secțiune Participanți cu tabel complet: Sportiv | Data Înscrierii | Categorie | Taxă | Status Plată"
    - "Fiecare rând din tabel corelează corect plata sportivului cu înscrierea (prin eveniment_id sau fallback)"
    - "Butonul Export CSV descarcă un fișier CSV valid cu toți participanții"
    - "Secțiunea Participanți este vizibilă doar pentru permissions.isAdminClub sau mai sus"
  artifacts:
    - path: "components/Competitii/StagiiCompetitii.tsx"
      provides: "Secțiune tab Participanți în EvenimentDetail cu tabel + export CSV"
      contains: "exportParticipantiCSV"
  key_links:
    - from: "EvenimentDetail (filteredData.rezultate)"
      to: "EvenimentDetail (filteredData.plati)"
      via: "join pe sportiv_id + eveniment_id (sau fallback tip='Taxa Stagiu')"
      pattern: "platiParticipanti"
    - from: "exportParticipantiCSV"
      to: "Blob download"
      via: "URL.createObjectURL pattern din index.tsx:593"
      pattern: "URL\\.createObjectURL"
---

<objective>
Implementare tab Participanți în EvenimentDetail cu tabel complet + export CSV (STG-04).

Purpose: Adminul are nevoie să vadă cine e înscris la stagiu, ce taxă are și dacă a plătit. Fără această vedere, trebuie să navigheze în două locuri separate (Stagii și Plăți). Feliei verticale precedente (04-02) i-a lipsit vizibilitatea statusului plăților per participant.

Output: Secțiune extinsă în EvenimentDetail cu tabel participanți + CSV download, condiționat de permisiune isAdminClub.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-stagii-completare/04-RESEARCH.md
@.planning/phases/04-stagii-completare/04-01-SUMMARY.md
@.planning/phases/04-stagii-completare/04-02-SUMMARY.md
@components/Competitii/StagiiCompetitii.tsx
@components/Competitii/index.tsx
@types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tabel Participanți cu status plată în EvenimentDetail</name>
  <files>components/Competitii/StagiiCompetitii.tsx</files>
  <behavior>
    - Test 1: `platiParticipanti.get(sportivId)` returnează plata corectă când eveniment_id există pe plată
    - Test 2: Fallback funcționează — când eveniment_id este null pe plată veche, filtrarea prin sportiv_id + tip='Taxa Stagiu' găsește totuși plata
    - Test 3: Statusul "Achitat" se afișează cu styling verde (clasa text-green-400), "Neachitat" cu roșu (text-red-400)
    - Test 4: Rânduri tabel: un sportiv cu plată → 4 coloane completate; un sportiv fără plată → taxa N/A și status "Fără plată"
  </behavior>
  <action>
    Modificări în `components/Competitii/StagiiCompetitii.tsx`, în interiorul componentei `EvenimentDetail`:

    **A. Derivă datele pentru tabel (useMemo)**

    Adaugă importul `usePermissions` dacă nu există deja în componentă. Obține `permissions` din `usePermissions(activeRoleContext)` — sau ia din destructurarea `useData()` dacă `permissions` e deja disponibil acolo.

    Adaugă un `useMemo` pentru `platiParticipanti`:
    ```
    const platiParticipanti = useMemo(() => {
      const map = new Map<string, Plata>();
      filteredData.plati
        .filter(p =>
          p.tip === 'Taxa Stagiu' &&
          (p.eveniment_id === eveniment.id ||
           (p.eveniment_id == null && rezultate.some(r => r.sportiv_id === p.sportiv_id)))
        )
        .forEach(p => { if (p.sportiv_id) map.set(p.sportiv_id, p); });
      return map;
    }, [filteredData.plati, eveniment.id, rezultate]);
    ```

    Adaugă un `useMemo` pentru `randuriiParticipanti` care combină rezultate cu plăți:
    Fiecare rând: `{ sportiv: Sportiv | undefined, dataInscriere: string, categorie: string, taxa: number | null, statusPlata: string }`.
    - `dataInscriere`: `rezultat.created_at` (din Planul 04-01, adăugat în Rezultat type) formatat ca `DD.MM.YYYY`, sau `'-'` dacă lipsește
    - `categorie`: dacă sportivul există, apelează `calculeazaCategorieStagiu` (definit în Task 2 din 04-02) — returnează 'Copii' / 'Grade' / 'Centuri Negre' (string afișabil)
    - `taxa`: `platiParticipanti.get(sportiv_id)?.suma ?? null`
    - `statusPlata`: `platiParticipanti.get(sportiv_id)?.status ?? 'Fără plată'`

    NOTĂ: `rezultate` este deja filtrat pe `eveniment.id` (linia ~127 din componenta actuală). Nu adăuga un al doilea filtru.

    **B. Secțiunea vizuală tabel (JSX)**

    Adaugă o secțiune nouă în return-ul componentei `EvenimentDetail`, DUPĂ lista de participanți existentă și ÎNAINTE de formularul "Înscrie Participant":

    ```jsx
    {permissions.isAdminClub && rezultate.length > 0 && (
      <div className="mt-6 border-t border-slate-700 pt-6">
        <div className="flex items-center justify-between mb-4">
          <h4 className="text-xl font-semibold text-white">
            Raport Participanți ({rezultate.length})
          </h4>
          <Button variant="secondary" size="sm" onClick={exportParticipantiCSV}>
            Export CSV
          </Button>
        </div>
        <div className="overflow-x-auto">
          <table className="w-full text-sm">
            <thead>
              <tr className="border-b border-slate-700 text-slate-400">
                <th className="text-left py-2 pr-4">Sportiv</th>
                <th className="text-left py-2 pr-4">Data Înscrierii</th>
                <th className="text-left py-2 pr-4">Categorie</th>
                <th className="text-right py-2 pr-4">Taxă (lei)</th>
                <th className="text-left py-2">Status Plată</th>
              </tr>
            </thead>
            <tbody>
              {randuriiParticipanti.map((rand, i) => (
                <tr key={i} className="border-b border-slate-800 hover:bg-slate-800/30">
                  <td className="py-2 pr-4 font-medium">
                    {rand.sportiv ? `${rand.sportiv.prenume} ${rand.sportiv.nume}` : '—'}
                  </td>
                  <td className="py-2 pr-4 text-slate-400">{rand.dataInscriere}</td>
                  <td className="py-2 pr-4 text-slate-300">{rand.categorie}</td>
                  <td className="py-2 pr-4 text-right">
                    {rand.taxa != null ? rand.taxa.toFixed(2) : '—'}
                  </td>
                  <td className={`py-2 font-medium ${
                    rand.statusPlata === 'Achitat' ? 'text-green-400' :
                    rand.statusPlata === 'Neachitat' ? 'text-red-400' :
                    rand.statusPlata === 'Achitat Parțial' ? 'text-amber-400' :
                    'text-slate-500'
                  }`}>
                    {rand.statusPlata}
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    )}
    ```

    IMPORTANT: Condiția `permissions.isAdminClub` — dacă `useData()` nu expune `permissions`, importează `usePermissions` explicit: `import { usePermissions } from '../../hooks/usePermissions'` și `const permissions = usePermissions(activeRoleContext)` unde `activeRoleContext` vine din `useData()` sau `useAuth()`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compilation trece. Secțiunea "Raport Participanți" cu tabel 5 coloane apare în EvenimentDetail când utilizatorul are permisiuni isAdminClub.</done>
</task>

<task type="auto">
  <name>Task 2: Export CSV participanți cu Blob download</name>
  <files>components/Competitii/StagiiCompetitii.tsx</files>
  <action>
    Adaugă funcția `exportParticipantiCSV` în interiorul componentei `EvenimentDetail`, înainte de `return`:

    Funcția nu primește parametri — citește `randuriiParticipanti` din closure (useMemo derivat în Task 1).

    Construiește CSV fără PapaParse (pattern Blob existent din `components/Competitii/index.tsx` linia ~593 — copiat și adaptat):
    - Rând header: `Sportiv,Data Inscrierii,Categorie,Taxa (lei),Status Plata`
    - Pentru fiecare rand din `randuriiParticipanti`:
      - Sportiv: `"${rand.sportiv?.prenume || ''} ${rand.sportiv?.nume || ''}"` (ghilimele pentru a evita probleme cu virgulele în nume)
      - Data Înscrierii: `"${rand.dataInscriere}"`
      - Categorie: `"${rand.categorie}"`
      - Taxă: `${rand.taxa != null ? rand.taxa.toFixed(2) : ''}` (fără ghilimele, e numeric)
      - Status: `"${rand.statusPlata}"`
    - Join linii cu `\n`
    - Blob cu `type: 'text/csv;charset=utf-8;'`
    - Filename: `` `participanti_${eveniment.denumire.replace(/\s+/g, '_')}_${eveniment.data}.csv` ``
    - Pattern standard: `URL.createObjectURL` → `a.href = url` → `a.click()` → `URL.revokeObjectURL(url)`

    Nu importa PapaParse — construcția manuală e suficientă pentru un CSV cu 5 coloane fixe fără caractere speciale complexe.

    Butonul "Export CSV" din Task 1 apelează direct această funcție prin `onClick={exportParticipantiCSV}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compilation trece. Funcția `exportParticipantiCSV` este definită și referențiată corect de butonul Export CSV. CSV-ul generat are header-ul corect și câte un rând per participant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task-urile anterioare au adăugat în EvenimentDetail:
    - Secțiune "Raport Participanți" cu tabel 5 coloane (vizibil doar pentru admini)
    - Export CSV cu download automat
    - Integrare cu statusul plăților din filteredData
  </what-built>
  <how-to-verify>
    1. Pornire dev: `npm run dev` în directorul proiectului
    2. Login cu un cont ADMIN_CLUB
    3. Navighează la Stagii → selectează un stagiu existent cu cel puțin 1 participant înscris
    4. Verifică că apare secțiunea "Raport Participanți" sub lista de participanți existentă
    5. Verifică că tabelul are coloanele: Sportiv | Data Înscrierii | Categorie | Taxă (lei) | Status Plată
    6. Verifică că statusul plăților este colorat corect (verde=Achitat, roșu=Neachitat)
    7. Click "Export CSV" → verifică că un fișier .csv se descarcă cu datele corecte
    8. Deschide CSV-ul descărcat și verifică că are header-ul: `Sportiv,Data Inscrierii,Categorie,Taxa (lei),Status Plata`
    9. Înscrie un sportiv nou la stagiu → verifică că apare în tabel + plata în PlatiScadente cu tip="Taxa Stagiu"
    10. Verifică că un cont INSTRUCTOR nu vede secțiunea "Raport Participanți"
  </how-to-verify>
  <resume-signal>Scrie "aprobat" dacă toate verificările trec, sau descrie problema găsită.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin→CSV download | Datele exportate includ nume sportivi și sume — nu conțin CNP sau date sensibile suplimentare |
| filteredData→tabel | Datele din tabel vin din filteredData deja filtrat pe club — nu există risc de cross-club data leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | exportParticipantiCSV | mitigate | Guard `permissions.isAdminClub` pe buton și secțiune; datele vin din filteredData filtrat pe club activ |
| T-04-07 | Information Disclosure | platiParticipanti (join fallback) | accept | Fallback-ul prin sportiv_id + tip='Taxa Stagiu' poate asocia o plată greșită dacă sportivul are 2 stagii în aceeași zi — risc scăzut, documentat în STG-04 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Nu se instalează pachete în această fază |
</threat_model>

<verification>
1. `grep -n "exportParticipantiCSV" components/Competitii/StagiiCompetitii.tsx` returnează definiție și apel
2. `grep -n "isAdminClub" components/Competitii/StagiiCompetitii.tsx` returnează match în guard-ul secțiunii participanți
3. `grep -n "platiParticipanti" components/Competitii/StagiiCompetitii.tsx` returnează definire useMemo și utilizare în randuriiParticipanti
4. `npx tsc --noEmit` trece fără erori
5. Checkpoint vizual: admin vede tabel cu status plată colorat; instructor nu vede secțiunea
</verification>

<success_criteria>
- Tabelul participanți afișează date corecte (sportiv, data, taxă, status plată) pentru cel puțin 1 participant existent
- CSV-ul descărcat are header-ul corect și conținut valid (poate fi deschis în Excel)
- Guard-ul de permisiune funcționează: admin vede tabelul, instructor nu îl vede
- Fallback pentru plăți fără eveniment_id funcționează (plăți existente premerge fix-ului din 04-02)
</success_criteria>

<output>
Creează `.planning/phases/04-stagii-completare/04-03-SUMMARY.md` când gata.
</output>
