---
phase: "04-stagii-completare"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migrations/add_pret_per_categorie_stagiu.sql
  - types.ts
autonomous: true
requirements: [STG-02, STG-03, STG-04, STG-05]

must_haves:
  truths:
    - "Tabela `evenimente` acceptă coloanele pret_copii, pret_grade, pret_centuri fără erori SQL"
    - "Tabela `plati` are coloana eveniment_id (FK nullable spre evenimente.id)"
    - "Interfața Eveniment din types.ts expune pret_copii?, pret_grade?, pret_centuri?"
    - "Interfața Plata din types.ts expune eveniment_id?"
    - "Interfața Rezultat din types.ts expune created_at?"
  artifacts:
    - path: "sql/migrations/add_pret_per_categorie_stagiu.sql"
      provides: "Migrație SQL completă pentru STG-04 și STG-05"
      contains: "ALTER TABLE public.evenimente ADD COLUMN IF NOT EXISTS pret_copii"
    - path: "types.ts"
      provides: "Tipuri TypeScript extinse pentru Eveniment, Plata, Rezultat"
      contains: "pret_copii"
  key_links:
    - from: "types.ts (Eveniment)"
      to: "components/Competitii/StagiiCompetitii.tsx (EvenimentDetail)"
      via: "prop eveniment: Eveniment"
      pattern: "pret_copii|pret_grade|pret_centuri"
    - from: "types.ts (Plata)"
      to: "hooks/useDataProvider.ts"
      via: "AppData.plati: Plata[]"
      pattern: "eveniment_id"
---

<objective>
Pregătire schema DB și contracte TypeScript pentru prețuri per categorie la stagii (STG-05) și tracking plată per eveniment (STG-03, STG-04).

Purpose: Planurile 02 și 03 depind de coloanele DB și tipurile TypeScript definite aici. Fără această fundație, compiler-ul TypeScript va raporta erori la orice referință la `pret_copii`, `pret_grade`, `pret_centuri`, `eveniment_id`.

Output: Fișier SQL migrație gata de aplicat manual în Supabase + types.ts actualizat.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-stagii-completare/04-RESEARCH.md
@types.ts
@sql/migrations/add_pret_tipuri_stagii.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrație SQL — prețuri per categorie pe eveniment + FK plati→evenimente</name>
  <files>sql/migrations/add_pret_per_categorie_stagiu.sql</files>
  <action>
    Creează fișierul `sql/migrations/add_pret_per_categorie_stagiu.sql` cu două blocuri ALTER TABLE:

    Bloc 1 — coloane prețuri per categorie pe `public.evenimente`:
    - `pret_copii NUMERIC(10,2)` nullable — prețul pentru sportivi 7–12 ani
    - `pret_grade NUMERIC(10,2)` nullable — prețul pentru sportivi min 13 ani cu grade colorate
    - `pret_centuri NUMERIC(10,2)` nullable — prețul pentru centuri negre (Dan 1+)
    Toate cu `ADD COLUMN IF NOT EXISTS` pentru idempotență.
    NULL pe oricare coloană = fallback la `tipuri_stagii.pret`, apoi la `preturiConfig` global.

    Bloc 2 — coloana FK pe `public.plati`:
    - `eveniment_id UUID` nullable cu `REFERENCES public.evenimente(id) ON DELETE SET NULL`
    - `ADD COLUMN IF NOT EXISTS` pentru idempotență
    Plățile existente rămân cu eveniment_id = NULL (nu se face populare retroactivă).

    Include comentarii explicative în SQL pentru fiecare coloană.
    Respectă pattern-ul existent din `add_pret_tipuri_stagii.sql`: header cu dată și descriere, fără DEFAULT pe coloane nullable.
  </action>
  <verify>
    <automated>grep -c "pret_copii" sql/migrations/add_pret_per_categorie_stagiu.sql</automated>
  </verify>
  <done>Fișierul există și conține `pret_copii`, `pret_grade`, `pret_centuri`, `eveniment_id` în instrucțiuni ALTER TABLE.</done>
</task>

<task type="auto">
  <name>Task 2: Update types.ts — extinde Eveniment, Plata, Rezultat</name>
  <files>types.ts</files>
  <action>
    Modifică interfețele existente în `types.ts` (nu se adaugă fișiere noi):

    1. Interfața `Eveniment` (în jurul liniei 375): adaugă câmpurile opționale:
       - `pret_copii?: number | null`
       - `pret_grade?: number | null`
       - `pret_centuri?: number | null`
       Adaugă după câmpul `tip_stagiu?: string | null` existent.

    2. Interfața `Plata` (în jurul liniei 151): adaugă câmpul opțional:
       - `eveniment_id?: string | null`
       Adaugă după câmpul `sesiune_id?: string | null` existent, cu comentariu:
       `// FK → evenimente.id (pentru 'Taxa Stagiu' și 'Taxa Competitie')`

    3. Interfața `Rezultat` (în jurul liniei 390): adaugă câmpul opțional:
       - `created_at?: string`
       Adaugă după câmpul `probe?: string` existent.

    Nu modifica niciun alt câmp sau interfață. Tipul strict mode este dezactivat în proiect — câmpurile nullable se declară ca `type | null`, nu ca `type | undefined | null`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>`tsc --noEmit` trece fără erori noi introduse de aceste modificări. Interfețele Eveniment, Plata și Rezultat conțin câmpurile adăugate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin→Supabase | Migrația SQL se aplică direct în producție de către admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | sql/migrations/add_pret_per_categorie_stagiu.sql | mitigate | Coloana eveniment_id are ON DELETE SET NULL — ștergerea unui eveniment nu corupe plățile existente |
| T-04-02 | Information Disclosure | types.ts (Plata.eveniment_id) | accept | Câmpul e nullable; datele existente fără eveniment_id rămân anonime față de eveniment |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Nu se instalează pachete în această fază |
</threat_model>

<verification>
1. Fișierul `sql/migrations/add_pret_per_categorie_stagiu.sql` există și conține toate 4 coloane noi
2. `npx tsc --noEmit` trece fără erori TypeScript (verificare că tipurile sunt consistent)
3. `grep -n "pret_copii\|pret_grade\|pret_centuri" types.ts` returnează 3 matches în interfața Eveniment
4. `grep -n "eveniment_id" types.ts` returnează match în interfața Plata
5. `grep -n "created_at" types.ts` returnează match în interfața Rezultat
</verification>

<success_criteria>
- Migrația SQL este idempotentă (IF NOT EXISTS) și poate fi aplicată fără risc în Supabase
- TypeScript compilation trece fără erori după modificările în types.ts
- Planul 04-02 poate referenția `eveniment.pret_copii` fără erori de tip
- Planul 04-02 poate include `eveniment_id` în obiectul INSERT pentru `plati` fără erori de tip
</success_criteria>

<output>
Creează `.planning/phases/04-stagii-completare/04-01-SUMMARY.md` când gata.
</output>
