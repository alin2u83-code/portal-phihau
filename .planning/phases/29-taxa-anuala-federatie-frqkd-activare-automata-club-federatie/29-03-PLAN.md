---
phase: 29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - types.ts
  - utils/anFiscal.ts
  - hooks/useDataProvider.ts
  - components/FederationInvoices.tsx
  - components/AppRouter.tsx
autonomous: true
requirements: [TAF-06, TAF-07]
must_haves:
  truths:
    - "Adminul deschide un decont si vede lista sportivilor acoperiti, adusa din decont_sportivi, fara sa mai bifeze pe nimeni manual"
    - "Confirmarea platii cere alegerea metodei dintre Cash, Transfer Bancar si Revolut si nu poate fi trimisa fara ea"
    - "Dupa confirmare decontul apare Achitat, cu metoda vizibila in tabel si cu dovada legata in dovada_transfer_url"
    - "Tabelul de deconturi arata sezonul sub forma 2025-2026, derivat din an_fiscal"
    - "Adminul de federatie poate confirma plata unui decont, nu doar adminul de club"
  artifacts:
    - path: "utils/anFiscal.ts"
      provides: "getAnFiscalFederatie(date) si formatSezon(anFiscal) — regula 1 septembrie, reutilizata de tot frontendul"
      exports: ["getAnFiscalFederatie", "formatSezon"]
    - path: "types.ts"
      provides: "DecontFederatie extins cu an_fiscal/metoda_plata/data_decont/dovada_transfer_url/confirmata_federatie + interfata TaxaAnualaFederatieConfig"
      contains: "TaxaAnualaFederatieConfig"
    - path: "hooks/useDataProvider.ts"
      provides: "Incarcarea tabelei taxa_anuala_config in starea aplicatiei + setter"
      contains: "taxa_anuala_config"
    - path: "components/FederationInvoices.tsx"
      provides: "Lista read-only a sportivilor din decont + camp Metoda Plata + persistarea dovezii"
      contains: "metoda_plata"
  key_links:
    - from: "components/FederationInvoices.tsx"
      to: "public.decont_sportivi"
      via: "query pe decont_id la deschiderea modalului de confirmare"
      pattern: "from\\('decont_sportivi'\\)"
    - from: "components/FederationInvoices.tsx"
      to: "public.deconturi_federatie"
      via: "update cu metoda_plata + status_plata + confirmata_federatie + dovada_transfer_url"
      pattern: "confirmata_federatie"
    - from: "hooks/useDataProvider.ts"
      to: "public.taxa_anuala_config"
      via: "deferredQueries select('*')"
      pattern: "taxa_anuala_config"
---

<objective>
Aliniaza contractele TypeScript cu schema reparata in 29-01 si rescrie fluxul de confirmare a platii din `FederationInvoices.tsx`: lista sportivilor acoperiti vine din `decont_sportivi`, iar confirmarea cere metoda de plata.

Purpose: pana acum `FederationInvoices.tsx` cerea adminului sa bifeze manual sportivii acoperiti de un decont — munca redundanta si sursa de erori, acum ca triggerul ii leaga automat. In plus, componenta citeste azi coloane care nu existau live (`data_generare`, `club_id`), deci ecranul era nefunctional.
Output: tipuri corecte, tabela de configurare incarcata in starea aplicatiei, si un ecran de deconturi care functioneaza pe schema reala.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-CONTEXT.md
@.planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-01-SUMMARY.md
@sql/migrations/taxa_anuala_federatie_schema_260912.sql
@types.ts
@components/FederationInvoices.tsx
@hooks/useDataProvider.ts
@utils/formatareSportiv.ts
@components/ui.tsx
</context>

## Decizii utilizator aplicate in acest plan

Tabela D-NN completa este in `29-01-PLAN.md`. Acest plan implementeaza D-01 (varianta TypeScript a regulii de an fiscal), D-02 (campurile noi expuse in tipuri), D-09 (lista din `decont_sportivi` + metoda de plata).

## Contract de intrare din planul 29-01

Coloanele `deconturi_federatie.an_fiscal`, `.metoda_plata`, `.data_generare`, `.club_id`, `.tip_activitate`, `.nr_participanti`, `.status_plata` si tabela `public.taxa_anuala_config (id, an_fiscal, suma, created_at)` trebuie sa existe live inainte de a rula acest plan.

## Contract de iesire catre planul 29-04

Planul 29-04 consuma din acest plan:
- `utils/anFiscal.ts` → `getAnFiscalFederatie(d?: Date): number`, `formatSezon(anFiscal: number): string`
- `types.ts` → `export interface TaxaAnualaFederatieConfig { id: string; an_fiscal: number; suma: number; created_at?: string }`
- `useData()` → `taxaAnualaFederatieConfig: TaxaAnualaFederatieConfig[]` si `setTaxaAnualaFederatieConfig`

## Artifacts this phase produces

| Tip | Simbol | Creat in |
|-----|--------|----------|
| Coloane + constrangeri pe `deconturi_federatie`, `decont_sportivi`, `vize_sportivi` | vezi 29-01 | 29-01 |
| Tabela + RLS | `public.taxa_anuala_config`, politicile `_select` / `_insert` / `_update` | 29-01 |
| Functii SQL | `an_fiscal_federatie`, `activeaza_taxa_anuala`, `trg_activeaza_taxa_anuala` | 29-02 |
| Triggere | `trg_taxa_anuala_inscrieri_examene`, `trg_taxa_anuala_stagii_cvd`, `trg_taxa_anuala_participare_stagiu`, `trg_taxa_anuala_inscrieri_competitie` | 29-02 |
| Util TS | `utils/anFiscal.ts` → `getAnFiscalFederatie`, `formatSezon` | 29-03 |
| Tip TS | `TaxaAnualaFederatieConfig` | 29-03 |
| Campuri TS | `DecontFederatie.an_fiscal`, `.metoda_plata`, `.data_decont`, `.dovada_transfer_url`, `.confirmata_federatie`, `.created_at` | 29-03 |
| Stare app | `AppData.taxaAnualaFederatieConfig` + `setTaxaAnualaFederatieConfig` | 29-03 |
| Componenta UI | `PaymentConfirmationModal` reconstruit (lista read-only + select `metoda_plata`) | 29-03 |
| Componenta UI | `TabTaxaFederatieFRQKD` (tab in `components/Plati/TaxeAnuale.tsx`) | 29-04 |

<tasks>

<task type="auto">
  <name>Task 1: Contracte — utils/anFiscal.ts, tipuri si incarcarea taxa_anuala_config in starea aplicatiei</name>
  <files>utils/anFiscal.ts, types.ts, hooks/useDataProvider.ts</files>
  <read_first>
    - types.ts liniile 491-530 (`DecontFederatie`, `DecontSportiv`, `TaxaAnualeConfig`, `VizaSportiv` existente — atentie, `TaxaAnualeConfig` este o interfata VECHE, a tabelei `taxe_anuale_config`, alta tabela)
    - hooks/useDataProvider.ts liniile 40-60 (forma `AppData` si `initialData`), 335-360 (`criticalQueries` si `deferredQueries`), 445-470 (imbinarea rezultatelor), 555-580 (setterii generati)
    - sql/migrations/taxa_anuala_federatie_schema_260912.sql (forma reala a tabelei create in 29-01)
    - utils/date.ts (conventia locala pentru utilitare de data)
  </read_first>
  <action>
Creeaza `utils/anFiscal.ts` cu doua functii exportate, versiunea TypeScript a regulii D-01, care trebuie sa dea acelasi rezultat ca functia SQL `public.an_fiscal_federatie`:
`getAnFiscalFederatie(d: Date = new Date()): number` — returneaza `d.getFullYear()` cand `d.getMonth() >= 8` (getMonth este 0-indexat, deci 8 inseamna septembrie), altfel `d.getFullYear() - 1`.
`formatSezon(anFiscal: number): string` — returneaza sirul `${anFiscal}-${anFiscal + 1}`.
Adauga un comentariu care leaga explicit fisierul de `public.an_fiscal_federatie` din `sql/migrations/taxa_anuala_federatie_trigger_260912.sql`: cele doua implementari trebuie modificate impreuna.

In `types.ts`, extinde `DecontFederatie` cu campurile aduse de migratia 29-01, pastrand campurile existente: `an_fiscal: number | null`, `metoda_plata: 'Cash' | 'Transfer Bancar' | 'Revolut' | null`, `data_decont?: string | null`, `dovada_transfer_url?: string | null`, `confirmata_federatie?: boolean | null`, `created_at?: string`. Nu sterge `data_generare` — coloana exista acum live dupa 29-01 si este citita de `components/AdminMasterMap.tsx` si de tabelul din `FederationInvoices.tsx`.

Tot in `types.ts`, adauga o interfata NOUA, imediat dupa `TaxaAnualeConfig`: `export interface TaxaAnualaFederatieConfig { id: string; an_fiscal: number; suma: number; created_at?: string }`. Numele difera deliberat de `TaxaAnualeConfig` existent (tabela `taxe_anuale_config`, cu alta forma: `an`, `descriere`, `club_id`, `data_inceput`, `data_sfarsit`) — sunt doua tabele distincte si confuzia intre ele ar produce bug-uri financiare. Adauga deasupra fiecareia dintre cele doua interfete un comentariu de o linie care numeste tabela sursa, ca sa fie imposibil de confundat la citire.

In `hooks/useDataProvider.ts`:
adauga `taxaAnualaFederatieConfig: TaxaAnualaFederatieConfig[]` in tipul `AppData` (langa `taxeAnualeConfig`, linia ~46) si `taxaAnualaFederatieConfig: []` in `initialData`;
importa tipul nou din `../types`;
adauga in `deferredQueries` (langa `vizeSportivi`, linia ~351) intrarea `taxaAnualaFederatieConfig: cleanedSupabase.from('taxa_anuala_config').select('*')`;
adauga in blocul de imbinare a datelor deferred (langa `vizeSportivi: deferredData.vizeSportivi || prev.vizeSportivi`, linia ~466) linia corespunzatoare;
adauga `setTaxaAnualaFederatieConfig: createSetter('taxaAnualaFederatieConfig')` in obiectul returnat (langa `setVizeSportivi`, linia ~572).
`contexts/DataContext.tsx` nu se modifica — tipul lui este `ReturnType<typeof useDataProvider>`, deci preia automat campul nou.

Nu modifica nimic legat de `taxe_anuale_config` sau `TaxaAnualeConfig` — ecranul existent de Taxe Anuale trebuie sa continue sa functioneze neschimbat.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; grep -q "export function getAnFiscalFederatie" utils/anFiscal.ts &amp;&amp; grep -q "export function formatSezon" utils/anFiscal.ts &amp;&amp; grep -q "TaxaAnualaFederatieConfig" types.ts &amp;&amp; grep -q "taxa_anuala_config" hooks/useDataProvider.ts &amp;&amp; grep -q "setTaxaAnualaFederatieConfig" hooks/useDataProvider.ts &amp;&amp; grep -q "TaxaAnualeConfig" types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` (care ruleaza `tsc --noEmit`) se incheie cu cod de iesire 0.
    - `utils/anFiscal.ts` exporta `getAnFiscalFederatie` si `formatSezon`; `getAnFiscalFederatie(new Date('2026-09-01'))` da 2026, `getAnFiscalFederatie(new Date('2026-08-31'))` da 2025, `formatSezon(2025)` da `'2025-2026'`.
    - `types.ts` contine ambele interfete — `TaxaAnualeConfig` (nemodificata) si `TaxaAnualaFederatieConfig` (noua) — fiecare cu un comentariu care numeste tabela sursa.
    - `DecontFederatie` contine `an_fiscal`, `metoda_plata`, `dovada_transfer_url`, `confirmata_federatie` si pastreaza `data_generare`.
    - `hooks/useDataProvider.ts` contine exact o referinta la `from('taxa_anuala_config')` si expune `setTaxaAnualaFederatieConfig`.
    - Nicio referinta existenta la `taxe_anuale_config` sau `TaxaAnualeConfig` nu a fost stearsa sau redenumita (`grep -c` pe ambele siruri da o valoare mai mare sau egala cu cea de dinaintea modificarii).
  </acceptance_criteria>
  <done>Tipurile reflecta schema live, regula anului fiscal exista o singura data in frontend, iar pretul sezonului este disponibil oriunde prin `useData()`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FederationInvoices — lista sportivilor din decont_sportivi si confirmare cu metoda de plata</name>
  <files>components/FederationInvoices.tsx, components/AppRouter.tsx</files>
  <read_first>
    - components/FederationInvoices.tsx (integral — se rescriu `PaymentConfirmationModal`, `handleConfirmPayment` si tabelul)
    - components/AppRouter.tsx linia 235 (locul de apel al `Lazy.FederationInvoices`, unde se propaga `setDecontSportivi`)
    - utils/anFiscal.ts si types.ts (contractele produse in Task 1)
    - utils/formatareSportiv.ts (`formatNume`, `sortBySportivNume` — formatarea unificata a numelui, obligatorie in acest proiect)
    - components/ui.tsx liniile 590-626 (`Input`, `Select`) si 284-320 (`Card`, `EmptyState`)
    - .planning/phases/.../29-CONTEXT.md sectiunea UI (D-09)
  </read_first>
  <action>
Rescrie fluxul de confirmare din `components/FederationInvoices.tsx` conform D-09.

Sterge complet sub-componenta `SportivSelectItem` si intreaga logica de selectie manuala din `PaymentConfirmationModal`: starea `selectedIds`, `search`, `handleToggle`, `handleSelectAll`, memo-urile `sportiviDejaAcoperiti` si `sportiviDisponibili`, si blocul de UI cu checkbox-uri. Nu sunt un compromis de scop — devin cod mort odata ce triggerul din 29-02 leaga sportivii automat.

`PaymentConfirmationModal` primeste noile props: `decont: DecontFederatie`, `onClose`, `onConfirm: (decont: DecontFederatie, file: File, metodaPlata: MetodaPlataDecont) => Promise<void>`. Props-urile `sportivi`, `decontSportivi` si `anCurent` nu ii mai sunt necesare.

La montare, modalul incarca lista sportivilor acoperiti direct din baza de date (D-09: lista vine dintr-un query pe `decont_sportivi`, nu din starea filtrata a aplicatiei — un admin de federatie care deschide decontul altui club nu are acei sportivi in `filteredData.sportivi`). Foloseste `useEffect` cu `supabase.from('decont_sportivi').select('id, sportiv_id, an, sportivi(id, nume, prenume)').eq('decont_id', decont.id)`, cu stari `loadingSportivi` si `eroareSportivi`. Randeaza lista read-only, sortata cu `sortBySportivNume` si afisata cu `formatNume`, intr-un container cu inaltime maxima si scroll, sub un titlu care arata numarul: sportivi acoperiti si `decont.nr_participanti`. Cand lista e goala, foloseste `EmptyState` din `components/ui.tsx` cu un mesaj care explica faptul ca decontul nu are inca sportivi legati. Cand numarul de randuri returnate difera de `decont.nr_participanti`, afiseaza un avertisment vizibil (acelasi pattern amber ca la liniile 738-742 din `components/Plati/TaxeAnuale.tsx`) — este semnul unei desincronizari intre contor si legaturi.

Adauga in modal un camp obligatoriu "Metoda Plata" folosind `Select` din `components/ui.tsx`, cu optiunile `Cash`, `Transfer Bancar`, `Revolut` si o optiune initiala goala cu textul "Alege metoda...". Defineste in `types.ts`-ul deja extins sau local tipul `MetodaPlataDecont = NonNullable<DecontFederatie['metoda_plata']>` si tipeaza starea cu el. Butonul "Confirma si Incarca" ramane `disabled` pana cand exista si fisier, si metoda aleasa.

Rescrie `handleConfirmPayment(decont, file, metodaPlata)`:
pastreaza incarcarea in bucket-ul `chitante_deconturi` pe calea `public/${decont.club_id}/${decont.id}/${Date.now()}.${ext}`;
dupa upload reusit, persista calea in `dovada_transfer_url` — azi fisierul se incarca si referinta se pierde, ceea ce face coloana inutila;
inlocuieste update-ul actual cu unul care seteaza toate cele patru campuri intr-o singura operatie: `metoda_plata: metodaPlata`, `status_plata: 'Platit'`, `confirmata_federatie: true`, `dovada_transfer_url: <calea>`;
sterge complet blocul care insera in `decont_sportivi` (liniile 273-296 din versiunea curenta) impreuna cu variabila `anDecont` — legaturile sunt create de trigger, iar un insert manual ar incalca acum `decont_sportivi_sportiv_an_key`;
pastreaza actualizarea optimista a starii prin `setDeconturi` si mesajul de succes, dar reformuleaza-l ca sa numeasca metoda aleasa.

In componenta principala: elimina prop-ul `setDecontSportivi` din interfata `FederationInvoicesProps`, din destructurare si din apelul `Lazy.FederationInvoices` de la linia 235 din `components/AppRouter.tsx`. Pastreaza prop-ul `decontSportivi`, care este in continuare folosit pentru contorul afisat in coloana de actiuni. Prop-ul `sportivi` devine nefolosit — elimina-l si din interfata si din apel.

In tabel, adauga doua coloane: "Sezon", intre "Activitate" si "Data", randand `formatSezon(d.an_fiscal)` cand `an_fiscal` nu e null si `'-'` altfel; si "Metoda", intre "Suma" si "Status", randand `d.metoda_plata ?? '-'`. Actualizeaza si `colSpan`-urile daca exista.

Schimba conditia butonului "Confirma Plata" din `isAdminClub` in `(isAdminClub || isFederationAdmin)` — spec-ul cere explicit ca adminul federatiei sa poata confirma un decont istoric, iar cu conditia actuala butonul nu apare niciodata pentru el. `isFederationAdmin` este deja destructurat din `permissions` la linia 233.

Nu modifica filtrarea `filteredDeconturi` pe `currentUser.club_id` — ramane apararea de fatada peste gap-ul RLS consemnat ca T-29-07 in 29-01.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; grep -q "from('decont_sportivi')" components/FederationInvoices.tsx &amp;&amp; grep -q "confirmata_federatie" components/FederationInvoices.tsx &amp;&amp; grep -q "dovada_transfer_url" components/FederationInvoices.tsx &amp;&amp; grep -q "formatSezon" components/FederationInvoices.tsx &amp;&amp; ! grep -q "SportivSelectItem" components/FederationInvoices.tsx &amp;&amp; ! grep -q "selectedIds" components/FederationInvoices.tsx &amp;&amp; ! grep -q "setDecontSportivi" components/FederationInvoices.tsx &amp;&amp; ! grep -q "setDecontSportivi={setDecontSportivi}" components/AppRouter.tsx</automated>
    <human-check>
      Verificare la finalul fazei, ca SUPER_ADMIN_FEDERATIE si apoi ca ADMIN_CLUB:
      1. Meniu → Deconturi catre Federatie. Decontul FRQKD pentru sezonul 2025-2026 (clubul cu 37 de facturi istorice) apare in tabel, cu coloana "Sezon" aratand `2025-2026` si status NEACHITAT.
      2. Apasa "Confirma Plata". Modalul listeaza sportivii acoperiti cu nume complete, fara niciun checkbox, iar numarul afisat coincide cu `nr_participanti` din tabel.
      3. Butonul "Confirma si Incarca" ramane dezactivat cat timp nu ai ales metoda SI nu ai incarcat un fisier.
      4. Alege "Revolut", incarca o imagine, confirma. Randul devine ACHITAT si coloana "Metoda" arata "Revolut".
      5. Reincarca pagina: statusul si metoda persista.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` se incheie cu cod de iesire 0.
    - `components/FederationInvoices.tsx` nu mai contine `SportivSelectItem`, `selectedIds`, `handleSelectAll` sau `sportiviDisponibili`.
    - Componenta interogheaza `decont_sportivi` filtrat pe `decont_id` si nu mai insereaza niciodata in `decont_sportivi`.
    - Update-ul pe `deconturi_federatie` seteaza simultan `metoda_plata`, `status_plata`, `confirmata_federatie` si `dovada_transfer_url`.
    - `components/AppRouter.tsx` nu mai transmite `setDecontSportivi` sau `sportivi` catre `FederationInvoices`.
    - Tabelul randeaza `formatSezon(d.an_fiscal)` si `d.metoda_plata`.
    - Butonul de confirmare este vizibil si pentru `isFederationAdmin`.
    - Numele sportivilor sunt randate prin `formatNume` din `utils/formatareSportiv.ts`, nu prin concatenare ad-hoc.
  </acceptance_criteria>
  <done>Ecranul de deconturi functioneaza pe schema reala: lista sportivilor este automata, metoda de plata este obligatorie si persistata, iar dovada incarcata este legata de decont.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase Storage (`chitante_deconturi`) | fisier incarcat de utilizator, cu nume si continut necontrolate |
| browser → PostgREST `deconturi_federatie` UPDATE | clientul propune valorile pentru `metoda_plata` si `status_plata` |
| browser → PostgREST `decont_sportivi` SELECT | citirea listei de sportivi ai unui decont care poate apartine altui club |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-15 | Tampering | valoare arbitrara in `metoda_plata` | mitigate | constrangerea `deconturi_federatie_metoda_plata_check` din 29-01 respinge orice valoare in afara de `Cash`, `Transfer Bancar`, `Revolut` — controlul din UI este doar ergonomie, baza de date este autoritatea |
| T-29-16 | Tampering | marcarea unui decont ca platit fara dovada | mitigate | butonul de confirmare ramane `disabled` pana la upload reusit, iar update-ul ruleaza doar dupa ce `supabase.storage.upload` a returnat fara eroare; calea fisierului se persista in `dovada_transfer_url`, deci confirmarea devine auditabila |
| T-29-17 | Spoofing | utilizator neautorizat confirma plata | mitigate | randarea butonului este gardata de `isAdminClub \|\| isFederationAdmin`, iar politica RLS de UPDATE pe `deconturi_federatie` (din `create_missing_tables.sql`, aplicata live) este verificarea reala; controlul din UI nu este considerat securitate |
| T-29-18 | Information Disclosure | numele sportivilor altui club, expuse prin query-ul pe `decont_sportivi` | accept | politicile `Federatie - Full Access Decont Sportivi` si `Club - Manage Decont Sportivi` din `add_decont_sportivi.sql` limiteaza randurile la propriul club sau la SUPER_ADMIN_FEDERATIE; query-ul nu adauga expunere peste ce permite RLS |
| T-29-19 | Tampering | nume de fisier controlat de utilizator la upload | mitigate | calea este construita din `Date.now()` si extensia extrasa, sub prefixul `public/${club_id}/${decont.id}/` — numele original al fisierului nu ajunge niciodata in cale |
| T-29-20 | Denial of Service | fisier foarte mare incarcat | accept | atributul `accept` limiteaza tipurile, iar limita reala este cea a bucket-ului Supabase; nu se schimba fata de comportamentul actual |
| T-29-SC | Tampering | instalari npm/pip/cargo | accept | faza 29 nu instaleaza pachete noi |
</threat_model>

<verification>
1. `npm run lint` trece pe intregul proiect.
2. `grep -c "decont_sportivi" components/FederationInvoices.tsx` returneaza o valoare mai mare ca 0 si niciuna dintre aparitii nu este intr-un `insert` sau `upsert`.
3. Verificare live dupa o confirmare de test: `SELECT status_plata, metoda_plata, confirmata_federatie, dovada_transfer_url FROM deconturi_federatie WHERE id = <decont>` returneaza toate cele patru campuri populate.
4. Verificarea umana din Task 2, rulata la finalul fazei.
</verification>

<success_criteria>
- Tipurile TypeScript corespund schemei live dupa 29-01, iar `npm run lint` trece.
- Lista sportivilor acoperiti este automata si citita din `decont_sportivi`.
- Confirmarea platii scrie metoda, statusul, flag-ul de confirmare si calea dovezii intr-o singura operatie.
- Ecranul afiseaza sezonul in format `2025-2026` si metoda de plata.
- Adminul de federatie poate confirma deconturi.
</success_criteria>

<output>
Create `.planning/phases/29-taxa-anuala-federatie-frqkd-activare-automata-club-federatie/29-03-SUMMARY.md` when done.
</output>
