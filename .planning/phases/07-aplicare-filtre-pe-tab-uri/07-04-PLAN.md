---
phase: 07-aplicare-filtre-pe-tab-uri
plan: "04"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - components/Competitii/CategoriiTemplateManager.tsx
  - components/Competitii/index.tsx
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02

must_haves:
  truths:
    - "filterGenSet și setFilterGenSet eliminate din CategoriiTemplateManager"
    - "toggleFilterGen local eliminat"
    - "filtered useMemo folosește filtre.gen.has(t.gen) în loc de filterGenSet.has(t.gen)"
    - "nrFiltreActive local înlocuiește (filterGenSet.size > 0 ? 1 : 0) cu (nrFiltreActiveHook > 0 ? 1 : 0)"
    - "resetFiltre local elimină apelul setFilterGenSet(new Set()) și apelează resetFiltreHook() în schimb"
    - "filterTipProba, filterParticipare, filterVarsteValues, search, filtreVisible rămân locale — neschimbate"
    - "UI-ul vizual al panoului de filtre din Template rămâne identic (TMPL-02)"
    - "CompetitieFilterBar NU este montat în CategoriiTemplateManager"
  artifacts:
    - path: "components/Competitii/CategoriiTemplateManager.tsx"
      provides: "filterGenSet înlocuit cu filtre.gen din props"
      contains: "filtre.gen"
    - path: "components/Competitii/index.tsx"
      provides: "Props filtre/toggleGen/resetFiltre/nrFiltreActive pasate la CategoriiTemplateManager"
      contains: "filtre={filtre}"
  key_links:
    - from: "filtered useMemo"
      to: "filtre.gen"
      via: ".has(t.gen)"
      pattern: "filtre\\.gen\\.has"
    - from: "CategoriiTemplateManagerProps"
      to: "CompetitieFiltre"
      via: "filtre prop"
      pattern: "filtre: CompetitieFiltre"
---

<objective>
Migrarea parțială a CategoriiTemplateManager — înlocuiește EXCLUSIV filterGenSet cu filtre.gen din hook, fără a modifica restul filtrelor locale (filterTipProba, filterParticipare, filterVarsteValues, search) și fără a adăuga CompetitieFilterBar ca UI separat.

Purpose: TMPL-01 cere înlocuirea filtrelor gen/vârstă/grad cu useCompetitieFilters. Per deciziile pre-luate, în tab Template înlocuim NUMAI filterGenSet (singurul câmp cu corespondent direct în CompetitieFiltre). Celelalte filtre din Template (tip_proba string, participare, vârste discrete, search) nu au echivalent în hook. CompetitieFilterBar NU se montează — panoul vizual existent al Template-ului rămâne intact (TMPL-02).

Output: CategoriiTemplateManager cu filterGenSet eliminat și filtre.gen primit via props. UI-ul Template vizual identic cu ce există acum.
</objective>

<execution_context>
@C:/Users/lungu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lungu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-infrastructur-filtrare-unificat/06-01-SUMMARY.md
@.planning/phases/07-aplicare-filtre-pe-tab-uri/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorizare CategoriiTemplateManager — elimină filterGenSet, adaugă props filtre.gen</name>
  <files>components/Competitii/CategoriiTemplateManager.tsx</files>

  <read_first>
    components/Competitii/CategoriiTemplateManager.tsx liniile 230-270 (props interface + state declarations + toggleFilterGen)
    components/Competitii/CategoriiTemplateManager.tsx liniile 284-296 (filtered useMemo)
    components/Competitii/CategoriiTemplateManager.tsx liniile 380-394 (nrFiltreActive + resetFiltre local)
    hooks/useCompetitieFilters.ts liniile 5-12 (CompetitieFiltre interface)
  </read_first>

  <behavior>
    IDENTIFICARE props interface: CategoriiTemplateManager are o interfață de props (căutare: `interface CategoriiTemplateManagerProps` sau `export default function CategoriiTemplateManager(`) — adaugă 4 props noi:
    ```
    filtre: CompetitieFiltre;
    toggleGen: (gen: string) => void;
    resetFiltreHook: () => void;
    nrFiltreActiveHook: number;
    ```
    NOTĂ: Propul `resetFiltre` nu se poate numi `resetFiltre` deoarece există deja o funcție locală cu același nume. Se numește `resetFiltreHook` pentru a evita conflictul de nume. Alternativ, se poate redenumi funcția locală.

    ELIMINARE state local:
    - Linia 254: `const [filterGenSet, setFilterGenSet] = useState<Set<string>>(new Set());`

    ELIMINARE funcție locală:
    - Liniile 262-266: `const toggleFilterGen = (g: string) => setFilterGenSet(prev => { ... });`

    MODIFICARE filtered useMemo (linia 287):
    - `filterGenSet.size > 0 && !filterGenSet.has(t.gen)` → `filtre.gen.size > 0 && !filtre.gen.has(t.gen)`
    - Dependență array: înlocuiește `filterGenSet` cu `filtre` în `[templates, filterTipProba, filterGenSet, filterParticipare, filterVarsteValues, search]`

    MODIFICARE nrFiltreActive (linia ~382):
    - `(filterGenSet.size > 0 ? 1 : 0)` → `(nrFiltreActiveHook > 0 ? 1 : 0)`

    MODIFICARE resetFiltre local (linia ~388-394):
    - Înlocuiește `setFilterGenSet(new Set());` cu `resetFiltreHook();`
    - Restul liniilor din resetFiltre (setFilterTipProba, setFilterParticipare, setFilterVarsteValues, setSearch) rămân neschimbate

    ADĂUGIRE import type CompetitieFiltre (după linia 1 — după importul React):
    ```
    import type { CompetitieFiltre } from '../../hooks/useCompetitieFilters';
    ```

    CE NU SE MODIFICĂ (TMPL-02 — zero regresii vizuale):
    - filterTipProba + setFilterTipProba (tabs tip probă)
    - filterParticipare + setFilterParticipare (pills participare)
    - filterVarsteValues + toggleVarstaValue (grid vârste)
    - search + setSearch (input search)
    - filtreVisible + setFiltreVisible (toggle panou)
    - UI complet al panoului de filtre (tabs, pills gen existente, grid vârste, search)
    - CompetitieFilterBar NU se importă, NU se montează

    ATENȚIE: Pills de gen din UI (secțiunea gen în panoul de filtre existent) refereau `filterGenSet.has(g)` și `toggleFilterGen(g)`. Trebuie actualizate la:
    - `filterGenSet.has(g)` → `filtre.gen.has(g)`
    - `toggleFilterGen(g)` → `toggleGen(g)`
    Caută toate referințele la `filterGenSet` și `toggleFilterGen` în JSX și înlocuiește-le.
  </behavior>

  <action>
    Modifică components/Competitii/CategoriiTemplateManager.tsx:

    1. IMPORT: Adaugă `import type { CompetitieFiltre } from '../../hooks/useCompetitieFilters';` după importurile existente.

    2. PROPS INTERFACE: Localizează interfața sau signatura componentei și adaugă 4 props noi (filtre, toggleGen, resetFiltreHook, nrFiltreActiveHook). Asigură-te că `filtre: CompetitieFiltre` este în props interface și în destructurare.

    3. ELIMINĂ `filterGenSet` useState și `toggleFilterGen` funcție locală.

    4. filtered useMemo: Înlocuiește `filterGenSet` → `filtre.gen` în condiție și în deps array.

    5. nrFiltreActive: Înlocuiește contribuția `filterGenSet` cu `nrFiltreActiveHook`.

    6. resetFiltre local: Înlocuiește `setFilterGenSet(new Set())` cu `resetFiltreHook()`.

    7. JSX pills gen: Caută toate aparițiile `filterGenSet.has(g)` și `toggleFilterGen(g)` în JSX și înlocuiește cu `filtre.gen.has(g)` și `toggleGen(g)`.
  </action>

  <verify>
    <automated>cd C:/Users/lungu/portal-phihau && npm run lint</automated>
  </verify>

  <done>
    npm run lint zero erori. `filterGenSet` absent din fișier (zero aparițiii grep). `filtre.gen.has` prezent în filtered useMemo și în JSX. UI-ul panoului de filtre nemodificat structural.
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizare apel CategoriiTemplateManager în index.tsx cu props noi</name>
  <files>components/Competitii/index.tsx</files>

  <read_first>
    components/Competitii/index.tsx liniile 610-618 (apelul curent al CategoriiTemplateManager)
  </read_first>

  <behavior>
    La apelul `<CategoriiTemplateManager ...>` din index.tsx (linia ~611-618), adaugă 4 props noi:
    ```
    filtre={filtre}
    toggleGen={toggleGen}
    resetFiltreHook={resetFiltre}
    nrFiltreActiveHook={nrFiltreActive}
    ```
    Valorile `filtre`, `toggleGen`, `resetFiltre`, `nrFiltreActive` sunt din `useCompetitieFilters()` instanțiat în planul 07-01.
    NOTĂ: `resetFiltre` din hook se pasează ca `resetFiltreHook` pentru a evita conflictul de nume cu funcția locală din CategoriiTemplateManager.
    Props existente (permissions, competitieId, probe, categoriiExistente, onImported) rămân neschimbate.
  </behavior>

  <action>
    Modifică components/Competitii/index.tsx la apelul `<CategoriiTemplateManager`:

    Adaugă cele 4 props noi: `filtre={filtre}`, `toggleGen={toggleGen}`, `resetFiltreHook={resetFiltre}`, `nrFiltreActiveHook={nrFiltreActive}`.
  </action>

  <verify>
    <automated>cd C:/Users/lungu/portal-phihau && npm run lint</automated>
  </verify>

  <done>
    npm run lint zero erori. Apelul CategoriiTemplateManager include cele 4 props noi. Nicio eroare TypeScript "Missing required prop".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filtre.gen → filtered useMemo | filtre.gen este Set<string> din hook — același tip ca filterGenSet eliminat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | filtered useMemo cu filtre.gen | accept | Comparare string Set.has() — fără cast numeric, fără risc injection |
| T-07-04-02 | Spoofing | filtre props din CompetitieDetail | accept | Datele vin din useCompetitieFilters() instanțiat în același component tree |
</threat_model>

<verification>
1. `npm run lint` — zero erori TypeScript
2. `grep -n "filterGenSet" components/Competitii/CategoriiTemplateManager.tsx` — zero rezultate (eliminat complet)
3. `grep -n "toggleFilterGen" components/Competitii/CategoriiTemplateManager.tsx` — zero rezultate (eliminat)
4. `grep -n "filtre\.gen\.has" components/Competitii/CategoriiTemplateManager.tsx` — cel puțin 2 linii (useMemo + JSX)
5. `grep -n "filtre={filtre}" components/Competitii/index.tsx` — cel puțin 2 aparații (InscrieriView + CategoriiTemplateManager din 07-02 și 07-04)
6. `grep -n "resetFiltreHook" components/Competitii/index.tsx` — cel puțin 1 linie
</verification>

<success_criteria>
- filterGenSet și toggleFilterGen eliminate din CategoriiTemplateManager (TMPL-01)
- filtered useMemo folosește filtre.gen din props (TMPL-01)
- UI-ul panoului de filtre din Template rămâne vizual identic — filterTipProba/filterParticipare/filterVarsteValues/search neatinse (TMPL-02)
- CompetitieFilterBar absent din CategoriiTemplateManager (decizie pre-luată)
- index.tsx pasează cele 4 props noi la CategoriiTemplateManager
- npm run lint zero erori
</success_criteria>

<output>
Creează `.planning/phases/07-aplicare-filtre-pe-tab-uri/07-04-SUMMARY.md` când ai terminat.
</output>
