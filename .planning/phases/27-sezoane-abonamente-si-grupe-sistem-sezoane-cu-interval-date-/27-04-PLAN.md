---
phase: 27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-
plan: 04
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - utils/abonamente.ts
  - utils/abonamente.test.ts
  - components/Plati/PlatiScadente.tsx
  - components/Plati/GestiuneFacturi.tsx
  - components/Plati/LuniLipsaWizard.tsx
  - components/Plati/JurnalIncasari.tsx
autonomous: true
requirements: [SEZ-08, SEZ-09]

must_haves:
  truths:
    - "Generarea automata a facturilor nu mai poate alege un tip de abonament dintr-un sezon arhivat prin fallback silentios (RESEARCH Pitfall 1)"
    - "Un sportiv cu tip_abonament_id setat explicit este facturat cu acel tip, chiar daca apartine unui sezon arhivat, si adminul primeste un avertisment vizibil (Open Question 1, optiunea a)"
    - "Cluburile fara niciun sezon definit factureaza exact ca inainte de faza 27 (zero regresie)"
    - "Facturile deja emise raman neschimbate (SEZ-09)"
  artifacts:
    - path: "utils/abonamente.ts"
      provides: "regula unica de rezolvare a tipului de abonament pe sezonul activ"
      exports: ["filtreazaTipuriSezon", "gasesteTipDupaId", "esteTipDinSezonArhivat"]
    - path: "utils/abonamente.test.ts"
      provides: "test colocat pentru cele 3 functii pure"
      contains: "ruleazaTeste"
    - path: "components/Plati/PlatiScadente.tsx"
      provides: "generare facturi lunare filtrata pe sezonul activ + avertisment tip arhivat"
      contains: "filtreazaTipuriSezon"
  key_links:
    - from: "components/Plati/PlatiScadente.tsx"
      to: "utils/abonamente.ts"
      via: "filtreazaTipuriSezon inainte de orice .find() de facturare"
      pattern: "filtreazaTipuriSezon"
    - from: "components/Plati/GestiuneFacturi.tsx"
      to: "utils/abonamente.ts"
      via: "acelasi filtru inainte de calculul sumei"
      pattern: "filtreazaTipuriSezon"
    - from: "components/Plati/LuniLipsaWizard.tsx"
      to: "hooks/useSezoane.ts"
      via: "useSezonActiv(sportiv.club_id)"
      pattern: "useSezonActiv"
---

<objective>
Elimina bug-ul de facturare introdus de existenta mai multor tipuri de abonament "Individual" (unul per sezon, D-09): toate cele 4 fluxuri care calculeaza suma unui abonament rezolva tipul printr-o regula unica, filtrata pe sezonul activ al clubului, cu tipurile istorice (fara sezon) pastrate ca fallback compatibil.

Purpose: `PlatiScadente.tsx` linia 252 face azi `find(ab => ab.numar_membri === 1)` pe tot catalogul; din momentul in care fiecare sezon isi are propriile tipuri, acest fallback poate factura silentios pretul unui sezon arhivat (RESEARCH.md Pitfall 1 — bug financiar, nu cosmetic).
Output: `utils/abonamente.ts` + test colocat, si aplicarea regulii in `PlatiScadente.tsx`, `GestiuneFacturi.tsx`, `LuniLipsaWizard.tsx`, `JurnalIncasari.tsx`.

Decizie explicita pentru Open Question 1 din RESEARCH.md (soarta lui `sportivi.tip_abonament_id` la trecerea la sezon nou): **optiunea (a) — reasignare manuala**. Asignarea explicita a sportivului este respectata intocmai, chiar daca tipul apartine unui sezon arhivat; sistemul nu remapeaza automat nimic, dar semnaleaza vizibil cazurile la generarea facturilor. Aceasta alegere este consistenta cu D-07/D-08 (totul manual) si evita schimbari tacite de pret.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-CONTEXT.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-RESEARCH.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: utils/abonamente.ts — regula unica de rezolvare pe sezon + test colocat</name>
  <files>utils/abonamente.ts, utils/abonamente.test.ts</files>
  <read_first>
    - utils/parola.test.ts (structura obligatorie a testelor colocate: `assert()`, `ruleazaTeste()` exportat, auto-run pe `process.argv[1]`, iesire cu cod 1 la esec — proiectul NU are vitest/jest)
    - utils/luniLipsa.ts si utils/luniLipsa.test.ts (acelasi pattern, cu tipuri din `../types`)
    - types.ts (`interface TipAbonament` cu `sezon_id` adaugat in 27-01)
    - .planning/phases/27-.../27-RESEARCH.md sectiunea "Common Pitfalls" (Pitfall 1 si Pitfall 2)
  </read_first>
  <behavior>
    - `filtreazaTipuriSezon(tipuri, null)` returneaza toate tipurile, in aceeasi ordine (club fara sezoane / admin de federatie fara club activ = comportament identic cu inainte de faza 27).
    - `filtreazaTipuriSezon(tipuri, 'S2')` returneaza tipurile cu `sezon_id === 'S2'` SI pe cele cu `sezon_id` null/undefined (randuri istorice), dar exclude tipurile cu `sezon_id === 'S1'`.
    - `filtreazaTipuriSezon([], 'S2')` returneaza `[]`.
    - `gasesteTipDupaId(tipuri, 'T9')` returneaza tipul cu acel id chiar daca apartine sezonului arhivat 'S1' (asignarea explicita a sportivului e respectata — optiunea (a)).
    - `gasesteTipDupaId(tipuri, null)` si `gasesteTipDupaId(tipuri, undefined)` returneaza `undefined` (nu primul element, nu un tip cu id undefined).
    - `esteTipDinSezonArhivat(tip cu sezon_id 'S1', 'S2')` este `true`.
    - `esteTipDinSezonArhivat(tip cu sezon_id 'S2', 'S2')` este `false`; `esteTipDinSezonArhivat(tip cu sezon_id null, 'S2')` este `false`; `esteTipDinSezonArhivat(orice tip, null)` este `false`; `esteTipDinSezonArhivat(null, 'S2')` este `false`.
    - Nicio functie nu muteaza array-ul primit (verificat prin compararea lungimii si a ordinii originalului dupa apel).
  </behavior>
  <action>
Scrie INTAI `utils/abonamente.test.ts` cu cazurile din blocul `<behavior>` (folosind fixture-uri locale de tip `TipAbonament`: 'T1' Individual sezon 'S1' pret 100, 'T2' Individual sezon 'S2' pret 120, 'T9' Familie 2 sezon 'S1', 'T0' Individual fara sezon), ruleaza-l ca sa esueze (functiile nu exista inca), apoi implementeaza `utils/abonamente.ts` pana trece.

`utils/abonamente.ts` exporta exact trei functii pure, fara efecte secundare si fara acces la Supabase:
- `filtreazaTipuriSezon(tipuri: TipAbonament[], sezonActivId: string | null | undefined): TipAbonament[]` — cand `sezonActivId` e falsy returneaza o copie a listei; altfel `tipuri.filter(t => !t.sezon_id || t.sezon_id === sezonActivId)`.
- `gasesteTipDupaId(tipuri: TipAbonament[], tipAbonamentId: string | null | undefined): TipAbonament | undefined` — returneaza `undefined` cand id-ul e falsy, altfel `tipuri.find(t => t.id === tipAbonamentId)`. Se apeleaza intotdeauna pe lista COMPLETA, nu pe cea filtrata.
- `esteTipDinSezonArhivat(tip: TipAbonament | null | undefined, sezonActivId: string | null | undefined): boolean` — `true` doar cand exista si `tip.sezon_id` si `sezonActivId` si sunt diferite.

Comentariul de fisier trebuie sa documenteze explicit contractul de folosire in fluxurile de facturare: cautarea dupa `tip_abonament_id` se face pe lista completa (respecta asignarea manuala, optiunea (a) din Open Question 1), iar ORICE fallback pe `numar_membri` se face exclusiv pe lista intoarsa de `filtreazaTipuriSezon` (aici era bug-ul din Pitfall 1).

Testul se incheie cu blocul de auto-run identic cu cel din `utils/parola.test.ts` (verificare `process.argv[1]` pe `abonamente.test.ts`/`.js`, `process.exit(1)` la erori).
  </action>
  <verify>
    <automated>node --import tsx utils/abonamente.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx utils/abonamente.test.ts` iese cu cod 0 si raporteaza cel putin 8 asertii PASS, acoperind fiecare punct din `<behavior>`.
    - `utils/abonamente.ts` exporta exact `filtreazaTipuriSezon`, `gasesteTipDupaId`, `esteTipDinSezonArhivat` si nu importa `supabaseClient`.
    - `filtreazaTipuriSezon` cu `sezonActivId` falsy returneaza aceleasi elemente, in aceeasi ordine, ca lista de intrare.
    - `npm run lint` trece.
  </acceptance_criteria>
  <done>Exista o singura sursa de adevar, testata, pentru "care tipuri de abonament sunt curente" si "acest tip provine dintr-un sezon arhivat".</done>
</task>

<task type="auto">
  <name>Task 2: PlatiScadente + GestiuneFacturi — filtrare pe sezon inaintea oricarui fallback</name>
  <files>components/Plati/PlatiScadente.tsx, components/Plati/GestiuneFacturi.tsx</files>
  <read_first>
    - components/Plati/PlatiScadente.tsx liniile 70-90 (destructurarea `useData()`, linia 81 `tipuriAbonament`) si 200-340 (generarea facturilor: ramura familie 216-248, ramura individuala 250-275, avertismentul `sportiviIgnorati` 332-336)
    - components/Plati/GestiuneFacturi.tsx liniile 40-50 (`useData()`), 92-110 si 270-285 (cele doua locuri cu acelasi calcul de suma)
    - utils/abonamente.ts (creat la Task 1)
    - hooks/useSezoane.ts (`useSezonActiv`)
  </read_first>
  <action>
`components/Plati/PlatiScadente.tsx`:
1. Importa `filtreazaTipuriSezon`, `gasesteTipDupaId`, `esteTipDinSezonArhivat` din `../../utils/abonamente` si `useSezonActiv` din `../../hooks/useSezoane`. Adauga, langa celelalte hook-uri de la inceputul componentei (inainte de orice `return` timpuriu — Rules of Hooks), `const { sezonActivId } = useSezonActiv(activeRoleContext?.club_id ?? currentUser?.club_id ?? null);`.
2. In functia de generare, imediat dupa `const sportiviIgnorati: string[] = [];` (linia ~208), adauga `const tipuriSezon = filtreazaTipuriSezon(tipuriAbonament || [], sezonActivId);` si `const sportiviCuTipArhivat: string[] = [];`.
3. Ramura familie (liniile 222-225): inlocuieste ambele referinte `(tipuriAbonament || [])` cu `tipuriSezon` — atat `find(ab => ab.numar_membri === nrMembri)` cat si fallback-ul sortat descrescator pe `numar_membri > 1`. Semantica lantului de fallback ramane identica; se schimba doar multimea pe care se cauta.
4. Ramura individuala (liniile 252-253): inlocuieste expresia cu `const abonamentConfig = gasesteTipDupaId(tipuriAbonament || [], sportiv.tip_abonament_id) || tipuriSezon.find(ab => ab.numar_membri === 1);`. Imediat dupa, daca `esteTipDinSezonArhivat(abonamentConfig, sezonActivId)` este true, adauga `${sportiv.nume} ${sportiv.prenume}` in `sportiviCuTipArhivat`.
5. Langa avertismentul existent pentru `sportiviIgnorati` (liniile 332-336), adauga un al doilea avertisment, cu acelasi mecanism de afisare, cand `sportiviCuTipArhivat.length > 0`, cu textul: `${sportiviCuTipArhivat.length} sportivi facturați cu un tip de abonament dintr-un sezon arhivat (reasignare manuală necesară): ${sportiviCuTipArhivat.join(', ')}`. Acest avertisment este contrapartida vizibila a deciziei "reasignare manuala" (optiunea (a)) — fara el, cazul ar ramane silentios, exact defectul semnalat de Pitfall 2.

`components/Plati/GestiuneFacturi.tsx`:
6. Aceleasi importuri; `const { sezonActivId } = useSezonActiv(activeRoleContext?.club_id ?? null);` langa `useData()`.
7. In blocul de la liniile 97-106: calculeaza `const tipuriSezon = filtreazaTipuriSezon(tipuriAbonament, sezonActivId);` si inlocuieste `tipuriAbonament.find(ab => ab.numar_membri === nr) || tipuriAbonament.find(ab => ab.numar_membri === 1)` cu aceleasi doua `.find()` pe `tipuriSezon`, iar `tipuriAbonament.find(ab => ab.id === sportiv.tip_abonament_id)` cu `gasesteTipDupaId(tipuriAbonament, sportiv.tip_abonament_id)`.
8. Repeta exact aceeasi transformare in al doilea loc, liniile 274-280. Adauga `sezonActivId` in array-ul de dependinte al `useMemo`/`useEffect` care contine deja `tipuriAbonament` (linia ~142) ca sa se recalculeze la schimbarea sezonului.
9. Nu modifica formulele de pret, reducerile, descrierile generate sau structura payload-ului `plati` — singura schimbare este multimea de tipuri pe care se face cautarea.
  </action>
  <verify>
    <automated>npm run lint && grep -c "filtreazaTipuriSezon" components/Plati/PlatiScadente.tsx && grep -c "gasesteTipDupaId" components/Plati/PlatiScadente.tsx && grep -c "sezon arhivat" components/Plati/PlatiScadente.tsx && test "$(grep -c "filtreazaTipuriSezon" components/Plati/GestiuneFacturi.tsx)" -ge "1" && test "$(grep -n "numar_membri === 1" components/Plati/PlatiScadente.tsx components/Plati/GestiuneFacturi.tsx | grep -c "tipuriAbonament")" = "0"</automated>
    <human-check>Cu doua sezoane in club, fiecare cu un tip "Individual" de pret diferit, genereaza facturile lunare: sumele corespund pretului din sezonul ACTIV, iar sportivii ramasi pe tipul vechi apar in avertismentul de reasignare.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece.
    - Nicio linie din cele doua fisiere nu mai contine un `.find(ab => ab.numar_membri === ...)` aplicat direct pe `tipuriAbonament` (toate fallback-urile ruleaza pe `tipuriSezon`).
    - Cautarea dupa `tip_abonament_id` foloseste `gasesteTipDupaId` pe lista completa, nu pe cea filtrata.
    - `PlatiScadente.tsx` colecteaza si afiseaza lista sportivilor facturati cu un tip dintr-un sezon arhivat.
    - Hook-ul `useSezonActiv` este apelat neconditionat, la inceputul componentei (fara return timpuriu inaintea lui).
  </acceptance_criteria>
  <done>Cele doua fluxuri principale de emitere a facturilor nu mai pot cadea pe pretul unui sezon arhivat, iar exceptiile ramase sunt vizibile pentru admin.</done>
</task>

<task type="auto">
  <name>Task 3: LuniLipsaWizard + JurnalIncasari — acelasi contract de rezolvare</name>
  <files>components/Plati/LuniLipsaWizard.tsx, components/Plati/JurnalIncasari.tsx</files>
  <read_first>
    - components/Plati/LuniLipsaWizard.tsx liniile 1-100 (props `sportiv`/`tipuriAbonament`, functia `calculeazaSuma` la 79-91)
    - components/Plati/JurnalIncasari.tsx liniile 160-300 (semnatura componentei la 172, calculul de suma la 249-258, array-ul de dependinte la 292)
    - utils/abonamente.ts (creat la Task 1)
    - hooks/useSezoane.ts (`useSezonActiv`)
    - contexts/DataContext.tsx (`useData` — sursa `activeRoleContext` pentru JurnalIncasari, care nu il primeste prin props)
  </read_first>
  <action>
`components/Plati/LuniLipsaWizard.tsx`:
1. Importa `filtreazaTipuriSezon`, `gasesteTipDupaId` din `../../utils/abonamente` si `useSezonActiv` din `../../hooks/useSezoane`. Adauga `const { sezonActivId } = useSezonActiv(sportiv.club_id ?? null);` langa celelalte hook-uri, inaintea oricarui return conditionat (componenta are ramuri de early-return pentru `isLoadingData` — hook-ul trebuie sa fie deasupra lor).
2. In `calculeazaSuma` (liniile 79-91) calculeaza `const tipuriSezon = filtreazaTipuriSezon(tipuriAbonament, sezonActivId);` si inlocuieste, pe ambele ramuri (familie si individual), `tipuriAbonament.find(ab => ab.id === sportiv.tip_abonament_id)` cu `gasesteTipDupaId(tipuriAbonament, sportiv.tip_abonament_id)` si `?? tipuriAbonament.find(ab => ab.numar_membri === 1)` cu `?? tipuriSezon.find(ab => ab.numar_membri === 1)`. Clubul folosit este cel al SPORTIVULUI (`sportiv.club_id`), nu contextul activ — wizardul poate fi deschis de un admin de federatie pe un sportiv din alt club.

`components/Plati/JurnalIncasari.tsx`:
3. Adauga importurile `useData` din `../../contexts/DataContext`, `useSezonActiv` din `../../hooks/useSezoane` si helperele din `../../utils/abonamente`. In corpul componentei, imediat dupa destructurarea props-urilor si inaintea oricarui `return` timpuriu, adauga `const { activeRoleContext } = useData();` si `const { sezonActivId } = useSezonActiv(activeRoleContext?.club_id ?? currentUser?.club_id ?? null);`.
4. In blocul de calcul al sumei (liniile 249-258): `const tipuriSezon = filtreazaTipuriSezon(tipuriAbonament, sezonActivId);`, apoi ramura familie foloseste `tipuriSezon.find(ab => ab.numar_membri === nr) || tipuriSezon.find(ab => ab.numar_membri === 1)`, iar ramura individuala `gasesteTipDupaId(tipuriAbonament, sportiv.tip_abonament_id)`.
5. Adauga `sezonActivId` in array-ul de dependinte al `useMemo`/`useEffect` de la linia ~292 (cel care contine deja `tipuriAbonament`).
6. In ambele fisiere: nu schimba nimic la reduceri, la formatarea descrierii sau la salvarea propriu-zisa a platii.
  </action>
  <verify>
    <automated>npm run lint && grep -c "filtreazaTipuriSezon" components/Plati/LuniLipsaWizard.tsx && grep -c "filtreazaTipuriSezon" components/Plati/JurnalIncasari.tsx && test "$(grep -n "numar_membri === 1" components/Plati/LuniLipsaWizard.tsx components/Plati/JurnalIncasari.tsx | grep -c "tipuriAbonament\.")" = "0" && grep -c "sezonActivId" components/Plati/JurnalIncasari.tsx</automated>
    <human-check>Deschide wizardul "Luni fără factură" pentru un sportiv fara tip asignat: suma propusa corespunde tipului Individual din sezonul activ, nu unuia dintr-un sezon vechi.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece.
    - In niciunul dintre cele doua fisiere nu mai exista `tipuriAbonament.find(ab => ab.numar_membri === ...)` ca fallback.
    - `LuniLipsaWizard` deriva sezonul din `sportiv.club_id`, nu din contextul activ.
    - `JurnalIncasari` apeleaza `useData()` si `useSezonActiv` inaintea oricarui return conditionat.
    - `sezonActivId` apare in dependintele memo-ului care recalculeaza suma.
  </acceptance_criteria>
  <done>Toate cele 4 fluxuri de facturare identificate in RESEARCH Pitfall 1 folosesc aceeasi regula de rezolvare a tipului de abonament.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog `tipuriAbonament` din cache client -> suma facturata | Datele vin din `useDataProvider` (cache React Query); o presupunere gresita produce facturi cu suma gresita |
| sportiv din alt club (admin federatie) -> sezonul folosit la calcul | Sezonul trebuie derivat din clubul entitatii facturate, nu global |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-13 | Tampering (integritate financiara) | Fallback silentios pe primul tip cu `numar_membri === 1` din alt sezon | mitigate | `filtreazaTipuriSezon` aplicat inaintea fiecarui fallback in toate cele 4 fisiere; gate automat care interzice `tipuriAbonament.find(... numar_membri ...)` |
| T-27-14 | Repudiation | Sportiv facturat pe un pret vechi fara urma vizibila | mitigate | `esteTipDinSezonArhivat` + avertisment cu lista nominala la generarea facturilor in `PlatiScadente.tsx` |
| T-27-15 | Information Disclosure | Wizardul de luni lipsa foloseste sezonul altui club pentru un sportiv strain | mitigate | `useSezonActiv(sportiv.club_id)` in `LuniLipsaWizard`; RLS `sezoane_select` blocheaza oricum citirea sezoanelor unui club fara acces |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | Zero pachete noi instalate in acest plan |
</threat_model>

<verification>
- `node --import tsx utils/abonamente.test.ts` -> exit 0, toate asertiile PASS.
- `npm run lint` -> zero erori.
- Cautare in cod: zero aparitii de `tipuriAbonament.find(ab => ab.numar_membri` in `components/Plati/` (toate fallback-urile ruleaza pe lista filtrata).
- Regresie: pentru un club fara niciun rand in `sezoane`, sumele generate raman identice cu cele de dinaintea fazei (verificat prin `filtreazaTipuriSezon(..., null)` care returneaza lista completa).
</verification>

<success_criteria>
- Cele 4 fluxuri de facturare rezolva tipul de abonament prin acelasi contract testat.
- Un tip dintr-un sezon arhivat nu mai poate fi ales prin fallback; poate fi folosit doar prin asignare explicita, si atunci cazul este raportat adminului.
- Cluburile fara sezoane definite si facturile deja emise raman neafectate.
</success_criteria>

## Artifacts this phase produces

Simboluri noi create in acest plan:

- Fisiere noi: `utils/abonamente.ts`, `utils/abonamente.test.ts`
- Functii noi: `filtreazaTipuriSezon`, `gasesteTipDupaId`, `esteTipDinSezonArhivat`, `ruleazaTeste` (in `utils/abonamente.test.ts`)
- Variabile locale noi in fluxurile de facturare: `tipuriSezon`, `sezonActivId`, `sportiviCuTipArhivat`

<output>
Create `.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-04-SUMMARY.md` when done
</output>
