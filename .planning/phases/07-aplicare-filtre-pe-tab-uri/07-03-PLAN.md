---
phase: 07-aplicare-filtre-pe-tab-uri
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - components/Competitii/RaportInscrieri.tsx
  - components/Competitii/index.tsx
autonomous: true
requirements:
  - RAP-01
  - RAP-02

must_haves:
  truths:
    - "RaportInscrieriProps are 6 props noi: filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive, grade"
    - "CompetitieFilterBar randat deasupra listei de sportivi din raport"
    - "filteredIns și filteredEc filtrate suplimentar prin categoriiVizibile (Set<string> din aplicaFiltreCategorie)"
    - "raportul agregat reflectă filtrele active — sportivii din categorii excluse nu apar în raport"
    - "index.tsx pasează grade={grade} la RaportInscrieri"
  artifacts:
    - path: "components/Competitii/RaportInscrieri.tsx"
      provides: "Props noi + CompetitieFilterBar + filtrare via categoriiVizibile"
      contains: "CompetitieFilterBar"
    - path: "components/Competitii/index.tsx"
      provides: "grade={grade} pasat la RaportInscrieri"
      contains: "grade={grade}"
  key_links:
    - from: "RaportInscrieri"
      to: "aplicaFiltreCategorie"
      via: "useMemo categoriiVizibile"
      pattern: "aplicaFiltreCategorie"
    - from: "filteredIns"
      to: "categoriiVizibile"
      via: ".has(i.categorie_id)"
      pattern: "categoriiVizibile\\.has"
---

<objective>
Adăugare netă de filtrare în RaportInscrieri — fișierul nu are niciun filtru existent, deci nu există eliminări sau riscuri de regresie. Se adaugă 6 props noi, CompetitieFilterBar deasupra listei și filtrarea datelor prin categoriiVizibile.

Purpose: RAP-01 și RAP-02 cer bara de filtre și aplicarea filtrelor pe datele de raport. Raportul agregă sportivi per categorie — filtrarea se face pe categoriile vizibile (la fel ca în InscrieriView), nu direct pe sportivi.

Output: RaportInscrieri cu CompetitieFilterBar funcțional și filteredIns/filteredEc filtrate prin categoriiVizibile. index.tsx actualizat cu prop grade nou la apelul RaportInscrieri.
</objective>

<execution_context>
@C:/Users/lungu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lungu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-infrastructur-filtrare-unificat/06-01-SUMMARY.md
@.planning/phases/07-aplicare-filtre-pe-tab-uri/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adăugare props noi + filtrare categorii + CompetitieFilterBar în RaportInscrieri</name>
  <files>components/Competitii/RaportInscrieri.tsx</files>

  <read_first>
    components/Competitii/RaportInscrieri.tsx liniile 1-38 (imports, props interface, filteredIns/filteredEc)
    components/Competitii/RaportInscrieri.tsx liniile 80-107 (zona return: early return + header cu buton Imprimă)
    hooks/useCompetitieFilters.ts liniile 5-12 (CompetitieFiltre interface)
    components/Competitii/CompetitieFilterBar.tsx liniile 5-13 (CompetitieFilterBarProps)
  </read_first>

  <behavior>
    IMPORTURI noi (după linia 1 — înlocuiește importul existent de tipuri sau adaugă după):
    - Adaugă `Grad` la importul din `../../types` (linia 2)
    - Adaugă import `aplicaFiltreCategorie` și type `CompetitieFiltre` din `../../hooks/useCompetitieFilters`
    - Adaugă import `CompetitieFilterBar` din `./CompetitieFilterBar`
    - Adaugă `useMemo` la importul React (linia 1 — deja prezent, nu duplica)

    MODIFICARE RaportInscrieriProps (liniile 4-12): Adaugă 6 câmpuri noi la interfață:
    ```
    filtre: CompetitieFiltre;
    toggleGen: (gen: string) => void;
    setFiltre: (partial: Partial<CompetitieFiltre>) => void;
    resetFiltre: () => void;
    nrFiltreActive: number;
    grade: Grad[];
    ```

    MODIFICARE destructurare componentă (linia 14-15): Adaugă cele 6 props noi în parametrii.

    ADĂUGIRE categoriiVizibile useMemo (după destructurare, înainte de filteredIns):
    ```
    const categoriiVizibile = useMemo(
      () => new Set(aplicaFiltreCategorie(categorii, filtre).map(c => c.id)),
      [categorii, filtre]
    );
    ```
    NOTĂ: Spre deosebire de InscrieriView, `categoriiVizibile` este MEREU un Set (nu null) — `aplicaFiltreCategorie` returnează `categorii` întreg când zero filtre active. Deci `.has()` funcționează corect în ambele cazuri (cu sau fără filtre).

    MODIFICARE filteredIns (linia 32-34): Adaugă condiție suplimentară:
    ```
    const filteredIns = inscrieri.filter(i =>
      i.status?.toLowerCase() !== 'retras' &&
      (isAdmin || i.club_id === myClubId) &&
      categoriiVizibile.has(i.categorie_id)
    );
    ```

    MODIFICARE filteredEc (linia 35-37): Adaugă condiție suplimentară:
    ```
    const filteredEc = echipe.filter(e =>
      e.status?.toLowerCase() !== 'retrasa' &&
      (isAdmin || e.club_id === myClubId) &&
      categoriiVizibile.has(e.categorie_id)
    );
    ```

    ADĂUGIRE CompetitieFilterBar în JSX: Imediat după header-ul cu butonul Imprimă (după `</div>` care închide `<div className="flex items-center justify-between...">`), ÎNAINTE de `<div className="border border-[var(--t-border)]...">`:
    ```
    <CompetitieFilterBar
      filtre={filtre}
      toggleGen={toggleGen}
      setFiltre={setFiltre}
      resetFiltre={resetFiltre}
      nrFiltreActive={nrFiltreActive}
      probe={probe}
      grade={grade}
    />
    ```

    ATENȚIE early return: Componenta are `if (raport.length === 0) return (...)` la linia 82. `raport` se calculează din `filteredIns` și `filteredEc` — deci dacă toate categoriile sunt filtrate, raportul devine gol și se afișează mesajul "Niciun sportiv". Acest comportament este corect și dorit.
    ATENȚIE: `useMemo` este deja importat pe linia 1 — NU mai adăuga a doua oară.
  </behavior>

  <action>
    Modifică components/Competitii/RaportInscrieri.tsx:

    1. IMPORTURI: Adaugă `Grad` la importul din types; adaugă import de `aplicaFiltreCategorie` + type `CompetitieFiltre` din hook; adaugă import `CompetitieFilterBar`.

    2. RaportInscrieriProps: Extinde interfața cu cele 6 props noi (filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive, grade).

    3. Destructurare: Adaugă cele 6 props noi în parametrii funcției componentă.

    4. categoriiVizibile: Adaugă useMemo care returnează `Set<string>` (mereu prezent — nu null). Dependențe: `[categorii, filtre]`.

    5. filteredIns și filteredEc: Adaugă `.has(i.categorie_id)` / `.has(e.categorie_id)` via `categoriiVizibile`.

    6. JSX: Inserează `<CompetitieFilterBar .../>` cu 7 props între header (div cu buton Imprimă) și lista de sportivi (div cu border). Respectă ordinea: header → filter bar → lista.
  </action>

  <verify>
    <automated>cd C:/Users/lungu/portal-phihau && npm run lint</automated>
  </verify>

  <done>
    npm run lint zero erori. RaportInscrieriProps are 6 props noi. CompetitieFilterBar prezent în JSX. filteredIns și filteredEc filtrează prin categoriiVizibile.has().
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizare apel RaportInscrieri în index.tsx cu grade={grade}</name>
  <files>components/Competitii/index.tsx</files>

  <read_first>
    components/Competitii/index.tsx liniile 514-525 (apelul curent al RaportInscrieri)
  </read_first>

  <behavior>
    La apelul `<RaportInscrieri ...>` din index.tsx (linia ~516-524), adaugă prop `grade={grade}`.
    `grade` este deja disponibil în CompetitieDetail via `const { ..., grade, ... } = useData()` (linia 49).
    Props existente (competitie, categorii, probe, inscrieri, echipe, isAdmin, myClubId) rămân neschimbate.
    NOTĂ: Filtrele (filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive) NU se pasează ca props la RaportInscrieri — componenta își instanțiază propriul useCompetitieFilters() intern (per Task 1 din 07-03). Aceasta este diferit de InscrieriView care primește filtrele ca props.
  </behavior>

  <action>
    Modifică components/Competitii/index.tsx la apelul `<RaportInscrieri`:

    Adaugă `grade={grade}` la lista de props existente.

    NOTĂ IMPORTANTĂ: RaportInscrieri își instanțiază propriul `useCompetitieFilters()` intern (per PATTERNS.md linia 163: hook call în interiorul componentei). Spre deosebire de InscrieriView (unde hook-ul este la nivel de CompetitieDetail și filtrele se pasează ca props), RaportInscrieri are filtre locale independente. Singurul prop nou din index.tsx este `grade={grade}`.
  </action>

  <verify>
    <automated>cd C:/Users/lungu/portal-phihau && npm run lint</automated>
  </verify>

  <done>
    npm run lint zero erori. Apelul RaportInscrieri din index.tsx include `grade={grade}`. Nicio eroare TypeScript de "Missing required prop grade".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → filtre RaportInscrieri | filtre locale în RaportInscrieri (useCompetitieFilters propriu) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | categoriiVizibile Set.has() | mitigate | aplicaFiltreCategorie are garduri isNaN (Phase 6) |
| T-07-03-02 | Information Disclosure | filtre pe date raport | accept | Date deja în memorie, nicio suprafață nouă |
</threat_model>

<verification>
1. `npm run lint` — zero erori TypeScript
2. `grep -n "CompetitieFilterBar" components/Competitii/RaportInscrieri.tsx` — cel puțin 2 linii (import + JSX)
3. `grep -n "aplicaFiltreCategorie" components/Competitii/RaportInscrieri.tsx` — cel puțin 1 linie
4. `grep -n "categoriiVizibile" components/Competitii/RaportInscrieri.tsx` — cel puțin 3 linii (useMemo + filteredIns + filteredEc)
5. `grep -n "grade={grade}" components/Competitii/index.tsx` — cel puțin 1 linie (în apelul RaportInscrieri)
</verification>

<success_criteria>
- RaportInscrieriProps extinsă cu 6 props noi (inclusiv grade: Grad[])
- CompetitieFilterBar montat în RaportInscrieri (RAP-01)
- filteredIns și filteredEc filtrate prin categoriiVizibile.has() (RAP-02)
- raportul agregat reflectă filtrele — sportivii din categorii excluse sunt absenți
- index.tsx pasează grade={grade} la RaportInscrieri
- npm run lint zero erori
</success_criteria>

<output>
Creează `.planning/phases/07-aplicare-filtre-pe-tab-uri/07-03-SUMMARY.md` când ai terminat.
</output>
