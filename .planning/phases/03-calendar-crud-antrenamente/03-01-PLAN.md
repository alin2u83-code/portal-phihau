---
phase: 03-calendar-crud-antrenamente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Grupe/GrupaDetailView.tsx
autonomous: false
requirements: [CAL-01, CAL-02, CAL-03, CAL-04]
tags: [calendar, antrenamente, grupe, crud, supabase]

must_haves:
  truths:
    - "Tab Antrenamente afișează un calendar lunar cu dot-uri verzi (planificat/efectuat) și roșii (anulat) pe zilele cu antrenamente"
    - "Săgețile prev/next schimbă luna afișată, header arată 'Lună An' în română (ex: Iunie 2026)"
    - "Clic pe o zi din calendar afișează panel fix dedesubt cu antrenamentele zilei"
    - "Pe fiecare antrenament: ora start–sfârșit, badge status, butoane Anulează/Șterge (și Reactivează dacă anulat)"
    - "Butonul Anulează deschide modal cu textarea motiv opțional → status devine 'anulat' și dot devine roșu"
    - "Butonul Reactivează pe antrenament anulat → status redevine 'planificat', motiv_anulare devine null, dot redevine verde"
    - "ConfirmButton Șterge elimină definitiv antrenamentul din DB și din calendar"
    - "Butonul Adaugă Antrenament (header + empty state) deschide modal one-off → INSERT cu is_recurent=false → apare în calendar"
  artifacts:
    - path: "components/Grupe/GrupaDetailView.tsx"
      provides: "TabAntrenamente funcțional cu CalendarGrid, DayPanel, ModalAdaugare, ModalAnulare + buton header Adaugă Antrenament"
      contains: "useCalendarView"
  key_links:
    - from: "components/Grupe/GrupaDetailView.tsx (TabAntrenamente)"
      to: "hooks/useCalendarView.ts"
      via: "useCalendarView(grupa.id) — fetch lunar + handleSaveCustom"
      pattern: "useCalendarView\\(grupa\\.id\\)"
    - from: "components/Grupe/GrupaDetailView.tsx (handleAnulare/handleReactivare/handleStergere)"
      to: "supabase program_antrenamente"
      via: "supabase.from('program_antrenamente').update/delete"
      pattern: "program_antrenamente'\\)\\.(update|delete)"
---

<objective>
Înlocuiește placeholder-ul `TabAntrenamente` din `GrupaDetailView.tsx` cu un calendar lunar funcțional complet: grid Tailwind cu dot-uri colorate, panel cu antrenamentele zilei selectate, și CRUD complet (adăugare one-off, anulare cu motiv opțional, reactivare, ștergere hard) pe tabelul `program_antrenamente`.

Purpose: Adminul/instructorul poate vedea, adăuga și anula antrenamente dintr-un calendar lunar per grupă — closing the last incomplete success criterion al modulului Grupe.
Output: `components/Grupe/GrupaDetailView.tsx` modificat — `TabAntrenamente` complet + buton Adaugă Antrenament în header-ul GrupaDetailView.
</objective>

<phase_goal>
**As a** admin sau instructor de club, **I want to** vedea, adăuga, anula, reactiva și șterge antrenamente dintr-un calendar lunar per grupă, **so that** gestionez complet programul real al antrenamentelor (nu doar șablonul recurent din tab Orar).
</phase_goal>

<artifacts_this_phase_produces>
Simboluri noi create în această fază (toate în `components/Grupe/GrupaDetailView.tsx`, cu excepția unde se notează altfel). Niciun simbol exportat nou la nivel de modul — toate sunt componente/funcții interne fișierului:

- `TabAntrenamente` — componentă internă rescrisă de la placeholder la implementare completă (props extinse: `grupa`, `isModalAdaugareOpen`, `setIsModalAdaugareOpen`)
- `CalendarGrid` — sub-componentă internă (grid lunar Tailwind)
- `DayPanel` — sub-componentă internă (panel antrenamente zi selectată)
- `ModalAdaugare` — sub-componentă internă (form one-off)
- `ModalAnulare` — sub-componentă internă (textarea motiv)
- `getCalendarCells(year, month)` — funcție pură internă (offset Luni-first)
- `antrenamenteByDate` — useMemo intern (Record<string, Antrenament[]>)
- `statusVariant(status)` — helper intern (mapează status → variant Badge)
- `navigateMonth(direction)` — funcție internă (prev/next lună + reset selectedDate)
- `handleAnulare(id, motiv)`, `handleReactivare(id)`, `handleStergere(id)` — funcții async interne (mutații Supabase)
- `handleSubmitAdaugare()` — funcție internă (apel handleSaveCustom cu is_recurent=false)
- `LUNI_RO` — constantă array (12 luni în română)
- State nou ridicat în `GrupaDetailView`: `isModalAdaugareOpen` / `setIsModalAdaugareOpen` (useState)

Nu se creează fișiere noi. Nu se modifică `hooks/useCalendarView.ts` (D-13: funcțiile mutație se definesc local în TabAntrenamente).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calendar-crud-antrenamente/03-CONTEXT.md
@.planning/phases/03-calendar-crud-antrenamente/03-RESEARCH.md
@.planning/phases/03-calendar-crud-antrenamente/03-PATTERNS.md
@.planning/phases/03-calendar-crud-antrenamente/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CalendarGrid lunar cu dot-uri + navigare lună (slice vizibil CAL-01)</name>
  <files>components/Grupe/GrupaDetailView.tsx</files>
  <read_first>
    - components/Grupe/GrupaDetailView.tsx — fișierul de modificat; placeholder TabAntrenamente liniile 24-36, pattern TabOrar/TabSportivi, header liniile 252-260, mount tab liniile 280-285
    - hooks/useCalendarView.ts — hook reutilizat: `date`, `setDate`, `selectedDate`, `setSelectedDate`, `todayLocal`, `antrenamente`, `loading`, `fetchAntrenamente`, `handleSaveCustom(data, clubId?)`
    - types.ts liniile 341-363 — interface `Antrenament` (status?, motiv_anulare?, data, ora_start, ora_sfarsit)
    - .planning/phases/03-calendar-crud-antrenamente/03-RESEARCH.md — Pattern 1 (getCalendarCells offset `(getDay()+6)%7`), Pattern 2 (navigateMonth), Pattern 3 (antrenamenteByDate), Pattern 6 (statusVariant); Pitfall 1, 2, 3, 4
    - .planning/phases/03-calendar-crud-antrenamente/03-UI-SPEC.md — Layout Calendar Grid, Color (emerald-400/rose-400 dots, ring-indigo today/selected), Typography header, Copywriting (LUNI_RO, "L M M J V S D")
    - components/icons.tsx liniile 14, 43-44 — `PlusIcon`, `ChevronLeftIcon`, `ChevronRightIcon`
  </read_first>
  <action>
    Rescrie componenta `TabAntrenamente` (liniile 24-36) de la placeholder la componentă funcțională cu signature `TabAntrenamente: React.FC<{ grupa: GrupaWithDetails; isModalAdaugareOpen: boolean; setIsModalAdaugareOpen: (v: boolean) => void }>`. Consumă `useCalendarView(grupa.id)` (D-13: reutilizare directă, NU crea useQuery local).

    Adaugă la importurile existente din linia 1-8: `useEffect` din react; `Modal, ConfirmButton, Badge` din `'../ui'`; `XCircleIcon, ChevronLeftIcon, ChevronRightIcon` din `'../icons'`; `useCalendarView` din `'../../hooks/useCalendarView'`; `formatTime` din `'../../utils/date'`; tipul `Antrenament` din `'../../types'`.

    Definește constanta module-level `LUNI_RO` = array cu 12 luni în română: 'Ianuarie','Februarie','Martie','Aprilie','Mai','Iunie','Iulie','August','Septembrie','Octombrie','Noiembrie','Decembrie'.

    Definește funcția pură module-level `getCalendarCells(year: number, month: number): (string | null)[]` conform RESEARCH.md Pattern 1: offset prima zi `(new Date(year, month, 1).getDay() + 6) % 7` (Luni-first), push `null` pentru celule offset, apoi push string-uri `YYYY-MM-DD` construite cu `padStart(2,'0')` pentru fiecare zi a lunii (NU folosi `new Date().toISOString()` — Pitfall 2).

    În TabAntrenamente derivă `currentYear = parseInt(date.substring(0,4))` și `currentMonth = parseInt(date.substring(5,7)) - 1` (0-indexed). Construiește `antrenamenteByDate` cu `useMemo` (RESEARCH.md Pattern 3) — `Record<string, Antrenament[]>` grupat pe câmpul `data`. Definește `statusVariant(status?: string): 'green'|'red'|'amber'|'slate'` (RESEARCH.md Pattern 6): planificat→green, anulat→red, efectuat→amber, default→slate.

    Definește `navigateMonth(direction: -1 | 1)`: ia `new Date(date)`, `setMonth(getMonth()+direction)`, apelează `setDate(d.toLocaleDateString('sv-SE'))` ȘI `setSelectedDate(null)` (Pitfall 4 — reset selectedDate la schimbarea lunii).

    Randează: (a) Header cu `<ChevronLeftIcon>` (onClick navigateMonth(-1)), titlu `${LUNI_RO[currentMonth]} ${currentYear}` (text-base font-bold), `<ChevronRightIcon>` (onClick navigateMonth(1)). (b) Rând zile săptămână "L M M J V S D" (7 coloane, text-xs bold slate-400). (c) Grid `grid grid-cols-7 gap-1` cu celulele din `getCalendarCells(currentYear, currentMonth)`: celulă null = div gol fără hover; celulă cu dată = div clickable (onClick `setSelectedDate(cellDate)`), min-h 40px, cursor-pointer, număr zi, și rând de dot-uri max 3 (`antrenamenteByDate[cellDate]?.slice(0,3)`) — `bg-rose-400` dacă status==='anulat', altfel `bg-emerald-400`; dacă length>3 afișează "..." (text-[9px]). Highlight: `ring-2 ring-indigo-400 font-bold` dacă `cellDate===todayLocal` (D-04), `bg-indigo-600/30 ring-1 ring-indigo-500` dacă `cellDate===selectedDate` (D-05).

    În acest task, DayPanel și modalele NU sunt încă implementate — randează sub grid un placeholder temporar simplu `{selectedDate && <div className="mt-4 text-sm text-slate-400">Panel pentru {selectedDate} — Task 2</div>}` care va fi înlocuit în Task 2. Dacă `loading` afișează "Se încarcă antrenamentele…".

    Actualizează mount-ul din `GrupaDetailView` (linia ~282): adaugă state `const [isModalAdaugareOpen, setIsModalAdaugareOpen] = useState(false);` în `GrupaDetailView`; pasează `grupa={grupa} isModalAdaugareOpen={isModalAdaugareOpen} setIsModalAdaugareOpen={setIsModalAdaugareOpen}` la `<TabAntrenamente />`. NU implementa încă butonul header (Task 3).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` (tsc --noEmit) trece fără erori noi
    - Grep `getCalendarCells` în GrupaDetailView.tsx returnează ≥1 rezultat
    - Grep `(getDay() + 6) % 7` (sau echivalent `getDay()+6`) prezent — offset Luni-first
    - Grep `bg-emerald-400` ȘI `bg-rose-400` prezente — dot-uri verde/roșu
    - Grep `LUNI_RO` prezent cu cele 12 luni în română
    - Grep `setSelectedDate(null)` prezent în navigateMonth — reset lună
    - Grep `useCalendarView(grupa.id)` prezent — hook reutilizat cu dependency pe id
    - Grep `ring-indigo-400` (today) ȘI `ring-indigo-500` (selected) prezente
    - NU există `toISOString()` în logica de construcție a datelor calendar
  </acceptance_criteria>
  <done>Tab Antrenamente afișează un calendar lunar cu navigare prev/next, header lună în română, dot-uri colorate pe zilele cu antrenamente, highlight pe azi și pe ziua selectată. Clic pe zi setează selectedDate (panel placeholder temporar).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DayPanel + CRUD anulare/reactivare/ștergere + ModalAnulare (slice CAL-02, CAL-03)</name>
  <files>components/Grupe/GrupaDetailView.tsx</files>
  <read_first>
    - components/Grupe/GrupaDetailView.tsx — starea de după Task 1; pattern empty state TabSportivi liniile 218-223, pattern rând compact liniile 224-238, pattern mutation TabOrar liniile 51-75
    - hooks/useCalendarView.ts liniile 18-39, 95-111 — `fetchAntrenamente` (refetch după mutație), `antrenamente`, `selectedDate`
    - .planning/phases/03-calendar-crud-antrenamente/03-RESEARCH.md — Pattern 4 (handleAnulare/handleReactivare), Pattern 5 (ConfirmButton delete), structura rând DayPanel (liniile 467-499); Pitfall 5 (motiv NULL vs ''), anti-pattern invalidateQueries
    - .planning/phases/03-calendar-crud-antrenamente/03-UI-SPEC.md — Layout DayPanel, Copywriting (empty state, badge labels, toasts, "Motiv anulare (opțional)", "Renunță"), Interaction States, Destructive Actions
    - components/ui.tsx liniile 208, 328, 655 — `ConfirmButton`, `Modal`, `Badge` API; Button variant union linia 7 (`success`, `danger`, `secondary`)
    - components/icons.tsx liniile 15, 41, 61 — `TrashIcon`, `CheckCircleIcon`, `XCircleIcon`
    - utils/date.ts linia 36 — `formatTime(time)`
  </read_first>
  <action>
    Înlocuiește placeholder-ul temporar din Task 1 cu sub-componenta `DayPanel` (definită inline în TabAntrenamente sau ca element JSX). Definește local în TabAntrenamente cele trei funcții mutație (D-13 — local, NU în hook):

    `handleAnulare(id: string, motiv: string | null)`: `await supabase.from('program_antrenamente').update({ status: 'anulat', motiv_anulare: motiv }).eq('id', id)`. Pe eroare `showError('Eroare anulare', error.message)`; pe succes `showSuccess('Succes', 'Antrenamentul a fost anulat.')` apoi `await fetchAntrenamente()`. NU apela `queryClient.invalidateQueries(['grupe'])` (anti-pattern documentat — `program_antrenamente` nu e în queryKey 'grupe').

    `handleReactivare(id: string)`: update `{ status: 'planificat', motiv_anulare: null }` .eq('id', id); succes toast "Antrenamentul a fost reactivat." + fetchAntrenamente().

    `handleStergere(id: string)`: `await supabase.from('program_antrenamente').delete().eq('id', id)`; succes toast "Antrenamentul a fost șters." + fetchAntrenamente().

    Adaugă state pentru modalul de anulare: `const [modalAnulareId, setModalAnulareId] = useState<string|null>(null);` și `const [motivAnulare, setMotivAnulare] = useState('');`. Funcție `deschideModalAnulare(id)` setează `modalAnulareId=id` și `motivAnulare=''`.

    Derivă `antrenamenteForSelectedDay = selectedDate ? (antrenamenteByDate[selectedDate] || []) : []`.

    Randează DayPanel sub grid (D-05 — element DOM fix, NU modal, NU expand pe rând):
    - Dacă `selectedDate === null`: mesaj "Selectează o zi din calendar pentru a vedea antrenamentele."
    - Dacă `selectedDate` setat și `antrenamenteForSelectedDay.length === 0` (D-06): Card center cu "Niciun antrenament pe {selectedDate}" + body "Apasă butonul pentru a adăuga un antrenament one-off în această zi." + `<Button variant="primary" size="sm" onClick={() => setIsModalAdaugareOpen(true)}>` cu `<PlusIcon>` + "Adaugă Antrenament".
    - Dacă există antrenamente (D-07): pentru fiecare, rând compact (`flex items-center gap-2 py-2 px-3 rounded-lg bg-slate-800/30 border border-slate-700/50`, min-h 44px) cu: `formatTime(a.ora_start)–formatTime(a.ora_sfarsit ?? '')` (font-mono text-sm), `<Badge variant={statusVariant(a.status)}>{a.status || 'planificat'}</Badge>`, apoi `ml-auto` grup butoane: dacă `a.status !== 'anulat'` → `<Button variant="secondary" size="sm" onClick={() => deschideModalAnulare(a.id)}>` cu XCircleIcon + "Anulează"; dacă `a.status === 'anulat'` → `<Button variant="success" size="sm" onClick={() => handleReactivare(a.id)}>` cu CheckCircleIcon + "Reactivează"; întotdeauna `<ConfirmButton variant="danger" size="sm" aria-label="Șterge antrenament" onConfirm={() => handleStergere(a.id)}>` cu `<TrashIcon className="w-3.5 h-3.5" />`.

    Randează ModalAnulare (D-10) cu `<Modal isOpen={modalAnulareId !== null} onClose={() => setModalAnulareId(null)} title="Anulare Antrenament">`: textarea label "Motiv anulare (opțional)" (resize-none, 3 rows, value=motivAnulare, onChange setMotivAnulare), notă "Antrenamentul rămâne vizibil în calendar cu status anulat.", footer cu `<Button variant="secondary" onClick={() => setModalAnulareId(null)}>Renunță</Button>` și `<Button variant="danger" onClick>` "Anulează Antrenament" care apelează `handleAnulare(modalAnulareId, motivAnulare.trim() || null)` (Pitfall 5 — NULL dacă gol) apoi `setModalAnulareId(null)`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fără erori noi
    - Grep `program_antrenamente'\).update` returnează ≥1 (anulare + reactivare)
    - Grep `program_antrenamente'\).delete` returnează ≥1 (ștergere)
    - Grep `status: 'anulat'` ȘI `status: 'planificat'` prezente
    - Grep `motivAnulare.trim() || null` prezent — Pitfall 5 NULL vs ''
    - Grep `fetchAntrenamente()` prezent în ≥3 locuri (după fiecare mutație)
    - Grep `invalidateQueries` NU apare în legătură cu mutații antrenamente (zero ocurențe noi cu queryKey 'grupe' în TabAntrenamente)
    - Grep `ConfirmButton` prezent cu onConfirm handleStergere
    - Grep `Reactivează` apare doar condiționat de `status === 'anulat'`
    - Grep `aria-label="Șterge antrenament"` prezent — accesibilitate icon-only
  </acceptance_criteria>
  <done>Clic pe o zi cu antrenamente afișează panel cu rânduri compacte (oră, badge status, acțiuni). Anulează deschide modal cu motiv opțional → status anulat + dot roșu. Reactivează readuce status planificat. Șterge (ConfirmButton) elimină din DB. Empty state oferă buton Adaugă.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ModalAdaugare one-off + buton header Adaugă Antrenament (slice CAL-04)</name>
  <files>components/Grupe/GrupaDetailView.tsx</files>
  <read_first>
    - components/Grupe/GrupaDetailView.tsx — starea de după Task 2; header GrupaDetailView liniile 252-260, tab bar liniile 264-278, mount tab liniile 280-285
    - hooks/useCalendarView.ts liniile 64-93 — `handleSaveCustom(data, clubId?)` (ramura is_recurent=false face INSERT în program_antrenamente)
    - .planning/phases/03-calendar-crud-antrenamente/03-RESEARCH.md — Pattern 7 (date pre-filled cu selectedDate), Pitfall 6 (handleSaveCustom necesită grupa.club_id ca al doilea param)
    - .planning/phases/03-calendar-crud-antrenamente/03-PATTERNS.md — pattern buton header (liniile 154-186), critical decision 2 (state ridicat în GrupaDetailView)
    - .planning/phases/03-calendar-crud-antrenamente/03-UI-SPEC.md — Layout ModalAdaugare, Copywriting (titlu "Adaugă Antrenament", submit "Salvează Antrenament", cancel "Anulează"), defaults 18:00/19:30
    - components/ui.tsx liniile 328 (Modal), Input component; components/icons.tsx linia 14 (PlusIcon)
  </read_first>
  <action>
    Adaugă în TabAntrenamente state form: `const [formData, setFormData] = useState({ data: selectedDate || todayLocal, ora_start: '18:00', ora_sfarsit: '19:30' });`. Adaugă `useEffect` care la deschiderea modalului re-sincronizează data cu ziua selectată (RESEARCH.md Pattern 7): `useEffect(() => { if (isModalAdaugareOpen) setFormData(f => ({ ...f, data: selectedDate || todayLocal })); }, [isModalAdaugareOpen, selectedDate]);`.

    Definește `handleSubmitAdaugare()` async: apelează `await handleSaveCustom({ grupa_id: grupa.id, is_recurent: false, data: formData.data, ora_start: formData.ora_start, ora_sfarsit: formData.ora_sfarsit }, grupa.club_id ?? undefined)` (Pitfall 6 — club_id ca al doilea param, OBLIGATORIU pentru RLS). Apoi `setIsModalAdaugareOpen(false)`. handleSaveCustom apelează deja fetchAntrenamente intern pe ramura is_recurent=false.

    Randează ModalAdaugare (D-08) cu `<Modal isOpen={isModalAdaugareOpen} onClose={() => setIsModalAdaugareOpen(false)} title="Adaugă Antrenament">`: `<Input label="Data" type="date" value={formData.data} onChange={e => setFormData(f => ({...f, data: e.target.value}))} />`, `<Input label="Ora start" type="time" value={formData.ora_start} onChange={...} />`, `<Input label="Ora sfârșit" type="time" value={formData.ora_sfarsit} onChange={...} />`, footer cu `<Button variant="secondary" onClick={() => setIsModalAdaugareOpen(false)}>Anulează</Button>` și `<Button variant="primary" onClick={handleSubmitAdaugare}>Salvează Antrenament</Button>`.

    Adaugă butonul header "Adaugă Antrenament" (D-09) în `GrupaDetailView` în rândul header (liniile 252-260), vizibil doar când `activeTab === 'antrenamente'`: `{activeTab === 'antrenamente' && <Button variant="primary" size="sm" onClick={() => setIsModalAdaugareOpen(true)} className="min-h-[40px] touch-manipulation"><PlusIcon className="w-4 h-4 mr-1" /> Adaugă Antrenament</Button>}`. State-ul `isModalAdaugareOpen` a fost deja ridicat în GrupaDetailView în Task 1 — folosește-l direct, NU re-declara.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fără erori noi
    - Grep `is_recurent: false` prezent în apelul handleSaveCustom
    - Grep `grupa.club_id` prezent ca al doilea argument al handleSaveCustom (Pitfall 6)
    - Grep `activeTab === 'antrenamente'` prezent în GrupaDetailView header (buton condiționat)
    - Grep `Salvează Antrenament` prezent (submit modal)
    - Grep `type="date"` ȘI `type="time"` prezente în ModalAdaugare
    - Grep `isModalAdaugareOpen` declarat exact O dată cu useState (în GrupaDetailView, nu duplicat în TabAntrenamente)
    - useEffect re-sync formData.data cu selectedDate prezent
  </acceptance_criteria>
  <done>Butonul Adaugă Antrenament apare în header-ul tab-ului Antrenamente (și în empty state). Clic deschide modal cu data pre-completată cu ziua selectată, ore default 18:00/19:30. Submit face INSERT one-off (is_recurent=false, club_id corect) și antrenamentul nou apare imediat în calendar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificare vizuală end-to-end calendar antrenamente</name>
  <action>Checkpoint de verificare umană. Toată automatizarea (Task 1-3) este completă; acest task pauzează execuția pentru confirmarea vizuală a fluxului end-to-end conform pașilor din how-to-verify de mai jos.</action>
  <what-built>
    Calendar lunar funcțional în tab Antrenamente al GrupaDetailView: grid cu dot-uri colorate, navigare lună, panel cu antrenamentele zilei, CRUD complet (adăugare one-off, anulare cu motiv, reactivare, ștergere).
  </what-built>
  <how-to-verify>
    1. Pornește `npm run dev` și loghează-te ca ADMIN_CLUB sau INSTRUCTOR.
    2. Navighează la modulul Grupe → deschide o grupă (buton Detalii) → tab Antrenamente.
    3. Confirmă: calendarul lunar afișează luna curentă în română (ex: "Iunie 2026"), zilele aliniate sub "L M M J V S D" (luni-first), azi cu border indigo.
    4. Apasă săgețile prev/next — luna se schimbă, panelul se golește.
    5. Apasă butonul "Adaugă Antrenament" din header → modal cu data pre-completată → salvează → confirmă dot verde apărut pe zi.
    6. Clic pe ziua cu antrenament → panel arată rândul (oră, badge "planificat" verde, butoane).
    7. Apasă "Anulează" → modal motiv (lasă gol sau scrie text) → "Anulează Antrenament" → confirmă dot devine roșu + badge "anulat".
    8. Apasă "Reactivează" pe antrenamentul anulat → confirmă revine verde "planificat".
    9. Apasă butonul Șterge (coș) → "Ești sigur? Da" → confirmă antrenamentul dispare din calendar.
    10. Confirmă că modificările pe alt club nu sunt posibile (RLS — test opțional dacă ai acces multi-club).
  </how-to-verify>
  <resume-signal>Scrie "approved" dacă totul funcționează, sau descrie problemele observate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase (program_antrenamente) | UPDATE/DELETE/INSERT pe antrenamente traversează aici; input necontrolat (motiv_anulare, date, ore) ajunge la DB |
| Client → DOM (motiv_anulare render) | Text introdus de user randat în JSX (badge, panel) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | UPDATE/DELETE pe antrenament dintr-un alt club | mitigate | RLS pe `program_antrenamente` filtrează by `club_id` via header `active-role-context-id` injectat de supabaseClient; mutațiile folosesc clientul standard care trimite header-ul automat |
| T-03-02 | Tampering | INSERT one-off cu club_id greșit/invalid | mitigate | handleSaveCustom apelat cu `grupa.club_id` ca al doilea param (Pitfall 6); RLS blochează inserarea pentru cluburi neautorizate |
| T-03-03 | Tampering (XSS) | motiv_anulare randat în JSX | mitigate | React escapează automat textul în JSX; NU se folosește dangerouslySetInnerHTML nicăieri în TabAntrenamente |
| T-03-04 | Information Disclosure | fetch lunar expune antrenamente cross-club | mitigate | `fetchAntrenamente` filtrează `.eq('grupa_id', grupaId)` + RLS pe rând; grupa.id vine din context autorizat (drill-down) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Nicio instalare de pachet în această fază — toate dependențele deja în package.json (RESEARCH.md Package Legitimacy Audit: zero pachete noi) |
</threat_model>

<verification>
Phase-level checks după toate task-urile:
- `npm run lint` (tsc --noEmit) trece fără erori noi
- Toate 4 success criteria din ROADMAP Phase 3 sunt observabile în UI (verificat manual la Task 4):
  1. Calendar lunar cu dot-uri verde/roșu — CAL-01
  2. Clic pe zi → panel cu acțiuni — CAL-02
  3. Modal anulare cu motiv (opțional per D-10) → status anulat — CAL-03
  4. Buton Adaugă → form one-off → INSERT is_recurent=false — CAL-04
- Tab Orar și Tab Sportivi NU au regresii (neatinse de această fază)
</verification>

<success_criteria>
- TabAntrenamente nu mai e placeholder — afișează calendar funcțional (CAL-01)
- Panel zi selectată cu acțiuni Anulează/Șterge/Reactivează (CAL-02)
- Modal anulare cu textarea motiv opțional → UPDATE status='anulat' + motiv_anulare (sau NULL) (CAL-03)
- Modal adăugare one-off → INSERT cu is_recurent=false și club_id corect (CAL-04)
- Anularea reversibilă prin Reactivează; ștergerea hard cu ConfirmButton
- Zero dependențe noi; zero modificări în hooks/useCalendarView.ts
- `npm run lint` curat; checkpoint vizual approved
</success_criteria>

<output>
Create `.planning/phases/03-calendar-crud-antrenamente/03-01-SUMMARY.md` when done
</output>
