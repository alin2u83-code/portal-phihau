---
phase: 26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - components/AppRouter.tsx
  - components/CluburiManagement.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-06, D-07]
user_setup: []

must_haves:
  truths:
    - "La 'Adaugă Club' SUPER_ADMIN vede în același formular secțiunea 'Date Club' și secțiunea 'Date Prim Administrator'"
    - "La 'Editează' pe un club existent secțiunea de admin NU apare deloc"
    - "Submit cu nume/prenume/email admin lipsă sau email invalid este blocat inline, fără a insera clubul"
    - "Un singur click pe 'Creează Club și Admin' creează clubul ȘI contul ADMIN_CLUB legat de acel club"
    - "După succes SUPER_ADMIN vede pe ecran emailul și parola generată, cu buton de copiere pentru fiecare"
    - "Dacă inserarea clubului reușește dar crearea contului eșuează, clubul rămâne creat, modalul rămâne deschis cu banner de eroare și buton de reîncercare"
    - "Reîncercarea creează doar contul de admin — nu inserează al doilea club"
    - "Noul admin se poate autentifica cu credențialele afișate și e forțat să-și schimbe parola"
  artifacts:
    - path: "components/CluburiManagement.tsx"
      provides: "ClubFormModal cu secțiune admin condiționată + handleSave secvențial club->cont + sub-stare retry D-07 + CredentialeContModal"
      contains: "genereazaParolaTemporara"
      min_lines: 380
    - path: "components/AppRouter.tsx"
      provides: "Prop allRoles transmis către CluburiManagement"
      contains: "allRoles={allRoles}"
  key_links:
    - from: "components/CluburiManagement.tsx"
      to: "utils/parola.ts"
      via: "import genereazaParolaTemporara"
      pattern: "genereazaParolaTemporara"
    - from: "components/CluburiManagement.tsx"
      to: "hooks/useRoleAssignment.ts"
      via: "useRoleAssignment(currentUser, allRoles).createAccountAndAssignRole"
      pattern: "createAccountAndAssignRole"
    - from: "components/CluburiManagement.tsx"
      to: "components/ui.tsx CredentialeContModal"
      via: "randare condiționată pe state-ul credentiale"
      pattern: "CredentialeContModal"
    - from: "components/AppRouter.tsx"
      to: "components/CluburiManagement.tsx"
      via: "prop allRoles"
      pattern: "CluburiManagement[\\s\\S]*allRoles"
---

<objective>
Extinde `ClubFormModal` din `components/CluburiManagement.tsx` astfel încât „Adaugă Club Nou" să creeze într-un singur submit clubul ȘI primul lui administrator (cont `auth.users` + rol `ADMIN_CLUB` legat de clubul nou), afișând la final parola generată pe ecran (D-01..D-07).

Purpose: Azi onboarding-ul unui club nou cere 3 acțiuni în 2 module diferite (Gestiune Cluburi -> Adaugă Club, apoi User Management -> Creează Cont, apoi asignare rol), cu risc de a lăsa un club fără admin. Sezonul nou aduce cluburi noi și SUPER_ADMIN trebuie să le poată porni dintr-un singur ecran.

Output: `components/CluburiManagement.tsx` extins (formular + orchestrare secvențială + sub-stare retry), `components/AppRouter.tsx` cu prop-ul `allRoles` transmis.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-CONTEXT.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-PATTERNS.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-UI-SPEC.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Simboluri **noi** create de acest plan (nu există astăzi în cod — nu le trata ca drift):

| Simbol | Fișier | Tip |
|---|---|---|
| `ClubFormModalProps.adminError` | `components/CluburiManagement.tsx` | prop nou (`string \| null`) |
| `ClubFormModalProps.clubCreatNume` | `components/CluburiManagement.tsx` | prop nou (`string \| null`) |
| `ClubFormModalProps.onRetryAdmin` | `components/CluburiManagement.tsx` | prop nou (`() => void`) |
| `ClubFormModalProps.retryLoading` | `components/CluburiManagement.tsx` | prop nou (`boolean`) |
| `ClubFormModalProps.onSave` semnătură nouă | `components/CluburiManagement.tsx` | `(clubData: ClubSaveData) => Promise<boolean>` (era `Promise<void>`) |
| `ClubSaveData` | `components/CluburiManagement.tsx` | tip local nou (`Partial<Club>` + `numeAdmin`/`prenumeAdmin`/`emailAdmin`) |
| `CluburiManagementProps.allRoles` | `components/CluburiManagement.tsx` | prop nou (`Rol[]`) |
| `pendingAdmin` | `components/CluburiManagement.tsx` | state nou |
| `adminError` / `retryLoading` / `credentiale` | `components/CluburiManagement.tsx` | state-uri noi |
| `creeazaAdminClub` | `components/CluburiManagement.tsx` | funcție locală nouă |
| `handleRetryAdmin` | `components/CluburiManagement.tsx` | funcție locală nouă |

Simboluri consumate din planul 26-01 (deja create acolo, nu aici): `genereazaParolaTemporara` din `utils/parola.ts`.

<tasks>

<task type="auto">
  <name>Task 1: Extinde ClubFormModal cu secțiunea „Date Prim Administrator" și transmite allRoles (D-01, D-03, D-04)</name>
  <files>components/AppRouter.tsx, components/CluburiManagement.tsx</files>
  <read_first>
    - `components/CluburiManagement.tsx` — fișierul integral: `ClubFormModalProps` (liniile 12-17), `ClubFormModal` (liniile 19-61), `CluburiManagementProps` (63-69), empty state (182-185), randarea modalului (linia 299)
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-UI-SPEC.md` — secțiunile „Implementation Notes (form structure)", „Color", „Typography", „Copywriting Contract" (copy-ul exact în română pentru fiecare element)
    - `components/ui.tsx` — `InputProps` (liniile 585-605, prop-ul `error?: string` care randează mesajul inline cu ⚠️) și `ModalProps` (liniile 356-364, prop-ul `persistent`)
    - `components/icons.tsx` — confirmă exporturile `BuildingOfficeIcon` (alias `Building2`), `UserPlusIcon` (alias `UserPlus`), `AlertCircleIcon` (alias `AlertCircle`)
    - `components/AppRouter.tsx` linia 43 (destructurarea care include deja `allRoles`) și linia 236 (randarea `Lazy.CluburiManagement`)
    - `components/UserManagement.tsx` linia 234 din `AppRouter.tsx` — pattern-ul deja folosit pentru a transmite `allRoles` unui modul
  </read_first>
  <action>
**A. `components/AppRouter.tsx`** — la randarea `Lazy.CluburiManagement` (linia 236) adaugă prop-ul `allRoles={allRoles}`. Variabila `allRoles` este deja destructurată la linia 43, nu adăuga import nou.

**B. `components/CluburiManagement.tsx` — tipuri și importuri.**
Adaugă la importul din `../types` tipul `Rol`. Adaugă la importul din `./ui` componenta `CredentialeContModal`. Adaugă la importul din `./icons` iconițele `BuildingOfficeIcon`, `UserPlusIcon`, `AlertCircleIcon`. Adaugă importul `import { useRoleAssignment } from '../hooks/useRoleAssignment';` și `import { genereazaParolaTemporara } from '../utils/parola';` (ambele consumate în Task 2, importă-le acum ca fișierul să compileze într-un singur pas).

Definește tipul local exportabil-intern `ClubSaveData = Partial<Club> & { numeAdmin?: string; prenumeAdmin?: string; emailAdmin?: string }`.

Extinde `CluburiManagementProps` cu `allRoles: Rol[];`, iar semnătura componentei `CluburiManagement` cu destructurarea `allRoles`.

**C. `ClubFormModalProps`** — schimbă `onSave` la `(clubData: ClubSaveData) => Promise<boolean>` (`true` = închide modalul) și adaugă:
`adminError: string | null;`, `clubCreatNume: string | null;`, `onRetryAdmin: () => void;`, `retryLoading: boolean;`.

**D. `ClubFormModal` — state.**
Extinde `formData` la `{ nume: '', cif: '', oras: '', numeAdmin: '', prenumeAdmin: '', emailAdmin: '' }`. În `useEffect`-ul de reset (declanșat pe `isOpen`/`clubToEdit`) resetează întotdeauna cele trei câmpuri admin la string gol, indiferent de mod. Adaugă state nou `fieldErrors` de tip `{ numeAdmin?: string; prenumeAdmin?: string; emailAdmin?: string }` inițializat cu obiect gol, resetat odată cu `formData`.

**E. `ClubFormModal` — validare (D-03).**
În `handleSubmit`, doar în modul creare (`!clubToEdit`), validează înainte de a apela `onSave`:
- `numeAdmin` gol după `trim()` -> eroare `"Numele administratorului este obligatoriu."`
- `prenumeAdmin` gol după `trim()` -> eroare `"Prenumele administratorului este obligatoriu."`
- `emailAdmin` gol după `trim()` -> eroare `"Emailul administratorului este obligatoriu."`; altfel dacă nu se potrivește cu regexul `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` -> eroare `"Adresa de email nu este validă."`
Dacă există cel puțin o eroare: setează `fieldErrors`, NU apela `onSave`, NU seta `loading`, returnează. Erorile se transmit componentelor `Input` prin prop-ul existent `error`.

**F. `ClubFormModal` — structura formularului (UI-SPEC).**
Împarte corpul formularului în două secțiuni cu antete proprii (nu folosi `FormSection` din `ui.tsx` — grid-ul lui de 2 coloane nu se potrivește cu „Nume Club" pe lățime completă):
- Secțiunea 1 „Date Club": antet cu `BuildingOfficeIcon` (`className="w-4 h-4"`, culoare `text-[var(--t-primary)]`) + text „Date Club" în stil Label (`text-xs font-bold uppercase tracking-wide text-slate-300`). Sub el, câmpurile existente exact ca acum: `Nume Club` pe lățime completă (required), apoi grid `grid-cols-1 sm:grid-cols-2 gap-4` cu `CIF / CUI (Opțional)` și `Oraș (Opțional)`.
- Secțiunea 2 „Date Prim Administrator": randată **doar când `!clubToEdit`** (D-01). Antet cu `UserPlusIcon` (aceleași clase, aceeași culoare accent) + text „Date Prim Administrator". Imediat sub antet, text ajutător în stil Body, `text-sm`, culoare `var(--t-text-muted)`, cu textul exact: „Parola va fi generată automat și afișată după creare — o veți transmite manual noului admin." Apoi grid `grid-cols-1 sm:grid-cols-2 gap-4` cu `Input` `label="Nume Admin"` `name="numeAdmin"` și `Input` `label="Prenume Admin"` `name="prenumeAdmin"`, ambele `required`, ambele cu `error={fieldErrors.numeAdmin}` respectiv `error={fieldErrors.prenumeAdmin}`. Apoi pe lățime completă `Input` `label="Email Admin"` `name="emailAdmin"` `type="email"` `placeholder="email@exemplu.ro"` `required` `error={fieldErrors.emailAdmin}`.
- NU adăuga câmp de username (se derivă tăcut din email) și NU adăuga câmp de parolă (D-05) și NU adăuga selector de rol (D-04, rolul e fix `ADMIN_CLUB`).
Separă vizual secțiunile prin `space-y-6` pe formular (câmpurile din interiorul unei secțiuni rămân `space-y-4`).

**G. `ClubFormModal` — buton de submit (Copywriting Contract).**
Eticheta devine condiționată: `clubToEdit ? "Salvează" : "Creează Club și Admin"`. Butonul „Anulează" rămâne neschimbat.

**H. `ClubFormModal` — închidere controlată de părinte.**
`handleSubmit` nu mai apelează `onClose()` necondiționat. Noua secvență: `setLoading(true)`, `const inchide = await onSave(dataToSave)`, `setLoading(false)`, `if (inchide) onClose()`. `dataToSave` rămâne `{ id: clubToEdit?.id, ...formData }` — spread-ul generic transportă și câmpurile admin, deci nu e nevoie de mapare manuală.

**I. `ClubFormModal` — sub-starea de eroare/retry D-07.**
Când `adminError` este non-null, corpul modalului NU mai randează formularul, ci un banner inline plus butonul de retry:
- container cu `text-sm rounded-lg px-3 py-2 border` colorat cu `var(--t-status-danger)` (text și border cu opacitate redusă pe fundal, în spiritul bannerului amber existent din `CredentialeContModal` liniile 532-534, dar cu tokenul danger), conținând `AlertCircleIcon` (`w-4 h-4`, `shrink-0`) și textul exact: „Clubul '{clubCreatNume}' a fost creat, dar contul de admin nu a putut fi creat: {adminError}. Clubul nu se creează din nou — apăsați mai jos pentru a reîncerca doar crearea contului de admin."
- sub banner, rând de butoane: `Button` secundar „Închide" care apelează `onClose`, și `Button` primar „Reîncearcă Crearea Contului Admin" care apelează `onRetryAdmin` cu `isLoading={retryLoading}`.
Setează `persistent={!!adminError}` pe `Modal` ca Escape/click-pe-fundal să nu închidă accidental modalul cât timp SUPER_ADMIN e în starea de retry. Titlul modalului rămâne „Adaugă Club Nou" în acest sub-stare.

**J. Empty state (Copywriting Contract).**
În `CluburiManagement`, înlocuiește textul empty state de la linia 184 cu: „Nu există cluburi înregistrate. Adăugați primul club — formularul creează automat și primul admin al clubului. Folosiți butonul de mai sus."

**K. Randarea modalului** (linia 299) — transmite noile prop-uri. În acest task pot fi conectate provizoriu la valori statice (`adminError={null}`, `clubCreatNume={null}`, `onRetryAdmin={() => {}}`, `retryLoading={false}`) doar dacă e nevoie pentru compilare; Task 2 le leagă de state real. `handleSave` trebuie totuși actualizat în acest task să returneze `boolean` (`true` pe toate căile actuale, inclusiv pe early-return-urile de eroare CIF/permisiune unde trebuie să returneze `false` ca modalul să rămână deschis cu datele introduse).
  </action>
  <verify>
    <automated>npm run lint && grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "Date Prim Administrator" && grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "Creează Club și Admin" && grep -v '^\s*//' components/AppRouter.tsx | grep -c "allRoles={allRoles}"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fără erori
    - `grep -v '^\s*//' components/AppRouter.tsx | grep -c "allRoles={allRoles}"` este >= 2 (UserManagement existent + CluburiManagement nou)
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "Date Prim Administrator"` este >= 1
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "Creează Club și Admin"` este >= 1
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "Parola va fi generată automat"` este >= 1
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "formularul creează automat și primul admin"` este >= 1
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -cE "name=\"(numeAdmin|prenumeAdmin|emailAdmin)\""` este 3
    - Secțiunea admin este randată sub o condiție `!clubToEdit` — verificabil prin `grep -c "!clubToEdit &&" components/CluburiManagement.tsx` >= 1
    - Nu există câmp de parolă și nici selector de rol în formular: `grep -cE "label=\"Parol|label=\"Rol" components/CluburiManagement.tsx` este 0
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "Promise<boolean>"` este >= 1
  </acceptance_criteria>
  <done>În modul creare formularul afișează ambele secțiuni cu copy-ul exact din UI-SPEC; în modul editare secțiunea admin lipsește; submit fără date admin valide e blocat inline; `allRoles` ajunge în componentă.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrare secvențială club -> cont ADMIN_CLUB, afișare credențiale și retry fără duplicare (D-02, D-04, D-06, D-07)</name>
  <files>components/CluburiManagement.tsx</files>
  <read_first>
    - `components/CluburiManagement.tsx` — starea după Task 1, în special `handleSave` (ramura de creare, fost linii 111-134: verificare CIF duplicat, insert în `cluburi`, `clearCache('cache_clubs')`, `setClubs`, `showSuccess`) și blocul `catch` (fost 135-145) care trebuie păstrat DOAR pentru erorile de insert club
    - `hooks/useRoleAssignment.ts` — semnătura `createAccountAndAssignRole(email, parola, sportivData, rolesToAssign)` și forma returnată `{ success, sportiv?, error?, generatedPassword? }`, plus modificările din 26-01 (header Authorization, refuz pe `club_id` lipsă)
    - `components/UserManagement.tsx` liniile 70-120 — pattern-ul de referință: construire `newSportivProfile`, apel `createAccountAndAssignRole`, apoi `setCredentialeModal({ email, parola, numeSportiv })`
    - `components/UserProfile/CreateAccountModal.tsx` liniile 35-55 și 83-93 — pattern-ul „succes -> setează state credentiale -> randează `CredentialeContModal` în locul formularului"
    - `components/ui.tsx` liniile 480-575 — props `CredentialeContModal` (`isOpen`, `onClose`, `email`, `parola`, `numeSportiv`)
    - `sql/refactor/REFACTOR_CREATE_ACCOUNT.sql` — comportamentul RPC: găsește sportivul existent după email și îl actualizează (deci retry-ul nu duplică), și setează `is_primary = (rol = 'SPORTIV')`, ceea ce înseamnă că un cont creat DOAR cu rol `ADMIN_CLUB` nu primește niciun rol cu `is_primary = true`
    - `hooks/useUserRoles.ts` linia 112 și `utils/auth.ts` liniile 56-90 — cum se alege contextul de rol la login și de ce `is_primary` contează
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-UI-SPEC.md` — punctele 4 și 5 din „Implementation Notes" și rândurile „Result screen" din Copywriting Contract
  </read_first>
  <action>
Lucrezi doar în componenta `CluburiManagement` (părintele), nu în `ClubFormModal`.

**A. Hook și rol.**
Instanțiază `const { createAccountAndAssignRole } = useRoleAssignment(currentUser, allRoles);`. Rezolvă rolul fix (D-04) cu `const rolAdminClub = allRoles.find(r => r.nume === 'ADMIN_CLUB');`. NU adăuga rolul `SPORTIV` în listă (spre deosebire de `UserManagement.tsx`, unde e adăugat automat) — D-04 cere rol fix `ADMIN_CLUB`.

**B. State nou.**
- `pendingAdmin: { clubId: string; clubNume: string; nume: string; prenume: string; email: string; parola: string } | null` — datele necesare pentru (re)crearea contului, inclusiv parola generată o singură dată
- `adminError: string | null`
- `retryLoading: boolean`
- `credentiale: { email: string; parola: string; numeSportiv: string } | null`

**C. `creeazaAdminClub(pending)` — funcție locală reutilizată de primul apel și de retry.**
Apelează `createAccountAndAssignRole(pending.email, pending.parola, { nume: pending.nume, prenume: pending.prenume, email: pending.email, club_id: pending.clubId, status: 'Activ' }, [rolAdminClub])`. NU trimite `username` — hook-ul îl derivă din `email.split('@')[0]` (pattern confirmat în 26-PATTERNS.md, secțiunea „Username derivation"). NU trimite `data_nasterii`/`gen`/`grad_actual_id` — hook-ul aplică deja valorile implicite.

La `success === false`: setează `adminError` cu `result.error` (mesajul serverului, verbatim — e singura informație care explică cauza reală, vezi T-26-09) și returnează `false`.

La `success === true`:
1. **Gardă `is_primary`.** Rulează `supabase.from('utilizator_roluri_multicont').update({ is_primary: true }).eq('sportiv_id', result.sportiv.id).eq('rol_denumire', 'ADMIN_CLUB')`. Motiv: RPC-ul `refactor_create_user_account` setează `is_primary` doar pentru rolul `SPORTIV`, deci un cont creat exclusiv cu `ADMIN_CLUB` ar rămâne fără rol primar, iar `useUserRoles`/`utils/auth.ts` s-ar baza pe fallback-uri fragile la login. Eroarea acestui update se loghează cu `console.warn` și NU invalidează crearea contului (contul e deja creat cu succes).
2. Setează `credentiale` la `{ email: pending.email, parola: result.generatedPassword ?? pending.parola, numeSportiv: \`${pending.prenume} ${pending.nume} — Admin ${pending.clubNume}\` }` — formatul compus cerut de UI-SPEC („Result screen subtitle").
3. Golește `adminError` și `pendingAdmin`, apelează `showSuccess("Succes", "Clubul și contul de administrator au fost create.")`, returnează `true`.

**D. `handleSave` — ramura de creare.**
Ordinea obligatorie:
1. Verifică `permissions.isSuperAdmin` (gardă existentă, păstrată) și, **înainte de orice scriere**, verifică `rolAdminClub`. Dacă lipsește, `showError("Eroare", "Rolul 'ADMIN_CLUB' nu a fost găsit. Reîncărcați pagina.")` și returnează `false` — fail-fast, ca să nu creezi un club orfan.
2. Separă câmpurile admin de payload-ul de club: destructurează `const { id, numeAdmin, prenumeAdmin, emailAdmin, ...insertData } = clubData;`. Acest pas este **critic** — `insertData` merge direct în `supabase.from('cluburi').insert(...)` și un câmp `numeAdmin` inexistent în schema `cluburi` ar face insert-ul să eșueze.
3. Păstrează neschimbate: curățarea `cif === ''` -> `null`, verificarea de CIF duplicat (early-return cu `showError` — acum returnează `false`), insert-ul, `clearCache('cache_clubs')`, `setClubs(prev => [...prev, data])`.
4. Imediat după insert-ul reușit, generează parola O SINGURĂ DATĂ: `const parola = genereazaParolaTemporara();`. Construiește obiectul `pending` cu `clubId: data.id`, `clubNume: data.nume`, numele/prenumele/emailul admin trimise (`.trim()` pe toate trei) și `parola`. Setează state-ul `pendingAdmin` cu acest obiect.
5. Apelează `await creeazaAdminClub(pending)` și returnează rezultatul lui ca valoare de retur a lui `handleSave` (deci pe eșec modalul rămâne deschis, în sub-starea de banner).
6. Înlocuiește `showSuccess("Succes", "Club adăugat.")` din fluxul de creare — mesajul de succes vine acum din `creeazaAdminClub` după ce ambii pași reușesc.

**Fără rollback (D-07).** Nu adăuga niciun `delete` pe `cluburi` pe calea de eroare a pasului admin. Blocul `catch` existent (RLS 42501 / 23505 cif / 23505 generic / generic) rămâne pentru erorile pasului de club și trebuie să returneze `false`; erorile pasului de admin NU trec prin el, ele merg în `adminError`.

**Ramura de editare.** Rămâne funcțional identică; returnează `true` pe succes și `false` pe early-return-urile de eroare.

**E. `handleRetryAdmin` (D-07).**
Funcție care, dacă `pendingAdmin` este non-null: setează `retryLoading` true, golește `adminError`, apelează `await creeazaAdminClub(pendingAdmin)` reutilizând **aceeași parolă** deja stocată în `pendingAdmin` (esențial: dacă primul apel a creat deja user-ul în `auth.users` și a eșuat ulterior, `api/creare-cont.ts` rezolvă user-ul existent fără a-i schimba parola — o parolă nouă generată la retry ar fi afișată dar invalidă), apoi `retryLoading` false. Nu atinge tabela `cluburi`.

**F. Închiderea în sub-starea de eroare.**
Când modalul se închide cu `adminError` setat (butonul „Închide" din banner), golește `pendingAdmin`, `adminError` și afișează `showError("Club fără administrator", "Clubul a fost creat dar nu are încă administrator. Îi puteți crea contul din User Management.")` ca SUPER_ADMIN să știe calea alternativă.

**G. Randare.**
Transmite `ClubFormModal` prop-urile reale: `onSave={handleSave}`, `adminError={adminError}`, `clubCreatNume={pendingAdmin?.clubNume ?? null}`, `onRetryAdmin={handleRetryAdmin}`, `retryLoading={retryLoading}`.
Randează `CredentialeContModal` când `credentiale` este non-null, cu `isOpen={true}`, `onClose={() => setCredentiale(null)}`, `email`, `parola` și `numeSportiv` din state (D-06 — parola e afișată o singură dată; nu o persista în localStorage și nu o loga).
  </action>
  <verify>
    <automated>npm run lint && grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "genereazaParolaTemporara" && grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "createAccountAndAssignRole" && grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "CredentialeContModal"</automated>
    <human-check>
      Pornește `npm run dev`, autentifică-te ca SUPER_ADMIN_FEDERATIE, mergi la Gestiune Cluburi.
      1. Click „Adaugă Club". Așteptat: două secțiuni — „Date Club" (icon clădire) și „Date Prim Administrator" (icon user+), text ajutător despre parola generată automat, buton „Creează Club și Admin". Fără câmp de parolă, fără selector de rol.
      2. Apasă Salvează cu câmpurile admin goale. Așteptat: erori inline roșii sub cele trei câmpuri; clubul NU apare în listă.
      3. Completează club + admin (email real, ex. `test.admin+26@exemplu.ro`) și trimite. Așteptat: clubul apare în tabel ȘI se deschide ecranul „Cont creat cu succes" cu emailul și parola generată, fiecare cu buton „Copiază". Parola trebuie să arate aleatoare (nu derivată din nume).
      4. Click „Editează" pe orice club existent. Așteptat: secțiunea „Date Prim Administrator" NU apare, butonul zice „Salvează".
      5. Deloghează-te și loghează-te cu emailul + parola afișate. Așteptat: ecranul de schimbare obligatorie a parolei; după schimbare, utilizatorul intră în contextul clubului NOU (nu Phi Hau Iași), cu meniu de ADMIN_CLUB.
      6. Simulare eșec pasul 2 (D-07): repetă pasul 3 dar folosește un email care există deja în `auth.users` fără profil sportiv, SAU oprește temporar funcția `/api/creare-cont` (ex. DevTools -> Network -> Block request URL pe `/api/creare-cont`) și trimite. Așteptat: clubul APARE în listă, modalul RĂMÂNE deschis cu bannerul roșu care numește clubul creat și eroarea, plus butonul „Reîncearcă Crearea Contului Admin". Deblochează request-ul și apasă retry. Așteptat: contul se creează, NU apare un al doilea club cu același nume în listă.
      Raportează „aprobat" sau descrie exact ce a deviat.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fără erori
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "genereazaParolaTemporara"` este >= 1
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "createAccountAndAssignRole"` este >= 1
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "CredentialeContModal"` este >= 1
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "ADMIN_CLUB"` este >= 2 (căutarea rolului + garda `is_primary`)
    - `grep -v '^\s*//' components/CluburiManagement.tsx | grep -c "clearCache('cache_clubs')"` este >= 3 (editare, creare, ștergere — pattern păstrat)
    - Parola este generată exact o dată pe flux: `grep -c "genereazaParolaTemporara()" components/CluburiManagement.tsx` este 1
    - Nu există rollback pe club: `grep -cE "from\('cluburi'\)[\s\S]{0,40}\.delete" components/CluburiManagement.tsx` rămâne 1 (doar `confirmDelete`, funcția de ștergere manuală preexistentă)
    - Parola nu e logată: `grep -cE "console\.(log|warn|error)\(.*parola" components/CluburiManagement.tsx` este 0
    - Comportament (human-check pasul 3): un singur submit produce atât rândul de club, cât și contul de admin cu rol ADMIN_CLUB pe acel club
    - Comportament (human-check pasul 6): pe eșecul pasului admin clubul rămâne creat, iar retry-ul nu produce un al doilea club
  </acceptance_criteria>
  <done>Un singur click pe „Creează Club și Admin" creează clubul și contul `ADMIN_CLUB` legat de el, afișează credențialele o dată, iar pe eșec parțial păstrează clubul și oferă retry idempotent doar pe pasul de cont.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SUPER_ADMIN (browser) -> `cluburi` prin PostgREST | Insert direct din frontend, protejat de RLS + garda `permissions.isSuperAdmin` |
| SUPER_ADMIN (browser) -> `/api/creare-cont` | Creare identitate privilegiată; gardat server-side în 26-01 |
| server -> DOM (`CredentialeContModal`) | Parola în clar ajunge pe ecran și în clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-10 | Information Disclosure | Parola generată persistată accidental (state global, localStorage, cache React Query, log) | mitigate | Task 2: parola trăiește doar în state local `pendingAdmin`/`credentiale` din `CluburiManagement`, golit la închiderea `CredentialeContModal`; interdicție explicită de `console.log` pe parolă, cu criteriu grep în acceptance criteria; `CredentialeContModal` folosește `navigator.clipboard` fără a scrie în storage |
| T-26-11 | Information Disclosure | Parola afișată pe ecran vizibilă unui terț (shoulder surfing) în biroul federației | accept | Decizie explicită a utilizatorului (D-06): livrarea prin magic link a fost respinsă; fereastra de risc e limitată de `trebuie_schimbata_parola = true` (setat în 26-01), care invalidează parola la primul login al adminului |
| T-26-12 | Tampering | Retry-ul D-07 creează un al doilea club cu același nume/CIF | mitigate | Task 2 punctul E: `handleRetryAdmin` apelează exclusiv `creeazaAdminClub`, nu atinge tabela `cluburi`; `clubId` este citit din `pendingAdmin`, nu re-derivat; criteriu de acceptare grep pe absența unui al doilea `delete`/`insert` pe `cluburi` |
| T-26-13 | Tampering | Retry cu parolă nouă produce credențiale afișate care nu se potrivesc cu parola reală din `auth.users` (user-ul fusese deja creat la prima încercare) | mitigate | Task 2 punctele D.4 și E: parola se generează o singură dată, la insertul clubului, și se reutilizează identic la fiecare retry; criteriu de acceptare: exact o apariție a `genereazaParolaTemporara()` în fișier |
| T-26-14 | Elevation of Privilege | Câmpurile admin din formular sunt injectate în insert-ul pe `cluburi` sau folosite pentru a alege alt rol decât ADMIN_CLUB | mitigate | Task 2 punctul D.2: destructurare explicită care exclude `numeAdmin`/`prenumeAdmin`/`emailAdmin` din `insertData`; rolul este rezolvat server-side-agnostic ca `allRoles.find(r => r.nume === 'ADMIN_CLUB')`, fără input de utilizator (D-04); gărzile server-side din 26-01 (T-26-01/T-26-02) rămân linia de apărare finală |
| T-26-15 | Spoofing | Un `ADMIN_CLUB` obișnuit accesează ecranul Gestiune Cluburi și creează cluburi/administratori | mitigate | Ruta este deja gardată cu `renderProtected(..., isFederationAdmin)` în `AppRouter.tsx` linia 236; `handleSave` verifică `permissions.isSuperAdmin` la creare; garda server-side de scoping pe club din 26-01 (T-26-02) blochează un bypass direct al UI-ului |
| T-26-16 | Denial of Service | Club creat fără administrator, blocând onboarding-ul (stare orfană) | accept | Consecință acceptată a D-07 (fără rollback); Task 2 punctul F afișează calea de recuperare („creați contul din User Management"), iar clubul orfan e vizibil în listă și editabil |
| T-26-SC | Tampering | Instalări npm/pip/cargo | mitigate | Fără dependențe noi — se reutilizează `lucide-react` (deja instalat, re-exportat prin `components/icons.tsx`) și module interne. Nu se rulează niciun `npm install`; nu e necesară verificare de legitimitate pachete. |
</threat_model>

<verification>
1. `npm run lint` — zero erori TypeScript
2. Asserțiuni de sursă (grep) din `<acceptance_criteria>` fiecărui task
3. Human-check din Task 2, pașii 1-6 (inclusiv simularea eșecului parțial D-07 și verificarea că retry-ul nu duplică clubul)
4. Verificare vizuală conform `26-UI-SPEC.md`: iconițe accent `var(--t-primary)`, banner de eroare pe `var(--t-status-danger)`, copy exact din Copywriting Contract
</verification>

<success_criteria>
- Un singur submit din „Adaugă Club Nou" produce: rând în `cluburi` + user în `auth.users` + rol `ADMIN_CLUB` legat de clubul nou + rând în `sportivi`
- Câmpurile admin apar doar la creare, niciodată la editare
- Validarea blochează submitul incomplet fără a scrie nimic în DB
- Credențialele sunt afișate o singură dată, cu copiere, și parola forțează schimbarea la primul login
- Eșecul pasului admin păstrează clubul, ține modalul deschis cu banner și permite retry care nu duplică nici clubul, nici user-ul, nici parola
</success_criteria>

<output>
Create `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-02-SUMMARY.md` when done
</output>
