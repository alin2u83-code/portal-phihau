---
phase: 31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa
plan: 07
type: execute
wave: 2
depends_on: [31-02, 31-03]
files_modified:
  - components/UserProfile/FinanciarTab.tsx
  - components/UserProfile.tsx
autonomous: true
requirements: [D-08, D-09]
user_setup: []

must_haves:
  truths:
    - "Un INSTRUCTOR (care nu are acces la hub — guard canManageFinances) poate deschide ecranul de detaliu factura din profilul sportivului → tab 'Istoric Financiar' → butonul 'Detalii' (D-09)"
    - "Acelasi buton e disponibil si pentru ADMIN_CLUB / adminii de federatie in profilul sportivului"
    - "Se stie, pe baza politicilor RLS live, daca INSTRUCTOR poate efectiv face UPDATE pe plati din clubul sau si daca trigger-ul de recalculare tine cont de alocarile normalizate — rezultat consemnat in SUMMARY, fara nicio modificare DB"
  artifacts:
    - path: "components/UserProfile/FinanciarTab.tsx"
      provides: "Prop optional onDeschideDetalii + buton Detalii pe fiecare factura"
      contains: "onDeschideDetalii"
    - path: "components/UserProfile.tsx"
      provides: "State plataDetaliuId + montarea FacturaDetaliu"
      contains: "<FacturaDetaliu"
  key_links:
    - from: "components/UserProfile.tsx"
      to: "components/Plati/FacturaDetaliu.tsx"
      via: "<FacturaDetaliu plataId={plataDetaliuId} onClose=... />"
      pattern: "<FacturaDetaliu"
    - from: "components/UserProfile/FinanciarTab.tsx"
      to: "components/UserProfile.tsx"
      via: "onDeschideDetalii={setPlataDetaliuId}"
      pattern: "onDeschideDetalii"
---

<objective>
Punctul de intrare INSTRUCTOR in ecranul de detaliu factura (D-09), plus verificarea read-only pe DB-ul live a premiselor de care depind actiunea rapida si accesul INSTRUCTOR.

De ce profilul sportivului: hub-ul "Plăți & Facturi" pastreaza guard-ul `canManageFinances` (ADMIN_CLUB + federatie) pentru ca listele din tab-ul Facturi expun actiuni distructive (stergere/anulare/generare). INSTRUCTOR are deja acces la `profil-sportiv` (AppRouter.tsx:179, guard `isAtLeastInstructor`) si la tab-ul "Istoric Financiar" (UserProfile.tsx:831, fara `hidden`), unde fiecare factura are o bara de actiuni (FinanciarTab.tsx:355-387). Adaugarea butonului "Detalii" acolo da INSTRUCTOR-ului exact ce cere D-09 — detaliul si butonul de corectie rapida — fara a-i deschide listele distructive.

Output: buton "Detalii" in FinanciarTab, montarea `FacturaDetaliu` in UserProfile, raport de verificare live in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-CONTEXT.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-02-SUMMARY.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Contract consumat (31-03):
```ts
// components/Plati/FacturaDetaliu.tsx
export const FacturaDetaliu: React.FC<{ plataId: string | null; onClose: () => void }>;
// calculeaza singur permisiunile (usePermissions) si citeste plata din useData().filteredData.plati
```

Contract creat de acest plan:
```ts
// components/UserProfile/FinanciarTab.tsx — prop nou optional in FinanciarTabProps
onDeschideDetalii?: (plataId: string) => void;
```

Ancore (inainte de editare; UserProfile.tsx a fost modificat in 31-02 doar in handleSavePlataEdit — re-localizeaza prin grep):
- FinanciarTab.tsx:22-37 `interface FinanciarTabProps`; :119-124 destructurarea; :355-360 inceputul barei "Acțiuni" (`<div className="flex items-center gap-2 px-4 py-2.5 border-t ...">` urmat de butonul "Editează"); in bucla, `p` este un rand `VizualizarePlata`/FacturaEntry cu `p.plata_id`.
- FinanciarTab.tsx:4 importul de iconite (adauga `ClipboardListIcon` din '../icons').
- UserProfile.tsx:148-150 starile `plataToEdit` / `plataToDelete` / `plataToAnula` (adauga `plataDetaliuId` langa ele); :905-921 montarea `<FinanciarTab ... />`; :951 `<PlataEditModal ... />` (monteaza FacturaDetaliu imediat dupa).

Proiect Supabase live: `wuhidifzsutwgdfkwhmd`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Buton "Detalii" in Istoric Financiar + montarea FacturaDetaliu in profilul sportivului</name>
  <read_first>
    - components/UserProfile/FinanciarTab.tsx (liniile 1-40, 115-130, 280-392)
    - components/UserProfile.tsx (liniile 140-160, 440-470, 820-840, 900-955)
    - components/Plati/FacturaDetaliu.tsx (props si comportament la plataId inexistent)
  </read_first>
  <files>components/UserProfile/FinanciarTab.tsx, components/UserProfile.tsx</files>
  <action>
FinanciarTab.tsx (per D-08/D-09):
- In `FinanciarTabProps` adauga `onDeschideDetalii?: (plataId: string) => void;` (comentariu: "Faza 31 — deschide ecranul de detaliu factura; disponibil si pentru INSTRUCTOR (D-09)") si adauga `onDeschideDetalii` in destructurarea semnaturii.
- In bara "Acțiuni" a fiecarei facturi (div-ul de la ~355), ca PRIM buton, inainte de "Editează": `{onDeschideDetalii && (<Button size="sm" variant="secondary" onClick={() => onDeschideDetalii(p.plata_id)}><ClipboardListIcon className="w-3.5 h-3.5 mr-1" /> Detalii</Button>)}`.
- Adauga `ClipboardListIcon` la importul existent din `'../icons'`.
- Nu modifica butoanele existente (Editează, Anulează, Reactivează, Șterge) si nici modalul `selectedFactura`.

UserProfile.tsx:
- Adauga `import { FacturaDetaliu } from './Plati/FacturaDetaliu';` langa celelalte importuri de componente.
- Langa starile `plataToEdit`/`plataToDelete`/`plataToAnula` adauga `const [plataDetaliuId, setPlataDetaliuId] = useState<string | null>(null);`.
- In montarea `<FinanciarTab ... />` (bloc `activeTab === 'financiar'`) adauga prop-ul `onDeschideDetalii={setPlataDetaliuId}`.
- Imediat dupa `<PlataEditModal ... />` monteaza `<FacturaDetaliu plataId={plataDetaliuId} onClose={() => setPlataDetaliuId(null)} />`.
- NU atinge `handleSavePlataEdit` (reparat in 31-02) si NU modifica linia `onNavigate('plati-scadente')` (~804) — ramane valida prin alias-ul din 31-08 si duce in hub pe tab-ul Facturi.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && grep -c 'onDeschideDetalii' components/UserProfile/FinanciarTab.tsx && grep -c '<FacturaDetaliu' components/UserProfile.tsx && grep -c 'onDeschideDetalii={setPlataDetaliuId}' components/UserProfile.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - `grep -c "onDeschideDetalii" components/UserProfile/FinanciarTab.tsx` >= 3 (interfata, destructurare, buton).
    - `grep -c "onDeschideDetalii?: (plataId: string) => void" components/UserProfile/FinanciarTab.tsx` = 1.
    - `components/UserProfile.tsx` contine `useState<string | null>(null)` pentru `plataDetaliuId`, `onDeschideDetalii={setPlataDetaliuId}` si `<FacturaDetaliu plataId={plataDetaliuId}`.
    - `grep -c "onNavigate('plati-scadente')" components/UserProfile.tsx` = 1 (link-ul vechi neatins).
    - `grep -c "descriere: editedPlata.descriere" components/UserProfile.tsx` = 1 (reparatia 31-02 intacta).
  </acceptance_criteria>
  <done>Din profilul oricarui sportiv, ADMIN_CLUB si INSTRUCTOR pot deschide detaliul unei facturi si folosi actiunea rapida.</done>
</task>

<task type="auto">
  <name>Task 2: Verificare live READ-ONLY a premiselor D-09 si a actiunii rapide (RLS, trigger, view)</name>
  <read_first>
    - .planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-03-SUMMARY.md (logica actiunii rapide)
    - sql/refactor/REFACTOR_FINANCIAL.sql (liniile 1-123 — versiunea din repo a recalculare_stare_plata / on_tranzactie_change / proceseaza_plata_factura)
    - supabase/migrations/20260309_create_rpc_proceseaza_incasare.sql (legatura normalizata prin tranzactie_plata)
    - .planning/phases/15-fix-rls-izolare-cross-club-pe-tabele-financiare-alocari-plat/15-01-SUMMARY.md (starea RLS financiara dupa faza 15)
  </read_first>
  <files>.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-07-SUMMARY.md</files>
  <action>
Regula: DOAR interogari SELECT. Nicio migratie, niciun DDL/DML, nicio modificare de politica — RLS si triggerele sunt explicit in afara scope-ului (31-CONTEXT domain). Lectie din memoria proiectului: auditurile RLS se fac pe `pg_policies` live, nu pe fisierele de migratie.

Daca sesiunea care executa are tool-ul Supabase MCP `execute_sql` (project_id `wuhidifzsutwgdfkwhmd`), ruleaza:
1. `SELECT tablename, policyname, cmd, roles, qual, with_check FROM pg_policies WHERE schemaname = 'public' AND tablename IN ('plati', 'tranzactii', 'tranzactie_plata') ORDER BY tablename, cmd, policyname;`
2. `SELECT proname, prosecdef, pg_get_functiondef(oid) FROM pg_proc WHERE pronamespace = 'public'::regnamespace AND proname IN ('recalculare_stare_plata', 'on_tranzactie_change', 'proceseaza_plata_factura');`
3. `SELECT tgname, tgrelid::regclass AS tabela, pg_get_triggerdef(oid) FROM pg_trigger WHERE NOT tgisinternal AND tgrelid IN ('public.tranzactii'::regclass, 'public.tranzactie_plata'::regclass, 'public.plati'::regclass);`
4. `SELECT pg_get_viewdef('public.view_plata_sportiv'::regclass, true);`

Consemneaza in `31-07-SUMMARY.md`, sectiunea "Verificare live D-09 / actiune rapida", raspunsuri explicite DA/NU la:
- (a) Poate un utilizator cu contextul activ INSTRUCTOR face UPDATE pe un rand `plati` din clubul sau? (citeste `qual`/`with_check` ale politicilor cu `cmd` UPDATE sau ALL pe `plati`, inclusiv functiile helper referite, ex. `plata_club_id`, `get_active_club_id`, `is_super_admin`).
- (b) `recalculare_stare_plata` live aduna si alocarile din `tranzactie_plata`, sau doar `tranzactii.plata_ids`? (daca doar plata_ids: o factura platita partial prin Jurnal Încasări multiplu poate ramane 'Achitat Parțial' dupa actiunea rapida — FacturaDetaliu afiseaza deja avertisment explicit in acest caz).
- (c) Exista trigger pe `tranzactie_plata` care recalculeaza starea facturii?
- (d) `view_plata_sportiv` leaga tranzactiile prin `plata_ids`, prin `tranzactie_plata` sau prin ambele?
- (e) `proceseaza_plata_factura` live: SECURITY DEFINER si fara verificare de rol? (confirma riscul acceptat T-31-03-04).

Daca raspunsul la (a) e NU: NU modifica RLS; scrie in SUMMARY un bloc "BLOCKER PENTRU UTILIZATOR" care explica ca INSTRUCTOR va vedea detaliul si butonul, dar corectia va fi refuzata de DB cu mesajul "Verificați permisiunile." (comportament deja implementat in 31-03), si propune un todo separat de RLS. Daca (b) sau (c) indica nealiniere, consemneaza-o ca risc cunoscut, fara reparatie.

Daca MCP Supabase NU e disponibil in sesiune: scrie in SUMMARY "NEVERIFICAT — sesiunea de executie nu are acces la Supabase MCP", listeaza cele 4 interogari de mai sus pentru rulare manuala si marcheaza itemul pentru verificarea umana de final de faza (31-09).
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && grep -c 'Verificare live D-09' .planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-07-SUMMARY.md"</automated>
  </verify>
  <acceptance_criteria>
    - `31-07-SUMMARY.md` contine sectiunea "Verificare live D-09 / actiune rapida" cu raspunsuri explicite (DA / NU / NEVERIFICAT) la punctele (a)-(e).
    - `git status` nu arata fisiere noi sub `supabase/migrations/` create de acest task si nu s-a rulat niciun `apply_migration` / DDL / DML (doar SELECT).
    - Daca (a) = NU, SUMMARY contine blocul "BLOCKER PENTRU UTILIZATOR".
  </acceptance_criteria>
  <done>Premisele D-09 si ale actiunii rapide sunt verificate pe DB-ul live (sau explicit marcate NEVERIFICAT cu interogarile gata de rulare), fara nicio modificare a bazei de date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INSTRUCTOR (browser) → `public.plati` UPDATE prin ecranul de detaliu | Accesul real e decis de RLS, nu de butonul din UI |
| sesiunea de executie → DB live | Verificarea trebuie sa fie strict read-only pe un sistem financiar in productie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07-01 | Elevation of Privilege | Buton "Detalii" + actiune rapida pentru INSTRUCTOR (D-09) | mitigate | Gating UI in FacturaDetaliu (31-03) + verificare live a politicilor RLS (Task 2); daca RLS refuza, UI-ul afiseaza refuzul, fara stare locala falsa; nicio largire de RLS in aceasta faza |
| T-31-07-02 | Tampering | Verificarea live ar putea modifica accidental date de productie | mitigate | Doar SELECT-uri enumerate explicit; acceptance verifica absenta migratiilor/DDL |
| T-31-07-03 | Information Disclosure | Instructorul vede istoricul tranzactiilor unei facturi | accept | Instructorul vedea deja aceleasi date in tab-ul Istoric Financiar (vizualizarePlati) si in PlataEditModal |
| T-31-07-SC | Tampering | npm installs | accept | Zero pachete noi |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Inventarul complet al fazei e in `31-01-PLAN.md` (sectiunea omonima). Acest plan (31-07) produce:
- Prop optional nou `onDeschideDetalii?: (plataId: string) => void` pe `FinanciarTab`.
- In `UserProfile`: starea locala `plataDetaliuId` si montarea `<FacturaDetaliu />`.
- Raportul "Verificare live D-09 / actiune rapida" in `31-07-SUMMARY.md`.
</artifacts_this_phase_produces>

<verification>
1. `npm run lint` — cod 0.
2. Gardele grep din Task 1.
3. SUMMARY contine raspunsurile (a)-(e).
</verification>

<success_criteria>
- D-09: INSTRUCTOR are un drum functional catre ecranul de detaliu si actiunea rapida.
- Premisele de securitate si de calcul ale actiunii rapide sunt cunoscute si documentate, fara nicio modificare DB.
</success_criteria>

<output>
Creeaza `.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-07-SUMMARY.md` cu sectiunea "Verificare live D-09 / actiune rapida".
</output>
