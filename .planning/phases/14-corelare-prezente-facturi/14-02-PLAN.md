---
phase: 14-corelare-prezente-facturi
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - components/UserProfile/FinanciarTab.tsx
  - components/Plati/PlatiScadente.tsx
autonomous: false
requirements: [PLF-01, PLF-04]

must_haves:
  truths:
    - "Modalul de detalii factură din FinanciarTab afișează 'Prezențe în [luna]: N ▾' cu expandare listă date (PLF-01)"
    - "Fiecare rând de factură din PlatiScadente are o secțiune expandabilă inline cu prezențele lunii (PLF-01)"
    - "Butonul Șterge din PlatiScadente este dezactivat cu tooltip pentru facturi Achitat (PLF-04)"
    - "Butonul Șterge din FinanciarTab este dezactivat cu tooltip pentru facturi Achitat (PLF-04)"
    - "confirmDelete din PlatiScadente blochează ștergerea facturilor Achitat (guard server-side, PLF-04)"
  artifacts:
    - path: "components/Plati/PlatiScadente.tsx"
      provides: "Rând expandabil cu prezențe + restricție ștergere status"
      contains: "usePrezenteLuna"
    - path: "components/UserProfile/FinanciarTab.tsx"
      provides: "Secțiune prezențe în modal + restricție ștergere status"
      contains: "usePrezenteLuna"
  key_links:
    - from: "components/Plati/PlatiScadente.tsx"
      to: "hooks/usePrezenteLuna.ts"
      via: "usePrezenteLuna(sportivId, luna, an, isExpanded)"
      pattern: "usePrezenteLuna"
    - from: "components/UserProfile/FinanciarTab.tsx"
      to: "hooks/usePrezenteLuna.ts"
      via: "usePrezenteLuna în modal"
      pattern: "usePrezenteLuna"
---

<objective>
PLF-01 (prezențe corelate cu factura, în ambele locuri unde se vede o factură) și PLF-04 (restricție ștergere la facturi neplatite, în ambele module). Ambele cerințe ating aceleași două fișiere (`PlatiScadente.tsx` și `FinanciarTab.tsx`), deci sunt grupate într-un singur plan pentru a evita conflictul de fișiere cu celelalte planuri din wave 2.

Purpose: Adminul (și sportivul, read-only) văd câte prezențe a avut sportivul în luna facturii, direct în contextul financiar; facturile achitate devin imposibil de șters din UI și din handler.
Output: Două componente extinse cu afișare prezențe expandabilă + guard de ștergere pe status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-corelare-prezente-facturi/14-CONTEXT.md
@.planning/phases/14-corelare-prezente-facturi/14-RESEARCH.md
@CLAUDE.md
@hooks/usePrezenteLuna.ts
@utils/luniLipsa.ts
@components/Plati/PlatiScadente.tsx
@components/UserProfile/FinanciarTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PLF-01 + PLF-04 în PlatiScadente (rând expandabil + guard ștergere)</name>
  <read_first>
    - components/Plati/PlatiScadente.tsx liniile 619-744 (tabel desktop + carduri mobil + butoane acțiuni)
    - components/Plati/PlatiScadente.tsx liniile 354-389 (confirmDelete — handlerul de ștergere)
    - hooks/usePrezenteLuna.ts (semnătura: usePrezenteLuna(sportivId, luna, an, enabled))
    - utils/luniLipsa.ts (formatLuna)
    - 14-RESEARCH.md Pitfall 1 (buton disabled nu afișează title — wrap în span), Pitfall 3 (PlatiScadente NU are modal detalii — expandare inline), Pitfall 4 (facturi familie: omite prezențe), Pitfall 5 (luna 1-indexed)
  </read_first>
  <action>
    PLF-01 în PlatiScadente: adaugă un state `expandedId: string | null` și un toggle per rând. Pentru fiecare factură cu `p.sportiv_id` non-null și `p.tip === 'Abonament'`, derivă luna/an: folosește `p.luna`/`p.an` dacă sunt non-null, altfel derivă din `p.data` (`new Date(p.data.slice(0,10))` → `getMonth()+1`, `getFullYear()`). Pentru facturi cu `familie_id` (sportiv_id null) NU afișa prezențe (Pitfall 4). În tabelul desktop (după `</tr>` de date, linia ~661), adaugă un al doilea `<tr>` colapsabil afișat doar când `expandedId === p.id`, cu un `colSpan` care acoperă toate coloanele. În interiorul lui apelează `usePrezenteLuna(p.sportiv_id, luna, an, expandedId === p.id)` printr-un sub-component dedicat (ex. `PrezenteFacturaRow`) ca să respecți regulile hooks (un hook per rând nu se poate apela condiționat în map — extrage într-un component copil care primește sportivId/luna/an și se montează doar când e expandat). Sub-componentul afișează "Prezențe în [formatLuna(luna,an)]: N ▾" unde N = numărul de date returnate; la expandare listează datele formatate `toLocaleDateString('ro-RO')`. Adaugă un buton/chevron care setează `expandedId` în coloana Descriere sau printr-un buton nou cu CalendarDaysIcon. Pentru mobile cards (liniile 682-743), adaugă același accordion sub cardul fiecărei facturi.

    PLF-04 în PlatiScadente: în butonul de ștergere (TrashIcon) din desktop (linia ~658) și mobil (linia ~734), când `p.status === 'Achitat'` randează butonul wrapped într-un `<span title="Facturile achitate nu pot fi șterse" className="cursor-not-allowed">` cu butonul `disabled` și `className="pointer-events-none"` (Pitfall 1); altfel randează butonul activ existent. În handlerul `confirmDelete` (linia 354), adaugă la început un guard server-side: re-fetch statusul real din DB cu `supabase.from('plati').select('status').eq('id', id).maybeSingle()`; dacă `status === 'Achitat'` → `showError('Ștergere imposibilă', 'Facturile achitate nu pot fi șterse.')`, `setPlataToDelete(null)`, `setIsDeleting(false)`, return. Acest dublu-check previne bypass-ul UI (Security Domain).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "PlatiScadente" || echo "no type errors in PlatiScadente"</automated>
  </verify>
  <acceptance_criteria>
    - PlatiScadente importă și folosește `usePrezenteLuna` printr-un sub-component montat doar când rândul e expandat (lazy-load, enabled controlat)
    - Facturile de familie (sportiv_id null) NU afișează secțiunea de prezențe
    - Butonul Șterge e wrapped în `<span title=...>` cu buton `disabled pointer-events-none` când status === 'Achitat'
    - `confirmDelete` re-verifică statusul din DB și blochează ștergerea pentru 'Achitat' (grep: `status === 'Achitat'` în confirmDelete)
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>Fiecare rând Abonament din PlatiScadente expandează prezențele lunii; facturile achitate nu pot fi șterse din UI nici din handler.</done>
</task>

<task type="auto">
  <name>Task 2: PLF-01 + PLF-04 în FinanciarTab (secțiune prezențe în modal + guard ștergere)</name>
  <read_first>
    - components/UserProfile/FinanciarTab.tsx liniile 278-404 (modalul de detalii factură)
    - components/UserProfile/FinanciarTab.tsx liniile 259-269 (butoanele Editează/Șterge pe card)
    - components/UserProfile/FinanciarTab.tsx liniile 12-31 (FacturaEntry/VizualizarePlata: detalii are plata_id, sportiv_id, familie_id, data_emitere, status — NU are luna/an, deci se derivă din data_emitere)
    - hooks/usePrezenteLuna.ts, utils/luniLipsa.ts (formatLuna)
    - 14-RESEARCH.md Pitfall 1, 4, 5
  </read_first>
  <action>
    PLF-01 în FinanciarTab: în corpul modalului de detalii (după secțiunea "Sume + progress", înainte de "Timeline încasări", ~linia 337), adaugă o secțiune "Prezențe". Derivă luna/an din `p.data_emitere` (VizualizarePlata nu are luna/an): `const d = new Date(p.data_emitere.slice(0,10)); const luna = d.getMonth()+1; const an = d.getFullYear()`. Afișează prezențele DOAR dacă `!p.familie_id` și `p.sportiv_id` non-null (Pitfall 4). Folosește un sub-component (ex. `PrezenteModalSection`) care apelează `usePrezenteLuna(p.sportiv_id, luna, an, true)` și afișează "Prezențe în [formatLuna(luna,an)]: N ▾" cu un toggle local `useState` care expandează lista datelor formatate `toLocaleDateString('ro-RO')`. Folosește clase Tailwind existente (consistent cu restul modalului: bg-slate-800/50, border-slate-700/40). Sub-componentul se definește în același fișier (nu se modifică ui.tsx).

    PLF-04 în FinanciarTab: la butonul "Șterge" (liniile 265-268), când `p.status === 'Achitat'` randează butonul wrapped într-un `<span title="Facturile achitate nu pot fi șterse" className="cursor-not-allowed">` cu butonul `disabled className="pointer-events-none"` (Pitfall 1); altfel butonul activ existent care apelează `setPlataToDelete`. Notă: handlerul efectiv de ștergere pentru FinanciarTab e în UserProfile.tsx (PlataEditModal/setPlataToDelete) — guard-ul de UI aici previne acțiunea; guard-ul server-side rămâne în confirmDelete-urile din PlatiScadente/GestiuneFacturi. Pentru FinanciarTab e suficientă restricția de UI plus guard-ul existent în lanțul de ștergere; dacă există un handler de delete accesibil, adaugă același check de status.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "FinanciarTab" || echo "no type errors in FinanciarTab"</automated>
  </verify>
  <acceptance_criteria>
    - Modalul de detalii factură din FinanciarTab conține o secțiune "Prezențe în [luna]: N ▾" expandabilă, alimentată de `usePrezenteLuna`
    - Secțiunea prezențe e omisă pentru facturi de familie (familie_id non-null)
    - Butonul Șterge din FinanciarTab e dezactivat cu tooltip pentru status === 'Achitat' (wrap în span)
    - Niciun import din ui.tsx nu e modificat; ui.tsx nu e atins
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>Modalul de factură din profilul sportivului arată prezențele lunii facturate; butonul de ștergere e blocat pentru facturi achitate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PLF-01 (prezențe în ambele locuri) + PLF-04 (restricție ștergere) în PlatiScadente și în modalul de factură din profilul sportivului.</what-built>
  <how-to-verify>
    1. Loghează-te ca ADMIN_CLUB. Mergi la Management Facturi & Plăți (PlatiScadente).
    2. Pentru o factură de tip Abonament a unui sportiv cu prezențe în luna respectivă: extinde rândul → confirmă că apare "Prezențe în [luna]: N ▾" cu N corect și că expandarea listează datele exacte.
    3. Confirmă că o factură de familie NU afișează secțiunea de prezențe.
    4. Pentru o factură cu status Achitat: confirmă că butonul Șterge e dezactivat și că la hover apare tooltipul "Facturile achitate nu pot fi șterse".
    5. Pentru o factură Neachitat: confirmă că ștergerea funcționează normal.
    6. Deschide profilul unui sportiv → tab Istoric Financiar → click pe o factură Abonament → confirmă secțiunea "Prezențe în [luna]: N ▾" în modal și butonul Șterge dezactivat pentru facturi achitate.
  </how-to-verify>
  <resume-signal>Scrie "approved" sau descrie problemele observate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (plati delete) | ștergere factură — input id netrusted |
| client → Supabase (vedere_prezenta_sportiv) | citire prezențe per sportiv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | confirmDelete PlatiScadente | mitigate | Re-fetch status din DB + guard status === 'Achitat' înainte de delete (nu doar UI) |
| T-14-05 | Information Disclosure | usePrezenteLuna în modal | accept | View vedere_prezenta_sportiv e RLS-scoped; SPORTIV vede doar propriile prezențe |
| T-14-06 | Elevation of Privilege | buton Șterge UI | mitigate | UI disabled + guard server-side; RLS pe plati blochează delete cross-club |
</threat_model>

<verification>
- `npx tsc --noEmit` fără erori noi în PlatiScadente.tsx și FinanciarTab.tsx
- `git diff --name-only` NU include types.ts, ui.tsx, DataContext, NavigationContext
- Hooks `usePrezenteLuna` apelat doar prin sub-componente (nu condiționat în map) — fără warning React hooks
</verification>

<success_criteria>
- PLF-01 vizibil în ambele locuri (PlatiScadente inline + modal FinanciarTab)
- PLF-04 activ în ambele module (UI + guard server-side în PlatiScadente)
- Facturile de familie omit prezențele; luna e 1-indexed corect
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-02)
- `components/Plati/PlatiScadente.tsx` — rând/card expandabil cu prezențe (PLF-01) + restricție ștergere UI și server-side (PLF-04)
- `components/UserProfile/FinanciarTab.tsx` — secțiune prezențe în modalul de detalii (PLF-01) + restricție ștergere UI (PLF-04)
</artifacts_produced>

<output>
Create `.planning/phases/14-corelare-prezente-facturi/14-02-SUMMARY.md` when done
</output>
