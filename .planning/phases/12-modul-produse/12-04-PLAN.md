---
phase: 12-modul-produse
plan: 04
type: execute
wave: 4
depends_on: [12-03]
files_modified:
  - services/produseService.ts
  - components/Produse/VanzareModal.tsx
  - components/Produse/index.tsx
  - components/AppRouter.tsx
  - components/LazyComponents.tsx
  - components/SportivDashboard/index.tsx
autonomous: true
requirements: [PRD-04]

must_haves:
  truths:
    - "Există components/Produse/VanzareModal.tsx — modal selectare sportiv + produse + cantități"
    - "Tab 'vanzari' din ProduseManagement afișează lista vânzărilor + buton Vânzare Nouă"
    - "La creare vânzare: se creează Plata în plati cu suma totală + tip 'Echipamente'"
    - "Stoc_curent al variantelor scade la vânzare"
    - "SportivDashboard are tab/secțiune 'Echipamente' cu vânzările proprii ale sportivului"
    - "produseService.ts are fetchVanzari + createVanzare"
  artifacts:
    - path: "components/Produse/VanzareModal.tsx"
      provides: "Modal vânzare produs cu selectare sportiv + linii produse"
    - path: "components/Produse/index.tsx"
      provides: "Tab 'vanzari' funcțional"
    - path: "services/produseService.ts"
      provides: "fetchVanzari + createVanzare adăugate"
---

<objective>
Implementează modulul de vânzări produse. ADMIN_CLUB poate înregistra vânzări (selectează sportiv + produse), se generează automat o factură în modulul Plăți, stocul scade. Sportivii văd un tab "Echipamente" în dashboard cu istoricul propriu.

Purpose: Integrare completă catalog → vânzare → plată. Stocul e actualizat live.
Output: VanzareModal + tab vanzari în ProduseManagement + tab Echipamente în SportivDashboard.
</objective>

<context>
@.planning/phases/12-modul-produse/12-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: produseService.ts — fetchVanzari + createVanzare cu creare Plata</name>
  <files>services/produseService.ts</files>
  <read_first>
    - services/produseService.ts (starea curentă — adaugă la final)
    - types.ts (ProdusVanzareDB, ProdusVanzareDetaliuDB, ProdusVanzare, Plata — structura Plata liniile 153-175 pentru câmpurile required la insert)
  </read_first>
  <action>
    Adaugă la finalul `services/produseService.ts`:

    ```typescript
    export async function fetchVanzari(): Promise<ProdusVanzare[]> {
      const { data, error } = await supabaseClient
        .from('produse_vanzari')
        .select(`
          *,
          detalii:produse_vanzari_detalii(*),
          sportiv:sportivi(id, prenume, nume)
        `)
        .order('data_vanzare', { ascending: false })
        .order('created_at', { ascending: false });
      if (error) throw new Error(`fetchVanzari: ${error.message}`);
      return (data ?? []).map((v: any) => ({
        ...v,
        sportiv_nume: v.sportiv ? `${v.sportiv.prenume} ${v.sportiv.nume}` : null,
      })) as ProdusVanzare[];
    }

    export async function fetchVanzariSportiv(sportivId: string): Promise<ProdusVanzare[]> {
      const { data, error } = await supabaseClient
        .from('produse_vanzari')
        .select(`*, detalii:produse_vanzari_detalii(*)`)
        .eq('sportiv_id', sportivId)
        .order('data_vanzare', { ascending: false });
      if (error) throw new Error(`fetchVanzariSportiv: ${error.message}`);
      return (data ?? []) as ProdusVanzare[];
    }

    export async function createVanzare(input: {
      club_id: string;
      sportiv_id: string | null;
      data_vanzare: string;
      observatii?: string;
      linii: { varianta_id: string; cantitate: number; pret_vanzare_snapshot: number; pret_intrare_snapshot: number; denumire_snapshot: string }[];
      tip_plata_id: string;
    }): Promise<ProdusVanzareDB> {
      const total = input.linii.reduce((s, l) => s + l.pret_vanzare_snapshot * l.cantitate, 0);

      // 1. Creează Plata în modulul Plăți existent
      let plataId: string | null = null;
      if (input.sportiv_id) {
        const { data: plata, error: errPlata } = await supabaseClient
          .from('plati')
          .insert({
            sportiv_id: input.sportiv_id,
            club_id: input.club_id,
            tip_plata_id: input.tip_plata_id,
            suma: total,
            status: 'Neachitat',
            data: input.data_vanzare,
            descriere: `Echipamente: ${input.linii.map(l => l.denumire_snapshot).join(', ')}`,
          })
          .select('id')
          .single();
        if (errPlata) throw new Error(`createVanzare plata: ${errPlata.message}`);
        plataId = plata.id;
      }

      // 2. Creează header vânzare
      const { data: vanzare, error: errV } = await supabaseClient
        .from('produse_vanzari')
        .insert({
          club_id: input.club_id,
          sportiv_id: input.sportiv_id,
          plata_id: plataId,
          data_vanzare: input.data_vanzare,
          observatii: input.observatii ?? null,
          total_vanzare: total,
        })
        .select()
        .single();
      if (errV) throw new Error(`createVanzare header: ${errV.message}`);

      // 3. Inserează linii vânzare
      const detalii = input.linii.map(l => ({
        vanzare_id: vanzare.id,
        varianta_id: l.varianta_id,
        cantitate: l.cantitate,
        pret_vanzare_snapshot: l.pret_vanzare_snapshot,
        pret_intrare_snapshot: l.pret_intrare_snapshot,
        denumire_snapshot: l.denumire_snapshot,
      }));
      const { error: errD } = await supabaseClient.from('produse_vanzari_detalii').insert(detalii);
      if (errD) throw new Error(`createVanzare detalii: ${errD.message}`);

      // 4. Scade stoc per variantă
      await Promise.all(input.linii.map(async (l) => {
        const { data: v } = await supabaseClient
          .from('produse_variante')
          .select('stoc_curent')
          .eq('id', l.varianta_id)
          .single();
        await supabaseClient
          .from('produse_variante')
          .update({ stoc_curent: Math.max(0, (v?.stoc_curent ?? 0) - l.cantitate), updated_at: new Date().toISOString() })
          .eq('id', l.varianta_id);
      }));

      return vanzare;
    }
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "produseService" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -c "fetchVanzari\|createVanzare\|fetchVanzariSportiv" services/produseService.ts</automated>
  </verify>
  <acceptance_criteria>
    - `fetchVanzari`, `fetchVanzariSportiv`, `createVanzare` exportate
    - `createVanzare` crează Plata (când sportiv_id prezent) + header vânzare + linii + scade stoc
    - Stoc nu scade sub 0 (`Math.max(0, ...)`)
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>fetchVanzari + fetchVanzariSportiv + createVanzare adăugate cu creare Plata + scădere stoc.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: VanzareModal.tsx — modal înregistrare vânzare produse</name>
  <files>components/Produse/VanzareModal.tsx</files>
  <read_first>
    - components/Produse/IntrareMarfaModal.tsx (pattern tabel linii dinamic — replică structura)
    - components/ui.tsx (Modal, Button, Input, Select)
    - types.ts (Produs, ProdusVariantaDB, Sportiv)
  </read_first>
  <action>
    Creează `components/Produse/VanzareModal.tsx`.

    Props:
    ```typescript
    interface VanzareModalProps {
      produse: Produs[];
      sportivi: Sportiv[];          // pentru selectare sportiv
      clubId: string;
      tipPlataId: string;           // ID-ul tipului de plată 'Echipamente' (preluat din tipuriPlati)
      onSave: () => void;
      onClose: () => void;
      sportivPreSelectat?: Sportiv; // când se deschide din profilul sportivului
    }
    ```

    State:
    - `sportivId: string` (pre-populat dacă `sportivPreSelectat`)
    - `dataVanzare: string` (today ISO)
    - `observatii: string`
    - `linii: LinieVanzare[]` unde `LinieVanzare = { produs_id: string; varianta_id: string; cantitate: string }`
    - `saving: boolean`, `error: string | null`

    **UI:**

    Titlu modal: "Vânzare Echipamente"

    **Selectare sportiv** (ascuns dacă `sportivPreSelectat`):
    - Select cu opțiuni din `sportivi` sortați alfabetic
    - Label: `${s.prenume} ${s.nume}` | value: `s.id`

    **Data vânzării**: Input type="date"

    **Tabel linii** (identic cu IntrareMarfaModal dar fără câmpul preț intrare):
    - Coloane: Produs | Variantă | Cantitate | Preț unitar (RON) | Subtotal | X
    - Preț unitar: calculat automat din `varianta.pret_vanzare` (read-only display)
    - Subtotal: `pret_vanzare * cantitate` (display)

    **Sumar total** sub tabel:
    ```jsx
    <div className="flex justify-end items-center gap-2 mt-3">
      <span className="text-slate-400 text-sm">Total vânzare:</span>
      <span className="text-xl font-bold text-white">{totalCalculat.toFixed(2)} RON</span>
    </div>
    ```

    **Textarea Observații** (opțional)

    **Footer**: Anulează | Salvează vânzarea

    **handleSave:**
    ```
    1. Validare: sportivId selectat, cel puțin 1 linie, cantitate > 0
    2. Construiește linii cu snapshot-uri:
       linii = linii.map(l => {
         const varianta = produse.find(p => p.id === l.produs_id)?.variante.find(v => v.id === l.varianta_id)
         const produs = produse.find(p => p.id === l.produs_id)
         return {
           varianta_id: l.varianta_id,
           cantitate: +l.cantitate,
           pret_vanzare_snapshot: varianta?.pret_vanzare ?? 0,
           pret_intrare_snapshot: varianta?.pret_intrare ?? 0,
           denumire_snapshot: `${produs?.denumire} ${varianta?.culoare ?? ''} ${varianta?.marime ?? ''}`.trim()
         }
       })
    3. createVanzare({ club_id, sportiv_id, data_vanzare, observatii, linii, tip_plata_id })
    4. onSave()
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "VanzareModal" ; echo "exit:$?"</automated>
    <automated>test -f "C:/Users/lungu/portal-phihau/components/Produse/VanzareModal.tsx" && echo "exists"</automated>
  </verify>
  <acceptance_criteria>
    - `components/Produse/VanzareModal.tsx` există
    - Select sportiv + tabel linii + total calculat
    - handleSave construiește linii cu snapshots și apelează createVanzare
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>VanzareModal creat cu selectare sportiv, tabel linii, calcul total automat, snapshots la save.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ProduseManagement — activare tab 'vanzari'</name>
  <files>components/Produse/index.tsx</files>
  <read_first>
    - components/Produse/index.tsx (starea curentă)
    - types.ts (ProdusVanzare, Sportiv)
  </read_first>
  <action>
    Modifică `components/Produse/index.tsx`:

    1. Adaugă state: `vanzari: ProdusVanzare[]`, `showVanzareModal: boolean`
    2. Adaugă în useEffect: `fetchVanzari().then(setVanzari)` (paralel)
    3. Preia `sportivi` și `tipuriPlati` din `useData()` pentru a le pasa la VanzareModal
    4. Determină `tipPlataEchipamente`: `tipuriPlati.find(t => t.denumire.toLowerCase().includes('echipament'))?.id ?? ''`
       (dacă nu există, admin trebuie să creeze manual tipul de plată "Echipamente" — adaugă un `Alert warning` dacă `tipPlataEchipamente` e gol)

    5. Înlocuiește placeholder tab 'vanzari' cu:

    ```jsx
    {activeTab === 'vanzari' && (
      <div className="space-y-4">
        <div className="flex justify-between items-center">
          <h2 className="text-lg font-bold text-white">Vânzări Echipamente</h2>
          <Button onClick={() => setShowVanzareModal(true)}>+ Vânzare Nouă</Button>
        </div>

        {!tipPlataEchipamente && (
          <Alert variant="warning">
            Tipul de plată "Echipamente" lipsește. Adaugă-l în Config. Abonamente → Tipuri Plăți.
          </Alert>
        )}

        {/* Tabel vânzări desktop */}
        <div className="hidden md:block bg-[var(--t-bg)] border border-[var(--t-border)] rounded-xl overflow-hidden">
          <table className="w-full text-sm text-left">
            <thead style={{ background: 'var(--t-table-header-bg)', color: 'var(--t-table-header-text)' }}>
              <tr className="border-b border-[var(--t-border)]">
                <th className="px-4 py-3 text-xs font-bold uppercase tracking-wider">Data</th>
                <th className="px-4 py-3 text-xs font-bold uppercase tracking-wider">Sportiv</th>
                <th className="px-4 py-3 text-xs font-bold uppercase tracking-wider">Produse</th>
                <th className="px-4 py-3 text-xs font-bold uppercase tracking-wider text-right">Total</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-[var(--t-border)]">
              {vanzari.map(v => (
                <tr key={v.id} className="hover:bg-[var(--t-table-row-hover)]">
                  <td className="px-4 py-3 text-slate-300">{new Date(v.data_vanzare).toLocaleDateString('ro-RO')}</td>
                  <td className="px-4 py-3 text-white">{v.sportiv_nume ?? '—'}</td>
                  <td className="px-4 py-3 text-slate-300">{v.detalii.map(d => d.denumire_snapshot).join(', ')}</td>
                  <td className="px-4 py-3 text-right font-bold text-white">{v.total_vanzare.toFixed(2)} RON</td>
                </tr>
              ))}
            </tbody>
          </table>
          {vanzari.length === 0 && <div className="py-12 text-center text-slate-400">Nicio vânzare înregistrată</div>}
        </div>

        {/* Carduri mobile */}
        <div className="md:hidden space-y-2">
          {vanzari.map(v => (
            <div key={v.id} className="bg-[var(--t-bg)] border border-[var(--t-border)] rounded-xl px-4 py-3">
              <div className="flex justify-between">
                <span className="text-white font-bold text-sm">{v.sportiv_nume ?? '—'}</span>
                <span className="text-white font-bold text-sm">{v.total_vanzare.toFixed(2)} RON</span>
              </div>
              <div className="text-slate-400 text-xs mt-1">{new Date(v.data_vanzare).toLocaleDateString('ro-RO')} · {v.detalii.length} produse</div>
            </div>
          ))}
          {vanzari.length === 0 && <div className="py-8 text-center text-slate-400">Nicio vânzare</div>}
        </div>

        {showVanzareModal && (
          <VanzareModal
            produse={produse}
            sportivi={sportivi}
            clubId={activeRoleContext?.club_id ?? ''}
            tipPlataId={tipPlataEchipamente}
            onSave={async () => {
              setShowVanzareModal(false);
              const [p, v] = await Promise.all([fetchProduse(), fetchVanzari()]);
              setProduse(p); setVanzari(v);
            }}
            onClose={() => setShowVanzareModal(false)}
          />
        )}
      </div>
    )}
    ```

    Importă `VanzareModal`, `fetchVanzari`, `ProdusVanzare` corespunzător.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "Produse" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Tab 'vanzari' afișează lista vânzărilor + buton "Vânzare Nouă"
    - Alert warning când tipul de plată "Echipamente" lipsește
    - VanzareModal se deschide și crează vânzarea + scade stocul
    - Build + TypeScript trec
  </acceptance_criteria>
  <done>Tab Vânzări funcțional cu tabel, modal, alert tip plată lipsă.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: SportivDashboard — tab Echipamente cu catalog + istoricul propriu</name>
  <files>components/SportivDashboard/index.tsx</files>
  <read_first>
    - components/SportivDashboard/index.tsx (structura curentă — tabs existente, props disponibile)
    - services/produseService.ts (fetchProduse, fetchVanzariSportiv)
    - types.ts (Produs, ProdusVanzare, User)
  </read_first>
  <action>
    Modifică `components/SportivDashboard/index.tsx`:

    1. Adaugă state local: `produseCatalog: Produs[]`, `vanzariMele: ProdusVanzare[]`, `loadingProduse: boolean`

    2. Adaugă useEffect (la mount când sportiv):
    ```typescript
    useEffect(() => {
      if (!currentUser?.id) return;
      setLoadingProduse(true);
      Promise.all([
        fetchProduse(),
        fetchVanzariSportiv(viewedUser.id) // sau currentUser.id — sportivul propriu
      ]).then(([p, v]) => {
        setProduseCatalog(p.filter(p2 => p2.activ));
        setVanzariMele(v);
        setLoadingProduse(false);
      }).catch(() => setLoadingProduse(false));
    }, [viewedUser.id]);
    ```

    3. Adaugă tab "Echipamente" în lista de tab-uri existente (ultimul tab sau după Plăți).

    4. Render tab Echipamente:

    ```jsx
    {activeTab === 'echipamente' && (
      <div className="space-y-6">
        {/* Istoricul achizițiilor */}
        <div>
          <h3 className="text-base font-bold text-white mb-3">Achizițiile mele</h3>
          {loadingProduse ? (
            <div className="text-slate-400 text-sm">Se încarcă...</div>
          ) : vanzariMele.length === 0 ? (
            <div className="text-slate-400 text-sm py-4 text-center">Nicio achiziție înregistrată</div>
          ) : (
            <div className="space-y-2">
              {vanzariMele.map(v => (
                <div key={v.id} className="bg-[var(--t-bg)] border border-[var(--t-border)] rounded-xl px-4 py-3">
                  <div className="flex justify-between items-start">
                    <div>
                      <div className="text-white font-medium text-sm">
                        {v.detalii.map(d => d.denumire_snapshot).join(', ')}
                      </div>
                      <div className="text-slate-400 text-xs mt-1">
                        {new Date(v.data_vanzare).toLocaleDateString('ro-RO')} · {v.detalii.length} produse
                      </div>
                    </div>
                    <span className="text-white font-bold text-sm shrink-0">{v.total_vanzare.toFixed(2)} RON</span>
                  </div>
                </div>
              ))}
            </div>
          )}
        </div>

        {/* Catalog produse disponibile */}
        <div>
          <h3 className="text-base font-bold text-white mb-3">Produse disponibile</h3>
          {loadingProduse ? (
            <div className="text-slate-400 text-sm">Se încarcă...</div>
          ) : produseCatalog.length === 0 ? (
            <div className="text-slate-400 text-sm py-4 text-center">Niciun produs în catalog</div>
          ) : (
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
              {produseCatalog.map(p => {
                const varianteActive = p.variante.filter(v => v.activa);
                const pretMin = Math.min(...varianteActive.map(v => v.pret_vanzare));
                const pretMax = Math.max(...varianteActive.map(v => v.pret_vanzare));
                return (
                  <div key={p.id} className="bg-[var(--t-bg)] border border-[var(--t-border)] rounded-xl px-4 py-3">
                    <div className="text-white font-medium text-sm">{p.denumire}</div>
                    <div className="text-slate-400 text-xs mt-1">{p.categorie?.denumire}</div>
                    <div className="text-slate-300 text-sm font-bold mt-2">
                      {varianteActive.length === 0 ? 'Indisponibil' :
                        pretMin === pretMax ? `${pretMin.toFixed(2)} RON` :
                        `${pretMin.toFixed(2)} – ${pretMax.toFixed(2)} RON`
                      }
                    </div>
                    {varianteActive.length > 0 && (
                      <div className="flex flex-wrap gap-1 mt-2">
                        {varianteActive.map(v => (
                          <span key={v.id} className="text-xs bg-slate-700 text-slate-300 rounded px-2 py-0.5">
                            {[v.culoare, v.marime].filter(Boolean).join(' ') || 'Standard'}
                          </span>
                        ))}
                      </div>
                    )}
                  </div>
                );
              })}
            </div>
          )}
        </div>
      </div>
    )}
    ```

    **IMPORTANT:** Sportivul vede DOAR `pret_vanzare` — NU `pret_intrare`. Verifică că `pret_intrare` nu apare nicăieri în view-ul sportivului.

    Importă `fetchProduse`, `fetchVanzariSportiv` din `'../../services/produseService'`.
    Importă `Produs`, `ProdusVanzare` din `'../../types'`.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "SportivDashboard" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -c "echipamente\|fetchVanzariSportiv\|vanzariMele" components/SportivDashboard/index.tsx</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Tab "Echipamente" adăugat în SportivDashboard
    - Sportivul vede istoricul propriilor achiziții (data, produse, total)
    - Sportivul vede catalogul cu prețuri de vânzare (NU pret_intrare)
    - `npx tsc --noEmit` + `npx vite build` trec
  </acceptance_criteria>
  <done>SportivDashboard tab Echipamente: catalog + istoricul achizițiilor proprii, fără pret_intrare.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` + `npx vite build` — zero erori
2. Manual (admin): tab "Vânzări" → "Vânzare Nouă" → selectez sportiv + 2 produse → Salvez
3. Manual: vânzarea apare în tabel cu sportiv, produse, total
4. Manual: tab Catalog → stocul variantelor scăzut
5. Manual: în modulul Plăți → există factură nouă pe sportivul respectiv cu suma vânzării
6. Manual (sportiv): login ca SPORTIV → Dashboard → tab "Echipamente" → vede achiziția + catalogul produselor cu prețuri vânzare
7. Manual: sportivul NU vede pret_intrare nicăieri
</verification>

<success_criteria>
- [ ] Tab "Vânzări" în ProduseManagement funcțional (PRD-04)
- [ ] VanzareModal: selectare sportiv + produse → generează Plata în modulul Plăți (PRD-04)
- [ ] Stoc_curent scade la vânzare (PRD-04)
- [ ] SportivDashboard tab "Echipamente": catalog vizibil + istoricul propriu (PRD-05)
- [ ] Sportivul vede DOAR pret_vanzare (PRD-05)
- [ ] Build + TypeScript trec
</success_criteria>

<output>
Create `.planning/phases/12-modul-produse/12-04-SUMMARY.md` when done
</output>
