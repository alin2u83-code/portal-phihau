---
phase: 31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa
plan: 06
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - components/Plati/TipuriAbonament.tsx
  - components/Plati/ConfigurarePreturi.tsx
  - components/Plati/Reduceri.tsx
  - components/Plati/TaxeAnuale.tsx
  - components/Grade/GestionareNomenclatoare.tsx
  - components/Plati/hub/TabConfigurare.tsx
autonomous: true
requirements: [D-04]
user_setup: []

must_haves:
  truths:
    - "Tab-ul Configurare randeaza, dupa sectiunea activa, una din TipuriAbonamentManagement / ConfigurarePreturi / ReduceriManagement / TaxeAnuale / GestionareNomenclatoare cu props identice celor din AppRouter (D-04)"
    - "Sectiunea Taxe Anuale pastreaza guard-ul vechi isSuperAdmin || isAdminClub — alt rol primeste AccessDenied in interiorul tab-ului"
    - "GestionareNomenclatoare primeste in continuare plati NEFILTRAT si ReduceriManagement reduceri NEFILTRAT (verificarea de utilizare a tipului de plata vede toate facturile, ca azi)"
    - "Montate in hub, cele 5 componente nu mai afiseaza propriul buton de intoarcere; standalone se comporta identic"
  artifacts:
    - path: "components/Plati/hub/TabConfigurare.tsx"
      provides: "Tab-ul Configurare al hub-ului"
      exports: ["TabConfigurare"]
      min_lines: 45
    - path: "components/Plati/TaxeAnuale.tsx"
      provides: "Prop optional hideBackButton"
      contains: "hideBackButton"
    - path: "components/Grade/GestionareNomenclatoare.tsx"
      provides: "Prop optional hideBackButton"
      contains: "hideBackButton"
  key_links:
    - from: "components/Plati/hub/TabConfigurare.tsx"
      to: "components/Grade/GestionareNomenclatoare.tsx"
      via: "plati={plati} (nefiltrat, din useData)"
      pattern: "plati=\\{plati\\}"
    - from: "components/Plati/hub/TabConfigurare.tsx"
      to: "components/AccessDenied.tsx"
      via: "guard taxe-anuale isSuperAdmin || isAdminClub"
      pattern: "isSuperAdmin \\|\\| permissions\\.isAdminClub"
---

<objective>
Tab-ul "Configurare" al hub-ului (D-04: inlocuieste `tipuri-abonament`, `configurare-preturi`, `reduceri`, `taxe-anuale`, `nomenclatoare` — ultimul = nomenclatorul tipurilor de plata `tipuri_plati`), cu props identice hartii verificate in 31-01 si cu guard-ul special al vederii Taxe Anuale pastrat in interiorul tab-ului.

Plan independent de fluxurile de facturare/incasare — poate rula in paralel cu 31-04, 31-05, 31-07.

Output: `components/Plati/hub/TabConfigurare.tsx`; `hideBackButton` optional pe cele 5 componente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-CONTEXT.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-01-SUMMARY.md
@CLAUDE.md
@components/Plati/hub/platiHubConfig.ts
</context>

<interfaces>
Contract consumat (31-01):
```ts
export interface PlatiHubTabProps {
  sectiune: SectiuneHub; currentUser: User; permissions: Permissions; activeRoleContext: any;
  onViewSportiv: (sportiv: Sportiv, tab?: TabProfilSportiv) => void; onBack: () => void;
}
```

Contract creat (consumat de PlatiHub in 31-08):
```ts
// components/Plati/hub/TabConfigurare.tsx
export const TabConfigurare: React.FC<PlatiHubTabProps>;
```

Props exacte (harta 31-01, AppRouter.tsx):
- TipuriAbonamentManagement (254-255): onBack, tipuriAbonament=filteredData.tipuriAbonament, setTipuriAbonament, currentUser, clubs, activeRoleContext, permissions
- ConfigurarePreturi (256-257): grade (nefiltrat), onBack
- ReduceriManagement (262-263): onBack, reduceri (NEFILTRAT), setReduceri
- GestionareNomenclatoare (264-265): onBack, tipuriPlati (nefiltrat), setTipuriPlati, plati (NEFILTRAT — `plati`, nu `filteredData.plati`)
- TaxeAnuale (270-271): onBack, currentUser, sportivi=filteredData.sportivi, plati=filteredData.plati, setPlati — guard `permissions.isSuperAdmin || permissions.isAdminClub`

`components/AccessDenied.tsx`: export default `AccessDenied`, props `{ onBack: () => void }`.

Ancore butoane de intoarcere (inainte de editare): TipuriAbonament.tsx:213-215 (Button cu className "mb-6", continut pe 2 linii), ConfigurarePreturi.tsx:176, Reduceri.tsx:95, TaxeAnuale.tsx:893-895 (Button size sm in interiorul unui div de header — ascunde DOAR butonul, nu h1/p de langa), GestionareNomenclatoare.tsx:66. Interfete props: TipuriAbonament.tsx:11-19 (semnatura :21), ConfigurarePreturi.tsx:21-24 (:26), Reduceri.tsx:9-13 (:23), TaxeAnuale.tsx:12-18 (:669), GestionareNomenclatoare.tsx:9-14 (:16).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: hideBackButton optional pe cele 5 componente de configurare</name>
  <read_first>
    - components/Plati/TipuriAbonament.tsx (liniile 1-25 si 205-220)
    - components/Plati/ConfigurarePreturi.tsx (liniile 18-30 si 170-180)
    - components/Plati/Reduceri.tsx (liniile 1-30 si 90-100)
    - components/Plati/TaxeAnuale.tsx (liniile 10-20, 665-675, 885-900)
    - components/Grade/GestionareNomenclatoare.tsx (liniile 1-20 si 60-70)
  </read_first>
  <files>components/Plati/TipuriAbonament.tsx, components/Plati/ConfigurarePreturi.tsx, components/Plati/Reduceri.tsx, components/Plati/TaxeAnuale.tsx, components/Grade/GestionareNomenclatoare.tsx</files>
  <action>
In fiecare din cele 5 componente, acelasi tipar minimal:
1. Adauga `hideBackButton?: boolean;` in interfata de props (`TipuriAbonamentManagementProps`, `ConfigurarePreturiProps`, `ReduceriManagementProps`, `TaxeAnualeProps`, `GestionareNomenclatoareProps`) cu comentariul "Faza 31 — ascuns cand e montat in hub-ul Plăți & Facturi".
2. Adauga `hideBackButton` in destructurarea semnaturii componentei.
3. Invelste EXCLUSIV elementul `<Button onClick={onBack} ...>...</Button>` de intoarcere in `{!hideBackButton && (...)}`. La TaxeAnuale.tsx:893 butonul e in acelasi `<div>` cu `<h1>TAXE ANUALE & VIZE</h1>` si paragraful descriptiv — acestea raman afisate. La TipuriAbonament.tsx:213 butonul are `className="mb-6"` si continut pe mai multe linii — invelste tot elementul.

Nicio alta modificare (logica de configurare, interogari, stari raman neatinse).
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && for f in components/Plati/TipuriAbonament.tsx components/Plati/ConfigurarePreturi.tsx components/Plati/Reduceri.tsx components/Plati/TaxeAnuale.tsx components/Grade/GestionareNomenclatoare.tsx; do echo \"$f $(grep -c hideBackButton $f)\"; done"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - Fiecare din cele 5 fisiere are `grep -c hideBackButton` >= 3 (interfata, destructurare, conditie).
    - `grep -c "TAXE ANUALE & VIZE" components/Plati/TaxeAnuale.tsx` = 1 si h1-ul nu e in interiorul conditiei `!hideBackButton` (verificare prin citirea diff-ului).
    - `git diff --stat` pe cele 5 fisiere arata modificari mici (sub 15 linii per fisier).
  </acceptance_criteria>
  <done>Cele 5 componente de configurare pot fi montate in hub fara buton dublu de intoarcere; standalone identice.</done>
</task>

<task type="auto">
  <name>Task 2: Componenta TabConfigurare cu guard-ul Taxe Anuale</name>
  <read_first>
    - components/Plati/hub/platiHubConfig.ts (antetul "Harta props verificata")
    - components/AppRouter.tsx (liniile 254-265 si 270-271)
    - components/AccessDenied.tsx
    - components/LazyComponents.tsx (TipuriAbonamentManagement, ConfigurarePreturi, ReduceriManagement, TaxeAnuale, GestionareNomenclatoare)
    - contexts/DataContext.tsx
  </read_first>
  <files>components/Plati/hub/TabConfigurare.tsx</files>
  <action>
Creeaza `components/Plati/hub/TabConfigurare.tsx` (per D-04):
- Importuri: `React`; `useData` din `'../../../contexts/DataContext'`; `import * as Lazy from '../../LazyComponents';`; `import AccessDenied from '../../AccessDenied';`; `import type { PlatiHubTabProps } from './platiHubConfig';`.
- `export const TabConfigurare: React.FC<PlatiHubTabProps> = ({ sectiune, currentUser, permissions, activeRoleContext, onBack }) => { ... }`.
- `const { filteredData, setTipuriAbonament, clubs, grade, reduceri, setReduceri, tipuriPlati, setTipuriPlati, plati, setPlati } = useData();` — `grade`, `reduceri`, `tipuriPlati`, `plati` NEFILTRATE, exact ca AppRouter.tsx:257, 263, 265.
- `const poateVedeaTaxeAnuale = permissions.isSuperAdmin || permissions.isAdminClub;` (guard-ul vechi al vederii taxe-anuale, AppRouter.tsx:271).
- Ramuri:
  - `'tipuri-abonament'` → `<Lazy.TipuriAbonamentManagement onBack={onBack} tipuriAbonament={filteredData.tipuriAbonament} setTipuriAbonament={setTipuriAbonament} currentUser={currentUser} clubs={clubs} activeRoleContext={activeRoleContext} permissions={permissions} hideBackButton />`
  - `'configurare-preturi'` → `<Lazy.ConfigurarePreturi grade={grade} onBack={onBack} hideBackButton />`
  - `'reduceri'` → `<Lazy.ReduceriManagement onBack={onBack} reduceri={reduceri} setReduceri={setReduceri} hideBackButton />`
  - `'taxe-anuale'` → `poateVedeaTaxeAnuale ? <Lazy.TaxeAnuale onBack={onBack} currentUser={currentUser} sportivi={filteredData.sportivi} plati={filteredData.plati} setPlati={setPlati} hideBackButton /> : <AccessDenied onBack={onBack} />`
  - `'nomenclatoare'` → `<Lazy.GestionareNomenclatoare onBack={onBack} tipuriPlati={tipuriPlati} setTipuriPlati={setTipuriPlati} plati={plati} hideBackButton />`
- Comentariu-antet: tab Configurare (D-04); plati si reduceri NEFILTRATE intentionat (paritate cu AppRouter); guard Taxe Anuale pastrat (rezolvaPozitieHub face deja fallback, AccessDenied e a doua linie de aparare).
- Niciun Suspense propriu, nicio interogare Supabase.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && grep -c 'plati={plati}' components/Plati/hub/TabConfigurare.tsx && grep -c 'permissions.isSuperAdmin || permissions.isAdminClub' components/Plati/hub/TabConfigurare.tsx && grep -c '<AccessDenied' components/Plati/hub/TabConfigurare.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - `TabConfigurare.tsx` exporta `TabConfigurare: React.FC<PlatiHubTabProps>` si contine cele 5 ramuri `sectiune === '...'` pentru tipuri-abonament, configurare-preturi, reduceri, taxe-anuale, nomenclatoare.
    - `grep -c "plati={plati}" components/Plati/hub/TabConfigurare.tsx` = 1 (GestionareNomenclatoare nefiltrat) si `grep -c "reduceri={reduceri}"` = 1.
    - Contine `permissions.isSuperAdmin || permissions.isAdminClub` si `<AccessDenied onBack={onBack} />`.
    - Toate cele 5 montari au `hideBackButton`.
    - `grep -c "supabase" components/Plati/hub/TabConfigurare.tsx` = 0.
  </acceptance_criteria>
  <done>Tab-ul Configurare exista, compileaza, monteaza cele 5 ecrane cu props identice si pastreaza guard-ul Taxe Anuale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hub (guard canManageFinances, include rolul ADMIN) → TaxeAnuale (inainte interzis rolului ADMIN non-super) | Consolidarea poate largi accidental accesul |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06-01 | Elevation of Privilege | Sectiunea Taxe Anuale in hub | mitigate | Guard explicit `permissions.isSuperAdmin || permissions.isAdminClub` → AccessDenied; in plus rezolvaPozitieHub (31-01) redirectioneaza pe tipuri-abonament si PlatiHub (31-08) ascunde pastila |
| T-31-06-02 | Information Disclosure | GestionareNomenclatoare primeste `plati` nefiltrat | accept | Paritate exacta cu AppRouter.tsx:265; datele sunt deja limitate de RLS la nivel DB; folosit doar pentru verificarea "tip in uz" |
| T-31-06-SC | Tampering | npm installs | accept | Zero pachete noi |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Inventarul complet al fazei e in `31-01-PLAN.md` (sectiunea omonima). Acest plan (31-06) produce:
- `components/Plati/hub/TabConfigurare.tsx` → `TabConfigurare`.
- Props optionale noi: `hideBackButton?: boolean` pe `TipuriAbonamentManagement`, `ConfigurarePreturi`, `ReduceriManagement`, `TaxeAnuale`, `GestionareNomenclatoare`.
</artifacts_this_phase_produces>

<verification>
1. `npm run lint` — cod 0.
2. Gardele grep din acceptance (nefiltrate, guard taxe, hideBackButton).
</verification>

<success_criteria>
- D-04: tab-ul Configurare grupeaza cele 5 ecrane cu props identice.
- Guard-ul Taxe Anuale e pastrat in interiorul hub-ului.
</success_criteria>

<output>
Creeaza `.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-06-SUMMARY.md`.
</output>
