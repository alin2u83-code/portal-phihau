---
phase: 27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - components/Sezoane/SezonFormModal.tsx
  - components/Sezoane/index.tsx
  - components/LazyComponents.tsx
  - components/AppRouter.tsx
  - components/menuConfig.ts
autonomous: true
requirements: [SEZ-01, SEZ-02, SEZ-03, SEZ-06]

must_haves:
  truths:
    - "ADMIN_CLUB deschide un ecran dedicat 'Sezoane' din meniu si vede sezoanele propriului club, ordonate descrescator dupa data_start (D-01, D-02)"
    - "ADMIN_CLUB creeaza un sezon alegand manual data de start si data de final; datele inversate sunt respinse inainte de salvare (D-01)"
    - "Activarea unui sezon dezactiveaza explicit sezonul anterior — clubul are un singur sezon activ (D-03)"
    - "La activarea unui sezon nou, grupele per-sezon legate de sezonul anterior devin arhivate automat, fara stergere (D-06)"
    - "INSTRUCTOR nu vede intrarea de meniu 'Sezoane' si nu poate deschide ecranul (D-02)"
  artifacts:
    - path: "components/Sezoane/SezonFormModal.tsx"
      provides: "Modal creare/editare sezon cu validare de interval"
      exports: ["SezonFormModal"]
      min_lines: 60
    - path: "components/Sezoane/index.tsx"
      provides: "Ecran CRUD sezoane + flux activare + arhivare grupe per-sezon"
      exports: ["SezoaneView"]
      min_lines: 150
    - path: "components/AppRouter.tsx"
      provides: "ruta view 'sezoane' protejata la nivel ADMIN_CLUB+"
      contains: "case 'sezoane'"
  key_links:
    - from: "components/Sezoane/index.tsx"
      to: "public.sezoane"
      via: "useSezoane + supabase insert/update/delete"
      pattern: "from\\('sezoane'\\)"
    - from: "components/Sezoane/index.tsx"
      to: "public.grupe"
      via: "UPDATE arhivat=true pe grupele per-sezon ale sezonului anterior"
      pattern: "arhivat"
    - from: "components/AppRouter.tsx"
      to: "components/Sezoane/index.tsx"
      via: "Lazy.SezoaneView in case 'sezoane'"
      pattern: "SezoaneView"
---

<objective>
Ecranul nou "Sezoane" (ADMIN_CLUB + SUPER_ADMIN_FEDERATIE): listare, creare, editare, stergere si activare de sezoane per club, unde activarea unui sezon dezactiveaza sezonul anterior si arhiveaza automat grupele per-sezon legate de el.

Purpose: sezonul activ este comutatorul de care depind grupele per-sezon (27-03) si catalogul de tipuri de abonament (27-04, 27-05); fara acest ecran nu exista nicio cale prin care un admin sa creeze sau sa comute un sezon.
Output: `components/Sezoane/SezonFormModal.tsx`, `components/Sezoane/index.tsx`, plus cablarea in `LazyComponents.tsx`, `AppRouter.tsx` si `menuConfig.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-CONTEXT.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-UI-SPEC.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-PATTERNS.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SezonFormModal — creare/editare sezon cu validare de interval</name>
  <files>components/Sezoane/SezonFormModal.tsx</files>
  <read_first>
    - components/Plati/PerioadaVacanta.tsx liniile 1-100 (`CrudModal` — analogul exact: state local pe denumire/date, `dateError`, `Modal` + `Input` + butoane Anuleaza/Salveaza)
    - components/ui.tsx liniile 584-660 (`Input` cu prop `error`, `Select`, `Switch`) si 364-410 (`Modal`)
    - types.ts (`interface Sezon` adaugata in 27-01)
    - .planning/phases/27-.../27-UI-SPEC.md sectiunea "1. New screen" + "Copywriting Contract"
  </read_first>
  <action>
Creeaza `components/Sezoane/SezonFormModal.tsx` exportand `SezonFormModal`, copiind structura `CrudModal` din `components/Plati/PerioadaVacanta.tsx` (liniile 28-95) cu urmatoarele adaptari concrete.

Props: `{ mode: 'add' | 'edit'; item: Sezon | null; isFirstSezon: boolean; isSaving: boolean; onClose: () => void; onSave: (values: { denumire: string; data_start: string; data_final: string; activ: boolean }) => Promise<void> }`.

State local: `denumire` (init `item?.denumire ?? ''`), `dataStart` (init `item?.data_start ?? ''`), `dataFinal` (init `item?.data_final ?? ''`), `dateError` (string), `activeazaAcum` (boolean; init: la `mode === 'edit'` = `item?.activ ?? false`, la `mode === 'add'` = valoarea `isFirstSezon` — bifat implicit doar pentru primul sezon al clubului, conform UI-SPEC, ca sa nu dezactivezi din greseala sezonul curent la fiecare adaugare).

Campurile, in ordine, in `<div className="space-y-4">`: `Input label="Denumire *"` cu `placeholder="ex: Sezon 2026-2027"`; `Input label="Data Start *" type="date"`; `Input label="Data Final *" type="date"` cu `error={dateError || undefined}`; apoi `Switch` din `../ui` cu `label="Activează acest sezon acum"`, `name="activ"`, `checked={activeazaAcum}`.

`handleSave`: refuza salvarea daca `denumire.trim()` e gol sau vreo data lipseste; daca `dataFinal < dataStart` seteaza `dateError` la sirul exact `Data de sfârșit trebuie să fie după data de start.` si iese fara sa apeleze `onSave`; altfel goleste eroarea si apeleaza `onSave({ denumire: denumire.trim(), data_start: dataStart, data_final: dataFinal, activ: activeazaAcum })`.

Titlul modalului: `mode === 'add' ? 'Adaugă Sezon' : 'Editează Sezon'`. Butoane: `Button variant="secondary"` cu textul `Anulează` (disabled cand `isSaving`) si `Button variant="primary"` cu textul `Salvează`, `isLoading={isSaving}`, dezactivat cat timp denumirea sau vreo data lipseste. Sub `Switch`, text ajutator cu clasa `text-xs text-[var(--t-text-muted)]`: `Sezonul activ determină ce grupe per-sezon și ce tipuri de abonament sunt considerate curente.`

Fara librarii noi, fara componente din afara `components/ui.tsx` (CLAUDE.md interzice Shadcn/MUI).
  </action>
  <verify>
    <automated>npm run lint && grep -c "export const SezonFormModal" components/Sezoane/SezonFormModal.tsx && grep -c "Data de sfârșit trebuie să fie după data de start." components/Sezoane/SezonFormModal.tsx && grep -c "Activează acest sezon acum" components/Sezoane/SezonFormModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece.
    - Fisierul exporta `SezonFormModal` si importa doar din `../ui`, `../../types` si React.
    - Contine sirul de eroare exact `Data de sfârșit trebuie să fie după data de start.` si il seteaza doar pe ramura `dataFinal < dataStart`.
    - Contine `Switch` cu label-ul `Activează acest sezon acum`, initializat din `isFirstSezon` la `mode === 'add'`.
    - Titlurile modalului sunt exact `Adaugă Sezon` / `Editează Sezon`.
  </acceptance_criteria>
  <done>Modalul valideaza intervalul inainte de orice request si returneaza catre parinte inclusiv intentia de activare.</done>
</task>

<task type="auto">
  <name>Task 2: Ecran Sezoane — listare, activare cu dezactivarea sezonului anterior si arhivare grupe per-sezon</name>
  <files>components/Sezoane/index.tsx</files>
  <read_first>
    - components/Plati/PerioadaVacanta.tsx liniile 370-540 si 600-700 (`PerioadaVacantaView`: gate `isAdmin`, fetch per club, `handleSave`, lista de `Card`-uri cu butoane ghost, `ConfirmDeleteModal`) si linia 15 (`formatDataRo`)
    - components/Sezoane/SezonFormModal.tsx (creat la Task 1)
    - hooks/useSezoane.ts (creat in 27-01 — `useSezoane`, `useSezonActiv`)
    - components/ui.tsx (`Card`, `Button`, `Badge`, `EmptyState`, `ConfirmModal`) si components/ConfirmDeleteModal.tsx (props `tableName`, `customMessage`)
    - components/Grupe/index.tsx liniile 64-76 (pattern golire `cache_grupe_*` din localStorage + `queryClient.invalidateQueries({ queryKey: ['grupe'] })` — obligatoriu dupa arhivare, altfel lista de grupe ramane stale pana la 10 minute)
    - .planning/phases/27-.../27-UI-SPEC.md sectiunile "1. New screen" si "Copywriting Contract"
  </read_first>
  <action>
Creeaza `components/Sezoane/index.tsx` exportand `SezoaneView` cu props `{ onBack: () => void }`.

Setup: `const { activeRoleContext } = useData();`, `const permissions = usePermissions(activeRoleContext);`, `const isAdmin = permissions.isAdminClub || permissions.isFederationAdmin;`, `const clubId = activeRoleContext?.club_id ?? null;`, `const { sezoane, sezonActiv, isLoading } = useSezonActiv(clubId);`, `const queryClient = useQueryClient();`, `const { showError, showSuccess } = useError();`. Include local functia `formatDataRo` copiata identic din `PerioadaVacanta.tsx` (linia 15).

Header (pattern `PerioadaVacantaView`): `Button variant="secondary"` cu `ArrowLeftIcon` + textul `Înapoi`, titlu `h1` cu clasele `text-xl sm:text-2xl font-semibold text-white` si textul `Sezoane`, iar in dreapta `Button variant="info"` cu `PlusIcon` si textul exact `Adaugă Sezon` (randat doar cand `isAdmin`).

Lista: pentru fiecare sezon din `sezoane` (deja ordonate `data_start` descrescator de hook) randeaza un `Card` cu: `denumire` bold; sub el `formatDataRo(s.data_start) + ' — ' + formatDataRo(s.data_final)` cu `text-sm text-[var(--t-text-muted)]`; `Badge variant="green"` cu textul `Activ` cand `s.activ === true`, altfel `Badge variant="slate"` cu textul `Arhivat`; in dreapta, doar cand `isAdmin`: `Button variant="secondary" size="sm"` cu textul `Activează` (randat doar cand `!s.activ`), `Button variant="secondary" size="xs" ghost` cu `EditIcon` si `Button variant="danger" size="xs" ghost` cu `TrashIcon`.

Empty state (cand `!isLoading && sezoane.length === 0`): componenta `EmptyState` cu `icon={<CalendarDaysIcon className="w-10 h-10 text-[var(--t-text-muted)]" />}`, `title="Niciun sezon definit"`, `description` = sirul exact din UI-SPEC (`Adaugă primul sezon al clubului cu un interval de date liber (de exemplu 1 Sep 2026 — 30 Iun 2027). Sezonul activ determină ce grupe per-sezon și ce tipuri de abonament sunt vizibile ca fiind curente.`), `actionLabel="Adaugă primul sezon"`, `onAction` = deschide modalul in mod `add`.

`handleSave(values)` (apelat de `SezonFormModal`, `mode='add'`): daca `values.activ === true`, executa INTAI `supabase.from('sezoane').update({ activ: false }).eq('club_id', clubId).eq('activ', true)` (dezactivarea explicita ceruta de RESEARCH Pattern 2 — indexul unic e plasa de siguranta, nu mecanismul principal), memorand in prealabil `sezonAnteriorId = sezonActiv?.id ?? null`; apoi `insert({ ...values, club_id: clubId })`. La `mode='edit'` face `update(values).eq('id', item.id).eq('club_id', clubId)` (fara logica de activare — activarea are flux propriu). Dupa succes: `queryClient.invalidateQueries({ queryKey: ['sezoane'] })`, inchide modalul; daca noul sezon a fost activat si exista `sezonAnteriorId`, ruleaza acelasi pas de arhivare descris mai jos.

`handleActivare(sezon)` — deschis printr-un `ConfirmModal variant="warning"` cu `title` exact `Activează sezon nou`, `confirmLabel` exact `Activează Sezonul` si mesajul din UI-SPEC construit dinamic: `Sezonul '{denumire sezon activ curent}' va fi dezactivat și '{denumire sezon nou}' va deveni sezonul activ al clubului. Grupele per sezon din sezonul vechi vor fi arhivate automat. Continuați?`. La confirmare, in ordine:
  1. `const sezonAnteriorId = sezonActiv?.id ?? null;`
  2. `supabase.from('sezoane').update({ activ: false }).eq('club_id', clubId).eq('activ', true)` — opreste pe eroare.
  3. `supabase.from('sezoane').update({ activ: true }).eq('id', sezon.id).eq('club_id', clubId)` — opreste pe eroare.
  4. ARHIVARE D-06 (doar daca `sezonAnteriorId` nu e null): `supabase.from('grupe').update({ arhivat: true }).eq('club_id', clubId).eq('tip_grupa', 'per_sezon').eq('sezon_id', sezonAnteriorId).eq('arhivat', false).select('id')` — numarul de randuri returnate este numarul de grupe arhivate. Grupele cu `tip_grupa='permanent'` NU sunt atinse (D-05) si nici cele per-sezon cu `sezon_id` null (nu apartineau sezonului anterior).
  5. `queryClient.invalidateQueries({ queryKey: ['sezoane'] })`, `queryClient.invalidateQueries({ queryKey: ['grupe'] })` si golirea cheilor `cache_grupe_*` din `localStorage` cu `clearCache` (pattern `components/Grupe/index.tsx` liniile 67-71) — fara asta lista de grupe afiseaza date vechi pana la 10 minute.
  6. `showSuccess('Sezon activat', ...)` cu numarul de grupe arhivate.
Tratarea erorii de concurenta: daca eroarea Supabase are `code === '23505'` (violare `idx_sezoane_activ_per_club`), afiseaza prin `showError` sirul exact `Activarea a eșuat — este posibil ca alt admin să fi activat deja un sezon în același timp. Reîmprospătați lista și încercați din nou.` si re-invalideaza `['sezoane']`.

Stergere: `ConfirmDeleteModal` cu `tableName="sezoane"` si `customMessage` = `Sezonul '{denumire}' va fi șters. Grupele și tipurile de abonament legate de acest sezon NU se șterg — rămân ca istoric fără sezon activ.`; la confirmare `supabase.from('sezoane').delete().eq('id', id).eq('club_id', clubId)`, apoi invalidare `['sezoane']` + `['grupe']`.

Gate de acces: cand `!isAdmin`, randeaza doar butonul de `Înapoi` si un `EmptyState` cu titlul `Acces restricționat` si descrierea `Doar administratorii de club pot gestiona sezoanele.` — niciun buton de scriere nu se randeaza (aparare UX; aparearea reala este politica RLS `sezoane_write` din 27-01).

Toate erorile trec prin `useError().showError(titlu, error)` — fara `alert`, fara `console.error` ca singura semnalizare.
  </action>
  <verify>
    <automated>npm run lint && grep -c "export const SezoaneView" components/Sezoane/index.tsx && grep -c "tip_grupa" components/Sezoane/index.tsx && grep -c "23505" components/Sezoane/index.tsx && grep -c "Activează sezon nou" components/Sezoane/index.tsx && grep -c "cache_grupe_" components/Sezoane/index.tsx</automated>
    <human-check>Cu un club care are un sezon activ si cel putin o grupa per-sezon: apasa "Activează" pe alt sezon, confirma, apoi verifica in ecranul Grupe ca grupa per-sezon veche apare arhivata iar grupele permanente sunt neschimbate.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece.
    - Fluxul de activare contine, in aceasta ordine, dezactivarea sezonului anterior, activarea celui nou si UPDATE-ul de arhivare filtrat pe `club_id` + `tip_grupa='per_sezon'` + `sezon_id=<sezon anterior>` + `arhivat=false`.
    - Nicio interogare din fisier nu scrie in `grupe` fara filtrul `tip_grupa = 'per_sezon'` (grupele permanente raman neatinse — D-05).
    - Codul trateaza explicit `error.code === '23505'` cu mesajul de race condition din UI-SPEC.
    - Dupa arhivare se apeleaza atat `invalidateQueries(['grupe'])` cat si golirea cheilor `cache_grupe_*` din localStorage.
    - Textele `Adaugă Sezon`, `Activează`, `Activează sezon nou`, `Activează Sezonul`, `Niciun sezon definit`, `Adaugă primul sezon` apar identic cu Copywriting Contract din UI-SPEC.
  </acceptance_criteria>
  <done>Un admin poate crea, edita, sterge si activa sezoane; activarea muta clubul pe noul sezon si arhiveaza exact grupele per-sezon ale sezonului anterior.</done>
</task>

<task type="auto">
  <name>Task 3: Cablare navigare — LazyComponents, AppRouter, menuConfig</name>
  <files>components/LazyComponents.tsx, components/AppRouter.tsx, components/menuConfig.ts</files>
  <read_first>
    - components/LazyComponents.tsx liniile 15-35 (pattern `lazy(() => import(...).then(m => ({ default: m.X })))`, inclusiv `PerioadaVacantaView`)
    - components/AppRouter.tsx liniile 245-275 (`case 'perioade-vacanta'` cu `renderProtected(..., isAtLeastClubAdmin)`)
    - components/menuConfig.ts liniile 37-47 (`adminMenu` -> "Activitate Sală") si 117-128 (`adminClubMenu` -> "Activitate Sală")
    - types.ts (`type View` — valoarea `'sezoane'` a fost adaugata in 27-01)
  </read_first>
  <action>
1. `components/LazyComponents.tsx` — adauga `export const SezoaneView = lazy(() => import('./Sezoane').then(m => ({ default: m.SezoaneView })));` langa export-ul `PerioadaVacantaView`.
2. `components/AppRouter.tsx` — adauga, imediat dupa `case 'perioade-vacanta'`, un `case 'sezoane':` care returneaza `renderProtected(<Lazy.SezoaneView onBack={handleBackToDashboard} />, isAtLeastClubAdmin)`. Gate-ul `isAtLeastClubAdmin` (nu `isAtLeastInstructor`) este cerinta D-02.
3. `components/menuConfig.ts` — adauga `{ label: 'Sezoane', view: 'sezoane' }` ca PRIMA intrare in submeniul "Activitate Sală" atat in `adminMenu` (inainte de `{ label: 'Grupe & Orar', view: 'grupe' }`, linia ~40) cat si in `adminClubMenu` (linia ~120). NU adauga intrarea in niciun meniu de INSTRUCTOR sau SPORTIV (D-02).
  </action>
  <verify>
    <automated>npm run lint && grep -c "SezoaneView" components/LazyComponents.tsx && grep -c "case 'sezoane'" components/AppRouter.tsx && test "$(grep -c "view: 'sezoane'" components/menuConfig.ts)" = "2"</automated>
    <human-check>Logat ca INSTRUCTOR, meniul lateral nu contine intrarea "Sezoane"; logat ca ADMIN_CLUB, intrarea deschide ecranul.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece.
    - `case 'sezoane'` foloseste `isAtLeastClubAdmin` ca gate de protectie.
    - `view: 'sezoane'` apare de exact 2 ori in `menuConfig.ts` (adminMenu + adminClubMenu), niciodata intr-un meniu de instructor/sportiv.
  </acceptance_criteria>
  <done>Ecranul Sezoane este accesibil din meniu pentru ADMIN_CLUB si SUPER_ADMIN_FEDERATIE si inaccesibil pentru celelalte roluri.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI React -> Supabase PostgREST | Toate mutatiile pe `sezoane`/`grupe` pleaca din browser cu cheia anon; gate-ul real e RLS |
| gate de rol in UI (`usePermissions`) -> RLS | Ascunderea butoanelor e UX, nu securitate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Elevation of Privilege | Ecran Sezoane accesat de INSTRUCTOR | mitigate | `renderProtected(..., isAtLeastClubAdmin)` in AppRouter + gate `isAdmin` in componenta + politica `sezoane_write` (27-01) ca aparare finala |
| T-27-07 | Tampering | Mutatii pe `sezoane`/`grupe` care ignora clubul activ | mitigate | Fiecare `update`/`delete`/`insert` din `components/Sezoane/index.tsx` include `.eq('club_id', clubId)` derivat din `activeRoleContext`, nu din props sau input de utilizator |
| T-27-08 | Tampering (stare inconsistenta) | Doi admini activeaza simultan doua sezoane | mitigate | Dezactivare explicita inainte de activare + tratarea codului `23505` cu mesaj de reincercare, fara stare partiala silentioasa |
| T-27-09 | Repudiation / pierdere de date | Arhivarea sterge grupe in loc sa le marcheze | mitigate | Exclusiv `update({ arhivat: true })`; niciun `delete` pe `grupe` in acest plan (D-06) |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | Zero pachete noi instalate in acest plan |
</threat_model>

<verification>
- `npm run lint` -> zero erori.
- `grep -c "case 'sezoane'" components/AppRouter.tsx` >= 1 si `grep -c "view: 'sezoane'" components/menuConfig.ts` = 2.
- Verificare live dupa un ciclu de activare in aplicatie: `select id, denumire, activ from public.sezoane where club_id = '<club de test>'` returneaza exact un rand cu `activ = true`; `select count(*) from public.grupe where club_id='<club>' and tip_grupa='per_sezon' and sezon_id='<sezon anterior>' and arhivat = false` returneaza 0.
</verification>

<success_criteria>
- Un ADMIN_CLUB poate crea un sezon cu interval liber, il poate edita, sterge si activa dintr-un ecran dedicat.
- Dupa activare, clubul are exact un sezon activ, iar grupele per-sezon ale sezonului anterior sunt marcate `arhivat = true` fara nicio stergere.
- Grupele permanente si sportivii lor raman neatinsi.
</success_criteria>

## Artifacts this phase produces

Simboluri noi create in acest plan:

- Fisiere noi: `components/Sezoane/index.tsx`, `components/Sezoane/SezonFormModal.tsx`
- Simboluri TS noi: `SezoaneView`, `SezonFormModal`, export-ul lazy `SezoaneView` din `components/LazyComponents.tsx`
- Valori noi de configurare: intrarea de meniu `{ label: 'Sezoane', view: 'sezoane' }` in `adminMenu` si `adminClubMenu`, ramura `case 'sezoane'` in `components/AppRouter.tsx`

<output>
Create `.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-02-SUMMARY.md` when done
</output>
