---
phase: 17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql
autonomous: true
requirements: [DEDUP-VERIFY-01]

must_haves:
  truths:
    - "Rularea pg_get_functiondef pe merge_sportivi() live returneaza o definitie ce contine DELETE FROM public.sportivi si guard has_access_to_club"
    - "Rularea pg_get_functiondef pe find_similar_sportivi() live returneaza o definitie ce NU exclude status='Inactiv' dar exclude tombstone-uri (merge_in)"
    - "Comentariul din antetul fisierului SQL nu mai afirma ca migratia este neaplicata"
  artifacts:
    - path: "sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql"
      provides: "Antet SQL corectat: statut APLICAT LIVE cu data verificarii"
      contains: "APLICAT LIVE"
  key_links:
    - from: "sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql (antet)"
      to: "starea reala a functiilor merge_sportivi/find_similar_sportivi pe proiectul wuhidifzsutwgdfkwhmd"
      via: "verificare pg_get_functiondef reproductibila"
      pattern: "APLICAT LIVE"
---

<objective>
Confirma reproductibil ca migratia de deduplicare sportivi ESTE aplicata pe DB live si corecteaza comentariul stale din antetul fisierului SQL care afirma inca ca migratia "NU a fost aplicata live" (dezinformare pentru viitor). Per D-01 (CONTEXT.md: migratie deduplicare — verificat deja): nu se re-ruleaza migratia, doar se verifica live + se scrie back statutul corect.

Purpose: Elimina o afirmatie falsa din codebase care ar putea determina pe cineva sa re-aplice o migratie ce contine DELETE ireversibil in merge_sportivi(), sau sa presupuna ca UI-ul deja actualizat ruleaza pe schema veche.
Output: Fisier SQL cu antet corectat (statut APLICAT LIVE + data), verificat printr-un query reproductibil.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci/17-CONTEXT.md
@.planning/phases/17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci/17-RESEARCH.md
@sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verifica reproductibil starea live a functiilor de deduplicare</name>
  <read_first>
    - sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql (comportamentul asteptat post-migratie)
    - .planning/phases/17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci/17-RESEARCH.md (Pattern 3: aserturile de verificare)
  </read_first>
  <action>
    Ruleaza pe proiectul Supabase live `wuhidifzsutwgdfkwhmd` (via tool-ul Supabase MCP execute_sql, sau SQL Editor daca MCP nu e conectat la acest proiect) doua interogari de introspectie:
    (a) `SELECT pg_get_functiondef(oid) AS def FROM pg_proc WHERE proname = 'merge_sportivi';`
    (b) `SELECT pg_get_functiondef(oid) AS def FROM pg_proc WHERE proname = 'find_similar_sportivi';`
    Aplica urmatoarele aserturi pe textul returnat (nu "eyeball", verifica literal fiecare pattern):
    - merge_sportivi def CONTINE `DELETE FROM public.sportivi`
    - merge_sportivi def CONTINE `has_access_to_club`
    - find_similar_sportivi def NU contine `status <> 'Inactiv'` si NU contine `status = 'Activ'`
    - find_similar_sportivi def CONTINE `merge_in` (excluderea tombstone-urilor pastrata)
    Daca ORICARE assert esueaza (ex. DB a facut drift intre planning si executie), OPRESTE-TE si raporteaza discrepanta in SUMMARY — NU corecta antetul la "aplicat" si NU trece la Task 2. Daca toate trec, consemneaza textul aserturilor confirmate pentru citare in SUMMARY.
  </action>
  <verify>
    <automated>MISSING — verificarea ruleaza direct pe DB live via pg_get_functiondef; consemneaza in SUMMARY output-ul celor 4 aserturi (2 CONTINE, 2 NU-contine) ca dovada reproductibila</automated>
  </verify>
  <acceptance_criteria>
    - pg_get_functiondef('merge_sportivi') live returneaza text ce satisface: contine `DELETE FROM public.sportivi` AND contine `has_access_to_club`
    - pg_get_functiondef('find_similar_sportivi') live returneaza text ce satisface: NOT contine `status <> 'Inactiv'` AND NOT contine `status = 'Activ'` AND contine `merge_in`
    - Cele 4 aserturi si valorile lor (pass/fail) sunt consemnate textual in SUMMARY
  </acceptance_criteria>
  <done>Toate cele 4 aserturi trec pe DB live si sunt documentate; daca vreunul pica, faza se opreste cu raport de discrepanta.</done>
</task>

<task type="auto">
  <name>Task 2: Corecteaza antetul stale din fisierul SQL de migratie</name>
  <read_first>
    - sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql (liniile 36, 358-359, 364 contin afirmatii stale despre statutul de aplicare)
  </read_first>
  <action>
    Editeaza `sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql` inlocuind cele TREI afirmatii stale care spun ca migratia nu e aplicata cu statutul verificat:
    - Linia ~36: `-- ATENTIE: Aceasta migratie NU a fost aplicata live. Trebuie rulata manual in` -> devine un bloc de tip `-- STATUT: APLICAT LIVE — verificat 2026-07-08 pe proiectul wuhidifzsutwgdfkwhmd (Phase 17)` cu o nota scurta ca re-rularea NU mai e necesara (contine DELETE ireversibil).
    - Liniile ~358-359: comentariul `-- APLICA LIVE ... NU a fost aplicata automat de acest task` -> reformuleaza ca nota istorica: migratia a fost aplicata manual si verificata (Phase 17), pastrand referinta la 260705-pgg-SUMMARY.md pentru context.
    - Linia ~364: `RAISE NOTICE 'fix_deduplicare_include_inactivi_merge_delete ready — apply manually, not yet applied live.'` -> schimba mesajul in `'fix_deduplicare_include_inactivi_merge_delete — already applied live and verified (Phase 17).'`
    Nu modifica corpul SQL al functiilor (CREATE OR REPLACE etc.) — doar comentariile/antetul si mesajul RAISE NOTICE. Mentine limba romana pentru comentariile de domeniu conform CLAUDE.md. Include stringul literal `APLICAT LIVE` in antet (folosit ca marker de verificare).
  </action>
  <verify>
    <automated>grep -c "APLICAT LIVE" sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql | grep -qv '^0$' && grep -v '^--' sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql | grep -c "NU a fost aplicata live" | grep -q '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Fisierul contine cel putin o aparitie a markerului `APLICAT LIVE` cu data 2026-07-08 si proiectul wuhidifzsutwgdfkwhmd
    - Fisierul nu mai contine nicio afirmatie activa "NU a fost aplicata live" / "not yet applied live" (comentarii sau RAISE NOTICE)
    - Corpul functiilor SQL este neschimbat (doar comentarii/mesaje modificate)
  </acceptance_criteria>
  <done>Antetul si mesajul RAISE NOTICE reflecta statutul APLICAT LIVE verificat; nicio afirmatie stale ramasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → DB live | Introspectie read-only pe definitiile functiilor (pg_get_functiondef) — fara scriere |
| fisier SQL versionat → dezvoltator viitor | Comentariul din antet informeaza decizii viitoare de re-aplicare |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | comentariu stale in fisierul SQL | mitigate | Task 2 corecteaza afirmatia falsa care ar putea induce re-aplicarea unei migratii cu DELETE ireversibil |
| T-17-02 | Repudiation | statut de aplicare neverificat | mitigate | Task 1 encodeaza verificarea ca aserturi reproductibile, nu ca afirmatie preluata la incredere |
</threat_model>

<verification>
- pg_get_functiondef pe ambele functii live satisface toate cele 4 aserturi
- `grep "APLICAT LIVE" sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql` returneaza cel putin o linie
- Nicio afirmatie "NU a fost aplicata live" activa (non-comentata invalidata) ramasa
</verification>

<success_criteria>
- Statutul live al functiilor de deduplicare este confirmat reproductibil (4 aserturi documentate)
- Antetul fisierului SQL reflecta corect statutul APLICAT LIVE cu data si proiectul
- Zero modificari ale logicii SQL a functiilor
</success_criteria>

<output>
Create `.planning/phases/17-verifica-aplicare-live-migratie-deduplicare-sportivi-si-deci/17-01-SUMMARY.md` when done
</output>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- Modified file: `sql/fixes/fix_deduplicare_include_inactivi_merge_delete.sql` — antet corectat, marker literal `APLICAT LIVE` + data 2026-07-08 + proiect wuhidifzsutwgdfkwhmd; RAISE NOTICE actualizat.
- No new code symbols/functions/CLI flags — comment-only writeback plus reproducible DB introspection verification (pg_get_functiondef on merge_sportivi/find_similar_sportivi).
</artifacts_produced>
