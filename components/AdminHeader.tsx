// This component has been removed as per the user's request to clean up the UI.
// The profile and logout functionalities are now handled by the main Sidebar component.
import React from 'react';
export const AdminHeader: React.FC = () => null;
