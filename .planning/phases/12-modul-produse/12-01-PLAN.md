---
phase: 12-modul-produse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/12-produse-schema.sql
  - types.ts
autonomous: true
requirements: [PRD-01, PRD-02, PRD-03, PRD-04, PRD-05, PRD-06]

must_haves:
  truths:
    - "Există fișierul sql/12-produse-schema.sql cu 6 tabele: produse_categorii, produse, produse_variante, produse_intrari, produse_intrari_detalii, produse_vanzari, produse_vanzari_detalii"
    - "Tabelul produse_categorii are 8 rânduri seed cu categoriile predefinite"
    - "RLS activat pe toate tabelele noi — produse filtrate pe club_id prin header active-role-context-id"
    - "types.ts conține interfețele: ProdusCategorieDB, ProdusDB, ProdusVariantaDB, ProdusIntrareDB, ProdusIntrareDetaliuDB, ProdusVanzareDB, ProdusVanzareDetaliuDB și tipurile compuse Produs, ProdusVarianta, ProdusIntrare, ProdusVanzare"
  artifacts:
    - path: "sql/12-produse-schema.sql"
      provides: "Schema completă + seed + RLS pentru modulul Produse"
    - path: "types.ts"
      provides: "Tipuri TypeScript pentru toate entitățile modulului Produse"
  key_links:
    - from: "produse.club_id"
      to: "clubs.id"
      via: "FK ON DELETE CASCADE"
      pattern: "club_id"
    - from: "produse_variante.produs_id"
      to: "produse.id"
      via: "FK ON DELETE CASCADE"
      pattern: "produs_id"
    - from: "produse_vanzari.plata_id"
      to: "plati.id"
      via: "FK ON DELETE SET NULL"
      pattern: "plata_id"
---

<objective>
Creează schema DB completă pentru Modulul Produse și tipurile TypeScript corespunzătoare. Fundația de date pe care toate celelalte planuri (12-02..12-05) se construiesc.

Purpose: ADMIN_CLUB să poată gestiona catalog echipamente cu variante, stoc, intrări marfă și vânzări integrate cu modulul Plăți existent.
Output: 2 fișiere (sql/12-produse-schema.sql + types.ts extins), zero UI, zero migrații manuale — migrația se aplică manual prin Supabase SQL editor.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-modul-produse/12-CONTEXT.md
</context>

<artifacts_this_phase_produces>

**În `sql/12-produse-schema.sql`:**
- CREATE TABLE `produse_categorii` — categorii globale/predefinite
- CREATE TABLE `produse` — produse per club
- CREATE TABLE `produse_variante` — variante (mărime/culoare) cu prețuri + stoc
- CREATE TABLE `produse_intrari` — header intrare marfă (furnizor, nr. factură, dată)
- CREATE TABLE `produse_intrari_detalii` — linii intrare marfă
- CREATE TABLE `produse_vanzari` — header vânzare (sportiv, dată, plata_id)
- CREATE TABLE `produse_vanzari_detalii` — linii vânzare (snapshot preț la momentul vânzării)
- RLS policies pe fiecare tabel
- Seed INSERT pentru 8 categorii predefinite

**În `types.ts`:**
- `ProdusCategorieDB` — interfata pentru produse_categorii
- `ProdusDB` — interfata pentru produse  
- `ProdusVariantaDB` — interfata pentru produse_variante
- `ProdusIntrareDB` — interfata pentru produse_intrari
- `ProdusIntrareDetaliuDB` — interfata pentru produse_intrari_detalii
- `ProdusVanzareDB` — interfata pentru produse_vanzari
- `ProdusVanzareDetaliuDB` — interfata pentru produse_vanzari_detalii
- `ProdusVarianta` — tip compus cu produs_denumire inclus
- `Produs` — tip compus cu `variante: ProdusVariantaDB[]`
- `ProdusVanzare` — tip compus cu `detalii: ProdusVanzareDetaliuDB[]` + `sportiv_nume?: string`
- `'produse' | 'vanzari-produse'` adăugate în View type union

</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SQL Migration — schema produse, variante, intrări, vânzări + RLS + seed categorii</name>
  <files>sql/12-produse-schema.sql</files>
  <read_first>
    - sql/ (listează fișierele existente pentru a înțelege convenția de naming și structura)
    - types.ts (liniile 150-175 — structura Plata pentru a înțelege FK-ul plata_id)
  </read_first>
  <action>
    Creează `sql/12-produse-schema.sql` cu conținutul exact de mai jos. Fișierul este pentru aplicare manuală în Supabase SQL Editor — nu se execută automat. Include comentarii clare pe secțiuni.

    Structura fișierului:

    **Secțiunea 1: produse_categorii**
    ```sql
    CREATE TABLE IF NOT EXISTS produse_categorii (
      id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
      denumire text NOT NULL UNIQUE,
      ordine integer NOT NULL DEFAULT 0,
      activa boolean NOT NULL DEFAULT true,
      created_at timestamptz DEFAULT now()
    );

    ALTER TABLE produse_categorii ENABLE ROW LEVEL SECURITY;

    -- Oricine autentificat vede categoriile (sunt globale)
    CREATE POLICY "produse_categorii_select" ON produse_categorii
      FOR SELECT TO authenticated USING (true);

    -- Doar SUPER_ADMIN poate insera/modifica categorii
    CREATE POLICY "produse_categorii_insert" ON produse_categorii
      FOR INSERT TO authenticated
      WITH CHECK (
        EXISTS (
          SELECT 1 FROM roluri_utilizatori ru
          JOIN roluri r ON r.id = ru.rol_id
          WHERE ru.user_id = auth.uid() AND r.nume = 'SUPER_ADMIN_FEDERATIE'
        )
      );

    CREATE POLICY "produse_categorii_update" ON produse_categorii
      FOR UPDATE TO authenticated
      USING (
        EXISTS (
          SELECT 1 FROM roluri_utilizatori ru
          JOIN roluri r ON r.id = ru.rol_id
          WHERE ru.user_id = auth.uid() AND r.nume = 'SUPER_ADMIN_FEDERATIE'
        )
      );

    -- Seed categorii predefinite
    INSERT INTO produse_categorii (denumire, ordine) VALUES
      ('Vo-phuc', 1),
      ('Centuri / Esarfe', 2),
      ('Mănuși', 3),
      ('Tibiere', 4),
      ('Platose', 5),
      ('Veste', 6),
      ('Embleme', 7),
      ('Alte echipamente', 8)
    ON CONFLICT (denumire) DO NOTHING;
    ```

    **Secțiunea 2: produse**
    ```sql
    CREATE TABLE IF NOT EXISTS produse (
      id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
      club_id uuid NOT NULL REFERENCES clubs(id) ON DELETE CASCADE,
      categorie_id uuid NOT NULL REFERENCES produse_categorii(id),
      denumire text NOT NULL,
      descriere text,
      activ boolean NOT NULL DEFAULT true,
      created_at timestamptz DEFAULT now(),
      updated_at timestamptz DEFAULT now()
    );

    CREATE INDEX IF NOT EXISTS produse_club_id_idx ON produse(club_id);

    ALTER TABLE produse ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "produse_select" ON produse
      FOR SELECT TO authenticated
      USING (
        club_id = (
          SELECT club_id FROM roluri_utilizatori
          WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
        )
        OR EXISTS (
          SELECT 1 FROM roluri_utilizatori ru
          JOIN roluri r ON r.id = ru.rol_id
          WHERE ru.user_id = auth.uid() AND r.nume = 'SUPER_ADMIN_FEDERATIE'
        )
      );

    CREATE POLICY "produse_insert" ON produse
      FOR INSERT TO authenticated
      WITH CHECK (
        club_id = (
          SELECT club_id FROM roluri_utilizatori
          WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
        )
      );

    CREATE POLICY "produse_update" ON produse
      FOR UPDATE TO authenticated
      USING (
        club_id = (
          SELECT club_id FROM roluri_utilizatori
          WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
        )
      );

    CREATE POLICY "produse_delete" ON produse
      FOR DELETE TO authenticated
      USING (
        club_id = (
          SELECT club_id FROM roluri_utilizatori
          WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
        )
      );
    ```

    **Secțiunea 3: produse_variante**
    ```sql
    CREATE TABLE IF NOT EXISTS produse_variante (
      id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
      produs_id uuid NOT NULL REFERENCES produse(id) ON DELETE CASCADE,
      culoare text,
      marime text,
      pret_intrare numeric(10,2) NOT NULL DEFAULT 0,
      pret_vanzare numeric(10,2) NOT NULL DEFAULT 0,
      stoc_curent integer NOT NULL DEFAULT 0,
      stoc_minim integer NOT NULL DEFAULT 0,
      activa boolean NOT NULL DEFAULT true,
      created_at timestamptz DEFAULT now(),
      updated_at timestamptz DEFAULT now()
    );

    CREATE INDEX IF NOT EXISTS produse_variante_produs_id_idx ON produse_variante(produs_id);

    ALTER TABLE produse_variante ENABLE ROW LEVEL SECURITY;

    -- Variantele moștenesc securitatea produsului (join la produse)
    CREATE POLICY "produse_variante_select" ON produse_variante
      FOR SELECT TO authenticated
      USING (
        EXISTS (SELECT 1 FROM produse p WHERE p.id = produs_id AND (
          p.club_id = (
            SELECT club_id FROM roluri_utilizatori
            WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
          )
          OR EXISTS (
            SELECT 1 FROM roluri_utilizatori ru JOIN roluri r ON r.id = ru.rol_id
            WHERE ru.user_id = auth.uid() AND r.nume = 'SUPER_ADMIN_FEDERATIE'
          )
        ))
      );

    CREATE POLICY "produse_variante_insert" ON produse_variante
      FOR INSERT TO authenticated
      WITH CHECK (
        EXISTS (SELECT 1 FROM produse p WHERE p.id = produs_id AND
          p.club_id = (
            SELECT club_id FROM roluri_utilizatori
            WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
          )
        )
      );

    CREATE POLICY "produse_variante_update" ON produse_variante
      FOR UPDATE TO authenticated
      USING (
        EXISTS (SELECT 1 FROM produse p WHERE p.id = produs_id AND
          p.club_id = (
            SELECT club_id FROM roluri_utilizatori
            WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
          )
        )
      );

    CREATE POLICY "produse_variante_delete" ON produse_variante
      FOR DELETE TO authenticated
      USING (
        EXISTS (SELECT 1 FROM produse p WHERE p.id = produs_id AND
          p.club_id = (
            SELECT club_id FROM roluri_utilizatori
            WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
          )
        )
      );
    ```

    **Secțiunea 4: produse_intrari + produse_intrari_detalii**
    ```sql
    CREATE TABLE IF NOT EXISTS produse_intrari (
      id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
      club_id uuid NOT NULL REFERENCES clubs(id) ON DELETE CASCADE,
      furnizor text,
      nr_factura text,
      data_factura date NOT NULL DEFAULT CURRENT_DATE,
      observatii text,
      created_by uuid REFERENCES auth.users(id),
      created_at timestamptz DEFAULT now()
    );

    CREATE INDEX IF NOT EXISTS produse_intrari_club_id_idx ON produse_intrari(club_id);

    ALTER TABLE produse_intrari ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "produse_intrari_select" ON produse_intrari
      FOR SELECT TO authenticated
      USING (club_id = (
        SELECT club_id FROM roluri_utilizatori
        WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
      ));

    CREATE POLICY "produse_intrari_insert" ON produse_intrari
      FOR INSERT TO authenticated
      WITH CHECK (club_id = (
        SELECT club_id FROM roluri_utilizatori
        WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
      ));

    CREATE TABLE IF NOT EXISTS produse_intrari_detalii (
      id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
      intrare_id uuid NOT NULL REFERENCES produse_intrari(id) ON DELETE CASCADE,
      varianta_id uuid NOT NULL REFERENCES produse_variante(id),
      cantitate integer NOT NULL CHECK (cantitate > 0),
      pret_intrare_snapshot numeric(10,2) NOT NULL,
      created_at timestamptz DEFAULT now()
    );

    CREATE INDEX IF NOT EXISTS produse_intrari_det_intrare_id_idx ON produse_intrari_detalii(intrare_id);

    ALTER TABLE produse_intrari_detalii ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "produse_intrari_detalii_select" ON produse_intrari_detalii
      FOR SELECT TO authenticated
      USING (EXISTS (
        SELECT 1 FROM produse_intrari pi WHERE pi.id = intrare_id
        AND pi.club_id = (
          SELECT club_id FROM roluri_utilizatori
          WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
        )
      ));

    CREATE POLICY "produse_intrari_detalii_insert" ON produse_intrari_detalii
      FOR INSERT TO authenticated
      WITH CHECK (EXISTS (
        SELECT 1 FROM produse_intrari pi WHERE pi.id = intrare_id
        AND pi.club_id = (
          SELECT club_id FROM roluri_utilizatori
          WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
        )
      ));
    ```

    **Secțiunea 5: produse_vanzari + produse_vanzari_detalii**
    ```sql
    CREATE TABLE IF NOT EXISTS produse_vanzari (
      id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
      club_id uuid NOT NULL REFERENCES clubs(id) ON DELETE CASCADE,
      sportiv_id uuid REFERENCES sportivi(id) ON DELETE SET NULL,
      plata_id uuid REFERENCES plati(id) ON DELETE SET NULL,
      data_vanzare date NOT NULL DEFAULT CURRENT_DATE,
      observatii text,
      total_vanzare numeric(10,2) NOT NULL DEFAULT 0,
      created_by uuid REFERENCES auth.users(id),
      created_at timestamptz DEFAULT now()
    );

    CREATE INDEX IF NOT EXISTS produse_vanzari_club_id_idx ON produse_vanzari(club_id);
    CREATE INDEX IF NOT EXISTS produse_vanzari_sportiv_id_idx ON produse_vanzari(sportiv_id);

    ALTER TABLE produse_vanzari ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "produse_vanzari_select" ON produse_vanzari
      FOR SELECT TO authenticated
      USING (
        club_id = (
          SELECT club_id FROM roluri_utilizatori
          WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
        )
        OR sportiv_id IN (
          SELECT id FROM sportivi WHERE user_id = auth.uid()
        )
      );

    CREATE POLICY "produse_vanzari_insert" ON produse_vanzari
      FOR INSERT TO authenticated
      WITH CHECK (club_id = (
        SELECT club_id FROM roluri_utilizatori
        WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
      ));

    CREATE TABLE IF NOT EXISTS produse_vanzari_detalii (
      id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
      vanzare_id uuid NOT NULL REFERENCES produse_vanzari(id) ON DELETE CASCADE,
      varianta_id uuid NOT NULL REFERENCES produse_variante(id),
      cantitate integer NOT NULL CHECK (cantitate > 0),
      pret_vanzare_snapshot numeric(10,2) NOT NULL,
      pret_intrare_snapshot numeric(10,2) NOT NULL DEFAULT 0,
      denumire_snapshot text NOT NULL,
      created_at timestamptz DEFAULT now()
    );

    CREATE INDEX IF NOT EXISTS produse_vanzari_det_vanzare_id_idx ON produse_vanzari_detalii(vanzare_id);

    ALTER TABLE produse_vanzari_detalii ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "produse_vanzari_detalii_select" ON produse_vanzari_detalii
      FOR SELECT TO authenticated
      USING (EXISTS (
        SELECT 1 FROM produse_vanzari pv WHERE pv.id = vanzare_id
        AND (
          pv.club_id = (
            SELECT club_id FROM roluri_utilizatori
            WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
          )
          OR pv.sportiv_id IN (SELECT id FROM sportivi WHERE user_id = auth.uid())
        )
      ));

    CREATE POLICY "produse_vanzari_detalii_insert" ON produse_vanzari_detalii
      FOR INSERT TO authenticated
      WITH CHECK (EXISTS (
        SELECT 1 FROM produse_vanzari pv WHERE pv.id = vanzare_id
        AND pv.club_id = (
          SELECT club_id FROM roluri_utilizatori
          WHERE id = (current_setting('request.headers', true)::json->>'active-role-context-id')::uuid
        )
      ));
    ```
  </action>
  <verify>
    <automated>ls "C:/Users/lungu/portal-phihau/sql/" | grep produse</automated>
    <automated>grep -c "CREATE TABLE\|ENABLE ROW LEVEL SECURITY\|CREATE POLICY" "C:/Users/lungu/portal-phihau/sql/12-produse-schema.sql"</automated>
  </verify>
  <acceptance_criteria>
    - Există `sql/12-produse-schema.sql`
    - Conține cel puțin 7 `CREATE TABLE IF NOT EXISTS`
    - Conține `ENABLE ROW LEVEL SECURITY` pe fiecare tabel (7 apariții)
    - Conține seed INSERT cu 8 categorii predefinite
    - FK `produse.club_id REFERENCES clubs(id)` prezent
    - FK `produse_vanzari.plata_id REFERENCES plati(id) ON DELETE SET NULL` prezent
    - FK `produse_vanzari.sportiv_id REFERENCES sportivi(id) ON DELETE SET NULL` prezent
    - Câmp `pret_intrare_snapshot` în `produse_vanzari_detalii` (snapshot la momentul vânzării — D-09)
    - Câmp `denumire_snapshot` în `produse_vanzari_detalii` (denumire la momentul vânzării)
  </acceptance_criteria>
  <done>sql/12-produse-schema.sql creat cu schema completă, RLS pe toate tabelele, seed 8 categorii.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TypeScript types — interfețe DB + tipuri compuse + extindere View union</name>
  <files>types.ts</files>
  <read_first>
    - types.ts (liniile 150-240 — structura Plata, TipPlata; liniile 530-540 — View type union la finalul fișierului)
  </read_first>
  <action>
    Adaugă la FINALUL fișierului `types.ts` (după ultima linie existentă), înainte de exportul View type, tipurile pentru modulul Produse:

    ```typescript
    // ==================== MODUL PRODUSE ====================

    export interface ProdusCategorieDB {
      id: string;
      denumire: string;
      ordine: number;
      activa: boolean;
      created_at: string;
    }

    export interface ProdusDB {
      id: string;
      club_id: string;
      categorie_id: string;
      denumire: string;
      descriere: string | null;
      activ: boolean;
      created_at: string;
      updated_at: string;
    }

    export interface ProdusVariantaDB {
      id: string;
      produs_id: string;
      culoare: string | null;
      marime: string | null;
      pret_intrare: number;
      pret_vanzare: number;
      stoc_curent: number;
      stoc_minim: number;
      activa: boolean;
      created_at: string;
      updated_at: string;
    }

    export interface ProdusIntrareDB {
      id: string;
      club_id: string;
      furnizor: string | null;
      nr_factura: string | null;
      data_factura: string;
      observatii: string | null;
      created_by: string | null;
      created_at: string;
    }

    export interface ProdusIntrareDetaliuDB {
      id: string;
      intrare_id: string;
      varianta_id: string;
      cantitate: number;
      pret_intrare_snapshot: number;
      created_at: string;
    }

    export interface ProdusVanzareDB {
      id: string;
      club_id: string;
      sportiv_id: string | null;
      plata_id: string | null;
      data_vanzare: string;
      observatii: string | null;
      total_vanzare: number;
      created_by: string | null;
      created_at: string;
    }

    export interface ProdusVanzareDetaliuDB {
      id: string;
      vanzare_id: string;
      varianta_id: string;
      cantitate: number;
      pret_vanzare_snapshot: number;
      pret_intrare_snapshot: number;
      denumire_snapshot: string;
      created_at: string;
    }

    // Tipuri compuse (join-uri frecvente)

    export interface ProdusVarianta extends ProdusVariantaDB {
      produs_denumire?: string;
      categorie_denumire?: string;
    }

    export interface Produs extends ProdusDB {
      variante: ProdusVariantaDB[];
      categorie?: ProdusCategorieDB;
    }

    export interface ProdusIntrare extends ProdusIntrareDB {
      detalii: (ProdusIntrareDetaliuDB & {
        varianta?: ProdusVariantaDB & { produs?: Pick<ProdusDB, 'denumire'> };
      })[];
    }

    export interface ProdusVanzare extends ProdusVanzareDB {
      detalii: ProdusVanzareDetaliuDB[];
      sportiv_nume?: string;
    }
    ```

    Extinde View type union (linia care conține `export type View = ...`) adăugând la final: `| 'produse' | 'vanzari-produse'`

    Atenție: View type este pe o singură linie lungă în fișier — adaugă cele 2 valori la final, înainte de `;`
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "types.ts" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -c "ProdusVariantaDB\|ProdusVanzareDetaliuDB\|'produse'" types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` conține `export interface ProdusVariantaDB` cu câmpurile `pret_intrare`, `pret_vanzare`, `stoc_curent`, `stoc_minim`
    - `types.ts` conține `export interface ProdusVanzareDetaliuDB` cu câmpurile `pret_vanzare_snapshot`, `pret_intrare_snapshot`, `denumire_snapshot`
    - `types.ts` conține `export interface Produs extends ProdusDB` cu `variante: ProdusVariantaDB[]`
    - View type conține `'produse'` și `'vanzari-produse'`
    - `npx tsc --noEmit` trece fără erori noi
  </acceptance_criteria>
  <done>Toate tipurile Produse adăugate în types.ts, View union extins cu 'produse' | 'vanzari-produse', tsc trece.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (produse) | RLS filtrează pe club_id din header active-role-context-id |
| client → Supabase (produse_vanzari) | SPORTIV vede doar vânzările proprii (sportiv_id check pe auth.uid()) |
| SUPER_ADMIN → produse_categorii | Singura politică de insert/update pe categorii globale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | Sportiv accesând prețuri de intrare | mitigate | `pret_intrare` disponibil în DB dar UI sportiv afișează DOAR `pret_vanzare` (implementat în planul 12-05) |
| T-12-02 | Tampering | club_id incorect la insert produse | mitigate | RLS WITH CHECK compară club_id cu header active-role-context-id |
| T-12-03 | Information Disclosure | Cross-club data leak | mitigate | RLS SELECT filtrează strict pe club_id din header |
</threat_model>

<verification>
1. `sql/12-produse-schema.sql` există și conține toate CREATE TABLE + RLS + seed
2. `npx tsc --noEmit` — zero erori noi în types.ts
3. `npx vite build` — build complet (types noi nu sparg nicio componentă existentă)
4. Manual aplicare migration în Supabase SQL Editor — toate 7 tabele create cu succes
5. Manual: SELECT * FROM produse_categorii — returnează 8 rânduri seed
</verification>

<success_criteria>
- [ ] `sql/12-produse-schema.sql` creat cu 7 tabele + RLS + seed (PRD-01..PRD-06 foundation)
- [ ] `types.ts` extins cu 7 interfețe DB + 4 tipuri compuse
- [ ] View type conține `'produse'` și `'vanzari-produse'`
- [ ] `npx tsc --noEmit` și `npx vite build` trec
</success_criteria>

<output>
Create `.planning/phases/12-modul-produse/12-01-SUMMARY.md` when done
</output>
