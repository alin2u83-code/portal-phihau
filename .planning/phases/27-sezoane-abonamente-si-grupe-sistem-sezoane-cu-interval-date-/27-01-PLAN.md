---
phase: 27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql
  - tests/rls_izolare_sezoane_faza27.ts
  - types.ts
  - hooks/useSezoane.ts
  - hooks/useDataProvider.ts
  - components/icons.tsx
autonomous: true
requirements: [SEZ-01, SEZ-02, SEZ-03, SEZ-04, SEZ-06, SEZ-08]

must_haves:
  truths:
    - "Tabelul public.sezoane exista pe DB live cu interval de date liber (data_start, data_final) per club (D-01, D-02)"
    - "Un al doilea sezon activ in acelasi club este respins de DB, nu doar de UI (D-03)"
    - "Un ADMIN_CLUB din clubul A nu vede si nu poate scrie sezoanele clubului B; un INSTRUCTOR nu poate crea/edita niciun sezon (D-02)"
    - "Grupele existente raman functionale dupa migratie, implicit tip_grupa='permanent' (D-04, D-05)"
    - "Codul TypeScript expune tipul Sezon si campurile noi grupe.tip_grupa/sezon_id/arhivat si tipuri_abonament.sezon_id"
  artifacts:
    - path: "supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql"
      provides: "DDL sezoane + RLS + index unic partial + ALTER grupe/tipuri_abonament"
      contains: "idx_sezoane_activ_per_club"
    - path: "tests/rls_izolare_sezoane_faza27.ts"
      provides: "Test automat izolare cross-club + gate de rol pe sezoane"
      contains: "sezoane"
    - path: "hooks/useSezoane.ts"
      provides: "useSezoane / useSezonActiv / useSezonActivCurent"
      exports: ["useSezoane", "useSezonActiv", "useSezonActivCurent"]
    - path: "types.ts"
      provides: "interface Sezon + campuri noi pe Grupa si TipAbonament + View 'sezoane'"
      contains: "export interface Sezon"
  key_links:
    - from: "supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql"
      to: "public.has_access_to_club / public.is_super_admin"
      via: "politici RLS sezoane_select / sezoane_write"
      pattern: "has_access_to_club"
    - from: "hooks/useSezoane.ts"
      to: "public.sezoane"
      via: "supabase.from('sezoane').select"
      pattern: "from\\('sezoane'\\)"
    - from: "hooks/useDataProvider.ts"
      to: "tipuri_abonament.sezon_id"
      via: "coloana adaugata in select-ul explicit"
      pattern: "sezon_id"
---

<objective>
Creeaza fundatia de date si contractele TypeScript pentru sistemul de sezoane: tabelul nou `public.sezoane` (interval de date liber, per club, cu maxim un sezon activ impus de DB), coloanele noi pe `grupe` (`tip_grupa`, `sezon_id`, `arhivat`) si pe `tipuri_abonament` (`sezon_id`), aplicarea migratiei pe DB live si verificarea izolarii cross-club, plus tipurile/hook-urile pe care le consuma toate celelalte planuri ale fazei.

Purpose: fara schema live si fara tipurile TS, niciunul dintre planurile 27-02..27-05 nu poate fi implementat sau testat — build-ul TypeScript trece si cu o schema DB inexistenta (tipurile vin din `types.ts`, nu din DB), deci aplicarea live este obligatorie aici.
Output: migratie SQL aplicata live, test automat de izolare RLS, `interface Sezon` + campuri noi in `types.ts`, `hooks/useSezoane.ts`, `sezon_id` in select-ul `tipuri_abonament` din `useDataProvider.ts`, `CopyIcon` in `components/icons.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-CONTEXT.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-RESEARCH.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit live pg_policies + scrierea migratiei sezoane/grupe/tipuri_abonament</name>
  <files>supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql</files>
  <read_first>
    - supabase/migrations/20260828_fix_rls_izolare_cross_club_grupe_prezenta_abonamente.sql (Sectiunea 3 `tipuri_abonament_select`/`tipuri_abonament_write` si Sectiunea 4 `grupe` — pattern RLS canonic de copiat)
    - sql/migrations/fix_rls_context_aware_role_helpers.sql (definitiile exacte `is_super_admin()`, `este_staff_club()`, `has_access_to_club()`)
    - sql/migrations/create_perioade_vacanta.sql (precedent schema "interval liber + club_id"; RLS-ul din el este EXEMPLU NEGATIV — `USING (true)`, nu se copiaza)
    - supabase/migrations/20260829_add_status_anulat_plati.sql (stil antet + idempotenta migratie)
    - .planning/phases/27-.../27-RESEARCH.md sectiunile "Architecture Patterns" (Pattern 2, Pattern 3) si "Common Pitfalls" (Pitfall 3, Pitfall 4)
  </read_first>
  <action>
Pas A — audit live OBLIGATORIU inainte de a scrie o linie de SQL (Pitfall 3 din RESEARCH.md: politici "fantoma" aplicate direct pe DB, absente din repo). Prin MCP Supabase `execute_sql` ruleaza si noteaza rezultatele brute in SUMMARY:
1. `select tablename, policyname, cmd, roles, qual, with_check from pg_policies where schemaname='public' and tablename in ('grupe','tipuri_abonament','sezoane') order by tablename, policyname;`
2. `select to_regclass('public.sezoane') as sezoane_exista;`
3. `select table_name, column_name, data_type, is_nullable, column_default from information_schema.columns where table_schema='public' and table_name in ('grupe','tipuri_abonament','plati') order by table_name, column_name;`
4. `select count(*) as nr_grupe from public.grupe;` si `select count(*) as nr_tipuri from public.tipuri_abonament;`

Din interogarea 3 confirma explicit in SUMMARY doua fapte care contrazic RESEARCH.md si sunt load-bearing pentru planurile 27-04/27-05: (a) tabelul `plati` NU are coloana `tip_abonament_id` (istoricul facturilor este denormalizat prin `plati.suma` + `plati.descriere`, deci D-09/SEZ-09 este satisfacut prin constructie, nu prin FK); (b) referintele reale catre `tipuri_abonament.id` sunt `sportivi.tip_abonament_id`, `familii.tip_abonament_id` si `participare_vacanta.tip_abonament_anterior_id`.

Pas B — scrie fisierul `supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql`, idempotent (IF NOT EXISTS / DROP POLICY IF EXISTS inainte de CREATE POLICY), cu antet-comentariu in stilul migratiei 20260829 care documenteaza: data, scopul, deciziile D-01..D-04/D-06/D-09 acoperite, si rezultatul auditului de la Pas A. Continut exact:

SECTIUNEA 1 — `CREATE TABLE IF NOT EXISTS public.sezoane` cu coloanele: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `club_id UUID NOT NULL REFERENCES public.cluburi(id) ON DELETE CASCADE`, `denumire TEXT NOT NULL`, `data_start DATE NOT NULL`, `data_final DATE NOT NULL`, `activ BOOLEAN NOT NULL DEFAULT true`, plus `CONSTRAINT sezon_date_valide CHECK (data_final >= data_start)` (D-01).

SECTIUNEA 2 — indexuri: `CREATE INDEX IF NOT EXISTS sezoane_club_id_idx ON public.sezoane(club_id);` si enforcement-ul D-03 la nivel de DB: `CREATE UNIQUE INDEX IF NOT EXISTS idx_sezoane_activ_per_club ON public.sezoane(club_id) WHERE activ = true;`.

SECTIUNEA 3 — RLS pe `sezoane`: `ALTER TABLE public.sezoane ENABLE ROW LEVEL SECURITY;`, apoi `DROP POLICY IF EXISTS "sezoane_select"` + `CREATE POLICY "sezoane_select" ON public.sezoane FOR SELECT TO authenticated USING (public.is_super_admin() OR public.has_access_to_club(club_id));` si `DROP POLICY IF EXISTS "sezoane_write"` + `CREATE POLICY "sezoane_write" ON public.sezoane FOR ALL TO authenticated` cu acelasi predicat in USING si in WITH CHECK: `EXISTS (SELECT 1 FROM public.utilizator_roluri_multicont WHERE user_id = auth.uid() AND rol_denumire IN ('SUPER_ADMIN_FEDERATIE','ADMIN','ADMIN_CLUB')) AND (public.has_access_to_club(club_id) OR public.is_super_admin())`. Adauga comentariu care explica de ce NU se foloseste `este_staff_club(club_id)` ca predicat de scoping aici (include INSTRUCTOR, ar contrazice D-02 — asumptia A1 din RESEARCH.md, confirmata prin definitia functiei din `fix_rls_context_aware_role_helpers.sql`).

SECTIUNEA 4 — `ALTER TABLE public.grupe ADD COLUMN IF NOT EXISTS tip_grupa TEXT NOT NULL DEFAULT 'permanent'` cu `CHECK (tip_grupa IN ('permanent','per_sezon'))` adaugat ca constraint separat idempotent (`grupe_tip_grupa_check`, precedat de DROP CONSTRAINT IF EXISTS), `ADD COLUMN IF NOT EXISTS sezon_id UUID REFERENCES public.sezoane(id) ON DELETE SET NULL`, `ADD COLUMN IF NOT EXISTS arhivat BOOLEAN NOT NULL DEFAULT false`. DEFAULT 'permanent' este obligatoriu (Pitfall 4: `GrupaFormModal.tsx` nu trimite azi campul, iar cele ~7 cluburi au grupe reale — defaultul pastreaza comportamentul actual, D-05).

SECTIUNEA 5 — `ALTER TABLE public.tipuri_abonament ADD COLUMN IF NOT EXISTS sezon_id UUID REFERENCES public.sezoane(id) ON DELETE SET NULL;` nullable, fara default (randurile istorice raman valide, D-09).

SECTIUNEA 6 — index de suport `CREATE INDEX IF NOT EXISTS grupe_sezon_id_idx ON public.grupe(sezon_id);` si `CREATE INDEX IF NOT EXISTS tipuri_abonament_sezon_id_idx ON public.tipuri_abonament(sezon_id);`.

NU modifica nicio politica existenta pe `grupe` (politica "Staff - Full Access Grupe" bazata pe `has_access_to_club(club_id)` fara gate de rol ramane neatinsa — D-04 cere explicit ca INSTRUCTOR sa poata seta `tip_grupa`/`sezon_id`). NU folosi `get_my_clubs()`, `get_my_club_ids()` sau `USING (true)` nicaieri.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql && grep -c "idx_sezoane_activ_per_club" supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql && grep -c "sezoane_write" supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql && grep -c "DEFAULT 'permanent'" supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql && test "$(grep -v '^--' supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql | grep -c -E 'get_my_club|USING \(true\)')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Fisierul contine `CREATE TABLE IF NOT EXISTS public.sezoane` cu toate cele 7 coloane si constraint-ul `sezon_date_valide`.
    - Fisierul contine `CREATE UNIQUE INDEX IF NOT EXISTS idx_sezoane_activ_per_club ON public.sezoane(club_id) WHERE activ = true`.
    - Politica `sezoane_write` contine atat predicatul `rol_denumire IN ('SUPER_ADMIN_FEDERATIE', 'ADMIN', 'ADMIN_CLUB')` cat si `public.has_access_to_club(club_id)`, in USING si in WITH CHECK.
    - `ALTER TABLE public.grupe` adauga exact `tip_grupa` (NOT NULL DEFAULT 'permanent' + CHECK cu cele doua valori), `sezon_id` (nullable, ON DELETE SET NULL), `arhivat` (NOT NULL DEFAULT false).
    - `ALTER TABLE public.tipuri_abonament` adauga `sezon_id` nullable.
    - Zero aparitii de `get_my_club`, `get_my_club_ids` sau `USING (true)` in liniile de cod (comentariile explicative sunt permise).
    - SUMMARY contine rezultatele brute ale celor 4 interogari live si afirmatia explicita despre absenta coloanei `plati.tip_abonament_id`.
  </acceptance_criteria>
  <done>Migratia exista pe disc, este idempotenta, foloseste doar helperele RLS canonice post-Faza 25, iar starea live a politicilor pe `grupe`/`tipuri_abonament` este documentata in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicare migratie pe DB live + test automat izolare cross-club sezoane</name>
  <files>tests/rls_izolare_sezoane_faza27.ts</files>
  <read_first>
    - supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql (fisierul scris la Task 1 — sursa SQL exacta de aplicat)
    - tests/rls_izolare_sezoane_faza25.ts NU exista; citeste tests/rls_izolare_cross_club_faza25.ts integral (harness-ul de copiat: client service-role + client anon cu header `active-role-context-id`, `record()`, cleanup obligatoriu in `finally`, UUID-urile CLUB_A/CLUB_B de referinta)
    - .planning/phases/27-.../27-RESEARCH.md sectiunea "Security Domain" (cele 3 threat patterns)
  </read_first>
  <action>
Pas A — [BLOCKING] aplica migratia pe proiectul Supabase live prin MCP `apply_migration`, cu `name` = `add_sezoane_grupe_tipuri_abonament` si `query` = continutul integral al fisierului scris la Task 1. Nu considera build-ul/`npm run lint` drept dovada — tipurile TS nu sunt derivate din DB, deci un pas fals-pozitiv este posibil daca migratia nu ruleaza.

Pas B — verifica aplicarea prin `execute_sql`, si noteaza rezultatele in SUMMARY:
1. `select column_name, data_type, is_nullable, column_default from information_schema.columns where table_schema='public' and table_name='sezoane' order by ordinal_position;` (asteptat: 7 coloane)
2. `select indexname, indexdef from pg_indexes where schemaname='public' and tablename='sezoane';` (asteptat: PK + `sezoane_club_id_idx` + `idx_sezoane_activ_per_club` cu `WHERE (activ = true)`)
3. `select policyname, cmd, qual, with_check from pg_policies where schemaname='public' and tablename='sezoane';` (asteptat exact 2: `sezoane_select`, `sezoane_write`)
4. `select column_name, column_default, is_nullable from information_schema.columns where table_schema='public' and table_name='grupe' and column_name in ('tip_grupa','sezon_id','arhivat');`
5. `select count(*) as grupe_fara_tip from public.grupe where tip_grupa is null;` (asteptat 0)
6. `select column_name from information_schema.columns where table_schema='public' and table_name='tipuri_abonament' and column_name='sezon_id';`

Pas C — scrie `tests/rls_izolare_sezoane_faza27.ts`, modelat 1:1 dupa `tests/rls_izolare_cross_club_faza25.ts` (acelasi import `createClient` + `dotenv/config`, aceleasi constante CLUB_A/CLUB_B, prefix canar `ZZ_TEST_FAZA27_`, functia `record()` care arunca la prima esuare, cleanup complet in `finally`). Daca lipsesc `VITE_SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `VITE_SUPABASE_ANON_KEY` din `.env`, testul iese cu cod 1 si mesaj explicit — in acest caz opreste executia planului si raporteaza blocajul, nu marca task-ul ca reusit.

Verificarile obligatorii ale testului (fiecare cu `record(...)`):
1. Pregatire cu clientul service-role: un user efemer + rand ADMIN_CLUB @ CLUB_A si un rand INSTRUCTOR @ CLUB_A in `utilizator_roluri_multicont`; un sezon canar in CLUB_B si unul in CLUB_A.
2. SELECT ca ADMIN_CLUB@CLUB_A: zero randuri cu `club_id = CLUB_B`; sezonul canar din CLUB_A este vizibil (fara regresie).
3. INSERT ca ADMIN_CLUB@CLUB_A cu `club_id = CLUB_B`: respins (Information Disclosure/Tampering cross-club).
4. INSERT ca ADMIN_CLUB@CLUB_A in propriul club: acceptat; DELETE pe propriul rand: acceptat.
5. INSERT ca INSTRUCTOR@CLUB_A in propriul club: RESPINS (D-02 / Elevation of Privilege — este dovada ca `este_staff_club` nu a fost folosit ca gate).
6. Enforcement D-03: cu clientul service-role, un al doilea INSERT cu `activ = true` in acelasi club esueaza cu `unique_violation` (cod `23505`); dupa `UPDATE ... SET activ=false` pe primul, acelasi INSERT reuseste.

Ruleaza testul si include output-ul integral (PASS/FAIL per verificare) in SUMMARY.
  </action>
  <verify>
    <automated>npx tsx tests/rls_izolare_sezoane_faza27.ts</automated>
  </verify>
  <acceptance_criteria>
    - `select to_regclass('public.sezoane')` returneaza non-null pe DB live.
    - `pg_indexes` contine `idx_sezoane_activ_per_club` cu predicatul `WHERE (activ = true)`.
    - `pg_policies` pentru `sezoane` contine exact `sezoane_select` si `sezoane_write`, ambele cu `roles = {authenticated}`.
    - `select count(*) from public.grupe where tip_grupa is null` returneaza 0.
    - `tests/rls_izolare_sezoane_faza27.ts` ruleaza cu exit code 0 si toate cele 6 grupe de verificari raporteaza PASS, inclusiv respingerea INSERT-ului de INSTRUCTOR si `23505` la al doilea sezon activ.
    - Toate randurile canar `ZZ_TEST_FAZA27_` si userul efemer sunt sterse la final (verificat printr-un `select count(*) from public.sezoane where denumire like 'ZZ_TEST_FAZA27_%'` = 0).
  </acceptance_criteria>
  <done>Schema exista pe DB live, izolarea cross-club si gate-ul de rol D-02 sunt dovedite automat, iar invarianta "un singur sezon activ per club" este impusa de DB.</done>
</task>

<task type="auto">
  <name>Task 3: Contracte TypeScript — tip Sezon, campuri noi, hooks/useSezoane.ts, CopyIcon</name>
  <files>types.ts, hooks/useSezoane.ts, hooks/useDataProvider.ts, components/icons.tsx</files>
  <read_first>
    - types.ts liniile 340-360 (`interface Grupa`), 530-545 (`interface TipAbonament`), 598-602 (`type View`), 1046-1062 (`interface PerioadaVacanta` — forma de referinta pentru un interval per club)
    - hooks/useGrupe.ts (pattern React Query + cache localStorage + staleTime conditionat, de replicat)
    - hooks/useDataProvider.ts linia ~335 (select-ul explicit pe `tipuri_abonament`)
    - components/icons.tsx liniile 1-20 (import-ul lucide + conventia `export const XIcon = Lucide;`)
    - contexts/DataContext.tsx (sursa `useData()` pentru `activeRoleContext`)
  </read_first>
  <action>
1. `types.ts` — adauga in sectiunea de domeniu (langa `PerioadaVacanta`, care e cel mai apropiat model) `export interface Sezon { id: string; created_at: string; club_id: string; denumire: string; data_start: string; data_final: string; activ: boolean; }` cu comentarii `// 'YYYY-MM-DD' (DATE din Postgres)` pe cele doua date, la fel ca la `PerioadaVacanta`. Extinde `interface Grupa` cu `tip_grupa?: 'permanent' | 'per_sezon';`, `sezon_id?: string | null;`, `arhivat?: boolean;` (toate optionale — cache-ul localStorage `cache_grupe_*` poate contine inca randuri fara ele). Extinde `interface TipAbonament` cu `sezon_id?: string | null;`. Adauga valoarea `'sezoane'` in union-ul `type View`.

2. `hooks/useSezoane.ts` (fisier nou) — replica structura din `hooks/useGrupe.ts` (React Query v5, `queryKey`, `enabled`, `staleTime`), fara cache localStorage (lista e mica si trebuie sa reflecte rapid activarea unui sezon). Exporta trei simboluri:
   - `useSezoane(clubId: string | null | undefined)`: `useQuery<Sezon[], Error>` cu `queryKey: ['sezoane', clubId]`, `enabled: !!clubId`, `queryFn` = `supabase.from('sezoane').select('*').eq('club_id', clubId).order('data_start', { ascending: false })`, arunca la eroare, `staleTime: 5 * 60 * 1000`.
   - `useSezonActiv(clubId: string | null | undefined)`: apeleaza `useSezoane(clubId)` si returneaza `{ sezonActiv: (data ?? []).find(s => s.activ) ?? null, sezonActivId: <id sau null>, sezoane: data ?? [], isLoading }`.
   - `useSezonActivCurent()`: deriva `clubId` din `useData().activeRoleContext?.club_id ?? null` si returneaza `useSezonActiv(clubId)` — folosit de componentele de facturare care nu primesc clubul prin props.
   Documenteaza in comentariul de fisier regula de fallback: cand `clubId` este null (admin de federatie fara context de club) rezultatul este `sezonActivId = null`, iar consumatorii trebuie sa trateze asta ca "fara filtrare pe sezon" (comportament identic cu cel de dinaintea fazei 27).

3. `hooks/useDataProvider.ts` — la linia ~335 adauga `sezon_id` in select-ul explicit `from('tipuri_abonament').select('id, denumire, pret, club_id, numar_membri')`. Fara aceasta modificare, `sezon_id` ajunge `undefined` in `filteredData.tipuriAbonament` si intreaga filtrare pe sezon din planul 27-04 devine silentios inoperanta.

4. `components/icons.tsx` — adauga `Copy` in lista de importuri din `lucide-react` si `export const CopyIcon = Copy;` langa celelalte export-uri (folosit de actiunea "Dubleaza in sezon nou" din planul 27-03).
  </action>
  <verify>
    <automated>npm run lint && grep -c "export interface Sezon" types.ts && grep -c "'sezoane'" types.ts && grep -c "useSezonActivCurent" hooks/useSezoane.ts && grep -c "sezon_id" hooks/useDataProvider.ts && grep -c "export const CopyIcon" components/icons.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` (tsc --noEmit) trece fara erori.
    - `types.ts` exporta `interface Sezon` cu exact cele 7 campuri ale tabelului si contine `'sezoane'` in union-ul `View`.
    - `Grupa` are `tip_grupa`, `sezon_id`, `arhivat` (toate optionale); `TipAbonament` are `sezon_id`.
    - `hooks/useSezoane.ts` exporta `useSezoane`, `useSezonActiv`, `useSezonActivCurent` si foloseste `queryKey: ['sezoane', clubId]`.
    - `hooks/useDataProvider.ts` include `sezon_id` in select-ul `tipuri_abonament`.
    - `components/icons.tsx` exporta `CopyIcon`.
  </acceptance_criteria>
  <done>Toate planurile din wave 2 pot importa `Sezon`, `useSezonActiv`/`useSezonActivCurent` si `CopyIcon` fara sa mai exploreze codebase-ul, iar `sezon_id` ajunge efectiv in datele client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (client anon key) -> PostgREST/Supabase | Orice utilizator autentificat poate emite direct requesturi pe `sezoane`; singura aparare reala este RLS |
| context de rol activ (header `active-role-context-id`) -> RLS helpers | Headerul e controlat de client; `has_access_to_club()` il valideaza contra `utilizator_roluri_multicont` pentru `auth.uid()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | `public.sezoane` SELECT | mitigate | Politica `sezoane_select` = `is_super_admin() OR has_access_to_club(club_id)`; dovedit de tests/rls_izolare_sezoane_faza27.ts verificarea 2 |
| T-27-02 | Tampering | `public.sezoane` INSERT/UPDATE/DELETE cross-club | mitigate | `sezoane_write` cu `has_access_to_club(club_id)` in USING si WITH CHECK; dovedit de verificarea 3 |
| T-27-03 | Elevation of Privilege | INSTRUCTOR creeaza/editeaza sezoane (contrazice D-02) | mitigate | Predicat de rol explicit `rol_denumire IN ('SUPER_ADMIN_FEDERATIE','ADMIN','ADMIN_CLUB')`; NU se foloseste `este_staff_club()` (include INSTRUCTOR); dovedit de verificarea 5 |
| T-27-04 | Tampering (stare inconsistenta) | Doua cereri concurente activeaza doua sezoane in acelasi club | mitigate | `CREATE UNIQUE INDEX idx_sezoane_activ_per_club ... WHERE activ = true`; a doua tranzactie primeste `23505`; dovedit de verificarea 6 |
| T-27-05 | Denial of Service (functional) | Migratia sparge INSERT-urile existente de grupe (`tip_grupa` NOT NULL fara default) | mitigate | `DEFAULT 'permanent'` + verificare live `count(*) where tip_grupa is null = 0` |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | Faza nu instaleaza niciun pachet nou (RESEARCH.md "Package Legitimacy Audit: nu se aplica") — gate slopcheck neaplicabil |
</threat_model>

<verification>
- `npx tsx tests/rls_izolare_sezoane_faza27.ts` -> exit 0, toate verificarile PASS.
- `npm run lint` -> zero erori TypeScript.
- Interogare live: `select to_regclass('public.sezoane')` non-null; `pg_policies` pe `sezoane` = 2 politici; `pg_indexes` contine `idx_sezoane_activ_per_club`.
- Interogare live: `select count(*) from public.grupe where tip_grupa is null` = 0 si `select count(*) from public.grupe` neschimbat fata de auditul din Task 1.
</verification>

<success_criteria>
- Tabelul `sezoane` exista pe DB live cu RLS activ si cele 2 politici canonice.
- Invarianta D-03 (un singur sezon activ per club) este impusa de DB, nu de UI.
- Grupele si tipurile de abonament existente functioneaza neschimbat dupa migratie.
- `types.ts`, `hooks/useSezoane.ts`, `hooks/useDataProvider.ts` si `components/icons.tsx` expun contractele pe care le consuma planurile 27-02..27-05.
</success_criteria>

## Artifacts this phase produces

Simboluri noi create in acest plan (nu exista inainte de faza 27):

- Tabel DB: `public.sezoane` (coloane: `id`, `created_at`, `club_id`, `denumire`, `data_start`, `data_final`, `activ`)
- Constraint DB: `sezon_date_valide`, `grupe_tip_grupa_check`
- Indexuri DB: `sezoane_club_id_idx`, `idx_sezoane_activ_per_club`, `grupe_sezon_id_idx`, `tipuri_abonament_sezon_id_idx`
- Politici RLS: `sezoane_select`, `sezoane_write`
- Coloane DB noi: `grupe.tip_grupa`, `grupe.sezon_id`, `grupe.arhivat`, `tipuri_abonament.sezon_id`
- Fisiere noi: `supabase/migrations/20260902_add_sezoane_grupe_tipuri_abonament.sql`, `tests/rls_izolare_sezoane_faza27.ts`, `hooks/useSezoane.ts`
- Simboluri TS noi: `Sezon` (types.ts), `Grupa.tip_grupa`, `Grupa.sezon_id`, `Grupa.arhivat`, `TipAbonament.sezon_id`, valoarea `'sezoane'` in `View`, `useSezoane`, `useSezonActiv`, `useSezonActivCurent`, `CopyIcon`

<output>
Create `.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-01-SUMMARY.md` when done
</output>
