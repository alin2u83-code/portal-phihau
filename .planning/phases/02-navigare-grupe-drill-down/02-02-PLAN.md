---
phase: 02-navigare-grupe-drill-down
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/Grupe/GrupaCard.tsx
  - components/Grupe/index.tsx
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03]
must_haves:
  truths:
    - "GrupaCard are buton primar Detalii care apelează onDetalii(grupa); butoanele Sportivi și Orar nu mai apar ca butoane principale"
    - "Apăsarea Detalii înlocuiește grila de carduri cu GrupaDetailView; butonul Înapoi la Grupe revine la grilă"
    - "Tab Sportivi din GrupaDetailView deschide AdaugaSportiviModal-ul existent montat în index.tsx"
    - "Butonul Gestionează (onEdit) și dropdown-ul ... rămân funcționale pe card"
  artifacts:
    - path: "components/Grupe/GrupaCard.tsx"
      provides: "Card cu buton Detalii (onDetalii) în loc de Sportivi+Orar"
      contains: "onDetalii"
    - path: "components/Grupe/index.tsx"
      provides: "State grupaSelectedForDetail + render condiționat GrupaDetailView vs grilă"
      contains: "grupaSelectedForDetail"
  key_links:
    - from: "components/Grupe/GrupaCard.tsx"
      to: "onDetalii prop"
      via: "Button primary onClick={() => onDetalii(grupa)}"
      pattern: "onDetalii\\(grupa\\)"
    - from: "components/Grupe/index.tsx"
      to: "components/Grupe/GrupaDetailView.tsx"
      via: "render condiționat {grupaSelectedForDetail ? <GrupaDetailView .../> : <grilă>}"
      pattern: "GrupaDetailView"
    - from: "GrupaDetailView onOpenAdaugaSportivi"
      to: "setGrupaForAdaugaSportivi în index.tsx"
      via: "callback prop care setează state-ul modalului existent"
      pattern: "setGrupaForAdaugaSportivi"
---

<objective>
Conectează `GrupaDetailView` (creat în Plan 01) la modulul Grupe: adaugă butonul "Detalii" pe `GrupaCard` (D-07) și logica de render condiționat în `Grupe/index.tsx` (D-01). După acest plan, slice-ul vertical este complet end-to-end — utilizatorul apasă "Detalii" pe un card și folosește efectiv view-ul cu tab-uri.

Purpose: Livrează NAV-01 (buton Detalii care deschide GrupaDetailView) și activează NAV-02/NAV-03 wiring-ul real. Drill-down ca state local în index.tsx, NU NavigationContext view swap (D-01).
Output: `GrupaCard.tsx` modificat (prop `onDetalii`, butoane principale simplificate) și `index.tsx` modificat (state `grupaSelectedForDetail` + render condiționat + GrupaDetailView montat). Un checkpoint human-verify confirmă fluxul vizual.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigare-grupe-drill-down/02-CONTEXT.md
@.planning/phases/02-navigare-grupe-drill-down/02-PATTERNS.md
@.planning/phases/02-navigare-grupe-drill-down/02-UI-SPEC.md
@components/Grupe/GrupaCard.tsx
@components/Grupe/index.tsx
@components/Grupe/GrupaDetailView.tsx
</context>

## Artifacts this phase produces

Symbols created/modified by Plan 02 (treat as new/changed, not drift):
- `GrupaCard` prop `onDetalii: (g: GrupaWithDetails) => void` — NOU
- `GrupaCard` props `onAdaugaSportivi` și `onConfigurareOrar` — ELIMINATE din interfață și din butoanele principale
- `Grupe/index.tsx` state `grupaSelectedForDetail` + setter `setGrupaSelectedForDetail` — NOU
- `Grupe/index.tsx` render condiționat `{grupaSelectedForDetail ? <GrupaDetailView/> : <grilă>}` — NOU
- Import nou `GrupaDetailView` în `Grupe/index.tsx`

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: GrupaCard — adaugă buton Detalii, elimină butoanele principale Sportivi+Orar</name>
  <files>components/Grupe/GrupaCard.tsx</files>
  <read_first>
    - components/Grupe/GrupaCard.tsx (TOT fișierul — props liniile 43-63, butoane principale liniile 141-151, dropdown 154-214)
    - .planning/phases/02-navigare-grupe-drill-down/02-PATTERNS.md (GrupaCard modificări liniile 214-265)
    - .planning/phases/02-navigare-grupe-drill-down/02-UI-SPEC.md (GrupaCard button layout liniile 116-125)
    - .planning/phases/02-navigare-grupe-drill-down/02-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
    În interfața props `GrupaCard` (liniile 43-52): ELIMINĂ `onAdaugaSportivi: (g: GrupaWithDetails) => void` și `onConfigurareOrar: (g: GrupaWithDetails) => void`. ADAUGĂ `onDetalii: (g: GrupaWithDetails) => void` (per D-07). Păstrează `onEdit`, `onDelete`, `onModificareOrar?`, `onGestionareSecundari?`, `onGenerareAntrenamente?`, `nrSecundari?`. Actualizează și destructurarea props (liniile 53-63): scoate `onAdaugaSportivi` și `onConfigurareOrar`, adaugă `onDetalii`.

    În zona butoanelor principale (liniile 141-151): ELIMINĂ cele două butoane `onAdaugaSportivi` ("Sportivi", variant info cu UserPlusIcon) și `onConfigurareOrar` ("Orar", variant secondary cu CogIcon). ADAUGĂ ca prim buton un `<Button size="sm" variant="primary" onClick={() => onDetalii(grupa)} className="min-h-[40px] touch-manipulation">Detalii</Button>`. PĂSTREAZĂ butonul "Gestionează" existent (`onClick={() => onEdit(grupa)}`) — D-08 cere să rămână; schimbă-i `variant` la `secondary` (per UI-SPEC layout: Detalii primary indigo | Gestionează secondary). Ordinea finală: `[Detalii] [Gestionează] [...]`.

    PĂSTREAZĂ NESCHIMBAT tot blocul dropdown "..." (liniile 154-214: Modifică Program, Generează Antrenamente, Secundari, separator, Șterge Grupa). Dacă importul `UserPlusIcon` sau `CogIcon` rămâne nefolosit după eliminarea butoanelor, curăță-l din linia 4 pentru a evita warning TypeScript (CogIcon poate fi încă folosit altundeva — verifică înainte de a-l elimina; UserPlusIcon era folosit doar pe butonul Sportivi).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fără erori sau warning-uri de unused import.
    - Interfața `GrupaCard` conține `onDetalii` și NU conține `onAdaugaSportivi` sau `onConfigurareOrar`.
    - Există un `Button variant="primary"` cu `onClick={() => onDetalii(grupa)}` și label "Detalii".
    - Butonul "Gestionează" (`onClick={() => onEdit(grupa)}`) este prezent cu `variant="secondary"`.
    - NU mai există butoane principale cu label "Sportivi" sau "Orar".
    - Blocul dropdown "..." (Modifică Program / Generează Antrenamente / Secundari / Șterge Grupa) este neschimbat.
  </acceptance_criteria>
  <done>GrupaCard afișează [Detalii] (primary) [Gestionează] (secondary) [...] — butoanele Sportivi și Orar eliminate, dropdown intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Grupe/index.tsx — state grupaSelectedForDetail + render condiționat + montare GrupaDetailView</name>
  <files>components/Grupe/index.tsx</files>
  <read_first>
    - components/Grupe/index.tsx (TOT — state liniile 43-53, grila de carduri liniile 238-248, montarea modalelor 252-298)
    - components/Grupe/GrupaDetailView.tsx (semnătura props: grupa, onBack, onOpenAdaugaSportivi)
    - .planning/phases/02-navigare-grupe-drill-down/02-PATTERNS.md (index.tsx modificări liniile 269-314)
    - .planning/phases/02-navigare-grupe-drill-down/02-RESEARCH.md (Pitfall 4 liniile 341-346: un singur AdaugaSportiviModal)
    - .planning/phases/02-navigare-grupe-drill-down/02-CONTEXT.md (D-01)
  </read_first>
  <action>
    Adaugă importul `import { GrupaDetailView } from './GrupaDetailView';` lângă celelalte importuri din `Grupe/`.

    Adaugă state nou după linia 52 (lângă celelalte `grupaFor*`): `const [grupaSelectedForDetail, setGrupaSelectedForDetail] = useState<GrupaWithDetails | null>(null);`.

    Modifică blocul de la liniile 238-248. Învelește grila condiționat: dacă `grupaSelectedForDetail` este non-null, randează `<GrupaDetailView grupa={grupaSelectedForDetail} onBack={() => setGrupaSelectedForDetail(null)} onOpenAdaugaSportivi={(g) => setGrupaForAdaugaSportivi(g)} />`; altfel randează blocul existent `{grupe.length > 0 ? <grilă> : <Card empty>}`. Structura: `{grupaSelectedForDetail ? (<GrupaDetailView .../>) : (<>{grilă/empty existente}</>)}`.

    În call-ul către `GrupaCard` (linia 241): ELIMINĂ props `onAdaugaSportivi={setGrupaForAdaugaSportivi}` și `onConfigurareOrar={setGrupaForOrar}`. ADAUGĂ `onDetalii={setGrupaSelectedForDetail}`. Păstrează `onEdit`, `onDelete`, `onModificareOrar`, `onGestionareSecundari`, `onGenerareAntrenamente`.

    NU elimina state-urile `grupaForAdaugaSportivi`/`setGrupaForAdaugaSportivi` și `grupaForOrar`/`setGrupaForOrar` — `AdaugaSportiviModal` (liniile 271-281) rămâne montat și e acum deschis prin callback-ul din GrupaDetailView (Pitfall 4: un SINGUR AdaugaSportiviModal, în index.tsx). `OrarEditorModal` (liniile 254-261) rămâne montat pentru backward-compat dar nu mai este declanșat din card (Tab Orar inline îl înlocuiește vizual). Nu monta o a doua instanță de AdaugaSportiviModal.

    Header-ul paginii Grupe (titlu + butoane Actualizează/Adaugă Grupă, liniile dinainte de grilă) — opțional: ascunde-l când `grupaSelectedForDetail` este non-null pentru un view curat. Dacă e simplu, învelește și header-ul în condiția `!grupaSelectedForDetail`; dacă complică structura, lasă-l vizibil (acceptabil pentru MVP).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fără erori.
    - `index.tsx` importă `GrupaDetailView` din `./GrupaDetailView`.
    - Există state `grupaSelectedForDetail` + setter `setGrupaSelectedForDetail`.
    - Render condiționat: `GrupaDetailView` este randat când `grupaSelectedForDetail` este non-null, cu props `grupa`, `onBack` (setează null) și `onOpenAdaugaSportivi` (apelează `setGrupaForAdaugaSportivi`).
    - Call-ul `GrupaCard` conține `onDetalii={setGrupaSelectedForDetail}` și NU mai conține `onAdaugaSportivi=` sau `onConfigurareOrar=`.
    - Există exact O singură instanță `<AdaugaSportiviModal` în tot fișierul.
  </acceptance_criteria>
  <done>Apăsarea Detalii pe card setează grupaSelectedForDetail și afișează GrupaDetailView; Înapoi la Grupe revine la grilă; Adaugă Sportivi din tab deschide modalul existent. Slice end-to-end complet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificare vizuală flux drill-down</name>
  <action>Checkpoint de verificare umană — fără modificări de cod. Executorul livrează aplicația rulând și ghidează utilizatorul prin pașii din `<how-to-verify>`, apoi așteaptă semnalul de reluare.</action>
  <what-built>
    Buton "Detalii" pe GrupaCard care deschide GrupaDetailView (state local în index.tsx), cu tab bar Antrenamente | Orar | Sportivi. Tab Orar editează+salvează orarul inline; Tab Sportivi listează read-only + deschide AdaugaSportiviModal; Tab Antrenamente = placeholder.
  </what-built>
  <how-to-verify>
    1. Rulează `npm run dev` și deschide aplicația; loghează-te ca ADMIN_CLUB sau INSTRUCTOR și navighează la modulul Grupe.
    2. Confirmă că fiecare card de grupă arată butoanele `[Detalii]` (indigo/primary), `[Gestionează]` (slate/secondary) și `[...]` — și NU mai are butoane separate "Sportivi" sau "Orar".
    3. Apasă "Detalii" pe o grupă. Așteptat: grila de carduri este înlocuită de GrupaDetailView cu header "Înapoi la Grupe" + numele grupei + "Sala: X · N sportivi activi", iar tab-ul activ implicit este "Antrenamente" (placeholder dashed "Calendar antrenamente — disponibil în curând").
    4. Click pe tab "Orar": apar zilele Luni-Duminică cu intervalele existente. Adaugă/șterge un interval, apasă "Salvează Orar". Așteptat: toast "Orarul a fost salvat.", rămâi pe tab Orar. Apasă "Resetează" după o modificare nesalvată — revine la ultimul orar salvat.
    5. Click pe tab "Sportivi": apare lista read-only (nume + grad) sau empty state. Apasă "Adaugă Sportivi" — se deschide AdaugaSportiviModal existent. Închide-l.
    6. Apasă "Înapoi la Grupe" — revii la grila de carduri. Apasă și butonul hardware Back (mobil) sau Backspace — confirmă că nu crapă navigarea.
    7. Reîncarcă pagina după ce ai salvat orarul — confirmă că orarul salvat persistă (cache invalidat corect).
  </how-to-verify>
  <resume-signal>Tastează "approved" dacă fluxul funcționează, sau descrie problemele observate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (React state) → render | Drill-down e pur state local în index.tsx; niciun input extern nou |
| GrupaDetailView → index.tsx callbacks | onOpenAdaugaSportivi declanșează state-ul modalului existent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Elevation of Privilege | GrupaCard / GrupaDetailView vizibilitate | accept | Vizibilitatea cardurilor și grupelor este deja filtrată pe club via RLS + `usePermissions` în index.tsx (neschimbat); butonul Detalii nu adaugă acces nou |
| T-02-05 | Tampering | Render condiționat state local | accept | grupaSelectedForDetail este state React efemer, nu persistat; nicio suprafață de atac nouă |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Nicio instalare de pachete — doar modificări la fișiere existente (confirmat în RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run lint` (tsc --noEmit) trece fără erori.
- Checkpoint human-verify aprobat (fluxul drill-down funcționează vizual).
- O singură instanță `AdaugaSportiviModal` în index.tsx.
</verification>

<success_criteria>
- NAV-01: buton Detalii pe GrupaCard deschide GrupaDetailView. ✓
- NAV-02: tab bar funcțional, default Antrenamente, persistent cât componenta e montată. ✓
- NAV-03: Tab Orar inline funcțional, Tab Sportivi listă + Adaugă Sportivi. ✓
- Zero regresii: Gestionează, dropdown ..., AdaugaSportiviModal, OrarEditorModal montat — toate funcționale.
</success_criteria>

<output>
Create `.planning/phases/02-navigare-grupe-drill-down/02-02-SUMMARY.md` when done
</output>
