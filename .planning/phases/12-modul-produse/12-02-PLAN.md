---
phase: 12-modul-produse
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - services/produseService.ts
  - components/Produse/index.tsx
  - components/Produse/ProdusFormModal.tsx
  - components/LazyComponents.tsx
  - components/AppRouter.tsx
  - components/menuConfig.ts
autonomous: true
requirements: [PRD-01, PRD-02]

must_haves:
  truths:
    - "Există services/produseService.ts cu funcțiile fetchProduse, createProdus, updateProdus, deleteProdus, createVarianta, updateVarianta, deleteVarianta, fetchCategorii"
    - "Există components/Produse/index.tsx exportând ProduseManagement cu tab-ul Catalog funcțional"
    - "Există components/Produse/ProdusFormModal.tsx pentru adăugare/editare produs cu variante inline"
    - "Lazy.ProduseManagement înregistrat în LazyComponents.tsx"
    - "AppRouter.tsx are case 'produse' dispatch către Lazy.ProduseManagement"
    - "menuConfig.ts are secțiunea 'Echipamente' în adminClubMenu cu view 'produse'"
  artifacts:
    - path: "services/produseService.ts"
      provides: "CRUD complet pentru produse + variante + categorii"
    - path: "components/Produse/index.tsx"
      provides: "Componentă principală ProduseManagement cu tab Catalog"
    - path: "components/Produse/ProdusFormModal.tsx"
      provides: "Modal creare/editare produs cu variante inline"
  key_links:
    - from: "components/Produse/index.tsx"
      to: "services/produseService.ts"
      via: "import fetchProduse, createProdus, updateProdus, deleteProdus"
      pattern: "produseService"
    - from: "components/AppRouter.tsx case 'produse'"
      to: "Lazy.ProduseManagement"
      via: "renderProtected(..., isAtLeastClubAdmin)"
      pattern: "'produse'"
---

<objective>
Implementează service-ul de date și UI-ul de catalog admin pentru Modulul Produse. La finalul acestui plan, ADMIN_CLUB poate naviga la "Echipamente" în sidebar, vede lista produselor din catalog, adaugă/editează/șterge produse cu variante (mărime, culoare, prețuri).

Purpose: Prima funcționalitate vizibilă și utilizabilă — adminul poate construi catalogul de produse.
Output: 6 fișiere (service + 2 componente Produse + LazyComponents + AppRouter + menuConfig).
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-modul-produse/12-CONTEXT.md
</context>

<artifacts_this_phase_produces>

**În `services/produseService.ts`:**
- `fetchCategorii(): Promise<ProdusCategorieDB[]>`
- `fetchProduse(clubId: string): Promise<Produs[]>` — cu join variante
- `createProdus(data: Omit<ProdusDB, 'id'|'created_at'|'updated_at'>): Promise<ProdusDB>`
- `updateProdus(id: string, data: Partial<ProdusDB>): Promise<ProdusDB>`
- `deleteProdus(id: string): Promise<void>`
- `createVarianta(data: Omit<ProdusVariantaDB, 'id'|'created_at'|'updated_at'>): Promise<ProdusVariantaDB>`
- `updateVarianta(id: string, data: Partial<ProdusVariantaDB>): Promise<ProdusVariantaDB>`
- `deleteVarianta(id: string): Promise<void>`

**În `components/Produse/index.tsx`:**
- Componentă `ProduseManagement` cu props: `{ currentUser: User; permissions: Permissions; onBack: () => void }`
- Tab "Catalog" (default): tabel produse cu coloana denumire, categorie, nr. variante, stoc total, acțiuni Edit/Șterge
- Badge roșu "Stoc redus" lângă denumire când oricare variantă are `stoc_curent < stoc_minim`
- Buton "Produs Nou" → ProdusFormModal
- Filtrare locală după categorie (select) + text (search input)
- Stare loading/error cu componente Alert din ui.tsx

**În `components/Produse/ProdusFormModal.tsx`:**
- Modal creare/editare produs
- Câmpuri: denumire (required), categorie (select din fetchCategorii), descriere (textarea)
- Secțiune "Variante" inline în modal: tabel editabil cu coloane culoare, mărime, pret_intrare, pret_vanzare, stoc_minim
- Buton "Adaugă variantă" adaugă rând nou în tabel
- Buton X șterge varianta (soft — marchează pentru ștergere)
- La salvare: creare/update produs THEN creare/update/delete variante în paralel

</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: produseService.ts — CRUD produse, variante, categorii</name>
  <files>services/produseService.ts</files>
  <read_first>
    - services/ (listează pentru a vedea un serviciu existent ca referință de pattern)
    - services/sportiviService.ts sau services/grupeService.ts (pattern fetch + create + update + delete cu supabaseClient)
    - types.ts (ultimele linii adăugate în planul 12-01: ProdusCategorieDB, ProdusDB, ProdusVariantaDB, Produs)
  </read_first>
  <action>
    Creează `services/produseService.ts`. Pattern: același ca serviciile existente — import supabaseClient, funcții async care returnează `{ data, error }` sau throw cu context.

    ```typescript
    import { supabaseClient } from '../supabaseClient';
    import type {
      ProdusCategorieDB, ProdusDB, ProdusVariantaDB, Produs
    } from '../types';

    export async function fetchCategorii(): Promise<ProdusCategorieDB[]> {
      const { data, error } = await supabaseClient
        .from('produse_categorii')
        .select('*')
        .eq('activa', true)
        .order('ordine');
      if (error) throw new Error(`fetchCategorii: ${error.message}`);
      return data ?? [];
    }

    export async function fetchProduse(): Promise<Produs[]> {
      const { data, error } = await supabaseClient
        .from('produse')
        .select(`
          *,
          variante:produse_variante(*),
          categorie:produse_categorii(*)
        `)
        .eq('activ', true)
        .order('denumire');
      if (error) throw new Error(`fetchProduse: ${error.message}`);
      return (data ?? []) as Produs[];
    }

    export async function createProdus(
      input: Pick<ProdusDB, 'club_id' | 'categorie_id' | 'denumire' | 'descriere'>
    ): Promise<ProdusDB> {
      const { data, error } = await supabaseClient
        .from('produse')
        .insert(input)
        .select()
        .single();
      if (error) throw new Error(`createProdus: ${error.message}`);
      return data;
    }

    export async function updateProdus(
      id: string,
      input: Partial<Pick<ProdusDB, 'categorie_id' | 'denumire' | 'descriere' | 'activ'>>
    ): Promise<ProdusDB> {
      const { data, error } = await supabaseClient
        .from('produse')
        .update({ ...input, updated_at: new Date().toISOString() })
        .eq('id', id)
        .select()
        .single();
      if (error) throw new Error(`updateProdus: ${error.message}`);
      return data;
    }

    export async function deleteProdus(id: string): Promise<void> {
      const { error } = await supabaseClient
        .from('produse')
        .update({ activ: false })
        .eq('id', id);
      if (error) throw new Error(`deleteProdus: ${error.message}`);
    }

    export async function createVarianta(
      input: Pick<ProdusVariantaDB, 'produs_id' | 'culoare' | 'marime' | 'pret_intrare' | 'pret_vanzare' | 'stoc_curent' | 'stoc_minim'>
    ): Promise<ProdusVariantaDB> {
      const { data, error } = await supabaseClient
        .from('produse_variante')
        .insert(input)
        .select()
        .single();
      if (error) throw new Error(`createVarianta: ${error.message}`);
      return data;
    }

    export async function updateVarianta(
      id: string,
      input: Partial<Pick<ProdusVariantaDB, 'culoare' | 'marime' | 'pret_intrare' | 'pret_vanzare' | 'stoc_minim' | 'activa'>>
    ): Promise<ProdusVariantaDB> {
      const { data, error } = await supabaseClient
        .from('produse_variante')
        .update({ ...input, updated_at: new Date().toISOString() })
        .eq('id', id)
        .select()
        .single();
      if (error) throw new Error(`updateVarianta: ${error.message}`);
      return data;
    }

    export async function deleteVarianta(id: string): Promise<void> {
      const { error } = await supabaseClient
        .from('produse_variante')
        .update({ activa: false })
        .eq('id', id);
      if (error) throw new Error(`deleteVarianta: ${error.message}`);
    }
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "produseService" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -c "export async function" services/produseService.ts</automated>
  </verify>
  <acceptance_criteria>
    - `services/produseService.ts` există cu 8 funcții exportate
    - Funcțiile fac soft-delete (activ=false / activa=false) NU hard delete
    - `fetchProduse` include join variante + categorie
    - `npx tsc --noEmit` fără erori noi în produseService.ts
  </acceptance_criteria>
  <done>services/produseService.ts creat cu 8 funcții CRUD, soft-delete, join variante+categorie.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ProdusFormModal.tsx — modal creare/editare produs cu variante inline</name>
  <files>components/Produse/ProdusFormModal.tsx</files>
  <read_first>
    - components/Produse/ (directorul nou — creează dacă nu există)
    - components/ui.tsx (Modal, Button, Input, Select, Card, Badge — interfețe exacte)
    - components/Competitii/index.tsx sau components/Grupe/GrupaFormModal.tsx (pattern modal cu tabel editabil inline)
    - types.ts (ProdusDB, ProdusVariantaDB, ProdusCategorieDB, Produs)
  </read_first>
  <action>
    Creează `components/Produse/ProdusFormModal.tsx`. Structura componentei:

    Props:
    ```typescript
    interface ProdusFormModalProps {
      produs?: Produs | null;          // null = creare nouă
      categorii: ProdusCategorieDB[];
      clubId: string;
      onSave: (produs: Produs) => void;
      onClose: () => void;
    }
    ```

    State intern:
    - `denumire: string`
    - `categorieId: string`
    - `descriere: string`
    - `variante: VariantaForm[]` unde `VariantaForm = { id?: string; culoare: string; marime: string; pret_intrare: string; pret_vanzare: string; stoc_minim: string; _toDelete?: boolean }`
    - `saving: boolean`
    - `error: string | null`

    UI (în Modal cu titlu "Produs nou" / "Editare {produs.denumire}"):

    **Secțiunea Produs:**
    - Input "Denumire" (required) → `denumire`
    - Select "Categorie" cu opțiunile din `categorii` → `categorieId`
    - Textarea "Descriere" (opțional, 3 rânduri) → `descriere`

    **Secțiunea Variante** (sub separator):
    - Titlu "Variante" cu buton "Adaugă variantă" (+ icon)
    - Tabel cu coloane: Culoare | Mărime | Preț intrare (RON) | Preț vânzare (RON) | Stoc minim | Acțiuni
    - Fiecare rând: câmpuri input inline (text pentru culoare, select pentru mărime, number pentru prețuri + stoc_minim)
    - Mărimi predefinite în select: XS, S, M, L, XL, XXL, Unic (+ posibilitate text liber prin opțiunea "Alt...")
    - Buton X pe fiecare rând → marchează `_toDelete = true`, ascunde rândul din UI
    - Rândurile marcate `_toDelete` cu id existent se șterg la salvare

    **Footer Modal:**
    - Buton "Anulează" → onClose
    - Buton "Salvează" cu saving state → logica de save

    **Logica handleSave:**
    ```
    1. Validează: denumire non-empty, categorieId selectat, cel puțin 0 variante valide
    2. setSaving(true)
    3. Dacă produs existent: updateProdus(produs.id, { denumire, categorie_id, descriere })
       Altfel: createProdus({ club_id: clubId, categorie_id, denumire, descriere })
    4. Pentru variante:
       - Noile (_toDelete=false, fără id): createVarianta({ produs_id, culoare, marime, pret_intrare: +v.pret_intrare, ... })
       - Existente (_toDelete=false, cu id): updateVarianta(v.id, { culoare, marime, pret_intrare: +v.pret_intrare, ... })
       - De șters (_toDelete=true, cu id): deleteVarianta(v.id)
    5. Fetch produs actualizat cu variante → onSave(produsActualizat)
    ```
    Folosește `Promise.all` pentru operațiile paralele pe variante.
    Afișează erori prin `setError(err.message)` + `<Alert variant="error">` sub header modal.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "ProdusFormModal" ; echo "exit:$?"</automated>
    <automated>test -f "C:/Users/lungu/portal-phihau/components/Produse/ProdusFormModal.tsx" && echo "exists"</automated>
  </verify>
  <acceptance_criteria>
    - `components/Produse/ProdusFormModal.tsx` există
    - Exportă default component `ProdusFormModal`
    - Conține tabel editabil variante cu input-uri inline
    - handleSave face createProdus sau updateProdus + operații variante
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>ProdusFormModal.tsx creat cu form produs + tabel variante inline editabil + logica save.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: components/Produse/index.tsx — ProduseManagement cu tab Catalog</name>
  <files>components/Produse/index.tsx</files>
  <read_first>
    - components/Produse/ProdusFormModal.tsx (creat în Task 2)
    - services/produseService.ts (creat în Task 1)
    - components/ui.tsx (Card, Button, Badge, Input, Select, LoadingSpinner, EmptyState sau echivalent)
    - components/Plati/GestiuneFacturi.tsx sau components/Grupe/index.tsx (pattern tab-uri + tabel + modal CRUD)
    - types.ts (User, Permissions, Produs, ProdusCategorieDB)
  </read_first>
  <action>
    Creează `components/Produse/index.tsx`. Exportă named `ProduseManagement`.

    Props:
    ```typescript
    interface ProduseManagementProps {
      currentUser: User;
      permissions: Permissions;
      onBack: () => void;
    }
    ```

    State:
    - `produse: Produs[]`
    - `categorii: ProdusCategorieDB[]`
    - `loading: boolean`
    - `error: string | null`
    - `activeTab: 'catalog' | 'intrari' | 'vanzari' | 'raport'` (tabs 'intrari', 'vanzari', 'raport' = placeholder "În curând" pentru planurile 12-03..12-05)
    - `showForm: boolean`
    - `editProdus: Produs | null`
    - `filterCategorie: string` ('' = toate)
    - `filterText: string`

    **useEffect la mount:**
    Fetch paralel `fetchCategorii()` + `fetchProduse()` → setProduse + setCategorii + setLoading(false). Error → setError.

    **produseFiltrate useMemo:**
    - Filtrare text: `p.denumire.toLowerCase().includes(filterText.toLowerCase())`
    - Filtrare categorie: `filterCategorie === '' || p.categorie_id === filterCategorie`

    **Helper hasStocRedus(produs):**
    Returnează `true` dacă oricare variantă activă are `stoc_curent < stoc_minim` (și `stoc_minim > 0`)

    **UI principal:**

    Header: titlu "Echipamente" + onBack arrow + buton "Produs Nou" (vizibil doar admin).

    Tab-uri: Catalog | Intrări Marfă | Vânzări | Raport
    (Tab-urile 'intrari', 'vanzari', 'raport' afișează `<Card><div className="py-12 text-center text-slate-400">Modul în curând disponibil</div></Card>` — placeholder pentru planurile următoare)

    **Tab Catalog:**

    Bara de filtrare (deasupra tabelului):
    ```
    <div className="flex flex-col sm:flex-row gap-3 mb-4">
      <Input placeholder="Caută produs..." value={filterText} onChange={e => setFilterText(e.target.value)} className="flex-1" />
      <Select value={filterCategorie} onChange={e => setFilterCategorie(e.target.value)}>
        <option value="">Toate categoriile</option>
        {categorii.map(c => <option key={c.id} value={c.id}>{c.denumire}</option>)}
      </Select>
    </div>
    ```

    Tabel desktop (`hidden md:table w-full`):
    - Header: Produs | Categorie | Variante | Stoc total | Preț min→max | Acțiuni
    - Body: un rând per produs filtrat
      - Produs: `{p.denumire}` + dacă `hasStocRedus(p)` → `<Badge variant="danger" size="sm">Stoc redus</Badge>` inline
      - Categorie: `p.categorie?.denumire ?? '—'`
      - Variante: număr variante active (`p.variante.filter(v => v.activa).length`)
      - Stoc total: `p.variante.filter(v => v.activa).reduce((s, v) => s + v.stoc_curent, 0)`
      - Preț: `{minPretVanzare} – {maxPretVanzare} RON` (sau prețul unic dacă o singură variantă)
      - Acțiuni: buton "Editează" → setEditProdus(p) + setShowForm(true) | buton "Șterge" cu confirmare window.confirm

    Carduri mobile (`md:hidden space-y-2`):
    - Card per produs: denumire bold + badge stoc redus | categorie text-sm text-slate-400 | nr variante + stoc total

    Empty state când `produseFiltrate.length === 0`: text "Niciun produs în catalog" cu buton "Adaugă primul produs".

    **Modal:** `{showForm && <ProdusFormModal produs={editProdus} categorii={categorii} clubId={currentUser.club_id /* sau activeRoleContext.club_id */} onSave={handleSave} onClose={() => { setShowForm(false); setEditProdus(null); }} />}`

    **handleSave:** actualizează local `produse` state (replace sau push), setShowForm(false).
    **handleDelete:** deleteProdus(id) → remove din state local.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "Produse/index" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -c "ProduseManagement\|activeTab\|hasStocRedus\|ProdusFormModal" components/Produse/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `components/Produse/index.tsx` exportă named `ProduseManagement`
    - Conține 4 tab-uri: catalog, intrari, vanzari, raport
    - Tab catalog are filtrare text + categorie
    - `hasStocRedus` afișează badge roșu
    - Tab-urile intrari/vanzari/raport au placeholder "în curând"
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>ProduseManagement creat cu tab Catalog funcțional, filtrare, badge stoc redus, CRUD via modal.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Integrare — LazyComponents + AppRouter + menuConfig</name>
  <files>components/LazyComponents.tsx, components/AppRouter.tsx, components/menuConfig.ts</files>
  <read_first>
    - components/LazyComponents.tsx (ultimele 5 linii — pattern de înregistrare lazy)
    - components/AppRouter.tsx (liniile 210-260 — pattern case pentru ADMIN_CLUB views)
    - components/menuConfig.ts (liniile 155-168 — finalul adminClubMenu)
  </read_first>
  <action>
    **LazyComponents.tsx:** Adaugă la final:
    ```typescript
    export const ProduseManagement = lazy(() => import('./Produse').then(m => ({ default: m.ProduseManagement })));
    ```

    **AppRouter.tsx:** Adaugă în switch, după ultimul case pentru admin club (lângă celelalte `isAtLeastClubAdmin`):
    ```typescript
    case 'produse':
      return renderProtected(
        <Lazy.ProduseManagement
          currentUser={currentUser!}
          permissions={permissions}
          onBack={handleBackToDashboard}
        />,
        isAtLeastClubAdmin
      );
    ```

    **menuConfig.ts:** Adaugă secțiune nouă în `adminClubMenu` DUPĂ secțiunea "Activități Naționale" și ÎNAINTE de "Financiar & Plăți":
    ```typescript
    {
      label: 'Echipamente', icon: ShoppingBagIcon,
      submenu: [
        { label: 'Catalog Produse', view: 'produse' },
        { label: 'Vânzări', view: 'vanzari-produse' },
      ]
    },
    ```

    Importul `ShoppingBagIcon` din `lucide-react` (verifică dacă există sau folosește `PackageIcon` / `ShoppingCartIcon` — oricare există în lucide-react@0.400).
    Verifică importurile existente din lucide-react în menuConfig.ts și alege un icon disponibil din lista curentă.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -iE "LazyComponents|AppRouter|menuConfig" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -c "ProduseManagement\|'produse'" components/LazyComponents.tsx components/AppRouter.tsx components/menuConfig.ts</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `LazyComponents.tsx` conține `export const ProduseManagement = lazy(...)`
    - `AppRouter.tsx` conține `case 'produse':` cu `renderProtected(..., isAtLeastClubAdmin)`
    - `menuConfig.ts` conține `{ label: 'Catalog Produse', view: 'produse' }` în adminClubMenu
    - `npx tsc --noEmit` fără erori noi
    - `npx vite build` trece
  </acceptance_criteria>
  <done>ProduseManagement înregistrat lazy, view 'produse' dispatchat în AppRouter, meniu Echipamente în adminClubMenu.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero erori noi
2. `npx vite build` — build complet
3. Manual: login ca ADMIN_CLUB → sidebar afișează "Echipamente" cu "Catalog Produse" și "Vânzări"
4. Manual: click "Catalog Produse" → pagina ProduseManagement se încarcă (loading → empty state sau produse)
5. Manual: click "Produs Nou" → ProdusFormModal se deschide cu form + tabel variante
6. Manual: completează form (denumire + categorie + 2 variante cu prețuri) → Salvează → produsul apare în catalog
7. Manual: editează produsul → prețuri variante modificate → Salvează → se actualizează în catalog
8. Manual: șterge produsul → dispare din catalog (soft-delete)
9. Manual: adaugă produs cu `stoc_curent: 0, stoc_minim: 5` → badge "Stoc redus" apare
</verification>

<success_criteria>
- [ ] ADMIN_CLUB vede "Echipamente > Catalog Produse" în sidebar (PRD-01)
- [ ] Poate adăuga produs cu denumire + categorie + variante (culoare/mărime/prețuri) (PRD-01, PRD-02)
- [ ] Badge "Stoc redus" apare când stoc_curent < stoc_minim (PRD-02)
- [ ] 4 tab-uri prezente în ProduseManagement (catalog funcțional + 3 placeholder) 
- [ ] Build + TypeScript trec
</success_criteria>

<output>
Create `.planning/phases/12-modul-produse/12-02-SUMMARY.md` when done
</output>
