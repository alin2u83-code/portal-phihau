---
phase: 12-modul-produse
plan: 05
type: execute
wave: 5
depends_on: [12-04]
files_modified:
  - components/Produse/RaportProduse.tsx
  - components/Produse/index.tsx
autonomous: true
requirements: [PRD-06]

must_haves:
  truths:
    - "Există components/Produse/RaportProduse.tsx cu raport per produs: cantitate vândută, venit, cost, profit brut"
    - "Tab 'raport' din ProduseManagement afișează RaportProduse cu filtrare perioadă + export Excel + export PDF"
  artifacts:
    - path: "components/Produse/RaportProduse.tsx"
      provides: "Componentă raport vânzări cu calcul profit + export"
    - path: "components/Produse/index.tsx"
      provides: "Tab 'raport' activat"
---

<objective>
Implementează raportul de vânzări produse cu profit brut și export Excel/PDF. ADMIN_CLUB poate filtra pe perioadă și vedea per produs: cantitate vândută, venit total, cost total (din snapshot-uri), profit brut, margin %.

Purpose: Vizibilitate financiară completă pe modulul Produse — rentabilitate per produs.
Output: RaportProduse.tsx + activare tab 'raport'.
</objective>

<context>
@.planning/phases/12-modul-produse/12-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RaportProduse.tsx — raport vânzări cu profit + export Excel + PDF</name>
  <files>components/Produse/RaportProduse.tsx</files>
  <read_first>
    - utils/exportFinanciar.ts (pattern export CSV/PDF — replică structura pentru produse)
    - components/Plati/PeriodFilterBar.tsx (props interface: startDate, endDate, onChange)
    - components/ui.tsx (Card, Button, Select — interfețe exacte)
    - types.ts (ProdusVanzare, ProdusVanzareDetaliuDB)
  </read_first>
  <action>
    Creează `components/Produse/RaportProduse.tsx`.

    Props:
    ```typescript
    interface RaportProduseProps {
      vanzari: ProdusVanzare[];
      clubNume?: string;
    }
    ```

    State:
    - `startDate: string`, `endDate: string` (filtrare perioadă)
    - `groupBy: 'produs' | 'categorie'` (select grupare, default 'produs')

    **RandRaport tip** (local, nu în types.ts):
    ```typescript
    type RandRaport = {
      key: string;
      denumire: string;
      cantitate: number;
      venit: number;      // sum(pret_vanzare_snapshot * cantitate)
      cost: number;       // sum(pret_intrare_snapshot * cantitate)
      profit: number;     // venit - cost
      margin: number;     // profit / venit * 100 (sau 0 dacă venit=0)
    };
    ```

    **randuri useMemo** din `vanzari`:
    1. Filtrare perioadă: `vanzare.data_vanzare >= startDate && <= endDate` (cu guard empty)
    2. Aplatizare detalii din toate vânzările filtrate
    3. Grupare pe `denumire_snapshot` (group by produs) sau prefix categorie (group by categorie — ia primele cuvinte din denumire_snapshot dacă nu avem categorie direct pe detaliu)
    4. Per grup: sum cantitate, sum venit, sum cost, calcul profit + margin
    5. Sortare descrescător după `venit`

    **Sumarul total** (sub filtre):
    - Total vânzări (count vanzari filtrate)
    - Total cantitate
    - Total venit
    - Total cost
    - Total profit
    - Margin mediu

    **UI:**

    Bara sus: PeriodFilterBar + Select grupare + butoane Export Excel | Export PDF

    **Bar total** (similar cu RaportFinanciar):
    ```jsx
    <div className="flex flex-wrap gap-4 bg-[var(--t-surface-2)] border border-[var(--t-border)] rounded-xl px-4 py-3">
      <Stat label="Vânzări" value={vanzariFiltrate.length.toString()} />
      <Stat label="Venit total" value={`${totalVenit.toFixed(2)} RON`} className="text-emerald-400" />
      <Stat label="Cost total" value={`${totalCost.toFixed(2)} RON`} className="text-amber-400" />
      <Stat label="Profit brut" value={`${totalProfit.toFixed(2)} RON`} className={totalProfit >= 0 ? 'text-emerald-400' : 'text-red-400'} />
    </div>
    ```
    (Stat = div inline cu label text-xs text-slate-400 + value font-bold)

    **Tabel desktop**:
    Header: Produs/Categorie | Cantitate | Venit (RON) | Cost (RON) | Profit (RON) | Margin %
    Body: un rând per RandRaport
    - Profit colorat: `text-emerald-400` pozitiv, `text-red-400` negativ
    - Margin %: `${r.margin.toFixed(1)}%` cu culoare similară profitului
    Footer row: Totale (bold) — suma tuturor coloanelor

    **Carduri mobile**: `md:hidden` — card per RandRaport cu denumire, cantitate, profit bold

    **Empty state**: "Nicio vânzare în perioada selectată"

    **Export Excel (XLSX):**
    ```typescript
    const handleExportExcel = () => {
      const { utils, writeFile } = require('xlsx');  // dynamic import pattern
      // Sau: import('xlsx').then(({ utils, writeFile }) => { ... })
      const ws = utils.aoa_to_sheet([
        ['Produs', 'Cantitate', 'Venit (RON)', 'Cost (RON)', 'Profit (RON)', 'Margin (%)'],
        ...randuri.map(r => [r.denumire, r.cantitate, r.venit.toFixed(2), r.cost.toFixed(2), r.profit.toFixed(2), r.margin.toFixed(1)]),
        ['TOTAL', totalCantitate, totalVenit.toFixed(2), totalCost.toFixed(2), totalProfit.toFixed(2), totalMargin.toFixed(1)],
      ]);
      const wb = utils.book_new();
      utils.book_append_sheet(wb, ws, 'Raport Produse');
      writeFile(wb, `raport-produse-${startDate || 'tot'}-${endDate || 'tot'}.xlsx`);
    };
    ```
    Folosește `import('xlsx')` dinamic (xlsx@0.18.5 deja în package.json — Pattern din codebase existent).

    **Export PDF (jsPDF):**
    Similar cu exportIncasariPDF din exportFinanciar.ts — dynamic import jsPDF + autotable, orientare landscape, antet `[clubNume] — Raport Produse`, tabel cu 6 coloane, footer cu totale.
    `headStyles.fillColor: [16, 185, 129]` (emerald — verde = profit).
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "RaportProduse" ; echo "exit:$?"</automated>
    <automated>test -f "C:/Users/lungu/portal-phihau/components/Produse/RaportProduse.tsx" && echo "exists"</automated>
  </verify>
  <acceptance_criteria>
    - `components/Produse/RaportProduse.tsx` există cu export default `RaportProduse`
    - Conține `useMemo` cu randuri calculate din detalii vânzări (venit/cost/profit/margin)
    - Export Excel (import dinamic xlsx) și Export PDF (import dinamic jsPDF) funcționale
    - Profit pozitiv = emerald, negativ = red
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>RaportProduse creat cu calcul profit per produs, filtrare perioadă, export Excel + PDF.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ProduseManagement — activare tab 'raport'</name>
  <files>components/Produse/index.tsx</files>
  <read_first>
    - components/Produse/index.tsx (starea curentă — placeholder tab raport)
    - components/Produse/RaportProduse.tsx (creat în Task 1 — props: vanzari, clubNume)
  </read_first>
  <action>
    Modifică `components/Produse/index.tsx`:

    1. Importă `RaportProduse` din `'./RaportProduse'`
    2. Importă `useData` dacă nu e deja importat, extrage `clubs` și `activeRoleContext` pentru `clubNume`
    3. Calculează: `const clubNume = clubs?.find(c => c.id === activeRoleContext?.club_id)?.nume ?? 'Club QwanKiDo'`

    4. Înlocuiește placeholder tab 'raport' cu:
    ```jsx
    {activeTab === 'raport' && (
      <RaportProduse vanzari={vanzari} clubNume={clubNume} />
    )}
    ```

    Simplu — RaportProduse primește `vanzari` deja în state și le filtrează intern pe perioadă.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "Produse" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx vite build 2>&1 | tail -5</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -c "RaportProduse\|activeTab === 'raport'" components/Produse/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tab 'raport' nu mai e placeholder — afișează RaportProduse
    - `clubNume` pasat corect din clubs + activeRoleContext
    - `npx tsc --noEmit` + `npx vite build` trec
  </acceptance_criteria>
  <done>Tab Raport activat — RaportProduse integrat cu vanzari din state + clubNume.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` + `npx vite build` — zero erori
2. Manual: tab "Raport" → tabel cu produse, cantitate vândută, venit, cost, profit (culori verde/roșu)
3. Manual: aplică filtru perioadă → datele se actualizează live
4. Manual: Export Excel → fișier .xlsx descărcat cu 6 coloane + rând TOTAL
5. Manual: Export PDF → PDF landscape cu antet club + tabel formatat emerald
6. Manual: vânzare la zero profit (pret_vanzare = pret_intrare) → profit 0, margin 0%
7. Manual: vânzare cu pierdere (pret_vanzare < pret_intrare) → profit roșu
</verification>

<success_criteria>
- [ ] Tab "Raport" funcțional cu tabel per produs: cantitate, venit, cost, profit brut, margin % (PRD-06)
- [ ] Filtrare perioadă live (PRD-06)
- [ ] Export Excel descarcă .xlsx cu toate datele (PRD-06)
- [ ] Export PDF descarcă PDF landscape formatat (PRD-06)
- [ ] Profit pozitiv = emerald, negativ = roșu (UX claritate)
- [ ] `npx tsc --noEmit` + `npx vite build` trec
</success_criteria>

<output>
Create `.planning/phases/12-modul-produse/12-05-SUMMARY.md` when done

**Phase 12 COMPLETE** când toate 5 planuri sunt executate și verificate:
- PRD-01 ✓ Catalog produse per club cu variante
- PRD-02 ✓ Variante (culoare/mărime) cu prețuri + badge stoc redus
- PRD-03 ✓ Intrări marfă cu furnizor + factură + actualizare stoc
- PRD-04 ✓ Vânzări integrate → Plata generată + stoc scăzut
- PRD-05 ✓ Sportiv vede catalog + istoricul propriu (fără pret_intrare)
- PRD-06 ✓ Raport vânzări cu profit brut + export Excel + PDF
</output>
