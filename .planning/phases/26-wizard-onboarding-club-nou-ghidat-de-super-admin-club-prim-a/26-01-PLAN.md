---
phase: 26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/parola.ts
  - utils/parola.test.ts
  - api/creare-cont.ts
  - hooks/useRoleAssignment.ts
autonomous: true
requirements: [D-05, D-07]
user_setup: []

must_haves:
  truths:
    - "Parola temporară pentru noul admin este generată criptografic aleator, diferită la fiecare apel, imposibil de ghicit din numele adminului"
    - "Un apel POST /api/creare-cont fără token de sesiune valid este respins cu 401 și nu creează niciun cont"
    - "Un apelant autentificat nu poate crea un cont cu rol mai privilegiat decât rolul propriu (ex. ADMIN_CLUB nu poate crea SUPER_ADMIN_FEDERATIE)"
    - "Un apelant care nu e SUPER_ADMIN_FEDERATIE nu poate crea un cont într-un club în care nu are rol"
    - "Contul nou creat prin /api/creare-cont are trebuie_schimbata_parola = true, deci la primul login intră în MandatoryPasswordChange"
    - "Fluxurile existente de creare cont (UserManagement -> Adaugă Membru Staff, Sportivi -> Creează Cont) continuă să funcționeze"
  artifacts:
    - path: "utils/parola.ts"
      provides: "genereazaParolaTemporara() — generator parolă temporară criptografic aleatoare"
      exports: ["genereazaParolaTemporara"]
      min_lines: 30
    - path: "utils/parola.test.ts"
      provides: "Test colocat pentru genereazaParolaTemporara (pattern utils/luniLipsa.test.ts)"
      exports: ["ruleazaTeste"]
      min_lines: 40
    - path: "api/creare-cont.ts"
      provides: "Endpoint creare cont cu gardă de autentificare, gardă anti-escaladare rol, scoping club, rate limit, trebuie_schimbata_parola"
      contains: "auth.getUser"
    - path: "hooks/useRoleAssignment.ts"
      provides: "createAccountAndAssignRole trimite Authorization: Bearer <access_token> și refuză club_id lipsă"
      contains: "Authorization"
  key_links:
    - from: "hooks/useRoleAssignment.ts"
      to: "api/creare-cont.ts"
      via: "fetch cu header Authorization Bearer din supabase.auth.getSession()"
      pattern: "Authorization.*Bearer.*access_token"
    - from: "api/creare-cont.ts"
      to: "utilizator_roluri_multicont"
      via: "citire roluri apelant pentru gardă de privilegii"
      pattern: "from\\('utilizator_roluri_multicont'\\)"
    - from: "api/creare-cont.ts"
      to: "sportivi.trebuie_schimbata_parola"
      via: "update după RPC refactor_create_user_account"
      pattern: "trebuie_schimbata_parola"
---

<objective>
Pregătește fundația sigură pentru crearea primului admin al unui club nou: un generator de parolă temporară criptografic aleatoare (D-05) și închiderea găurii de securitate din `api/creare-cont.ts`, endpoint-ul pe care Faza 26 îl reutilizează pentru a emite conturi cu rol `ADMIN_CLUB`.

Purpose: Faza 26 rutează crearea unui cont privilegiat (`ADMIN_CLUB`) prin `api/creare-cont.ts`. Astăzi acel endpoint rulează cu `SUPABASE_SERVICE_ROLE_KEY` și **nu verifică deloc apelantul** — orice client anonim poate emite un cont cu `roles: ['SUPER_ADMIN_FEDERATIE']`. Nu putem construi fluxul de onboarding peste această suprafață fără gardă. În plus, `trebuie_schimbata_parola` cerut de D-05 nu este setat de fapt astăzi (câmpul e pus în `newSportivProfile` în `UserManagement.tsx` dar e filtrat de whitelist-ul `userData` din `useRoleAssignment.ts` și nu ajunge niciodată în RPC).

Output: `utils/parola.ts` + test colocat, `api/creare-cont.ts` cu 5 gărzi server-side, `hooks/useRoleAssignment.ts` care trimite token de sesiune.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-CONTEXT.md
@.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Simboluri **noi** create de acest plan (nu există astăzi în cod — nu le trata ca drift):

| Simbol | Fișier | Tip |
|---|---|---|
| `genereazaParolaTemporara(lungime?: number): string` | `utils/parola.ts` (fișier nou) | funcție exportată |
| `LUNGIME_MINIMA_PAROLA` | `utils/parola.ts` (fișier nou) | constantă exportată |
| `ruleazaTeste()` | `utils/parola.test.ts` (fișier nou) | funcție exportată |
| `ROLE_WEIGHTS` | `api/creare-cont.ts` | constantă locală nouă |

Simboluri noi create de planul 26-02 (pentru referință, nu se implementează aici): `ClubFormModalProps.adminError`, `ClubFormModalProps.onRetryAdmin`, `ClubFormModalProps.retryLoading`, `ClubFormModalProps.clubCreatNume`, `CluburiManagementProps.allRoles`, `creeazaAdminClub`, `handleRetryAdmin`, `pendingAdmin`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generator parolă temporară criptografic aleatoare (D-05)</name>
  <files>utils/parola.ts, utils/parola.test.ts</files>
  <read_first>
    - `utils/luniLipsa.test.ts` — pattern-ul de test colocat folosit în acest proiect (funcție `assert` locală, `console.log` PASS/FAIL, export `ruleazaTeste`, rulat cu `node --import tsx`); proiectul NU are vitest/jest
    - `utils/checkLeakedPassword.ts` — singurul utilitar existent legat de parole, ca să confirmi că nu duplici funcționalitate
    - `components/UserProfile/CreateAccountModal.tsx` linia 31 — pattern-ul actual de parolă implicită (`${nume}.1234!`), deterministic și de ghicit; acesta este exact anti-pattern-ul pe care îl înlocuim
    - `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-PATTERNS.md` secțiunea "Password generation" — confirmă că nu există analog de copiat
  </read_first>
  <behavior>
    - `genereazaParolaTemporara()` returnează un string de exact 16 caractere (lungime implicită)
    - `genereazaParolaTemporara(24)` returnează un string de exact 24 caractere
    - `genereazaParolaTemporara(8)` aruncă `Error` (sub `LUNGIME_MINIMA_PAROLA` = 12)
    - 200 de apeluri consecutive produc 200 de valori distincte
    - Fiecare rezultat conține cel puțin o literă mică, cel puțin o literă mare, cel puțin o cifră și cel puțin un simbol
    - Niciun rezultat nu conține caractere ambigue: `I`, `l`, `O`, `o`, `0`, `1`
    - Poziția claselor de caractere nu e fixă: pe 200 de generări, primul caracter nu aparține mereu aceleiași clase (dovada că shuffle-ul rulează, nu doar concatenare clasă-cu-clasă)
  </behavior>
  <action>
Creează `utils/parola.ts` cu:

1. Constanta exportată `LUNGIME_MINIMA_PAROLA = 12`.
2. Patru alfabete locale, fără caractere ambigue: `MAJUSCULE = 'ABCDEFGHJKLMNPQRSTUVWXYZ'` (fără `I` și `O`), `MINUSCULE = 'abcdefghijkmnpqrstuvwxyz'` (fără `l` și `o`), `CIFRE = '23456789'` (fără `0` și `1`), `SIMBOLURI = '!@#$%^&*-_=+?'`.
3. O funcție internă `indexAleator(limita: number): number` care folosește `crypto.getRandomValues(new Uint32Array(1))` cu **rejection sampling** ca să elimine modulo bias: calculează `prag = Math.floor(0xFFFFFFFF / limita) * limita` și re-eșantionează cât timp valoarea generată este `>= prag`, apoi returnează `valoare % limita`. NU folosi `Math.random()` nicăieri în fișier.
4. Funcția exportată `genereazaParolaTemporara(lungime: number = 16): string` care: aruncă `new Error(...)` cu mesaj în română dacă `lungime < LUNGIME_MINIMA_PAROLA`; garantează câte un caracter din fiecare din cele patru alfabete; completează restul până la `lungime` din alfabetul concatenat al tuturor celor patru; amestecă rezultatul cu Fisher-Yates folosind `indexAleator` (nu `Array.prototype.sort` cu comparator aleator); returnează string-ul.

Entropia țintă: alfabetul concatenat are 69 de caractere, deci 16 caractere ≈ 97 biți — cerința D-05 („generată automat", imposibil de ghicit din date publice ale adminului).

Adaugă un comentariu de fișier (română) care spune explicit: parola e afișată o singură dată (D-06), nu se stochează în clar nicăieri, nu se loghează.

Creează `utils/parola.test.ts` copiind structura din `utils/luniLipsa.test.ts`: funcție locală `assert(condition, message)` care aruncă la fals și loghează `PASS: ...` la adevărat, o funcție exportată `ruleazaTeste()` care rulează toate asserțiunile din blocul `<behavior>` de mai sus și afișează la final `Rezultat: N PASS, M FAIL`, plus apel automat la `ruleazaTeste()` la finalul fișierului (ca în `luniLipsa.test.ts`) astfel încât `node --import tsx utils/parola.test.ts` să ruleze testele direct.
  </action>
  <verify>
    <automated>node --import tsx utils/parola.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx utils/parola.test.ts` iese cu cod 0 și afișează `0 FAIL`
    - `grep -c "Math.random" utils/parola.ts` returnează 0
    - `grep -c "getRandomValues" utils/parola.ts` este >= 1
    - `grep -v '^\s*//' utils/parola.ts | grep -c "export function genereazaParolaTemporara"` este >= 1
    - Rulând `node --import tsx -e "import('./utils/parola.ts').then(m=>{const s=new Set();for(let i=0;i<200;i++)s.add(m.genereazaParolaTemporara());if(s.size!==200)throw new Error('coliziuni');console.log('OK unic')})"` afișează `OK unic`
  </acceptance_criteria>
  <done>`utils/parola.ts` exportă `genereazaParolaTemporara` cu sursă de entropie `crypto.getRandomValues` și rejection sampling; testul colocat trece cu 0 FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: Gardă de autentificare, anti-escaladare rol și scoping club pe api/creare-cont.ts + trebuie_schimbata_parola (D-05)</name>
  <files>api/creare-cont.ts</files>
  <read_first>
    - `api/creare-cont.ts` — fișierul integral, starea curentă: zero verificare a apelantului, `SUPABASE_SERVICE_ROLE_KEY`, ramura „email deja înregistrat" (liniile 36-60), apel RPC `refactor_create_user_account` (liniile 63-80), fetch `sportivi` final (liniile 88-96)
    - `api/_rateLimit.ts` — helper-ele `checkRateLimit(identifier, { windowMs, maxRequests })` și `getClientIp(req)` de reutilizat ca atare
    - `api/rag-index.ts` liniile 20-30 — singurul pattern existent de gardă pe un endpoint din acest proiect (secret partajat + `403`), ca referință de stil pentru early-return
    - `api/reset-parola-sportiv.ts` liniile 36-47 — pattern-ul „update `trebuie_schimbata_parola`, avertizează dar nu eșua tot request-ul" de replicat
    - `hooks/useRoleAssignment.ts` liniile 12-18 — tabela `roleWeights` care trebuie oglindită server-side
    - `sql/refactor/REFACTOR_CREATE_ACCOUNT.sql` — semnătura și comportamentul RPC `refactor_create_user_account` (NU setează `trebuie_schimbata_parola`; creează întotdeauna un rând în `sportivi`, inclusiv pentru conturi doar-`ADMIN_CLUB`)
  </read_first>
  <action>
Modifică `api/creare-cont.ts`, păstrând intactă logica existentă de creare user + RPC + fetch sportiv. Inserează gărzile în ordinea de mai jos, **înainte** de `const { email, password, userData, roles } = req.body` sau imediat după, dar în orice caz înainte de orice apel `supabaseAdmin.auth.admin.createUser`.

1. **Rate limit.** Importă `checkRateLimit` și `getClientIp` din `./_rateLimit`. Apelează `checkRateLimit(\`creare-cont:${getClientIp(req)}\`, { windowMs: 60000, maxRequests: 10 })`. Dacă `allowed` e fals, răspunde `429` cu `{ error: 'Prea multe cereri. Reîncercați peste un minut.' }`.

2. **Autentificare apelant.** Citește `req.headers.authorization`. Dacă lipsește sau nu începe cu `Bearer `, răspunde `401` cu `{ error: 'Autentificare necesară.' }`. Extrage token-ul și apelează `supabaseAdmin.auth.getUser(token)`. Dacă întoarce eroare sau `user` lipsă, răspunde `401` cu `{ error: 'Sesiune invalidă sau expirată. Reautentificați-vă.' }`.

3. **Rolurile apelantului.** Interoghează `supabaseAdmin.from('utilizator_roluri_multicont').select('rol_denumire, club_id').eq('user_id', <id-ul user-ului autentificat>)`. Definește constanta locală `ROLE_WEIGHTS` cu exact aceleași valori ca `roleWeights` din `hooks/useRoleAssignment.ts`: `SUPER_ADMIN_FEDERATIE: 5`, `ADMIN: 4`, `ADMIN_CLUB: 3`, `INSTRUCTOR: 2`, `SPORTIV: 1`. Calculează `greutateApelant` = maximul greutăților rândurilor returnate (0 dacă nu are niciun rol). Dacă `greutateApelant < 2`, răspunde `403` cu `{ error: 'Nu aveți permisiunea de a crea conturi.' }` — crearea de conturi este acțiune de staff (INSTRUCTOR și peste).

4. **Anti-escaladare de privilegii.** Validează că `roles` este un array nevid de string-uri; altfel `400` cu `{ error: 'Lista de roluri este invalidă.' }`. Dacă vreun element nu este cheie în `ROLE_WEIGHTS`, răspunde `400` cu `{ error: 'Rol necunoscut: <numele rolului>.' }`. Dacă vreun rol cerut are greutate `> greutateApelant`, răspunde `403` cu `{ error: 'Nu puteți acorda un rol cu privilegii mai mari decât rolul dumneavoastră.' }`.

5. **Scoping pe club.** Dacă `greutateApelant < 5` (adică apelantul NU este `SUPER_ADMIN_FEDERATIE`): cere ca `userData?.club_id` să existe și să se regăsească în mulțimea de `club_id` din rândurile de roluri ale apelantului; altfel răspunde `403` cu `{ error: 'Nu puteți crea conturi în alt club.' }`. Un `SUPER_ADMIN_FEDERATIE` poate crea în orice club (inclusiv în clubul nou creat la pasul 1 al wizard-ului din 26-02).

6. **`trebuie_schimbata_parola` (D-05).** După ce apelul RPC `refactor_create_user_account` reușește și **înainte** de `select('*, cluburi(*)')` pe `sportivi`, execută `supabaseAdmin.from('sportivi').update({ trebuie_schimbata_parola: true }).eq('user_id', userId)`. Dacă update-ul dă eroare, doar `console.warn` (nu eșua request-ul) — exact pattern-ul din `api/reset-parola-sportiv.ts`. Ordinea contează: update înainte de select, ca obiectul `sportiv` returnat clientului să reflecte flag-ul.

7. **Igienă de logare (expunere parolă).** Verifică și păstrează invariantul: nicăieri în fișier nu se loghează `password`, `req.body` sau `userData` întreg. Blocul `catch` existent loghează doar `error` — păstrează-l așa. Nu adăuga `console.log` de debug cu payload-ul.

Nu schimba forma răspunsului de succes: rămâne `{ success: true, userId, sportiv }` și **nu** include parola în răspuns.
  </action>
  <verify>
    <automated>npm run lint && grep -v '^\s*//' api/creare-cont.ts | grep -c "auth.getUser" && grep -v '^\s*//' api/creare-cont.ts | grep -c "checkRateLimit" && grep -v '^\s*//' api/creare-cont.ts | grep -c "trebuie_schimbata_parola"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` (tsc --noEmit) trece fără erori
    - `grep -v '^\s*//' api/creare-cont.ts | grep -c "auth.getUser"` este >= 1
    - `grep -v '^\s*//' api/creare-cont.ts | grep -c "checkRateLimit"` este >= 1
    - `grep -v '^\s*//' api/creare-cont.ts | grep -c "utilizator_roluri_multicont"` este >= 1
    - `grep -v '^\s*//' api/creare-cont.ts | grep -c "trebuie_schimbata_parola"` este >= 1
    - `grep -v '^\s*//' api/creare-cont.ts | grep -cE "console\.(log|warn|error)\(.*(password|req\.body)"` este 0
    - Sursa conține toate cele 5 coduri de răspuns de gardă: `429`, `401`, `403`, `400` — verificabil prin `grep -c "status(429)\|status(401)\|status(403)\|status(400)" api/creare-cont.ts` >= 4
    - `ROLE_WEIGHTS` din `api/creare-cont.ts` conține exact aceleași 5 chei și valori ca `roleWeights` din `hooks/useRoleAssignment.ts` (SUPER_ADMIN_FEDERATIE 5, ADMIN 4, ADMIN_CLUB 3, INSTRUCTOR 2, SPORTIV 1)
  </acceptance_criteria>
  <done>POST /api/creare-cont fără header `Authorization` returnează 401 fără a atinge `auth.admin.createUser`; un apelant ADMIN_CLUB nu poate cere rol SUPER_ADMIN_FEDERATIE (403) și nu poate crea în alt club (403); contul creat are `trebuie_schimbata_parola = true`.</done>
</task>

<task type="auto">
  <name>Task 3: Trimite token de sesiune din useRoleAssignment și elimină fallback-ul de club hardcodat</name>
  <files>hooks/useRoleAssignment.ts</files>
  <read_first>
    - `hooks/useRoleAssignment.ts` — funcția `createAccountAndAssignRole` (liniile 30-83), în special apelul `fetch('/api/creare-cont')` fără header de autorizare (liniile 34-59) și fallback-ul `sportivData.club_id || PHI_HAU_IASI_CLUB_ID` (linia 46)
    - `supabaseClient.ts` — cum e configurat clientul și cum se obține sesiunea curentă
    - `components/UserManagement.tsx` liniile 60-120 — apelantul principal (creare membru staff), ca să confirmi că `club_id` e mereu furnizat din `formData.club_id`
    - `components/UserProfile/CreateAccountModal.tsx` liniile 35-55 — al doilea apelant (creare cont sportiv), care trimite obiectul `sportiv` întreg
    - `CLAUDE.md` secțiunea "Anti-Pattern: Hardcoded Club IDs"
  </read_first>
  <action>
În `hooks/useRoleAssignment.ts`, în interiorul `createAccountAndAssignRole`, înainte de `fetch`:

1. **Validare club_id (anti-pattern CLAUDE.md).** Înlocuiește fallback-ul `sportivData.club_id || PHI_HAU_IASI_CLUB_ID` cu o verificare fail-fast: dacă `sportivData.club_id` lipsește, întoarce `{ success: false, error: 'Clubul destinație lipsește. Selectați un club înainte de a crea contul.' }` fără a apela endpoint-ul. În `userData` trimite `club_id: sportivData.club_id` direct. Elimină importul `PHI_HAU_IASI_CLUB_ID` din `../constants` dacă rămâne nefolosit în fișier (verifică — poate mai fi folosit în `updateRoles`).

2. **Token de sesiune.** Obține sesiunea curentă cu `await supabase.auth.getSession()`. Dacă `session?.access_token` lipsește, întoarce `{ success: false, error: 'Sesiune expirată. Reautentificați-vă și încercați din nou.' }` fără a apela endpoint-ul. Altfel adaugă în obiectul `headers` al apelului `fetch` cheia `Authorization` cu valoarea `Bearer ` concatenat cu `session.access_token`, păstrând `'Content-Type': 'application/json'`.

3. Nu modifica whitelist-ul câmpurilor din `userData` și nu modifica `updateRoles`. Nu loga niciodată `parola` — blocul `console.error('Account Creation Error:', err)` existent rămâne neschimbat (loghează doar eroarea, nu payload-ul).

Această modificare este cuplată cu gărzile din Task 2: fără ea, toate cele trei fluxuri existente de creare cont (UserManagement „Adaugă Membru Staff", UserManagement „Creează Cont", Sportivi „Creează Cont") ar primi 401. Verificarea umană de mai jos acoperă exact această regresie.
  </action>
  <verify>
    <automated>npm run lint && grep -v '^\s*//' hooks/useRoleAssignment.ts | grep -c "Authorization" && grep -v '^\s*//' hooks/useRoleAssignment.ts | grep -c "PHI_HAU_IASI_CLUB_ID"</automated>
    <human-check>
      Pornește `npm run dev`, autentifică-te ca SUPER_ADMIN_FEDERATIE și verifică regresia pe fluxurile EXISTENTE (nu cele noi din 26-02):
      1. User Management -> „Adaugă Membru Staff" -> completează nume/prenume/email/parolă/club/rol -> Salvează. Așteptat: contul se creează, apare `CredentialeContModal`, fără eroare 401.
      2. User Management -> pe un sportiv fără cont -> „Creează Cont" -> metoda „Email + Parolă" -> Generează și Asociază. Așteptat: contul se creează.
      3. Deloghează-te, loghează-te cu contul nou creat la pasul 1. Așteptat: ecranul `MandatoryPasswordChange` apare imediat (dovada că `trebuie_schimbata_parola = true` chiar se scrie acum — înainte de această fază NU se scria).
      4. Din DevTools -> Network, ia request-ul `/api/creare-cont` și confirmă că are header `Authorization: Bearer ...` și că răspunsul NU conține parola.
      Raportează „aprobat" sau descrie eroarea exactă.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fără erori
    - `grep -v '^\s*//' hooks/useRoleAssignment.ts | grep -c "Authorization"` este >= 1
    - `grep -v '^\s*//' hooks/useRoleAssignment.ts | grep -c "getSession"` este >= 1
    - `grep -v '^\s*//' hooks/useRoleAssignment.ts | grep -c "sportivData.club_id || PHI_HAU_IASI_CLUB_ID"` este 0
    - Cele 3 fluxuri existente de creare cont funcționează după modificare (human-check pașii 1-2)
    - Contul nou creat forțează schimbarea parolei la primul login (human-check pasul 3)
  </acceptance_criteria>
  <done>Toate apelurile către `/api/creare-cont` trimit `Authorization: Bearer <access_token>`; niciun cont nu mai poate fi creat cu `club_id` implicit hardcodat; fluxurile existente sunt nesparte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `/api/creare-cont` (Vercel function) | Input necontrolat traversează aici; funcția rulează cu `SUPABASE_SERVICE_ROLE_KEY` care **bypassează complet RLS** |
| `/api/creare-cont` -> Supabase `auth.users` + RPC `refactor_create_user_account` | Creare identitate + acordare de roluri; escaladarea de privilegii se materializează aici |
| server -> ecranul SUPER_ADMIN (`CredentialeContModal`) | Parola în clar traversează rețeaua (HTTPS) și ajunge în DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Elevation of Privilege | `api/creare-cont.ts` — endpoint neautentificat cu service role key; orice client anonim poate POSTa `roles: ['SUPER_ADMIN_FEDERATIE']` și prelua federația | mitigate | Task 2 gărzile 2-4: `Authorization: Bearer` obligatoriu, `supabaseAdmin.auth.getUser(token)` validează JWT, roluri citite din `utilizator_roluri_multicont`, refuz 403 dacă greutatea rolului cerut > greutatea apelantului |
| T-26-02 | Elevation of Privilege | `api/creare-cont.ts` — un `ADMIN_CLUB` al clubului A creează conturi în clubul B (bypass al izolării cross-club reparate în Faza 25) | mitigate | Task 2 garda 5: pentru apelanți cu greutate < 5, `userData.club_id` trebuie să fie în mulțimea de `club_id` a rolurilor apelantului, altfel 403 |
| T-26-03 | Information Disclosure | Parola temporară ghicibilă — pattern-ul existent `${nume}.1234!` derivă parola din date publice ale persoanei | mitigate | Task 1: `genereazaParolaTemporara` cu `crypto.getRandomValues` + rejection sampling, alfabet de 69 caractere, 16 caractere ≈ 97 biți entropie |
| T-26-04 | Information Disclosure | Parola în clar loggată accidental (server log Vercel, `console.log` de debug, răspuns HTTP) | mitigate | Task 2 punctul 7 + Task 3 punctul 3: invariant „nu se loghează `password`/`req.body`/`userData`"; răspunsul de succes rămâne `{ success, userId, sportiv }` fără parolă; criteriu de acceptare grep-abil |
| T-26-05 | Information Disclosure | Parola în tranzit / în DOM pe ecranul SUPER_ADMIN | accept | HTTPS obligatoriu (Vercel), parola e afișată intenționat o singură dată către SUPER_ADMIN (D-06, decizie explicită a utilizatorului), nu se persistă în localStorage/cache; `trebuie_schimbata_parola = true` limitează fereastra de valabilitate la primul login |
| T-26-06 | Denial of Service | Abuz de `/api/creare-cont` pentru a inunda `auth.users` sau a epuiza cota Vercel | mitigate | Task 2 garda 1: `checkRateLimit` 10 cereri/minut/IP, răspuns 429 (limitare in-memory, resetată la cold start — acceptabil per nota din `api/_rateLimit.ts`) |
| T-26-07 | Tampering | Retry pe pasul admin (D-07) creează utilizatori `auth.users` duplicați sau rânduri `sportivi` duplicate | mitigate | `api/creare-cont.ts` are deja ramura „email deja înregistrat" (rezolvă `user_id` existent) iar `refactor_create_user_account` face UPDATE pe sportivul existent găsit după email, nu INSERT; planul 26-02 reutilizează aceeași parolă generată la toate retry-urile ca parola afișată să rămână validă |
| T-26-08 | Repudiation | Nu există urmă de audit pentru „cine a creat clubul X și adminul lui" | accept | `audit_log` + triggerele CRUD introduse de quick task 260704-x9p acoperă deja INSERT pe `cluburi` și pe `utilizator_roluri_multicont`; nu se adaugă logare suplimentară în această fază |
| T-26-09 | Tampering | Caz rezidual: `auth.users` creat dar RPC eșuat -> nu există rând `sportivi`, iar retry-ul lovește eroarea „Emailul X există în autentificare dar nu are un profil sportiv asociat" | accept | Limitare pre-existentă a endpoint-ului, afectează identic toți apelanții actuali; rezolvarea ar cere lookup direct în schema `auth` (nu e expusă prin PostgREST) sau paginare `listUsers` fragilă la 3500+ conturi; mitigare parțială: mesajul de eroare al serverului e afișat verbatim în bannerul D-07 din 26-02, deci SUPER_ADMIN vede cauza reală |
| T-26-SC | Tampering | Instalări npm/pip/cargo | mitigate | Fără dependențe noi în această fază — se folosesc doar `crypto` (Web Crypto, built-in) și module deja prezente (`@supabase/supabase-js`, `@vercel/node`). Nu se rulează niciun `npm install`; nu e necesară verificare de legitimitate pachete. |
</threat_model>

<verification>
1. `npm run lint` — zero erori TypeScript pe tot proiectul
2. `node --import tsx utils/parola.test.ts` — 0 FAIL
3. Asserțiuni de sursă (grep) din `<acceptance_criteria>` fiecărui task
4. Human-check de regresie din Task 3 pe cele 3 fluxuri existente de creare cont
</verification>

<success_criteria>
- `genereazaParolaTemporara` există, e testată și nu folosește `Math.random`
- `/api/creare-cont` respinge apelanții neautentificați (401), rolurile escaladate (403), cluburile străine (403) și abuzul (429)
- Conturile create prin endpoint au `trebuie_schimbata_parola = true`
- Fluxurile existente de creare cont din UserManagement și Sportivi funcționează nemodificate funcțional
- Nicio parolă nu apare în loguri sau în răspunsul HTTP
</success_criteria>

<output>
Create `.planning/phases/26-wizard-onboarding-club-nou-ghidat-de-super-admin-club-prim-a/26-01-SUMMARY.md` when done
</output>
