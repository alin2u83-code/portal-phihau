---
phase: 12-modul-produse
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - services/produseService.ts
  - components/Produse/IntrareMarfaModal.tsx
  - components/Produse/index.tsx
autonomous: true
requirements: [PRD-03]

must_haves:
  truths:
    - "Există components/Produse/IntrareMarfaModal.tsx pentru înregistrare intrare marfă cu furnizor + nr. factură + linii produse"
    - "Tab-ul 'intrari' din ProduseManagement afișează lista intrărilor de marfă + buton Intrare Nouă"
    - "La salvare intrare marfă: stoc_curent al variantei crește cu cantitatea intrată"
    - "produseService.ts are funcțiile fetchIntrari, createIntrareMarfa"
  artifacts:
    - path: "components/Produse/IntrareMarfaModal.tsx"
      provides: "Modal înregistrare intrare marfă cu linii variante"
    - path: "components/Produse/index.tsx"
      provides: "Tab 'intrari' funcțional cu lista intrărilor"
    - path: "services/produseService.ts"
      provides: "fetchIntrari + createIntrareMarfa adăugate"
  key_links:
    - from: "IntrareMarfaModal handleSave"
      to: "produseService.createIntrareMarfa"
      via: "creare intrare + update stoc variante"
      pattern: "createIntrareMarfa"
---

<objective>
Implementează modulul de intrări marfă (jurnal stoc). ADMIN_CLUB poate înregistra primiri de marfă cu furnizor, număr factură, și linii de produse+cantități. Stocul variantelor se actualizează automat.

Purpose: Adminul poate gestiona stocul de intrare și vedea istoricul achizițiilor de marfă.
Output: Modal IntrareMarfa + tab 'intrari' funcțional + 2 funcții service noi.
</objective>

<context>
@.planning/phases/12-modul-produse/12-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: produseService.ts — fetchIntrari + createIntrareMarfa cu update stoc</name>
  <files>services/produseService.ts</files>
  <read_first>
    - services/produseService.ts (tot fișierul — adaugă la final)
    - types.ts (ProdusIntrareDB, ProdusIntrareDetaliuDB, ProdusIntrare)
  </read_first>
  <action>
    Adaugă la finalul `services/produseService.ts`:

    ```typescript
    export async function fetchIntrari(): Promise<ProdusIntrare[]> {
      const { data, error } = await supabaseClient
        .from('produse_intrari')
        .select(`
          *,
          detalii:produse_intrari_detalii(
            *,
            varianta:produse_variante(*, produs:produse(denumire))
          )
        `)
        .order('data_factura', { ascending: false })
        .order('created_at', { ascending: false });
      if (error) throw new Error(`fetchIntrari: ${error.message}`);
      return (data ?? []) as ProdusIntrare[];
    }

    export async function createIntrareMarfa(input: {
      club_id: string;
      furnizor?: string;
      nr_factura?: string;
      data_factura: string;
      observatii?: string;
      linii: { varianta_id: string; cantitate: number; pret_intrare_snapshot: number }[];
    }): Promise<ProdusIntrareDB> {
      // 1. Creează header intrare
      const { data: intrare, error: errIntrare } = await supabaseClient
        .from('produse_intrari')
        .insert({
          club_id: input.club_id,
          furnizor: input.furnizor ?? null,
          nr_factura: input.nr_factura ?? null,
          data_factura: input.data_factura,
          observatii: input.observatii ?? null,
        })
        .select()
        .single();
      if (errIntrare) throw new Error(`createIntrareMarfa header: ${errIntrare.message}`);

      // 2. Inserează linii
      const detalii = input.linii.map(l => ({
        intrare_id: intrare.id,
        varianta_id: l.varianta_id,
        cantitate: l.cantitate,
        pret_intrare_snapshot: l.pret_intrare_snapshot,
      }));
      const { error: errDetalii } = await supabaseClient
        .from('produse_intrari_detalii')
        .insert(detalii);
      if (errDetalii) throw new Error(`createIntrareMarfa detalii: ${errDetalii.message}`);

      // 3. Actualizează stoc per variantă (rpc sau update individual)
      await Promise.all(input.linii.map(async (l) => {
        const { data: v } = await supabaseClient
          .from('produse_variante')
          .select('stoc_curent')
          .eq('id', l.varianta_id)
          .single();
        await supabaseClient
          .from('produse_variante')
          .update({ stoc_curent: (v?.stoc_curent ?? 0) + l.cantitate, updated_at: new Date().toISOString() })
          .eq('id', l.varianta_id);
      }));

      return intrare;
    }
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "produseService" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -c "fetchIntrari\|createIntrareMarfa" services/produseService.ts</automated>
  </verify>
  <acceptance_criteria>
    - `fetchIntrari` și `createIntrareMarfa` exportate din produseService.ts
    - `createIntrareMarfa` crează header + detalii + actualizează `stoc_curent` per variantă
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>fetchIntrari + createIntrareMarfa adăugate la produseService.ts cu logică stoc.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: IntrareMarfaModal.tsx — modal înregistrare intrare marfă</name>
  <files>components/Produse/IntrareMarfaModal.tsx</files>
  <read_first>
    - components/Produse/ProdusFormModal.tsx (pattern tabel editabil inline cu rânduri dinamice)
    - components/ui.tsx (Modal, Button, Input, Select)
    - types.ts (Produs, ProdusVariantaDB)
  </read_first>
  <action>
    Creează `components/Produse/IntrareMarfaModal.tsx`.

    Props:
    ```typescript
    interface IntrareMarfaModalProps {
      produse: Produs[];
      clubId: string;
      onSave: () => void;
      onClose: () => void;
    }
    ```

    State:
    - `furnizor: string`
    - `nrFactura: string`
    - `dataFactura: string` (default: today ISO)
    - `observatii: string`
    - `linii: LiniIntrare[]` unde `LiniIntrare = { produs_id: string; varianta_id: string; cantitate: string; pret_intrare: string }`
    - `saving: boolean`, `error: string | null`

    **UI:**

    Header modal: "Intrare Marfă Nouă"

    Câmpuri header:
    - Input "Furnizor" (text liber, opțional) → `furnizor`
    - Input "Nr. Factură" (text, opțional) → `nrFactura`
    - Input "Data facturii" (type="date") → `dataFactura`
    - Textarea "Observații" (opțional) → `observatii`

    Separator + titlu "Produse intrate"

    Tabel linii (tabel editabil):
    - Coloane: Produs | Variantă | Cantitate | Preț intrare (RON) | Acțiuni
    - Select Produs: `produse.map(p => <option value={p.id}>{p.denumire}</option>)`
    - Select Variantă: filtrat pe produsul selectat din linia curentă; label: `${v.culoare ?? ''} ${v.marime ?? ''}`.trim() || 'Standard'; value: v.id
    - Input Cantitate: type="number" min="1"
    - Input Preț intrare: type="number" step="0.01" (pre-populat cu `varianta.pret_intrare` la selecția variantei)
    - Buton X: șterge linia

    Buton "+ Adaugă produs" sub tabel → push linie nouă vidă.

    Footer:
    - "Anulează" → onClose
    - "Salvează intrarea" → handleSave

    **handleSave:**
    1. Validare: cel puțin 1 linie completă
    2. `createIntrareMarfa({ club_id: clubId, furnizor, nr_factura: nrFactura, data_factura: dataFactura, observatii, linii: linii.map(l => ({ varianta_id: l.varianta_id, cantitate: +l.cantitate, pret_intrare_snapshot: +l.pret_intrare })) })`
    3. `onSave()` → triggers re-fetch în parent
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "IntrareMarfaModal" ; echo "exit:$?"</automated>
    <automated>test -f "C:/Users/lungu/portal-phihau/components/Produse/IntrareMarfaModal.tsx" && echo "exists"</automated>
  </verify>
  <acceptance_criteria>
    - `components/Produse/IntrareMarfaModal.tsx` există cu default export `IntrareMarfaModal`
    - Tabel linii cu Select produs + Select variantă + cantitate + preț
    - handleSave apelează `createIntrareMarfa`
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>IntrareMarfaModal creat cu form header + tabel linii dinamic + logică save.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: components/Produse/index.tsx — activare tab 'intrari'</name>
  <files>components/Produse/index.tsx</files>
  <read_first>
    - components/Produse/index.tsx (starea curentă — placeholder tabs)
    - components/Produse/IntrareMarfaModal.tsx (creat în Task 2)
    - services/produseService.ts (fetchIntrari nou)
  </read_first>
  <action>
    Modifică `components/Produse/index.tsx`:

    1. Adaugă state: `intrari: ProdusIntrare[]`, `showIntrareModal: boolean`

    2. Adaugă în useEffect: `fetchIntrari().then(setIntrari)` (parallel cu fetchProduse + fetchCategorii)

    3. Înlocuiește placeholder-ul tab-ului 'intrari' cu UI complet:

    ```jsx
    {activeTab === 'intrari' && (
      <div className="space-y-4">
        <div className="flex justify-between items-center">
          <h2 className="text-lg font-bold text-white">Intrări Marfă</h2>
          <Button onClick={() => setShowIntrareModal(true)}>+ Intrare Nouă</Button>
        </div>

        {/* Tabel intrări desktop */}
        <div className="hidden md:block bg-[var(--t-bg)] border border-[var(--t-border)] rounded-xl overflow-hidden">
          <table className="w-full text-sm text-left">
            <thead style={{ background: 'var(--t-table-header-bg)', color: 'var(--t-table-header-text)' }}>
              <tr className="border-b border-[var(--t-border)]">
                <th className="px-4 py-3 text-xs font-bold uppercase tracking-wider">Data</th>
                <th className="px-4 py-3 text-xs font-bold uppercase tracking-wider">Furnizor</th>
                <th className="px-4 py-3 text-xs font-bold uppercase tracking-wider">Nr. Factură</th>
                <th className="px-4 py-3 text-xs font-bold uppercase tracking-wider text-right">Produse</th>
                <th className="px-4 py-3 text-xs font-bold uppercase tracking-wider text-right">Total intrare</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-[var(--t-border)]">
              {intrari.map(i => (
                <tr key={i.id} className="hover:bg-[var(--t-table-row-hover)]">
                  <td className="px-4 py-3 text-slate-300">{new Date(i.data_factura).toLocaleDateString('ro-RO')}</td>
                  <td className="px-4 py-3 text-white">{i.furnizor ?? '—'}</td>
                  <td className="px-4 py-3 text-slate-300">{i.nr_factura ?? '—'}</td>
                  <td className="px-4 py-3 text-right text-slate-300">{i.detalii.length} linii</td>
                  <td className="px-4 py-3 text-right font-bold text-emerald-400">
                    {i.detalii.reduce((s, d) => s + d.pret_intrare_snapshot * d.cantitate, 0).toFixed(2)} RON
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
          {intrari.length === 0 && (
            <div className="py-12 text-center text-slate-400">Nicio intrare de marfă înregistrată</div>
          )}
        </div>

        {/* Carduri mobile */}
        <div className="md:hidden space-y-2">
          {intrari.map(i => (
            <div key={i.id} className="bg-[var(--t-bg)] border border-[var(--t-border)] rounded-xl px-4 py-3">
              <div className="flex justify-between">
                <span className="text-white font-bold text-sm">{i.furnizor ?? 'Fără furnizor'}</span>
                <span className="text-emerald-400 font-bold text-sm">
                  {i.detalii.reduce((s, d) => s + d.pret_intrare_snapshot * d.cantitate, 0).toFixed(2)} RON
                </span>
              </div>
              <div className="text-slate-400 text-xs mt-1">
                {new Date(i.data_factura).toLocaleDateString('ro-RO')} · {i.nr_factura ? `Nr. ${i.nr_factura}` : 'Fără nr. factură'} · {i.detalii.length} linii
              </div>
            </div>
          ))}
          {intrari.length === 0 && <div className="py-8 text-center text-slate-400">Nicio intrare de marfă</div>}
        </div>

        {showIntrareModal && (
          <IntrareMarfaModal
            produse={produse}
            clubId={activeRoleContext?.club_id ?? ''}
            onSave={async () => {
              setShowIntrareModal(false);
              const [updatedProduse, updatedIntrari] = await Promise.all([fetchProduse(), fetchIntrari()]);
              setProduse(updatedProduse);
              setIntrari(updatedIntrari);
            }}
            onClose={() => setShowIntrareModal(false)}
          />
        )}
      </div>
    )}
    ```

    Importă `IntrareMarfaModal` din `'./IntrareMarfaModal'`.
    Importă `fetchIntrari` din `'../../services/produseService'`.
    Importă `ProdusIntrare` din `'../../types'`.
    Importă `useData` din context pentru `activeRoleContext`.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "Produse/index" ; echo "exit:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -c "IntrareMarfaModal\|fetchIntrari\|intrari\b" components/Produse/index.tsx</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Tab 'intrari' nu mai are placeholder — afișează lista intrărilor + buton "Intrare Nouă"
    - `IntrareMarfaModal` se deschide pe click buton
    - La salvare: lista se re-fetch și stocul variantelor crește
    - `npx tsc --noEmit` + `npx vite build` trec
  </acceptance_criteria>
  <done>Tab Intrări Marfă funcțional cu istoric intrări + modal creare + actualizare stoc automat.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` + `npx vite build` — zero erori
2. Manual: tab "Intrări Marfă" → buton "Intrare Nouă" → modal se deschide
3. Manual: completează furnizor + nr factură + adaugă 2 linii (produs+variantă+cantitate+preț) → Salvează
4. Manual: intrarea apare în tabel cu data, furnizor, total
5. Manual: mergi în tab Catalog → produsul respectiv are stoc_curent crescut cu cantitatea intrată (badge stoc redus dispare dacă stocul e acum suficient)
</verification>

<success_criteria>
- [ ] Tab "Intrări Marfă" afișează lista intrărilor cu dată, furnizor, nr. factură, total (PRD-03)
- [ ] Modal "Intrare Nouă" permite selectare produs+variantă+cantitate+preț per linie (PRD-03)
- [ ] Stoc_curent crește la salvare intrare (PRD-03)
- [ ] Build + TypeScript trec
</success_criteria>

<output>
Create `.planning/phases/12-modul-produse/12-03-SUMMARY.md` when done
</output>
