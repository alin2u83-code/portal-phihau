---
phase: 07-aplicare-filtre-pe-tab-uri
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Competitii/index.tsx
autonomous: true
requirements:
  - TAB-01
  - TAB-02
  - TAB-03
  - INSC-03

must_haves:
  truths:
    - "useCompetitieFilters() instanțiat o singură dată în CompetitieDetail (linia ~74)"
    - "resetFiltre() apelat în handleSetActiveTab — filtrele se șterg la orice schimbare de tab"
    - "filteredCategorii calculat via aplicaFiltreCategorie(categorii, filtre) — nu mai filtrează doar după selectedProbaId"
    - "CompetitieFilterBar randat în tab Categorii deasupra tabelului de categorii"
    - "Numărul din butonul tab Categorii reflectă filteredCategorii.length (nu categorii.length)"
  artifacts:
    - path: "components/Competitii/index.tsx"
      provides: "Hook instanțiere + resetFiltre în handleSetActiveTab + CompetitieFilterBar în tab Categorii"
      contains: "useCompetitieFilters"
  key_links:
    - from: "CompetitieDetail"
      to: "useCompetitieFilters"
      via: "import + destructurare"
      pattern: "useCompetitieFilters"
    - from: "handleSetActiveTab"
      to: "resetFiltre"
      via: "useCallback deps"
      pattern: "resetFiltre\\(\\)"
    - from: "filteredCategorii"
      to: "aplicaFiltreCategorie"
      via: "useMemo"
      pattern: "aplicaFiltreCategorie"
---

<objective>
Instanțierea centralizată a hookului de filtrare în CompetitieDetail și integrarea CompetitieFilterBar în tab-ul Categorii — fundația de orchestrare pentru toate celelalte planuri din Wave 2.

Purpose: Hookul trebuie să existe O SINGURĂ DATĂ la nivel de CompetitieDetail pentru ca filtrele să fie partajate între tab-uri și resetul la schimbarea tab-ului să funcționeze consistent (INSC-03). Fără acest plan, planurile 07-02, 07-03 și 07-04 nu au unde să primească props.

Output: index.tsx cu hook instanțiat, resetFiltre în handleSetActiveTab, filteredCategorii via aplicaFiltreCategorie, CompetitieFilterBar montat în tab Categorii, numărătoarea din butonul tab actualizată.
</objective>

<execution_context>
@C:/Users/lungu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lungu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-infrastructur-filtrare-unificat/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instanțiere hook + resetFiltre în handleSetActiveTab + filteredCategorii via aplicaFiltreCategorie</name>
  <files>components/Competitii/index.tsx</files>

  <read_first>
    components/Competitii/index.tsx liniile 1-30 (imports existente)
    components/Competitii/index.tsx liniile 64 (selectedProbaId useState)
    components/Competitii/index.tsx liniile 73-82 (expandedCats + handleSetActiveTab)
    components/Competitii/index.tsx liniile 117-119 (filteredCategorii calculul curent)
    hooks/useCompetitieFilters.ts liniile 1-81 (API complet: CompetitieFiltre, aplicaFiltreCategorie, useCompetitieFilters)
  </read_first>

  <behavior>
    - Adaugă importuri după linia 28 (după `import { InscrieriView }`):
      `import { useCompetitieFilters, aplicaFiltreCategorie } from '../../hooks/useCompetitieFilters';`
      `import { CompetitieFilterBar } from './CompetitieFilterBar';`
    - Adaugă hook call în CompetitieDetail după linia 73 (după `const [expandedCats...]`):
      `const { filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive } = useCompetitieFilters();`
    - Modifică handleSetActiveTab (liniile 79-82): adaugă `resetFiltre()` ca primă acțiune în callback; adaugă `resetFiltre` în dependency array al useCallback (INSC-03)
    - Înlocuiește filteredCategorii (liniile 117-119) cu useMemo care combină selectedProbaId cu aplicaFiltreCategorie:
      `const filteredCategorii = useMemo(() => { const baza = selectedProbaId ? categorii.filter(c => c.proba_id === selectedProbaId) : categorii; return aplicaFiltreCategorie(baza, filtre); }, [categorii, selectedProbaId, filtre]);`
    - IMPORTANT: selectedProbaId rămâne în cod — pills-urile de probă standalone din tab Categorii (liniile 291-308) sunt înlocuite în Task 2, nu eliminate prematur
    - IMPORTANT: useMemo este deja importat (linia 1) — nu mai adăuga la import
  </behavior>

  <action>
    Modifică components/Competitii/index.tsx:

    1. IMPORTURI (după linia cu `import { InscrieriView }`): Adaugă cele două linii de import pentru hook și componentă (per comportamentul descris).

    2. HOOK INSTANȚIERE (după `const [expandedCats, setExpandedCats] = useState...`): Adaugă destructurarea `useCompetitieFilters()`. Hook-ul este instanțiat o singură dată în CompetitieDetail — NU în componentele de tab individuale.

    3. handleSetActiveTab — adaugă `resetFiltre()` în corpul callback-ului (înainte de `setActiveTab(tab)`) și `resetFiltre` în array-ul de dependențe `useCallback`. ATENȚIE la Pitfall 2 din RESEARCH: resetFiltre este stabilă referențial (apelează setState intern) — adăugarea în deps este corectă și nu cauzează re-creare infinită.

    4. filteredCategorii — înlocuiește calculul simplu cu `useMemo` care aplică mai întâi filtrul de probă existent (`selectedProbaId`) apoi `aplicaFiltreCategorie(baza, filtre)`. Dependențe: `[categorii, selectedProbaId, filtre]`.
  </action>

  <verify>
    <automated>cd C:/Users/lungu/portal-phihau && npm run lint</automated>
  </verify>

  <done>
    npm run lint returnează zero erori TypeScript. Funcția useCompetitieFilters importată și apelată în CompetitieDetail. handleSetActiveTab are resetFiltre în corp și în deps. filteredCategorii calculat via aplicaFiltreCategorie.
  </done>
</task>

<task type="auto">
  <name>Task 2: Înlocuire pills standalone cu CompetitieFilterBar în tab Categorii + actualizare counter tab</name>
  <files>components/Competitii/index.tsx</files>

  <read_first>
    components/Competitii/index.tsx liniile 288-312 (bloc tab categorii: pills probe + comentariu "Category list")
    components/Competitii/index.tsx liniile 208-213 (butonul tab Categorii cu categorii.length)
    components/Competitii/CompetitieFilterBar.tsx liniile 5-13 (CompetitieFilterBarProps — 7 props obligatorii)
  </read_first>

  <behavior>
    - Înlocuiește blocul pills probe standalone (liniile 291-309 — blocul `{probe.length > 1 && (...)}`) cu `<CompetitieFilterBar filtre={filtre} toggleGen={toggleGen} setFiltre={setFiltre} resetFiltre={resetFiltre} nrFiltreActive={nrFiltreActive} probe={probe} grade={grade} />` (TAB-01: filtrul de probă nu dispare, intră în bara unificată)
    - Actualizează butonul tab Categorii (linia ~212): `categorii.length` → `filteredCategorii.length` în label (TAB-03)
    - selectedProbaId + setSelectedProbaId RĂMÂN declarate în cod (sunt folosite în filteredCategorii useMemo). Nu le șterge.
    - CompetitieFilterBar include deja controlul de probă (select) — deci pills standalone sunt înlocuite complet, nu dublate
    - NOTĂ: `grade` este deja disponibil în CompetitieDetail via `const { ..., grade, ... } = useData()` (linia 49)
  </behavior>

  <action>
    Modifică components/Competitii/index.tsx în tab Categorii (activeTab === 'categorii'):

    1. PILLS PROBE (liniile ~291-309): Șterge întreg blocul `{probe.length > 1 && (<div className="flex gap-2 flex-wrap">...</div>)}` și înlocuiește cu `<CompetitieFilterBar filtre={filtre} toggleGen={toggleGen} setFiltre={setFiltre} resetFiltre={resetFiltre} nrFiltreActive={nrFiltreActive} probe={probe} grade={grade} />`. Plasează imediat după `<div className="space-y-3">` care deschide tab-ul categorii.

    2. COUNTER TAB (linia ~212 — butonul tab Categorii): Înlocuiește `({categorii.length})` cu `({filteredCategorii.length})` în span-ul din interiorul butonului tab. Aceasta implementează TAB-03.
  </action>

  <verify>
    <automated>cd C:/Users/lungu/portal-phihau && npm run lint</automated>
  </verify>

  <done>
    npm run lint zero erori. CompetitieFilterBar este prezent în JSX pentru tab categorii. Butonul tab Categorii afișează filteredCategorii.length (nu categorii.length). Blocul pills standalone absent din cod.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → filtre | Câmpurile varstaMin/varstaMax/gradMin/gradMax sunt string input de la user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | aplicaFiltreCategorie cast-uri numerice | mitigate | Garduri `!isNaN(Number(...))` implementate în Phase 6 — moștenite, nu se duplică |
| T-07-02 | Information Disclosure | filtre client-side | accept | Date deja în memorie (niciun query Supabase nou), nicio suprafață nouă |
</threat_model>

<verification>
1. `npm run lint` — zero erori TypeScript
2. `grep -n "useCompetitieFilters" components/Competitii/index.tsx` — returnează cel puțin 2 linii (import + apel)
3. `grep -n "resetFiltre" components/Competitii/index.tsx` — returnează cel puțin 2 linii (în handleSetActiveTab + în deps array)
4. `grep -n "aplicaFiltreCategorie" components/Competitii/index.tsx` — returnează cel puțin 1 linie (în filteredCategorii useMemo)
5. `grep -n "CompetitieFilterBar" components/Competitii/index.tsx` — returnează cel puțin 2 linii (import + JSX)
6. `grep -v "^//" components/Competitii/index.tsx | grep -c "filteredCategorii.length"` — returnează cel puțin 1 (counter tab actualizat)
</verification>

<success_criteria>
- useCompetitieFilters() instanțiat în CompetitieDetail, nu în componentele de tab
- resetFiltre() în handleSetActiveTab + în deps array (INSC-03 satisfăcut structural)
- filteredCategorii calculat via aplicaFiltreCategorie — combină selectedProbaId cu filtrele complete
- CompetitieFilterBar montat în tab Categorii în locul pills-urilor standalone (TAB-01)
- Counter tab Categorii afișează filteredCategorii.length (TAB-02/TAB-03 funcționale)
- npm run lint zero erori
</success_criteria>

<output>
Creează `.planning/phases/07-aplicare-filtre-pe-tab-uri/07-01-SUMMARY.md` când ai terminat.
</output>
