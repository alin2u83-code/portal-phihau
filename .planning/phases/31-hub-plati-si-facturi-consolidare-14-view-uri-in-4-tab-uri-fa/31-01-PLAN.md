---
phase: 31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Plati/hub/platiHubConfig.ts
  - components/Plati/hub/platiHubConfig.test.ts
  - types.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
user_setup: []

must_haves:
  truths:
    - "Harta exacta de props si fluxuri (AppRouter.tsx liniile 72-89 si 228-285) e verificata fata de codul real si fixata ca bloc-antet in platiHubConfig.ts INAINTE ca orice plan sa atinga rutarea"
    - "Exista un singur literal View nou, 'plati-hub', adaugat aditiv in uniunea View din types.ts; toate cele 12 literale vechi raman in uniune (functioneaza ca alias-uri)"
    - "Pentru orice (activeView, viewParams, poateVedeaTaxeAnuale) functia pura rezolvaPozitieHub intoarce un tab si o sectiune valide, conform gruparii D-01..D-04"
    - "familii si deconturi-federatie NU sunt vederi ale hub-ului (D-05) — esteVedereHub le respinge"
  artifacts:
    - path: "components/Plati/hub/platiHubConfig.ts"
      provides: "Contractul hub-ului (tipuri, constante de grupare, rezolvitor pur de pozitie, harta de props verificata)"
      exports: ["TabHub", "SectiuneHub", "PozitieHub", "PlatiHubTabProps", "TabProfilSportiv", "TABURI_HUB", "ETICHETE_TABURI", "SECTIUNI_PE_TAB", "ETICHETE_SECTIUNI", "SECTIUNE_IMPLICITA", "TAB_PENTRU_SECTIUNE", "VEDERI_HUB", "esteTabHub", "esteSectiuneHub", "esteVedereHub", "rezolvaPozitieHub"]
      min_lines: 120
    - path: "components/Plati/hub/platiHubConfig.test.ts"
      provides: "Script tsx de verificare a rezolvitorului si a gruparii D-01..D-05"
      contains: "rezolvaPozitieHub"
    - path: "types.ts"
      provides: "Literalul View 'plati-hub'"
      contains: "'plati-hub'"
  key_links:
    - from: "components/Plati/hub/platiHubConfig.ts"
      to: "types.ts View"
      via: "import type { View, User, Permissions, Plata, Sportiv }"
      pattern: "import type .* from '../../../types'"
    - from: "components/Plati/hub/platiHubConfig.test.ts"
      to: "components/Plati/hub/platiHubConfig.ts"
      via: "import { rezolvaPozitieHub, ... } from './platiHubConfig'"
      pattern: "from './platiHubConfig'"
---

<objective>
Fundatia hub-ului "Plăți & Facturi": (1) AUDITUL harta props/fluxuri a celor 12 vederi care intra in hub, facut pe codul real inainte de orice atingere a rutarii; (2) contractul TypeScript pe care il consuma toate planurile ulterioare (tab-uri, shell, rutare); (3) gruparea D-01..D-04 si excluderea D-05 codificate intr-o singura sursa de adevar, cu un rezolvitor pur testabil.

Decizii implementate: D-01 (tab Facturi = plati-scadente, gestiune-facturi, facturi-fara-prezenta), D-02 (tab Încasări = jurnal-incasari, istoric-plati), D-03 (tab Rapoarte = raport-financiar, financial-dashboard), D-04 (tab Configurare = tipuri-abonament, configurare-preturi, reduceri, taxe-anuale, nomenclatoare), D-05 (familii si deconturi-federatie raman in afara hub-ului).

Claude's Discretion (31-CONTEXT.md), alegeri documentate aici:
- Numele vederii hub: literal nou `'plati-hub'` (nu reutilizarea `plati-scadente`), ca intrarea unica sa aiba identitate proprie in Header/AI/meniu; toate cele 12 literale vechi raman in `View` ca alias-uri catre hub (activeView e persistat in localStorage cheia `phi-hau-active-view`, iar favoritele AdminMasterMap tin literale vechi — scoaterea lor ar produce "Lipsește Vizualizarea").
- Structura: un singur component cu tab-uri interne (pattern Competitii `components/Competitii/index.tsx:59-254`), NU sub-rutare. Pozitia (tab + sectiune) se citeste din `NavigationContext.viewParams` al unicei vederi `plati-hub` si se schimba cu `setViewParams` (fara intrare noua de history). Motiv: `goBack()` restaureaza `viewParams` din history, deci revenirea din `profil-sportiv` (onViewSportiv) readuce utilizatorul pe acelasi tab — un `useState` simplu s-ar pierde la remount.
- Ordinea tab-urilor: Facturi, Încasări, Rapoarte, Configurare.
- Singura atingere a `types.ts` (fisier de infrastructura, portal-debug skill): UN literal aditiv in uniunea `View`. Tipurile locale hub-ului (TabHub etc.) stau in `platiHubConfig.ts`, ca orice interfata de props de componenta din proiect.

Output: `components/Plati/hub/platiHubConfig.ts`, `components/Plati/hub/platiHubConfig.test.ts`, literalul `'plati-hub'` in `types.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-CONTEXT.md
@CLAUDE.md
@components/AppRouter.tsx
@contexts/NavigationContext.tsx
@hooks/usePermissions.ts
@utils/perioadaGratie.test.ts
</context>

<interfaces>
HARTA DE PROPS de verificat in Task 1 (extrasa la planificare din `components/AppRouter.tsx` la commit 6edee5a). "nefiltrat" = variabila din `useData()` direct, NU din `filteredData` — trebuie pastrat exact asa.

| Vedere | AppRouter | Componenta Lazy | Props exacte | Guard actual |
|---|---|---|---|---|
| financial-dashboard | 228-229 | FinancialDashboard | onBack=handleBackToDashboard, plati=filteredData.plati, tranzactii=filteredData.tranzactii, sportivi=filteredData.sportivi, familii=filteredData.familii | isAtLeastClubAdmin |
| gestiune-facturi | 230-231 | GestiuneFacturi | onBack=(canGoBack ? goBack : handleBackToDashboard), currentUser=currentUser!, sportivi=filteredData.sportivi, plati=filteredData.plati, setPlati, setTranzactii, tipuriPlati (nefiltrat), familii=filteredData.familii, onViewSportiv, initialSportivId=viewParams?.sportivId | canManageFinances |
| facturi-fara-prezenta | 232-233 | FacturiFaraPrezenta | onBack=handleBackToDashboard, onViewSportiv | canManageFinances |
| deconturi-federatie | 234-235 | FederationInvoices | NESCHIMBAT — ramane in afara hub-ului (D-05) | isAtLeastClubAdmin |
| plati-scadente | 236-237 | PlatiScadente | onIncaseazaMultiple=handleIncaseazaMultiple, onViewSportiv, permissions, onBack=handleBackToDashboard | canManageFinances |
| jurnal-incasari | 238-239 | JurnalIncasari | currentUser!, permissions, plati=filteredData.plati, setPlati, sportivi=filteredData.sportivi, familii=filteredData.familii, preturiConfig (nefiltrat), tipuriAbonament=filteredData.tipuriAbonament, tipuriPlati (nefiltrat), setTipuriPlati, tranzactii=filteredData.tranzactii, setTranzactii, platiInitiale=platiPentruIncasare, onIncasareProcesata=handleIncasareProcesata, onBack=handleJurnalBack, reduceri (nefiltrat), onViewSportiv | canManageFinances |
| raport-financiar | 240-241 | RaportFinanciar | onBack=handleBackToDashboard, istoricPlatiDetaliat=filteredData.istoricPlatiDetaliat, sportivi=filteredData.sportivi, familii=filteredData.familii, plati=filteredData.plati, setPlati, setTranzactii, onViewSportiv | isAtLeastClubAdmin |
| tipuri-abonament | 254-255 | TipuriAbonamentManagement | onBack, tipuriAbonament=filteredData.tipuriAbonament, setTipuriAbonament, currentUser!, clubs, activeRoleContext, permissions | isAtLeastClubAdmin |
| configurare-preturi | 256-257 | ConfigurarePreturi | grade (nefiltrat), onBack | isAtLeastClubAdmin |
| reduceri | 262-263 | ReduceriManagement | onBack, reduceri (nefiltrat), setReduceri | isAtLeastClubAdmin |
| nomenclatoare | 264-265 | GestionareNomenclatoare | onBack, tipuriPlati (nefiltrat), setTipuriPlati, plati (NEFILTRAT — `plati`, nu `filteredData.plati`) | isAtLeastClubAdmin |
| familii | 266-267 | FamiliiManagement | NESCHIMBAT — ramane in afara hub-ului (D-05) | isAtLeastInstructor |
| taxe-anuale | 270-271 | TaxeAnuale | onBack, currentUser!, sportivi=filteredData.sportivi, plati=filteredData.plati, setPlati | permissions.isSuperAdmin OR permissions.isAdminClub |
| istoric-plati | 284-285 | IstoricPlati | onBack, viewedUser=currentUser!, plati=filteredData.plati, tranzactii=filteredData.tranzactii | FARA guard — rolul SPORTIV il foloseste |

Echivalenta guard-urilor: `isAtLeastClubAdmin` (AppRouter.tsx:111 = isAdminClub OR isFederationAdmin) este identic cu `canManageFinances` (hooks/usePermissions.ts:39 = isFederationAdmin OR isAdminClub). Singurele exceptii: `taxe-anuale` (exclude rolul `ADMIN` non-super) si `istoric-plati` (fara guard).

FLUXURI INTER-COMPONENTE de verificat in Task 1:
- F1 Incasare multipla: `PlatiScadente.handleIncasareClick` (PlatiScadente.tsx:783-786) → `onIncaseazaMultiple(selected)` → `AppRouter.handleIncaseazaMultiple` (72-75) = `setPlatiPentruIncasare(selected)` (state in App.tsx:50, trecut prin AppLayout.tsx:32-33,95-96) + `setActiveView('jurnal-incasari')` (push in history).
- F2 JurnalIncasari consuma `platiInitiale` (JurnalIncasari.tsx:189-213 precompletare, 320-372 procesare prin RPC `proceseaza_incasare_normalizata`); dupa succes apeleaza `onIncasareProcesata()` si apoi `setTimeout(() => onBack(), 1500)` (liniile 493-494) — onBack e invocat AUTOMAT, nu doar din buton.
- F3 `handleJurnalBack` (AppRouter.tsx:78-85): `setPlatiPentruIncasare([])`; `canGoBack ? goBack() : setActiveView('plati-scadente')`.
- F4 `handleIncasareProcesata` (AppRouter.tsx:87-89): `setPlatiPentruIncasare([])`.
- F5 `components/Grupe/GrupaDetailView.tsx:657` `navigateTo('gestiune-facturi', { sportivId })` → `viewParams.sportivId` → `GestiuneFacturi.initialSportivId`.
- F6 Link-uri adanci pe literale vechi care TREBUIE sa ramana functionale prin alias: `components/GestiuneExamene/index.tsx:273` onNavigate('gestiune-facturi'); `components/UserProfile.tsx:804` onNavigate('plati-scadente'); `components/SportivDashboard/index.tsx:588,700` onNavigate('istoric-plati') (rol SPORTIV); `components/menuConfig.ts:220` sportivMenu 'istoric-plati'; favorite/topViews AdminMasterMap (useQuickAccess, localStorage); `NavigationContext` persista activeView in localStorage cheia `phi-hau-active-view` (reload direct pe orice literal vechi).

CONTRACTUL creat de acest plan (consumat de planurile 31-04, 31-05, 31-06, 31-08):

```ts
// components/Plati/hub/platiHubConfig.ts
import type { View, User, Permissions, Plata, Sportiv } from '../../../types';

export type TabHub = 'facturi' | 'incasari' | 'rapoarte' | 'configurare';
export type SectiuneHub =
  | 'plati-scadente' | 'gestiune-facturi' | 'facturi-fara-prezenta'
  | 'jurnal-incasari' | 'istoric-plati'
  | 'raport-financiar' | 'financial-dashboard'
  | 'tipuri-abonament' | 'configurare-preturi' | 'reduceri' | 'taxe-anuale' | 'nomenclatoare';
export interface PozitieHub { tab: TabHub; sectiune: SectiuneHub; }
export type TabProfilSportiv = 'profil' | 'contact' | 'grade' | 'financiar' | 'familie' | 'grupe-istoric';

export interface PlatiHubTabProps {
  sectiune: SectiuneHub;
  currentUser: User;
  permissions: Permissions;
  activeRoleContext: any;
  onViewSportiv: (sportiv: Sportiv, tab?: TabProfilSportiv) => void;
  onBack: () => void;
}

export const TABURI_HUB: readonly TabHub[];                       // ['facturi','incasari','rapoarte','configurare']
export const ETICHETE_TABURI: Record<TabHub, string>;             // Facturi / Încasări / Rapoarte / Configurare
export const SECTIUNI_PE_TAB: Record<TabHub, readonly SectiuneHub[]>;
export const ETICHETE_SECTIUNI: Record<SectiuneHub, string>;
export const SECTIUNE_IMPLICITA: Record<TabHub, SectiuneHub>;
export const TAB_PENTRU_SECTIUNE: Record<SectiuneHub, TabHub>;
export const VEDERI_HUB: readonly View[];                         // 'plati-hub' + cele 12 sectiuni (13 total)
export function esteTabHub(x: unknown): x is TabHub;
export function esteSectiuneHub(x: unknown): x is SectiuneHub;
export function esteVedereHub(view: string): boolean;
export function rezolvaPozitieHub(activeView: View, viewParams: unknown, poateVedeaTaxeAnuale: boolean): PozitieHub;
```

Conventia de test a proiectului (NU introduce alta): fara vitest/jest; fisiere `*.test.ts` colocate, cu `assert()` propriu, `runTests()` exportat si auto-rulare gardata pe `process.argv[1]`, executate cu `npx tsx`. Model exact: `utils/perioadaGratie.test.ts`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit harta props/fluxuri + contractul de tipuri al hub-ului</name>
  <read_first>
    - components/AppRouter.tsx (integral, 314 linii — in special 72-89 si 228-285)
    - App.tsx (liniile 45-55 si 200-215 — state-ul platiPentruIncasare)
    - components/AppLayout.tsx (liniile 28-100 — transmiterea platiPentruIncasare)
    - contexts/NavigationContext.tsx (integral — setViewParams, navigateTo, goBack, persistenta activeView)
    - hooks/usePermissions.ts (integral — definitia canManageFinances)
    - components/Plati/JurnalIncasari.tsx (liniile 56-75, 175-215, 480-495, 655-665)
    - components/Plati/PlatiScadente.tsx (liniile 25-30 si 783-786)
    - components/Plati/GestiuneFacturi.tsx (liniile 21-60)
    - components/Grupe/GrupaDetailView.tsx (liniile 650-660)
    - components/GestiuneExamene/index.tsx (liniile 270-276)
    - components/UserProfile.tsx (liniile 800-806)
    - components/SportivDashboard/index.tsx (liniile 585-590 si 698-702)
    - types.ts (liniile 146-183 si 620-640 si 668 — Tranzactie, Plata, Permissions, View)
  </read_first>
  <files>components/Plati/hub/platiHubConfig.ts</files>
  <action>
Pas A — AUDIT (fara modificari de cod in afara fisierului nou): pentru fiecare rand al tabelului HARTA DE PROPS si pentru fiecare flux F1-F6 din blocul interfaces, deschide linia indicata si confirma ca numele prop-ului, sursa valorii (filteredData vs nefiltrat) si guard-ul sunt identice cu cele din tabel. Daca gasesti o divergenta (prop lipsa/in plus, alta sursa, alt guard, alta linie), codul real castiga: corecteaza harta in blocul-antet de la Pasul B si noteaza divergenta in SUMMARY la sectiunea "Divergente harta props". Confirma explicit (si noteaza in SUMMARY) trei fapte critice pentru planurile urmatoare: (1) JurnalIncasari.tsx:494 invoca automat `onBack` la 1500 ms dupa orice incasare reusita; (2) GestionareNomenclatoare primeste `plati` NEFILTRAT, iar ReduceriManagement si JurnalIncasari primesc `reduceri` NEFILTRAT; (3) `istoric-plati` nu are guard si e folosit de rolul SPORTIV.

Pas B — creeaza `components/Plati/hub/platiHubConfig.ts` cu: (1) un comentariu-antet JSDoc in romana intitulat "Harta props verificata (Faza 31, plan 01)" care reproduce tabelul verificat (o linie per vedere: vedere → componenta → props → guard) si fluxurile F1-F6, marcand clar ca planurile 31-04/05/06/08 trebuie sa transmita EXACT aceste props; (2) `import type { View, User, Permissions, Plata, Sportiv } from '../../../types';` (doar `import type` — fisierul trebuie sa ruleze sub `npx tsx` fara a incarca cod runtime din types.ts); (3) tipurile exportate `TabHub`, `SectiuneHub`, `PozitieHub`, `TabProfilSportiv` si interfata `PlatiHubTabProps` EXACT ca in contractul din blocul interfaces (daca auditul a gasit ca AppRouter trimite alt tip pentru `onViewSportiv`, aliniaza semnatura la codul real si noteaza). `Plata` este importat pentru uz in planurile de tab (extensiile TabFacturiProps / TabIncasariProps il folosesc) — daca ramane neutilizat in acest fisier, pastreaza-l in import pentru documentarea contractului sau elimina-l; tsc nu are noUnusedLocals.

Nu adauga inca constantele si functiile (Task 2). Nu modifica niciun alt fisier.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && test -f components/Plati/hub/platiHubConfig.ts && grep -c \"export type SectiuneHub\" components/Plati/hub/platiHubConfig.ts && grep -c \"export interface PlatiHubTabProps\" components/Plati/hub/platiHubConfig.ts && grep -c \"Harta props verificata\" components/Plati/hub/platiHubConfig.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `components/Plati/hub/platiHubConfig.ts` exista si contine literal `export type TabHub = 'facturi' | 'incasari' | 'rapoarte' | 'configurare'`.
    - Tipul `SectiuneHub` contine exact cele 12 literale: plati-scadente, gestiune-facturi, facturi-fara-prezenta, jurnal-incasari, istoric-plati, raport-financiar, financial-dashboard, tipuri-abonament, configurare-preturi, reduceri, taxe-anuale, nomenclatoare — si NU contine `familii` sau `deconturi-federatie` (`grep -v '^\s*\*' components/Plati/hub/platiHubConfig.ts | grep -c "'familii'"` = 0 in afara comentariului-antet).
    - `export interface PlatiHubTabProps` are campurile sectiune, currentUser, permissions, activeRoleContext, onViewSportiv, onBack.
    - Comentariul-antet contine textul "Harta props verificata" si mentiunile `JurnalIncasari.tsx:494`, `plati (NEFILTRAT` si `istoric-plati` fara guard.
    - Singurul import al fisierului este `import type ... from '../../../types'`.
    - SUMMARY-ul planului va contine sectiunile "Harta props verificata" si "Divergente harta props" (chiar si cu mentiunea "niciuna").
  </acceptance_criteria>
  <done>Harta de props si fluxurile F1-F6 sunt confirmate pe codul real si fixate ca antet de contract; tipurile hub-ului exista si compileaza.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gruparea D-01..D-05, rezolvitorul pur de pozitie, testul colocat si literalul View 'plati-hub'</name>
  <read_first>
    - components/Plati/hub/platiHubConfig.ts (contractul creat in Task 1)
    - utils/perioadaGratie.test.ts (conventia exacta de test: assert propriu, runTests, garda process.argv[1])
    - types.ts (linia 668 — uniunea View)
    - .planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-CONTEXT.md (sectiunea decisions D-01..D-05)
  </read_first>
  <files>components/Plati/hub/platiHubConfig.ts, components/Plati/hub/platiHubConfig.test.ts, types.ts</files>
  <behavior>
    - rezolvaPozitieHub('plati-hub', null, true) → { tab: 'facturi', sectiune: 'plati-scadente' }
    - rezolvaPozitieHub('plati-hub', { tab: 'rapoarte' }, true) → { tab: 'rapoarte', sectiune: 'raport-financiar' }
    - rezolvaPozitieHub('plati-hub', { tab: 'incasari', sectiune: 'jurnal-incasari' }, true) → { tab: 'incasari', sectiune: 'jurnal-incasari' }
    - rezolvaPozitieHub('plati-hub', { tab: 'facturi', sectiune: 'raport-financiar' }, true) → { tab: 'rapoarte', sectiune: 'raport-financiar' } (sectiunea castiga, tab-ul se deriva)
    - rezolvaPozitieHub('plati-hub', { sectiune: 'inexistent' }, true) → { tab: 'facturi', sectiune: 'plati-scadente' }
    - rezolvaPozitieHub('gestiune-facturi', { sportivId: 'x' }, true) → { tab: 'facturi', sectiune: 'gestiune-facturi' }
    - rezolvaPozitieHub('plati-scadente', { tab: 'rapoarte', sectiune: 'financial-dashboard' }, true) → { tab: 'rapoarte', sectiune: 'financial-dashboard' } (viewParams castiga peste literalul vechi)
    - rezolvaPozitieHub('istoric-plati', null, true) → { tab: 'incasari', sectiune: 'istoric-plati' }
    - rezolvaPozitieHub('nomenclatoare', null, true) → { tab: 'configurare', sectiune: 'nomenclatoare' }
    - rezolvaPozitieHub('taxe-anuale', null, true) → { tab: 'configurare', sectiune: 'taxe-anuale' }
    - rezolvaPozitieHub('taxe-anuale', null, false) → { tab: 'configurare', sectiune: 'tipuri-abonament' } (fallback fara permisiune)
    - rezolvaPozitieHub('dashboard', null, true) → { tab: 'facturi', sectiune: 'plati-scadente' }
    - Fiecare din cele 12 sectiuni se mapeaza pe tab-ul cerut de D-01..D-04 (verificare pe tot TAB_PENTRU_SECTIUNE)
    - VEDERI_HUB are exact 13 elemente; esteVedereHub('familii') === false; esteVedereHub('deconturi-federatie') === false; esteVedereHub('plati-hub') === true (D-05)
  </behavior>
  <action>
In `components/Plati/hub/platiHubConfig.ts`, adauga sub tipuri:

- `TABURI_HUB = ['facturi', 'incasari', 'rapoarte', 'configurare'] as const` (tipizat readonly TabHub[]).
- `ETICHETE_TABURI`: facturi → 'Facturi', incasari → 'Încasări', rapoarte → 'Rapoarte', configurare → 'Configurare'.
- `SECTIUNI_PE_TAB` (gruparea LOCKED): facturi → ['plati-scadente', 'gestiune-facturi', 'facturi-fara-prezenta'] (per D-01); incasari → ['jurnal-incasari', 'istoric-plati'] (per D-02); rapoarte → ['raport-financiar', 'financial-dashboard'] (per D-03); configurare → ['tipuri-abonament', 'configurare-preturi', 'reduceri', 'taxe-anuale', 'nomenclatoare'] (per D-04). Ordinea din fiecare lista e ordinea pastilelor in UI.
- `ETICHETE_SECTIUNI`: 'plati-scadente' → 'Facturi & Plăți', 'gestiune-facturi' → 'Gestiune Facturi', 'facturi-fara-prezenta' → 'Facturi fără Prezență', 'jurnal-incasari' → 'Jurnal Încasări', 'istoric-plati' → 'Istoric Plăți Personale', 'raport-financiar' → 'Raport Financiar', 'financial-dashboard' → 'Dashboard Financiar', 'tipuri-abonament' → 'Config. Abonamente', 'configurare-preturi' → 'Configurare Prețuri', 'reduceri' → 'Reduceri', 'taxe-anuale' → 'Taxe Anuale', 'nomenclatoare' → 'Nomenclatoare (Tipuri Plăți)'. ('Istoric Plăți Personale' pentru ca IstoricPlati afiseaza platile utilizatorului logat — comportament neschimbat fata de vederea veche.)
- `SECTIUNE_IMPLICITA`: primul element al fiecarei liste din SECTIUNI_PE_TAB.
- `TAB_PENTRU_SECTIUNE`: derivat programatic din SECTIUNI_PE_TAB (nu scris de mana), tipizat Record<SectiuneHub, TabHub>.
- `VEDERI_HUB`: `['plati-hub', ...toate cele 12 sectiuni]` tipizat readonly View[] — 13 elemente; NU include 'familii' si 'deconturi-federatie' (per D-05).
- `esteTabHub(x)`: true daca x e string si apartine TABURI_HUB. `esteSectiuneHub(x)`: true daca x e string si e cheie in TAB_PENTRU_SECTIUNE. `esteVedereHub(view)`: `VEDERI_HUB.includes(view)`.
- `rezolvaPozitieHub(activeView, viewParams, poateVedeaTaxeAnuale)`: citeste defensiv `viewParams` (poate fi null, non-obiect sau poate avea alte chei ca `sportivId`). Precedenta: (1) daca `viewParams.sectiune` e SectiuneHub valida → { tab: TAB_PENTRU_SECTIUNE[sectiune], sectiune }; (2) altfel daca `viewParams.tab` e TabHub valid → { tab, sectiune: SECTIUNE_IMPLICITA[tab] }; (3) altfel daca `activeView` e SectiuneHub (literal vechi) → { tab: TAB_PENTRU_SECTIUNE[activeView], sectiune: activeView }; (4) altfel { tab: 'facturi', sectiune: 'plati-scadente' }. La final: daca sectiunea rezultata e 'taxe-anuale' si `poateVedeaTaxeAnuale` e false → { tab: 'configurare', sectiune: 'tipuri-abonament' } (pastreaza guard-ul vechi `isSuperAdmin || isAdminClub` al vederii taxe-anuale). Functie pura: fara React, fara Supabase, fara acces la window/localStorage.

Creeaza `components/Plati/hub/platiHubConfig.test.ts` copiind structura din `utils/perioadaGratie.test.ts` (antet NOTĂ despre lipsa vitest, `assert(condition, message)` propriu, `runTests()` exportat care intoarce { passed, failed, errors }, garda de auto-rulare `process.argv[1]?.endsWith('platiHubConfig.test.ts')` cu `process.exit(1)` la erori). Implementeaza cate un caz `run('Tn: ...')` pentru fiecare rand din <behavior>; pentru verificarea D-01..D-04 itereaza toate cele 12 sectiuni si compara cu o harta asteptata scrisa explicit in test. Comparatiile de obiecte se fac pe campuri (`r.tab === ... && r.sectiune === ...`).

In `types.ts` linia 668, adauga literalul `'plati-hub'` in uniunea `View` (aditiv — de exemplu imediat dupa `'plati-scadente'`). NU elimina si NU redenumi niciun literal existent. Aceasta este singura modificare in `types.ts` din toata faza.

Ordinea TDD: scrie intai testul (ruleaza-l — trebuie sa pice pentru ca functiile lipsesc), apoi implementarea, apoi re-ruleaza pana trece.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npx tsx components/Plati/hub/platiHubConfig.test.ts && npm run lint"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx components/Plati/hub/platiHubConfig.test.ts` iese cu cod 0 si afiseaza "0 FAIL".
    - `npm run lint` (tsc --noEmit) iese cu cod 0.
    - `grep -c "'plati-hub'" types.ts` = 1 si toate literalele vechi sunt inca prezente (`grep -o "'plati-scadente'\|'gestiune-facturi'\|'facturi-fara-prezenta'\|'jurnal-incasari'\|'istoric-plati'\|'raport-financiar'\|'financial-dashboard'\|'tipuri-abonament'\|'configurare-preturi'\|'reduceri'\|'taxe-anuale'\|'nomenclatoare'\|'familii'\|'deconturi-federatie'" types.ts | sort -u | wc -l` = 14).
    - `platiHubConfig.ts` exporta `rezolvaPozitieHub`, `VEDERI_HUB`, `esteVedereHub`, `SECTIUNI_PE_TAB`, `TAB_PENTRU_SECTIUNE`, `SECTIUNE_IMPLICITA`, `ETICHETE_TABURI`, `ETICHETE_SECTIUNI`, `TABURI_HUB`, `esteTabHub`, `esteSectiuneHub`.
    - `platiHubConfig.ts` nu contine `supabase`, `useState`, `localStorage` (`grep -c "supabase\|useState\|localStorage" components/Plati/hub/platiHubConfig.ts` = 0 in afara comentariilor — verificat cu `grep -v '^\s*\*\|^\s*//' ... | grep -c ...` = 0).
    - Testul contine cel putin 14 apeluri `run(`.
  </acceptance_criteria>
  <done>Gruparea D-01..D-05 si rezolvitorul de pozitie sunt codificate intr-o singura sursa, acoperite de test tsx verde; `'plati-hub'` e un View valid; tsc curat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewParams (NavigationContext, setat de orice componenta) → rezolvaPozitieHub | Parametrii de navigare pot contine orice valoare (inclusiv chei straine ca `sportivId` sau valori invalide) |
| localStorage `phi-hau-active-view` → activeView | Utilizatorul poate edita manual literalul persistat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | rezolvaPozitieHub primeste viewParams/activeView arbitrare | mitigate | Validare stricta prin `esteTabHub`/`esteSectiuneHub` (liste inchise); orice valoare necunoscuta cade pe pozitia implicita facturi/plati-scadente; acoperit de testul T5 ('inexistent') |
| T-31-01-02 | Elevation of Privilege | Deep-link `{ sectiune: 'taxe-anuale' }` pentru rolul ADMIN (ne-super) care inainte primea AccessDenied | mitigate | Parametrul `poateVedeaTaxeAnuale` forteaza fallback pe 'tipuri-abonament'; guard-ul real ramane RLS la nivel DB |
| T-31-01-03 | Denial of Service | Scoaterea literalelor vechi din View ar rupe reload-ul pe localStorage si favoritele | mitigate | Literalele vechi raman in uniune; acceptance verifica numarul lor (14) |
| T-31-01-SC | Tampering | npm installs | accept | Zero pachete noi |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Inventarul complet al fazei 31 (planul care creeaza fiecare simbol e indicat in paranteza):

- View literal nou: `'plati-hub'` in `types.ts` (31-01) — singura modificare a `types.ts` din faza.
- `components/Plati/hub/platiHubConfig.ts` (31-01): tipuri `TabHub`, `SectiuneHub`, `PozitieHub`, `TabProfilSportiv`, `PlatiHubTabProps`; constante `TABURI_HUB`, `ETICHETE_TABURI`, `SECTIUNI_PE_TAB`, `ETICHETE_SECTIUNI`, `SECTIUNE_IMPLICITA`, `TAB_PENTRU_SECTIUNE`, `VEDERI_HUB`; functii `esteTabHub`, `esteSectiuneHub`, `esteVedereHub`, `rezolvaPozitieHub`.
- `components/Plati/hub/platiHubConfig.test.ts` (31-01): `runTests()`.
- `utils/facturaDetaliu.ts` (31-03): `CAMPURI_EDITABILE_PLATA`, tipuri `SumarFactura`, `RandIstoricTranzactie`, `PlanAchitare`, `FormEditareFactura`; functii `calculeazaSumarFactura`, `construiesteIstoricTranzactii`, `planificaAchitareRapida`, `construiestePayloadEditareFactura`.
- `utils/facturaDetaliu.test.ts` (31-03): `runTests()`.
- `components/Plati/FacturaDetaliu.tsx` (31-03): componenta `FacturaDetaliu` (props `plataId: string | null`, `onClose: () => void`).
- `components/Plati/hub/TabFacturi.tsx` (31-04): `TabFacturi`, `TabFacturiProps`.
- `components/Plati/hub/TabIncasari.tsx` (31-05): `TabIncasari`, `TabIncasariProps`.
- `components/Plati/hub/TabRapoarte.tsx` (31-05): `TabRapoarte`.
- `components/Plati/hub/TabConfigurare.tsx` (31-06): `TabConfigurare`.
- `components/Plati/hub/PlatiHub.tsx` (31-08): `PlatiHub`, `PlatiHubProps`; export lazy `PlatiHub` in `components/LazyComponents.tsx` (31-08).
- Props optionale noi (API existent neschimbat): `hideBackButton?: boolean` pe PlatiScadente, GestiuneFacturi, FacturiFaraPrezenta (31-04), JurnalIncasari, IstoricPlati, FinancialDashboard (31-05), TipuriAbonamentManagement, ConfigurarePreturi, ReduceriManagement, TaxeAnuale, GestionareNomenclatoare (31-06); `onDeschideDetalii?: (plata: Plata) => void` pe PlatiScadente si GestiuneFacturi (31-04); `onDeschideDetalii?: (plataId: string) => void` pe FinanciarTab (31-07).
- Rutare: `case 'plati-hub'` + 11 alias-uri legacy + ramura `istoric-plati` in `components/AppRouter.tsx` (31-08).
- Intrari: card/meniu `'plati-hub'` in AdminMasterMap, menuConfig, UnifiedDashboard, ReportsDashboard; titlu Header; harti AI (31-09).

Acest plan (31-01) produce: literalul `'plati-hub'`, `platiHubConfig.ts`, `platiHubConfig.test.ts`.
</artifacts_this_phase_produces>

<verification>
1. `npx tsx components/Plati/hub/platiHubConfig.test.ts` — cod 0.
2. `npm run lint` — cod 0.
3. Antetul "Harta props verificata" exista in platiHubConfig.ts si SUMMARY contine sectiunile de audit.
4. `types.ts` are 'plati-hub' + toate cele 14 literale financiare vechi.
</verification>

<success_criteria>
- Auditul de props/fluxuri e facut si fixat inainte de orice atingere a AppRouter.
- Gruparea tab-urilor D-01..D-04 si excluderea D-05 exista intr-o singura sursa, testata.
- Contractul `PlatiHubTabProps` e gata de consumat de planurile de tab (wave 2).
</success_criteria>

<output>
Creeaza `.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-01-SUMMARY.md` cu sectiunile obligatorii "Harta props verificata" (tabelul final) si "Divergente harta props" (sau "niciuna").
</output>
