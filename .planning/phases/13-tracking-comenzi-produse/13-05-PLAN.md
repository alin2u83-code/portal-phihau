---
phase: 13-tracking-comenzi-produse
plan: 05
type: execute
wave: 5
# DECIZIE depends_on (revizuire checker Warning 3): 13-04 modifică DOAR
# services/comenziService.ts, components/Produse/ComenziProduse/index.tsx și
# FederatieComandaView.tsx — NU atinge PredareModal.tsx sau ComandaCard.tsx.
# 13-05 modifică ComandaCard.tsx, PredareModal.tsx, RaportProduse.tsx, index.tsx,
# exportBonPredare.ts. Nu există suprapunere de fișiere între 13-04 și 13-05,
# deci nu se adaugă "13-04" la depends_on; rămâne doar "13-03" (sursa reală a
# fișierelor PredareModal/ComandaCard pe care 13-05 le extinde).
depends_on: ["13-03"]
files_modified:
  - utils/exportBonPredare.ts
  - components/Produse/ComenziProduse/ComandaCard.tsx
  - components/Produse/ComenziProduse/PredareModal.tsx
  - components/Produse/RaportProduse.tsx
  - components/Produse/index.tsx
autonomous: true
requirements: [CMD-07, CMD-08]
user_setup: []
must_haves:
  truths:
    - "utils/exportBonPredare.ts exportă exportBonPredare (PDF A5 per sportiv) și exportExcelFurnizor (Excel produse+cantități) (per CMD-07)"
    - "La predare (PredareModal) sau din ComandaCard, adminul poate descărca PDF bon predare pentru sportiv (per CMD-07)"
    - "Din ComandaCard, adminul poate descărca Excel cu lista produse+cantități pentru furnizor când comanda e PLASATA (per CMD-07)"
    - "RaportProduse afișează secțiune extinsă cu metrici comenzi (total cereri, total predate, total plătite, valoare restantă) și export Excel cu sheet Comenzi (per CMD-08)"
  artifacts:
    - path: "utils/exportBonPredare.ts"
      provides: "Export PDF bon predare + Excel furnizor"
      exports: ["exportBonPredare", "exportExcelFurnizor"]
    - path: "components/Produse/RaportProduse.tsx"
      provides: "Raport extins cu date comenzi"
      contains: "cereri"
  key_links:
    - from: "components/Produse/ComenziProduse/PredareModal.tsx"
      to: "utils/exportBonPredare.ts"
      via: "import exportBonPredare"
      pattern: "exportBonPredare"
    - from: "components/Produse/index.tsx"
      to: "components/Produse/RaportProduse.tsx"
      via: "prop cereri"
      pattern: "cereri="
---

<objective>
Adaugă exportul de documente: PDF bon predare per sportiv (la predare), Excel cu produse+cantități pentru furnizor (din comandă PLASATĂ), și extinde RaportProduse.tsx cu date de comenzi (metrici + sheet Excel suplimentar). Cablează exportul în UI-ul tab-ului Comenzi și în Raport.

Purpose: Închide cerințele de export (CMD-07) și raportare (CMD-08), refolosind pattern-urile jsPDF/xlsx deja validate în Phase 9 și Phase 12-05.
Output: Utilitar export + integrare în PredareModal/ComandaCard + raport extins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tracking-comenzi-produse/13-RESEARCH.md
@.planning/phases/13-tracking-comenzi-produse/13-PATTERNS.md
@components/Produse/RaportProduse.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: utils/exportBonPredare.ts — PDF bon + Excel furnizor</name>
  <read_first>
    - components/Produse/RaportProduse.tsx (liniile 96-160 — pattern dinamic import xlsx și jsPDF+autoTable, structura doc.text/autoTable, stiluri headStyles)
    - types.ts (CerereProdusFull, ComandaProduseiFull — pentru semnături)
  </read_first>
  <files>utils/exportBonPredare.ts</files>
  <action>
    Creează utils/exportBonPredare.ts (pattern PATTERNS.md liniile 593-658, identic cu RaportProduse.tsx liniile 96-160):

    - `exportBonPredare(cerere: CerereProdusFull, clubNume: string): Promise<void>` (per CMD-07 — PDF bon predare per sportiv):
      - import dinamic: `const { jsPDF } = await import('jspdf'); const { default: autoTable } = await import('jspdf-autotable');`
      - doc A5 portrait. Header bold: "Bon Predare Echipament" + clubNume. Subtext: Sportiv (cerere.sportiv_nume), Data (toLocaleDateString ro-RO).
      - autoTable: head ['Produs','Variantă','Cantitate','Preț (RON)'], body cu denumire, culoare+mărime, cantitate, suma totală (pret_vanzare * cantitate). Stiluri din RaportProduse (fillColor [30,41,59]).
      - doc.save(`bon-predare-${cerere.id.slice(0,8)}.pdf`).

    - `exportExcelFurnizor(comanda: ComandaProduseiFull, clubNume: string): Promise<void>` (per CMD-07 — Excel produse+cantități furnizor):
      - import dinamic xlsx: `const { utils, writeFile } = await import('xlsx');`
      - rows din comanda.iteme SAU agregat din comanda.cereri (cantitate totală per variantă): { Produs, Variantă, Cantitate, Club }. Dacă comanda.iteme e gol, agregă din cereri cu reduce.
      - json_to_sheet + book_new + book_append_sheet('Comenzi Furnizor') + writeFile(`comanda-furnizor-${comanda.id.slice(0,8)}.xlsx`).

    Error handling: try/catch în UI-ul apelant (utilitarul poate arunca).
  </action>
  <acceptance_criteria>
    - utils/exportBonPredare.ts exportă exportBonPredare și exportExcelFurnizor.
    - Ambele folosesc import dinamic (await import) pentru jspdf/xlsx.
    - npx tsc --noEmit trece.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export async function exportBonPredare" utils/exportBonPredare.ts && grep -q "export async function exportExcelFurnizor" utils/exportBonPredare.ts && grep -q "await import('jspdf')" utils/exportBonPredare.ts && grep -q "await import('xlsx')" utils/exportBonPredare.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Utilitarul de export PDF bon predare și Excel furnizor există și compilează.</done>
</task>

<task type="auto">
  <name>Task 2: Cablare export în PredareModal + ComandaCard</name>
  <read_first>
    - components/Produse/ComenziProduse/PredareModal.tsx (din 13-03 — handleConfirm, props cerere)
    - components/Produse/ComenziProduse/ComandaCard.tsx (din 13-03 — render comandă, stare, butoane, inclusiv butonul Reminder plată)
    - utils/exportBonPredare.ts (Task 1)
    - components/ui.tsx (Button)
  </read_first>
  <files>components/Produse/ComenziProduse/PredareModal.tsx, components/Produse/ComenziProduse/ComandaCard.tsx</files>
  <action>
    1. PredareModal.tsx: după predarea reușită (în handleConfirm, după marcheazaPredare), oferă buton/auto-descărcare "Descarcă bon predare (PDF)" care apelează exportBonPredare(cerere, clubNume). Adaugă prop clubNume la PredareModal (propagat din ComandaCard). Wrap în try/catch cu toast pe eroare (per CMD-07).

    2. ComandaCard.tsx: adaugă buton "Export furnizor (Excel)" vizibil când comanda.stare === 'PLASATA' (per CMD-07 — Excel când comanda e PLASATĂ), care apelează exportExcelFurnizor(comanda, clubNume). Adaugă și buton "Bon predare" pe cererile cu stare PREDATA/PLATITA care apelează exportBonPredare pentru cererea respectivă. Propagă clubNume prin props (de la ComenziProduse → ComandaCard → PredareModal). NU elimina butonul "Reminder plată" adăugat în 13-03 — coexistă cu butonul "Bon predare" pe cererile PREDATA.
  </action>
  <acceptance_criteria>
    - PredareModal importă și apelează exportBonPredare.
    - ComandaCard importă și apelează exportExcelFurnizor (condiționat pe stare PLASATA) și exportBonPredare.
    - ComandaCard păstrează butonul trimiteReminderPlata din 13-03.
    - npx tsc --noEmit trece.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "exportBonPredare" components/Produse/ComenziProduse/PredareModal.tsx && grep -q "exportExcelFurnizor" components/Produse/ComenziProduse/ComandaCard.tsx && grep -q "PLASATA" components/Produse/ComenziProduse/ComandaCard.tsx && grep -q "trimiteReminderPlata" components/Produse/ComenziProduse/ComandaCard.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Adminul poate descărca bon predare PDF și Excel furnizor din UI-ul comenzilor; butonul reminder plată din 13-03 e păstrat.</done>
</task>

<task type="auto">
  <name>Task 3: RaportProduse extins cu date comenzi</name>
  <read_first>
    - components/Produse/RaportProduse.tsx (liniile 19-22 props interface, 26-41 StatItem, 53-94 calcule reduce, 96-122 export Excel, 260-280 render statistici)
    - components/Produse/index.tsx (linia 530 — render tab raport, locul unde se pasează vanzari; state comenzi/cereri din 13-03)
    - types.ts (CerereProdusFull)
  </read_first>
  <files>components/Produse/RaportProduse.tsx, components/Produse/index.tsx</files>
  <action>
    1. RaportProduse.tsx (pattern PATTERNS.md liniile 561-583, per CMD-08):
    - Extinde interfața RaportProduseProps (linia 19) cu `cereri?: CerereProdusFull[]`.
    - Adaugă filtrare cereri pe perioada selectată (startDate/endDate, pe created_at) cu useMemo, similar cu vanzariFiltrate.
    - Calcule metrici comenzi cu reduce/filter: totalCereri, totalPredate (stare PREDATA sau PLATITA), totalPlatite (stare PLATITA), valoareRestanta (cereri PREDATA cu !platit_dupa_predare → sum pret_vanzare*cantitate).
    - Secțiune nouă "Comenzi" în render (sub statisticile de vânzări) cu StatItem-uri pentru cele 4 metrici + un tabel cereri grupate pe produs cu stare.
    - Extinde exportul Excel existent (liniile 96-122) cu un sheet suplimentar "Comenzi" (book_append_sheet) conținând cererile pe perioadă (Produs, Variantă, Sportiv, Cantitate, Stare, Dată).

    2. components/Produse/index.tsx: la render-ul tab-ului raport (linia ~530), pasează prop `cereri={cereri}` (state-ul deja existent din 13-03). Asigură-te că cereri se încarcă și când activeTab === 'raport' (extinde useEffect lazy să fetch cereri și pentru raport, sau refolosește state-ul deja populat).
  </action>
  <acceptance_criteria>
    - RaportProduse.tsx interfața conține cereri?: CerereProdusFull[].
    - RaportProduse calculează metrici comenzi (totalPredate, valoareRestanta).
    - Exportul Excel include sheet-ul "Comenzi".
    - components/Produse/index.tsx pasează cereri către RaportProduse.
    - npx tsc --noEmit trece.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "cereri" components/Produse/RaportProduse.tsx && grep -q "valoareRestanta\|totalPredate" components/Produse/RaportProduse.tsx && grep -q "cereri=" components/Produse/index.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Raportul afișează metrici comenzi și exportă sheet Comenzi; tsc trece.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→fișier | Generare PDF/Excel in-browser din date deja încărcate (filtrate de RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-15 | Information Disclosure | export conține date din alt club | mitigate | datele provin din fetch-uri RLS-filtrate (cereri/comenzi club curent) |
| T-13-16 | Tampering | bon predare cu sumă incorectă | accept | suma derivată din pret_vanzare*cantitate, aceeași sursă ca factura |
</threat_model>

<verification>
- PDF bon predare și Excel furnizor se descarcă din UI comenzi.
- RaportProduse afișează metrici comenzi + sheet Excel Comenzi.
- npx tsc --noEmit trece.
</verification>

<success_criteria>
- Export PDF bon predare + Excel furnizor funcțional (CMD-07).
- Raport lunar extins cu date comenzi (CMD-08).
</success_criteria>

<output>
Create `.planning/phases/13-tracking-comenzi-produse/13-05-SUMMARY.md` when done
</output>
