---
phase: 25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui.tsx
  - components/Grupe/index.tsx
  - components/Prezenta/GrupeList.tsx
  - components/Plati/TipuriAbonament.tsx
autonomous: true
requirements: [MCLB-07]
must_haves:
  truths:
    - "Un club nou, fara nicio grupa, vede pe ecranul Grupe un mesaj explicativ si un buton care deschide direct formularul de adaugare grupa"
    - "Acelasi club, in modulul Prezenta tab Grupe, vede un mesaj explicativ si un buton care duce la modulul Grupe, nu o pagina goala"
    - "Acelasi club, in Tipuri Abonament, vede un mesaj explicativ si un buton care duce la formularul de definire abonament, atat pe mobil cat si pe desktop"
    - "Empty-state-ul apare doar dupa incarcare, nu in timpul loading-ului si nu la eroare"
  artifacts:
    - path: "components/ui.tsx"
      provides: "Componenta reutilizabila EmptyState (mesaj + descriere + CTA optional)"
      contains: "export const EmptyState"
    - path: "components/Grupe/index.tsx"
      provides: "Empty state cu CTA Adauga Grupa pe lista de grupe"
      contains: "EmptyState"
    - path: "components/Prezenta/GrupeList.tsx"
      provides: "Empty state cu CTA catre modulul Grupe"
      contains: "EmptyState"
    - path: "components/Plati/TipuriAbonament.tsx"
      provides: "Empty state cu CTA pe ambele variante (mobil + desktop)"
      contains: "EmptyState"
  key_links:
    - from: "components/Grupe/index.tsx"
      to: "components/ui.tsx EmptyState"
      via: "import + randare conditionata pe grupe.length === 0"
      pattern: "EmptyState"
    - from: "components/Prezenta/GrupeList.tsx"
      to: "contexts/NavigationContext navigateTo('grupe')"
      via: "CTA din empty state"
      pattern: "navigateTo\\('grupe'\\)"
---

<objective>
Creeaza o componenta `EmptyState` reutilizabila in design system-ul intern (`components/ui.tsx`) si aplic-o pe cele 3 ecrane-lista principale ale modulelor Grupe, Prezenta si Abonamente, ca un club nou (zero date) sa primeasca mesaj explicativ + buton de actiune, nu tabel gol sau ecran gol.

Purpose: D-05 din CONTEXT.md. La sezonul nou intra cluburi cu baza de date goala; azi ecranul Prezenta/Grupe afiseaza un grid gol fara niciun indiciu, iar Grupe si TipuriAbonament afiseaza doar text italic fara CTA.
Output: componenta noua `EmptyState` + 3 ecrane care o folosesc.

Nu depinde de si nu atinge nimic din migratia RLS (25-01 / 25-04) — fisiere complet disjuncte, ruleaza in paralel.
</objective>

## Cerinte acoperite de acest plan

| REQ | Descriere | Sursa |
|-----|-----------|-------|
| MCLB-07 | Empty-state cu mesaj + CTA pe ecranele principale Grupe / Prezenta / TipuriAbonament pentru club fara date | CONTEXT D-05, RESEARCH "Pattern 3" (nu exista EmptyState azi in `ui.tsx`) |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-CONTEXT.md
@.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-RESEARCH.md
@CLAUDE.md
</context>

## Constrangeri de proiect (din CLAUDE.md)

- Design system intern `components/ui.tsx` — fara Shadcn, fara MUI, fara librarii noi.
- Tailwind direct in JSX, fara fisiere CSS separate; foloseste tokenii de tema existenti (`var(--t-surface-2)`, `var(--t-border)`, `var(--t-text-muted)`) unde componentele vecine ii folosesc deja.
- Denumiri de domeniu in romana (mesajele catre user), pattern-uri tehnice in engleza (numele componentei, props).
- Tipurile componentei stau langa componenta in `ui.tsx` (pattern-ul existent: `interface ButtonProps`, `interface CardProps`), nu in `types.ts` (acolo stau tipurile de domeniu).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adauga componenta EmptyState in components/ui.tsx</name>
  <files>components/ui.tsx</files>
  <read_first>
    - `components/ui.tsx` — liniile 1-20 (importuri, `ButtonProps`), 284-300 (`Card`), 300-344 (`StatCard`, pentru conventia de props cu icon), 642-646 (`Skeleton`)
    - `.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-RESEARCH.md` — sectiunea "Pattern 3: Empty State cu CTA (de creat)"
  </read_first>
  <action>
Adauga in `components/ui.tsx`, dupa componenta `Card`, o interfata `EmptyStateProps` si o componenta exportata `EmptyState`.

Props: `icon` (ReactNode optional), `title` (string, obligatoriu), `description` (string optional), `actionLabel` (string optional), `onAction` (functie fara argumente, optional), `className` (string optional).

Comportament: randeaza un `Card` centrat, cu padding vertical generos, care afiseaza in ordine iconita (daca exista), titlul cu accent de text principal, descrierea cu text secundar si latime limitata pentru lizibilitate, si — numai daca AMBELE `actionLabel` si `onAction` sunt furnizate — un `Button` cu `variant="info"` care apeleaza `onAction`. Fara `actionLabel`/`onAction` componenta ramane un mesaj simplu, ca sa poata fi refolosita si pe ecrane read-only.

Stilizare: refoloseste `Card` existent si clasele Tailwind din vecinatate; culorile de text prin tokenii de tema deja folositi in fisier (`var(--t-text-muted)` pentru descriere), fara culori hardcodate noi in afara paletei folosite in `ui.tsx`. Nu introduce animatii noi si nu modifica nicio componenta existenta din fisier.

Componenta nu are stare proprie, nu face fetch si nu apeleaza `supabase` — este pur prezentationala.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>grep -c "export const EmptyState" components/ui.tsx | grep -qx 1 &amp;&amp; grep -q "interface EmptyStateProps" components/ui.tsx &amp;&amp; echo EMPTYSTATE_EXPORTED</automated>
    <automated>grep -A 25 "export const EmptyState" components/ui.tsx | grep -c "supabase\|useState\|useEffect" | grep -qx 0 &amp;&amp; echo PRESENTATIONAL_ONLY</automated>
  </verify>
  <acceptance_criteria>
    - `components/ui.tsx` exporta `EmptyState` si defineste `EmptyStateProps` cu exact props-urile: `icon`, `title`, `description`, `actionLabel`, `onAction`, `className`.
    - Butonul de actiune se randeaza doar cand `actionLabel` si `onAction` sunt ambele definite (conditie explicita in JSX).
    - `npm run lint` (tsc --noEmit) trece fara erori noi.
    - Componenta nu contine `useState`, `useEffect` sau referinte la `supabase`.
    - Nicio alta componenta existenta din `ui.tsx` nu e modificata (diff-ul atinge doar blocul nou adaugat).
  </acceptance_criteria>
  <done>EmptyState exista in design system-ul intern si compileaza.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Aplica EmptyState pe Grupe si pe lista de grupe din Prezenta</name>
  <files>components/Grupe/index.tsx, components/Prezenta/GrupeList.tsx</files>
  <read_first>
    - `components/Grupe/index.tsx` — liniile 36-46 (sursa `grupe` si `grupeLoading`), 220-240 (starea de loading, care NU trebuie atinsa), 272-282 (blocul curent `grupe.length > 0 ? ... : <Card>...` care se inlocuieste), 135 (`handleOpenAdd`)
    - `components/Prezenta/GrupeList.tsx` — tot fisierul (79 linii); gridul de la linia 24 nu are azi nicio ramura pentru lista goala
    - `components/Prezenta/index.tsx` — liniile 173-180 si 307-326 (cum e folosit `useNavigation()` ca `navTo` si cum e randat `GrupeList`), pentru a confirma ca `navigateTo('grupe')` e ruta catre modulul Grupe
    - `components/AppRouter.tsx` linia ~209 — confirma ca `'grupe'` e un `activeView` valid
  </read_first>
  <action>
In `components/Grupe/index.tsx`: importa `EmptyState` din `../ui` (adauga-l la importul existent, nu crea un import nou separat) si inlocuieste blocul de fallback de la ramura `grupe.length > 0 ? ... :` — azi un `Card` cu textul italic "Nicio grupa definita pentru acest club." — cu `EmptyState` avand titlu "Nicio grupă creată încă", descriere care explica in romana ca grupele organizeaza sportivii pe zile si ore de antrenament si ca prezenta se ia pe grupa, `actionLabel` "Adaugă Grupă" si `onAction` pointat pe `handleOpenAdd` (aceeasi functie folosita de butonul din header). Foloseste `UsersIcon` (deja importat in fisier) ca `icon`. Nu modifica ramura de loading de la linia 220 — empty-state-ul trebuie sa apara doar dupa ce `grupeLoading` e false.

In `components/Prezenta/GrupeList.tsx`: importa `EmptyState` din `../ui` si `useNavigation` din `../../contexts/NavigationContext`. Randeaza conditionat: daca `grupe.length === 0`, in locul gridului afiseaza `EmptyState` cu titlu "Nicio grupă disponibilă", descriere care explica in romana ca prezenta se ia pe grupe si ca trebuie creata intai o grupa in modulul Grupe & Orar, `actionLabel` "Creează prima grupă" si `onAction` care apeleaza `navigateTo('grupe')` din `useNavigation()`. Pastreaza neschimbat headerul cu titlul "Management Prezență" si butonul "Istoric Global" — empty-state-ul inlocuieste doar gridul de carduri. NU adauga props noi la `GrupeList` si NU modifica `components/Prezenta/index.tsx` (fisierul apartine planului 25-03; navigarea se face din interiorul `GrupeList` prin context).
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>grep -q "EmptyState" components/Grupe/index.tsx &amp;&amp; grep -q "handleOpenAdd" components/Grupe/index.tsx &amp;&amp; test $(grep -c "Nicio grupă definită pentru acest club" components/Grupe/index.tsx) -eq 0 &amp;&amp; echo GRUPE_EMPTYSTATE_OK</automated>
    <automated>grep -q "EmptyState" components/Prezenta/GrupeList.tsx &amp;&amp; grep -q "grupe.length === 0" components/Prezenta/GrupeList.tsx &amp;&amp; grep -q "navigateTo('grupe')" components/Prezenta/GrupeList.tsx &amp;&amp; echo PREZENTA_EMPTYSTATE_OK</automated>
    <automated>git diff --name-only | grep -c "components/Prezenta/index.tsx" | grep -qx 0 &amp;&amp; echo NO_CROSS_PLAN_FILE_TOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - Cu `grupe` gol, `components/Grupe/index.tsx` randeaza `EmptyState` cu `actionLabel` "Adaugă Grupă" legat de `handleOpenAdd` (aceeasi functie ca butonul din header, deci deschide `GrupaFormModal`).
    - Textul vechi "Nicio grupă definită pentru acest club." nu mai exista in fisier.
    - Ramura de loading (skeleton-urile de la linia ~220) e neschimbata — empty-state-ul nu se poate afisa cat timp `grupeLoading` e true.
    - `components/Prezenta/GrupeList.tsx` randeaza `EmptyState` cand `grupe.length === 0` si CTA-ul apeleaza `navigateTo('grupe')`.
    - Signature-ul props al `GrupeList` e neschimbat (`onSelect`, `onSelectToday`, `onGlobalHistory`, `grupe`).
    - `components/Prezenta/index.tsx` NU apare in diff-ul acestui plan.
    - `npm run lint` trece fara erori noi.
  </acceptance_criteria>
  <done>Ambele ecrane de grupe (modulul Grupe si tabul Grupe din Prezenta) au empty state cu CTA functional.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Aplica EmptyState pe Tipuri Abonament (mobil + desktop)</name>
  <files>components/Plati/TipuriAbonament.tsx</files>
  <read_first>
    - `components/Plati/TipuriAbonament.tsx` — liniile 1-36 (importuri, props, `isMobile`), 120-145 (cardul "Definește Abonament Nou" cu inputul de denumire si butonul "Adaugă în Listă"), 188-191 (fallback-ul curent pe varianta mobila), 234 (fallback-ul curent pe varianta desktop)
    - `components/ui.tsx` — semnatura `EmptyState` adaugata in Task 1 si `Input` (forwardRef, linia ~570) pentru a sti ca inputul accepta `ref`
  </read_first>
  <action>
In `components/Plati/TipuriAbonament.tsx`, importa `EmptyState` din `../ui` (adauga-l la importul existent) si creeaza un `useRef` pentru inputul de denumire din formularul "Definește Abonament Nou". Ataseaza ref-ul la `Input`-ul de denumire (componenta `Input` din `ui.tsx` e `React.forwardRef`, deci accepta `ref` direct).

Inlocuieste AMBELE fallback-uri actuale pentru lista goala:
- varianta mobila: divul cu textul "Nu există tipuri de abonament definite."
- varianta desktop: divul cu textul "Nu există tipuri de abonament definite. Folosiți formularul de mai sus pentru a începe."

cu acelasi `EmptyState`, avand titlu "Niciun tip de abonament definit", descriere care explica in romana ca tipurile de abonament stabilesc tarifele lunare folosite la generarea automata a platilor, `actionLabel` "Adaugă Tip Abonament" si `onAction` care face focus pe inputul de denumire prin ref si il aduce in vizor (`scrollIntoView` cu comportament smooth). Foloseste `PlusIcon` (deja importat) ca `icon`.

Conditia de randare ramane pe `tipuriAbonament.length === 0` (prop-ul primit), nu pe `sortedAbonamente`. Nu modifica logica de add/edit/delete si nu schimba props-urile componentei.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>test $(grep -c "EmptyState" components/Plati/TipuriAbonament.tsx) -ge 2 &amp;&amp; test $(grep -c "Nu există tipuri de abonament definite" components/Plati/TipuriAbonament.tsx) -eq 0 &amp;&amp; grep -q "useRef" components/Plati/TipuriAbonament.tsx &amp;&amp; echo ABONAMENT_EMPTYSTATE_OK</automated>
    <automated>grep -q "tipuriAbonament.length === 0" components/Plati/TipuriAbonament.tsx &amp;&amp; echo CONDITION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `EmptyState` apare de cel putin 2 ori in fisier (ramura mobila si ramura desktop).
    - Ambele texte vechi "Nu există tipuri de abonament definite..." au disparut din fisier.
    - CTA-ul face focus pe inputul de denumire din formularul de adaugare (ref atasat la `Input`-ul de denumire, apelat in `onAction`).
    - Conditia de afisare foloseste `tipuriAbonament.length === 0`.
    - Props-urile componentei `TipuriAbonamentManagement` sunt neschimbate.
    - `npm run lint` trece fara erori noi.
  </acceptance_criteria>
  <done>Ecranul Tipuri Abonament afiseaza empty state cu CTA pe ambele layout-uri.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> UI React | Doar randare conditionata; nicio decizie de securitate nu se ia in acest plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08 | Information Disclosure | Textul empty-state ar putea sugera existenta datelor altui club (ex. "clubul X nu are grupe") | mitigate | Mesajele sunt generice, la persoana clubului curent, fara nume de club si fara numere agregate cross-club |
| T-25-09 | Spoofing / UX | CTA care navigheaza spre un ecran fara permisiune (INSTRUCTOR fara drept de creare grupa) | accept | Modulul tinta aplica deja `usePermissions` si RLS; empty-state-ul nu ocoleste nicio verificare, iar butonul duce la acelasi flux ca butonul principal deja existent |
| T-25-SC | Tampering | Instalari de pachete | accept | Zero pachete noi (constrangere CLAUDE.md + RESEARCH "Standard Stack") |
</threat_model>

## Artifacts this phase produces (contributia acestui plan)

| Artefact | Tip | Detalii |
|----------|-----|---------|
| `EmptyState` | componenta React noua, exportata din `components/ui.tsx` | props: `icon`, `title`, `description`, `actionLabel`, `onAction`, `className` |
| `EmptyStateProps` | interfata TypeScript noua in `components/ui.tsx` | co-locata cu componenta, conform pattern-ului existent din fisier |
| Empty state Grupe | modificare `components/Grupe/index.tsx` | CTA "Adaugă Grupă" -> `handleOpenAdd` |
| Empty state Prezenta | modificare `components/Prezenta/GrupeList.tsx` | CTA "Creează prima grupă" -> `navigateTo('grupe')` |
| Empty state Abonamente | modificare `components/Plati/TipuriAbonament.tsx` | CTA "Adaugă Tip Abonament" -> focus pe inputul de denumire |

<verification>
- `npm run lint` (tsc --noEmit) trece fara erori noi dupa fiecare task.
- Gate-urile de grep confirma: `EmptyState` exportat o singura data, folosit in cele 3 fisiere tinta, textele vechi de fallback eliminate.
- `components/Prezenta/index.tsx` nu apare in diff (evita conflictul cu planul 25-03 din aceeasi unda).
- <human-check>Cu un cont de ADMIN_CLUB la un club fara grupe si fara tipuri de abonament: ecranul Grupe, tabul Grupe din Prezenta si ecranul Tipuri Abonament afiseaza fiecare mesaj + buton, iar butonul deschide actiunea corecta (modal grupa / modulul Grupe / focus pe formular). Verificarea consolidata se face in 25-04.</human-check>
</verification>

<success_criteria>
- Un club cu zero date nu mai vede niciun ecran gol fara context in Grupe, Prezenta (tab Grupe) sau Tipuri Abonament.
- Empty-state-ul este o singura componenta refolosita in 3 locuri, nu 3 blocuri JSX duplicate.
- Zero librarii noi, zero fisiere CSS noi, zero modificari de API la componentele existente.
</success_criteria>

<output>
Create `.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-02-SUMMARY.md` when done.
</output>
