---
phase: quick-260717-f99
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migrations/add_abonament_participare_vacanta.sql
  - types.ts
  - components/Plati/PerioadaVacanta.tsx
autonomous: true
requirements: [F99-abonamente-vacanta]

must_haves:
  truths:
    - "Admin poate deschide un modal cu tipurile de abonament ale clubului dintr-o perioada de vacanta expandata"
    - "La confirmare, sportivii participanti fara tip_abonament_id primesc tipul ales; cei cu tip_abonament_id existent sunt sariti (nu se suprascrie)"
    - "La incheierea perioadei (data_end < azi), tip_abonament_id revine automat la valoarea anterioara pentru participantii procesati, la incarcarea componentei"
    - "Lista participantilor afiseaza un indicator cand abonamentul le-a fost creat"
  artifacts:
    - path: "sql/migrations/add_abonament_participare_vacanta.sql"
      provides: "Coloane noi tip_abonament_anterior_id, abonament_procesat, abonament_revertit pe participare_vacanta"
      contains: "ALTER TABLE public.participare_vacanta"
    - path: "types.ts"
      provides: "Campuri noi pe interfata ParticipareVacanta"
      contains: "abonament_procesat"
    - path: "components/Plati/PerioadaVacanta.tsx"
      provides: "Buton Creeaza abonamente, modal selectie tip, logica creare + revert automat"
      contains: "tip_abonament_anterior_id"
  key_links:
    - from: "components/Plati/PerioadaVacanta.tsx"
      to: "sportivi.tip_abonament_id"
      via: "supabase update filtrat pe club_id"
      pattern: "\\.from\\('sportivi'\\)\\.update"
    - from: "components/Plati/PerioadaVacanta.tsx"
      to: "participare_vacanta.abonament_procesat"
      via: "supabase update dupa procesare/revert"
      pattern: "abonament_procesat"
---

<objective>
Adauga in modulul Perioade Vacanta (components/Plati/PerioadaVacanta.tsx) posibilitatea de a crea in masa un abonament pentru sportivii participanti la o perioada de vacanta, cu revert automat la incheierea perioadei.

Purpose: Adminii de club vor sa comute temporar sportivii pe un tip de abonament de "vacanta" pe durata unei perioade, si sa revina automat la abonamentul anterior cand perioada s-a incheiat — fara interventie manuala per sportiv.

Output:
- Migratie SQL cu 3 coloane noi pe `participare_vacanta`
- `types.ts` actualizat (interfata `ParticipareVacanta`)
- Buton + modal + logica creare/skip + logica revert automat in `PerioadaVacanta.tsx`
- Indicator vizual de status pe lista de participanti
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Fisier principal de modificat (deja citit in planning — contine fetchPerioade, fetchParticipanti, handleSavePeriada ca model de update filtrat pe club_id)
@components/Plati/PerioadaVacanta.tsx

# Tipuri relevante: ParticipareVacanta (L1042), PerioadaVacanta (L1033), TipAbonament (L517), Sportiv.tip_abonament_id (L97)
@types.ts

# Conventie migratie existenta pentru acest tabel (pattern de urmat: header comentat, ADD COLUMN IF NOT EXISTS)
@sql/migrations/create_perioade_vacanta.sql

# Nota: tipurile de abonament ale clubului sunt deja disponibile client-side ca `filteredData.tipuriAbonament` (TipAbonament[]) prin useData() — NU face fetch nou din Supabase pentru ele.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migratie SQL + tipuri</name>
  <files>sql/migrations/add_abonament_participare_vacanta.sql, types.ts</files>
  <action>
Creeaza fisierul `sql/migrations/add_abonament_participare_vacanta.sql` urmand conventia din create_perioade_vacanta.sql (header comentat cu titlu, data 2026-07-17, scop). Continut: `ALTER TABLE public.participare_vacanta` cu trei coloane adaugate idempotent — `ADD COLUMN IF NOT EXISTS tip_abonament_anterior_id UUID NULL REFERENCES public.tipuri_abonament(id)`, `ADD COLUMN IF NOT EXISTS abonament_procesat BOOLEAN NOT NULL DEFAULT false`, `ADD COLUMN IF NOT EXISTS abonament_revertit BOOLEAN NOT NULL DEFAULT false`. Foloseste `ADD COLUMN IF NOT EXISTS` (nu un singur ALTER multi-coloana fara IF NOT EXISTS) ca sa fie re-rulabil. Nu adauga RLS — coloanele mostenesc politicile existente ale tabelului.

In `types.ts`, la interfata `ParticipareVacanta` (in jur de L1042), adauga cele trei campuri optionale: `tip_abonament_anterior_id?: string | null;`, `abonament_procesat?: boolean;`, `abonament_revertit?: boolean;`. Nu modifica alte interfete.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String -NotMatch "add_abonament" ; grep -c "abonament_procesat" types.ts</automated>
  </verify>
  <done>Fisierul SQL exista cu cele 3 ADD COLUMN IF NOT EXISTS si FK spre tipuri_abonament; ParticipareVacanta are cele 3 campuri noi; `tsc --noEmit` nu raporteaza erori noi.</done>
</task>

<task type="auto">
  <name>Task 2: Buton + modal Creeaza abonamente + logica creare/skip + badge status</name>
  <files>components/Plati/PerioadaVacanta.tsx</files>
  <action>
Adauga in `PerioadaVacanta.tsx` un nou modal-component `CreeazaAbonamenteModal` (in stilul lui `AdaugaParticipantiModal`) care primeste `perioadaId`, lista participantilor perioadei si `onSaved`/`onClose`. Modalul foloseste `Modal`, `Button` din '../ui' — fara librarii noi.

Sursa tipurilor de abonament: `const { filteredData } = useData();` -> `filteredData.tipuriAbonament` (TipAbonament[], deja scope-uite pe club). Afiseaza-le ca lista selectabila (un singur tip ales, radio/click-select in stilul liniilor din AdaugaParticipantiModal). Buton confirmare dezactivat pana e ales un tip.

Logica la confirmare (`handleCreeaza`): pentru fiecare participant din lista perioadei:
- daca `participant.sportivi?.tip_abonament_id` NU e disponibil pe join-ul actual (join-ul din fetchParticipanti NU aduce tip_abonament_id) — de aceea EXTINDE mai intai select-ul din `fetchParticipanti` (functia din view) sa includa `tip_abonament_id` in join-ul `sportivi(...)`. Actualizeaza si tipul join din `ParticipareVacanta.sportivi` in types.ts daca e nevoie (adauga `tip_abonament_id?: string | null`).
- Skip: daca sportivul are deja `tip_abonament_id` non-null -> NU il atinge (nu update sportiv, nu marca procesat). 
- Altfel (`tip_abonament_id` null): `supabase.from('sportivi').update({ tip_abonament_id: tipAles }).eq('id', sportiv_id).eq('club_id', clubId)` (filtreaza pe club_id dupa modelul handleSavePeriada); apoi `supabase.from('participare_vacanta').update({ tip_abonament_anterior_id: null, abonament_procesat: true, abonament_revertit: false }).eq('id', participareId)`. (Valoarea anterioara e null exact pentru cei procesati, conform spec — se salveaza pentru simetria revert-ului.)
Gestioneaza erorile prin `showError`. La final apeleaza `onSaved()` (care va reface fetchParticipanti) si `onClose()`.

In view: adauga un state `creeazaAbonamentePerioadaId` si un buton "Creează abonamente" langa/sub butonul existent "Adaugă Participanți" in zona expandata (doar pentru `isAdmin`), care seteaza acel state. Randeaza `CreeazaAbonamenteModal` conditionat, pasand `participari[id]` si `onSaved={() => fetchParticipanti(id)}`.

Badge status: in lista participantilor (`<li>` din zona expandata), daca `lp.abonament_procesat && !lp.abonament_revertit` afiseaza un mic badge Tailwind (ex text "abonament creat", stil pastilat slate/emerald consistent cu badge-ul de participanti existent). Nu introduce culori/componente noi in afara stilului deja folosit in fisier.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String "PerioadaVacanta" ; grep -c "CreeazaAbonamenteModal\|tip_abonament_anterior_id\|abonament_procesat" components/Plati/PerioadaVacanta.tsx</automated>
  </verify>
  <done>Buton "Creează abonamente" apare in zona expandata pentru admin; modalul listeaza filteredData.tipuriAbonament; la confirmare sportivii fara tip_abonament_id primesc tipul (update filtrat pe club_id) iar cei cu tip existent sunt sariti; participare_vacanta marcat abonament_procesat=true; fetchParticipanti aduce acum tip_abonament_id in join; badge de status vizibil; `tsc --noEmit` fara erori noi in fisier.</done>
</task>

<task type="auto">
  <name>Task 3: Revert automat la incheierea perioadei</name>
  <files>components/Plati/PerioadaVacanta.tsx</files>
  <action>
Adauga logica de revert automat client-side, fara cron backend. Dupa ce `fetchPerioade` incarca perioadele, pentru fiecare perioada cu `data_end < azi` (comparatie string 'YYYY-MM-DD' vs `new Date().toISOString().slice(0,10)`), verifica participantii care au `abonament_procesat === true && abonament_revertit !== true`.

Implementeaza intr-o functie `revertAbonamenteExpirate(perioade)` apelata la finalul lui `fetchPerioade` (sau intr-un `useEffect` dependent de `perioade`). Pentru fiecare perioada expirata: fetch participantii ei (`select id, sportiv_id, tip_abonament_anterior_id, abonament_procesat, abonament_revertit from participare_vacanta where perioada_id = ...`), filtreaza cei de revertit, si pentru fiecare:
- `supabase.from('sportivi').update({ tip_abonament_id: participant.tip_abonament_anterior_id }).eq('id', sportiv_id).eq('club_id', clubId)` (revine la valoarea anterioara — poate fi null),
- apoi `supabase.from('participare_vacanta').update({ abonament_revertit: true }).eq('id', participareId)`.
Gestioneaza erorile cu showError dar nu bloca UI (best-effort). Evita bucle infinite: revert-ul marcheaza abonament_revertit=true deci la urmatorul load nu se mai reprocreseaza; nu apela fetchPerioade recursiv din interiorul revertului. Daca perioada e expandata, poti reface fetchParticipanti pentru ea ca badge-urile sa dispara.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String "PerioadaVacanta" ; grep -c "revertAbonamenteExpirate\|abonament_revertit" components/Plati/PerioadaVacanta.tsx</automated>
  </verify>
  <done>La incarcarea view-ului, perioadele cu data_end trecut si participanti abonament_procesat=true & abonament_revertit=false readuc sportivii la tip_abonament_anterior_id (update filtrat pe club_id) si marcheaza abonament_revertit=true; nu exista reprocesare la load-uri repetate; `tsc --noEmit` fara erori noi.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` nu introduce erori noi (erorile pre-existente din alte fisiere raman neschimbate).
- Migratia SQL e idempotenta (re-rulabila) si respecta conventia din create_perioade_vacanta.sql.
- Toate update-urile pe `sportivi` sunt filtrate suplimentar pe `.eq('club_id', clubId)` (defense-in-depth peste RLS), la fel ca handleSavePeriada.
- Nu se face fetch nou pentru tipuri abonament — se foloseste filteredData.tipuriAbonament.
- Flow-ul manual per sportiv (profil sportiv) ramane neatins.
</verification>

<success_criteria>
- Admin creeaza in masa un abonament pentru participantii unei perioade; cei cu tip_abonament_id deja setat sunt sariti.
- La incheierea perioadei, sportivii procesati revin automat la abonamentul anterior fara actiune manuala.
- Migratia SQL e scrisa in sql/migrations/ (aplicare manuala de catre user — nu se ruleaza automat).
- UI foloseste doar design system-ul existent; niciun query nou pentru date deja cache-uite.
</success_criteria>

<output>
Create `.planning/quick/260717-f99-adauga-logica-creare-abonament-pentru-sp/260717-f99-SUMMARY.md` when done.
Reminder pentru user in SUMMARY: migratia `sql/migrations/add_abonament_participare_vacanta.sql` trebuie aplicata manual in Supabase inainte ca feature-ul sa functioneze live.
</output>
