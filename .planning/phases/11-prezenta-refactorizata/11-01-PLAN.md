---
phase: 11-prezenta-refactorizata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useMultiCalendarView.ts
  - components/Prezenta/CalendarActivitatiMultiGrupa.tsx
autonomous: true
requirements: [PRZ-01]
must_haves:
  truths:
    - "Calendarul multi-grupă afișează antrenamentele tuturor grupelor instructorului într-o singură vizualizare lunară"
    - "Fiecare zi cu antrenamente arată dots colorate distincte per grupă (culoare stabilă per grupaId)"
    - "Click pe o zi afișează lista antrenamentelor acelei zile cu denumirea grupei vizibilă pe fiecare"
    - "Antrenamentele cu același (ora_start, ora_sfarsit) din grupe diferite sunt grupate vizual ca 'simultane'"
  artifacts:
    - path: "hooks/useMultiCalendarView.ts"
      provides: "Hook fetch multi-grupă cu .in('grupa_id', grupeIds)"
      contains: ".in('grupa_id'"
      min_lines: 40
    - path: "components/Prezenta/CalendarActivitatiMultiGrupa.tsx"
      provides: "Calendar lunar multi-grupă cu dots colorate și detectare simultane"
      exports: ["CalendarActivitatiMultiGrupa"]
      min_lines: 120
  key_links:
    - from: "components/Prezenta/CalendarActivitatiMultiGrupa.tsx"
      to: "hooks/useMultiCalendarView.ts"
      via: "useMultiCalendarView(grupeIds)"
      pattern: "useMultiCalendarView"
    - from: "hooks/useMultiCalendarView.ts"
      to: "program_antrenamente"
      via: "supabase query"
      pattern: "from\\('program_antrenamente'\\)"
---

<objective>
Construiește fundația pentru calendarul multi-grupă (PRZ-01): un hook `useMultiCalendarView` care fetch-uiește antrenamentele tuturor grupelor instructorului simultan, și o componentă `CalendarActivitatiMultiGrupa` care le afișează într-un singur calendar lunar cu dots colorate per grupă.

Această fază NU atinge `index.tsx` — componenta și hook-ul sunt construite izolat, ca interfețe consumabile de Plan 02 (wiring-ul navigării). Astfel Plan 01 și Plan 03/04 rulează în paralel fără conflict de fișiere.

Purpose: Instructorul vede toate antrenamentele într-un singur loc (azi: doar o grupă la un moment dat).
Output: `hooks/useMultiCalendarView.ts`, `components/Prezenta/CalendarActivitatiMultiGrupa.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-prezenta-refactorizata/11-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creează hook-ul useMultiCalendarView</name>
  <files>hooks/useMultiCalendarView.ts</files>
  <read_first>
    - hooks/useCalendarView.ts (sursa de pornire — copiază structura state + fetchAntrenamente, schimbă DOAR filtrul de grupă)
    - types.ts (interface Antrenament, Grupa — linia ~323+)
  </read_first>
  <action>
    Creează `hooks/useMultiCalendarView.ts` pornind de la structura din `useCalendarView`. Semnătura: `export const useMultiCalendarView = (grupeIds: string[], initialDate?: string)`.
    Păstrează identic: state-urile `date`, `setDate`, `selectedDate`, `setSelectedDate`, `todayLocal` (calculat cu `new Date().toLocaleDateString('sv-SE')`), `antrenamente`, `loading`.
    În `fetchAntrenamente`: calculează `startOfMonth = date.substring(0, 7) + '-01'` și `endOfMonth = new Date(d.getFullYear(), d.getMonth() + 1, 0).toLocaleDateString('sv-SE')` exact ca în original.
    DIFERENȚA cheie: dacă `grupeIds.length === 0`, setează `setAntrenamente([])` și return imediat (V5 input validation, evită query fără filtru). Altfel folosește `.in('grupa_id', grupeIds)` în loc de `.eq('grupa_id', grupaId)`. Restul query-ului identic: `.select('*, grupe(*), prezenta:prezenta_antrenament(sportiv_id, status_id)')`, `.gte('data', startOfMonth)`, `.lte('data', endOfMonth)`, `.order('data')`, `.order('ora_start')`.
    Wrap în `try/finally` cu `setLoading(true)` la început și `setLoading(false)` în finally (pattern WR-04 din original). Pe eroare apelează `showError("Eroare la încărcarea calendarului", error.message)` din `useError()`.
    Dependențele `useCallback` și `useEffect`: `[grupeIds, date, showError]` și `[fetchAntrenamente]`.
    NU include `handleGenerate` și `handleSaveCustom` (acelea sunt per-grupă, nu fac sens multi-grupă). Returnează doar: `{ date, setDate, selectedDate, setSelectedDate, todayLocal, antrenamente, loading, fetchAntrenamente }`.
    IMPORTANT pentru stabilitatea `grupeIds`: callerul (Plan 02) va memoiza array-ul; aici tratează `grupeIds` ca referință în dep array — documentează cu un comentariu că array-ul trebuie memoizat de caller pentru a evita re-fetch infinit.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "useMultiCalendarView" | grep -v "^#"; test $? -ne 0 && echo "NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - Fișierul `hooks/useMultiCalendarView.ts` exportă `useMultiCalendarView`
    - Conține literalmente `.in('grupa_id', grupeIds)` (grep confirmă)
    - Conține guard `grupeIds.length === 0` înainte de query
    - `npx tsc --noEmit` nu raportează erori noi în acest fișier
  </acceptance_criteria>
  <done>Hook-ul fetch-uiește antrenamente din multiple grupe via `.in()`, cu guard pe array gol și loading în try/finally.</done>
</task>

<task type="auto">
  <name>Task 2: Creează componenta CalendarActivitatiMultiGrupa</name>
  <files>components/Prezenta/CalendarActivitatiMultiGrupa.tsx</files>
  <read_first>
    - components/Prezenta/CalendarActivitati.tsx (sursa pentru grid lunar, helpers ymd/buildCalendarGrid/monthLabel, navigare luni, render zile)
    - components/Prezenta/RaportPrezenta.tsx (linia 32 — array GROUP_COLORS pentru culori per grupă)
    - hooks/useMultiCalendarView.ts (creat la Task 1)
    - utils/date.ts (formatTime)
  </read_first>
  <action>
    Creează `components/Prezenta/CalendarActivitatiMultiGrupa.tsx` exportând componenta `CalendarActivitatiMultiGrupa`.
    Props: `{ grupe: Grupa[]; onSelect: (id: string) => void; onSelectMultiple: (ids: string[]) => void; onBack: () => void }`.
    Calculează `grupeIds = useMemo(() => grupe.map(g => g.id), [grupe])` (memoizat — critic pentru stabilitatea hook-ului). Apelează `const { date, setDate, selectedDate, setSelectedDate, todayLocal, antrenamente, loading } = useMultiCalendarView(grupeIds)`.
    Copiază din `CalendarActivitati`: helperii `ymd`, `buildCalendarGrid`, `monthLabel`, constanta `ZILE_SCURTE`, parsarea `[year, month]` din `date`, `cells = buildCalendarGrid(year, month)`, navigarea `goToPrevMonth`/`goToNextMonth`, și structura grid-ului de zile.
    CULORI PER GRUPĂ (PRZ-01 dots colorate, evită pitfall 2 din research): construiește `colorByGrupa = useMemo(() => { const m = new Map<string,string>(); grupe.forEach((g, i) => m.set(g.id, GROUP_COLORS[i % GROUP_COLORS.length])); return m; }, [grupe])`. Importă/copiază `GROUP_COLORS` din `RaportPrezenta.tsx` (array de hex). Folosirea Map pe `grupaId` garantează culoare stabilă indiferent de ordinea render.
    `antrenamenteByDate`: `useMemo` care grupează `antrenamente` pe `a.data` (Record<string, Antrenament[]>).
    În fiecare celulă cu antrenamente: randează un dot per grupă DISTINCTĂ prezentă în acea zi (deduplică `grupa_id`-urile zilei), fiecare dot cu `style={{ backgroundColor: colorByGrupa.get(grupaId) }}` (folosește style inline cu hex, nu clase Tailwind, fiindcă GROUP_COLORS sunt hex). Maxim 4 dots vizibile + un "+N" dacă sunt mai multe.
    DETECTARE SIMULTANE (PRZ-01/03 fundație): pentru ziua selectată, calculează `simultaneByInterval = useMemo` care grupează `selectedAntrenamente` pe cheia `\`${a.ora_start}_${a.ora_sfarsit}\``. Rezultat: `Map<string, Antrenament[]>`.
    În panoul "zi selectată", pentru fiecare interval:
      - dacă grupul are 1 antrenament: card cu denumirea grupei (`a.grupe?.denumire`), un dot colorat al grupei, ora formatată cu `formatTime`, și buton "Bifează Prezența" care apelează `onSelect(a.id)`.
      - dacă grupul are N>1 antrenamente (grupe simultane): un singur card care listează denumirile tuturor grupelor (cu dot colorat fiecare), badge "SIMULTAN", și buton "Bifează Prezența (N grupe)" care apelează `onSelectMultiple(antrenamenteSimultane.map(a => a.id))`.
    Header: titlu "Calendar — Toate grupele" cu buton Back (`onBack`). Legendă: listează fiecare grupă cu dot-ul ei colorat (max afișaj rezonabil).
    NU include controalele "Generează Calendar" / "Adaugă Antrenament" (acelea sunt per-grupă în `CalendarActivitati`; aici e read+navigate only).
    Respectă design system: folosește `Card`, `Button` din `../ui`; clase Tailwind dark (slate-800/900, indigo). NU importa Shadcn/MUI.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "CalendarActivitatiMultiGrupa" | grep -v "^#"; test $? -ne 0 && echo "NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - Fișierul exportă `CalendarActivitatiMultiGrupa`
    - Apelează `useMultiCalendarView(grupeIds)` cu `grupeIds` memoizat (grep `useMemo` + `grupe.map`)
    - Conține `colorByGrupa` ca `Map` (culoare stabilă per grupaId — grep `new Map`)
    - Conține grupare simultane pe cheia `ora_start`+`ora_sfarsit` (grep `ora_start` + `ora_sfarsit` în cheia Map)
    - Apelează `onSelectMultiple` pentru grupe simultane și `onSelect` pentru grupă unică
    - `npx tsc --noEmit` fără erori noi în fișier
  </acceptance_criteria>
  <done>Calendar multi-grupă cu dots colorate stabile per grupă, panou zi selectată ce distinge antrenamente unice de grupe simultane, fără a atinge index.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase | grupeIds furnizate de frontend traversează către query DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing/IDOR | useMultiCalendarView query | mitigate | `grupeIds` provin din `grupe` deja filtrate per club/rol în caller (Plan 02 folosește `filteredData.grupe`); RLS pe `program_antrenamente` filtrează per `active-role-context-id` |
| T-11-02 | Information Disclosure | query fără filtru | mitigate | Guard `grupeIds.length === 0` previne query fără `.in()` care ar putea returna toate grupele clubului |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Faza nu instalează pachete noi (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` rulează fără erori noi introduse de cele două fișiere
- Hook folosește `.in('grupa_id', grupeIds)` și are guard pe array gol
- Componenta folosește Map color stabil per grupaId și detectează grupe simultane
</verification>

<success_criteria>
- `useMultiCalendarView` fetch-uiește antrenamente din N grupe într-un singur query
- `CalendarActivitatiMultiGrupa` randează calendar lunar cu dots colorate per grupă
- Niciun fișier `index.tsx` modificat (izolare pentru paralelism)
</success_criteria>

<output>
Create `.planning/phases/11-prezenta-refactorizata/11-01-SUMMARY.md` when done
</output>
