---
phase: 07-aplicare-filtre-pe-tab-uri
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - components/Competitii/InscrieriView.tsx
  - components/Competitii/index.tsx
autonomous: true
requirements:
  - INSC-01
  - INSC-02
  - INSC-03

must_haves:
  truths:
    - "InscrieriView nu mai are useState-uri de filtrare locale (filtreVisible, filterGen, filterProbaId, filterVarstaMin, filterVarstaMax, filterGradMin, filterGradMax)"
    - "InscrieriView acceptă props filtre/toggleGen/setFiltre/resetFiltre/nrFiltreActive"
    - "CompetitieFilterBar randat în InscrieriView deasupra listei de sportivi înscriși"
    - "categoriiVizibile useMemo recalculat din props filtre via aplicaFiltreCategorie"
    - "filteredInscrieri și filteredEchipe filtrate prin categoriiVizibile"
    - "index.tsx pasează props noi la InscrieriView"
  artifacts:
    - path: "components/Competitii/InscrieriView.tsx"
      provides: "Props interface actualizată + CompetitieFilterBar montat + filtrare prin hook"
      contains: "CompetitieFilterBar"
    - path: "components/Competitii/index.tsx"
      provides: "Props noi pasate la InscrieriView"
      contains: "filtre={filtre}"
  key_links:
    - from: "InscrieriView"
      to: "CompetitieFilterBar"
      via: "JSX mount"
      pattern: "CompetitieFilterBar"
    - from: "categoriiVizibile"
      to: "aplicaFiltreCategorie"
      via: "useMemo"
      pattern: "aplicaFiltreCategorie"
    - from: "filteredInscrieri"
      to: "categoriiVizibile"
      via: ".filter"
      pattern: "categoriiVizibile"
---

<objective>
Refactorizarea InscrieriView pentru a elimina state-ul local duplicat de filtrare (7 useState + 2 useMemo + 2 funcții + ~104 linii JSX) și a-l înlocui cu props din hook-ul central + CompetitieFilterBar.

Purpose: InscrieriView are cel mai mult cod de filtrare duplicat față de hook-ul din Phase 6. Eliminarea sa reduce suprafața de mentenanță și asigură comportament identic cu celelalte tab-uri. INSC-03 este satisfăcut structural prin Task 1 (resetFiltre în handleSetActiveTab din index.tsx) — planul 07-02 nu mai trebuie să implementeze resetul.

Output: InscrieriView refactorizată fără state local de filtrare, cu CompetitieFilterBar montat, filteredInscrieri/filteredEchipe filtrate via categoriiVizibile din hook. index.tsx actualizat cu props noi la apelul InscrieriView.
</objective>

<execution_context>
@C:/Users/lungu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lungu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-infrastructur-filtrare-unificat/06-01-SUMMARY.md
@.planning/phases/07-aplicare-filtre-pe-tab-uri/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorizare InscrieriView — elimină state local, adaugă props + CompetitieFilterBar</name>
  <files>components/Competitii/InscrieriView.tsx</files>

  <read_first>
    components/Competitii/InscrieriView.tsx liniile 1-10 (imports + InscrieriViewProps)
    components/Competitii/InscrieriView.tsx liniile 29-97 (tot blocul state + filtrare existentă)
    components/Competitii/InscrieriView.tsx liniile 131-238 (blocul JSX Panou filtre)
    hooks/useCompetitieFilters.ts liniile 1-50 (CompetitieFiltre interface + aplicaFiltreCategorie)
    components/Competitii/CompetitieFilterBar.tsx liniile 5-13 (CompetitieFilterBarProps)
  </read_first>

  <behavior>
    ELIMINĂRI din InscrieriView.tsx:
    - Linia 36: `const [filtreVisible, setFiltreVisible] = useState(false);`
    - Linia 37: `const [filterGen, setFilterGen] = useState<Set<string>>(new Set());`
    - Linia 38: `const [filterProbaId, setFilterProbaId] = useState<string>('');`
    - Linia 39: `const [filterVarstaMin, setFilterVarstaMin] = useState('');`
    - Linia 40: `const [filterVarstaMax, setFilterVarstaMax] = useState('');`
    - Linia 41: `const [filterGradMin, setFilterGradMin] = useState('');`
    - Linia 42: `const [filterGradMax, setFilterGradMax] = useState('');`
    - Liniile 44-58: `const categoriiVizibile = useMemo(...)` (logica inline cu 6 state-uri)
    - Liniile 60-67: `const nrFiltreActive = useMemo(...)`
    - Liniile 69-75: `const toggleGen = (gen: string) => {...}`
    - Liniile 77-84: `const resetFiltre = () => {...}`
    - Liniile 133-237: întreg blocul JSX `{/* Panou filtre */}` (div cu buton toggle + panou collapsibil)

    ADĂUGIRI la InscrieriViewProps (după linia 23 — după `onRefresh`):
    ```
    filtre: CompetitieFiltre;
    toggleGen: (gen: string) => void;
    setFiltre: (partial: Partial<CompetitieFiltre>) => void;
    resetFiltre: () => void;
    nrFiltreActive: number;
    ```

    ADĂUGIRI în destructurarea componentei (după `onRefresh`):
    `filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive`

    ÎNLOCUIRE categoriiVizibile (useMemo nou, după `const echipeRetraseLocal`):
    ```
    const categoriiVizibile = useMemo(() => {
      const areFiltre = filtre.gen.size > 0 || filtre.probaId || filtre.varstaMin ||
        filtre.varstaMax || filtre.gradMin || filtre.gradMax;
      if (!areFiltre) return null;
      return new Set(aplicaFiltreCategorie(categorii, filtre).map(c => c.id));
    }, [categorii, filtre]);
    ```
    NOTĂ: `null` return când zero filtre este păstrat intenționat — compatibil cu `!categoriiVizibile` check din filteredInscrieri/filteredEchipe (liniile 90, 96).

    ÎNLOCUIRE JSX Panou filtre (linia 133-237): Înlocuiește întreg blocul cu:
    ```
    <CompetitieFilterBar
      filtre={filtre}
      toggleGen={toggleGen}
      setFiltre={setFiltre}
      resetFiltre={resetFiltre}
      nrFiltreActive={nrFiltreActive}
      probe={probe}
      grade={grade}
    />
    ```

    IMPORTURI noi (după linia 8, după `import { areVizaFRAM... }`):
    ```
    import { CompetitieFilterBar } from './CompetitieFilterBar';
    import { aplicaFiltreCategorie } from '../../hooks/useCompetitieFilters';
    import type { CompetitieFiltre } from '../../hooks/useCompetitieFilters';
    ```

    NOTĂ compatibilitate grad: Filtrul local folosea `Number(filterGradMin)` fără garduri isNaN. `aplicaFiltreCategorie` are garduri — comportamentul este mai corect, nu mai slab.
  </behavior>

  <action>
    Modifică components/Competitii/InscrieriView.tsx:

    1. IMPORTURI: Adaugă cele 3 linii de import (CompetitieFilterBar, aplicaFiltreCategorie, CompetitieFiltre type) după importurile existente.

    2. InscrieriViewProps: Adaugă 5 props noi (filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive) cu tipurile corecte. `filtre: CompetitieFiltre` necesită importul type adăugat mai sus.

    3. Destructurare componentă: Adaugă cele 5 props noi în parametrii funcției componentă.

    4. ELIMINĂ liniile 36-84 complet (7 useState + 2 useMemo + 2 funcții locale de filtrare).

    5. ADAUGĂ categoriiVizibile useMemo nou (imediat după `const echipeRetraseLocal...`) care folosește `aplicaFiltreCategorie` din hook. Păstrează `null` return când `!areFiltre` — compatibil cu check-ul existent `!categoriiVizibile` din liniile 90 și 96.

    6. ELIMINĂ JSX Panou filtre (liniile 133-237) și înlocuiește cu `<CompetitieFilterBar ... />` cu toate cele 7 props. Plasează imediat la începutul return-ului, înainte de secțiunea `{/* Individual */}`.

    7. NOTĂ: `filteredInscrieri` (linia ~88-92) și `filteredEchipe` (linia ~93-96) nu se modifică — continuă să folosească `!categoriiVizibile || categoriiVizibile.has(...)` care funcționează cu noul useMemo.
  </action>

  <verify>
    <automated>cd C:/Users/lungu/portal-phihau && npm run lint</automated>
  </verify>

  <done>
    npm run lint zero erori. Niciun `useState` de filtrare în InscrieriView (filtreVisible, filterGen, filterProbaId, filterVarstaMin, filterVarstaMax, filterGradMin, filterGradMax absent). CompetitieFilterBar prezent în JSX. categoriiVizibile recalculat din props filtre.
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizare apel InscrieriView în index.tsx cu props noi</name>
  <files>components/Competitii/index.tsx</files>

  <read_first>
    components/Competitii/index.tsx liniile 495-510 (apelul curent al InscrieriView)
  </read_first>

  <behavior>
    La apelul `<InscrieriView ...>` din index.tsx (linia ~495-509), adaugă 5 props noi după props existente:
    ```
    filtre={filtre}
    toggleGen={toggleGen}
    setFiltre={setFiltre}
    resetFiltre={resetFiltre}
    nrFiltreActive={nrFiltreActive}
    ```
    Toate cele 5 valori sunt disponibile din `useCompetitieFilters()` instanțiat în Task 1 din planul 07-01.
    Props existente (competitie, categorii, probe, inscrieri, echipe, grade, isAdmin, isClubAdmin, myClubId, numeClub, vizeSportivi, sportivi, onRefresh) rămân neschimbate.
  </behavior>

  <action>
    Modifică components/Competitii/index.tsx la apelul `<InscrieriView`:

    Adaugă cele 5 props noi (filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive) la lista de props existente. Valorile sunt din destructurarea `useCompetitieFilters()` din planul 07-01.
  </action>

  <verify>
    <automated>cd C:/Users/lungu/portal-phihau && npm run lint</automated>
  </verify>

  <done>
    npm run lint zero erori TypeScript. Apelul InscrieriView din index.tsx include cele 5 props noi. Nicio eroare de tip "Missing required prop".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → filtre props | filtre vin prin props din CompetitieDetail — aceeași suprafață ca în planul 07-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | categoriiVizibile useMemo | mitigate | aplicaFiltreCategorie are garduri isNaN (Phase 6) — comportament mai corect decât filtrul inline eliminat |
| T-07-02-02 | Information Disclosure | filtre client-side InscrieriView | accept | Date deja în memorie, niciun query nou Supabase |
</threat_model>

<verification>
1. `npm run lint` — zero erori TypeScript
2. `grep -n "filterGen\|filterProbaId\|filterVarstaMin\|filterVarstaMax\|filterGradMin\|filterGradMax\|filtreVisible" components/Competitii/InscrieriView.tsx | grep -v "^.*//.*filter"` — zero rezultate (state local eliminat)
3. `grep -n "CompetitieFilterBar" components/Competitii/InscrieriView.tsx` — cel puțin 2 linii (import + JSX)
4. `grep -n "aplicaFiltreCategorie" components/Competitii/InscrieriView.tsx` — cel puțin 1 linie (în useMemo)
5. `grep -n "filtre={filtre}" components/Competitii/index.tsx` — cel puțin 1 linie (în apelul InscrieriView)
</verification>

<success_criteria>
- InscrieriView fără cele 7 useState locale de filtrare
- InscrieriView fără nrFiltreActive useMemo local și fără toggleGen/resetFiltre locale
- CompetitieFilterBar montat în InscrieriView (INSC-01)
- categoriiVizibile recalculat via aplicaFiltreCategorie din props filtre (INSC-02)
- filteredInscrieri și filteredEchipe filtrate prin categoriiVizibile (comportament păstrat)
- index.tsx pasează cele 5 props noi la InscrieriView
- npm run lint zero erori
</success_criteria>

<output>
Creează `.planning/phases/07-aplicare-filtre-pe-tab-uri/07-02-SUMMARY.md` când ai terminat.
</output>
