---
context: default
phase: competitii-inscriere-fixes
task: 5
total_tasks: 10
status: in_progress
last_updated: 2026-06-05T11:56:39.907Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Critical Anti-Patterns

_No anti-patterns discovered through failure this session._

<current_state>
Modul Competitii — InscriereClubWizard. Am finalizat bugfix-uri si filtrele pentru Pas2. Prop `onOpenInscriereModal` adaugat in types.ts. Urmeaza implementarea Task1 (inline expand echipe) si Task2 (ani individuali varsta) — nicio modificare insa in InscriereClubCards.tsx, InscriereClubWizard/index.tsx, Competitii/index.tsx, Pas1.tsx sau Pas2Quyen.tsx pentru aceste feature-uri.

Fisierele modificate sunt necommise.
</current_state>

<completed_work>

- Task BugFix 1: Hub probe — carduri nu mai arata "exclus" cand selectedSportivi=0; giao_dau eligibilitate directa; echipe consistency
- Task BugFix 2: Pas3 sincron/song luyen — sportivi filtrati la club+activ (myClubId + status='Activ')
- Task Feature Pas2: Filtre adaugate — gen (Masculin/Feminin/Toti), varsta (preset buckets), toggle "Fara Q1" cu contor
- types.ts: `onOpenInscriereModal?: (cat: CategorieCompetitie) => void` adaugat la `InscriereClubWizardProps`
</completed_work>

<remaining_work>

### Task 1 — Probe echipa inline expand cu InscriereModal

**InscriereClubCards.tsx:**
- Adauga `const [expandedTeamProbaId, setExpandedTeamProbaId] = useState<string | null>(null)`
- Detecteaza proba echipa: `const isTeamProba = categorii.some(c => c.proba_id === proba.id && esteEchipaSauPereche(c))`
- Pt carduri echipa: click toggle `expandedTeamProbaId` in loc de `onDeschideProba`
- Cand expandat: arata lista categorii (`categorii.filter(c => c.proba_id === proba.id)`) cu buton "Inscriere" per categorie → `onOpenInscriereModal?.(cat)`

**InscriereClubWizard/index.tsx** (aproximativ linia 200-250 unde se randeaza InscriereClubCards):
- Pass `onOpenInscriereModal={props.onOpenInscriereModal}` catre `<InscriereClubCards>`

**Competitii/index.tsx** (aproximativ linia 1152-1166 unde e `<InscriereClubWizard>`):
- Adauga prop: `onOpenInscriereModal={cat => setInscriereModal(cat)}`
- `setInscriereModal` deja exista (state la linia ~770), `InscriereModal` randat la linia ~2959

### Task 2 — Ani individuali varsta (Pas1 + Pas2)

**InscriereClubWizard/index.tsx:**
```typescript
const varsteCompetitie = useMemo(() => {
  const set = new Set<number>();
  for (const cat of categorii) {
    const min = cat.varsta_min;
    const max = cat.varsta_max ?? 80;
    for (let v = min; v <= Math.min(max, 80); v++) set.add(v);
  }
  return Array.from(set).sort((a, b) => a - b);
}, [categorii]);
```
Pass `varsteCompetitie` catre `<Pas1>` si `<Pas2Quyen>`.

**Pas1.tsx:**
- Adauga prop `varsteCompetitie?: number[]`
- Inlocuieste preset buttons (Copii/Jun.Mici/etc) cu butoane individuale per an
- Click an X: `setFilterVarstaMin(String(X)); setFilterVarstaMax(String(X))`
- Click acelasi an: clear ambele
- Wrap in `overflow-x-auto` scroll orizontal pentru multe varste

**Pas2Quyen.tsx:**
- `varsteCompetitie` calculat din `categorii` prop (Pas2 primeste toate categoriile)
- Inlocuieste preset buttons cu ani individuali — aceeasi logica ca Pas1
</remaining_work>

<decisions_made>

- Reuse `InscriereModal` din `Competitii/index.tsx` — nu wizard separat, evita duplicare
- `onOpenInscriereModal` prop thread prin `InscriereClubWizardProps` → `InscriereClubCards` (prin extends)
- `InscriereClubCardsProps extends InscriereClubWizardProps` → prop automat disponibil, nu necesita modificari separate la interfata Cards
- Varste individuale din toate categoriile competitiei (nu buckets preset) — user explicit: fiecare an vizibil
</decisions_made>

<blockers>
Niciun blocaj activ.
</blockers>

## Required Reading (in order)
1. `components/Competitii/InscriereClubWizard/types.ts` — prop onOpenInscriereModal deja adaugat
2. `components/Competitii/InscriereClubWizard/InscriereClubCards.tsx` — fisier principal Task1
3. `components/Competitii/InscriereClubWizard/shared.tsx` — `esteEchipaSauPereche(cat)` util
4. `components/Competitii/index.tsx` linia 770 (`inscriereModal` state) si linia 1152 (wizard render) si 2959 (InscriereModal render)
5. `components/Competitii/InscriereClubWizard/Pas1.tsx` — pentru Task2 varste

## Critical Anti-Patterns (do NOT repeat these)
_Niciun anti-pattern descoperit prin esec in aceasta sesiune._

## Infrastructure State
- Dev server: neraport (opreste/porneste cu `npm run dev`)
- TypeScript: `npx tsc --noEmit` trecut fara erori dupa ultimele modificari
- Fisiere necommise: 4 fisiere InscriereClubWizard

<context>
Lucram la modulul Competitii. Utilizatorul raportat: echipele incomplete nu pot fi vizualizate/editate din hub-ul probe. Solutia: click pe cardul probe echipa (song_luyen/sincron) deschide inline lista categorii, fiecare cu buton "Inscriere" care apeleaza InscriereModal existent. Simultan: varste preset buckets → ani individuali din competitie.
</context>

<next_action>
Start cu: deschide `components/Competitii/InscriereClubWizard/InscriereClubCards.tsx`, adauga `expandedTeamProbaId` state, modifica `CardProbaItem` sau grid-ul principal sa detecteze probe echipa si sa afiseze expand inline cu categorii + buton Inscriere.
</next_action>
