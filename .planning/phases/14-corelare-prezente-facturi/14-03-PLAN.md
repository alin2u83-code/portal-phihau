---
phase: 14-corelare-prezente-facturi
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - components/Plati/GestiuneFacturi.tsx
autonomous: false
requirements: [PLF-02, PLF-04]

must_haves:
  truths:
    - "ADMIN_CLUB poate selecta sportiv + lună (trecut/viitor) și genera o factură Abonament din GestiuneFacturi (PLF-02)"
    - "Generarea blochează duplicatele: dacă există deja Abonament pentru luna/an/sportiv, apare eroare (PLF-02)"
    - "Butonul Șterge din GestiuneFacturi e dezactivat cu tooltip pentru facturi Achitat (PLF-04)"
    - "handleDelete din GestiuneFacturi blochează ștergerea facturilor Achitat (guard server-side, PLF-04)"
  artifacts:
    - path: "components/Plati/GestiuneFacturi.tsx"
      provides: "Generare factură per lună cu month picker + verificare duplicat + restricție ștergere"
      contains: "genereazaFacturaAbonament"
  key_links:
    - from: "components/Plati/GestiuneFacturi.tsx"
      to: "services/facturaService.ts"
      via: "genereazaFacturaAbonament / facturaAbonamentExista"
      pattern: "genereazaFacturaAbonament"
---

<objective>
PLF-02 (generare factură Abonament pentru orice lună, trecut sau viitor, fără duplicate) și PLF-04 (restricție ștergere) localizate exclusiv în `GestiuneFacturi.tsx`. Acest fișier nu se suprapune cu cele din 14-02 și 14-04, deci rulează în paralel în wave 2.

Purpose: Adminul recuperează facturile neemise pentru lunile anterioare (sau pregătește luni viitoare) folosind un selector de lună, refolosind serviciul `genereazaFacturaAbonament` din 14-01; ștergerea facturilor achitate e blocată și aici.
Output: Un mod de generare per-lună în GestiuneFacturi + guard de ștergere pe status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-corelare-prezente-facturi/14-CONTEXT.md
@.planning/phases/14-corelare-prezente-facturi/14-RESEARCH.md
@CLAUDE.md
@services/facturaService.ts
@utils/luniLipsa.ts
@components/Plati/GestiuneFacturi.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PLF-02 — generare factură Abonament per lună cu month picker</name>
  <read_first>
    - components/Plati/GestiuneFacturi.tsx liniile 28-39 (initialFormState), 76-132 (auto-calc preț abonament), 171-239 (handleAddFactura)
    - components/Plati/GestiuneFacturi.tsx liniile 504-576 (formularul Adaugă Factură)
    - services/facturaService.ts (genereazaFacturaAbonament, facturaAbonamentExista)
    - utils/luniLipsa.ts (formatLuna)
    - 14-RESEARCH.md Pattern 3 (input type="month", value format YYYY-MM, split), Pitfall 5 (luna 1-indexed)
    - types.ts liniile 153-175 (Plata cu luna, an)
  </read_first>
  <action>
    Adaugă în GestiuneFacturi un mod de generare Abonament per lună fără a strica formularul generic existent. Cea mai simplă cale: adaugă un buton/secțiune nouă "Generează Abonament pentru o lună" deasupra sau lângă formularul existent, vizibil doar pentru `permissions.isAdminClub` (importă permissions din context dacă nu e deja disponibil — verifică prop-urile; dacă nu primește permissions, derivă din `useData().activeRoleContext` + `usePermissions`). Această secțiune are: (1) un `<Select>` Sportiv (refolosește `clubSportivi` existent), (2) un `<Input type="month">` cu value `${an}-${String(luna).padStart(2,'0')}` și onChange care face `value.split('-')` → `setAnGen(parseInt(an))`, `setLunaGen(parseInt(luna))` (1-indexed direct din string), (3) un buton "Generează" care apelează un handler nou `handleGenerateAbonamentLuna`.

    `handleGenerateAbonamentLuna`: validează că sportivul e selectat și luna 1-12 / an 2020-2100. Calculează suma abonamentului refolosind logica de preț existentă (tipuriAbonament: pentru sportiv cu familie → config pe nr membri activi; altfel `tipuriAbonament.find(ab => ab.id === sportiv.tip_abonament_id)` sau fallback numar_membri===1) — extrage această sumă identic cu blocul useEffect liniile 87-95. Apelează `genereazaFacturaAbonament({ sportiv, luna: lunaGen, an: anGen, suma, descriere: 'Abonament ' + formatLuna(lunaGen, anGen) })` din serviciu. Dacă `error` → `showError('Generare eșuată', error.message)` (serviciul returnează mesajul de duplicat când factura există). Dacă `data` → `setPlati(prev => [data, ...prev])`, `showSuccess('Succes', 'Factura a fost generată pentru ' + formatLuna(...))`. Blochează apăsări duble cu un state `isGeneratingLuna`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "GestiuneFacturi" || echo "no type errors in GestiuneFacturi"</automated>
  </verify>
  <acceptance_criteria>
    - GestiuneFacturi are un selector `<Input type="month">` și generează Abonament prin `genereazaFacturaAbonament` (grep confirmă importul)
    - Luna citită din input e 1-indexed corect (split pe '-', parseInt fără offset)
    - Duplicatele sunt blocate (serviciul verifică existența; UI afișează eroarea returnată)
    - Secțiunea de generare per lună e vizibilă doar pentru ADMIN_CLUB
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>Adminul poate genera o factură Abonament pentru orice lună selectată, fără a crea duplicate.</done>
</task>

<task type="auto">
  <name>Task 2: PLF-04 — restricție ștergere în GestiuneFacturi</name>
  <read_first>
    - components/Plati/GestiuneFacturi.tsx liniile 278-314 (handleDelete)
    - components/Plati/GestiuneFacturi.tsx liniile 436-455 (coloana Acțiuni desktop) și 483-495 (acțiuni mobil)
    - 14-RESEARCH.md Pitfall 1 (buton disabled + span title)
  </read_first>
  <action>
    PLF-04 în GestiuneFacturi: la butonul Șterge (TrashIcon) din coloana de acțiuni desktop (linia ~452) și mobil (linia ~494), când `p.status === 'Achitat'` randează butonul wrapped într-un `<span title="Facturile achitate nu pot fi șterse" className="cursor-not-allowed">` cu butonul `disabled className="pointer-events-none"` (Pitfall 1); altfel butonul activ existent care apelează `setPlataToDelete(p)`. În handlerul `handleDelete` (linia 278), adaugă la început guard server-side: re-fetch status cu `supabase.from('plati').select('status').eq('id', plataToDelete.id).maybeSingle()`; dacă `status === 'Achitat'` → `showError('Ștergere imposibilă', 'Facturile achitate nu pot fi șterse.')`, `setIsDeleting(false)`, `setPlataToDelete(null)`, return. Păstrează verificarea existentă pentru inscrieri_examene după acest guard.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "GestiuneFacturi" || echo "no type errors in GestiuneFacturi"</automated>
  </verify>
  <acceptance_criteria>
    - Butonul Șterge din GestiuneFacturi (desktop + mobil) e dezactivat cu tooltip wrap-span pentru status === 'Achitat'
    - `handleDelete` re-verifică statusul din DB și blochează ștergerea facturilor 'Achitat' (grep: `status === 'Achitat'` în handleDelete)
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>Facturile achitate nu pot fi șterse din GestiuneFacturi, nici din UI nici din handler.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PLF-02 (generare Abonament per lună cu month picker, fără duplicate) + PLF-04 (restricție ștergere) în Gestiune Facturi Manuale.</what-built>
  <how-to-verify>
    1. Loghează-te ca ADMIN_CLUB. Deschide Gestiune Facturi Manuale.
    2. În secțiunea "Generează Abonament pentru o lună": selectează un sportiv și o lună din trecut (ex. acum 2 luni) → Generează. Confirmă că factura apare în listă cu descrierea "Abonament [luna]".
    3. Repetă cu aceeași lună/sportiv → confirmă mesajul de eroare "Factură existentă pentru luna selectată." (fără duplicat).
    4. Selectează o lună viitoare → confirmă că generarea reușește.
    5. Pentru o factură Achitat din listă: confirmă butonul Șterge dezactivat + tooltip. Pentru una Neachitat: ștergerea funcționează.
  </how-to-verify>
  <resume-signal>Scrie "approved" sau descrie problemele observate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (plati insert) | generare Abonament — luna/an/sumă netrusted |
| client → Supabase (plati delete) | ștergere factură — id netrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Data Integrity / DoS | handleGenerateAbonamentLuna | mitigate | facturaAbonamentExista verifică duplicat la DB; isGeneratingLuna blochează click multiplu |
| T-14-08 | Tampering | handleDelete GestiuneFacturi | mitigate | Re-fetch status + guard 'Achitat' înainte de delete |
| T-14-09 | Tampering | generare pentru sportiv din alt club | accept | RLS pe plati verifică club_id = get_active_club_id() la nivel DB |
</threat_model>

<verification>
- `npx tsc --noEmit` fără erori noi în GestiuneFacturi.tsx
- `git diff --name-only` NU include types.ts, ui.tsx, DataContext, NavigationContext
- Generarea refolosește serviciul din 14-01 (nu reimplementează logica de insert/duplicat)
</verification>

<success_criteria>
- PLF-02: generare per orice lună (trecut/viitor) cu blocare duplicate
- PLF-04: ștergere blocată pentru facturi achitate (UI + server-side)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-03)
- `components/Plati/GestiuneFacturi.tsx` — secțiune generare Abonament per lună cu month picker (PLF-02) + restricție ștergere UI și server-side (PLF-04)
</artifacts_produced>

<output>
Create `.planning/phases/14-corelare-prezente-facturi/14-03-SUMMARY.md` when done
</output>
