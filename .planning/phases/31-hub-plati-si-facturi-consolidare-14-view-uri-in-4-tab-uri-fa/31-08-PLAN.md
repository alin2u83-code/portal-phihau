---
phase: 31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa
plan: 08
type: execute
wave: 3
depends_on: [31-01, 31-04, 31-05, 31-06]
files_modified:
  - components/Plati/hub/PlatiHub.tsx
  - components/LazyComponents.tsx
  - components/AppRouter.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
user_setup: []

must_haves:
  truths:
    - "Vederea 'plati-hub' afiseaza un singur ecran 'Plăți & Facturi' cu 4 tab-uri (Facturi / Încasări / Rapoarte / Configurare) si pastile de sectiune in interiorul fiecarui tab"
    - "Toate cele 12 literale vechi (plati-scadente, gestiune-facturi, facturi-fara-prezenta, jurnal-incasari, raport-financiar, financial-dashboard, tipuri-abonament, configurare-preturi, reduceri, taxe-anuale, nomenclatoare, istoric-plati pentru admini) deschid hub-ul direct pe tab-ul si sectiunea corespunzatoare — link-urile vechi, favoritele si reload-ul din localStorage continua sa functioneze"
    - "Rolul SPORTIV (si orice rol fara canManageFinances) care deschide 'istoric-plati' vede in continuare IstoricPlati standalone, exact ca inainte"
    - "Selectarea mai multor facturi in Facturi si 'Încasează N facturi' duce in tab-ul Încasări → Jurnal cu facturile precompletate; dupa salvare (auto-back la 1500 ms) utilizatorul revine pe Facturi"
    - "familii si deconturi-federatie raman vederi separate, neschimbate (D-05)"
    - "Revenirea din profilul sportivului (onViewSportiv → goBack) readuce utilizatorul pe acelasi tab si aceeasi sectiune"
  artifacts:
    - path: "components/Plati/hub/PlatiHub.tsx"
      provides: "Shell-ul hub-ului: header, bara de tab-uri, pastile de sectiune, fluxul de incasare multipla"
      exports: ["PlatiHub", "PlatiHubProps"]
      min_lines: 110
    - path: "components/LazyComponents.tsx"
      provides: "Export lazy PlatiHub"
      contains: "PlatiHub"
    - path: "components/AppRouter.tsx"
      provides: "case 'plati-hub' + alias-uri legacy + ramura istoric-plati"
      contains: "case 'plati-hub'"
  key_links:
    - from: "components/AppRouter.tsx"
      to: "components/Plati/hub/PlatiHub.tsx"
      via: "renderProtected(<Lazy.PlatiHub ... />, canManageFinances)"
      pattern: "Lazy\\.PlatiHub"
    - from: "components/Plati/hub/PlatiHub.tsx"
      to: "components/Plati/hub/platiHubConfig.ts"
      via: "rezolvaPozitieHub(activeView, viewParams, poateVedeaTaxeAnuale)"
      pattern: "rezolvaPozitieHub\\("
    - from: "components/Plati/hub/PlatiHub.tsx"
      to: "contexts/NavigationContext.tsx"
      via: "setViewParams pentru schimbare tab / navigateTo('plati-hub', ...) pentru incasare multipla"
      pattern: "navigateTo\\('plati-hub'"
---

<objective>
Shell-ul hub-ului "Plăți & Facturi" si comutarea rutarii: cele 12 vederi financiare top-level se reduc la o singura vedere `plati-hub` cu 4 tab-uri interne (D-01..D-04), iar literalele vechi devin alias-uri catre hub. `familii` si `deconturi-federatie` raman neatinse (D-05).

Aceasta e singura atingere a `components/AppRouter.tsx` din faza si se face DUPA ce toate tab-urile exista (31-04/05/06) si dupa auditul de props/fluxuri (31-01), per cerinta de siguranta pentru modulul financiar live.

Pozitia in hub (Claude's Discretion, documentata in 31-01): citita din `NavigationContext.viewParams` al vederii unice prin `rezolvaPozitieHub`, schimbata cu `setViewParams({ tab, sectiune })` (fara intrare de history). Motiv: `goBack()` restaureaza `viewParams`, deci iesirea spre `profil-sportiv` si revenirea pastreaza tab-ul. Singura tranzitie care intra in history este incasarea multipla (Facturi → Jurnal), ca butonul/gestul "inapoi" sa revina pe Facturi, exact ca vechiul `setActiveView('jurnal-incasari')`.

Fluxul F1-F4 (antetul platiHubConfig.ts) se muta din AppRouter in PlatiHub; state-ul `platiPentruIncasare` ramane in App.tsx (nu se atinge App.tsx/AppLayout.tsx), ca selectia sa supravietuiasca navigarii spre profil si inapoi — comportament identic cu azi.

Output: `components/Plati/hub/PlatiHub.tsx`, export lazy, rutarea noua in AppRouter.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-CONTEXT.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-01-SUMMARY.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-04-SUMMARY.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-05-SUMMARY.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-06-SUMMARY.md
@CLAUDE.md
@components/Plati/hub/platiHubConfig.ts
@contexts/NavigationContext.tsx
</context>

<interfaces>
Contracte consumate (nu le redefini):
```ts
// platiHubConfig.ts (31-01)
type TabHub; type SectiuneHub; interface PozitieHub { tab; sectiune }; interface PlatiHubTabProps;
const TABURI_HUB, ETICHETE_TABURI, SECTIUNI_PE_TAB, ETICHETE_SECTIUNI, SECTIUNE_IMPLICITA;
function rezolvaPozitieHub(activeView: View, viewParams: unknown, poateVedeaTaxeAnuale: boolean): PozitieHub;
// TabFacturi.tsx (31-04)
interface TabFacturiProps extends PlatiHubTabProps { onIncaseazaMultiple: (plati: Plata[]) => void; initialSportivId?: string; }
// TabIncasari.tsx (31-05)
interface TabIncasariProps extends PlatiHubTabProps { platiPentruIncasare: Plata[]; onIncasareProcesata: () => void; onJurnalBack: () => void; }
// TabRapoarte.tsx (31-05), TabConfigurare.tsx (31-06)
const TabRapoarte: React.FC<PlatiHubTabProps>; const TabConfigurare: React.FC<PlatiHubTabProps>;
// NavigationContext
useNavigation(): { activeView, setActiveView, viewParams, setViewParams, navigateTo(view, params?), navigateRoot, goBack, canGoBack, previousView, history, jumpToHistory }
```

Contract creat de acest plan:
```ts
// components/Plati/hub/PlatiHub.tsx
export interface PlatiHubProps {
  currentUser: User;
  permissions: Permissions;
  activeRoleContext: any;
  onViewSportiv: (sportiv: Sportiv, tab?: TabProfilSportiv) => void;
  onBack: () => void;                               // handleBackToDashboard din AppRouter
  platiPentruIncasare: Plata[];                      // state App.tsx:50
  setPlatiPentruIncasare: (plati: Plata[]) => void;
}
export const PlatiHub: React.FC<PlatiHubProps>;
```

Pattern vizual de urmat pentru bara de tab-uri: `components/Competitii/index.tsx:193-254` (container `overflow-x-hidden -mx-4` > `flex gap-1.5 overflow-x-auto pb-1 px-4 scroll-smooth scrollbar-hide`; buton activ `bg-brand-primary text-white shadow-md`, inactiv `bg-slate-800 text-slate-300 hover:bg-slate-700 hover:text-white border border-slate-700`; `h-9 px-3 rounded-lg text-sm font-medium`; `style={{ touchAction: 'manipulation' }}`).

Iconite existente in `components/icons.tsx` (folosite deja de menuConfig.ts): `FileTextIcon`, `BanknotesIcon`, `ChartBarIcon`, `CogIcon`, `ArrowLeftIcon`. Skeleton: `import { MartialArtsSkeleton } from '../../MartialArtsSkeleton'`.

AppRouter.tsx — ancore (inainte de editare): 72-89 handleIncaseazaMultiple / handleJurnalBack / handleIncasareProcesata; 77 `const { goBack, canGoBack, viewParams } = useNavigation();`; 110-113 guard-urile; 228-241 financial-dashboard, gestiune-facturi, facturi-fara-prezenta, deconturi-federatie (NU se atinge), plati-scadente, jurnal-incasari, raport-financiar; 254-257 tipuri-abonament, configurare-preturi; 262-265 reduceri, nomenclatoare; 266-267 familii (NU se atinge); 270-271 taxe-anuale; 284-285 istoric-plati.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Componenta PlatiHub (shell cu tab-uri, pastile, fluxul de incasare multipla)</name>
  <read_first>
    - components/Plati/hub/platiHubConfig.ts (integral — contract, constante, harta props, fluxuri F1-F6)
    - components/Plati/hub/TabFacturi.tsx, components/Plati/hub/TabIncasari.tsx, components/Plati/hub/TabRapoarte.tsx, components/Plati/hub/TabConfigurare.tsx (props exacte)
    - contexts/NavigationContext.tsx (integral)
    - components/AppRouter.tsx (liniile 72-89 — logica de mutat)
    - components/Competitii/index.tsx (liniile 76-88 si 192-256 — pattern tab-uri)
    - components/ui.tsx (Button, liniile 1-60)
  </read_first>
  <files>components/Plati/hub/PlatiHub.tsx</files>
  <action>
Creeaza `components/Plati/hub/PlatiHub.tsx` care exporta `PlatiHubProps` (exact ca in interfaces) si `export const PlatiHub: React.FC<PlatiHubProps>`.

Logica:
- `const { activeView, viewParams, setViewParams, navigateTo, goBack, canGoBack } = useNavigation();`
- `const poateVedeaTaxeAnuale = permissions.isSuperAdmin || permissions.isAdminClub;`
- `const pozitie = useMemo(() => rezolvaPozitieHub(activeView, viewParams, poateVedeaTaxeAnuale), [activeView, viewParams, poateVedeaTaxeAnuale]);`
- `const schimbaPozitie = (tab: TabHub, sectiune?: SectiuneHub) => setViewParams({ tab, sectiune: sectiune ?? SECTIUNE_IMPLICITA[tab] });` — fara intrare in history.
- `const handleIncaseazaMultiple = (platiSelectate: Plata[]) => { setPlatiPentruIncasare(platiSelectate); navigateTo('plati-hub', { tab: 'incasari', sectiune: 'jurnal-incasari' }); };` — muta F1 din AppRouter.tsx:72-75; `navigateTo` pune pozitia curenta in history, deci "inapoi" revine pe Facturi.
- `const handleIncasareProcesata = () => setPlatiPentruIncasare([]);` — F4.
- `const handleJurnalBack = () => { setPlatiPentruIncasare([]); if (canGoBack) goBack(); else schimbaPozitie('facturi', 'plati-scadente'); };` — oglinda exacta a AppRouter.tsx:78-85 (inlocuieste `setActiveView('plati-scadente')` cu pozitia echivalenta din hub). Invocat si automat de JurnalIncasari la 1500 ms dupa salvare.
- `const handleBack = () => { setPlatiPentruIncasare([]); onBack(); };` — butonul unic de intoarcere al hub-ului.
- `useEffect(() => { if (pozitie.sectiune !== 'jurnal-incasari' && platiPentruIncasare.length > 0) setPlatiPentruIncasare([]); }, [pozitie.sectiune]);` — goleste selectia cand utilizatorul paraseste Jurnalul prin tab/pastila (previne precompletarea cu facturi vechi, T-31-05-01). Comentariu care explica de ce nu goleste la intrarea prin handleIncaseazaMultiple (cele doua setState-uri sunt in acelasi batch React 18, deci primul render are deja sectiunea jurnal-incasari).
- `const initialSportivId = (viewParams && typeof viewParams === 'object' && typeof (viewParams as any).sportivId === 'string') ? (viewParams as any).sportivId : undefined;` — pastreaza F5 (GrupaDetailView → gestiune-facturi cu sportivId).
- `const sectiuniVizibile = SECTIUNI_PE_TAB[pozitie.tab].filter(s => s !== 'taxe-anuale' || poateVedeaTaxeAnuale);`
- `const propsComune = { sectiune: pozitie.sectiune, currentUser, permissions, activeRoleContext, onViewSportiv, onBack: handleBack };`

Randare (Tailwind + `Button` din `components/ui.tsx`, fara Shadcn/MUI):
1. Header: rand flex cu `<Button onClick={handleBack} variant="secondary"><ArrowLeftIcon className="w-5 h-5 mr-2" /> Meniu</Button>` si `<h1 className="text-xl md:text-2xl font-bold text-white uppercase tracking-tight">Plăți & Facturi</h1>`.
2. Bara de tab-uri (pattern Competitii): container `div.overflow-x-hidden.-mx-4` > `div` cu `role="tablist"` si clasele `flex gap-1.5 overflow-x-auto pb-1 px-4 scroll-smooth scrollbar-hide`; cate un `<button role="tab" aria-selected={pozitie.tab === tab}>` pentru fiecare `tab` din `TABURI_HUB`, cu iconita (facturi → FileTextIcon, incasari → BanknotesIcon, rapoarte → ChartBarIcon, configurare → CogIcon; harta locala `ICONITE_TABURI`) si eticheta `ETICHETE_TABURI[tab]`; `onClick={() => schimbaPozitie(tab)}`; clase activ/inactiv exact ca Competitii.
3. Pastile de sectiune, doar daca `sectiuniVizibile.length > 1`: `div.flex.flex-wrap.gap-1.5` cu cate un `<button>` per sectiune, eticheta `ETICHETE_SECTIUNI[s]`, `onClick={() => schimbaPozitie(pozitie.tab, s)}`; activ `bg-slate-600 text-white`, inactiv `bg-slate-800/60 text-slate-400 hover:text-white border border-slate-700`; `h-8 px-3 rounded-full text-xs font-medium`.
4. Continut in `<Suspense fallback={<MartialArtsSkeleton count={4} />}>`: `pozitie.tab === 'facturi'` → `<TabFacturi {...propsComune} onIncaseazaMultiple={handleIncaseazaMultiple} initialSportivId={initialSportivId} />`; `'incasari'` → `<TabIncasari {...propsComune} platiPentruIncasare={platiPentruIncasare} onIncasareProcesata={handleIncasareProcesata} onJurnalBack={handleJurnalBack} />`; `'rapoarte'` → `<TabRapoarte {...propsComune} />`; `'configurare'` → `<TabConfigurare {...propsComune} />`.

Importuri statice ale celor 4 tab-uri (sunt mici; componentele grele raman lazy in interiorul lor). Comentariu-antet: hub Faza 31, D-01..D-05, de ce pozitia sta in viewParams, fluxurile F1-F4 mutate din AppRouter.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && grep -c 'rezolvaPozitieHub(' components/Plati/hub/PlatiHub.tsx && grep -c \"navigateTo('plati-hub'\" components/Plati/hub/PlatiHub.tsx && grep -c 'onJurnalBack={handleJurnalBack}' components/Plati/hub/PlatiHub.tsx && grep -c 'role=\"tablist\"' components/Plati/hub/PlatiHub.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - `PlatiHub.tsx` exporta `PlatiHub` si `PlatiHubProps` cu cele 7 campuri din contract.
    - Contine `rezolvaPozitieHub(activeView, viewParams, poateVedeaTaxeAnuale)`, `setViewParams({ tab, sectiune`, `navigateTo('plati-hub', { tab: 'incasari', sectiune: 'jurnal-incasari' })`.
    - `handleJurnalBack` contine `setPlatiPentruIncasare([])`, `goBack()` si `schimbaPozitie('facturi', 'plati-scadente')`.
    - Contine `<TabFacturi`, `<TabIncasari`, `<TabRapoarte`, `<TabConfigurare`, `<Suspense`, `role="tablist"`, textul `Plăți & Facturi`.
    - Contine filtrul `s !== 'taxe-anuale' || poateVedeaTaxeAnuale`.
    - `grep -c "supabase" components/Plati/hub/PlatiHub.tsx` = 0; `grep -c "@mui\|shadcn" components/Plati/hub/PlatiHub.tsx` = 0.
  </acceptance_criteria>
  <done>Shell-ul hub-ului exista si compileaza, cu tab-uri, pastile, fluxul de incasare multipla complet si pozitie persistenta la goBack.</done>
</task>

<task type="auto">
  <name>Task 2: Comutarea rutarii in AppRouter (plati-hub + alias-uri legacy + ramura istoric-plati) si exportul lazy</name>
  <read_first>
    - components/AppRouter.tsx (integral)
    - components/LazyComponents.tsx (integral)
    - components/Plati/hub/PlatiHub.tsx (props din Task 1)
    - components/Plati/hub/platiHubConfig.ts (antetul — guard-urile si fluxurile)
  </read_first>
  <files>components/LazyComponents.tsx, components/AppRouter.tsx</files>
  <action>
LazyComponents.tsx: adauga `export const PlatiHub = lazy(() => import('./Plati/hub/PlatiHub').then(m => ({ default: m.PlatiHub })));` (ex. dupa linia exportului `IstoricPlati`). NU sterge niciun export existent — tab-urile si ramura SPORTIV le folosesc.

AppRouter.tsx:
1. Sterge functiile `handleIncaseazaMultiple`, `handleJurnalBack`, `handleIncasareProcesata` (liniile 72-89) — logica traieste acum in PlatiHub (31-08 Task 1). Pastreaza `const { goBack, canGoBack } = useNavigation();` (folosite de `profil-sportiv`); elimina `viewParams` din destructurare doar daca nu mai are nicio utilizare in fisier.
2. Dupa definitia `onViewSportiv` (linia ~114-118) construieste o singura data elementul: `const platiHubElement = <Lazy.PlatiHub currentUser={currentUser!} permissions={permissions} activeRoleContext={activeRoleContext} onViewSportiv={onViewSportiv} onBack={handleBackToDashboard} platiPentruIncasare={platiPentruIncasare} setPlatiPentruIncasare={setPlatiPentruIncasare} />;` (crearea unui element JSX nu il randeaza; e returnat doar din ramurile de mai jos). Acest element trebuie definit DUPA return-urile timpurii (OnboardingCompletare / MandatoryPasswordChange) si nu introduce hook-uri noi (Rules of Hooks — vezi comentariul CR-03 de la liniile 91-94).
3. Sterge case-urile individuale: `financial-dashboard` (228-229), `gestiune-facturi` (230-231), `facturi-fara-prezenta` (232-233), `plati-scadente` (236-237), `jurnal-incasari` (238-239), `raport-financiar` (240-241), `tipuri-abonament` (254-255), `configurare-preturi` (256-257), `reduceri` (262-263), `nomenclatoare` (264-265), `taxe-anuale` (270-271). In locul primului (unde era `financial-dashboard`) adauga un bloc grupat: `case 'plati-hub': case 'plati-scadente': case 'gestiune-facturi': case 'facturi-fara-prezenta': case 'jurnal-incasari': case 'raport-financiar': case 'financial-dashboard': case 'tipuri-abonament': case 'configurare-preturi': case 'reduceri': case 'taxe-anuale': case 'nomenclatoare':` urmat de `return renderProtected(platiHubElement, canManageFinances);` si un comentariu de 3 randuri: Faza 31 — un singur hub (D-01..D-04); literalele vechi raman alias-uri (localStorage `phi-hau-active-view`, favorite, link-uri adanci); guard `canManageFinances` ≡ vechiul `isAtLeastClubAdmin`, iar guard-ul special taxe-anuale e aplicat in interiorul hub-ului.
4. Inlocuieste case-ul `istoric-plati` (284-285) cu: `case 'istoric-plati': return canManageFinances ? platiHubElement : <Lazy.IstoricPlati onBack={handleBackToDashboard} viewedUser={currentUser!} plati={filteredData.plati} tranzactii={filteredData.tranzactii} />;` — ramura non-admin e IDENTICA cu linia 285 de azi (SPORTIV neschimbat); comentariu scurt care explica.
5. NU modifica case-urile `deconturi-federatie` (234-235) si `familii` (266-267) — raman separate (per D-05). NU modifica niciun alt case.
6. Dupa editare, `grep -n "setActiveView('jurnal-incasari')\|setActiveView('plati-scadente')" components/AppRouter.tsx` trebuie sa nu intoarca nimic.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && npm run build && npx tsx components/Plati/hub/platiHubConfig.test.ts && grep -c \"case 'plati-hub'\" components/AppRouter.tsx && ! grep -n 'handleIncaseazaMultiple' components/AppRouter.tsx && grep -c \"case 'deconturi-federatie'\" components/AppRouter.tsx && grep -c \"case 'familii'\" components/AppRouter.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` si `npm run build` ies cu cod 0; `npx tsx components/Plati/hub/platiHubConfig.test.ts` ramane verde.
    - `components/LazyComponents.tsx` contine `export const PlatiHub = lazy(` si toate exporturile anterioare (numarul de linii `export const` a crescut cu exact 1).
    - `components/AppRouter.tsx`: `grep -c "case 'plati-hub'"` = 1; fiecare din cele 11 literale (plati-scadente, gestiune-facturi, facturi-fara-prezenta, jurnal-incasari, raport-financiar, financial-dashboard, tipuri-abonament, configurare-preturi, reduceri, taxe-anuale, nomenclatoare) apare exact o data ca `case '<literal>':`; `case 'istoric-plati'` apare o data si contine `canManageFinances ? platiHubElement :`.
    - `grep -c "handleIncaseazaMultiple\|handleJurnalBack\|handleIncasareProcesata" components/AppRouter.tsx` = 0.
    - `grep -c "setActiveView('jurnal-incasari')" components/AppRouter.tsx` = 0.
    - Liniile case-urilor `deconturi-federatie` (FederationInvoices) si `familii` (FamiliiManagement) sunt identice cu cele dinainte (`git diff` nu le atinge).
    - `grep -c "renderProtected(platiHubElement, canManageFinances)" components/AppRouter.tsx` = 1.
  </acceptance_criteria>
  <done>Cele 12 vederi financiare sunt servite de un singur hub; literalele vechi functioneaza ca alias-uri; SPORTIV, Familii si Deconturi Federatie sunt neschimbate; build-ul trece.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| activeView (localStorage / favorite / link-uri adanci) → AppRouter | Orice literal persistat trebuie sa ajunga intr-o ramura cu guard corect |
| hub → tab-uri cu actiuni financiare distructive | Guard-ul unic trebuie sa fie echivalent cu guard-urile individuale vechi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08-01 | Elevation of Privilege | Guard unic `canManageFinances` pentru 12 vederi cu guard-uri diferite | mitigate | Echivalenta demonstrata in 31-01 (isAtLeastClubAdmin ≡ canManageFinances); exceptia taxe-anuale aplicata in hub (fallback in rezolvaPozitieHub + pastila ascunsa + AccessDenied in TabConfigurare); exceptia istoric-plati tratata prin ramura dedicata |
| T-31-08-02 | Elevation of Privilege | SPORTIV/INSTRUCTOR care deschid 'istoric-plati' | mitigate | Ramura non-admin identica cu cea veche (IstoricPlati standalone cu viewedUser=currentUser) — fara acces la hub |
| T-31-08-03 | Tampering | `platiPentruIncasare` persistent in App state dupa parasirea Jurnalului | mitigate | Golire in handleBack, handleJurnalBack, handleIncasareProcesata si in efectul pe schimbarea sectiunii |
| T-31-08-04 | Denial of Service | Literal vechi fara case → "Lipsește Vizualizarea" | mitigate | Toate cele 12 literale mapate explicit; acceptance numara case-urile |
| T-31-08-SC | Tampering | npm installs | accept | Zero pachete noi |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Inventarul complet al fazei e in `31-01-PLAN.md` (sectiunea omonima). Acest plan (31-08) produce:
- `components/Plati/hub/PlatiHub.tsx` → `PlatiHub`, `PlatiHubProps` (+ harta locala `ICONITE_TABURI`).
- `components/LazyComponents.tsx` → export `PlatiHub` (lazy).
- `components/AppRouter.tsx` → `case 'plati-hub'` + 11 alias-uri legacy grupate, ramura `istoric-plati` (hub pentru canManageFinances / IstoricPlati standalone altfel), element `platiHubElement`; eliminate `handleIncaseazaMultiple`, `handleJurnalBack`, `handleIncasareProcesata` (mutate in PlatiHub).
</artifacts_this_phase_produces>

<verification>
1. `npm run lint` si `npm run build` — cod 0.
2. `npx tsx components/Plati/hub/platiHubConfig.test.ts` — cod 0.
3. Gardele grep pe AppRouter (case-uri, handlere eliminate, D-05 neatins).
</verification>

<success_criteria>
- O singura vedere hub cu 4 tab-uri inlocuieste 12 vederi top-level (D-01..D-04).
- Familii si Deconturi Federatie neschimbate (D-05).
- Fluxul de incasare multipla si link-urile vechi functioneaza fara regresii.
</success_criteria>

<output>
Creeaza `.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-08-SUMMARY.md`.
</output>
