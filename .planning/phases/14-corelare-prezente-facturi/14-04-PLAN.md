---
phase: 14-corelare-prezente-facturi
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - hooks/useDataStartFacturareAll.ts
  - components/Plati/LuniLipsaWizard.tsx
  - components/Plati/RaportFinanciar.tsx
  - components/UserProfile.tsx
autonomous: false
requirements: [PLF-03, PLF-05]

must_haves:
  truths:
    - "Wizard 'Luni fără factură' detectează automat lunile lipsă per sportiv activ față de data_start_facturare (PLF-03)"
    - "Wizardul permite generare bulk a facturilor pentru lunile lipsă cu un singur click (PLF-03)"
    - "Tab 'Luni Lipsă' în RaportFinanciar listează toți sportivii activi cu luni neacoperite (PLF-05)"
    - "Badge 'X luni fără factură' apare pe profilul fiecărui sportiv activ afectat (PLF-05)"
    - "Adminul poate seta data_start_facturare per sportiv (necesar pentru calcul)"
  artifacts:
    - path: "components/Plati/LuniLipsaWizard.tsx"
      provides: "Wizard detectare + generare bulk luni lipsă"
      exports: ["LuniLipsaWizard"]
      min_lines: 60
    - path: "components/Plati/RaportFinanciar.tsx"
      provides: "Tab nou luni_lipsa cu lista sportivilor afectați + acces wizard"
      contains: "luni_lipsa"
    - path: "components/UserProfile.tsx"
      provides: "Badge luni fără factură în header profil + setare data_start_facturare"
      contains: "fără factură"
  key_links:
    - from: "components/Plati/LuniLipsaWizard.tsx"
      to: "services/facturaService.ts"
      via: "genereazaFacturaAbonament în loop bulk"
      pattern: "genereazaFacturaAbonament"
    - from: "components/Plati/LuniLipsaWizard.tsx"
      to: "utils/luniLipsa.ts"
      via: "calculeazaLuniLipsa"
      pattern: "calculeazaLuniLipsa"
    - from: "components/UserProfile.tsx"
      to: "utils/luniLipsa.ts"
      via: "calculeazaLuniLipsa pentru badge"
      pattern: "calculeazaLuniLipsa"
---

<objective>
PLF-03 (wizard luni lipsă + generare bulk) și PLF-05 (badge pe profil + tab centralizat). Aceste cerințe ating fișiere disjuncte de cele din 14-02/14-03 (componentă nouă + RaportFinanciar + UserProfile), deci rulează în paralel în wave 2. Depinde doar de 14-01 (util `calculeazaLuniLipsa`, hook `useDataStartFacturare`, serviciu `genereazaFacturaAbonament`).

Purpose: Adminul are vizibilitate completă asupra lunilor neacoperite — un raport centralizat și un badge pe fiecare profil — și poate genera bulk facturile lipsă fără a le emite una câte una.
Output: O componentă wizard nouă + un tab nou în raportul financiar + un badge și un câmp de setare în profilul sportivului.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-corelare-prezente-facturi/14-CONTEXT.md
@.planning/phases/14-corelare-prezente-facturi/14-RESEARCH.md
@CLAUDE.md
@utils/luniLipsa.ts
@hooks/useDataStartFacturare.ts
@services/facturaService.ts
@components/Plati/RaportFinanciar.tsx
@components/UserProfile.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: LuniLipsaWizard — detectare + generare bulk</name>
  <read_first>
    - utils/luniLipsa.ts (calculeazaLuniLipsa, formatLuna)
    - services/facturaService.ts (genereazaFacturaAbonament)
    - hooks/useDataStartFacturare.ts (citire data_start_facturare per sportiv)
    - components/Plati/GestiuneFacturi.tsx liniile 87-95 (calcul sumă abonament per sportiv/familie) — refolosește aceeași logică pentru suma bulk
    - components/ui.tsx (Modal, Button, Badge — citire only, NU modificare)
    - 14-RESEARCH.md Pattern 2 (calcul luni lipsă), Anti-pattern (useMemo, nu loop sincron în render), Pitfall 6 (data_start_facturare poate fi null → skip)
    - types.ts (Sportiv: status 'Activ', Plata)
  </read_first>
  <action>
    Creează `components/Plati/LuniLipsaWizard.tsx` ca un Modal (folosind `Modal` din ui.tsx, fără a modifica ui.tsx) care primește props: `sportiv: Sportiv` (sau o listă pentru mod multi — vezi mai jos), `plati: Plata[]`, `tipuriAbonament: TipAbonament[]`, `setPlati: React.Dispatch<...>`, `onClose: () => void`. Suportă două moduri de deschidere: (a) pentru un singur sportiv (apelat din tab cu un sportiv selectat). 

    Logică: pentru sportivul dat, citește data_start_facturare via `useDataStartFacturare(sportiv.id)`. Dacă e null → afișează mesaj "Setează mai întâi data de start facturare pentru acest sportiv" și un câmp `<Input type="date">` care salvează prin mutation-ul din hook. Dacă e setat: calculează `luniLipsa = useMemo(() => calculeazaLuniLipsa(dataStart, plati.filter(p => p.sportiv_id === sportiv.id)), [dataStart, plati, sportiv.id])`. Afișează lista lunilor lipsă (formatLuna pentru fiecare) cu checkbox-uri (toate bifate default) și un buton "Generează N facturi". 

    Handler bulk `handleGenereazaBulk`: pentru fiecare lună selectată, calculează suma abonamentului (refolosește logica din GestiuneFacturi liniile 87-95: familie → config pe nr membri activi; altfel tip_abonament_id sau numar_membri===1) și apelează `await genereazaFacturaAbonament({ sportiv, luna, an, suma, descriere: 'Abonament ' + formatLuna(luna, an), observatii: 'Generat bulk din wizard luni lipsă' })`. Colectează rezultatele; pentru fiecare `data` non-null fă `setPlati(prev => [data, ...prev])`; numără succesele și duplicatele sărite. La final `showSuccess('Generare finalizată', '${nr} facturi generate.')` și `onClose()`. Folosește un state `isGenerating` pentru a bloca dublu-click. NU recalcula soldul (anti-pattern interzis). Calculul luni lipsă se face DOAR pentru sportivi cu `status === 'Activ'` (calculeazaLuniLipsa deja are guard pe dataStart; adaugă guard suplimentar `sportiv.status === 'Activ'` la nivel de wizard).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "LuniLipsaWizard" || echo "no type errors in LuniLipsaWizard"</automated>
  </verify>
  <acceptance_criteria>
    - `components/Plati/LuniLipsaWizard.tsx` exportă `LuniLipsaWizard`, folosește `calculeazaLuniLipsa` și `genereazaFacturaAbonament` (grep confirmă ambele importuri)
    - Wizardul cere data_start_facturare dacă lipsește și o salvează prin `useDataStartFacturare`
    - Generarea bulk apelează serviciul per lună, sare duplicatele și actualizează setPlati
    - Calcul restrâns la sportivi activi; folosește useMemo pentru luniLipsa (nu calcul în render loop)
    - NU modifică ui.tsx, types.ts
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>Wizardul detectează lunile lipsă pentru un sportiv activ cu data_start_facturare setată și generează bulk facturile selectate.</done>
</task>

<task type="auto">
  <name>Task 2: Tab 'Luni Lipsă' în RaportFinanciar (PLF-05)</name>
  <read_first>
    - components/Plati/RaportFinanciar.tsx liniile 48-67 (state, activeTab union type), 338-364 (array tabs + tab bar), 995-1106 (pattern tab Restanțe: tabel desktop + carduri mobil)
    - components/Plati/RaportFinanciar.tsx liniile 1108-1117 (modalele la final — locul de montare a wizardului)
    - utils/luniLipsa.ts (calculeazaLuniLipsa, formatLuna)
    - components/Plati/LuniLipsaWizard.tsx (props LuniLipsaWizard)
    - hooks/useDataStartFacturare.ts — NOTĂ: pentru tabel ai nevoie de data_start_facturare pentru MULȚI sportivi; nu apela hook per rând. Creează `hooks/useDataStartFacturareAll.ts` care face un singur `useQuery` cu `supabase.from('sportivi').select('id, data_start_facturare').eq('status', 'Activ')` și returnează `Record<string, string | null>`. Importă-l în RaportFinanciar.tsx în loc de apel direct supabase în useEffect.
    - 14-RESEARCH.md Pitfall 6 (null skip)
  </read_first>
  <action>
    Adaugă tab-ul `luni_lipsa` în RaportFinanciar. Extinde union type-ul `activeTab` (linia 54) cu `'luni_lipsa'`. Adaugă în array-ul `tabs` (liniile 338-346) o intrare `{ id: 'luni_lipsa' as const, label: 'Luni Lipsă', icon: <ExclamationTriangleIcon className="w-4 h-4 text-amber-400" /> }` (importul ExclamationTriangleIcon există deja). 

    Adaugă fetch unic al datelor de start: importă și apelează `useDataStartFacturareAll()` (hook din `hooks/useDataStartFacturareAll.ts`, creat anterior în Task 1 sau creat acum dacă Task 1 nu l-a creat — hook-ul face un singur React Query `useQuery` cu supabase.from('sportivi').select('id, data_start_facturare').eq('status','Activ') și returnează `Record<string, string | null>`) — salvează rezultatul în `dataStartMap`. Calculează cu `useMemo` `sportiviCuLuniLipsa`: pentru fiecare sportiv din `sportivi` cu `status === 'Activ'` și `dataStartMap[s.id]` non-null, `calculeazaLuniLipsa(dataStartMap[s.id], plati.filter(p => p.sportiv_id === s.id))`; păstrează doar cei cu `length > 0`, ordonați descrescător după numărul de luni lipsă. Adaugă blocul de randare `{activeTab === 'luni_lipsa' && (...)}` (după blocul tab restante, înainte de modale ~linia 1106): tabel desktop (Sportiv | Data Start Facturare | Nr. Luni Lipsă | Acțiune) și carduri mobil, urmând pattern-ul vizual al tabului Restanțe. Coloana Acțiune are un buton "Generează" care setează `wizardSportiv: Sportiv | null`. Empty state când lista e goală: "Toți sportivii activi au facturile la zi." 

    La finalul componentei (lângă celelalte modale, ~linia 1117) montează `{wizardSportiv && <LuniLipsaWizard sportiv={wizardSportiv} plati={plati} tipuriAbonament={...} setPlati={setPlati} onClose={() => setWizardSportiv(null)} />}`. Pentru tipuriAbonament: dacă RaportFinanciar nu le primește ca prop, ia-le din `useData()` (DataContext — doar citire, NU modificare). Importă `calculeazaLuniLipsa`, `formatLuna` din utils și `LuniLipsaWizard`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "RaportFinanciar" || echo "no type errors in RaportFinanciar"</automated>
  </verify>
  <acceptance_criteria>
    - RaportFinanciar are tab `luni_lipsa` în union type și în array-ul tabs
    - data_start_facturare e citit printr-un singur fetch (nu hook per rând)
    - Tabul listează doar sportivi activi cu luni lipsă > 0, sortați descrescător
    - Butonul "Generează" deschide LuniLipsaWizard pentru sportivul respectiv
    - Empty state prezent; nu modifică ui.tsx, types.ts, DataContext
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>Tabul Luni Lipsă afișează toți sportivii activi cu facturi neemise și deschide wizardul de generare per sportiv.</done>
</task>

<task type="auto">
  <name>Task 3: Badge luni fără factură + setare data_start_facturare în UserProfile (PLF-05)</name>
  <read_first>
    - components/UserProfile.tsx liniile 630-655 (header: nume, status badge, cod_sportiv — locul badge-ului nou)
    - components/UserProfile.tsx liniile 756-771 (randarea FinanciarTab — sportiv, plati disponibile aici)
    - components/UserProfile.tsx liniile 100-111 (handleToggleStatus — pattern update pe sportivi via supabase, model pentru salvarea data_start_facturare)
    - utils/luniLipsa.ts (calculeazaLuniLipsa)
    - hooks/useDataStartFacturare.ts (citire/scriere data_start_facturare)
    - components/ui.tsx (Badge variant amber — citire only)
    - 14-RESEARCH.md Code Examples (Badge amber), Pitfall 6 (null → badge absent)
  </read_first>
  <action>
    PLF-05 badge în UserProfile header: lângă status și cod_sportiv (liniile 649-653), adaugă un badge condiționat. Citește data_start_facturare via `useDataStartFacturare(sportiv.id)`. Calculează `const luniLipsa = useMemo(() => calculeazaLuniLipsa(dataStart, plati.filter(p => p.sportiv_id === sportiv.id)), [dataStart, plati, sportiv.id])`. Afișează badge DOAR dacă `sportiv.status === 'Activ' && dataStart && luniLipsa.length > 0`: un `<span>` amber stilizat consistent cu badge-urile existente din header (ex. `px-3 py-1 text-xs font-bold uppercase tracking-wider rounded-full bg-amber-500/20 text-amber-400 border border-amber-500/30`) cu textul `${luniLipsa.length} luni fără factură`. NU folosi Badge din ui.tsx dacă necesită import nou care complică — pattern-ul span existent din header e suficient și consistent.

    Setare data_start_facturare: adaugă în header (sub status, vizibil doar pentru `isAdminOrAbove`) un mic control — fie un buton "Set. dată facturare" care deschide un mic popover/modal cu `<Input type="date">`, fie un câmp inline. La salvare folosește mutation-ul `setDataStartFacturare` din `useDataStartFacturare`. La succes, badge-ul se recalculează automat (queryKey invalidat în hook). Plasează controlul discret ca să nu aglomereze header-ul. Verifică ce nume are variabila de permisiuni admin în UserProfile (isAdminOrAbove / isClubAdmin — vezi liniile existente).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "UserProfile.tsx" || echo "no type errors in UserProfile"</automated>
  </verify>
  <acceptance_criteria>
    - UserProfile afișează badge "X luni fără factură" doar pentru sportivi activi cu data_start_facturare setat și luniLipsa > 0
    - Adminul poate seta data_start_facturare din profil prin `setDataStartFacturare` (mutation din hook)
    - Badge-ul se recalculează după setarea datei (queryKey invalidat)
    - calculeazaLuniLipsa folosit (grep confirmă import); nu modifică ui.tsx, types.ts, DataContext
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>Profilul sportivului activ afișează badge-ul de luni fără factură și permite adminului să seteze data de start facturare.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PLF-03 (wizard luni lipsă + generare bulk) și PLF-05 (tab centralizat în RaportFinanciar + badge pe profil + setare data_start_facturare).</what-built>
  <how-to-verify>
    1. Loghează-te ca ADMIN_CLUB. Deschide profilul unui sportiv activ și setează data_start_facturare la o lună din trecut (ex. acum 4 luni).
    2. Confirmă că pe header apare badge-ul "X luni fără factură" cu numărul corect de luni neemise.
    3. Mergi la Raport Financiar → tab "Luni Lipsă". Confirmă că sportivul apare în listă cu numărul corect de luni și data de start.
    4. Click pe "Generează" → wizardul arată lunile lipsă bifate → click "Generează N facturi".
    5. Confirmă că facturile au fost create (verifică în Management Facturi / Gestiune Facturi) și că badge-ul de pe profil scade la 0 / dispare.
    6. Re-deschide wizardul → confirmă că nu mai sunt luni lipsă (duplicate blocate).
    7. Confirmă că un sportiv inactiv NU apare în tabul Luni Lipsă și NU are badge.
  </how-to-verify>
  <resume-signal>Scrie "approved" sau descrie problemele observate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (plati bulk insert) | generare bulk Abonament — luni/sume netrusted |
| client → Supabase (sportivi update) | setare data_start_facturare |
| client → Supabase (sportivi select) | citire data_start_facturare pentru raport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Data Integrity | generare bulk wizard | mitigate | genereazaFacturaAbonament verifică duplicat per lună; isGenerating blochează dublu-click |
| T-14-11 | Tampering | update data_start_facturare | mitigate | RLS existent pe sportivi limitează UPDATE la clubul propriu |
| T-14-12 | Information Disclosure | fetch sportivi data_start_facturare în raport | accept | RLS pe sportivi returnează doar sportivii vizibili (visibleClubIds) |
</threat_model>

<verification>
- `npx tsc --noEmit` fără erori noi în LuniLipsaWizard.tsx, RaportFinanciar.tsx, UserProfile.tsx
- `git diff --name-only` NU include types.ts, ui.tsx, DataContext, NavigationContext
- Calculul luni lipsă în tabel folosește un singur fetch (nu hook per rând) — fără warning React hooks
</verification>

<success_criteria>
- PLF-03: wizard detectează și generează bulk lunile lipsă pentru sportivi activi
- PLF-05: tab centralizat + badge pe profil; ambele restrânse la sportivi activi cu data_start_facturare setat
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-04)
- `hooks/useDataStartFacturareAll.ts` (NOU) — React Query hook bulk fetch `data_start_facturare` pentru toți sportivii activi → `Record<string, string | null>`
- `components/Plati/LuniLipsaWizard.tsx` (NOU) — wizard detectare + generare bulk luni lipsă (PLF-03)
- `components/Plati/RaportFinanciar.tsx` — tab nou luni_lipsa cu listă sportivi activi afectați (PLF-05)
- `components/UserProfile.tsx` — badge luni fără factură + setare data_start_facturare în header (PLF-05)
</artifacts_produced>

<output>
Create `.planning/phases/14-corelare-prezente-facturi/14-04-SUMMARY.md` when done
</output>
