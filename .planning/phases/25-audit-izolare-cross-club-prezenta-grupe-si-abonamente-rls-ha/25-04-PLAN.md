---
phase: 25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha
plan: 04
type: execute
wave: 2
depends_on: [25-01, 25-02, 25-03]
files_modified:
  - tests/rls_izolare_cross_club_faza25.ts
  - .planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-VERIFICARE.md
autonomous: true
requirements: [MCLB-01, MCLB-02, MCLB-03, MCLB-04, MCLB-05, MCLB-06, MCLB-07, MCLB-08]
must_haves:
  truths:
    - "Migratia din 25-01 este aplicata pe Supabase live, confirmata prin interogarea pg_policies dupa aplicare"
    - "Un client autentificat cu contextul activ la Clubul A nu poate citi niciun rand al Clubului B din grupe, evenimente, perioade_vacanta, participare_vacanta, tipuri_abonament, program_antrenamente"
    - "Un client autentificat cu contextul activ la Clubul A nu poate INSERA un rand cu club_id-ul Clubului B"
    - "Un cont de SPORTIV vede in continuare tipurile de abonament ale clubului sau (fara regresie functionala)"
    - "UI-ul live pentru un cont de ADMIN_CLUB al unui al doilea club incarca Grupe, Prezenta si Abonamente fara erori in consola si fara date din alt club"
  artifacts:
    - path: "tests/rls_izolare_cross_club_faza25.ts"
      provides: "Test automat de izolare cross-club (canary rows + client cu header de context activ)"
      min_lines: 80
    - path: ".planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-VERIFICARE.md"
      provides: "Dovezi post-aplicare: pg_policies, rezultate test izolare, verificare UI"
      contains: "pg_policies"
  key_links:
    - from: "tests/rls_izolare_cross_club_faza25.ts"
      to: "header active-role-context-id"
      via: "createClient cu global.headers pentru simularea contextului activ"
      pattern: "active-role-context-id"
    - from: "supabase/migrations/20260828_fix_rls_izolare_cross_club_grupe_prezenta_abonamente.sql"
      to: "Supabase live (proiect wuhidifzsutwgdfkwhmd)"
      via: "MCP apply_migration"
      pattern: "apply_migration"
---

<objective>
Aplica migratia scrisa in 25-01 pe baza de date live si DOVEDESTE, cu date reale din doua cluburi, ca izolarea cross-club functioneaza pe modulele Grupe, Prezenta si Abonamente — la citire si la scriere — fara regresii functionale pentru rolurile ADMIN_CLUB, INSTRUCTOR si SPORTIV.

Purpose: D-01 cere audit SI fix aplicat live, in aceeasi faza, cu verificare pe date reale si pe UI. Un fix RLS nedovedit nu are valoare inainte de sezonul nou.
Output: DB live modificat, `tests/rls_izolare_cross_club_faza25.ts` (test reutilizabil), `25-VERIFICARE.md` (dovezi).
</objective>

## Cerinte acoperite de acest plan

| REQ | Descriere |
|-----|-----------|
| MCLB-01..05 | Verificarea live ca fix-urile RLS scrise in 25-01 sunt aplicate si eficace |
| MCLB-06, MCLB-07 | Verificarea ca fix-urile de frontend (context activ) si empty-state-urile se comporta corect pe un club real fara date |
| MCLB-08 | Aplicare live + dovada de izolare (SQL + test automat + UI) |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-CONTEXT.md
@.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-AUDIT.md
@.planning/phases/15-fix-rls-izolare-cross-club-pe-tabele-financiare-alocari-plat/15-01-SUMMARY.md
@.planning/phases/16-elimina-politici-rls-using-true-ramase-rezultate-facturi-fed/16-01-SUMMARY.md
</context>

## Nota de infrastructura (precedent Faza 15 si 16)

Aplicarea live se face prin tool-ul MCP Supabase `apply_migration`, NU printr-o comanda CLI. In acest proiect nu exista pas `supabase db push` — folderele `sql/` si `supabase/` sunt in `.gitignore` (la fel si `*.sql`), iar sursa de adevar este baza de date live.

Ambele SUMMARY-uri anterioare (15-01, 16-01) documenteaza ca subagentul executor NU are acces la tool-urile MCP Supabase in acest mediu si ca executia a fost preluata de orchestrator. Daca acest agent nu vede tool-urile MCP, escaladeaza catre orchestrator cu calea fisierului de migratie si continutul lui. Daca nici orchestratorul nu are acces, opreste-te si raporteaza BLOCAT, cu instructiuni de aplicare manuala prin Supabase SQL Editor — NU marca planul complet si NU declara verificarea trecuta.

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Aplica migratia live si confirma politicile in pg_policies</name>
  <files>(fara fisiere in repo — modificare de stare pe Supabase live)</files>
  <read_first>
    - `supabase/migrations/20260828_fix_rls_izolare_cross_club_grupe_prezenta_abonamente.sql` — migratia completa produsa de 25-01 (citeste-o integral inainte de aplicare)
    - `.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-AUDIT.md` — inventarul politicilor dinainte de aplicare, folosit ca baza de comparatie
    - `.planning/phases/15-fix-rls-izolare-cross-club-pe-tabele-financiare-alocari-plat/15-01-SUMMARY.md` — procedura exacta de aplicare acceptata anterior (nume de migratie, proiect `wuhidifzsutwgdfkwhmd`, verificare ulterioara)
  </read_first>
  <action>
Aplica migratia pe proiectul Supabase live `wuhidifzsutwgdfkwhmd` cu tool-ul MCP `apply_migration`, folosind numele de migratie `fix_rls_izolare_cross_club_grupe_prezenta_abonamente`. Aplica fisierul integral, intr-o singura tranzactie; daca aplicarea esueaza partial, opreste-te si raporteaza starea exacta (ce sectiuni au trecut), nu incerca reparatii improvizate.

Imediat dupa aplicare, ruleaza cu `execute_sql` urmatoarele verificari si salveaza output-ul brut pentru `25-VERIFICARE.md`:

1. `SELECT tablename, policyname, cmd, qual, with_check FROM pg_policies WHERE tablename IN ('grupe','evenimente','perioade_vacanta','participare_vacanta','tipuri_abonament','orar_exceptii','program_antrenamente','sesiune_activitate') ORDER BY tablename, policyname;` — confirma ca politicile noi exista si ca cele vechi au disparut.
2. `SELECT tablename, policyname FROM pg_policies WHERE qual = 'true' AND tablename IN (...aceeasi lista...);` — trebuie sa returneze ZERO randuri.
3. `SELECT tablename, policyname FROM pg_policies WHERE qual ILIKE '%get_my_club_ids%' OR with_check ILIKE '%get_my_club_ids%';` — trebuie sa returneze ZERO randuri.
4. `SELECT tablename, policyname FROM pg_policies WHERE qual ILIKE '%club_id IS NULL OR%';` — trebuie sa returneze ZERO randuri (fail-closed, D-02).
5. `SELECT count(*) FILTER (WHERE club_id IS NULL) AS fara_club, count(*) AS total FROM public.program_antrenamente;` si echivalentul pentru `public.sesiune_activitate` — confirma backfill-ul.
6. `SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'sesiune_activitate' AND column_name = 'club_id';` — confirma coloana noua.
7. Verifica supravietuirea cailor SPORTIV: `SELECT policyname FROM pg_policies WHERE tablename IN ('plati','prezenta_antrenament') AND policyname IN ('rbv_plati_own','rbv_prezenta_own');` — ambele trebuie sa existe in continuare.

Daca oricare din verificarile 2, 3, 4 sau 7 esueaza, migratia e incompleta: opreste-te, documenteaza si corecteaza inainte de a trece la Task 2.
  </action>
  <verify>
    <automated>MISSING — verificarea se face prin MCP execute_sql (pg_catalog nu e accesibil prin PostgREST); output-ul brut al celor 7 interogari se lipeste in 25-VERIFICARE.md si e validat de gate-ul automat din Task 3</automated>
  </verify>
  <acceptance_criteria>
    - `apply_migration` a returnat succes pentru numele `fix_rls_izolare_cross_club_grupe_prezenta_abonamente`.
    - Interogarea (1) arata, pentru fiecare din cele 8 tabele, politici care apeleaza `has_access_to_club` / `este_staff_club` / `is_super_admin` si nicio politica ramasa din setul vechi.
    - Interogarile (2), (3) si (4) returneaza fiecare ZERO randuri.
    - `sesiune_activitate` are coloana `club_id` de tip uuid, iar numarul de randuri fara club dupa backfill e cel prezis in `25-AUDIT.md`.
    - `program_antrenamente` are numarul de randuri cu `club_id NULL` egal cu cel prezis in `25-AUDIT.md` (0 daca backfill-ul a fost posibil).
    - Politicile `rbv_plati_own` si `rbv_prezenta_own` exista in continuare.
  </acceptance_criteria>
  <done>Migratia e aplicata live si structura politicilor e confirmata prin pg_policies.</done>
</task>

<task type="auto">
  <name>Task 2: Test automat de izolare cross-club pe date reale (citire + scriere)</name>
  <files>tests/rls_izolare_cross_club_faza25.ts</files>
  <read_first>
    - `tests/rls_admin_test.ts` — pattern-ul existent de test RLS: client admin cu `SUPABASE_SERVICE_ROLE_KEY`, creare de utilizator de test, cleanup
    - `supabaseClient.ts` liniile 1-30 — cum se injecteaza headerul `active-role-context-id` (in test se pune prin optiunea `global.headers` la `createClient`)
    - `sql/migrations/fix_rls_context_aware_role_helpers.sql` — de ce headerul trebuie sa corespunda unui rand din `utilizator_roluri_multicont` care apartine userului autentificat
    - `.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-AUDIT.md` — sectiunea "Cluburi de referinta pentru testul de izolare" (cele doua UUID-uri reale)
  </read_first>
  <action>
Scrie `tests/rls_izolare_cross_club_faza25.ts`, un test executabil cu `npx tsx`, structurat astfel:

Pregatire (client admin cu `SUPABASE_SERVICE_ROLE_KEY`): creeaza un utilizator de test efemer cu `auth.admin.createUser` (email cu timestamp, `email_confirm: true`), insereaza pentru el un rand in `utilizator_roluri_multicont` cu `rol_denumire = 'ADMIN_CLUB'` si `club_id` = Clubul A din audit, si retine id-ul acelui rand (va fi valoarea headerului `active-role-context-id`). Insereaza randuri canar apartinand Clubului B in: `grupe`, `perioade_vacanta`, `tipuri_abonament` si `program_antrenamente` (respecta coloanele obligatorii reale ale fiecarei tabele, citite din schema, si prefixeaza denumirile cu `ZZ_TEST_FAZA25_` pentru identificare).

Test (client cu cheia anon + `global: { headers: { 'active-role-context-id': <id context Club A> } }`, autentificat cu `signInWithPassword` pe utilizatorul de test):
- SELECT pe fiecare din `grupe`, `evenimente`, `perioade_vacanta`, `participare_vacanta`, `tipuri_abonament`, `program_antrenamente`: asserteaza ca ZERO randuri returnate au `club_id` egal cu Clubul B, si ca niciun rand canar (`ZZ_TEST_FAZA25_`) nu apare in rezultat.
- INSERT cross-club: incearca sa inserezi in `perioade_vacanta` si in `tipuri_abonament` un rand cu `club_id` = Clubul B; asserteaza ca ambele esueaza (eroare RLS, nu succes).
- INSERT in propriul club: insereaza in `tipuri_abonament` un rand cu `club_id` = Clubul A; asserteaza ca reuseste (dovada ca fix-ul nu a blocat operarea normala). Sterge randul dupa test.
- Fara regresie SPORTIV: daca `25-AUDIT.md` a identificat cel putin un cont/context SPORTIV utilizabil, repeta SELECT pe `tipuri_abonament` cu headerul acelui context si asserteaza ca returneaza cel putin randurile clubului propriu. Daca nu exista un context SPORTIV de test, creeaza unul efemer prin acelasi mecanism (rand `utilizator_roluri_multicont` cu `rol_denumire = 'SPORTIV'` legat de un sportiv existent al Clubului A) si documenteaza in output.

Cleanup obligatoriu, in `finally`: sterge toate randurile canar `ZZ_TEST_FAZA25_`, randurile din `utilizator_roluri_multicont` create de test si utilizatorul de test (`auth.admin.deleteUser`). Testul nu trebuie sa lase reziduuri in DB nici cand pica.

Iesire: `process.exit(1)` la primul assert picat, cu mesaj clar care numeste tabela si clubul; `process.exit(0)` si un rezumat tabelar cand toate trec.
  </action>
  <verify>
    <automated>npx tsx tests/rls_izolare_cross_club_faza25.ts</automated>
    <automated>grep -q "active-role-context-id" tests/rls_izolare_cross_club_faza25.ts &amp;&amp; grep -q "ZZ_TEST_FAZA25_" tests/rls_izolare_cross_club_faza25.ts &amp;&amp; grep -q "deleteUser" tests/rls_izolare_cross_club_faza25.ts &amp;&amp; echo TEST_STRUCTURE_OK</automated>
    <automated>npx tsx scripts/audit_rls_faza25.ts | grep -ci "ZZ_TEST_FAZA25_" | grep -qx 0 &amp;&amp; echo NO_TEST_RESIDUE</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx tests/rls_izolare_cross_club_faza25.ts` iese cu cod 0 si afiseaza rezultat PASS pentru fiecare din cele 6 tabele verificate la citire.
    - Testul verifica explicit si scrierea: 2 INSERT-uri cross-club respinse + 1 INSERT in clubul propriu acceptat.
    - Testul acopera calea SPORTIV pe `tipuri_abonament` (fara regresie).
    - Dupa rulare, o re-rulare a `scripts/audit_rls_faza25.ts` nu mai gaseste niciun rand `ZZ_TEST_FAZA25_` (cleanup complet).
    - Testul nu contine UUID-uri de club hardcodate in afara celor doua preluate explicit din `25-AUDIT.md` (documentate ca atare intr-o constanta la inceputul fisierului).
  </acceptance_criteria>
  <done>Izolarea cross-club e demonstrata automat, pe date reale, la citire si la scriere.</done>
</task>

<task type="auto">
  <name>Task 3: Verificare UI live pe al doilea club si consemnarea dovezilor</name>
  <files>.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-VERIFICARE.md</files>
  <read_first>
    - `.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-AUDIT.md` — cluburile de referinta si predictiile de date
    - `.planning/phases/15-fix-rls-izolare-cross-club-pe-tabele-financiare-alocari-plat/15-01-SUMMARY.md` — sectiunea de verificare (SQL + UI Playwright + confirmare user), formatul acceptat anterior
    - `.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-02-SUMMARY.md` si `25-03-SUMMARY.md` — ce s-a schimbat in UI (empty states, derivare club activ), ca sa stii ce verifici
  </read_first>
  <action>
Porneste aplicatia local (`npm run dev`) si verifica in browser cu un cont real de ADMIN_CLUB al celui de-al doilea club (NU super admin), folosind Playwright sau navigare manuala asistata:

1. Modulul Grupe: lista se incarca; apar doar grupele clubului propriu (compara cu numarul din `scripts/audit_rls_faza25.ts` pentru acel club); butonul "Adaugă Grupă" deschide modalul.
2. Modulul Prezenta, tabul Grupe si tabul Rapid: se incarca fara eroare; nicio grupa/antrenament din alt club.
3. Plati -> Tipuri Abonament: se incarca; apar doar tipurile clubului propriu (plus nomenclatorul federal, daca `25-AUDIT.md` a decis ca randurile cu club_id NULL sunt partajate).
4. Perioade vacanta (`components/Plati/PerioadaVacanta.tsx`): lista se incarca fara eroare de permisiune.
5. Empty states (D-05): daca al doilea club are date, verifica empty-state-urile pe un club fara date sau, in lipsa lui, prin filtrarea temporara a listei — obiectivul e sa confirmi ca mesajul si butonul apar si ca butonul declanseaza actiunea corecta.
6. Consola browserului: zero erori (in special zero erori 401/403 sau "permission denied for table") pe toate ecranele de mai sus.

Scrie `25-VERIFICARE.md` cu sectiunile: `## Aplicare migratie` (nume migratie, data, rezultat), `## Dovezi pg_policies` (output-ul brut al celor 7 interogari din Task 1), `## Test automat izolare` (output-ul rularii din Task 2), `## Verificare UI live` (ecranele parcurse, contul si clubul folosit, ce s-a observat, erori de consola), `## Risc rezidual` (randuri ramase orfane vizibile doar super_admin, decizii amanate, follow-up-uri).

Actualizeaza `.planning/STATE.md` la sectiunea Decisions cu decizia luata pentru `get_my_club_ids()` si cu semantica aleasa pentru `tipuri_abonament.club_id IS NULL`.
  </action>
  <verify>
    <automated>V=.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-VERIFICARE.md; for s in "Aplicare migratie" "Dovezi pg_policies" "Test automat izolare" "Verificare UI live" "Risc rezidual"; do grep -q "$s" $V || { echo "LIPSA sectiune: $s"; exit 1; }; done; grep -q "pg_policies" $V &amp;&amp; echo VERIFICARE_SECTIONS_OK</automated>
    <automated>npx tsx tests/rls_izolare_cross_club_faza25.ts</automated>
    <automated>npm run lint</automated>
    <human-check>Cu contul de ADMIN_CLUB al celui de-al doilea club: Grupe, Prezenta (Rapid + Grupe), Tipuri Abonament si Perioade Vacanta se incarca, arata DOAR date ale clubului propriu, empty-state-urile apar acolo unde nu exista date si butoanele lor deschid actiunea corecta, iar consola browserului nu are nicio eroare.</human-check>
  </verify>
  <acceptance_criteria>
    - `25-VERIFICARE.md` contine toate cele 5 sectiuni, cu output brut (nu parafrazat) pentru pg_policies si pentru testul automat.
    - Sectiunea "Verificare UI live" numeste contul si clubul folosit si listeaza cele 6 puncte de verificare cu rezultat individual.
    - Zero erori de consola raportate; daca apar, sunt listate cu mesaj exact si tratate inainte de inchiderea planului.
    - `.planning/STATE.md` contine decizia despre `get_my_club_ids()` si despre semantica `tipuri_abonament.club_id IS NULL`.
    - Sectiunea "Risc rezidual" enumera explicit randurile orfane ramase vizibile doar prin `is_super_admin()` si follow-up-ul de cablare a prop-ului `activeClubId` din `GrupaFormModal` (ramas din 25-03).
  </acceptance_criteria>
  <done>Faza are dovada scrisa, reproductibila, ca izolarea cross-club functioneaza pe DB si in UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> Supabase live (`apply_migration`) | Operatiune ireversibila pe date de productie; se aplica un fisier revizuit, nu SQL improvizat |
| test efemer (user + randuri canar) -> date de productie | Testul scrie in DB-ul real; cleanup obligatoriu si prefix identificabil |
| client autentificat cu header de context -> RLS | Suprafata reala de atac testata de acest plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-14 | Denial of Service | Migratie aplicata partial care lasa tabele fara politici (acces blocat total) | mitigate | Aplicare intr-o singura tranzactie; la esec partial, oprire si raportare, fara reparatii improvizate; verificarile (1)-(7) ruleaza imediat dupa aplicare |
| T-25-15 | Tampering | Randurile canar ale testului raman in productie | mitigate | Prefix `ZZ_TEST_FAZA25_`, cleanup in `finally`, gate automat care re-ruleaza auditul si cere zero reziduuri |
| T-25-16 | Elevation of Privilege | Utilizatorul de test efemer ramane activ cu rol ADMIN_CLUB | mitigate | `auth.admin.deleteUser` + stergerea randurilor din `utilizator_roluri_multicont` in `finally` |
| T-25-17 | Information Disclosure | Se declara "verificat" fara dovada reala (parafrazare in loc de output) | mitigate | Acceptance criteria cer output brut in `25-VERIFICARE.md` si re-rularea testului ca gate automat |
| T-25-18 | Denial of Service | Restrangerea RLS blocheaza accidental un rol legitim (SPORTIV / INSTRUCTOR) | mitigate | Test dedicat pentru calea SPORTIV + verificare UI pe cont non-super-admin + verificarea supravietuirii politicilor `rbv_*_own` |
| T-25-SC | Tampering | Instalari de pachete | accept | Zero pachete noi; `tsx`, `@supabase/supabase-js` si Playwright exista deja in `package.json` |
</threat_model>

## Artifacts this phase produces (contributia acestui plan)

| Artefact | Tip | Detalii |
|----------|-----|---------|
| Migratie aplicata live | stare DB | `fix_rls_izolare_cross_club_grupe_prezenta_abonamente` pe proiectul `wuhidifzsutwgdfkwhmd` |
| Politici RLS active | obiecte DB | pe `grupe`, `evenimente`, `perioade_vacanta`, `participare_vacanta`, `tipuri_abonament`, `orar_exceptii`, `program_antrenamente`, `sesiune_activitate` |
| `public.sesiune_activitate.club_id` | coloana noua live | `UUID REFERENCES public.cluburi(id)`, backfill din `evenimente` |
| `tests/rls_izolare_cross_club_faza25.ts` | fisier nou (tracked) | test automat de izolare cross-club, reutilizabil in fazele urmatoare |
| `.planning/phases/25-.../25-VERIFICARE.md` | fisier nou (tracked) | dovezi post-aplicare + risc rezidual |

<verification>
- `apply_migration` a returnat succes si cele 7 interogari post-aplicare confirma starea asteptata.
- `npx tsx tests/rls_izolare_cross_club_faza25.ts` iese cu cod 0.
- `npm run lint` trece (frontend-ul din 25-02 si 25-03 e integrat).
- `25-VERIFICARE.md` contine toate cele 5 sectiuni cu output brut.
- <human-check>Confirmarea finala a userului dupa parcurgerea UI-ului pe contul celui de-al doilea club.</human-check>
</verification>

<success_criteria>
- Izolarea cross-club pe Grupe / Prezenta / Abonamente e activa pe DB live si demonstrata cu date reale din doua cluburi, la citire si la scriere.
- Zero regresii: ADMIN_CLUB, INSTRUCTOR si SPORTIV isi vad in continuare datele proprii; zero erori de consola.
- Riscul rezidual (randuri orfane, follow-up-uri) e documentat explicit, nu ascuns.
</success_criteria>

<output>
Create `.planning/phases/25-audit-izolare-cross-club-prezenta-grupe-si-abonamente-rls-ha/25-04-SUMMARY.md` when done.
</output>
