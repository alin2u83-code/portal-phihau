---
phase: quick-260926-b7z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types.ts
  - utils/notificariRestantieri.ts
  - utils/notificariRestantieri.test.ts
  - components/Plati/NotificariRestantieriModal.tsx
  - components/Plati/PlatiScadente.tsx
autonomous: true
requirements: [QUICK-260926-B7Z-01, QUICK-260926-B7Z-02, QUICK-260926-B7Z-03]

must_haves:
  truths:
    - "In ecranul 'Management Facturi & Plăți' (PlatiScadente), un utilizator cu permissions.canManageFinances vede butonul 'Trimite notificări restanțieri', vizibil indiferent daca are sau nu facturi selectate"
    - "Click pe buton deschide un modal care listeaza destinatarii pentru facturile tip 'Abonament' de incasat (esteDeIncasat: Neachitat / Achitat Parțial; exclus Achitat si Anulat) din luna curenta — calculat client-side din filteredData, fara niciun query Supabase nou"
    - "Fiecare destinatar are mesajul pre-generat 'Bună ziua! {Nume sportiv} are de achitat taxa lunară {sumă} lei pentru {lună an}. Mulțumim!' (verb 'au' si nume unite cu 'și' cand mesajul acopera mai multi sportivi)"
    - "Butonul 'Copiază mesaj' pune textul in clipboard (feedback 'Copiat!'), iar butonul 'WhatsApp' deschide https://wa.me/{numar international doar cifre}?text={mesaj URL-encodat} intr-un tab nou"
    - "Factura de familie foloseste telefonul reprezentantului familiei (fallback: primul membru cu telefon valid); doua facturi cu acelasi numar de telefon (frati fara familie) produc UN singur mesaj cu suma totala"
    - "Sportivii fara telefon valid apar marcati 'Fără telefon valid', au 'Copiază mesaj' disponibil, dar nu au link WhatsApp"
    - "SUPER_ADMIN cu filtru de club activ vede doar restantierii acelui club; ADMIN_CLUB vede doar datele propriului club (filteredData deja scopat RLS + visibleClubIds)"
  artifacts:
    - path: "utils/notificariRestantieri.ts"
      provides: "Logica pura: normalizare telefon wa.me, mesaj template, link WhatsApp, generare+grupare notificari, luni cu restante"
      exports: ["normalizeazaTelefonWa", "formateazaSumaLei", "construiesteMesajRestanta", "construiesteLinkWhatsApp", "genereazaNotificariRestantieri", "luniCuAbonamenteRestante"]
    - path: "utils/notificariRestantieri.test.ts"
      provides: "Teste colocate rulate cu npx tsx (pattern utils/perioadaGratie.test.ts)"
      contains: "runTests"
    - path: "types.ts"
      provides: "Tipul NotificareRestanta (tipuri centralizate)"
      contains: "export interface NotificareRestanta"
    - path: "components/Plati/NotificariRestantieriModal.tsx"
      provides: "UI lista notificari: selector perioada, rand per destinatar, textarea editabil, Copiază mesaj, link WhatsApp, Copiază toate"
      exports: ["NotificariRestantieriModal"]
    - path: "components/Plati/PlatiScadente.tsx"
      provides: "Buton 'Trimite notificări restanțieri' + randare modal"
      contains: "NotificariRestantieriModal"
  key_links:
    - from: "components/Plati/PlatiScadente.tsx"
      to: "components/Plati/NotificariRestantieriModal.tsx"
      via: "import + randare conditionata de state isNotificariRestantieriOpen, cu plati/sportivi/familii din filteredData"
      pattern: "<NotificariRestantieriModal"
    - from: "components/Plati/NotificariRestantieriModal.tsx"
      to: "utils/notificariRestantieri.ts"
      via: "useMemo(() => genereazaNotificariRestantieri({...}))"
      pattern: "genereazaNotificariRestantieri\\("
    - from: "utils/notificariRestantieri.ts"
      to: "utils/paymentStatus.ts"
      via: "esteDeIncasat refolosit (nu duplicat) ca predicat de restanta"
      pattern: "import \\{[^}]*esteDeIncasat"
    - from: "utils/notificariRestantieri.ts"
      to: "utils/luniLipsa.ts + utils/formatareSportiv.ts"
      via: "formatLuna pentru textul lunii, formatNume pentru numele sportivului"
      pattern: "formatLuna|formatNume"
---

<objective>
Implementeaza Varianta 1 (semi-auto) din planul de notificari restantieri (memory project_notificare_taxe_whatsapp.md): un buton "Trimite notificări restanțieri" in modulul Plati (PlatiScadente) care genereaza, pur client-side din datele deja incarcate (plati, sportivi, familii din filteredData), lista parintilor cu taxa lunara (Abonament) neachitata pentru luna curenta, cu mesaj personalizat per destinatar si actiuni rapide: copiere in clipboard si link wa.me cu textul pre-completat.

Purpose: Elimina procesul manual (cauta sportiv -> istoric plati -> scrie manual pe WhatsApp), estimat 2-4h/saptamana. Fara WhatsApp Business API, fara integrare Meta, fara backend nou.
Output: util pur testat (utils/notificariRestantieri.ts + test), tip NotificareRestanta in types.ts, componenta NotificariRestantieriModal.tsx, buton + wiring in PlatiScadente.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@utils/paymentStatus.ts
@utils/formatareSportiv.ts

<interfaces>
<!-- Contracte existente extrase din cod — executorul NU trebuie sa exploreze pentru ele. -->

types.ts (existente):
- Sportiv: id, nume, prenume, status: 'Activ' | 'Inactiv', club_id?: string|null, familie_id: string|null, telefon?: string|null (coloana telefon E incarcata — vezi selectString din hooks/useSportivi.ts linia 29)
- Familie: id, nume, club_id?: string|null, reprezentant_id?: string|null  (reprezentant_id = id-ul unui SPORTIV membru al familiei — vezi hooks/useFamilyManager.ts:163, components/Plati/Familii.tsx:118). Familie NU are camp telefon.
- Plata: id, sportiv_id: string|null, familie_id: string|null, club_id?: string|null, suma: number, data: string, status: 'Achitat'|'Neachitat'|'Achitat Parțial'|'Anulat', tip: string, luna?: number|null (1-12), an?: number|null, sportiv_nume?: string|null, sportiv_prenume?: string|null
- Permissions: isSuperAdmin, isFederationAdmin, isAdminClub, canManageFinances (boolean)

utils/paymentStatus.ts: export function esteDeIncasat(p: Pick<Plata,'status'>): boolean  -> status !== 'Achitat' && status !== 'Anulat'
utils/luniLipsa.ts: export function formatLuna(luna: number, an: number): string  -> ex. "septembrie 2026" (toLocaleString ro-RO)
utils/formatareSportiv.ts: export function formatNume(s: {nume?, prenume?} | null | undefined): string  -> "Nume Prenume" sau '—'

components/ui.tsx: Button (variant 'primary'|'secondary'|'danger'|'success'|'info'|'warning', size 'xs'|'sm'|'md'|'lg', isLoading, leftIcon), Modal ({isOpen, onClose, title, children, persistent?} — max-w-2xl, body overflow-y-auto), Select (forwardRef, label + children <option>), Card, Badge (variant 'green'|'red'|'amber'|'blue'|'slate'), EmptyState ({icon?, title, description?}).
components/icons.tsx: ChatBubbleLeftEllipsisIcon, CopyIcon, CheckIcon, BellIcon, ExclamationTriangleIcon exista.
components/ErrorProvider.tsx: useError() -> { showError(title, message), showSuccess(title, message) }

components/Plati/PlatiScadente.tsx (relevant):
- linia 82-85: const { filteredData, ... } = useData(); plati = filteredData.plati; sportivi = filteredData.sportivi; familii = filteredData.familii
- linia 94: filter (useLocalStorage 'phi-hau-plati-scadente-filter') cu filter.clubId (ClubSelect afisat DOAR cand permissions.isSuperAdmin — dar valoarea persista in localStorage intre roluri)
- linia 868-877: bara de butoane actiuni (div flex flex-wrap gap-2) cu "Generează Abonamente" si "Notifică {n} debitori" (handleNotifyOverdue = notificari in-app existente — NU se atinge)
- linia 957-958: fallback luna/an din p.data cand p.luna/p.an lipsesc
- linia 1185+: zona modalelor (plataForPayment, editingPlata, ConfirmDeleteModal)

Pattern test colocat (fara vitest/jest): utils/perioadaGratie.test.ts — functii assert/run locale, export runTests() -> {passed, failed, errors}, auto-run la final cand process.argv[1] se termina in numele fisierului, process.exit(1) daca exista erori. Rulare: npx tsx utils/<fisier>.test.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tip NotificareRestanta + util pur utils/notificariRestantieri.ts cu teste (QUICK-260926-B7Z-01, -02)</name>
  <files>types.ts, utils/notificariRestantieri.ts, utils/notificariRestantieri.test.ts</files>
  <read_first>utils/perioadaGratie.test.ts (pattern runner), utils/paymentStatus.ts, utils/luniLipsa.ts (doar formatLuna), utils/formatareSportiv.ts</read_first>
  <behavior>
    - normalizeazaTelefonWa('0722 123 456') === '40722123456'; '+40 722-123-456' -> '40722123456'; '0040722123456' -> '40722123456'; '722123456' -> '40722123456'; '0722123456 / 0733111222' -> '40722123456' (se ia primul numar); '+39 333 1234567' -> '393331234567'; null / '' / '123' / 'abc' -> null
    - formateazaSumaLei(150) === '150'; formateazaSumaLei(150.5) === '150,50'
    - construiesteMesajRestanta(['Popescu Ana'], 150, 9, 2026) === `Bună ziua! Popescu Ana are de achitat taxa lunară 150 lei pentru ${formatLuna(9, 2026)}. Mulțumim!` (expected construit cu formatLuna ca testul sa nu depinda de ICU)
    - construiesteMesajRestanta(['Popescu Ana','Popescu Ion'], 300, 9, 2026) contine 'Popescu Ana și Popescu Ion au de achitat taxa lunară 300 lei'; cu 3 nume -> 'A, B și C au de achitat'
    - construiesteLinkWhatsApp('40722123456', 'Bună ziua! A & B?') incepe cu 'https://wa.me/40722123456?text=', partea text nu contine spatii sau '&' neencodate, iar decodeURIComponent(partea text) === mesajul original
    - genereazaNotificariRestantieri exclude: status 'Achitat', status 'Anulat', tip diferit de 'Abonament' (ex. 'Taxa Examen'), alta luna/an decat cea ceruta, factura din alt club cand clubId e dat
    - genereazaNotificariRestantieri include factura individuala 'Neachitat' cu telefonul sportivului (sursaTelefon 'sportiv') si factura 'Achitat Parțial' cu areAchitariPartiale === true
    - Factura de familie (familie_id setat, sportiv_id null) foloseste telefonul reprezentantului (sursaTelefon 'reprezentant_familie'); daca reprezentantul nu are telefon valid, telefonul primului membru cu telefon valid (sursaTelefon 'membru_familie'); numeSportivi = membrii activi ai familiei formatati cu formatNume
    - Doua facturi individuale ale unor sportivi diferiti cu acelasi telefon (scris diferit: '0722 123 456' vs '+40722123456') -> O singura notificare, suma = suma totala, 2 nume, mesaj cu 'au'
    - Doua facturi fara telefon -> DOUA notificari separate cu telefonWa null (nu se grupeaza intre ele)
    - Factura fara luna/an dar cu data '2026-09-05' e tratata ca luna 9 / an 2026
    - luniCuAbonamenteRestante returneaza perechile {luna, an} distincte ale facturilor Abonament de incasat, sortate descrescator (cea mai recenta prima)
  </behavior>
  <action>
Adauga in types.ts (in sectiunea "Domain: Financiar", dupa interfata Plata) interfata exportata NotificareRestanta cu campurile: cheie (string — telefonWa sau 'fara-telefon:' + id plata), telefonAfisat (string|null — valoarea bruta din DB pentru afisare), telefonWa (string|null — doar cifre, format international, null daca lipsa/invalid), sursaTelefon ('sportiv' | 'reprezentant_familie' | 'membru_familie' | null), numeSportivi (string[]), sportivIds (string[]), plataIds (string[]), suma (number), areAchitariPartiale (boolean), luna (number), an (number), mesaj (string). Tipul sta in types.ts conform conventiei proiectului (tipuri centralizate), nu in util.

Creeaza utils/notificariRestantieri.ts (fisier nou, functii pure, zero React, zero supabase, zero Date.now — determinist pentru teste). Importa type-only Plata, Sportiv, Familie, NotificareRestanta din '../types'; importa esteDeIncasat din './paymentStatus' (refolosit, NU se rescrie predicatul status !== 'Achitat' inline), formatLuna din './luniLipsa', formatNume din './formatareSportiv'. Exporta:

1. normalizeazaTelefonWa(telefon?: string | null): string | null — ia doar primul numar daca campul contine mai multe (split pe caracterele , ; / | si pe cuvantul ' sau '), pastreaza informatia de '+' initial, apoi elimina tot ce nu e cifra (regex \D global). Reguli: prefix '00' -> se elimina; prefix '+' -> deja international; numar de 10 cifre care incepe cu '0' -> '40' + restul fara 0 (format RO); numar de 9 cifre care incepe cu '7' -> '40' + cifrele. Rezultatul final e valid doar daca are intre 10 si 15 cifre (limita E.164) — altfel null. Returneaza DOAR cifre (wa.me cere numar international fara '+', fara zerouri initiale).
2. formateazaSumaLei(suma: number): string — intreg -> String(suma); altfel toFixed(2) cu '.' inlocuit de ',' (format RO determinist, fara dependenta de ICU).
3. construiesteMesajRestanta(numeSportivi: string[], suma: number, luna: number, an: number): string — template exact cerut de user (varianta cu diacritice din memory project_notificare_taxe_whatsapp.md): 'Bună ziua! {nume} {verb} de achitat taxa lunară {formateazaSumaLei(suma)} lei pentru {formatLuna(luna, an)}. Mulțumim!'. {nume}: 1 nume -> numele; 2 -> 'A și B'; 3+ -> 'A, B și C'. {verb}: 'are' pentru 1 nume, 'au' pentru 2+.
4. construiesteLinkWhatsApp(telefonWa: string, mesaj: string): string — 'https://wa.me/' + telefonWa + '?text=' + encodeURIComponent(mesaj). Prefixul e literal (nu se construieste din input).
5. luniCuAbonamenteRestante(plati: Plata[]): { luna: number; an: number }[] — perechi distincte pentru facturile tip 'Abonament' cu esteDeIncasat, sortate descrescator dupa an apoi luna.
6. genereazaNotificariRestantieri(params: { plati: Plata[]; sportivi: Sportiv[]; familii: Familie[]; luna: number; an: number; clubId?: string | null }): NotificareRestanta[].
   Helper intern lunaAnFactura(p): p.luna/p.an daca exista, altfel parseaza direct din sirul p.data (primele 10 caractere, format YYYY-MM-DD: an = caracterele 0-4, luna = 5-7) — parsare pe sir, NU new Date(), ca sa nu apara decalaj de fus orar (acelasi fallback semantic ca PlatiScadente.tsx liniile 957-958).
   Construieste o singura data Map-uri: sportivi dupa id, membri dupa familie_id, familii dupa id — NU folosi .find() in bucla pe fiecare plata (super admin poate avea mii de randuri).
   Filtru: p.tip === 'Abonament' && esteDeIncasat(p) && luna/an potrivite && (fara clubId sau clubul facturii === clubId). Clubul facturii: p.club_id, altfel club_id-ul sportivului, altfel club_id-ul familiei sau al primului membru.
   Rezolvare per factura: individuala (sportiv_id) -> nume = formatNume(sportiv) sau, daca sportivul nu mai e in lista, formatNume({ nume: p.sportiv_nume, prenume: p.sportiv_prenume }); telefon = sportiv.telefon, sursa 'sportiv'. Familie (familie_id) -> membri = membrii cu status 'Activ' (fallback: toti membrii; fallback final: ['Familia ' + familie.nume]); telefon = telefonul reprezentantului (familie.reprezentant_id cautat in Map-ul de sportivi) daca normalizeazaTelefonWa il accepta, sursa 'reprezentant_familie'; altfel primul membru cu telefon valid, sursa 'membru_familie'; altfel null.
   Grupare "per parinte": facturile cu acelasi telefonWa non-null se unesc intr-o singura notificare (numeSportivi uniune fara duplicate, pastrand ordinea; sportivIds si plataIds concatenate; suma adunata; areAchitariPartiale = oricare status 'Achitat Parțial'; telefonAfisat = primul brut). Facturile fara telefon valid NU se grupeaza — fiecare devine notificare separata cu cheie 'fara-telefon:' + p.id si telefonWa null (telefonAfisat pastreaza valoarea bruta invalida daca exista, ca UI-ul sa poata arata 'Număr invalid').
   mesaj = construiesteMesajRestanta(numeSportivi, suma, luna, an). Sortare finala: intai cele cu telefonWa, apoi cele fara; in interiorul fiecarui grup localeCompare pe primul nume cu locale 'ro'.
   Decizie (discretia plannerului): pentru 'Achitat Parțial' suma din mesaj este suma facturii (Plata nu are un camp "rest de plata" fiabil — tranzactii.plata_ids poate acoperi mai multe facturi); UI-ul din Task 2 marcheaza randul si permite editarea mesajului inainte de trimitere.

Creeaza utils/notificariRestantieri.test.ts urmand EXACT pattern-ul din utils/perioadaGratie.test.ts (header cu nota 'Rulare: npx tsx utils/notificariRestantieri.test.ts', functii locale assert/run, export runTests, auto-run cand process.argv[1] se termina cu 'notificariRestantieri.test.ts', process.exit(1) la erori). Acopera TOATE cazurile din <behavior>. Fixture-uri minimale construite cu cast 'as Sportiv' / 'as Plata' / 'as Familie'. Scrie intai testele (RED — rularea esueaza pe import lipsa), apoi implementarea (GREEN).
  </action>
  <verify>
    <automated>cd /c/Users/lungu/portal-phihau && npx tsx utils/notificariRestantieri.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>npx tsx utils/notificariRestantieri.test.ts afiseaza doar PASS si iese cu cod 0; npx tsc --noEmit iese cu cod 0 (baseline curat verificat la planificare); types.ts exporta NotificareRestanta; utils/notificariRestantieri.ts importa esteDeIncasat din ./paymentStatus si nu contine niciun import din supabaseClient sau react.</done>
</task>

<task type="auto">
  <name>Task 2: NotificariRestantieriModal + buton "Trimite notificări restanțieri" in PlatiScadente (QUICK-260926-B7Z-03)</name>
  <files>components/Plati/NotificariRestantieriModal.tsx, components/Plati/PlatiScadente.tsx</files>
  <read_first>components/Plati/PlatiScadente.tsx liniile 1-25, 81-110, 825-885, 1185-1279 (NU tot fisierul — 1279 linii); components/ui.tsx liniile 355-405 (Modal) si 288-310 (EmptyState); utils/notificariRestantieri.ts (creat in Task 1)</read_first>
  <action>
Creeaza components/Plati/NotificariRestantieriModal.tsx (componenta noua, named export NotificariRestantieriModal, React.FC). Props (interfata locala de props a componentei, ca in restul componentelor Plati): isOpen: boolean; onClose: () => void; plati: Plata[]; sportivi: Sportiv[]; familii: Familie[]; clubId?: string | null. Foloseste EXCLUSIV design system-ul intern din '../ui' (Modal, Button, Select, Badge, EmptyState) + Tailwind + iconite din '../icons' (ChatBubbleLeftEllipsisIcon, CopyIcon, CheckIcon, ExclamationTriangleIcon) — fara librarii noi, fara Shadcn/MUI. Texte UI in romana cu diacritice.

Stare si date:
- perioada selectata (cheie 'an-luna'), implicit luna curenta calculata la deschidere (new Date(): getMonth()+1, getFullYear()). Select 'Perioadă' cu optiunile = luna curenta + luniCuAbonamenteRestante(plati), fara duplicate, eticheta formatLuna(luna, an). Decizie (discretia plannerului): selectorul exista pentru ca la inceput de luna facturile lunii curente pot sa nu fie inca generate, iar restantele lunii trecute raman relevante; implicitul respecta cerinta "luna curenta".
- notificari = useMemo(() => genereazaNotificariRestantieri({ plati, sportivi, familii, luna, an, clubId }), [plati, sportivi, familii, luna, an, clubId]). Zero apeluri supabase in aceasta componenta.
- mesajeEditate: Record<cheie, string> (override per rand; valoarea afisata = mesajeEditate[cheie] ?? n.mesaj), deschise: Set<cheie> (marcaj local "Deschis în WhatsApp"/"Copiat", doar pe sesiunea modalului, nepersistat), copiatCheie: string | null pentru feedback 'Copiat!' care dispare dupa 2s (setTimeout, ca in CredentialeContModal din ui.tsx). useEffect pe isOpen: la fiecare deschidere reseteaza perioada la luna curenta, mesajeEditate, deschise. La schimbarea perioadei reseteaza mesajeEditate.
- helper async copiazaText(text, cheie): await navigator.clipboard.writeText in try/catch; succes -> copiatCheie + adauga in deschise; esec -> showError('Copiere eșuată', 'Browserul nu a permis accesul la clipboard — selectează textul manual din căsuță.') via useError din '../ErrorProvider'.

Layout (Modal title 'Notificări restanțieri — taxa lunară'):
- Antet: Select Perioadă; rezumat 'N destinatari · {formateazaSumaLei(total)} lei de încasat · M fără telefon valid · K/N deschise'; nota scurta gri: 'Mesajele se trimit manual — aplicația nu trimite nimic automat.'; buton 'Copiază toate' (Button size sm variant secondary, CopyIcon) care copiaza toate mesajele curente (inclusiv editarile) concatenate: pentru fiecare, linie cu telefonAfisat (sau 'FĂRĂ TELEFON') + ' — ' + nume unite cu ', ', apoi mesajul, separate prin linie goala.
- Lista goala: EmptyState cu title 'Niciun restanțier pentru {formatLuna}' si description 'Toate facturile de abonament din această lună sunt achitate, anulate sau încă negenerate.'
- Rand per notificare (card: rounded-xl border border-[var(--t-border)] bg-[var(--t-surface)] p-3 space-y-2; cand cheia e in deschise -> opacity-60 + CheckIcon verde): nume sportivi (font-semibold text-white), suma '{formateazaSumaLei} lei' (font-bold, aliniat dreapta); linia de telefon: telefonAfisat + eticheta sursei ('tel. sportiv' / 'tel. reprezentant familie' / 'tel. alt membru familie') SAU, cand telefonWa e null, text rosu 'Fără telefon valid' (+ ' — număr invalid: {telefonAfisat}' daca exista o valoare bruta); Badge amber 'Achitat parțial — verifică suma' cand areAchitariPartiale; textarea controlat (rows 3, w-full, clase Tailwind cu var(--t-border)/var(--t-surface) ca Input din ui.tsx, text-sm) pentru mesaj, editabil; actiuni: Button size sm variant secondary 'Copiază mesaj' (CopyIcon; eticheta devine 'Copiat!' cu CheckIcon cand copiatCheie === cheie) si — DOAR cand telefonWa nu e null — un element ancora a (Button din ui.tsx nu suporta href) stilizat ca buton verde (bg-green-700 hover:bg-green-600 text-white text-sm font-semibold px-3 py-1.5 rounded-lg inline-flex items-center gap-1.5), cu ChatBubbleLeftEllipsisIcon si textul 'WhatsApp', href = construiesteLinkWhatsApp(n.telefonWa, mesajCurent), target '_blank', rel 'noopener noreferrer', onClick adauga cheia in deschise. Randarea textelor din DB se face exclusiv prin JSX (escape automat) — NU dangerouslySetInnerHTML. Nu scrie identificatorii dangerouslySetInnerHTML sau supabaseClient nici macar in comentariile fisierului: verify-ul face grep -c pe ei si asteapta 0.

Wiring in components/Plati/PlatiScadente.tsx (modificari minime, fara schimbarea PlatiScadenteProps si fara a atinge handleNotifyOverdue / butonul existent 'Notifică {n} debitori'):
- import { NotificariRestantieriModal } from './NotificariRestantieriModal'; adauga ChatBubbleLeftEllipsisIcon la importul existent din '../icons'.
- state nou isNotificariRestantieriOpen (useState false), declarat langa celelalte useState din componenta.
- in bara de butoane (div flex flex-wrap gap-2, ~linia 869), dupa butonul 'Generează Abonamente': daca permissions.canManageFinances, Button variant 'primary' size 'sm' className 'flex-1 sm:flex-none justify-center' cu ChatBubbleLeftEllipsisIcon (w-4 h-4 mr-1) si textul 'Trimite notificări restanțieri', onClick deschide modalul. Butonul NU depinde de selectedIds.
- in zona modalelor (dupa blocul editingPlata, inainte de ConfirmDeleteModal): randare conditionata {isNotificariRestantieriOpen && <NotificariRestantieriModal isOpen onClose={...false} plati={plati || []} sportivi={sportivi || []} familii={familii || []} clubId={permissions.isSuperAdmin ? (filter.clubId || null) : null} />}. Randarea conditionata evita calculul useMemo cand modalul e inchis. clubId se trimite doar pentru super admin pentru ca filter.clubId persista in localStorage intre roluri si un ADMIN_CLUB ar putea mosteni o valoare veche; pentru ceilalti, scoping-ul vine deja din filteredData (RLS + visibleClubIds).
  </action>
  <verify>
    <automated>cd /c/Users/lungu/portal-phihau && npx tsc --noEmit && npx tsx utils/notificariRestantieri.test.ts && grep -c "NotificariRestantieriModal" components/Plati/PlatiScadente.tsx && grep -cE "supabaseClient|supabase\.(from|rpc)" components/Plati/NotificariRestantieriModal.tsx | grep -qx 0 && grep -c "dangerouslySetInnerHTML" components/Plati/NotificariRestantieriModal.tsx | grep -qx 0 && grep -c 'noopener noreferrer' components/Plati/NotificariRestantieriModal.tsx</automated>
    <human-check>
      1. npm run dev, login ADMIN_CLUB (sau Super Admin cu filtru club), meniu Plăți -> Management Facturi & Plăți.
      2. Butonul 'Trimite notificări restanțieri' e vizibil fara nicio factura selectata; click -> modal cu Perioadă = luna curenta.
      3. Verifica: doar facturi Abonament neachitate/partial apar; mesajul are formatul 'Bună ziua! {Nume} are de achitat taxa lunară {sumă} lei pentru {lună an}. Mulțumim!'.
      4. 'Copiază mesaj' -> lipeste in Notepad, textul e identic cu cel din casuta (inclusiv o editare manuala facuta inainte).
      5. 'WhatsApp' pe un rand -> se deschide tab nou wa.me/40... cu mesajul pre-completat; randul devine marcat 'deschis'.
      6. Un sportiv fara telefon apare 'Fără telefon valid' fara buton WhatsApp.
      7. Consola browserului fara erori; tab Network: deschiderea modalului nu declanseaza niciun request Supabase nou.
    </human-check>
  </verify>
  <done>npx tsc --noEmit iese cu cod 0; testele din Task 1 trec in continuare; PlatiScadente.tsx randeaza butonul 'Trimite notificări restanțieri' gated de permissions.canManageFinances si NotificariRestantieriModal cu plati/sportivi/familii din filteredData; modalul nu contine niciun apel supabase si niciun dangerouslySetInnerHTML; linkul WhatsApp are target _blank + rel 'noopener noreferrer' si este randat doar pentru randuri cu telefonWa valid; PlatiScadenteProps si handleNotifyOverdue raman neschimbate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB (sportivi.nume/prenume/telefon, familii.nume) -> URL wa.me | Text controlat de utilizatori (nume introduse la inscriere/import) ajunge in path/query-ul unui link extern |
| DB -> DOM (modal) | Nume si telefoane randate in UI |
| Portal -> WhatsApp (tert, Meta) | Date personale (nume sportiv, adesea minor, + suma datorata) parasesc aplicatia la actiunea explicita a adminului |
| Rol activ -> date financiare | Telefoane + datorii vizibile doar rolurilor cu drept financiar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-b7z-01 | Information Disclosure | Buton + NotificariRestantieriModal in PlatiScadente.tsx | mitigate | Butonul si modalul se randeaza doar cand permissions.canManageFinances (ruta PlatiScadente e deja renderProtected cu canManageFinances in AppRouter.tsx:237); datele vin exclusiv din filteredData (RLS pe active-role-context-id + visibleClubIds), zero query nou |
| T-b7z-02 | Tampering | construiesteLinkWhatsApp / normalizeazaTelefonWa | mitigate | Telefonul e redus la doar cifre (regex \D global) inainte de a intra in path, deci nu poate injecta '/', '?', '#' sau scheme; textul trece prin encodeURIComponent; prefixul 'https://wa.me/' e literal — acoperit de testele din Task 1 |
| T-b7z-03 | Tampering (XSS) | Randare nume/telefon/mesaj in modal | mitigate | Doar JSX cu escape automat React si textarea controlat; interzis dangerouslySetInnerHTML (gate grep in verify Task 2) |
| T-b7z-04 | Tampering (reverse tabnabbing) | Ancora WhatsApp target _blank | mitigate | rel="noopener noreferrer" obligatoriu (gate grep in verify Task 2) |
| T-b7z-05 | Information Disclosure (cross-club) | clubId trimis modalului | mitigate | clubId = filter.clubId DOAR cand permissions.isSuperAdmin; pentru ADMIN_CLUB/INSTRUCTOR se ignora valoarea persistata in localStorage si ramane scoping-ul din filteredData |
| T-b7z-06 | Information Disclosure (GDPR, minori) | Mesaj trimis prin WhatsApp | accept | Nimic nu pleaca automat: fiecare mesaj e deschis/copiat manual de admin (acelasi canal folosit azi manual de club, ca operator); template-ul contine minimul necesar (nume, suma, luna) — fara CNP, adresa, grad; fara API tert, fara stocare noua. Varianta 2 (full-auto prin Meta API) ramane in afara scope-ului |
| T-b7z-07 | Repudiation | Evidenta notificarilor trimise | accept | Semi-auto: marcajul 'deschis' e doar local pe sesiunea modalului, nepersistat; nu se creeaza jurnal DB (ar insemna migratie + scriere noua, in afara scope-ului 'fara query-uri Supabase noi') |
</threat_model>

<verification>
- npx tsx utils/notificariRestantieri.test.ts -> toate PASS, exit 0
- npx tsc --noEmit -> exit 0
- grep: PlatiScadente.tsx contine '<NotificariRestantieriModal' si 'Trimite notificări restanțieri'; NotificariRestantieriModal.tsx fara import supabaseClient / supabase.from / supabase.rpc si fara 'dangerouslySetInnerHTML'; utils/notificariRestantieri.ts importa esteDeIncasat din './paymentStatus'
- git diff components/Plati/PlatiScadente.tsx nu modifica interfata PlatiScadenteProps si nici corpul handleNotifyOverdue
- package.json neschimbat (zero dependinte noi)
</verification>

<success_criteria>
- Adminul de club deschide lista restantierilor lunii curente dintr-un singur click in Plăți, cu mesaj gata formulat per parinte
- Trimiterea pe WhatsApp = un click pe rand (wa.me cu text pre-completat) sau copy-paste din clipboard ca fallback
- Familiile primesc un singur mesaj (telefon reprezentant), fratii cu acelasi numar sunt grupati; sportivii fara telefon sunt vizibili si marcati
- Zero query-uri Supabase noi, zero librarii noi, zero schimbari de API pe componentele Plati existente
</success_criteria>

<output>
Create `.planning/quick/260926-b7z-implementeaza-varianta-1-notificari-rest/260926-b7z-SUMMARY.md` when done
</output>
