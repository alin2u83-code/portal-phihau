---
phase: quick-260709-m7m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/GestiuneExamene/ManagementInscrieri.tsx
  - hooks/useExamManager.ts
  - hooks/useDataProvider.ts
  - sql/fixes/fix_grad_actual_by_exam_date.sql
  - sql/fixes/fix_rbv_sportivi_complet_include_noi.sql
autonomous: true
requirements: [BUG-DIACRITICE, BUG-SPORTIV-LIPSA, BUG-GRAD-CURENT]
must_haves:
  truths:
    - "Diacriticele/glifele din tabelul de introducere rezultate examen se afiseaza corect (fara mojibake)"
    - "Un sportiv nou creat in DB apare in lista de selectie pentru sesiunea de examen (individual, multiplu, import bulk)"
    - "Gradul curent al sportivului = gradul cel mai mare din examenele PROMOVATE ordonate dupa data examenului, nu dupa ordinea de insertie"
  artifacts:
    - path: "components/GestiuneExamene/ManagementInscrieri.tsx"
      provides: "Tabel introducere rezultate cu literale corect encodate"
    - path: "hooks/useExamManager.ts"
      provides: "Logica finalizare examen care nu downgradeaza gradul curent"
  key_links:
    - from: "components/GestiuneExamene/index.tsx"
      to: "filteredData.sportivi"
      via: "useData() -> useDataProvider -> useSportivi(rbv_sportivi_complet)"
      pattern: "filteredData\\.sportivi"
---

<objective>
Repara trei bug-uri conexe in modulul Examene (GestiuneExamene), toate intr-un singur quick task:

1. **Diacritice** afisate incorect (mojibake) in tabelul de introducere/scriere rezultate examen — DOAR randare UI, datele din DB sunt corecte (decizie CONTEXT: fara migrare de date).
2. **Sportiv nou** creat in DB nu apare in lista de selectie pentru sesiune examen — lipseste in TOATE fluxurile (adaugare individuala, multipla, import bulk), deci cauza e comuna la nivel de fetch sportivi, nu specifica unui flux.
3. **Grad curent** setat gresit: se ia dupa ultimul examen introdus in sistem (ordine de insertie) in loc de gradul cel mai mare din examenele PROMOVATE ordonate dupa DATA examenului.

Purpose: Modulul Examene este folosit live de cluburi pentru inscrieri si actualizare grade; aceste bug-uri produc afisare gresita, sportivi invizibili la inscriere si grade incorecte in profil.
Output: Fix-uri frontend + SQL (aplicate live prin Supabase MCP), verificate cu Playwright pe partea de diacritice.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260709-m7m-fix-3-bug-uri-modul-examene-1-diacritice/260709-m7m-CONTEXT.md
@CLAUDE.md
@components/GestiuneExamene/index.tsx
@components/GestiuneExamene/ManagementInscrieri.tsx
@hooks/useExamManager.ts
@hooks/useDataProvider.ts
@hooks/useSportivi.ts

Note investigatie deja facuta de planner (punct de plecare, NU concluzii finale):
- `sportivi` din ecranul examen = `filteredData.sportivi` (index.tsx L45) -> `useData()` -> `useDataProvider` (L100-121) -> `useSportivi({ clubId })` -> query pe view-ul `rbv_sportivi_complet` (useSportivi.ts L34-36). `useSportivi` e apelat FARA `loadAll`/`pagination`, deci se aplica limita implicita PostgREST (~1000 randuri). Candidati bug 2: (a) view-ul `rbv_sportivi_complet` are un JOIN care exclude sportivii noi fara grad/grupa/rol; (b) lipsa invalidare React Query `['sportivi']` la creare sportiv; (c) limita 1000 randuri la nivel federatie.
- Modalele de adaugare (SingleAddInscriereModal L194-196, BulkAddSportiviModal L320-322) filtreaza `s.status === 'Activ'` din acelasi prop `sportivi` — deci daca lipseste din prop, lipseste peste tot.
- Bug 3: `useExamManager.finalizeExamen` (L152-186) actualizeaza `grad_actual_id` in DB doar daca `ordine` e mai mare (corect), DAR update-ul de state local L173-186 seteaza `grad_actual_id = inscriere.grad_sustinut_id` FARA verificare ordine. Comentariul L153-155 mentioneaza un trigger LIVE `sync_grad_actual_on_exam_result` care NU exista in repo (doar in DB) — probabil acesta seteaza grad dupa randul inserat (insertion order) fara sa compare data examenului.
- Bug 1: 8 aparitii mojibake in `ManagementInscrieri.tsx` (ex. `â€"`, `â†'`, `â"€`) — literale UTF-8 corupte in sursa; unele in text vizibil (mesaj eroare L807, sageata card mobil L1466).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigheaza si confirma cauza radacina a celor 3 bug-uri</name>
  <files>components/GestiuneExamene/ManagementInscrieri.tsx, components/ResponsiveTable.tsx, hooks/useSportivi.ts, hooks/useDataProvider.ts, hooks/useExamManager.ts</files>
  <action>
    Confirma cauza exacta a fiecarui bug inainte de a scrie fix-uri. NU modifica cod in acest task.

    BUG 1 (diacritice): Deschide aplicatia cu Playwright MCP, navigheaza la o sesiune de examen cu participanti si fa un snapshot al tabelului de introducere rezultate. Identifica EXACT ce glife se afiseaza gresit (nume sportivi din DB, etichete statice, sagata `→`, em-dash `—`). Coreleaza cu cele 8 aparitii mojibake din `ManagementInscrieri.tsx`. Decide daca fix-ul e: (a) corectare literale UTF-8 corupte in sursa, si/sau (b) asigurare `<meta charset="utf-8">` / randare. Per CONTEXT: fara migrare de date DB.

    BUG 2 (sportiv nou lipsa): Foloseste Supabase MCP (`list_tables` + interogare definitie view) pentru a obtine definitia view-ului `rbv_sportivi_complet`. Verifica daca are INNER JOIN care exclude sportivii fara grad_actual_id / grupa_id / rol / club. Verifica si daca `useSportivi` (apelat in useDataProvider L102) sufera de limita 1000 randuri (lipsa loadAll) si daca crearea unui sportiv invalideaza cache-ul React Query `['sportivi']`. Determina cauza COMUNA (view sau cache/limita) care explica absenta in toate cele 3 fluxuri.

    BUG 3 (grad curent): Foloseste Supabase MCP pentru a obtine definitia trigger-ului/functiei `sync_grad_actual_on_exam_result` din DB live (referit in useExamManager.ts L153-155 dar absent din repo). Confirma daca seteaza `sportivi.grad_actual_id` dupa randul inserat in `istoric_grade` (insertion order) fara a compara `data_obtinere`/data examenului. Confirma si bug-ul de state local din finalizeExamen L173-186.

    Documenteaza in SUMMARY cauza confirmata a fiecarui bug + fisierul/linia tinta pentru fix.
  </action>
  <verify>
    <automated>MISSING — task de investigatie; output = cauze root documentate in SUMMARY + definitiile live ale view-ului `rbv_sportivi_complet` si trigger-ului `sync_grad_actual_on_exam_result` obtinute prin Supabase MCP</automated>
  </verify>
  <done>Cele 3 cauze radacina sunt confirmate cu dovezi (snapshot Playwright pentru bug 1, definitie view pentru bug 2, definitie trigger pentru bug 3); fiecare are un fisier/linie tinta identificat.</done>
</task>

<task type="auto">
  <name>Task 2: Fix diacritice (bug 1) + verificare Playwright pe tabelul de rezultate</name>
  <files>components/GestiuneExamene/ManagementInscrieri.tsx</files>
  <action>
    Pe baza cauzei confirmate in Task 1, corecteaza randarea diacriticelor in tabelul de introducere rezultate examen. Conform CONTEXT, e DOAR problema de randare — NU face migrare de date DB.

    Daca sunt literale UTF-8 corupte in sursa: inlocuieste cele 8 secvente mojibake (`â€"` -> `—`, `â†'` -> `→`, `â"€` -> `─`, etc.) cu caracterele UTF-8 corecte, atat in text vizibil (ex. mesaj eroare "Grad deja obtinut", sageata din cardul mobil) cat si in comentarii. Scrie fisierul salvat ca UTF-8 curat. Verifica sa nu ramana nicio secventa `â€`/`â†`/`â"` in fisier.

    Daca investigatia arata alta cauza (ex. dublu-encoding la nivel de query/view), aplica fix-ul de randare corespunzator fara a atinge datele din DB.

    NU introduce abstractii noi — doar corectarea encodarii.
  </action>
  <verify>
    <automated>grep -c $'\xc3\xa2\xe2\x82\xac' components/GestiuneExamene/ManagementInscrieri.tsx | grep -q '^0$' && npm run lint</automated>
  </verify>
  <done>Nicio secventa mojibake ramasa in ManagementInscrieri.tsx; `tsc --noEmit` (lint) trece; snapshot Playwright MCP pe tabelul de introducere rezultate arata diacriticele/glifele corect (ă, â, î, ș, ț, →, —) — verificat vizual de executor prin mcp__playwright__* (navigheaza la sesiune examen cu participanti, screenshot tabel).</done>
</task>

<task type="auto">
  <name>Task 3: Fix sportiv nou nu apare in lista examen (bug 2)</name>
  <files>hooks/useDataProvider.ts, sql/fixes/fix_rbv_sportivi_complet_include_noi.sql</files>
  <action>
    Aplica fix-ul pentru cauza comuna confirmata in Task 1, astfel incat un sportiv nou creat sa apara imediat in lista de selectie pentru sesiune examen (individual, multiplu, import bulk).

    - Daca cauza e view-ul `rbv_sportivi_complet` (INNER JOIN care exclude sportivii noi fara grad/grupa/rol): scrie migratia in `sql/fixes/fix_rbv_sportivi_complet_include_noi.sql` care schimba JOIN-urile problematice in LEFT JOIN (pastrand coloanele existente si RLS-ul). Aplica migratia LIVE prin Supabase MCP `apply_migration`. Atentie: nu strica scoparea pe club existenta.
    - Daca cauza e limita 1000 randuri PostgREST la nivel federatie: adauga `loadAll`/paginare corespunzatoare in apelul `useSportivi` din `useDataProvider.ts` (L102).
    - Daca cauza e lipsa invalidare React Query la creare sportiv: adauga `queryClient.invalidateQueries(['sportivi'])` in mutatia de creare sportiv.

    Aplica DOAR fix-ul(rile) care corespund cauzei confirmate. Daca nu e nevoie de SQL, nu crea fisierul SQL (actualizeaza `files_modified` implicit).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Un sportiv nou creat apare in lista disponibila din SingleAddInscriereModal, BulkAddSportiviModal si in fluxul de import bulk; `tsc --noEmit` trece; daca s-a aplicat SQL, migratia e confirmata live prin Supabase MCP.</done>
</task>

<task type="auto">
  <name>Task 4: Fix grad curent dupa data examenului promovat (bug 3)</name>
  <files>hooks/useExamManager.ts, sql/fixes/fix_grad_actual_by_exam_date.sql</files>
  <action>
    Corecteaza regula de setare a gradului curent: `grad_actual_id` = gradul cel mai mare din examenele PROMOVATE ale sportivului, ordonate dupa DATA examenului (nu dupa ordinea de insertie in sistem).

    - DB: scrie in `sql/fixes/fix_grad_actual_by_exam_date.sql` corectarea functiei/trigger-ului `sync_grad_actual_on_exam_result` (definitie obtinuta live in Task 1) astfel incat sa recalculeze `sportivi.grad_actual_id` ca gradul cu cea mai mare `ordine`/data din `istoric_grade` PROMOVATE pentru acel sportiv, ordonat dupa `data_obtinere` (data examenului) — NU dupa randul curent inserat. Nu trebuie sa faca downgrade cand se introduce ulterior un examen cu data anterioara. Aplica migratia LIVE prin Supabase MCP `apply_migration`.
    - Frontend: in `useExamManager.ts` finalizeExamen (L173-186), corecteaza update-ul de state local astfel incat `grad_actual_id` sa nu fie suprascris cu un grad de ordine inferioara — aliniaza cu garda `ordine` deja folosita la update-ul DB (L156-166).

    Optional (daca Task 1 arata ca datele existente sunt deja gresite): include in migratie un UPDATE retroactiv unic care recalculeaza grad_actual_id pentru sportivii afectati pe baza istoricului promovat ordonat dupa data.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Trigger-ul/functia recalculeaza grad_actual_id dupa gradul cel mai mare promovat ordonat pe data examenului; introducerea unui examen cu data anterioara nu mai downgradeaza gradul curent; state-ul local din finalizeExamen respecta garda de ordine; `tsc --noEmit` trece; migratia confirmata live prin Supabase MCP.</done>
</task>

</tasks>

<verification>
- `npm run lint` (tsc --noEmit) trece dupa toate fix-urile.
- Bug 1: verificare vizuala Playwright MCP pe tabelul de introducere rezultate — diacritice si glife corecte.
- Bug 2: sportiv nou vizibil in toate cele 3 fluxuri de adaugare la sesiune.
- Bug 3: grad curent reflecta cel mai mare grad promovat ordonat dupa data examenului; fara downgrade la introducere examen vechi.
- Migratiile SQL (daca exista) aplicate live prin Supabase MCP, nu doar scrise in fisier.
</verification>

<success_criteria>
- Cele 3 bug-uri confirmate root-caused (Task 1) si reparate (Task 2-4).
- Zero mojibake in ManagementInscrieri.tsx.
- Sportiv nou apare in lista examen imediat dupa creare.
- Grad curent = grad maxim promovat ordonat dupa data examenului.
- Diacriticele verificate vizual cu Playwright pe tabelul de rezultate.
- Fara abstractii noi peste ce e strict necesar; fara migrare de date pentru bug 1.
</success_criteria>

<output>
Create `.planning/quick/260709-m7m-fix-3-bug-uri-modul-examene-1-diacritice/260709-m7m-SUMMARY.md` when done
</output>
