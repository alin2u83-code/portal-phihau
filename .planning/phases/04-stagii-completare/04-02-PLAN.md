---
phase: "04-stagii-completare"
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - hooks/useDataProvider.ts
  - components/Competitii/StagiiCompetitii.tsx
autonomous: true
requirements: [STG-01, STG-02, STG-03, STG-05]

must_haves:
  truths:
    - "preturiConfig este populat din DB la mount (nu mai e array vid)"
    - "Formularul EvenimentForm afișează câmpuri pret_copii, pret_grade, pret_centuri pentru stagii"
    - "handleAddParticipant calculează categoria sportivului (copii/grade/centuri) și taxa corectă cu 3-level fallback"
    - "Plata inserată include eveniment_id (FK spre eveniment)"
    - "Formularul arată taxa calculată înainte de confirmare (text preview sub butonul Înscrie)"
    - "STG-01: ADMIN_CLUB poate crea stagiu de club fără să vadă switch-ul federație"
  artifacts:
    - path: "hooks/useDataProvider.ts"
      provides: "preturiConfig fetchat din DB"
      contains: "preturi_config"
    - path: "components/Competitii/StagiiCompetitii.tsx"
      provides: "calculeazaCategorieStagiu + getTaxaStagiu + pret câmpuri în formular"
      contains: "calculeazaCategorieStagiu"
  key_links:
    - from: "hooks/useDataProvider.ts (deferredQueries)"
      to: "AppData.preturiConfig"
      via: "setData cu preturiConfig"
      pattern: "preturi_config"
    - from: "components/Competitii/StagiiCompetitii.tsx (handleAddParticipant)"
      to: "supabase.from('plati').insert"
      via: "newPlata cu eveniment_id"
      pattern: "eveniment_id.*eveniment\\.id"
---

<objective>
Implementare feature slice complet STG-02 + STG-05: fix fetch preturiConfig + logică prețuri per categorie în fluxul de înscriere la stagiu, plus câmpuri preț în formularul de creare stagiu.

Purpose: Acesta este core-ul fazei 4. Fără taxa calculată corect, înscrierea la stagiu generează plăți cu suma 0 (bug critic existent). Această felie verticală completă permite unui admin să creeze un stagiu cu prețuri diferențiate și să înscrie un sportiv cu taxa corect calculată și salvată.

Output: preturiConfig funcțional + EvenimentForm cu câmpuri preț + handleAddParticipant cu calcul categorie + INSERT plată cu eveniment_id.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-stagii-completare/04-RESEARCH.md
@.planning/phases/04-stagii-completare/04-01-SUMMARY.md
@hooks/useDataProvider.ts
@components/Competitii/StagiiCompetitii.tsx
@utils/eligibilitateCompetitie.ts
@types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix useDataProvider — fetch preturiConfig din DB</name>
  <files>hooks/useDataProvider.ts</files>
  <behavior>
    - Test 1: După mount cu rol activ, `preturiConfig` în AppData nu este array vid
    - Test 2: `preturiConfig` conține obiecte cu proprietățile `id`, `categorie`, `suma`
    - Test 3: `preturiConfig.find(p => p.categorie === 'Taxa Stagiu')` returnează un obiect valid dacă există date în DB
  </behavior>
  <action>
    În `hooks/useDataProvider.ts`, în blocul `deferredQueries` (linia ~343), adaugă intrarea:
    `preturiConfig: cleanedSupabase.from('preturi_config').select('*')`

    Adaugă această linie imediat după intrarea `decontSportivi` existentă (ultima din deferredQueries).

    Verifică că în callback-ul `Promise.allSettled(Object.values(deferredQueries)).then(...)` există deja logica care preia toate cheile din deferredQueries și apelează `setData` — dacă da, nu e nevoie de nicio altă modificare, deoarece cheia `preturiConfig` va fi preluată automat de același mecanism.

    Dacă callback-ul deferred face `setData(prev => ({ ...prev, ...deferredResult }))` cu toate cheile dinamic, adăugarea în deferredQueries este suficientă. Dacă setData enumeră cheile explicit, adaugă și `preturiConfig: deferredResult.preturiConfig || prev.preturiConfig`.

    ATENȚIE: tabela se numește `preturi_config` (din DB), cheia în AppData este `preturiConfig` (camelCase) — nu confunda.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compilation trece. Cuvântul `preturi_config` apare în deferredQueries din useDataProvider.ts.</done>
</task>

<task type="auto">
  <name>Task 2a: EvenimentForm — câmpuri preț per categorie</name>
  <files>components/Competitii/StagiiCompetitii.tsx</files>
  <behavior>
    - Formularul EvenimentForm afișează 3 câmpuri Input tip number pentru pret_copii, pret_grade, pret_centuri când `type === 'Stagiu'`
    - La submit, prețurile se salvează ca NUMERIC în DB (null dacă câmp gol)
    - La editare stagiu existent, câmpurile se populează cu valorile existente
  </behavior>
  <action>
    Modificări în `components/Competitii/StagiiCompetitii.tsx` — DOAR în componenta EvenimentForm:

    1. Extinde formState inițial cu `pret_copii: '', pret_grade: '', pret_centuri: ''`
    2. În JSX, după Select tip stagiu, adaugă grid 3 coloane cu 3 Input-uri:
       - label="Preț Copii (7-12 ani) lei", name="pret_copii", type="number", min="0", step="0.01", placeholder="Opțional"
       - label="Preț Grade (13+ ani) lei", name="pret_grade", type="number", min="0", step="0.01", placeholder="Opțional"
       - label="Preț Centuri Negre lei", name="pret_centuri", type="number", min="0", step="0.01", placeholder="Opțional"
       Toate condiționate pe `{type === 'Stagiu' && (...)}`
    3. În handleSubmit, include în eventData:
       `pret_copii: formState.pret_copii ? parseFloat(formState.pret_copii) : null` (idem grade, centuri)
    4. În useEffect reset form, populează din `evToEdit.pret_copii ?? ''`

    IMPORTANT: Nu modifica EvenimentDetail, handleAddParticipant sau logica CVD.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript trece. `grep -n "pret_copii" components/Competitii/StagiiCompetitii.tsx` returnează matches în EvenimentForm JSX și handleSubmit.</done>
</task>

<task type="auto">
  <name>Task 2b: EvenimentDetail — calcul categorie + taxă + handleAddParticipant + guard permisiuni</name>
  <files>components/Competitii/StagiiCompetitii.tsx</files>
  <behavior>
    - calculeazaCategorieStagiu('2016-01-01', '2026-06-01', null) returnează 'copii' (10 ani)
    - calculeazaCategorieStagiu('2010-01-01', '2026-06-01', null) returnează 'grade' (16 ani, fără Dan)
    - getTaxaStagiu cu eveniment.pret_copii=150, categorie='copii' returnează 150
    - getTaxaStagiu cu toate null + preturiConfig 'Taxa Stagiu' suma=100 returnează 100
    - INSERT în plati include `eveniment_id: eveniment.id`
    - Formularul "Înscrie Participant" vizibil doar pentru permissions.isAdminClub
    - Preview taxă apare când sportiv e selectat
  </behavior>
  <action>
    Modificări în `components/Competitii/StagiiCompetitii.tsx` — DOAR în componenta EvenimentDetail:

    1. **Importă** `calculeazaVarstaLaData` din `utils/eligibilitateCompetitie.ts`

    2. **Adaugă funcția** `calculeazaCategorieStagiu(dataNasterii, dataStagiu, gradActualId, grade)`:
       - Folosește `calculeazaVarstaLaData` pentru calcul vârstă corect
       - Dacă vârstă 7-12 → 'copii'
       - Dacă gradActualId definit și grade.find(g=>g.id===gradActualId)?.denumire include 'Dan' → 'centuri'
       - Altfel → 'grade'

    3. **Adaugă funcția** `getTaxaStagiu(eveniment, categorie, preturiConfig)`:
       - Nivel 1: eveniment.pret_copii / pret_grade / pret_centuri (după categorie)
       - Nivel 2: skip (tipuri_stagii.pret nu e în context EvenimentDetail)
       - Nivel 3: getPretValabil(preturiConfig, 'Taxa Stagiu', eveniment.data)?.suma
       - Returnează null dacă niciun nivel

    4. **Actualizează handleAddParticipant** (linia ~228-244):
       - Guard: `if (!permissions.isAdminClub) return`
       - Calculează categorie și suma folosind funcțiile de mai sus
       - `newPlata` include `eveniment_id: eveniment.id`
       - Generează plată doar dacă suma > 0; altfel showSuccess fără taxă

    5. **Guard UI**: condiționează formularul/butonul "Înscrie Participant" pe `permissions.isAdminClub`

    6. **Preview taxă**: `useMemo` pe sportivul selectat → text `Taxa estimată: X lei (Categorie: Y)` sub butonul Înscrie

    IMPORTANT: Nu sparge logica CVD existentă (secțiunea armă per sportiv). Modificările sunt additive.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript trece. `grep -n "eveniment_id.*eveniment\.id" components/Competitii/StagiiCompetitii.tsx` returnează match. Guard permissions.isAdminClub prezent pe formular.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→Supabase | INSERT plată cu suma calculată client-side — suma nu e validată server-side |
| ADMIN_CLUB→evenimente | Admin poate seta prețuri per eveniment — nu există validare RLS pe valorile prețurilor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | handleAddParticipant (suma calculată client) | accept | Suma e din config propriu al adminului; RLS plati_insert restricționează scrierile la ADMIN_CLUB+ |
| T-04-04 | Elevation of Privilege | EvenimentForm (pret_copii fields) | mitigate | Câmpurile preț sunt vizibile doar dacă `type === 'Stagiu'` — nu se pot seta prețuri pe competiții accidental |
| T-04-05 | Information Disclosure | preturiConfig fetch (deferred) | accept | Date de configurare propriu club — filtrarea RLS existentă asigură că nu se văd config-uri ale altor cluburi |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Nu se instalează pachete în această fază |
</threat_model>

<verification>
1. `grep -n "preturi_config" hooks/useDataProvider.ts` returnează match în deferredQueries
2. `grep -n "pret_copii\|pret_grade\|pret_centuri" components/Competitii/StagiiCompetitii.tsx` returnează matches în EvenimentForm și handleSubmit
3. `grep -n "eveniment_id.*eveniment\.id" components/Competitii/StagiiCompetitii.tsx` returnează match în handleAddParticipant
4. `grep -n "calculeazaCategorieStagiu" components/Competitii/StagiiCompetitii.tsx` returnează definiție și apel
5. `npx tsc --noEmit` trece fără erori noi
6. Manual (dacă Supabase disponibil): înscrie un sportiv la stagiu → plata apare în PlatiScadente cu suma > 0 și eveniment_id non-null
</verification>

<success_criteria>
- Un admin poate crea un stagiu cu prețuri diferite pentru Copii / Grade / Centuri Negre
- La înscrierea unui sportiv de 10 ani, taxa selectată este `pret_copii` (sau fallback)
- Plata generată are `tip='Taxa Stagiu'`, `status='Neachitat'`, `eveniment_id` completat
- preturiConfig nu mai este array vid după mount (fix bug critic STG-02)
</success_criteria>

<output>
Creează `.planning/phases/04-stagii-completare/04-02-SUMMARY.md` când gata.
</output>
