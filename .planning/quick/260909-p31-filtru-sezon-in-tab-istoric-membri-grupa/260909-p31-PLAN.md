---
phase: quick-260909-p31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Grupe/GrupaDetailView.tsx
  - components/UserProfile.tsx
autonomous: true
requirements: [QUICK-260909-P31-01, QUICK-260909-P31-02]

must_haves:
  truths:
    - "In tab-ul 'Istoric Membri' (Detalii grupa) adminul vede un select 'Sezon' populat cu sezoanele clubului grupei, ordonate descrescator dupa data_start"
    - "Optiunea implicita 'Toate sezoanele' arata exact aceleasi randuri ca inainte de aceasta modificare (zero regresie)"
    - "La selectarea unui sezon raman doar randurile al caror interval [data_intrare, data_iesire sau azi] se suprapune cu [data_start, data_final] al sezonului"
    - "Cand filtrul de sezon nu returneaza niciun rand dar grupa are istoric, apare un mesaj explicit cu buton de resetare a filtrului, nu mesajul generic 'Niciun istoric inregistrat'"
    - "Cand grupa nu are club_id sau clubul nu are niciun sezon definit, selectul nu se randeaza si tab-ul se comporta ca azi"
    - "Eticheta tab-ului 'Istoric Membri' din Detalii grupa afiseaza numarul de inregistrari de istoric inainte de a da click pe tab"
    - "Eticheta tab-ului 'Istoric Grupe' de pe profilul sportivului afiseaza numarul de inregistrari de istoric inainte de a da click pe tab"
    - "Ambele tab-uri au un tooltip (atribut title) care explica pe scurt ce contine tab-ul"
    - "npm run lint (tsc --noEmit) trece fara erori noi"
  artifacts:
    - path: "components/Grupe/GrupaDetailView.tsx"
      provides: "TabIstoricMembri cu filtru sezon + contor pe eticheta tab-ului Istoric Membri"
      contains: "useSezoane"
    - path: "components/UserProfile.tsx"
      provides: "Contor + tooltip pe eticheta tab-ului 'Istoric Grupe'"
      contains: "useIstoricGrupeSportiv"
  key_links:
    - from: "components/Grupe/GrupaDetailView.tsx"
      to: "hooks/useSezoane.ts"
      via: "import useSezoane(grupa.club_id) in TabIstoricMembri"
      pattern: "useSezoane"
    - from: "components/Grupe/GrupaDetailView.tsx"
      to: "hooks/useGrupeIstoric.ts"
      via: "useIstoricMembriGrupa apelat si in GrupaDetailView pentru contorul de pe tab (aceeasi queryKey => deduplicat de React Query)"
      pattern: "useIstoricMembriGrupa"
    - from: "components/UserProfile.tsx"
      to: "hooks/useGrupeIstoric.ts"
      via: "useIstoricGrupeSportiv(sportiv.id) pentru contorul de pe eticheta tab-ului"
      pattern: "useIstoricGrupeSportiv"
---

<objective>
Adauga un filtru de sezon in tab-ul "Istoric Membri" din Detalii grupa si fa vizibile (prin contor + tooltip) cele doua tab-uri de istoric de grupe care exista deja dar nu erau observate de utilizatori.

Purpose: componenta unei grupe dintr-un sezon arhivat trebuie consultabila ca "evidenta din arhiva" — fara filtru pe sezon, lista bruta de intrari/iesiri e greu de citit, iar tab-urile care o contin nu erau descoperite deloc.
Output: `components/Grupe/GrupaDetailView.tsx` cu select de sezon in `TabIstoricMembri` + contor pe eticheta tab-ului; `components/UserProfile.tsx` cu contor + tooltip pe eticheta tab-ului "Istoric Grupe".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Fisiere de modificat:
@components/Grupe/GrupaDetailView.tsx
@components/UserProfile.tsx

Infrastructura existenta (NU se modifica, doar se consuma):
@hooks/useSezoane.ts
@hooks/useGrupeIstoric.ts
@services/grupeIstoricService.ts
@components/UserProfile/GrupeIstoricTab.tsx

## Contracte deja verificate — nu re-explora codul pentru ele

**`hooks/useSezoane.ts`** exporta `useSezoane(clubId: string | null | undefined)` — React Query, `queryKey: ['sezoane', clubId]`, `enabled: !!clubId`, returneaza `Sezon[]` deja ordonat `data_start` descrescator, scoping `.eq('club_id', clubId)`. Exporta si `useSezonActiv` / `useSezonActivCurent` — **nu** sunt potrivite aici (returneaza doar sezonul activ ca focus); foloseste `useSezoane`.

**`types.ts` -> `Sezon`**: `{ id, created_at, club_id, denumire, data_start, data_final, activ }`. `data_start` / `data_final` sunt coloane DATE Postgres, deci string-uri `'YYYY-MM-DD'`.

**`types.ts` -> `Grupa`**: are `club_id?: string | null`. `GrupaWithDetails` (definit local in `GrupaDetailView.tsx` la linia ~15) extinde `Grupa`, deci `grupa.club_id` e disponibil in `GrupaDetailView`.

**`types.ts` -> `SportivGrupaIstoric`**: `{ id, sportiv_id, grupa_id, club_id, tip, data_intrare, data_iesire (nullable), motiv_iesire, created_at?, created_by }`. `data_intrare` / `data_iesire` sunt tot coloane DATE -> `'YYYY-MM-DD'`.

**`hooks/useGrupeIstoric.ts`**: `useIstoricMembriGrupa(grupaId)` -> `queryKey: ['grupe-istoric-membri', grupaId]`, randuri cu join `sportivi(id, nume, prenume)`. `useIstoricGrupeSportiv(sportivId)` -> `queryKey: ['grupe-istoric-sportiv', sportivId]`, randuri cu join `grupe(denumire)`. Ambele `staleTime: 2 min`. Apelarea aceluiasi hook cu aceeasi cheie in doua componente NU produce request suplimentar (React Query deduplica) — pe asta se bazeaza contorul de pe eticheta tab-ului.

**`components/ui.tsx`** exporta `Select` (forwardRef pe `<select>`, prop optional `label`, primeste `<option>` ca children) si `Badge` (`variant?: 'green' | 'red' | 'amber' | 'blue' | 'slate'`). Se folosesc astea — **nu** se importa Shadcn/MUI, **nu** se modifica `ui.tsx`.

**Structura curenta relevanta din `components/Grupe/GrupaDetailView.tsx`:**
- linia ~20: `type TabId = 'antrenamente' | 'orar' | 'sportivi' | 'istoric'`
- linia ~692: helper local `formatData(dateStr)` — reutilizabil
- linia ~697: `const TabIstoricMembri: React.FC<{ grupaId: string }> = ({ grupaId }) => {...}` — apeleaza `useIstoricMembriGrupa(grupaId)`, are early return pe `isLoading` / `error` / `istoric.length === 0`, apoi randeaza un `<h3>` cu "N inregistrari" si un `<Card>` cu tabel (coloane Sportiv / Intrare / Iesire)
- linia ~755: `GrupaDetailView` — `useState<TabId>('antrenamente')`
- liniile ~788-802: bara de tab-uri, `(['antrenamente','orar','sportivi','istoric'] as TabId[]).map(tab => <button ...>{ternar imbricat pentru eticheta}</button>)`
- linia ~815: `{activeTab === 'istoric' && <TabIstoricMembri grupaId={grupa.id} />}`

**Structura curenta relevanta din `components/UserProfile.tsx`:**
- linia 36: `import { GrupeIstoricTab } from './UserProfile/GrupeIstoricTab';`
- liniile 94-269: bloc de hook-uri top-level (`useDataStartFacturare`, apoi o serie de `useMemo`). Primul `return` al componentei e la linia ~645 — **nu exista early return inaintea lui**, deci un hook nou adaugat langa linia 94-99 respecta Rules of Hooks.
- linia ~169: `canViewSensitiveInfo` (useMemo)
- liniile 821-828: array inline de tab-uri; intrarea tinta e `{ id: 'grupe-istoric', label: 'Istoric Grupe', icon: CalendarDaysIcon, hidden: !canViewSensitiveInfo }`, urmata de `.filter(tab => !tab.hidden).map(tab => <button ...>)`; butonul randeaza `<tab.icon .../>` apoi `{tab.label}`
- linia ~924: `{activeTab === 'grupe-istoric' && <GrupeIstoricTab sportiv={sportiv} />}`

## Scope protection (confirmat cu utilizatorul in intake-ul portal-debug — nu se redeschide)

NU modifica: logica de arhivare/duplicare grupe la schimbare sezon; modulul Prezenta; calculul de abonamente; filtrul sezon-grupe din `components/Grupe/index.tsx` (`viewParams.sezonId`); `contexts/NavigationContext.tsx`; `types.ts`; `components/ui.tsx`; `contexts/DataContext.tsx`; `hooks/useDataProvider.ts`; `hooks/useSezoane.ts`; `hooks/useGrupeIstoric.ts`; `services/grupeIstoricService.ts`; `components/UserProfile/GrupeIstoricTab.tsx`.

TypeScript strict mode e DEZACTIVAT in acest proiect — tipurile sunt flexibile, dar `npm run lint` (`tsc --noEmit`) trebuie sa treaca fara erori noi.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filtru sezon in TabIstoricMembri</name>
  <files>components/Grupe/GrupaDetailView.tsx</files>
  <action>
In `components/Grupe/GrupaDetailView.tsx` adauga un filtru de sezon peste tabelul din `TabIstoricMembri`.

Importuri de adaugat in capul fisierului: `useSezoane` din `../../hooks/useSezoane`, `Select` adaugat la importul existent din `../ui` (linia 4 importa deja `Button, Card, Input, Modal, ConfirmButton, Badge`), si `type { Sezon }` adaugat la importul de tipuri din `../../types` (linia 3).

Schimba semnatura `TabIstoricMembri` din `{ grupaId: string }` in `{ grupaId: string; clubId?: string | null }` si actualizeaza call-site-ul de la linia ~815 sa treaca si `clubId={grupa.club_id}` pe langa `grupaId={grupa.id}`.

In corpul componentei, dupa `useIstoricMembriGrupa(grupaId)`:
- Apeleaza `useSezoane(clubId)` si destructureaza lista cu default `[]` (numeste-o `sezoane`). Cand `clubId` e null/undefined hook-ul e dezactivat si lista ramane goala — asta e ramura "fara filtru", nu o eroare.
- Adauga `useState<string>('')` pentru `sezonFiltruId`; string gol inseamna "Toate sezoanele".
- Deriva `sezonSelectat` din `sezoane.find(s => s.id === sezonFiltruId) ?? null`.

Adauga langa `formatData` (linia ~692) un helper modul-level pur, `seSuprapuneCuSezon(entry, sezon)`, care returneaza `true` daca intervalul de apartenenta la grupa se suprapune cu intervalul sezonului. Reguli exacte:
- normalizeaza fiecare data cu `String(x).slice(0, 10)` inainte de comparatie (coloanele sunt DATE, comparatia lexicografica pe `'YYYY-MM-DD'` e corecta si evita capcanele de fus orar ale lui `new Date()`);
- `intrare = slice(entry.data_intrare)`;
- `iesire = entry.data_iesire ? slice(entry.data_iesire) : dataAzi` unde `dataAzi` se calculeaza cu `new Date().toISOString().slice(0, 10)` (acelasi pattern ca `today()` din `services/grupeIstoricService.ts`) — un membru inca activ se considera prezent pana azi;
- returneaza `intrare <= sezon.data_final && iesire >= sezon.data_start`;
- daca `entry.data_intrare` lipseste, returneaza `false`.

Filtreaza randurile cu `useMemo` (`useMemo` e deja importat la linia 1): daca `sezonSelectat` e null returneaza lista completa nemodificata (referinta identica — zero regresie pe "Toate sezoanele"), altfel `istoric.filter(e => seSuprapuneCuSezon(e, sezonSelectat))`. Dependinte: lista de istoric si `sezonSelectat`.

ATENTIE la ordinea hook-urilor: `TabIstoricMembri` are early return-uri pe `isLoading` / `error` / lista goala. TOATE hook-urile noi (`useSezoane`, `useState`, `useMemo`) trebuie declarate INAINTE de aceste early return-uri, altfel se incalca Rules of Hooks.

Randare:
- Deasupra tabelului, randeaza selectul DOAR cand `sezoane.length > 0`. Foloseste `Select` din `../ui` cu `label="Filtreaza dupa sezon"`, `value={sezonFiltruId}`, `onChange` care seteaza `e.target.value`. Prima optiune: `value=""` cu textul "Toate sezoanele". Apoi cate o optiune per sezon, cu `key={s.id}`, `value={s.id}` si text `s.denumire` (adauga sufixul " (activ)" pentru sezonul cu `s.activ === true`). Limiteaza latimea selectului pe desktop (ex. wrapper cu `w-full sm:max-w-xs`) ca sa nu se intinda pe toata pagina.
- Inlocuieste `<h3>` cu "N inregistrari": cand nu e filtru activ pastreaza textul exact ca azi (numarul total + " inregistrari"); cand e filtru activ afiseaza numarul filtrat, totalul si denumirea sezonului (ex. forma "X din Y inregistrari · <denumire sezon>"). Pastreaza aceleasi clase de stil ca azi.
- Muta early return-ul pentru lista complet goala astfel incat sa ramana mesajul actual "Niciun istoric inregistrat pentru aceasta grupa" DOAR cand istoricul brut e gol. Cand istoricul brut are randuri dar filtrul pe sezon returneaza zero, randeaza in locul tabelului (dar SUB select, ca utilizatorul sa poata schimba filtrul) un `Card` cu mesajul ca niciun membru nu a fost in grupa in sezonul selectat, plus un `Button` variant secondary care reseteaza `sezonFiltruId` la string gol.
- Tabelul itereaza peste lista filtrata, nu peste `istoric`. Nu schimba coloanele, formatarea, `key`-urile sau stilurile existente.

Nu introduce niciun query Supabase nou in acest fisier — singura sursa noua de date e hook-ul `useSezoane`, deja scopat pe `club_id` si acoperit de RLS.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>grep -c "useSezoane" components/Grupe/GrupaDetailView.tsx</automated>
    <automated>grep -c "seSuprapuneCuSezon" components/Grupe/GrupaDetailView.tsx</automated>
    <automated>grep -c "clubId={grupa.club_id}" components/Grupe/GrupaDetailView.tsx</automated>
    <automated>grep -c "supabase.from('sezoane')" components/Grupe/GrupaDetailView.tsx</automated>
    <human-check>Deschide Grupe -> o grupa -> tab "Istoric Membri". Selectul "Filtreaza dupa sezon" apare deasupra tabelului cu "Toate sezoanele" preselectat si aceleasi randuri ca inainte. Alege un sezon vechi: raman doar membrii care au fost in grupa in acel interval, iar antetul arata "X din Y inregistrari". Alege un sezon fara membri: apare mesajul dedicat + butonul de resetare, iar butonul readuce lista completa. Consola browserului: zero erori.</human-check>
  </verify>
  <done>`npm run lint` trece; primele 3 grep-uri returneaza >= 1; ultimul grep returneaza 0 (fara query Supabase nou in componenta); "Toate sezoanele" produce exact randurile de dinainte; selectarea unui sezon filtreaza pe suprapunere de intervale; lista goala din filtru are mesaj propriu si buton de resetare.</done>
</task>

<task type="auto">
  <name>Task 2: Contor + tooltip pe etichetele tab-urilor de istoric</name>
  <files>components/Grupe/GrupaDetailView.tsx, components/UserProfile.tsx</files>
  <action>
Fa cele doua tab-uri de istoric descoperibile inainte de click, prin contor pe eticheta si tooltip.

**A. `components/Grupe/GrupaDetailView.tsx` — tab "Istoric Membri"**

In componenta `GrupaDetailView` (linia ~755), apeleaza `useIstoricMembriGrupa(grupa.id)` la nivel top-level, imediat dupa `useState`-urile existente si inainte de orice return, si deriva `istoricCount` din lungimea listei (default 0). Hook-ul e deja importat la linia 11. Este aceeasi `queryKey` folosita de `TabIstoricMembri`, deci React Query serveste ambele consumatoare dintr-un singur request — nu adauga trafic.

Refactorizeaza randarea etichetelor din bara de tab-uri (liniile ~788-802): inlocuieste ternarul imbricat din corpul butonului cu un obiect constant modul-level `TAB_LABELS: Record<TabId, string>` cu valorile actuale ('Antrenamente', 'Orar', 'Sportivi', 'Istoric Membri') si un obiect constant `TAB_HINTS: Record<TabId, string>` cu descrieri scurte in romana; foloseste `TAB_HINTS[tab]` ca `title` pe fiecare `<button>`. Hint-ul pentru 'istoric' trebuie sa explice ca tab-ul contine evidenta din arhiva a intrarilor si iesirilor din grupa, filtrabila pe sezon.

Pe tab-ul 'istoric', dupa eticheta, randeaza contorul doar cand `istoricCount > 0`: un `<span>` mic cu numarul, stilizat discret si consistent cu bara existenta (ex. `ml-2 text-xs font-normal px-1.5 py-0.5 rounded-full bg-slate-700 text-slate-300`). Nu folosi `Badge` daca inaltimea lui sparge alinierea butonului de 40px — prefera span-ul.

Nu schimba `TabId`, ordinea tab-urilor, clasele de stare activ/inactiv sau comportamentul `setActiveTab`.

**B. `components/UserProfile.tsx` — tab "Istoric Grupe"**

Adauga importul `useIstoricGrupeSportiv` din `../hooks/useGrupeIstoric` (verifica calea relativa corecta fata de `components/UserProfile.tsx`).

Apeleaza `useIstoricGrupeSportiv(sportiv.id)` in blocul de hook-uri top-level, langa linia 94-99 (dupa `useDataStartFacturare`), si deriva `istoricGrupeCount` din lungimea listei (default 0). Componenta nu are early return inaintea return-ului principal de la linia ~645, deci pozitia respecta Rules of Hooks. Aceeasi `queryKey` e folosita de `GrupeIstoricTab`, deci nu se adauga request suplimentar cand tab-ul se deschide.

In array-ul inline de tab-uri (liniile 821-828):
- adauga un camp `hint` pe intrarea `grupe-istoric` cu o descriere scurta in romana (ex. ca listeaza toate grupele prin care a trecut sportivul, cu date de intrare/iesire si durata, exportabil CSV);
- pentru consistenta, poti adauga `hint` si pe celelalte intrari, dar nu e obligatoriu — daca il adaugi doar pe unele, randeaza `title={(tab as any).hint}` astfel incat lipsa lui sa nu produca eroare de tip (strict mode e dezactivat, dar evita accesul la un camp inexistent pe unele obiecte din array prin tipare `as any` locala, nu prin modificarea `types.ts`);
- randeaza `title` pe `<button>`-ul de tab.

Dupa `{tab.label}` in butonul de tab, randeaza contorul doar pentru `tab.id === 'grupe-istoric'` si doar cand `istoricGrupeCount > 0`, cu acelasi stil discret de span folosit la punctul A.

Nu schimba `hidden: !canViewSensitiveInfo`, `initialTab`, tipul uniunii de la liniile 48 si 91, ordinea tab-urilor sau continutul randat la linia ~924.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>grep -c "TAB_HINTS" components/Grupe/GrupaDetailView.tsx</automated>
    <automated>grep -c "useIstoricGrupeSportiv" components/UserProfile.tsx</automated>
    <automated>grep -c "hint" components/UserProfile.tsx</automated>
    <automated>grep -rn "useIstoricMembriGrupa" components/Grupe/GrupaDetailView.tsx</automated>
    <human-check>Grupe -> o grupa cu istoric: eticheta tab-ului arata "Istoric Membri" cu numarul de inregistrari alaturi, iar hover-ul afiseaza tooltip-ul explicativ; o grupa fara istoric nu arata contorul. Profil sportiv (ca admin club): tab-ul "Istoric Grupe" arata contorul si tooltip-ul; click pe el deschide acelasi continut ca inainte. In DevTools -> Network, deschiderea tab-urilor NU declanseaza un al doilea request pe sportiv_grupa_istoric. Consola: zero erori, zero warning "Rendered more hooks than during the previous render".</human-check>
  </verify>
  <done>`npm run lint` trece; grep-urile returneaza >= 1 (ultimul arata 2 call-site-uri `useIstoricMembriGrupa` in GrupaDetailView: parinte + TabIstoricMembri); contorul apare doar cand exista inregistrari; tooltip-urile sunt vizibile la hover; niciun request de retea suplimentar; niciun warning de hooks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Supabase PostgREST | Toate citirile de `sportiv_grupa_istoric` si `sezoane` trec prin RLS + headerul `active-role-context-id` injectat de `supabaseClient.ts` |
| utilizator -> UI de filtrare | Valoarea selectata pentru sezon e input de utilizator folosit exclusiv client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P31-01 | Information Disclosure | citire sezoane in TabIstoricMembri | mitigate | Se refoloseste `useSezoane(clubId)` existent, care aplica `.eq('club_id', clubId)` peste RLS-ul tabelei `sezoane`; `clubId` provine din `grupa.club_id` (rand deja autorizat de RLS), nu din input de utilizator. Gate automat: `grep -c "supabase.from('sezoane')" components/Grupe/GrupaDetailView.tsx` trebuie sa fie 0 — interzice un query nou nescopat in componenta. |
| T-P31-02 | Information Disclosure | contor pe eticheta tab-ului "Istoric Grupe" (UserProfile) | mitigate | Contorul se afiseaza doar in interiorul tab-ului deja gardat de `hidden: !canViewSensitiveInfo`; nu se relaxeaza aceasta conditie. Datele raman filtrate de RLS pe `sportiv_grupa_istoric`. |
| T-P31-03 | Tampering | filtrare sezon | accept | Filtrarea e pur client-side, read-only, pe date deja livrate si autorizate de RLS; manipularea `sezonFiltruId` in DevTools nu poate dezvalui randuri pe care serverul nu le-a trimis deja. |
| T-P31-04 | Denial of Service | query suplimentar la fiecare randare de tab | mitigate | Contoarele refolosesc `queryKey`-urile existente (`grupe-istoric-membri`, `grupe-istoric-sportiv`) cu `staleTime` de 2 minute — React Query deduplica; verificat prin `human-check` pe tab-ul Network. |
| T-P31-SC | Tampering | supply chain (npm) | accept | Zero pachete noi instalate — constrangerea de proiect interzice librarii externe noi; nu exista pas de `npm install` in acest plan, deci gate-ul de legitimitate a pachetelor nu se aplica. |
</threat_model>

<verification>
1. `npm run lint` (tsc --noEmit) trece fara erori noi fata de baseline-ul de pe `main`.
2. `git diff --stat` atinge exact doua fisiere: `components/Grupe/GrupaDetailView.tsx` si `components/UserProfile.tsx`. Orice alt fisier modificat inseamna incalcarea scope protection.
3. Gate anti-scope-creep: `git diff --name-only` NU contine `types.ts`, `components/ui.tsx`, `contexts/NavigationContext.tsx`, `contexts/DataContext.tsx`, `hooks/useDataProvider.ts`, `hooks/useSezoane.ts`, `hooks/useGrupeIstoric.ts`, `services/grupeIstoricService.ts`, `components/Grupe/index.tsx`, `components/UserProfile/GrupeIstoricTab.tsx` sau vreun fisier din `components/Prezenta/`.
4. Regresie "Toate sezoanele": cu filtrul pe valoarea implicita, tabelul din "Istoric Membri" arata acelasi numar de randuri ca inainte de modificare.
5. Zero erori si zero warning-uri de hooks in consola browserului pe ambele suprafete.
</verification>

<success_criteria>
- Adminul poate selecta un sezon in tab-ul "Istoric Membri" si vede doar membrii care au apartinut grupei in acel interval de sezon.
- "Toate sezoanele" (implicit) pastreaza comportamentul de dinainte, fara nicio regresie.
- Grupele fara `club_id` sau cluburile fara sezoane definite nu vad selectul si functioneaza ca azi.
- Tab-ul "Istoric Membri" (Detalii grupa) si tab-ul "Istoric Grupe" (profil sportiv) arata numarul de inregistrari pe eticheta si un tooltip explicativ, fara request-uri de retea suplimentare.
- `npm run lint` trece; doar cele doua fisiere din `files_modified` sunt atinse.
</success_criteria>

<output>
Create `.planning/quick/260909-p31-filtru-sezon-in-tab-istoric-membri-grupa/260909-p31-SUMMARY.md` when done
</output>
