---
phase: 11-prezenta-refactorizata
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Prezenta/RaportIntervalExamen.tsx
  - components/LazyComponents.tsx
  - components/AppRouter.tsx
  - types.ts
autonomous: true
requirements: [PRZ-05]
must_haves:
  truths:
    - "Există un raport per interval examen la nivel de club care afișează, per sportiv, numărul de prezențe în fiecare interval dintre examene (PRZ-05c)"
    - "Intervalele sunt: [data_start sau primul antrenament -> examen1], [examen1 -> examen2], ..., [ultimul examen -> azi]"
    - "Raportul afișează DOAR numere absolute (count prezențe), niciun procent"
    - "Raportul este accesibil printr-o rută globală 'raport-interval-examen' înregistrată în AppRouter"
  artifacts:
    - path: "components/Prezenta/RaportIntervalExamen.tsx"
      provides: "Raport per interval examen la nivel club"
      exports: ["RaportIntervalExamen"]
      min_lines: 100
    - path: "components/AppRouter.tsx"
      provides: "Rută raport-interval-examen"
      contains: "raport-interval-examen"
  key_links:
    - from: "components/AppRouter.tsx"
      to: "components/LazyComponents.tsx"
      via: "Lazy.RaportIntervalExamen"
      pattern: "RaportIntervalExamen"
    - from: "components/Prezenta/RaportIntervalExamen.tsx"
      to: "rezultate_examene"
      via: "fetch grade per sportiv pentru calcul intervale"
      pattern: "from\\('(rezultate_examene|sesiuni_examene)'\\)"
---

<objective>
Creează raportul per interval examen la nivel de club (PRZ-05c): pentru fiecare sportiv al clubului, numărul absolut de prezențe în fiecare interval delimitat de examenele lui de grad. Reutilizează logica de calcul a intervalelor din useAttendanceStats, extrasă într-o funcție pură.

Sub-cerința (c) din PRZ-05. Atinge un fișier nou + AppRouter + LazyComponents + types (rută) — NU atinge index.tsx (shortcut-ul către această rută se adaugă în Plan 02, care deține index.tsx). Rulează în paralel cu Plan 01 și Plan 03 în Wave 1.

Purpose: Adminul vede progresul de prezență al fiecărui sportiv pe perioade între examene, pentru a evalua consistența înainte de următorul examen.
Output: RaportIntervalExamen.tsx + înregistrare lazy + rută AppRouter + tip View extins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-prezenta-refactorizata/11-RESEARCH.md
@CLAUDE.md
@hooks/useAttendanceStats.ts
@components/Prezenta/RaportLunarPrezenta.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Creează RaportIntervalExamen.tsx cu calcul intervale per sportiv</name>
  <files>components/Prezenta/RaportIntervalExamen.tsx</files>
  <read_first>
    - hooks/useAttendanceStats.ts (logica gradeStats liniile 33-99 — sursa pentru calculul intervalelor; extrage-o ca funcție pură)
    - components/Prezenta/RaportLunarPrezenta.tsx (pattern fetch sesiuni_examene liniile 140-161, useData/filteredData, permissions, exportToCsv liniile 12-44)
    - types.ts (IstoricGrade linia 310, Grad, Sportiv)
    - contexts/DataContext (filteredData: sportivi, grupe, antrenamente, grade)
  </read_first>
  <action>
    Creează components/Prezenta/RaportIntervalExamen.tsx exportând componenta RaportIntervalExamen cu props `{ onBack: () => void; onViewSportiv?: (s: Sportiv) => void }`.
    1. Extrage logica de intervale într-o funcție pură în acest fișier: `function calculateGradeIntervals(istoricPrezenta: { data: string; status: string }[], sortedGrades: IstoricGrade[], grade: Grad[]): { period: string; count: number; date: string }[]`. Copiază algoritmul din useAttendanceStats.gradeStats (liniile 33-99): interval Început -> primul examen, intervale între examene consecutive, ultimul examen -> Prezent. Numără doar `p.status.toLowerCase() === 'prezent'`. Returnează numere absolute (count), fără procente. Păstrează ordinea reversed (recent first) ca în original.
    2. Folosește `useData()` pentru `filteredData` (sportivi, antrenamente, grade) și `usePermissions(activeRoleContext)`. clubId = permissions.isFederationLevel ? null : activeRoleContext?.club_id.
    3. Fetch grade per sportiv: `useEffect` care interoghează `rezultate_examene` join `sesiuni_examene` pentru a obține perechile (sportiv_id, grad_id, data_obtinere) ale sportivilor clubului. Schema sugerată: `supabase.from('rezultate_examene').select('sportiv_id, grad_id, sesiuni_examene(data, club_id)')` filtrat pe sportivii vizibili. Dacă structura reală diferă (vezi Open Question 1/3 din research), executorul verifică schema în docs/baza-de-date.md sau prin grep pe alte fetch-uri de rezultate_examene înainte de a finaliza query-ul. Construiește `gradesBySportiv: Map<string, IstoricGrade[]>`.
    4. Construiește prezențele per sportiv din filteredData.antrenamente: pentru fiecare sportiv, `istoricPrezenta = antrenamente.flatMap(a => (a.prezenta||[]).filter(p => p.sportiv_id === sportiv.id && p.status?.este_prezent).map(p => ({ data: a.data, status: 'prezent' })))`. (Atenție: prezenta din filteredData are status?.este_prezent boolean; mapează la string 'prezent' pentru funcția pură.)
    5. `reportData = useMemo`: pentru fiecare sportiv activ al clubului, apelează calculateGradeIntervals(istoricPrezentaSportiv, gradesBySportiv.get(sportiv.id) || [], grade). Rezultat: listă de { sportiv, intervale[] }.
    6. Randare: filtru opțional pe grupă (reutilizează pattern Select din RaportLunarPrezenta). Pentru fiecare sportiv, un card/rând cu numele și o listă de intervale: fiecare interval afișează `period` (ex. "5 Cup -> 4 Cup") și `count` (număr absolut de prezențe). NICIUN procent. Buton/click pe nume apelează onViewSportiv dacă există.
    7. Export CSV: reutilizează helper-ul exportToCsv (copiază-l din RaportLunarPrezenta sau mută-l în utils — la alegerea executorului; dacă îl copiezi, păstrează BOM-ul pentru Excel). Coloane: Nume Sportiv, Interval, Prezențe (count). NICIUN procent.
    Header cu buton Back (onBack) și titlu "Raport Prezență per Interval Examen".
    Respectă design system: Card, Button, Select din ../ui, clase Tailwind dark. NU Shadcn/MUI. NU instala librării noi.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "RaportIntervalExamen" | grep -v "^#"; test $? -ne 0 && echo "NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - Fișierul exportă RaportIntervalExamen (grep)
    - Conține funcția pură calculateGradeIntervals care numără doar prezențe (grep "calculateGradeIntervals")
    - Fetch din rezultate_examene sau sesiuni_examene pentru grade per sportiv (grep)
    - Niciun calcul de procent în fișier (grep "* 100" returnează 0 linii necomentate)
    - npx tsc --noEmit fără erori noi în fișier
  </acceptance_criteria>
  <done>Raport per interval examen care calculează count absolut de prezențe per sportiv per interval, cu export CSV fără procente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Înregistrează ruta raport-interval-examen (Lazy + AppRouter + types)</name>
  <files>components/LazyComponents.tsx, components/AppRouter.tsx, types.ts</files>
  <read_first>
    - components/LazyComponents.tsx (linia 11 RaportPrezenta, linia 49 RaportLunarPrezenta — pattern lazy import)
    - components/AppRouter.tsx (case 'raport-prezenta' linia 206, case 'raport-lunar-prezenta' linia 196 — pattern renderProtected cu isAtLeastInstructor)
    - types.ts (type View linia 521 — union de string-uri)
  </read_first>
  <action>
    1. types.ts (linia 521): adaugă `'raport-interval-examen'` în union-ul `type View` (oriunde în listă, ex. lângă 'raport-lunar-prezenta').
    2. components/LazyComponents.tsx: adaugă `export const RaportIntervalExamen = lazy(() => import('./Prezenta/RaportIntervalExamen').then(m => ({ default: m.RaportIntervalExamen })));` (pattern identic cu liniile 11 și 49).
    3. components/AppRouter.tsx: adaugă un nou case în switch, imediat după case 'raport-prezenta' (linia 207): `case 'raport-interval-examen': return renderProtected(<Lazy.RaportIntervalExamen onViewSportiv={onViewSportiv} onBack={handleBackToDashboard} />, isAtLeastInstructor);`. Verifică în context că `onViewSportiv`, `handleBackToDashboard` și `isAtLeastInstructor` sunt disponibile în scope-ul switch-ului (sunt — folosite de case-urile vecine).
    NU modifica index.tsx (shortcut-ul către această rută se adaugă în Plan 02).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "AppRouter|LazyComponents|raport-interval" | grep -v "^#"; test $? -ne 0 && echo "NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - types.ts type View conține 'raport-interval-examen' (grep)
    - LazyComponents.tsx exportă RaportIntervalExamen (grep)
    - AppRouter.tsx are case 'raport-interval-examen' randând Lazy.RaportIntervalExamen cu gate isAtLeastInstructor (grep)
    - npx tsc --noEmit fără erori noi
  </acceptance_criteria>
  <done>Ruta globală raport-interval-examen este înregistrată și protejată la nivel instructor+, încărcată lazy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase | fetch rezultate_examene per sportiv traversează către DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information Disclosure | fetch rezultate_examene | mitigate | Query filtrat pe sportivii din filteredData (deja per visibleClubIds); RLS pe rezultate_examene / sesiuni_examene filtrează per active-role-context-id |
| T-11-08 | Access Control | rută raport nouă | mitigate | renderProtected cu isAtLeastInstructor (V4 Access Control); SUPER_ADMIN și ADMIN_CLUB au acces, SPORTIV nu |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Faza nu instalează pachete noi (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- npx tsc --noEmit fără erori noi
- Funcția pură calculateGradeIntervals produce count absolut (fără procente)
- Ruta răspunde și e protejată la nivel instructor+
</verification>

<success_criteria>
- PRZ-05c: raport per interval examen la nivel club, count absolut, fără procente, accesibil prin rută globală
</success_criteria>

<output>
Create `.planning/phases/11-prezenta-refactorizata/11-04-SUMMARY.md` when done
</output>
