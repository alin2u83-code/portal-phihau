---
phase: 09-raport-financiar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/exportFinanciar.ts
  - components/Plati/RaportFinanciar.tsx
autonomous: true
requirements: [FIN-01, FIN-02, FIN-03, FIN-04]

must_haves:
  truths:
    - "Adminul vede un tab 'Restanțe' în RaportFinanciar și un tabel cu un rând per sportiv: nume, sumă totală datorată, nr. facturi, cea mai veche scadență (FIN-01, D-01, D-03)"
    - "Tabelul e sortat descrescător după sumă totală, secundar ascendent după cea mai veche scadență (D-04)"
    - "Adminul filtrează restanțele pe interval dată cu PeriodFilterBar; tabelul se actualizează live fără reload (FIN-02, D-06, D-07)"
    - "Butonul Export CSV descarcă restante.csv cu separator ';' și BOM UTF-8, compatibil Excel (FIN-03, D-09)"
    - "Butonul Export PDF descarcă restante.pdf cu antet [ClubNume] — Raport Restanțe + data generării și tabel formatat (FIN-04, D-10)"
  artifacts:
    - path: "utils/exportFinanciar.ts"
      provides: "RestantaRow type + exportRestanteCSV + exportRestantePDF"
      exports: ["RestantaRow", "exportRestanteCSV", "exportRestantePDF"]
    - path: "components/Plati/RaportFinanciar.tsx"
      provides: "Tab 'restante' cu state, useMemo de agregare, render JSX"
      contains: "'restante'"
  key_links:
    - from: "components/Plati/RaportFinanciar.tsx"
      to: "utils/exportFinanciar.ts"
      via: "import RestantaRow, exportRestanteCSV, exportRestantePDF"
      pattern: "exportRestante(CSV|PDF)"
    - from: "components/Plati/RaportFinanciar.tsx restanteRows useMemo"
      to: "plati prop"
      via: "filtrare status !== 'Achitat' + grupare per sportiv_id"
      pattern: "restanteRows"
    - from: "tab JSX export buttons"
      to: "exportRestanteCSV / exportRestantePDF"
      via: "onClick handlers cu restanteRows + clubNume"
      pattern: "exportRestante(CSV|PDF)\\(restanteRows"
---

<objective>
Adaugă tab-ul "Restanțe" în componenta existentă `RaportFinanciar.tsx` și două funcții de export în `utils/exportFinanciar.ts`. Tab-ul afișează un tabel per-sportiv cu suma totală datorată, numărul de facturi neachitate și cea mai veche scadență, cu filtrare pe interval dată (PeriodFilterBar) și export CSV + PDF. Acoperă integral FIN-01..FIN-04.

Purpose: Adminul de club vede dintr-un singur ecran cine datorează bani, de cât timp, cu posibilitate de filtrare și export pentru contabilitate (per ROADMAP Phase 9 Goal).
Output: 2 fișiere modificate, zero fișiere noi, zero dependențe noi, zero migrații DB.
</objective>

<mvp_note>
Aceasta este o singură felie verticală end-to-end (slice complet): definirea contractului de export → logica de agregare → UI tab cu render. La finalul planului, adminul poate face ceva nou și real — vede și exportă restanțele. Task-urile sunt ordonate interface-first (contract → logică → wiring UI), nu pe straturi orizontale.
</mvp_note>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-raport-financiar/09-CONTEXT.md
@.planning/phases/09-raport-financiar/09-RESEARCH.md
@.planning/phases/09-raport-financiar/09-PATTERNS.md
@.planning/phases/09-raport-financiar/09-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symboluri create/modificate de această fază (sursă pentru source-grounding):

**În `utils/exportFinanciar.ts`:**
- `RestantaRow` — interface exportată: `{ sportiv_id: string; numeSportiv: string; sumaTotala: number; nrFacturi: number; ceaMaiVecheScadenta: string }`
- `exportRestanteCSV(rows: RestantaRow[], clubNume?: string, filename?: string): void` — funcție exportată
- `exportRestantePDF(rows: RestantaRow[], clubNume?: string, filename?: string): Promise<void>` — funcție exportată

**În `components/Plati/RaportFinanciar.tsx`:**
- Valoarea `'restante'` adăugată în union-ul tipului `activeTab` (state)
- `restanteStart` / `setRestanteStart` — state local `useState('')`
- `restanteEnd` / `setRestanteEnd` — state local `useState('')`
- `restanteRows` — `useMemo` returnând `RestantaRow[]`
- Intrarea `{ id: 'restante' as const, label: 'Restanțe', icon: <ExclamationTriangleIcon ... /> }` în array-ul `tabs`
- `clubNume` — derivat din `clubs.find(...).nume`
- Block JSX `activeTab === 'restante'` (PeriodFilterBar + bar total/export + tabel desktop + carduri mobile + empty state)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Contract de export — RestantaRow + exportRestanteCSV + exportRestantePDF în exportFinanciar.ts</name>
  <files>utils/exportFinanciar.ts</files>
  <read_first>
    - utils/exportFinanciar.ts (fișierul modificat — vezi structura curentă: helper privat `fmtSum` liniile 14-15, `exportIncasariCSV` liniile 19-47, `exportIncasariPDF` liniile 51-115)
    - .planning/phases/09-raport-financiar/09-PATTERNS.md (Pattern 6, Pattern 7 — cod exact de replicat)
    - .planning/phases/09-raport-financiar/09-UI-SPEC.md (secțiunea Copywriting Contract — anteturi coloane PDF/CSV; secțiunea Culori — antet tabel PDF amber)
  </read_first>
  <action>
    Adaugă la FINALUL fișierului `utils/exportFinanciar.ts` (după linia 115), fără a modifica funcțiile existente:

    1. Exportă interfața `RestantaRow` cu câmpurile exact: `sportiv_id: string`, `numeSportiv: string`, `sumaTotala: number`, `nrFacturi: number`, `ceaMaiVecheScadenta: string` (string ISO YYYY-MM-DD sau ''). Definește-o AICI (nu în RaportFinanciar.tsx) pentru ca RaportFinanciar.tsx să o importe — evită importul circular (Pitfall 5 din RESEARCH.md, D-08).

    2. Exportă funcția `exportRestanteCSV(rows: RestantaRow[], clubNume = 'Club QwanKiDo', filename = 'restante.csv')`. Replică EXACT structura din `exportIncasariCSV`: BOM `'﻿'`, separator `;` între celule, fiecare celulă învelită în ghilimele cu escape `"` → `""`, rânduri separate prin `\r\n`, `Blob` cu type `text/csv;charset=utf-8;`, download via `URL.createObjectURL` + element `<a>` + `a.click()` + `URL.revokeObjectURL`. HEADER = `['Sportiv', 'Suma Totala (RON)', 'Nr Facturi', 'Cea Mai Veche Scadenta']` (D-09). Pentru fiecare rând: `r.numeSportiv`, `fmtSum(r.sumaTotala)`, `String(r.nrFacturi)`, iar pentru scadență `r.ceaMaiVecheScadenta ? new Date(r.ceaMaiVecheScadenta).toLocaleDateString('ro-RO') : ''`. Reutilizează helperul privat `fmtSum` deja existent în fișier — NU îl redefini.

    3. Exportă funcția async `exportRestantePDF(rows: RestantaRow[], clubNume = 'Club QwanKiDo', filename = 'restante.pdf')`. Replică EXACT structura din `exportIncasariPDF`: dynamic import `const { jsPDF } = await import('jspdf')` și `const { default: autoTable } = await import('jspdf-autotable')` (NU import static — Pitfall 5). `new jsPDF({ orientation: 'landscape', unit: 'mm', format: 'a4' })`. Antet: `doc.text(clubNume, 14, 14)` cu font bold size 16 color (30,41,59); apoi normal size 10 color (100,116,139): `doc.text('Raport Restanțe', 14, 21)` și `doc.text(\`Generat: ${new Date().toLocaleDateString('ro-RO')} · ${rows.length} sportivi\`, 14, 27)` (D-10). Tabel via `autoTable(doc, {...})`: `head: [['Sportiv', 'Sumă Totală (RON)', 'Nr. Facturi', 'Cea Mai Veche Scadență']]`, `body` din rows (sportiv, fmtSum sumaTotala, String nrFacturi, scadență formatată ro-RO sau '—'), `foot: [['TOTAL', fmtSum(totalSuma), String(totalFacturi), '']]` unde totalSuma și totalFacturi sunt sumele agregate. `startY: 33`. `headStyles.fillColor: [245, 158, 11]` (amber pentru restanțe, NU indigo — vezi UI-SPEC secțiunea Culori), `headStyles.textColor: [255,255,255]`. Restul stilurilor (styles, alternateRowStyles, footStyles, columnStyles cu coloana index 1 halign right bold) identice cu exportIncasariPDF. Final: `doc.save(filename)`.

    Nu adăuga `RestantaRow` în `types.ts` central — este tip local de modul (convenție CLAUDE.md: tipurile interne UI nu poluează types.ts).
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "exportFinanciar" ; echo "exit-grep:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -E "export (interface RestantaRow|function exportRestanteCSV|async function exportRestantePDF)" utils/exportFinanciar.ts | grep -vc '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `utils/exportFinanciar.ts` conține `export interface RestantaRow` cu cele 5 câmpuri (sportiv_id, numeSportiv, sumaTotala, nrFacturi, ceaMaiVecheScadenta)
    - `utils/exportFinanciar.ts` conține `export function exportRestanteCSV` cu separator `;` și `const BOM = '﻿'`
    - `utils/exportFinanciar.ts` conține `export async function exportRestantePDF` cu `await import('jspdf')` și `await import('jspdf-autotable')`
    - `exportRestantePDF` folosește `fillColor: [245, 158, 11]` în headStyles (amber, nu indigo [99,102,241])
    - `npx tsc --noEmit` nu raportează nicio eroare nouă în `exportFinanciar.ts`
    - Funcțiile existente `exportIncasariCSV` și `exportIncasariPDF` rămân neschimbate
  </acceptance_criteria>
  <done>RestantaRow, exportRestanteCSV, exportRestantePDF sunt exportate din exportFinanciar.ts, tsc trece, funcțiile existente neatinse.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Agregare restanțe + wiring tab state în RaportFinanciar.tsx</name>
  <files>components/Plati/RaportFinanciar.tsx</files>
  <read_first>
    - components/Plati/RaportFinanciar.tsx (fișierul modificat — vezi: importuri liniile 1-17, props/state liniile 19-56, useData destructure linia 51, array tabs liniile 283-290)
    - utils/exportFinanciar.ts (pentru a importa RestantaRow, exportRestanteCSV, exportRestantePDF — definite în Task 1)
    - components/Plati/AgingReport.tsx (pattern `plata.data.toString().slice(0, 10)` pentru câmpul de scadență — confirmă că NU există data_scadenta)
    - utils/formatareSportiv.ts (semnătura formatNume)
    - .planning/phases/09-raport-financiar/09-PATTERNS.md (Pattern 1, 2, 3, 4, 5 — cod exact)
  </read_first>
  <action>
    Modifică `components/Plati/RaportFinanciar.tsx` (NU adăuga query Supabase — folosește prop-ul `plati` deja existent):

    1. Import: pe linia 10 extinde importul existent din `'../../utils/exportFinanciar'` ca să includă `exportRestanteCSV`, `exportRestantePDF` și tipul `RestantaRow` (`import { exportIncasariCSV, exportIncasariPDF, exportRestanteCSV, exportRestantePDF, RestantaRow } from '../../utils/exportFinanciar';`).

    2. Extinde destructurarea `useData()` de pe linia 51 din `const { currentUser } = useData();` în `const { currentUser, activeRoleContext, clubs } = useData();`. (Confirmat: useDataProvider expune `clubs: Club[]` și `activeRoleContext` — vezi RESEARCH.md Pattern 4. Nu schimba alte utilizări ale `currentUser`.)

    3. Extinde union-ul `activeTab` de pe linia 54 adăugând `| 'restante'` la finalul tipului: `useState<'incasari' | 'lunar' | 'taxe_anuale' | 'abonamente' | 'grafice' | 'familii' | 'restante'>('incasari')` (D-01).

    4. Adaugă DOUĂ state-uri locale noi (lângă celelalte `useState`, după linia 56), folosind `useState('')` — NU `useLocalStorage` (Pitfall 2: filtrele de restanțe nu trebuie să persiste între sesiuni și nu trebuie băgate în obiectul `filters`): `const [restanteStart, setRestanteStart] = useState('');` și `const [restanteEnd, setRestanteEnd] = useState('');`.

    5. Adaugă `clubNume` derivat (poate fi inline lângă render sau ca const în corpul componentei): `const clubNume = clubs?.find(c => c.id === activeRoleContext?.club_id)?.nume ?? 'Club QwanKiDo';` (D-10, FIN-04 antet PDF).

    6. Adaugă `restanteRows` ca `useMemo` returnând `RestantaRow[]` (FIN-01, FIN-02). Logica (vezi Pattern 3 din PATTERNS.md): filtrează `plati` cu `p.status !== 'Achitat'` (status outstanding); aplică filtrul de interval pe `p.data.toString().slice(0,10)` cu guard `isNaN` (CRITIC — câmpul de scadență este `p.data`, NU `p.data_scadenta`, care nu există pe interfața Plata — Pitfall 1); fără filtru = toate restanțele. Grupează per `p.sportiv_id ?? p.familie_id ?? '__necunoscut__'` (Pitfall 4: facturile de familie au sportiv_id null). Pentru fiecare grup calculează `sumaTotala = Σ p.suma`, `nrFacturi = count`, `ceaMaiVecheScadenta = min(date ISO)`. Mapează numeSportiv: dacă există `sportivi.find(s => s.id === sid)` → `formatNume(sp)`; altfel dacă `familii.find(f => f.id === sid)` → `\`${fam.nume} [Familie]\``; altfel `'—'`. Sortează descrescător după `sumaTotala`, secundar ascendent după `ceaMaiVecheScadenta` (D-04 + specifics CONTEXT.md). Dependențe useMemo: `[plati, sportivi, familii, restanteStart, restanteEnd]`.

    7. Adaugă intrarea tab în array-ul `tabs` (liniile 283-290), la finalul array-ului: `{ id: 'restante' as const, label: 'Restanțe', icon: <ExclamationTriangleIcon className="w-4 h-4" /> }` (D-02). `ExclamationTriangleIcon` este deja importat (linia 5) — nu adăuga import nou.

    NU adăuga încă block-ul JSX de render al tabelului — acela e Task 3. Acest task se oprește la state + useMemo + intrarea în array-ul tabs.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "RaportFinanciar" ; echo "exit-grep:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -cE "'restante'|restanteRows|restanteStart|restanteEnd" components/Plati/RaportFinanciar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Union-ul `activeTab` din RaportFinanciar.tsx conține `'restante'`
    - `useData()` este destructurat ca `{ currentUser, activeRoleContext, clubs }`
    - Există `const [restanteStart, setRestanteStart] = useState('')` și `const [restanteEnd, setRestanteEnd] = useState('')` (NU în useLocalStorage)
    - Există `const restanteRows = useMemo` cu dependențe `[plati, sportivi, familii, restanteStart, restanteEnd]`
    - Filtrarea folosește `p.data` (NU `p.data_scadenta`) și `p.status !== 'Achitat'`
    - Gruparea folosește `p.sportiv_id ?? p.familie_id ?? '__necunoscut__'`
    - Array-ul `tabs` conține o intrare cu `id: 'restante'` și `label: 'Restanțe'`
    - Importul din `exportFinanciar` include `exportRestanteCSV, exportRestantePDF, RestantaRow`
    - `npx tsc --noEmit` nu raportează eroare nouă în RaportFinanciar.tsx
  </acceptance_criteria>
  <done>activeTab acceptă 'restante', state-urile local de filtru există, restanteRows agregează corect din plati, intrarea de tab e în array, tsc trece.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Render JSX tab 'restante' — PeriodFilterBar + bar total/export + tabel + carduri mobile + empty state</name>
  <files>components/Plati/RaportFinanciar.tsx</files>
  <read_first>
    - components/Plati/RaportFinanciar.tsx (fișierul modificat — vezi: tab-ul 'incasari' render liniile 364-474 ca analog de structură; PeriodFilterBar usage liniile 384-388; butoane export liniile 451-474; `formatSum` linia 45, `formatDate` linia 39)
    - .planning/phases/09-raport-financiar/09-UI-SPEC.md (TOATE secțiunile de render: Tabel desktop, Carduri mobile, Empty State, Bar total + Export, Copywriting Contract, Culori Semantice — clase Tailwind exacte)
    - .planning/phases/09-raport-financiar/09-PATTERNS.md (Pattern 6, 7, 8, 9 — butoane export, PeriodFilterBar, empty state, tabel)
  </read_first>
  <action>
    Adaugă un block de render condiționat `{activeTab === 'restante' && (...)}` în RaportFinanciar.tsx, după block-urile de render ale celorlalte tab-uri (în interiorul container-ului principal, alături de `{activeTab === 'incasari' && (...)}`). Respectă EXACT clasele din 09-UI-SPEC.md. Structura (toate label-urile în română):

    1. Wrapper `<div className="space-y-4">`.

    2. `<PeriodFilterBar startDate={restanteStart} endDate={restanteEnd} onChange={(s, e) => { setRestanteStart(s); setRestanteEnd(e); }} />` (FIN-02, D-07, drop-in cu aceleași 3 props ca în tab-ul incasari).

    3. Bar total + export imediat sub PeriodFilterBar — container `flex flex-col sm:flex-row sm:items-center bg-[var(--t-surface-2)] border border-[var(--t-border)] rounded-xl px-4 py-3 gap-3`. Stânga: label `text-xs text-slate-400 uppercase tracking-wider font-bold` cu textul "Total Restanțe" + subtext `text-xs text-slate-500` cu `{restanteRows.length} sportivi cu restanțe` (sau "Niciun sportiv cu restanțe" când 0). Dreapta: suma totală agregată (`restanteRows.reduce((s,r) => s + r.sumaTotala, 0)` prin `formatSum`) cu clasa `text-2xl font-bold text-amber-400`. Două butoane export:
       - CSV: `onClick={() => exportRestanteCSV(restanteRows, clubNume)}`, `disabled={restanteRows.length === 0}`, clasele din UI-SPEC (slate: `bg-slate-700/60 hover:bg-slate-700 border border-slate-600/50 ... text-slate-300`), iconiță `<DownloadIcon className="w-3.5 h-3.5" />` + label "CSV".
       - PDF: `onClick={() => exportRestantePDF(restanteRows, clubNume)}`, `disabled={restanteRows.length === 0}`, clasele PDF din UI-SPEC (`bg-indigo-600/70 hover:bg-indigo-600 border border-indigo-500/50 ... text-white`), iconiță `<DocumentArrowDownIcon className="w-3.5 h-3.5" />` + label "PDF" (FIN-03, FIN-04, D-08).

    4. Empty state când `restanteRows.length === 0`: `<Card>` cu `<div className="flex flex-col items-center gap-3 py-12 text-center">`, `<CheckCircleIcon className="w-10 h-10 text-emerald-400 opacity-60" />`, heading `text-slate-300 font-bold` "Nicio restanță în intervalul selectat", body `text-slate-500 text-sm` "Toți sportivii sunt la zi cu plățile." (Notă UI-SPEC: empty = succes, deci CheckCircle verde, nu warning.)

    5. Tabel desktop când `restanteRows.length > 0`: wrapper `hidden md:block bg-[var(--t-bg)] border border-[var(--t-border)] rounded-xl overflow-hidden`, `<table className="w-full text-left text-sm">`. `<thead>` cu `<tr style={{ background: 'var(--t-table-header-bg)', color: 'var(--t-table-header-text)' }} className="border-b border-[var(--t-border)]">` și 4 `<th>` cu clasa `px-4 py-3 text-xs font-bold uppercase tracking-wider` (coloanele 2 și 3 cu `text-right`): "Sportiv" | "Sumă Totală (RON)" | "Nr. Facturi" | "Cea Mai Veche Scadență". `<tbody className="divide-y divide-[var(--t-border)]">`, fiecare rând `<tr className="hover:bg-[var(--t-table-row-hover)] transition-colors">` cu `<td>`: Sportiv `px-4 py-3 text-white font-medium` (`r.numeSportiv`); Sumă `px-4 py-3 text-right font-bold text-amber-400 whitespace-nowrap` (`formatSum(r.sumaTotala)`); Nr. Facturi `px-4 py-3 text-right text-slate-300` (`r.nrFacturi`); Scadență `px-4 py-3 text-slate-300 whitespace-nowrap` (`formatDate(r.ceaMaiVecheScadenta)`).

    6. Carduri mobile când `restanteRows.length > 0`: wrapper `md:hidden space-y-2`, fiecare card `bg-[var(--t-bg)] border border-[var(--t-border)] rounded-xl px-4 py-3` cu layout din UI-SPEC: nume sportiv `text-white font-bold text-sm truncate`, subtext `{r.nrFacturi} {r.nrFacturi === 1 ? 'factură' : 'facturi'} neachitate` în `text-slate-400 text-xs mt-1`, suma `text-amber-400 font-bold text-sm whitespace-nowrap shrink-0` (`formatSum(r.sumaTotala)`), iar dedesubt `text-xs text-slate-500` cu `Scadent din {formatDate(r.ceaMaiVecheScadenta)}`.

    Toate sumele prin `formatSum` (linia 45), toate datele prin `formatDate` (linia 39). Folosește `key={r.sportiv_id}` pe rânduri și carduri.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx tsc --noEmit 2>&1 | grep -i "RaportFinanciar" ; echo "exit-grep:$?"</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && grep -cE "activeTab === 'restante'|exportRestanteCSV\(restanteRows|exportRestantePDF\(restanteRows|PeriodFilterBar" components/Plati/RaportFinanciar.tsx</automated>
    <automated>cd "C:/Users/lungu/portal-phihau" && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Există block JSX `{activeTab === 'restante' && (...)}` în RaportFinanciar.tsx
    - Block-ul conține `<PeriodFilterBar startDate={restanteStart} endDate={restanteEnd}` cu onChange care apelează setRestanteStart și setRestanteEnd
    - Butonul CSV apelează `exportRestanteCSV(restanteRows, clubNume)` cu `disabled={restanteRows.length === 0}`
    - Butonul PDF apelează `exportRestantePDF(restanteRows, clubNume)` cu `disabled={restanteRows.length === 0}`
    - Tabelul desktop are 4 anteturi: Sportiv, Sumă Totală (RON), Nr. Facturi, Cea Mai Veche Scadență
    - Coloana sumă folosește `text-amber-400` (identitate vizuală restanțe)
    - Empty state folosește `CheckCircleIcon` text-emerald-400 cu textul "Nicio restanță în intervalul selectat"
    - Există ramura de carduri mobile `md:hidden`
    - `npx vite build` se termină cu succes (build complet, fără erori)
  </acceptance_criteria>
  <done>Tab-ul 'restante' randează complet: filtru perioadă funcțional, tabel desktop + carduri mobile, butoane export legate, empty state; build trece.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → Supabase | `plati: Plata[]` vine deja filtrat pe `club_id` prin header `active-role-context-id` (RLS). Faza nu adaugă query nou. |
| browser → fișier descărcat (CSV/PDF) | Export local din date deja afișate în UI — nicio transmisie nouă către server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Export CSV/PDF al restanțelor (sume, nume sportivi) | accept | Datele sunt deja vizibile în UI pentru adminul autentificat (ADMIN_CLUB/SUPER_ADMIN); exportul nu extinde suprafața de atac. RLS a filtrat deja pe club. |
| T-09-02 | Tampering | Filtrare client-side pe `restanteStart`/`restanteEnd` | mitigate | Filtrare pe date deja încărcate din query RLS-protected; input-uri `<input type="date">` (format YYYY-MM-DD garantat de browser) cu guard `isNaN(d.getTime())` în useMemo. Niciun SQL nou. |
| T-09-03 | Elevation of Privilege | Vizibilitate tab Restanțe | mitigate | RaportFinanciar e deja protejat de `isAtLeastClubAdmin` în AppRouter (V4 Access Control). Tab-ul nu introduce view nou (D-01). RLS filtrează `plati` pe `club_id`. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Faza nu instalează niciun pachet. jsPDF 4.2.1, jspdf-autotable 5.0.7 deja în package.json (vezi RESEARCH.md Standard Stack — Package Legitimacy Audit nu se aplică). |
</threat_model>

<verification>
Verificări end-to-end pentru întreaga fază:

1. `npx tsc --noEmit` — zero erori noi în `exportFinanciar.ts` și `RaportFinanciar.tsx`.
2. `npx vite build` — build complet cu succes.
3. Manual (dev server): navighează la RaportFinanciar → click tab "Restanțe" → tabelul afișează un rând per sportiv sortat descrescător după sumă (FIN-01, D-03, D-04).
4. Manual: aplică un interval în PeriodFilterBar → tabelul se reactualizează live fără reload (FIN-02).
5. Manual: click "CSV" → se descarcă `restante.csv`; deschide în Excel → separator `;`, diacritice corecte (BOM), coloane Sportiv/Sumă/Nr Facturi/Scadență (FIN-03, D-09).
6. Manual: click "PDF" → se descarcă `restante.pdf` cu antet `[ClubNume] — Raport Restanțe` + data generării + tabel cu antet amber (FIN-04, D-10).
7. Manual: filtru fără rezultate → empty state "Nicio restanță în intervalul selectat" cu CheckCircle verde.
</verification>

<success_criteria>
- [ ] Tab "Restanțe" vizibil în RaportFinanciar, tabel 1 rând/sportiv: Sportiv | Sumă Totală RON | Nr. Facturi | Cea Mai Veche Scadență, sortat descrescător după sumă (FIN-01)
- [ ] PeriodFilterBar filtrează live pe `plata.data` a facturilor cu status outstanding (FIN-02)
- [ ] Export CSV descarcă fișier compatibil Excel cu separator `;` și BOM (FIN-03)
- [ ] Export PDF descarcă fișier cu antet club + dată + tabel formatat (FIN-04)
- [ ] Zero fișiere noi, zero dependențe noi, zero migrații DB, zero query Supabase nou
- [ ] `npx tsc --noEmit` și `npx vite build` trec
- [ ] Toate deciziile D-01..D-10 implementate
</success_criteria>

<output>
Create `.planning/phases/09-raport-financiar/09-01-SUMMARY.md` when done
</output>
