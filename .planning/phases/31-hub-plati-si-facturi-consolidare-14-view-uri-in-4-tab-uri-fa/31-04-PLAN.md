---
phase: 31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa
plan: 04
type: execute
wave: 2
depends_on: [31-01, 31-02, 31-03]
files_modified:
  - components/Plati/PlatiScadente.tsx
  - components/Plati/GestiuneFacturi.tsx
  - components/Plati/FacturiFaraPrezenta.tsx
  - components/Plati/hub/TabFacturi.tsx
autonomous: true
requirements: [D-01, D-08, D-09]
user_setup: []

must_haves:
  truths:
    - "Tab-ul Facturi randeaza, dupa sectiunea activa, exact una din componentele PlatiScadente / GestiuneFacturi / FacturiFaraPrezenta cu props identice celor din AppRouter (D-01)"
    - "Din listele Facturi & Plati si Gestiune Facturi (desktop si mobil) se deschide ecranul de detaliu factura cu un buton dedicat (D-08)"
    - "Cand sunt montate in hub, cele 3 componente nu mai afiseaza propriul buton 'Meniu' (hub-ul are unul singur); montate in alta parte, se comporta exact ca inainte"
    - "Selectia multipla din Facturi & Plati continua sa trimita facturile selectate prin onIncaseazaMultiple (fluxul F1 neschimbat)"
  artifacts:
    - path: "components/Plati/hub/TabFacturi.tsx"
      provides: "Tab-ul Facturi al hub-ului"
      exports: ["TabFacturi", "TabFacturiProps"]
      min_lines: 50
    - path: "components/Plati/PlatiScadente.tsx"
      provides: "Props optionale hideBackButton si onDeschideDetalii + buton Detalii desktop/mobil"
      contains: "onDeschideDetalii"
    - path: "components/Plati/GestiuneFacturi.tsx"
      provides: "Props optionale hideBackButton si onDeschideDetalii + buton Detalii desktop/mobil"
      contains: "onDeschideDetalii"
    - path: "components/Plati/FacturiFaraPrezenta.tsx"
      provides: "Prop optional hideBackButton"
      contains: "hideBackButton"
  key_links:
    - from: "components/Plati/hub/TabFacturi.tsx"
      to: "components/Plati/FacturaDetaliu.tsx"
      via: "<FacturaDetaliu plataId={plataDetaliuId} onClose=... />"
      pattern: "<FacturaDetaliu"
    - from: "components/Plati/hub/TabFacturi.tsx"
      to: "components/LazyComponents.tsx"
      via: "Lazy.PlatiScadente / Lazy.GestiuneFacturi / Lazy.FacturiFaraPrezenta"
      pattern: "Lazy\\.(PlatiScadente|GestiuneFacturi|FacturiFaraPrezenta)"
    - from: "components/Plati/hub/TabFacturi.tsx"
      to: "components/Plati/hub/platiHubConfig.ts"
      via: "extends PlatiHubTabProps"
      pattern: "PlatiHubTabProps"
---

<objective>
Tab-ul "Facturi" al hub-ului (D-01: inlocuieste `plati-scadente`, `gestiune-facturi`, `facturi-fara-prezenta`) plus punctele de intrare in ecranul de detaliu factura (D-08) din listele de facturi, pentru ADMIN_CLUB si adminii de federatie (singurii care ajung in hub — guard `canManageFinances`; accesul INSTRUCTOR la detaliu e livrat prin profilul sportivului in 31-07, per D-09).

Componentele existente primesc DOAR props optionale noi (API-ul existent ramane compatibil — constrangere CLAUDE.md "Nu se sparge API-ul existent al componentelor"):
- `hideBackButton?: boolean` (implicit false) — ascunde butonul propriu "Meniu" cand hub-ul e parinte.
- `onDeschideDetalii?: (plata: Plata) => void` — cand e furnizat, apare butonul "Detalii" pe fiecare rand.

Props-urile transmise de TabFacturi sunt EXACT cele din harta verificata in 31-01 (antetul `components/Plati/hub/platiHubConfig.ts`).

Output: `components/Plati/hub/TabFacturi.tsx`; modificari minime in PlatiScadente, GestiuneFacturi, FacturiFaraPrezenta.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-CONTEXT.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-01-SUMMARY.md
@.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-03-SUMMARY.md
@CLAUDE.md
@components/Plati/hub/platiHubConfig.ts
</context>

<interfaces>
Contracte consumate (create in 31-01 si 31-03 — nu le redefini):
```ts
// components/Plati/hub/platiHubConfig.ts
export interface PlatiHubTabProps {
  sectiune: SectiuneHub;
  currentUser: User;
  permissions: Permissions;
  activeRoleContext: any;
  onViewSportiv: (sportiv: Sportiv, tab?: TabProfilSportiv) => void;
  onBack: () => void;
}
// components/Plati/FacturaDetaliu.tsx
export const FacturaDetaliu: React.FC<{ plataId: string | null; onClose: () => void }>;
```

Contract creat de acest plan (consumat de PlatiHub in 31-08):
```ts
// components/Plati/hub/TabFacturi.tsx
export interface TabFacturiProps extends PlatiHubTabProps {
  onIncaseazaMultiple: (plati: Plata[]) => void;
  initialSportivId?: string;
}
export const TabFacturi: React.FC<TabFacturiProps>;
```

Props exacte (harta 31-01, AppRouter.tsx):
- PlatiScadente: onIncaseazaMultiple, onViewSportiv, permissions, onBack
- GestiuneFacturi: onBack, currentUser, sportivi=filteredData.sportivi, plati=filteredData.plati, setPlati, setTranzactii, tipuriPlati (nefiltrat), familii=filteredData.familii, onViewSportiv, initialSportivId
- FacturiFaraPrezenta: onBack, onViewSportiv

Ancore (numere de linie inainte de editare):
- PlatiScadente.tsx:25-30 `interface PlatiScadenteProps`; :82 semnatura componentei; :839 `<Button onClick={onBack} variant="secondary"><ArrowLeftIcon .../> Meniu</Button>`; :1024-1054 celula Actiuni desktop (dupa butonul WalletIcon "Încasează Rapid" de la 1026-1033); :1158-1189 actiunile cardului mobil.
- GestiuneFacturi.tsx:21-32 `interface GestiuneFacturiProps`; :46 semnatura; :541-561 coloana 'actions' (render); :590-608 actiunile `renderMobileItem`; :614 butonul "Meniu".
- FacturiFaraPrezenta.tsx:15-18 props; :36 semnatura; :160 butonul "Meniu".
- `components/icons.tsx` exporta `ClipboardListIcon` (linia 22) — folosit pentru butonul Detalii (EyeIcon e deja ocupat in GestiuneFacturi de "Vizualizează factura" = modalul printabil FacturaChitantaModal).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Props optionale hideBackButton + onDeschideDetalii pe PlatiScadente, GestiuneFacturi, FacturiFaraPrezenta</name>
  <read_first>
    - components/Plati/PlatiScadente.tsx (liniile 1-30, 82-112, 835-845, 1020-1060, 1155-1195)
    - components/Plati/GestiuneFacturi.tsx (liniile 1-60, 536-615)
    - components/Plati/FacturiFaraPrezenta.tsx (liniile 1-40, 155-165)
    - components/icons.tsx (liniile 1-90 — confirma ClipboardListIcon)
  </read_first>
  <files>components/Plati/PlatiScadente.tsx, components/Plati/GestiuneFacturi.tsx, components/Plati/FacturiFaraPrezenta.tsx</files>
  <action>
PlatiScadente.tsx:
- In `interface PlatiScadenteProps` adauga `hideBackButton?: boolean;` si `onDeschideDetalii?: (plata: Plata) => void;` (cu comentariu de o linie: "Faza 31 — setate de hub-ul Plăți & Facturi"). Adauga-le in destructurarea semnaturii de la linia 82.
- Linia 839: invelste butonul "Meniu" in `{!hideBackButton && (...)}`. Nimic altceva din header nu se schimba.
- Celula Actiuni desktop (1024-1054): imediat dupa blocul `esteDeIncasat(p) && (...WalletIcon...)`, adauga `{onDeschideDetalii && (<Button size="sm" variant="secondary" onClick={() => onDeschideDetalii(p)} title="Detalii & corecție"><ClipboardListIcon className="w-4 h-4" /></Button>)}`.
- Actiunile cardului mobil (1158-1189): acelasi buton dupa blocul WalletIcon, cu `className="w-3.5 h-3.5"` pe icon si `title="Detalii"`.
- Adauga `ClipboardListIcon` la importul existent din `'../icons'` (linia 5).
- NU modifica handleSaveEdit, handleProcessPayment, handleGenerateSubscriptions, selectia multipla sau `handleIncasareClick`.

GestiuneFacturi.tsx:
- In `interface GestiuneFacturiProps` adauga `hideBackButton?: boolean;` si `onDeschideDetalii?: (plata: Plata) => void;`; adauga-le in destructurarea de la linia 46.
- Linia 614: `{!hideBackButton && (<Button onClick={onBack} ...>Meniu</Button>)}`.
- Coloana 'actions' (render, ~541-561): dupa blocul WalletIcon adauga `{onDeschideDetalii && (<Button size="sm" variant="secondary" onClick={() => onDeschideDetalii(p)} title="Detalii & corecție"><ClipboardListIcon className="w-4 h-4" /></Button>)}`.
- `renderMobileItem` (~590-608): dupa butonul "Încasează" adauga `{onDeschideDetalii && (<Button size="sm" variant="secondary" onClick={() => onDeschideDetalii(p)} className="flex-1 justify-center"><ClipboardListIcon className="w-4 h-4 mr-2" /> Detalii</Button>)}`.
- Adauga `ClipboardListIcon` la importul din `'../icons'` (linia 4).
- NU atinge modificarile facute in 31-02 (merge de stare, refetch dupa RPC).

FacturiFaraPrezenta.tsx:
- Adauga `hideBackButton?: boolean;` in `FacturiFaraPrezentaProps` si in destructurare; linia 160 → `{!hideBackButton && (...)}`.

Toate prop-urile noi sunt optionale; apelurile existente din AppRouter (care nu le transmit) raman valide si comportamentul lor e identic.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && grep -c 'hideBackButton' components/Plati/PlatiScadente.tsx components/Plati/GestiuneFacturi.tsx components/Plati/FacturiFaraPrezenta.tsx && grep -c 'onDeschideDetalii' components/Plati/PlatiScadente.tsx components/Plati/GestiuneFacturi.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - Fiecare din cele 3 fisiere contine `hideBackButton?: boolean` in interfata de props si `!hideBackButton &&` langa butonul de intoarcere.
    - `grep -c "onDeschideDetalii" components/Plati/PlatiScadente.tsx` >= 4 (interfata, destructurare, desktop, mobil); idem `components/Plati/GestiuneFacturi.tsx` >= 4.
    - `grep -c "onDeschideDetalii?: (plata: Plata) => void" components/Plati/PlatiScadente.tsx components/Plati/GestiuneFacturi.tsx` → 1 in fiecare.
    - `grep -c "ClipboardListIcon" components/Plati/PlatiScadente.tsx` >= 3 (import + 2 utilizari); idem GestiuneFacturi.
    - `git diff -- components/Plati/PlatiScadente.tsx` nu atinge liniile functiilor `handleSaveEdit`, `handleProcessPayment`, `handleGenerateSubscriptions`, `handleIncasareClick` (verificare vizuala a diff-ului: doar interfata, destructurarea, header si celulele de actiuni).
  </acceptance_criteria>
  <done>Cele 3 componente accepta montarea in hub fara buton dublu de intoarcere si expun deschiderea detaliului factura, cu API vechi 100% compatibil.</done>
</task>

<task type="auto">
  <name>Task 2: Componenta TabFacturi cu ecranul de detaliu integrat</name>
  <read_first>
    - components/Plati/hub/platiHubConfig.ts (antetul "Harta props verificata" + PlatiHubTabProps)
    - components/Plati/FacturaDetaliu.tsx (props plataId/onClose)
    - components/LazyComponents.tsx (exporturile PlatiScadente, GestiuneFacturi, FacturiFaraPrezenta)
    - components/AppRouter.tsx (liniile 228-241 — props-urile exacte transmise azi)
    - contexts/DataContext.tsx
  </read_first>
  <files>components/Plati/hub/TabFacturi.tsx</files>
  <action>
Creeaza `components/Plati/hub/TabFacturi.tsx`:

- Importuri: `React, { useState }`; `import type { Plata } from '../../../types';`; `import { useData } from '../../../contexts/DataContext';`; `import * as Lazy from '../../LazyComponents';`; `import { FacturaDetaliu } from '../FacturaDetaliu';`; `import type { PlatiHubTabProps } from './platiHubConfig';`.
- `export interface TabFacturiProps extends PlatiHubTabProps { onIncaseazaMultiple: (plati: Plata[]) => void; initialSportivId?: string; }`.
- `export const TabFacturi: React.FC<TabFacturiProps> = ({ sectiune, currentUser, permissions, onViewSportiv, onBack, onIncaseazaMultiple, initialSportivId }) => { ... }`.
- `const { filteredData, setPlati, setTranzactii, tipuriPlati } = useData();` (tipuriPlati NEFILTRAT, exact ca AppRouter.tsx:231).
- `const [plataDetaliuId, setPlataDetaliuId] = useState<string | null>(null);` si `const deschideDetalii = (p: Plata) => setPlataDetaliuId(p.id);`.
- Randare (fara Suspense propriu — boundary-ul e in PlatiHub, planul 31-08), un fragment cu:
  - `sectiune === 'plati-scadente'` → `<Lazy.PlatiScadente onIncaseazaMultiple={onIncaseazaMultiple} onViewSportiv={onViewSportiv} permissions={permissions} onBack={onBack} hideBackButton onDeschideDetalii={deschideDetalii} />`
  - `sectiune === 'gestiune-facturi'` → `<Lazy.GestiuneFacturi onBack={onBack} currentUser={currentUser} sportivi={filteredData.sportivi} plati={filteredData.plati} setPlati={setPlati} setTranzactii={setTranzactii} tipuriPlati={tipuriPlati} familii={filteredData.familii} onViewSportiv={onViewSportiv} initialSportivId={initialSportivId} hideBackButton onDeschideDetalii={deschideDetalii} />`
  - `sectiune === 'facturi-fara-prezenta'` → `<Lazy.FacturiFaraPrezenta onBack={onBack} onViewSportiv={onViewSportiv} hideBackButton />`
  - mereu la final: `<FacturaDetaliu plataId={plataDetaliuId} onClose={() => setPlataDetaliuId(null)} />` (FacturaDetaliu intoarce null cand plataId e null).
- Comentariu-antet de 3-4 randuri: tab Facturi (D-01), props identice cu AppRouter (harta 31-01), detaliul factura (D-08) deschis din PlatiScadente si GestiuneFacturi.

Nota: `onBack` pentru GestiuneFacturi era `canGoBack ? goBack : handleBackToDashboard` in AppRouter; in hub butonul propriu e ascuns (`hideBackButton`), iar intoarcerea o face butonul unic al hub-ului — de aceea se transmite `onBack`-ul hub-ului doar pentru compatibilitatea tipului.
  </action>
  <verify>
    <automated>bash -lc "cd /c/Users/lungu/portal-phihau && npm run lint && grep -c '<FacturaDetaliu' components/Plati/hub/TabFacturi.tsx && grep -c 'Lazy.PlatiScadente\|Lazy.GestiuneFacturi\|Lazy.FacturiFaraPrezenta' components/Plati/hub/TabFacturi.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` iese cu cod 0.
    - `components/Plati/hub/TabFacturi.tsx` exporta `TabFacturi` si `TabFacturiProps`; `TabFacturiProps` extinde `PlatiHubTabProps`.
    - Contine exact cele 3 ramuri `sectiune === 'plati-scadente'`, `sectiune === 'gestiune-facturi'`, `sectiune === 'facturi-fara-prezenta'`.
    - Fiecare din cele 3 componente e montata cu `hideBackButton`; PlatiScadente si GestiuneFacturi cu `onDeschideDetalii={deschideDetalii}`.
    - GestiuneFacturi primeste `tipuriPlati={tipuriPlati}` (din useData, nefiltrat) si `initialSportivId={initialSportivId}`.
    - Contine `<FacturaDetaliu plataId={plataDetaliuId}`.
    - Nu contine interogari Supabase (`grep -c "supabase" components/Plati/hub/TabFacturi.tsx` = 0).
  </acceptance_criteria>
  <done>Tab-ul Facturi exista, compileaza, monteaza cele 3 componente cu props identice celor de azi si deschide ecranul de detaliu factura.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hub (guard canManageFinances) → componente de facturare cu actiuni distructive (stergere, anulare, generare) | Montarea in alt context nu trebuie sa extinda cine le vede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-01 | Elevation of Privilege | TabFacturi expune PlatiScadente/GestiuneFacturi (stergere/anulare facturi) | mitigate | Tab-ul e montat doar de PlatiHub, al carui guard in AppRouter ramane `canManageFinances` (identic cu guard-ul vechi al celor 3 vederi) — verificat in 31-08; INSTRUCTOR ajunge la detaliu doar prin profil (31-07), fara acces la aceste liste |
| T-31-04-02 | Tampering | Butonul Detalii transmite obiectul rand (cu campuri calculate) | mitigate | TabFacturi retine DOAR `p.id`; FacturaDetaliu reciteste randul din stare si scrie prin lista alba (31-03) |
| T-31-04-03 | Denial of Service | Regresie a API-ului componentelor folosite si in alte locuri | mitigate | Doar props optionale; lint verifica apelurile existente din AppRouter |
| T-31-04-SC | Tampering | npm installs | accept | Zero pachete noi |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Inventarul complet al fazei e in `31-01-PLAN.md` (sectiunea omonima). Acest plan (31-04) produce:
- `components/Plati/hub/TabFacturi.tsx` → `TabFacturi`, `TabFacturiProps`.
- Props optionale noi: `hideBackButton?: boolean` pe `PlatiScadente`, `GestiuneFacturi`, `FacturiFaraPrezenta`; `onDeschideDetalii?: (plata: Plata) => void` pe `PlatiScadente` si `GestiuneFacturi`.
</artifacts_this_phase_produces>

<verification>
1. `npm run lint` — cod 0.
2. Gardele grep din acceptance.
3. AppRouter inca monteaza cele 3 componente pe rutele vechi fara erori de tip (rutarea se schimba abia in 31-08).
</verification>

<success_criteria>
- D-01: tab-ul Facturi grupeaza cele 3 ecrane cu props identice.
- D-08: detaliul factura se deschide din ambele liste de facturi (desktop + mobil).
- Zero schimbari de comportament pe rutele existente pana la 31-08.
</success_criteria>

<output>
Creeaza `.planning/phases/31-hub-plati-si-facturi-consolidare-14-view-uri-in-4-tab-uri-fa/31-04-SUMMARY.md`.
</output>
