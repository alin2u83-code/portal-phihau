---
phase: 02-navigare-grupe-drill-down
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Grupe/GrupaDetailView.tsx
autonomous: true
requirements: [NAV-02, NAV-03]
must_haves:
  truths:
    - "GrupaDetailView afișează un tab bar cu Antrenamente | Orar | Sportivi, tab implicit Antrenamente"
    - "Tab Orar permite editarea programului săptămânal și salvarea în orar_saptamanal cu toast de succes"
    - "Tab Sportivi afișează lista read-only a sportivilor activi din grupă + buton Adaugă Sportivi"
    - "Tab Antrenamente afișează un placeholder dashed-border (fără calendar)"
  artifacts:
    - path: "components/Grupe/GrupaDetailView.tsx"
      provides: "Drill-down view cu 3 tab-uri (Antrenamente placeholder, Orar inline, Sportivi read-only)"
      exports: ["GrupaDetailView"]
      min_lines: 180
  key_links:
    - from: "components/Grupe/GrupaDetailView.tsx"
      to: "orar_saptamanal"
      via: "supabase.from('orar_saptamanal').delete()/insert() în handleSave Tab Orar"
      pattern: "orar_saptamanal"
    - from: "components/Grupe/GrupaDetailView.tsx"
      to: "sportivi"
      via: "useQuery supabase.from('sportivi') filtrat pe grupa_id în Tab Sportivi"
      pattern: "sportivi-grupa"
---

<objective>
Creează componenta nouă `components/Grupe/GrupaDetailView.tsx` — view-ul drill-down cu tab bar (Antrenamente | Orar | Sportivi) și conținutul fiecărui tab. Acesta este nucleul Phase 2: tot ce vede utilizatorul după ce apasă "Detalii" pe un card de grupă.

Purpose: Concentrează gestionarea unei grupe (orar, sportivi) într-un singur view coerent, eliminând nevoia de modale separate Orar+Sportivi (NAV-02, NAV-03). Tab Antrenamente este placeholder pregătit pentru calendarul din Phase 3 (D-10).
Output: Fișier nou `components/Grupe/GrupaDetailView.tsx` care exportă `GrupaDetailView`. Componenta este standalone și încă neconectată — Plan 02 o montează în `Grupe/index.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigare-grupe-drill-down/02-CONTEXT.md
@.planning/phases/02-navigare-grupe-drill-down/02-RESEARCH.md
@.planning/phases/02-navigare-grupe-drill-down/02-PATTERNS.md
@.planning/phases/02-navigare-grupe-drill-down/02-UI-SPEC.md
@components/Grupe/OrarEditorModal.tsx
@components/Grupe/GrupaCard.tsx
</context>

## Artifacts this phase produces

Symbols created by Plan 01 (treat as new, not drift):
- `GrupaDetailView` — exported React FC în `components/Grupe/GrupaDetailView.tsx`
- `TabAntrenamente` — componentă internă (placeholder)
- `TabOrar` — componentă/secțiune internă (logică copiată din OrarEditorModal)
- `TabSportivi` — componentă internă (listă read-only + buton)
- `TabId` — type alias local `'antrenamente' | 'orar' | 'sportivi'`
- `GrupaWithDetails` — interfață locală (copiată din GrupaCard.tsx liniile 9-12)
- Props `GrupaDetailViewProps`: `grupa`, `onBack`, `onOpenAdaugaSportivi`
- queryKey nou: `['sportivi-grupa', grupa.id]`

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schelet GrupaDetailView + tab bar + Tab Antrenamente placeholder</name>
  <files>components/Grupe/GrupaDetailView.tsx</files>
  <read_first>
    - components/Grupe/GrupaCard.tsx (liniile 9-12: definiția `GrupaWithDetails` de copiat; liniile 1-7: pattern import)
    - components/Grupe/OrarEditorModal.tsx (pattern import, structura componentei)
    - components/icons.tsx (confirmă: `ArrowLeftIcon`, `PlusIcon`, `TrashIcon`, `CheckCircleIcon`, `CogIcon` exportate)
    - .planning/phases/02-navigare-grupe-drill-down/02-UI-SPEC.md (Layout Contract liniile 168-188; Copywriting liniile 192-213)
    - .planning/phases/02-navigare-grupe-drill-down/02-PATTERNS.md (tab bar pattern liniile 49-67; placeholder liniile 188-201)
  </read_first>
  <action>
    Creează fișierul nou `components/Grupe/GrupaDetailView.tsx`. Importă din `react`: `React, { useState, useMemo }`; din `@tanstack/react-query`: `useQuery, useQueryClient`; din `../../types`: `Grupa as GrupaType, ProgramItem`; din `../ui`: `Button, Card, Input`; din `../icons`: `ArrowLeftIcon, PlusIcon, TrashIcon, CheckCircleIcon`; din `../../supabaseClient`: `supabase`; din `../ErrorProvider`: `useError`; din `../../utils/cache`: `clearCache`.

    Definește interfața locală `GrupaWithDetails extends GrupaType { sportivi: { count: number }[]; program: ProgramItem[]; }` (copiată din GrupaCard.tsx liniile 9-12). Definește `type TabId = 'antrenamente' | 'orar' | 'sportivi'`. Definește `interface GrupaDetailViewProps { grupa: GrupaWithDetails; onBack: () => void; onOpenAdaugaSportivi: (g: GrupaWithDetails) => void; }`.

    Exportă `GrupaDetailView: React.FC<GrupaDetailViewProps>`. State: `const [activeTab, setActiveTab] = useState<TabId>('antrenamente')` (default Antrenamente per D-09). Calculează `const sportiviCount = grupa.sportivi?.[0]?.count ?? 0`.

    Structura JSX (per UI-SPEC Layout Contract liniile 168-188):
    1. Header row: `flex items-center justify-between py-4 border-b border-slate-700` cu un `Button variant="secondary" size="sm" onClick={onBack}` conținând `<ArrowLeftIcon className="w-4 h-4 mr-1.5" />` + textul "Înapoi la Grupe", urmat de `<h1 className="text-xl font-bold text-white">{grupa.denumire}</h1>` și subtitlu `<p className="text-sm text-slate-400">Sala: {grupa.sala || 'Nespecificată'} · {sportiviCount} sportivi activi</p>`.
    2. Tab bar: `<div className="flex border-b border-slate-700 gap-1">` cu `(['antrenamente','orar','sportivi'] as TabId[]).map(tab => <button>)`. Fiecare buton: `onClick={() => setActiveTab(tab)}`, className condiționat — activ: `border-indigo-500 text-white`, inactiv: `border-transparent text-slate-400 hover:text-slate-200`, base: `h-10 px-4 text-sm font-semibold transition-colors border-b-2 -mb-px`. Label: `tab === 'antrenamente' ? 'Antrenamente' : tab === 'orar' ? 'Orar' : 'Sportivi'`.
    3. Tab content: `<div className="py-6 overflow-y-auto">` care randează condiționat `{activeTab === 'antrenamente' && <TabAntrenamente />}`, `{activeTab === 'orar' && <TabOrar grupa={grupa} />}`, `{activeTab === 'sportivi' && <TabSportivi grupa={grupa} onOpenAdaugaSportivi={onOpenAdaugaSportivi} />}`.

    Definește componenta internă `TabAntrenamente: React.FC` ca placeholder (D-10): un container centrat `flex items-center justify-center py-12` cu un card `border-dashed border-2 border-slate-700 rounded-xl py-12 px-8 text-center max-w-md` conținând `<h3>` cu textul exact "Calendar antrenamente — disponibil în curând" și un `<p className="text-sm text-slate-400">` cu textul exact din UI-SPEC linia 203: "Gestionarea completă a antrenamentelor va fi disponibilă în faza următoare. Folosește tab-ul Orar pentru a configura programul recurent." (corectează typo-ul "Folossește" din UI-SPEC la "Folosește").

    Lasă `TabOrar` și `TabSportivi` ca stub-uri minimale temporare (return un div gol) — vor fi completate în Task 2 și Task 3. NU include fenced code în acest fișier; scrie cod TypeScript real.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Fișierul `components/Grupe/GrupaDetailView.tsx` există și exportă `GrupaDetailView`.
    - `npm run lint` (tsc --noEmit) trece fără erori noi.
    - Sursa conține `useState<TabId>('antrenamente')` (default tab Antrenamente).
    - Sursa conține string-ul exact "Calendar antrenamente — disponibil în curând".
    - Sursa conține string-ul "Înapoi la Grupe".
    - Tab bar mapează exact `['antrenamente','orar','sportivi']`.
  </acceptance_criteria>
  <done>GrupaDetailView se compilează cu tab bar funcțional (3 tab-uri, default Antrenamente) și placeholder-ul Tab Antrenamente complet. TabOrar/TabSportivi sunt stub-uri.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tab Orar inline (logică copiată din OrarEditorModal, fără wrapper Modal)</name>
  <files>components/Grupe/GrupaDetailView.tsx</files>
  <read_first>
    - components/Grupe/OrarEditorModal.tsx (TOATĂ logica — liniile 22-173 sunt sursa de copiat)
    - components/Grupe/GrupaDetailView.tsx (fișierul curent din Task 1)
    - .planning/phases/02-navigare-grupe-drill-down/02-PATTERNS.md (Tab Orar adaptări liniile 69-168; Common Pitfalls 2 și 3 în 02-RESEARCH.md liniile 327-339)
    - .planning/phases/02-navigare-grupe-drill-down/02-UI-SPEC.md (Tab Orar liniile 148-156; States liniile 238-245)
  </read_first>
  <action>
    Înlocuiește stub-ul `TabOrar` cu o componentă completă `const TabOrar: React.FC<{ grupa: GrupaWithDetails }> = ({ grupa }) => {...}`. Copiază logica din `OrarEditorModal.tsx` liniile 22-173 cu următoarele 6 adaptări exacte (per D-02, D-03, D-04 și Common Pitfalls):

    1. NU props `isOpen`/`onClose` — componenta primește doar `grupa`. NU wrapper `<Modal>` — randează direct conținutul (`<div className="space-y-6">`).
    2. State identic cu original: `const [program, setProgram] = useState<ProgramItem[]>(grupa.program || [])`, `const [loading, setLoading] = useState(false)`, `const { showError, showSuccess } = useError()`, `const queryClient = useQueryClient()`, și array-ul `zileSaptamana: ProgramItem['ziua'][] = ['Luni','Marți','Miercuri','Joi','Vineri','Sâmbătă','Duminică']`.
    3. `useEffect` reset: `React.useEffect(() => { setProgram(grupa.program || []); }, [grupa.id])` — dependency pe `grupa.id` (string), NU pe `grupa` (obiect). Acesta este Pitfall 2 din RESEARCH — un dependency pe obiect ar reseta la fiecare re-render.
    4. `handleSave`: copiază exact din original liniile 36-64, dar ELIMINĂ apelul `setGrupe(...)` (liniile 49-51 din original — Tab Orar nu primește prop `setGrupe`) și ELIMINĂ `onClose()` (linia 58). Păstrează ORDINEA: mai întâi `Object.keys(localStorage).filter(k => k.startsWith('cache_grupe_')).forEach(k => clearCache(k))`, APOI `await queryClient.invalidateQueries({ queryKey: ['grupe'] })`, apoi `showSuccess('Succes', 'Orarul a fost salvat.')`. Aceasta este Pitfall 3 — clearCache trebuie ÎNAINTE de invalidateQueries.
    5. Handlerele `handleAddItem`, `handleRemoveItem`, `handleItemChange` și memo-ul `programByDay`: copiază identic din original liniile 66-78.
    6. JSX-ul grilei pe zile: copiază din original liniile 86-160, dar ELIMINĂ `max-h-[50vh] sm:max-h-[60vh] overflow-y-auto pr-1` de pe containerul de scroll (rămâne doar `space-y-6`). Butoanele de acțiune (din original liniile 162-169): înlocuiește butonul "Anulează" cu `<Button variant="secondary" onClick={() => setProgram(grupa.program || [])} disabled={loading} className="w-full sm:w-auto touch-manipulation">Resetează</Button>` (D-03: Undo local, NU navighează). Păstrează butonul "Salvează Orar" cu `variant="success"`, `isLoading={loading}` și iconul `<CheckCircleIcon className="w-4 h-4 mr-2" />`.

    Header-ul descriptiv din original (liniile 87-90, cu CogIcon + "Definește șablonul recurent...") poate fi păstrat sub tab bar — opțional. NU modifica `OrarEditorModal.tsx` (D-02: rămâne neschimbat).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fără erori noi.
    - `TabOrar` conține `supabase.from('orar_saptamanal').delete().eq('grupa_id', grupa.id)` și `.insert(`.
    - Sursa conține `useEffect(() => { setProgram(grupa.program || []); }, [grupa.id])` cu dependency `[grupa.id]` (NU `[grupa]` sau `[isOpen, grupa]`).
    - `handleSave` din `TabOrar` NU conține apel `setGrupe(` și NU conține `onClose(`.
    - `handleSave` rulează `clearCache` (filter `cache_grupe_`) ÎNAINTE de `queryClient.invalidateQueries({ queryKey: ['grupe'] })`.
    - Butonul de reset poartă label-ul "Resetează" și onClick `setProgram(grupa.program || [])`.
    - Sursa NU conține `max-h-[50vh]` în TabOrar.
    - `components/Grupe/OrarEditorModal.tsx` rămâne neschimbat (zero diff).
  </acceptance_criteria>
  <done>Tab Orar editează intervale, salvează în orar_saptamanal cu invalidare cache corectă (clearCache → invalidateQueries) și toast succes, rămâne pe tab după save (D-04). Butonul Resetează face Undo local (D-03).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Tab Sportivi (query read-only per grupă + buton Adaugă Sportivi)</name>
  <files>components/Grupe/GrupaDetailView.tsx</files>
  <read_first>
    - components/Grupe/GrupaDetailView.tsx (fișierul curent din Task 1+2)
    - .planning/phases/02-navigare-grupe-drill-down/02-PATTERNS.md (Tab Sportivi query liniile 170-184; TabSportivi UI 02-RESEARCH.md liniile 472-531)
    - .planning/phases/02-navigare-grupe-drill-down/02-UI-SPEC.md (Tab Sportivi liniile 157-163; Copywriting empty state liniile 204-205; States liniile 248-254)
    - .planning/phases/02-navigare-grupe-drill-down/02-RESEARCH.md (Open Question 2 liniile 601-604: join la grade pentru denumire)
  </read_first>
  <action>
    Înlocuiește stub-ul `TabSportivi` cu `const TabSportivi: React.FC<{ grupa: GrupaWithDetails; onOpenAdaugaSportivi: (g: GrupaWithDetails) => void }> = ({ grupa, onOpenAdaugaSportivi }) => {...}`.

    Query (D-05, read-only): `const { data: sportivi = [], isLoading, error } = useQuery({ queryKey: ['sportivi-grupa', grupa.id], queryFn: async () => { const { data, error } = await supabase.from('sportivi').select('id, nume, prenume, grad_actual_id, grade:grad_actual_id(denumire)').eq('grupa_id', grupa.id).eq('status', 'Activ'); if (error) throw error; return data ?? []; }, staleTime: 5 * 60 * 1000 })`. Include join-ul `grade:grad_actual_id(denumire)` per Open Question 2 din RESEARCH — necesar pentru afișarea denumirii gradului.

    States (per UI-SPEC liniile 248-254):
    - Loading: `if (isLoading) return <div className="text-center py-8 text-slate-400">Se încarcă...</div>`.
    - Error: `if (error) return <div className="text-center py-8 text-rose-400">Nu s-au putut încărca datele. Verifică conexiunea și încearcă din nou.</div>`.
    - Populated/Empty: header row cu `<h3>` arătând `{sportivi.length} sportivi activi` (uppercase, `text-slate-500 tracking-wide`) și un `<Button variant="info" size="sm" onClick={() => onOpenAdaugaSportivi(grupa)} className="min-h-[40px] touch-manipulation">Adaugă Sportivi</Button>`.
    - Empty (sportivi.length === 0): `<Card className="text-center py-8">` cu textele exacte "Niciun sportiv în această grupă" și "Adaugă primul sportiv folosind butonul de mai jos."
    - Lista: `sportivi.map(s => <div ... className="flex items-center gap-3 py-2 px-3 rounded-lg bg-slate-800/30 border border-slate-700/50">)` afișând `{(s.prenume || '')} {(s.nume || '')}` în `text-sm text-slate-300` și denumirea gradului din `s.grade?.denumire` (read-only, D-06 — NICIO acțiune de scoatere per sportiv).

    Butonul "Adaugă Sportivi" apelează DOAR `onOpenAdaugaSportivi(grupa)` — NU randează `AdaugaSportiviModal` aici (Pitfall 4 din RESEARCH: modalul rămâne montat o singură dată în `Grupe/index.tsx`, montat în Plan 02).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece fără erori noi.
    - `TabSportivi` folosește `useQuery` cu `queryKey: ['sportivi-grupa', grupa.id]` și `staleTime: 5 * 60 * 1000`.
    - Query-ul conține `.eq('grupa_id', grupa.id)` și `.eq('status', 'Activ')` și join-ul `grade:grad_actual_id(denumire)`.
    - Există ramuri pentru `isLoading` ("Se încarcă...") și `error` ("Nu s-au putut încărca datele...").
    - Empty state conține string-ul exact "Niciun sportiv în această grupă".
    - Butonul "Adaugă Sportivi" apelează `onOpenAdaugaSportivi(grupa)` și NU există import sau randare `AdaugaSportiviModal` în acest fișier.
    - Lista nu conține niciun buton/acțiune de scoatere per sportiv (D-06 read-only).
  </acceptance_criteria>
  <done>Tab Sportivi afișează lista read-only (loading/error/empty/populated) a sportivilor activi din grupă cu denumirea gradului, și butonul Adaugă Sportivi deleagă deschiderea modalului către parent. GrupaDetailView este complet și se compilează.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (orar_saptamanal) | Tab Orar trimite delete+insert pe orar_saptamanal pentru grupa.id |
| client → Supabase (sportivi) | Tab Sportivi citește lista sportivilor pentru grupa.id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | TabOrar handleSave (orar_saptamanal) | accept | RLS Supabase pe `orar_saptamanal` filtrează pe `club_id` via header `active-role-context-id` injectat de supabaseClient; nicio logică nouă de autorizare în acest plan |
| T-02-02 | Elevation of Privilege | TabSportivi query (sportivi) | accept | RLS Supabase pe `sportivi` filtrează pe `club_id` via `auth.uid()`; query frontend doar citește, nu poate ocoli RLS |
| T-02-03 | Tampering | Input-uri timp ora_start/ora_sfarsit | mitigate | `type="time"` HTML native (din componenta Input ui.tsx) validează format HH:MM; coloana TIME din Supabase validează la insert |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Nicio instalare de pachete în acest plan — toate dependențele deja în package.json (confirmat în RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run lint` (tsc --noEmit) trece fără erori.
- `components/Grupe/GrupaDetailView.tsx` exportă `GrupaDetailView` cu cele 3 tab-uri complete.
- `components/Grupe/OrarEditorModal.tsx` rămâne neschimbat (zero diff vs. HEAD).
</verification>

<success_criteria>
- GrupaDetailView se compilează și exportă componenta cu tab bar (default Antrenamente), Tab Orar funcțional (save → orar_saptamanal + cache invalidation + toast), Tab Sportivi read-only cu buton Adaugă Sportivi, Tab Antrenamente placeholder.
- Zero modificări la OrarEditorModal.tsx și AdaugaSportiviModal.tsx (D-02, D-05).
</success_criteria>

<output>
Create `.planning/phases/02-navigare-grupe-drill-down/02-01-SUMMARY.md` when done
</output>
