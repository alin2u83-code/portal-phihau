// Logo-ul clubului Phi Hau Iași în format PNG, encodat în Base64.
// Această metodă asigură că logo-ul este mereu disponibil ca parte a codului,
// eliminând dependența de fișiere externe care se pot pierde în procesul de build.
export const logoBase64 = '';