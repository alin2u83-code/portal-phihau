---
phase: 14-corelare-prezente-facturi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migrations/add_data_start_facturare.sql
  - hooks/usePrezenteLuna.ts
  - hooks/useDataStartFacturare.ts
  - utils/luniLipsa.ts
  - services/facturaService.ts
autonomous: false
requirements: [PLF-01, PLF-02, PLF-03, PLF-05]
user_setup:
  - service: supabase
    why: "Migrația ALTER TABLE pe sportivi trebuie aplicată în baza de date remote înainte ca PLF-03/PLF-05 să funcționeze"
    dashboard_config:
      - task: "Rulează conținutul sql/migrations/add_data_start_facturare.sql în Supabase SQL Editor (sau via MCP apply_migration)"
        location: "Supabase Dashboard -> SQL Editor"

must_haves:
  truths:
    - "Coloana data_start_facturare există pe tabelul sportivi (DATE, nullable)"
    - "usePrezenteLuna(sportivId, luna, an) returnează lista datelor de prezență pentru luna respectivă"
    - "calculeazaLuniLipsa(dataStart, plati) returnează lunile fără factură Abonament între dataStart și azi"
    - "genereazaFacturaAbonament(sportivId, luna, an) inserează o factură Abonament în plati fără să modifice calculul soldului"
  artifacts:
    - path: "sql/migrations/add_data_start_facturare.sql"
      provides: "Migrație ALTER TABLE sportivi ADD COLUMN data_start_facturare"
      contains: "data_start_facturare"
    - path: "hooks/usePrezenteLuna.ts"
      provides: "React Query hook prezențe per (sportiv, lună, an)"
      exports: ["usePrezenteLuna"]
    - path: "hooks/useDataStartFacturare.ts"
      provides: "Hook citire/scriere data_start_facturare per sportiv (evită modificarea types.ts)"
      exports: ["useDataStartFacturare"]
    - path: "utils/luniLipsa.ts"
      provides: "Funcție pură calcul luni lipsă"
      exports: ["calculeazaLuniLipsa", "formatLuna"]
    - path: "services/facturaService.ts"
      provides: "Serviciu generare factură Abonament reutilizabil (manual + bulk)"
      exports: ["genereazaFacturaAbonament", "facturaAbonamentExista"]
  key_links:
    - from: "hooks/usePrezenteLuna.ts"
      to: "vedere_prezenta_sportiv"
      via: "supabase.from('vedere_prezenta_sportiv')"
      pattern: "vedere_prezenta_sportiv"
    - from: "services/facturaService.ts"
      to: "plati"
      via: "supabase insert/select pe plati"
      pattern: "from\\('plati'\\)"
---

<objective>
Fundația fazei 14: o singură migrație DB (câmpul `data_start_facturare` pe `sportivi`) plus patru artefacte de cod reutilizabile pe care le consumă planurile 14-02/14-03/14-04. Niciun artefact din acest plan nu modifică `types.ts`, `ui.tsx`, `DataContext`, calculul soldului sau generarea automată existentă.

Purpose: Centralizează logica partajată (query prezențe, calcul luni lipsă, generare factură) într-un singur loc pentru a evita duplicarea în cele trei planuri consumatoare și a respecta interface-first ordering.
Output: 1 fișier SQL + 4 fișiere TypeScript noi (1 hook prezențe, 1 hook data_start_facturare, 1 util pur, 1 serviciu factură).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-corelare-prezente-facturi/14-CONTEXT.md
@.planning/phases/14-corelare-prezente-facturi/14-RESEARCH.md
@CLAUDE.md
@hooks/useAttendanceData.ts
@hooks/usePlati.ts
@components/Prezenta/IstoricPrezentaGlobal.tsx
@sql/fixes/FIX_VIEWS_AND_RLS.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrație data_start_facturare + hook citire/scriere</name>
  <read_first>
    - sql/fixes/FIX_VIEWS_AND_RLS.sql (pattern migrație + GRANT)
    - hooks/usePlati.ts (pattern useQuery simplu pe Supabase)
    - CLAUDE.md (convenții: română domeniu, engleză tehnic; tipuri în types.ts care NU se modifică)
    - types.ts liniile 77-102 (interfața Sportiv — NU se modifică, câmpul nou se accesează separat)
  </read_first>
  <action>
    Creează `sql/migrations/add_data_start_facturare.sql` cu trei instrucțiuni idempotente: (1) `ALTER TABLE public.sportivi ADD COLUMN IF NOT EXISTS data_start_facturare DATE;` (2) `CREATE INDEX IF NOT EXISTS idx_sportivi_data_start_facturare ON public.sportivi (data_start_facturare) WHERE data_start_facturare IS NOT NULL;` (3) un comentariu care explică că politicile RLS existente pe `sportivi` acoperă deja UPDATE pentru ADMIN_CLUB, deci nu se adaugă politici noi. Câmpul rămâne NULL pentru toți sportivii existenți (NULL = fără calcul luni lipsă, per decizia LOCKED din CONTEXT.md).

    Creează `hooks/useDataStartFacturare.ts` care expune `useDataStartFacturare`. Acesta evită modificarea `types.ts` (constraint LOCKED) prin fetch separat: o funcție query `useQuery` cu queryKey `['data-start-facturare', sportivId]`, enabled pe `!!sportivId`, staleTime 5 minute, care face `supabase.from('sportivi').select('data_start_facturare').eq('id', sportivId).maybeSingle()` și returnează `data?.data_start_facturare ?? null`. Expune și un mutation `useMutation` `setDataStartFacturare` care face `supabase.from('sportivi').update({ data_start_facturare: value }).eq('id', sportivId)`, cu `onSuccess` care invalidează queryKey-ul. Valoarea `value` este string ISO `YYYY-MM-DD` sau `null`. Importă `useQuery`, `useMutation`, `useQueryClient` din `@tanstack/react-query` și `supabase` din `../supabaseClient`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "useDataStartFacturare|add_data_start" || echo "no type errors in new files"</automated>
  </verify>
  <acceptance_criteria>
    - `sql/migrations/add_data_start_facturare.sql` conține `ADD COLUMN IF NOT EXISTS data_start_facturare DATE` și `CREATE INDEX IF NOT EXISTS idx_sportivi_data_start_facturare`
    - `hooks/useDataStartFacturare.ts` exportă `useDataStartFacturare` cu query + mutation; nu importă din `types.ts` câmpul nou; nu modifică `types.ts`
    - `npx tsc --noEmit` nu raportează erori noi în fișierele create
  </acceptance_criteria>
  <done>Migrația SQL există și e idempotentă; hook-ul de citire/scriere data_start_facturare compilează fără a atinge types.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: usePrezenteLuna + util calcul luni lipsă</name>
  <read_first>
    - components/Prezenta/IstoricPrezentaGlobal.tsx liniile 26-45 și 143-149 (view vedere_prezenta_sportiv: coloane data, sportiv_id, club_id, nume_grupa, status string interpretat ca 'prezent')
    - sql/fixes/FIX_VIEWS_AND_RLS.sql liniile 23-37 (definiția view-ului vedere_prezenta_sportiv)
    - hooks/usePlati.ts (pattern useQuery)
    - types.ts liniile 153-175 (interfața Plata: are luna, an, tip, data, status)
  </read_first>
  <behavior>
    - calculeazaLuniLipsa: dacă dataStart e null/undefined → returnează []
    - calculeazaLuniLipsa: pentru dataStart='2026-01-01' și plati cu Abonament luna=1/an=2026 și luna=3/an=2026, azi=iunie 2026 → returnează lunile 2,4,5,6/2026 (lipsesc), NU 1 și 3
    - calculeazaLuniLipsa: ignoră plățile cu tip != 'Abonament'
    - calculeazaLuniLipsa: tratează luna 1-indexed (plata.luna===1 înseamnă ianuarie) — fără offset de 1
    - calculeazaLuniLipsa: plăți cu luna/an null sunt ignorate (nu blochează nicio lună)
    - formatLuna(6, 2026) → 'iunie 2026' (toLocaleString ro-RO)
  </behavior>
  <action>
    Creează `utils/luniLipsa.ts` cu funcția pură `calculeazaLuniLipsa(dataStart: string | null, platiSportiv: Plata[]): { luna: number; an: number }[]`. Folosește `date-fns` (deja instalat): `eachMonthOfInterval`, `startOfMonth`, `parseISO`. Logică: dacă `!dataStart` return `[]`; calculează `toateLunile = eachMonthOfInterval({ start: startOfMonth(parseISO(dataStart)), end: new Date() })`; construiește `luniCuFactura = new Set` din plățile cu `tip === 'Abonament' && p.luna && p.an` mapate ca `${p.an}-${p.luna}`; returnează lunile din interval pentru care cheia `${getFullYear()}-${getMonth()+1}` NU e în set, mapate ca `{ luna: getMonth()+1, an: getFullYear() }`. Adaugă și `formatLuna(luna: number, an: number): string` care returnează `new Date(an, luna-1, 1).toLocaleString('ro-RO', { month: 'long', year: 'numeric' })`. Creează test colocat `utils/luniLipsa.test.ts` care acoperă cazurile din behavior (verifică dacă proiectul are vitest configurat; dacă nu, scrie testul ca funcție exportabilă cu asserții simple și documentează în SUMMARY).

    Creează `hooks/usePrezenteLuna.ts` care expune `usePrezenteLuna(sportivId: string | null, luna: number, an: number, enabled = true)`. Folosește `useQuery` cu queryKey `['prezente-luna', sportivId, luna, an]`, `enabled: enabled && !!sportivId`, staleTime 5 minute. queryFn: calculează `primaZi = new Date(an, luna-1, 1).toISOString().split('T')[0]` și `ultimaZi = new Date(an, luna, 0).toISOString().split('T')[0]`; face `supabase.from('vedere_prezenta_sportiv').select('data, status').eq('sportiv_id', sportivId).gte('data', primaZi).lte('data', ultimaZi)`; filtrează rândurile la cele cu `String(row.status ?? '').toLowerCase() === 'prezent'`; returnează lista datelor `row.data` sortate crescător. ASSUMPTION (din 14-RESEARCH A1/A2): view-ul `vedere_prezenta_sportiv` expune coloana `status` ca string ('prezent'/'absent') și `data` ca DATE — confirmat din IstoricPrezentaGlobal.tsx. Dacă la rulare `status` nu e string, documentează discrepanța în SUMMARY și ajustează filtrul.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "luniLipsa|usePrezenteLuna" || echo "no type errors in new files"</automated>
  </verify>
  <acceptance_criteria>
    - `utils/luniLipsa.ts` exportă `calculeazaLuniLipsa` și `formatLuna`; funcția e pură (fără efecte secundare, fără supabase)
    - calculeazaLuniLipsa tratează luna 1-indexed corect și ignoră tip != 'Abonament' (verificat de test)
    - `hooks/usePrezenteLuna.ts` exportă `usePrezenteLuna`, query pe `vedere_prezenta_sportiv`, filtru pe status 'prezent', `enabled` controlează lazy-load
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>Util-ul de luni lipsă e testat și pur; hook-ul de prezențe interoghează view-ul RLS-scoped și filtrează prezenții.</done>
</task>

<task type="auto">
  <name>Task 3: Serviciu generare factură Abonament reutilizabil</name>
  <read_first>
    - components/Plati/PlatiScadente.tsx liniile 73-291 (handleGenerateSubscriptions: structura obiect insert plati, verificare duplicat, creditare sold)
    - components/Plati/GestiuneFacturi.tsx liniile 171-239 (handleAddFactura: structura insert plati existentă)
    - types.ts liniile 153-175 (interfața Plata)
    - CLAUDE.md (servicii: camelCase cu sufix Service, returnează { data, error } unde aplicabil)
  </read_first>
  <action>
    Creează `services/facturaService.ts` cu două funcții. (1) `facturaAbonamentExista(sportivId: string, luna: number, an: number): Promise<boolean>` — face `supabase.from('plati').select('id').eq('tip', 'Abonament').eq('luna', luna).eq('an', an).eq('sportiv_id', sportivId)` și returnează `(data?.length ?? 0) > 0`. (2) `genereazaFacturaAbonament(params: { sportiv: Sportiv; luna: number; an: number; suma: number; descriere?: string; observatii?: string }): Promise<{ data: Plata | null; error: any }>`. Aceasta NU recalculează soldul (anti-pattern interzis din 14-RESEARCH) și NU atinge logica de generare automată. Verifică mai întâi `facturaAbonamentExista`; dacă există → returnează `{ data: null, error: { message: 'Factură existentă pentru luna selectată.' } }`. Altfel construiește obiectul insert după pattern-ul din PlatiScadente liniile 205-211: `sportiv_id: sportiv.id, familie_id: null, luna, an, suma, data: '${an}-${String(luna).padStart(2,'0')}-01', status: 'Neachitat', descriere: descriere ?? 'Abonament ' + formatLuna(luna, an), tip: 'Abonament', observatii: observatii ?? 'Generat manual de admin', club_id: sportiv.club_id`. Fă `supabase.from('plati').insert(newPlata).select().maybeSingle()` și returnează `{ data, error }`. Importă `formatLuna` din `../utils/luniLipsa`, `supabase` din `../supabaseClient`, tipurile `Plata` și `Sportiv` din `../types` (doar citire, nu modificare). Notă: luna e 1-indexed (validare 1-12), an validat 2020-2100 înainte de insert; dacă în afara intervalului → returnează error fără insert (V5 Input Validation din Security Domain).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "facturaService" || echo "no type errors in facturaService"</automated>
  </verify>
  <acceptance_criteria>
    - `services/facturaService.ts` exportă `genereazaFacturaAbonament` și `facturaAbonamentExista`
    - `genereazaFacturaAbonament` blochează duplicatele (verifică existența înainte de insert) și validează luna 1-12, an 2020-2100
    - serviciul NU recalculează soldul și NU modifică generarea automată (insert direct cu status 'Neachitat')
    - serviciul NU modifică types.ts
    - `npx tsc --noEmit` fără erori noi
  </acceptance_criteria>
  <done>Serviciul factură poate fi apelat cu (sportiv, lună, an, sumă) și inserează o factură Abonament fără duplicate, fără a atinge calculul soldului.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Migrația SQL `sql/migrations/add_data_start_facturare.sql` (ALTER TABLE sportivi ADD COLUMN data_start_facturare DATE + index). Aceasta este singura operație DB din fază.</what-built>
  <how-to-verify>
    1. Deschide Supabase Dashboard -> SQL Editor (sau folosește MCP apply_migration cu numele `add_data_start_facturare`).
    2. Rulează conținutul fișierului `sql/migrations/add_data_start_facturare.sql`.
    3. Confirmă în Table Editor -> sportivi că există coloana nouă `data_start_facturare` de tip date, nullable.
    4. Verifică (Assumption A3 din 14-RESEARCH): ca ADMIN_CLUB, un UPDATE pe data_start_facturare reușește (nu 403). Dacă eșuează, semnalează — va fi nevoie de o politică RLS suplimentară.
  </how-to-verify>
  <resume-signal>Scrie "aplicat" după ce coloana există în DB, sau descrie eroarea RLS dacă UPDATE eșuează.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (plati insert) | ADMIN_CLUB generează facturi; input luna/an/sumă netrusted |
| client → Supabase (sportivi update) | ADMIN_CLUB setează data_start_facturare |
| client → Supabase (vedere_prezenta_sportiv) | citire prezențe per sportiv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | genereazaFacturaAbonament insert plati | mitigate | Validare luna 1-12, an 2020-2100 înainte de insert; RLS pe plati verifică club_id |
| T-14-02 | Information Disclosure | usePrezenteLuna pe vedere_prezenta_sportiv | accept | View-ul e RLS-scoped (has_access_to_club) — sportivii din alt club nu sunt vizibili |
| T-14-03 | Tampering | update data_start_facturare pe sportivi | mitigate | RLS existent pe sportivi limitează UPDATE la clubul propriu (verificat în checkpoint) |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Nicio instalare nouă — toate librăriile (date-fns, react-query, supabase-js) deja în package.json (audit RESEARCH: zero pachete noi) |
</threat_model>

<verification>
- `npx tsc --noEmit` rulează fără erori noi introduse de cele 4 fișiere TypeScript
- Migrația SQL este idempotentă (IF NOT EXISTS) și aplicată în DB (checkpoint)
- Niciun fișier din lista LOCKED (types.ts, ui.tsx, DataContext, NavigationContext) nu este modificat: `git diff --name-only` nu include aceste fișiere
</verification>

<success_criteria>
- Coloana data_start_facturare există în DB remote
- usePrezenteLuna, useDataStartFacturare, calculeazaLuniLipsa, genereazaFacturaAbonament sunt exportate și compilează
- Toate consumate corect de planurile 14-02/14-03/14-04 fără refactor suplimentar
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-01)
- `sql/migrations/add_data_start_facturare.sql` — migrație coloană + index
- `hooks/usePrezenteLuna.ts` — hook React Query prezențe per (sportiv, lună, an)
- `hooks/useDataStartFacturare.ts` — hook citire/scriere data_start_facturare (fără atingere types.ts)
- `utils/luniLipsa.ts` (+ test) — calcul pur luni lipsă + formatLuna
- `services/facturaService.ts` — generare factură Abonament reutilizabilă + verificare duplicat
</artifacts_produced>

<output>
Create `.planning/phases/14-corelare-prezente-facturi/14-01-SUMMARY.md` when done
</output>
