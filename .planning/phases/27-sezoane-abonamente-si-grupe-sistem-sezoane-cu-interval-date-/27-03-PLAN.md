---
phase: 27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - components/Grupe/GrupaFormModal.tsx
  - components/Grupe/GrupaCard.tsx
  - components/Grupe/index.tsx
autonomous: true
requirements: [SEZ-04, SEZ-05, SEZ-07]

must_haves:
  truths:
    - "Instructorul sau adminul alege la creare/editare daca grupa e permanenta sau per-sezon (D-04)"
    - "Pentru o grupa per-sezon se alege explicit sezonul, presetat pe sezonul activ al clubului (D-06)"
    - "Grupele existente raman permanente si nu isi schimba comportamentul (D-05)"
    - "O grupa arhivata ramane vizibila in lista, marcata vizual, cu detaliile si istoricul accesibile (D-06)"
    - "Adminul poate dubla manual o grupa arhivata in sezonul activ, iar grupa noua se creeaza fara sportivi asignati (D-07, D-08)"
  artifacts:
    - path: "components/Grupe/GrupaFormModal.tsx"
      provides: "selector tip_grupa + selector conditional de sezon"
      contains: "per_sezon"
    - path: "components/Grupe/GrupaCard.tsx"
      provides: "badge-uri Permanenta/Per Sezon/Arhivata + actiunea de dublare"
      contains: "Dublează în sezon nou"
    - path: "components/Grupe/index.tsx"
      provides: "handler de clonare in sezonul activ, fara copierea sportivilor"
      contains: "tip_grupa: 'per_sezon'"
  key_links:
    - from: "components/Grupe/GrupaFormModal.tsx"
      to: "hooks/useSezoane.ts"
      via: "useSezonActiv pentru lista de sezoane a clubului selectat"
      pattern: "useSezonActiv"
    - from: "components/Grupe/index.tsx"
      to: "public.grupe"
      via: "INSERT grupa clonata cu sezon_id = sezon activ"
      pattern: "from\\('grupe'\\)"
    - from: "components/Grupe/index.tsx"
      to: "public.orar_saptamanal"
      via: "copierea programului saptamanal al grupei clonate"
      pattern: "orar_saptamanal"
---

<objective>
Extinde modulul Grupe cu proprietatea permanent/per-sezon (D-04), vizualizarea starii de arhivare (D-06) si actiunea manuala de dublare a unei grupe arhivate in sezonul activ, fara copierea sportivilor (D-07, D-08).

Purpose: fara aceste comenzi, coloanele `tip_grupa`/`sezon_id`/`arhivat` create in 27-01 raman inaccesibile din interfata, iar arhivarea declansata de 27-02 ar fi invizibila pentru utilizator.
Output: `GrupaFormModal.tsx` cu doua campuri noi, `GrupaCard.tsx` cu badge-uri si actiune de dublare, `Grupe/index.tsx` cu handler-ul de clonare.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-CONTEXT.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-UI-SPEC.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-PATTERNS.md
@.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GrupaFormModal — selector Tip Grupa + selector conditional de Sezon</name>
  <files>components/Grupe/GrupaFormModal.tsx</files>
  <read_first>
    - components/Grupe/GrupaFormModal.tsx integral (198 linii — `formState` la linia 85, `useEffect` de resetare la 98-109, campul conditional `showAddLocatie` la 166-188 ca pattern de show/hide, payload-ul `finalGrupa` la 142-150)
    - hooks/useSezoane.ts (`useSezonActiv` creat in 27-01)
    - types.ts (`Grupa.tip_grupa`, `Grupa.sezon_id`, `interface Sezon`)
    - .planning/phases/27-.../27-UI-SPEC.md sectiunea "2. GrupaFormModal.tsx extension"
  </read_first>
  <action>
1. Import `useSezonActiv` din `../../hooks/useSezoane`. In corpul componentei, dupa calculul existent al clubului, adauga `const clubSelectat = formState.club_id || activeClubId || currentUser.club_id || null;` si `const { sezoane, sezonActiv } = useSezonActiv(clubSelectat);` (hook-ul se re-evalueaza cand adminul de federatie schimba clubul din `Select label="Club"`).

2. Extinde `formState` (linia 85) cu `tip_grupa: 'permanent'` si `sezon_id: ''`. In `useEffect`-ul de resetare (liniile 98-109) initializeaza `tip_grupa: (grupaToEdit?.tip_grupa as 'permanent' | 'per_sezon') || 'permanent'` si `sezon_id: grupaToEdit?.sezon_id || ''`.

3. In `handleChange`, cand `name === 'tip_grupa'` si noua valoare e `'per_sezon'` iar `next.sezon_id` e gol, preseteaza `next.sezon_id = sezonActiv?.id ?? ''`; cand noua valoare e `'permanent'`, seteaza `next.sezon_id = ''`. Cand `name === 'club_id'` reseteaza si `sezon_id` la `''` (pe langa resetarea existenta a locatiei) — sezoanele sunt per club.

4. Randeaza sub blocul de locatie, in acelasi `space-y-4`, un `<div className="space-y-1">` cu: `Select label="Tip Grupă" name="tip_grupa" value={formState.tip_grupa} onChange={handleChange}` avand exact doua optiuni — `<option value="permanent">Permanentă</option>` (prima, implicita) si `<option value="per_sezon">Per Sezon</option>`; apoi, DOAR cand `formState.tip_grupa === 'per_sezon'`, un `Select label="Sezon" name="sezon_id" value={formState.sezon_id} onChange={handleChange} required` populat din `sezoane` (`{s.denumire}{s.activ ? ' (activ)' : ''}`) cu prima optiune `<option value="">Selectează sezon...</option>`; sub ele un `<p className="text-xs text-[var(--t-text-muted)] mt-1">` cu textul exact `Grupele permanente rămân active indiferent de sezon. Grupele per sezon se arhivează automat la trecerea la sezonul următor.` Foloseste acelasi mecanism de show/hide instant ca la `showAddLocatie` (conditional in JSX, fara animatie).

5. In `handleSubmit`, inainte de `onSave`, valideaza: daca `formState.tip_grupa === 'per_sezon'` si `sezoane.length === 0`, apeleaza `showError("Niciun sezon definit", "Clubul nu are niciun sezon. Creează un sezon din meniul Sezoane înainte de a marca grupa ca per-sezon.")` si iese fara salvare; daca `tip_grupa === 'per_sezon'` si `formState.sezon_id` e gol, apeleaza `showError("Validare eșuată", "Selectează sezonul căruia îi aparține grupa.")` si iese. Extinde obiectul `finalGrupa` cu `tip_grupa: formState.tip_grupa` si `sezon_id: formState.tip_grupa === 'per_sezon' ? (formState.sezon_id || null) : null`. NU adauga `arhivat` in payload — flag-ul de arhivare este gestionat exclusiv de fluxul de activare a sezonului (27-02) si de clonare (Task 3), iar includerea lui aici ar reactiva accidental o grupa arhivata la orice editare.
  </action>
  <verify>
    <automated>npm run lint && grep -c "Tip Grupă" components/Grupe/GrupaFormModal.tsx && grep -c "per_sezon" components/Grupe/GrupaFormModal.tsx && grep -c "useSezonActiv" components/Grupe/GrupaFormModal.tsx && test "$(grep -c "arhivat" components/Grupe/GrupaFormModal.tsx)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece.
    - `Select` cu `name="tip_grupa"` are exact optiunile `permanent` (implicita) si `per_sezon`.
    - `Select` cu `name="sezon_id"` este randat doar cand `formState.tip_grupa === 'per_sezon'`.
    - Payload-ul `finalGrupa` trimite `sezon_id: null` ori de cate ori `tip_grupa === 'permanent'`.
    - Fisierul nu contine deloc cuvantul `arhivat`.
    - Textul ajutator este identic cu cel din Copywriting Contract.
  </acceptance_criteria>
  <done>Orice grupa noua sau editata poarta explicit tipul ei, iar grupele per-sezon sunt intotdeauna legate de un sezon real al clubului.</done>
</task>

<task type="auto">
  <name>Task 2: GrupaCard — badge-uri tip/arhivare si actiunea "Dublează în sezon nou"</name>
  <files>components/Grupe/GrupaCard.tsx</files>
  <read_first>
    - components/Grupe/GrupaCard.tsx integral (210 linii — antetul cu badge-uri la 95-114, bara de actiuni la 139-206)
    - components/ui.tsx liniile 686-720 (`Badge`, variantele `green`/`red`/`amber`/`blue`/`slate`)
    - components/icons.tsx (`CopyIcon` adaugat in 27-01)
    - .planning/phases/27-.../27-UI-SPEC.md sectiunea "3. GrupaCard.tsx extension" + "Color"
  </read_first>
  <action>
1. Extinde tipul de props cu `sezonActivId?: string | null;` si `onDubleaza?: (g: GrupaWithDetails) => void;`.

2. Calculeaza local: `const tipGrupa = grupa.tip_grupa ?? 'permanent';` (cache-ul localStorage `cache_grupe_*` poate contine inca randuri fara campul nou — fallback obligatoriu), `const esteArhivata = grupa.arhivat === true;`, `const poateFiDublata = esteArhivata && !!onDubleaza && !!sezonActivId && sezonActivId !== grupa.sezon_id;`.

3. In randul de antet (linia ~98), langa `grupa.denumire`, randeaza cu componenta `Badge` din `../ui` (NU span-uri Tailwind inline): `<Badge variant="blue">Permanentă</Badge>` cand `tipGrupa === 'permanent'`, altfel `<Badge variant="amber">Per Sezon</Badge>`; suplimentar `<Badge variant="slate">Arhivată</Badge>` doar cand `esteArhivata`. Badge-urile existente "Anulat azi" / "Program modificat" raman neschimbate.

4. Pe `Card`, adauga clasa `opacity-60` doar cand `esteArhivata` (concateneaza in `className`-ul existent).

5. In bara de actiuni: cand `esteArhivata` este true, NU randa butonul `Gestionează` si nici blocul cu meniul `...` (care contine stergerea si modificarile de orar) — grupa arhivata ramane read-only; butonul `Detalii` ramane vizibil intotdeauna (D-06 cere ca grupa arhivata sa ramana consultabila in istoric/rapoarte). Cand `poateFiDublata`, randeaza in locul lor `<Button size="sm" variant="primary" onClick={() => onDubleaza!(grupa)} className="min-h-[40px] touch-manipulation">` cu `<CopyIcon className="w-4 h-4 mr-2" />` si textul exact `Dublează în sezon nou`. Cand grupa e arhivata dar `poateFiDublata` e false (nu exista sezon activ sau grupa apartine deja sezonului activ), nu se randeaza niciun buton suplimentar.
  </action>
  <verify>
    <automated>npm run lint && grep -c "Dublează în sezon nou" components/Grupe/GrupaCard.tsx && grep -c "Badge" components/Grupe/GrupaCard.tsx && grep -c "opacity-60" components/Grupe/GrupaCard.tsx && grep -c "tip_grupa ?? 'permanent'" components/Grupe/GrupaCard.tsx</automated>
    <human-check>O grupa arhivata apare estompata, cu badge-urile "Per Sezon" + "Arhivată", fara butonul "Gestionează" si fara meniul "...", dar cu "Detalii" si "Dublează în sezon nou".</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece.
    - Badge-urile folosesc componenta `Badge` cu variantele `blue` (Permanentă), `amber` (Per Sezon), `slate` (Arhivată) — fara clase de chip inventate.
    - `tip_grupa` este citit cu fallback `?? 'permanent'`.
    - Cand `grupa.arhivat === true`, butonul `Gestionează` si meniul `...` nu se randeaza, iar `Detalii` ramane.
    - Butonul de dublare apare doar cand grupa e arhivata, exista `sezonActivId` si acesta difera de `grupa.sezon_id`.
  </acceptance_criteria>
  <done>Starea unei grupe (tip + arhivare) este vizibila dintr-o privire, iar dublarea e oferita exact acolo unde are sens.</done>
</task>

<task type="auto">
  <name>Task 3: Grupe/index.tsx — clonare manuala in sezonul activ, fara sportivi</name>
  <files>components/Grupe/index.tsx</files>
  <read_first>
    - components/Grupe/index.tsx integral (345 linii — `useGrupe` la 41-47, `handleRefresh`/golire cache la 64-76, `handleSave` la 86-136 ca model de INSERT grupa + `orar_saptamanal`, randarea `GrupaCard` la 277-279)
    - hooks/useSezoane.ts (`useSezonActiv`)
    - components/ui.tsx (`ConfirmModal` — props `title`, `message`, `confirmLabel`, `variant`)
    - .planning/phases/27-.../27-UI-SPEC.md ("Clone grupă confirmation copy")
    - .planning/phases/27-.../27-CONTEXT.md deciziile D-07 si D-08
  </read_first>
  <action>
1. Adauga `const sezonClubId = grupeClubId ?? activeRoleContext?.club_id ?? null;` si `const { sezonActiv } = useSezonActiv(sezonClubId);`.

2. Ordoneaza lista de afisare astfel incat grupele arhivate sa apara ultimele, pastrand ordinea relativa existenta: construieste `const grupeAfisate = [...grupe].sort((a, b) => Number(a.arhivat ?? false) - Number(b.arhivat ?? false));` si foloseste-o in `.map(...)` in locul lui `grupe` (verificarea `grupe.length > 0` pentru empty state ramane pe lista originala).

3. Transmite la `GrupaCard` doua props noi: `sezonActivId={sezonActiv?.id ?? null}` si `onDubleaza={setGrupaToClone}`, unde `const [grupaToClone, setGrupaToClone] = useState<GrupaWithDetails | null>(null);` si `const [isCloning, setIsCloning] = useState(false);`.

4. Randeaza un `ConfirmModal` deschis cand `grupaToClone !== null`, cu `title="Dublează în sezon nou"`, `variant="info"`, `confirmLabel="Dublează"` si `message` construit dinamic dupa Copywriting Contract: `Se va crea o grupă nouă '{grupaToClone.denumire}' legată de sezonul '{sezonActiv?.denumire}', fără sportivi asignați — sportivii se re-asignează manual după clonare.`

5. `handleDubleaza()` (apelat la confirmare, cu garda `if (isCloning || !grupaToClone || !sezonActiv) return;`):
   a. INSERT in `grupe` cu payload explicit: `{ denumire: grupaToClone.denumire, sala: grupaToClone.sala, club_id: grupaToClone.club_id, locatie_id: (grupaToClone as any).locatie_id ?? null, tip_grupa: 'per_sezon', sezon_id: sezonActiv.id, arhivat: false }`, cu `.select().single()`. Pe eroare: `showError("Eroare la dublarea grupei", error)` si iesire.
   b. Copiaza programul saptamanal din `grupaToClone.program` cu mapare explicita pe campuri (nu spread de rand DB): pentru fiecare `p` insereaza in `orar_saptamanal` obiectul `{ ziua: p.ziua, ora_start: p.ora_start, ora_sfarsit: p.ora_sfarsit, is_activ: p.is_activ ?? true, grupa_id: <id-ul grupei noi>, club_id: <club_id-ul grupei noi> }`. Daca `program` e gol, sari peste pas. Pe eroare la acest pas, afiseaza `showError` dar considera clonarea reusita (grupa exista deja, orarul poate fi completat manual).
   c. NU copia sportivii si NU atinge tabelele `sportivi` / `sportivi_grupe_secundare` — D-08 interzice explicit copierea listei de sportivi; grupa noua se creeaza goala.
   d. Invalideaza cache-ul exact ca in `handleRefresh` (liniile 67-72): golire chei `cache_grupe_*` din localStorage cu `clearCache`, `queryClient.invalidateQueries({ queryKey: ['grupe'] })`, apoi `await refetchGrupe()`.
   e. `showSuccess("Grupă dublată", "Grupa '<denumire>' a fost creată în sezonul '<denumire sezon activ>'. Adaugă sportivii manual din Detalii → Adaugă Sportivi.")`, apoi `setGrupaToClone(null)` si `setIsCloning(false)` (in `finally`).

6. Nu modifica `handleSave`, `confirmDelete` sau fluxurile existente de sportivi — singura schimbare de comportament asupra grupelor existente este ordinea de afisare.
  </action>
  <verify>
    <automated>npm run lint && grep -c "tip_grupa: 'per_sezon'" components/Grupe/index.tsx && grep -c "sezon_id: sezonActiv.id" components/Grupe/index.tsx && grep -c "fără sportivi asignați" components/Grupe/index.tsx && test "$(grep -n "handleDubleaza" -A 40 components/Grupe/index.tsx | grep -c "from('sportivi')")" = "0"</automated>
    <human-check>Dubleaza o grupa arhivata: apare o grupa noua cu acelasi nume si acelasi orar, marcata "Per Sezon", cu 0 sportivi activi, iar grupa arhivata ramane in lista neschimbata.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` trece.
    - INSERT-ul de clonare seteaza explicit `tip_grupa: 'per_sezon'`, `sezon_id` = id-ul sezonului activ si `arhivat: false`.
    - Blocul `handleDubleaza` nu contine niciun apel `from('sportivi')` sau `from('sportivi_grupe_secundare')` (D-08).
    - Confirmarea afiseaza mesajul din Copywriting Contract, incluzand denumirea sezonului activ.
    - Dupa clonare se goleste cache-ul `cache_grupe_*` si se face refetch, deci grupa noua apare imediat.
    - Grupa sursa ramane cu `arhivat = true` (nicio scriere pe randul ei).
  </acceptance_criteria>
  <done>Adminul poate reporni manual o grupa in sezonul nou, cu orarul mostenit si lista de sportivi goala, exact cum cer D-07 si D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Grupe -> Supabase | INSTRUCTOR are drept de scriere pe `grupe` prin politica existenta `has_access_to_club(club_id)` (neschimbata, D-04) |
| lista de sezoane afisata -> club_id al grupei | Sezonul ales trebuie sa apartina aceluiasi club ca grupa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Tampering | O grupa este legata de un sezon al altui club | mitigate | `useSezonActiv(clubSelectat)` incarca doar sezoanele clubului selectat in formular, iar RLS `sezoane_select` interzice citirea sezoanelor altui club |
| T-27-11 | Information Disclosure | Clonarea copiaza sportivii intr-o grupa noua vizibila altcuiva | mitigate | Clonarea nu atinge deloc tabela `sportivi` (D-08); verificat prin gate automat `grep -c "from('sportivi')" = 0` in blocul de clonare |
| T-27-12 | Tampering | Editarea unei grupe arhivate o readuce silentios in starea activa | mitigate | `arhivat` nu apare deloc in payload-ul `GrupaFormModal`, iar butonul `Gestionează` nu se randeaza pe carduri arhivate |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | Zero pachete noi instalate in acest plan |
</threat_model>

<verification>
- `npm run lint` -> zero erori.
- Dupa o clonare in aplicatie, interogare live: `select denumire, tip_grupa, sezon_id, arhivat from public.grupe where club_id='<club>' order by denumire` arata perechea (grupa veche `arhivat=true`) + (grupa noua `arhivat=false`, `sezon_id`=sezon activ).
- `select count(*) from public.sportivi where grupa_id = '<id grupa clonata>'` = 0 imediat dupa clonare (D-08).
- `select count(*) from public.orar_saptamanal where grupa_id = '<id grupa clonata>'` = numarul de intervale din grupa sursa.
</verification>

<success_criteria>
- Fiecare grupa are un tip explicit vizibil in card si editabil in formular.
- Grupele arhivate raman in lista, marcate si read-only, cu detalii accesibile.
- Dublarea produce o grupa noua in sezonul activ, cu orarul copiat si zero sportivi.
</success_criteria>

## Artifacts this phase produces

Simboluri noi create in acest plan:

- Props noi: `GrupaCard.sezonActivId`, `GrupaCard.onDubleaza`
- Campuri noi in `formState` din `GrupaFormModal`: `tip_grupa`, `sezon_id`
- Handleri/state noi in `components/Grupe/index.tsx`: `grupaToClone`, `isCloning`, `handleDubleaza`, `grupeAfisate`
- Etichete UI noi: `Tip Grupă`, `Sezon`, `Permanentă`, `Per Sezon`, `Arhivată`, `Dublează în sezon nou`

<output>
Create `.planning/phases/27-sezoane-abonamente-si-grupe-sistem-sezoane-cu-interval-date-/27-03-SUMMARY.md` when done
</output>
