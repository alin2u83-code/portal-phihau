---
phase: 11-prezenta-refactorizata
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Prezenta/RaportLunarPrezenta.tsx
  - components/Prezenta/RaportPrezenta.tsx
autonomous: true
requirements: [PRZ-05]
must_haves:
  truths:
    - "Raportul lunar de prezență NU mai afișează nicio coloană sau valoare de procentaj, nici în tabel, nici în carduri mobile, nici în CSV (PRZ-05a)"
    - "Bannerul perioadă examen din raportul lunar afișează corect caracterele (fără em-dash garbled)"
    - "Raportul de prezență (RaportPrezenta) are un tab 'Per Grupă' care afișează count absolut per sportiv per grupă, fără procente și fără grafic (PRZ-05b)"
  artifacts:
    - path: "components/Prezenta/RaportLunarPrezenta.tsx"
      provides: "Raport lunar fără procente"
      contains: "Raport Lunar Prezență"
    - path: "components/Prezenta/RaportPrezenta.tsx"
      provides: "Tab Per Grupă cu count absolut fără procente"
      contains: "Per Grupă"
  key_links:
    - from: "components/Prezenta/RaportPrezenta.tsx"
      to: "filteredData.antrenamente"
      via: "agregare count per grupă per sportiv"
      pattern: "filteredData"
---

<objective>
Curăță rapoartele de prezență existente conform PRZ-05: elimină complet procentele din raportul lunar (tabel + carduri + CSV) și adaugă un tab "Per Grupă" în RaportPrezenta care arată numere absolute de prezențe per sportiv per grupă, fără procente și fără grafic.

Acoperă PRZ-05 sub-cerințele (a) raport lunar fără procente și (b) raport per grupă. Sub-cerința (c) raport per interval examen este în Plan 04. Acest plan atinge DOAR fișierele de raport, deci rulează în paralel cu Plan 01 (calendar) în Wave 1.

Purpose: Rapoartele afișează doar numere absolute (cerință explicită PRZ-05, niciun procent).
Output: RaportLunarPrezenta.tsx fără procente, RaportPrezenta.tsx cu tab Per Grupă.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-prezenta-refactorizata/11-RESEARCH.md
@CLAUDE.md
@components/Prezenta/RaportLunarPrezenta.tsx
@components/Prezenta/RaportPrezenta.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Elimină procentele din RaportLunarPrezenta și repară em-dash</name>
  <files>components/Prezenta/RaportLunarPrezenta.tsx</files>
  <read_first>
    - components/Prezenta/RaportLunarPrezenta.tsx (toată — CSV handleExport linia 315, tabel desktop coloana procent liniile 405-470, carduri mobile liniile 530-588, perioadaLabel linia 339, fmtDate linia 47)
    - .planning/phases/11-prezenta-refactorizata/11-RESEARCH.md (Pitfall 4 procente CSV, Pitfall 6 em-dash)
  </read_first>
  <action>
    În components/Prezenta/RaportLunarPrezenta.tsx:
    1. CSV (handleExport, ~linia 316-327): șterge complet proprietatea "Procentaj (luna)" din obiectul dataToExport. Păstrează "Planificate (luna)", "Prezențe (luna)" și restul coloanelor.
    2. Tabel desktop antet (liniile 405-421): șterge th-ul care conține textul procent ("%") din rândul secundar de antet (cel cu Planificate / Prezențe / procent). Ajustează colSpan al grupului "Luna curentă" de la 3 la 2 (rămân Planificate + Prezențe). Verifică și colSpan-ul rândului breakdown expandat (era 8) și redu-l la 7 ca să se alinieze după ștergerea coloanei.
    3. Tabel desktop corp (liniile 424-479): șterge variabila percentage și celula td care afișează valoarea procent. Pentru evidențierea "la risc", înlocuiește orice condiție bazată pe percentage cu prag pe numere absolute folosind atRisk = row.attendedTrainings < 5 (variabilă deja existentă) pentru clasa de fundal roșie și iconița warning. NU mai calcula sau afișa procente nicăieri.
    4. Carduri mobile (liniile 530-588): șterge percentage, barWidth, span-ul care afișează valoarea procent și bara de progres (div cu w-full bg-slate-700). Înlocuiește indicatorul "la risc" cu atRisk = row.attendedTrainings < 5. Păstrează afișarea Planificate / Prezențe ca numere.
    5. Breakdown grupe (atât desktop liniile 482-520 cât și mobile liniile 602-620): șterge orice afișare de procent per grupă (calculul attendedTrainings / totalTrainings * 100). Afișează doar attendedTrainings/totalTrainings ca numere absolute.
    6. Em-dash (Pitfall 6): la perioadaLabel (linia 343) construiește returnul cu separator ASCII spațiu-cratimă-spațiu între startStr și endStr, NU em-dash. La fmtDate (linia 47) înlocuiește valoarea de fallback (era em-dash) cu cratimă ASCII simplă.
    NU schimba logica de fetch sesiuni examene sau calculul reportData (numerele rămân corecte). Scopul e strict eliminarea procentelor și fix encoding.
  </action>
  <verify>
    <automated>cd "C:/Users/lungu/portal-phihau" && { grep -vE '^[[:space:]]*//' components/Prezenta/RaportLunarPrezenta.tsx | grep -E "Procentaj|\* 100\)" ; } | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Comanda de verificare returnează 0 (niciun calcul de procent rămas în cod necomentat)
    - Nu există proprietatea "Procentaj (luna)" în dataToExport (grep)
    - perioadaLabel folosește separator ASCII (fără em-dash); fmtDate fallback este cratimă ASCII
    - npx tsc --noEmit fără erori noi în fișier
  </acceptance_criteria>
  <done>Raportul lunar afișează doar numere absolute; CSV, tabel, carduri și breakdown nu mai conțin procente; encoding-ul bannerului e corect.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adaugă tab Per Grupă în RaportPrezenta (count absolut, fără procente)</name>
  <files>components/Prezenta/RaportPrezenta.tsx</files>
  <read_first>
    - components/Prezenta/RaportPrezenta.tsx (toată — filteredDetailedLog/athleteSummary liniile 71-143, columnsSummary cu coloana Procentaj liniile 183-194, render liniile 285-382)
    - components/ResponsiveTable.tsx (Column, ResponsiveTable — folosit pentru tabele)
  </read_first>
  <action>
    În components/Prezenta/RaportPrezenta.tsx adaugă un tab "Per Grupă" fără a strica vizualizarea existentă (graficul + sumar + jurnal rămân pe tab-ul "General").
    1. Adaugă state `const [activeReportTab, setActiveReportTab] = useState<'general' | 'per-grupa'>('general')`.
    2. Randează un selector de tab (două butoane "General" și "Per Grupă") imediat sub titlul "Analiză Prezențe" (linia ~288), cu stil consistent cu tab-bar-ul din index.tsx (pill indigo activ / slate inactiv).
    3. Wrap conținutul existent (grafic + filtre + Sumar pe Sportiv + Jurnal Detaliat) într-un bloc randat doar când `activeReportTab === 'general'`.
    4. Construiește datele pentru tab Per Grupă: `const perGrupaData = useMemo(...)` care, pornind de la `filteredDetailedLog` (deja calculat, conține rec.grupaNume, rec.sportiv, rec.status), agregă un Map cu cheia `\`${grupaNume}||${sportiv.id}\`` și valoarea `{ grupaNume, sportivNume, sportivId, prezente: number }` numărând doar rec.status === 'Prezent'. Convertește în array sortat după grupaNume apoi sportivNume. NU calcula procente — doar count absolut de prezențe.
    5. Randează tab Per Grupă (când `activeReportTab === 'per-grupa'`): un tabel grupat pe grupă. Folosește `ResponsiveTable` SAU o structură simplă: pentru fiecare grupă un sub-antet cu denumirea, apoi rânduri Sportiv | Nr. Prezențe (count absolut). Reutilizează filtrele existente (grupă/an/sală) — `filteredDetailedLog` le respectă deja. NU include coloană de procentaj și NU include grafic Recharts pe acest tab.
    NU modifica `columnsSummary` de pe tab General (acela rămâne cu procentaj — e tab-ul de analiză existent, nu intră sub PRZ-05b; PRZ-05b cere raport per grupă fără procente, ceea ce e exact noul tab). Doar dacă vrei coerență, lasă tab General neschimbat.
    Respectă design system: Card, Button din ../ui, clase Tailwind dark. NU Shadcn/MUI.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "RaportPrezenta" | grep -v "^#"; test $? -ne 0 && echo "NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - RaportPrezenta.tsx conține un selector de tab cu valoarea 'per-grupa' (grep "per-grupa")
    - Există `perGrupaData` agregat din filteredDetailedLog numărând doar status Prezent (grep "perGrupaData")
    - Tab-ul Per Grupă NU conține calcul de procent (fără "* 100" în secțiunea per-grupă) și NU randează BarChart pe acel tab
    - Tab General existent (grafic + sumar + jurnal) rămâne funcțional sub condiția activeReportTab === 'general'
    - npx tsc --noEmit fără erori noi
  </acceptance_criteria>
  <done>RaportPrezenta are tab Per Grupă cu count absolut de prezențe per sportiv per grupă, fără procente și fără grafic; tab-ul General rămâne intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase | datele de raport provin din filteredData (deja filtrat per club/rol) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Information Disclosure | export CSV / tab per grupă | mitigate | Datele provin din filteredData.sportivi/antrenamente, deja filtrate per visibleClubIds din usePermissions; nicio sursă nouă de date |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Faza nu instalează pachete noi (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- npx tsc --noEmit fără erori noi în ambele fișiere
- Niciun procent rămas în RaportLunarPrezenta (grep)
- Tab Per Grupă afișează numere absolute
</verification>

<success_criteria>
- PRZ-05a: raport lunar fără procente (tabel + carduri + CSV) și encoding corect
- PRZ-05b: tab Per Grupă cu count absolut per sportiv per grupă
</success_criteria>

<output>
Create `.planning/phases/11-prezenta-refactorizata/11-03-SUMMARY.md` when done
</output>
