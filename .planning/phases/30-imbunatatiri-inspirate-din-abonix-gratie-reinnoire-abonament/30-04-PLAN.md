---
phase: 30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament
plan: 04
type: execute
wave: 2
depends_on: ["30-02"]
files_modified:
  - supabase/migrations/20260918d_loialitate_politici_reducere.sql
  - types.ts
  - utils/loialitateReinnoiri.ts
  - utils/loialitateReinnoiri.test.ts
  - services/loialitateService.ts
autonomous: true
requirements: [ABX-05]
user_setup: []

must_haves:
  truths:
    - "Schema reala live a tabelelor politici_reducere si aplicare_reduceri e cunoscuta si documentata inainte de a se scrie orice cod impotriva lor"
    - "O politica de loialitate poate exprima 'dupa N reinnoiri consecutive, acorda bonus X', persistat in politici_reducere"
    - "Numarul de reinnoiri consecutive si suma bonusului se calculeaza pur, fara acces la DB"
    - "Bonusul calculat nu poate depasi pretul de lista si nu poate fi negativ"
    - "Serviciul de citire a politicilor returneaza { data, error } si nu arunca exceptii"
  artifacts:
    - path: "supabase/migrations/20260918d_loialitate_politici_reducere.sql"
      provides: "Coloane de loialitate pe politici_reducere + jurnal de aplicare"
      contains: "reinnoiri_necesare"
    - path: "utils/loialitateReinnoiri.ts"
      provides: "Numarare pura de luni consecutive achitate + calcul bonus plafonat"
      exports: ["numaraReinnoiriConsecutive", "calculeazaBonusLoialitate"]
      min_lines: 60
    - path: "utils/loialitateReinnoiri.test.ts"
      provides: "Script tsx de verificare a celor 7 cazuri de acceptanta"
      contains: "numaraReinnoiriConsecutive"
    - path: "services/loialitateService.ts"
      provides: "Citire politici de loialitate active per club"
      exports: ["getPoliticiLoialitate"]
      min_lines: 25
  key_links:
    - from: "services/loialitateService.ts"
      to: "public.politici_reducere"
      via: "supabase.from('politici_reducere').select(...)"
      pattern: "from\\('politici_reducere'\\)"
    - from: "types.ts"
      to: "politici_reducere"
      via: "interface PoliticaReducere"
      pattern: "interface PoliticaReducere"
---

<objective>
Feature 3 — loialitate automata, partea de fundatie. Acest plan NU atinge `components/Plati/PlatiScadente.tsx` (proprietar unic: planul 30-05). Livreaza: adevarul despre schema live, coloanele de loialitate, tipul TypeScript, functiile pure de numarare/calcul si serviciul de citire.

Constrangere absoluta din 30-CONTEXT.md § Feature 3: schema `politici_reducere` / `aplicare_reduceri` NU a fost verificata live in research (nu exista niciun `CREATE TABLE` comis in git pentru ele — precedentul "politici si tabele aplicate direct pe DB", documentat in memoria fazei 25). **Task 1 este obligatoriu primul si este read-only.** Nicio linie de cod impotriva acestor tabele nu se scrie inainte ca Task 1 sa fie complet.

Capcana de evitat (30-RESEARCH.md § Pitfall 4): `politici_reducere` NU e acelasi lucru cu `reduceri`. Tabelul `reduceri` are UI propriu (`components/Plati/Reduceri.tsx`) si interfata `Reducere` in `types.ts` (`nume`, `tip`, `valoare`, `este_activa`, `categorie_aplicabila`). `plati.reducere_id` este, foarte probabil, FK catre `reduceri`, nu catre `politici_reducere` — Task 1 trebuie sa stabileasca asta cu certitudine, pentru ca decide daca bonusul de loialitate poate sau nu sa populeze `plati.reducere_id`.

Purpose: Un sportiv care plateste constant primeste automat un beneficiu, fara interventie manuala a adminului — si se inchide un TODO vechi din cod in loc sa se construiasca un al doilea sistem paralel de reduceri.
Output: raport de schema live, migratie de extindere, `PoliticaReducere` in `types.ts`, `utils/loialitateReinnoiri.ts` + test colocat, `services/loialitateService.ts`.

Depinde de 30-02 doar pentru proprietatea fisierului `types.ts` (evitarea unui conflict de scriere in acelasi val), nu pentru logica.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-CONTEXT.md
@.planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-RESEARCH.md
@components/Plati/Reduceri.tsx
@components/Plati/PlatiScadente.tsx
@utils/luniLipsa.ts
@utils/luniLipsa.test.ts
@CLAUDE.md
</context>

<interfaces>
Contracte existente:
- `types.ts` → `interface Plata { id, sportiv_id, familie_id, club_id?, suma_initiala?, reducere_id?, reducereDetalii?, suma, data, status: 'Achitat'|'Neachitat'|'Achitat Parțial'|'Anulat', descriere, tip, observatii, luna?, an?, ... }`
  ATENTIE: numele real al coloanei in DB este `reducereDetalii` (camelCase), nu `reducere_detalii` — scrierea cu snake_case produce PGRST204 (comentariu existent in `types.ts` liniile 162-166).
- `types.ts` → `interface Reducere { id, nume, tip: 'procent'|'suma_fixa', valoare, este_activa, categorie_aplicabila }` — mapata pe tabelul `reduceri`, NU pe `politici_reducere`.
- Conventie servicii (CLAUDE.md): serviciile returneaza `{ data, error }`, nu arunca.
- Conventie de testare a proiectului (NU introduce alta): fara vitest/jest; utilitarele pure au teste colocate `*.test.ts` cu `assert()` propriu si auto-rulare gardata pe `process.argv[1]`, executate cu `npx tsx`. Vezi `utils/luniLipsa.test.ts`.

Contracte NOI create de acest plan, consumate de planul 30-05:
```
// types.ts
export interface PoliticaReducere {
  id: string;
  club_id: string | null;
  nume_reducere: string;
  procentaj: number | null;
  valoare_fixa: number | null;
  activ: boolean;
  reinnoiri_necesare: number | null;            // Faza 30
  tip_bonus: 'zile_gratis' | 'discount' | null; // Faza 30
}

// utils/loialitateReinnoiri.ts  (pur, fara Supabase)
export function numaraReinnoiriConsecutive(
  platiEntitate: Plata[],
  azi: Date
): number;

export function calculeazaBonusLoialitate(
  politici: PoliticaReducere[],
  reinnoiriConsecutive: number,
  pretLista: number
): { sumaBonus: number; politica: PoliticaReducere | null; detalii: string | null };

// services/loialitateService.ts  (IO)
export async function getPoliticiLoialitate(
  clubId: string
): Promise<{ data: PoliticaReducere[] | null; error: Error | null }>;
```
Separarea e deliberata (30-RESEARCH.md § Pattern 3): tot ce e calcul sta in `utils/` si e testabil fara DB; doar accesul la Supabase sta in `services/`.

Proiect Supabase live: `wuhidifzsutwgdfkwhmd`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verificare schema live (OBLIGATORIU primul, read-only)</name>
  <read_first>
    - .planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-RESEARCH.md (§ Pitfall 3 si § Pitfall 4, § Assumptions Log A1/A2)
    - .planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-CONTEXT.md (§ Feature 3)
    - types.ts (liniile 155-236 — `Plata` si `Reducere`)
  </read_first>
  <files>(read-only — nicio modificare de fisier in acest task)</files>
  <action>
Ruleaza prin `mcp__plugin_supabase_supabase__execute_sql` pe proiectul `wuhidifzsutwgdfkwhmd`, si transcrie rezultatele COMPLET in SUMMARY (tabele, nu parafraza):

1. Existenta: `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('politici_reducere','aplicare_reduceri','reduceri');`
2. Coloane: `SELECT table_name, ordinal_position, column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_schema='public' AND table_name IN ('politici_reducere','aplicare_reduceri','reduceri') ORDER BY table_name, ordinal_position;`
3. Chei straine (inclusiv tinta lui `plati.reducere_id`): `SELECT tc.table_name, kcu.column_name, ccu.table_name AS tabel_tinta, ccu.column_name AS coloana_tinta FROM information_schema.table_constraints tc JOIN information_schema.key_column_usage kcu ON tc.constraint_name = kcu.constraint_name AND tc.table_schema = kcu.table_schema JOIN information_schema.constraint_column_usage ccu ON tc.constraint_name = ccu.constraint_name AND tc.table_schema = ccu.table_schema WHERE tc.constraint_type='FOREIGN KEY' AND tc.table_schema='public' AND (tc.table_name IN ('politici_reducere','aplicare_reduceri') OR (tc.table_name='plati' AND kcu.column_name='reducere_id'));`
4. Volum de date: `SELECT 'politici_reducere' AS t, count(*) FROM public.politici_reducere UNION ALL SELECT 'aplicare_reduceri', count(*) FROM public.aplicare_reduceri UNION ALL SELECT 'reduceri', count(*) FROM public.reduceri;` — daca un tabel nu exista, noteaza asta si continua cu restul.
5. Continut real: `SELECT * FROM public.politici_reducere LIMIT 10;` si `SELECT * FROM public.aplicare_reduceri LIMIT 10;`
6. RLS: `SELECT tablename, policyname, cmd, qual, with_check FROM pg_policies WHERE schemaname='public' AND tablename IN ('politici_reducere','aplicare_reduceri') ORDER BY tablename, cmd;` si `SELECT relname, relrowsecurity FROM pg_class WHERE relname IN ('politici_reducere','aplicare_reduceri');`

Raspunde explicit, in SUMMARY, la aceste patru intrebari (Task 2 si planul 30-05 depind de raspunsuri):
- Q1: `politici_reducere` exista? Daca da, care sunt coloanele exacte si se potrivesc cu presupunerea `id, club_id, nume_reducere, procentaj, valoare_fixa, activ`?
- Q2: `aplicare_reduceri` exista si cum se leaga de o entitate? Are `sportiv_id`? Are `plata_id`? Are `(luna, an)`? Are FK catre `politici_reducere` sau catre `reduceri`?
- Q3: `plati.reducere_id` are FK catre `reduceri` sau catre `politici_reducere`? (Daca e catre `reduceri`, bonusul de loialitate NU poate popula `reducere_id` cu un id de politica — se va folosi doar `suma_initiala` + `reducereDetalii`.)
- Q4: RLS e activ pe ambele tabele si politicile folosesc `has_access_to_club(club_id)` sau leaga clubul prin `sportiv_id`?
  </action>
  <acceptance_criteria>
    - SUMMARY contine tabelul complet de coloane (interogarea 2) pentru fiecare dintre cele trei tabele care exista.
    - SUMMARY raspunde explicit la Q1, Q2, Q3, Q4, fiecare raspuns sprijinit de output-ul unei interogari.
    - SUMMARY afirma clar tinta FK a lui `plati.reducere_id` (nume de tabel), sau "fara FK" daca interogarea 3 nu returneaza nimic pentru el.
    - Niciun fisier sursa din repo nu a fost modificat in acest task (doar `.planning/` poate aparea ca netracked).
  </acceptance_criteria>
  <verify>
    <automated>mcp__plugin_supabase_supabase__execute_sql (project_id: wuhidifzsutwgdfkwhmd) cu: SELECT table_name, count(*) AS nr_coloane FROM information_schema.columns WHERE table_schema='public' AND table_name IN ('politici_reducere','aplicare_reduceri','reduceri') GROUP BY 1 ORDER BY 1; — asteptat: un rand pentru fiecare tabel care exista, cu numarul de coloane.</automated>
    <automated>bash -lc "git status --porcelain | grep -v '^?? .planning/' | grep -v '^ M .planning/' | wc -l"</automated>
  </verify>
  <done>Schema reala e cunoscuta si scrisa in SUMMARY; cele patru intrebari au raspuns documentat.</done>
</task>

<task type="auto">
  <name>Task 2: Migratie — coloane de loialitate + jurnal de aplicare</name>
  <read_first>
    - rezultatele Task 1 (raspunsurile Q1-Q4) — obligatorii inainte de a scrie o linie de SQL
    - supabase/migrations/20260523_sms_system.sql (liniile 124-172 — pattern RLS `has_access_to_club(club_id)` de urmat pentru orice tabel nou)
    - .planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-CONTEXT.md (§ Feature 3 — coloanele cerute: `reinnoiri_necesare`, `tip_bonus`)
    - types.ts (liniile 229-243 — locul unde se insereaza `PoliticaReducere`, dupa `interface Reducere`)
  </read_first>
  <files>supabase/migrations/20260918d_loialitate_politici_reducere.sql, types.ts</files>
  <action>
Scrie migratia PE BAZA raspunsurilor din Task 1, respectand aceste reguli deterministe:

1. `politici_reducere`:
   - Daca Q1 = "nu exista": `CREATE TABLE public.politici_reducere (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), club_id UUID REFERENCES public.cluburi(id) ON DELETE CASCADE, nume_reducere TEXT NOT NULL, procentaj NUMERIC(5,2), valoare_fixa NUMERIC(10,2), activ BOOLEAN NOT NULL DEFAULT true, created_at TIMESTAMPTZ NOT NULL DEFAULT now());` + `ALTER TABLE ... ENABLE ROW LEVEL SECURITY;` + politica `FOR ALL USING (public.has_access_to_club(club_id)) WITH CHECK (public.has_access_to_club(club_id))`.
   - Daca exista: NU redenumi si NU sterge nicio coloana existenta. Daca numele reale difera de presupunere (ex. `nume` in loc de `nume_reducere`), foloseste numele REALE peste tot — in migratie, in `types.ts` si in serviciu — si noteaza abaterea in SUMMARY.
   - In ambele cazuri adauga: `ALTER TABLE public.politici_reducere ADD COLUMN IF NOT EXISTS reinnoiri_necesare INTEGER;` si `ALTER TABLE public.politici_reducere ADD COLUMN IF NOT EXISTS tip_bonus TEXT;` plus, intr-un bloc `DO $$ ... $$` gardat pe `pg_constraint`, `CHECK (tip_bonus IS NULL OR tip_bonus IN ('zile_gratis','discount'))` si `CHECK (reinnoiri_necesare IS NULL OR reinnoiri_necesare > 0)`.
   - `COMMENT ON COLUMN public.politici_reducere.tip_bonus IS 'Faza 30. discount = reducere pe factura urmatoare (procentaj sau valoare_fixa). zile_gratis = luna urmatoare de abonament e gratuita (suma facturata 0). Denumirea zile_gratis vine din 30-CONTEXT.md; in Portal PhiHau facturarea e pe luna calendaristica, nu pe zile, deci echivalentul corect al bonusului in zile este o luna gratuita (vezi 30-RESEARCH.md, Anti-Patterns).';`
   - `COMMENT ON COLUMN public.politici_reducere.reinnoiri_necesare IS 'Faza 30: numarul de luni consecutive cu factura Abonament achitata, necesar pentru a declansa bonusul.';`

2. `aplicare_reduceri` (jurnal "cine a primit ce si cand"):
   - Daca Q2 arata ca tabelul NU exista: `CREATE TABLE public.aplicare_reduceri (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), politica_id UUID REFERENCES public.politici_reducere(id) ON DELETE SET NULL, sportiv_id UUID REFERENCES public.sportivi(id) ON DELETE CASCADE, familie_id UUID REFERENCES public.familii(id) ON DELETE CASCADE, plata_id UUID REFERENCES public.plati(id) ON DELETE CASCADE, suma_redusa NUMERIC(10,2), created_at TIMESTAMPTZ NOT NULL DEFAULT now());` + RLS. (Nota: acest tabel figureaza in planul 15-01 ca avand deja politica `club_member_access` — daca politica exista dar tabelul nu, raporteaza discrepanta si opreste-te.)
   - Daca exista si are deja o legatura utilizabila catre o plata (`plata_id`, sau perechea `(luna, an)` plus `sportiv_id`): NU schimba nimic din structura; doar noteaza in SUMMARY care coloane va folosi planul 30-05 pentru a scrie jurnalul.
   - Daca exista dar NU are nicio legatura catre plata: `ALTER TABLE public.aplicare_reduceri ADD COLUMN IF NOT EXISTS plata_id UUID REFERENCES public.plati(id) ON DELETE CASCADE;` — doar adaugare, fara modificarea coloanelor existente.
   - Garda anti-dublare, in orice caz in care exista `plata_id`: `CREATE UNIQUE INDEX IF NOT EXISTS uq_aplicare_reduceri_plata ON public.aplicare_reduceri(plata_id) WHERE plata_id IS NOT NULL;`

3. Incheie cu `NOTIFY pgrst, 'reload schema';` si aplica live prin `mcp__plugin_supabase_supabase__apply_migration`, nume `loialitate_politici_reducere`.

4. In `types.ts`, adauga `export interface PoliticaReducere` cu campurile REALE confirmate in Task 1 (forma-tinta e in sectiunea `<interfaces>` de mai sus). Plaseaz-o imediat dupa `interface Reducere` (in jurul liniei 236) si adauga un comentariu de doua linii care spune explicit ca `PoliticaReducere` (tabel `politici_reducere`, folosit de generarea automata de abonamente) e DISTINCT de `Reducere` (tabel `reduceri`, folosit de `components/Plati/Reduceri.tsx`).
  </action>
  <acceptance_criteria>
    - `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='politici_reducere' AND column_name IN ('reinnoiri_necesare','tip_bonus');` returneaza 2 randuri.
    - Un `INSERT` cu `tip_bonus='altceva'` esueaza pe CHECK; unul cu `tip_bonus='discount'` reuseste (randul de test se sterge dupa).
    - `SELECT relrowsecurity FROM pg_class WHERE relname='politici_reducere';` este `true`.
    - `types.ts` contine `interface PoliticaReducere` cu campurile `reinnoiri_necesare` si `tip_bonus`, si pastreaza neatinsa `interface Reducere`.
    - `npm run lint` trece cu 0 erori.
    - SUMMARY listeaza exact ce coloane va folosi planul 30-05 pentru jurnalul din `aplicare_reduceri`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "npm run lint &amp;&amp; grep -c 'interface PoliticaReducere' types.ts &amp;&amp; grep -c 'interface Reducere' types.ts"</automated>
    <automated>mcp__plugin_supabase_supabase__execute_sql (project_id: wuhidifzsutwgdfkwhmd) cu: SELECT column_name, data_type FROM information_schema.columns WHERE table_schema='public' AND table_name='politici_reducere' AND column_name IN ('reinnoiri_necesare','tip_bonus') ORDER BY 1; SELECT relrowsecurity FROM pg_class WHERE relname='politici_reducere'; — asteptat: 2 coloane, RLS true.</automated>
  </verify>
  <done>Coloanele de loialitate exista live cu validare, RLS e activ, `PoliticaReducere` reflecta schema reala, lint curat.</done>
</task>

<task type="auto">
  <name>Task 3: Calcul pur de loialitate + serviciu de citire</name>
  <read_first>
    - utils/luniLipsa.ts (pattern de functie pura pe `Plata[]`, inclusiv filtrul `p.tip === 'Abonament'` si tratarea `luna`/`an` null)
    - utils/luniLipsa.test.ts (pattern de test colocat rulabil cu `npx tsx`)
    - types.ts (interfata `Plata` si noua `PoliticaReducere`)
    - services/facturaService.ts (pattern de serviciu care returneaza `{ data, error }`)
  </read_first>
  <files>utils/loialitateReinnoiri.ts, utils/loialitateReinnoiri.test.ts, services/loialitateService.ts</files>
  <action>
1. `utils/loialitateReinnoiri.ts` — DOUA functii pure, fara Supabase, in stilul `utils/luniLipsa.ts`:

   a. `export function numaraReinnoiriConsecutive(platiEntitate: Plata[], azi: Date): number`
      Regula de numarare, fixata explicit conform 30-CONTEXT.md § Feature 3 ("pe baza randurilor `plati` cu `status='Achitat'`, nu tranzactii brute") si ca raspuns la intrebarea deschisa 2 din 30-RESEARCH.md:
      - Se iau in considerare DOAR randurile cu `tip === 'Abonament'`, `status === 'Achitat'`, `luna != null`, `an != null`.
      - Se porneste de la luna precedenta lunii lui `azi` (luna curenta e cea care tocmai se factureaza, deci nu conteaza ca reinnoire) si se merge inapoi luna cu luna cat timp exista o factura achitata pentru acea luna. Prima luna lipsa opreste numararea.
      - `'Achitat Parțial'`, `'Neachitat'` si `'Anulat'` NU conteaza ca reinnoire. Documenteaza aceasta alegere in JSDoc. O plata stinsa automat din credit conteaza, pentru ca ajunge tot cu `status='Achitat'`.
      - Returneaza 0 daca lista e goala sau daca luna precedenta nu e achitata.
      Foloseste acelasi truc de cheie ca `luniLipsa.ts` — un `Set<string>` cu `${an}-${luna}` — si iterarea inapoi cu `subMonths` din `date-fns`. Limiteaza bucla la maximum 60 de iteratii (5 ani) ca sa nu poata rula la infinit pe date corupte.

   b. `export function calculeazaBonusLoialitate(politici: PoliticaReducere[], reinnoiriConsecutive: number, pretLista: number): { sumaBonus: number; politica: PoliticaReducere | null; detalii: string | null }`
      - Filtreaza politicile cu `activ === true` si `reinnoiri_necesare != null && reinnoiriConsecutive >= reinnoiri_necesare`.
      - Daca nu ramane niciuna → `{ sumaBonus: 0, politica: null, detalii: null }`.
      - Altfel alege politica cu cel mai mare `reinnoiri_necesare` (pragul cel mai greu atins castiga; la egalitate, prima din lista).
      - `tip_bonus === 'zile_gratis'` → `sumaBonus = pretLista` (luna urmatoare e gratuita; vezi comentariul de coloana din Task 2).
      - `tip_bonus === 'discount'` sau `null` → `sumaBonus = procentaj != null ? pretLista * procentaj / 100 : (valoare_fixa ?? 0)`.
      - Plafoneaza: `sumaBonus = Math.min(Math.max(sumaBonus, 0), pretLista)` — bonusul nu poate depasi pretul si nu poate fi negativ.
      - Rotunjeste la 2 zecimale.
      - `detalii` = text scurt in romana, potrivit pentru `plati.reducereDetalii`, de forma `Loialitate: ${nume_reducere} (${reinnoiriConsecutive} luni consecutive)`.
      Aceasta functie NU apeleaza Supabase — sta in `utils/` tocmai ca sa fie testabila fara DB.

2. `utils/loialitateReinnoiri.test.ts` — test colocat dupa modelul exact din `utils/luniLipsa.test.ts` (header explicativ, `assert(condition, message)`, `runTests()` care intoarce `{passed, failed, errors}`, auto-rulare gardata pe `process.argv[1]?.endsWith('loialitateReinnoiri.test.ts')` cu `process.exit(1)` la esec). Acopera exact cele 7 cazuri din acceptance criteria.

3. `services/loialitateService.ts` — doar IO, respecta conventia `{ data, error }` (fara `throw`):
   `export async function getPoliticiLoialitate(clubId: string): Promise<{ data: PoliticaReducere[] | null; error: Error | null }>`
   Implementare: `supabase.from('politici_reducere').select('*').eq('club_id', clubId).eq('activ', true).not('reinnoiri_necesare', 'is', null)`; la eroare returneaza `{ data: null, error }`, la succes `{ data, error: null }`. Foloseste numele REALE de coloane confirmate in Task 1. Nu adauga alte functii aici.
  </action>
  <acceptance_criteria>
    - Cu `azi = new Date(2026, 8, 18)` si facturi Abonament achitate pentru (8/2026), (7/2026), (6/2026) → `numaraReinnoiriConsecutive` returneaza 3.
    - Aceeasi lista fara luna 7/2026 → returneaza 1 (se opreste la prima lipsa).
    - O singura factura cu `status='Achitat Parțial'` pentru 8/2026 → returneaza 0.
    - Lista goala → 0.
    - `calculeazaBonusLoialitate([], 10, 200)` → `sumaBonus` 0 si `politica` null.
    - Politica `{activ: true, reinnoiri_necesare: 6, tip_bonus: 'discount', procentaj: 10, valoare_fixa: null}` cu `reinnoiri=6`, `pretLista=200` → `sumaBonus` 20.
    - Politica `{activ: true, reinnoiri_necesare: 12, tip_bonus: 'zile_gratis'}` cu `reinnoiri=12`, `pretLista=200` → `sumaBonus` 200.
    - `npx tsx utils/loialitateReinnoiri.test.ts` iese cu cod 0 si tipareste doar linii `PASS:`.
    - `utils/loialitateReinnoiri.ts` nu importa Supabase.
    - `npm run lint` trece cu 0 erori.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "npx tsx utils/loialitateReinnoiri.test.ts"</automated>
    <automated>bash -lc "npm run lint &amp;&amp; ! grep -q 'supabase' utils/loialitateReinnoiri.ts &amp;&amp; grep -c \"from('politici_reducere')\" services/loialitateService.ts"</automated>
  </verify>
  <done>Testul colocat trece cu exit 0 pe toate cele 7 cazuri; calculul e pur si plafonat; serviciul respecta `{ data, error }`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (ADMIN_CLUB) → `politici_reducere` | Cine defineste politicile decide efectiv cati bani se incaseaza mai putin |
| `politici_reducere` → calculul sumei facturate (planul 30-05) | O politica manipulata reduce automat facturi reale |
| cross-club: `politici_reducere.club_id` → sportivii altui club | Fara RLS, o politica a unui club ar putea fi citita/aplicata pe alt club |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Information Disclosure | Citire cross-club a politicilor de reducere | mitigate | RLS obligatoriu de la crearea/extinderea tabelului, cu `public.has_access_to_club(club_id)` — verificat automat prin `pg_class.relrowsecurity = true` in Task 2; serviciul filtreaza suplimentar `.eq('club_id', clubId)` |
| T-30-04-02 | Tampering | `procentaj` sau `valoare_fixa` absurde (ex. 500%) produc facturi negative | mitigate | `calculeazaBonusLoialitate` plafoneaza `sumaBonus` in intervalul `[0, pretLista]`, testat explicit in testul colocat; DB pastreaza `NUMERIC(5,2)` pentru procentaj |
| T-30-04-03 | Elevation of Privilege (business logic) | Un ADMIN_CLUB isi seteaza `reinnoiri_necesare = 1` si reduce facturile propriului club | accept | Semnalat deja in 30-RESEARCH.md § Security Domain ca decizie de business (posibil restrictionare la SUPER_ADMIN_FEDERATIE); in afara scope-ului fazei 30 — se noteaza in SUMMARY ca risc deschis, nu se implementeaza tacit |
| T-30-04-04 | Tampering | Bonus aplicat de doua ori pe aceeasi factura | mitigate | `CREATE UNIQUE INDEX uq_aplicare_reduceri_plata ON aplicare_reduceri(plata_id)` (Task 2); efectul se verifica in planul 30-05 |
| T-30-04-05 | Repudiation | Reducere aplicata fara urma cine/cand/de ce | mitigate | Jurnal in `aplicare_reduceri` (politica_id, plata_id, suma_redusa, created_at) + `plati.suma_initiala` si `plati.reducereDetalii` completate de planul 30-05 |
| T-30-04-SC | Tampering | npm/pip/cargo installs | accept | Zero pachete noi (`date-fns` deja prezent) |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (planul 30-04)

**Fisiere noi:**
- `supabase/migrations/20260918d_loialitate_politici_reducere.sql`
- `utils/loialitateReinnoiri.ts`
- `utils/loialitateReinnoiri.test.ts`
- `services/loialitateService.ts`

**Coloane DB noi:**
- `public.politici_reducere.reinnoiri_necesare INTEGER` (+ CHECK `> 0`)
- `public.politici_reducere.tip_bonus TEXT` (+ CHECK `IN ('zile_gratis','discount')`)
- `public.aplicare_reduceri.plata_id UUID` — doar daca Task 1 arata ca lipseste o legatura catre plata
- indexul unic `uq_aplicare_reduceri_plata`
- (conditionat, doar daca tabelele lipsesc live) tabelele `public.politici_reducere` si `public.aplicare_reduceri` cu RLS `has_access_to_club(club_id)`

**Simboluri TypeScript noi:**
- `types.ts` → `export interface PoliticaReducere`
- `utils/loialitateReinnoiri.ts` → `export function numaraReinnoiriConsecutive(platiEntitate: Plata[], azi: Date): number`
- `utils/loialitateReinnoiri.ts` → `export function calculeazaBonusLoialitate(politici, reinnoiriConsecutive, pretLista): { sumaBonus, politica, detalii }`
- `utils/loialitateReinnoiri.test.ts` → `export function runTests(): { passed, failed, errors }`
- `services/loialitateService.ts` → `export async function getPoliticiLoialitate(clubId: string)`

**Fara pachete npm noi.**
</artifacts_this_phase_produces>

<verification>
1. SUMMARY contine schema live completa a `politici_reducere` / `aplicare_reduceri` / `reduceri` si raspunsurile Q1-Q4.
2. Live: `politici_reducere` are `reinnoiri_necesare` si `tip_bonus`, cu CHECK-uri active si RLS pornit.
3. `types.ts` are `PoliticaReducere` distinct de `Reducere`, cu comentariu care explica diferenta.
4. `npx tsx utils/loialitateReinnoiri.test.ts` iese cu cod 0 (7 cazuri).
5. `npm run lint` — 0 erori.
</verification>

<success_criteria>
- Nicio linie de cod nu a fost scrisa impotriva unei scheme presupuse — schema a fost verificata live intai.
- Confuzia `reduceri` vs `politici_reducere` e rezolvata explicit si documentata in tipuri.
- Bonusul de loialitate e calculabil determinist, plafonat, si gata de consumat de planul 30-05.
</success_criteria>

<output>
Creeaza `.planning/phases/30-imbunatatiri-inspirate-din-abonix-gratie-reinnoire-abonament/30-04-SUMMARY.md`, cu schema live completa, raspunsurile Q1-Q4 si coloanele exacte de folosit pentru jurnalul `aplicare_reduceri`.
</output>
