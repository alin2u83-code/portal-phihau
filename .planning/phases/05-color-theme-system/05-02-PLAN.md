---
phase: 05-color-theme-system
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - contexts/ThemeContext.tsx
  - components/ui.tsx
  - hooks/useDataProvider.ts
autonomous: true
requirements:
  - THEME-01
  - THEME-02

must_haves:
  truths:
    - "ThemeContext injectează cele 12 variabile --t-* în :root la mount cu prioritatea: user override > club default > DEFAULT_THEME"
    - "ThemeContext re-fetchuiește tema clubului la schimbarea activeRoleContext.club_id"
    - "Button primary și secondary folosesc style prop cu var(--t-primary)/var(--t-secondary) în loc de clase Tailwind hardcodate"
    - "Hover pe Button primary/secondary aplică --t-primary-hover/--t-secondary-hover via useState(isHovered)"
    - "AMBELE branch-uri din Button (as=label și button) au style prop și onMouseEnter/Leave"
  artifacts:
    - path: "contexts/ThemeContext.tsx"
      provides: "ThemeProvider, useTheme hook"
      exports: ["ThemeProvider", "useTheme"]
    - path: "components/ui.tsx"
      provides: "Button migrat la CSS vars pentru primary/secondary"
      contains: "var(--t-primary)"
  key_links:
    - from: "contexts/ThemeContext.tsx"
      to: "contexts/DataContext.tsx"
      via: "useData()"
      pattern: "useData"
    - from: "contexts/ThemeContext.tsx"
      to: "lib/themes.ts"
      via: "import PREDEFINED_THEMES, DEFAULT_THEME, applyTheme"
      pattern: "applyTheme"
    - from: "components/ui.tsx"
      to: "CSS vars --t-primary"
      via: "style prop inline"
      pattern: "var\\(--t-primary\\)"
---

<objective>
ThemeContext ca provider React cu injectare CSS vars + Button migrat la CSS vars.

Purpose: THEME-01 și THEME-02 sunt cerințele core — fără ele, sistemul de teme nu funcționează. ThemeContext este creierul sistemului: citește din DB, aplică pe DOM. Button este prima componentă care urmează tema.

Output: contexts/ThemeContext.tsx (nou), components/ui.tsx (modificat Button primary/secondary), hooks/useDataProvider.ts (adaugă tema_config la SELECT cluburi).
</objective>

<execution_context>
@C:/Users/lungu/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lungu/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-color-theme-system/05-CONTEXT.md
@.planning/phases/05-color-theme-system/05-RESEARCH.md
@.planning/phases/05-color-theme-system/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: contexts/ThemeContext.tsx — provider cu fetch + applyTheme</name>
  <files>contexts/ThemeContext.tsx, hooks/useDataProvider.ts</files>

  <read_first>
    - contexts/NavigationContext.tsx (complet): pattern exact de urmat — createContext, Provider, hook de export
    - contexts/DataContext.tsx (complet): cum se exportă useData(), tipul DataContextType
    - hooks/useDataProvider.ts (liniile 320-340): SELECT-ul curent pentru cluburi — adaugă tema_config la el
    - hooks/useDataProvider.ts (liniile 40-60): structura stateului inițial — clubs array
    - types.ts: structura Club interface și ThemeConfig (adăugat în Plan 01)
    - supabaseClient.ts (liniile 1-20): cum se importă clientul Supabase (export default sau named)
  </read_first>

  <action>
  Pasul 1 — Modifică hooks/useDataProvider.ts:

  Găsește linia cu SELECT pentru cluburi (linia ~329, caută `clubs: cleanedSupabase.from('cluburi').select(`). Adaugă `tema_config` la lista de câmpuri selectate:

  ÎNAINTE: `.select('id, nume, cif, oras, theme_config')`
  DUPĂ: `.select('id, nume, cif, oras, theme_config, tema_config')`

  Aceasta este singura modificare în useDataProvider.ts. ThemeContext va citi `tema_config` din clubs array via useData().

  Pasul 2 — Creează contexts/ThemeContext.tsx:

  Structura completă a fișierului:

  Importuri:
  - React, createContext, useContext, useState, useEffect, useCallback, ReactNode din react
  - useData din ./DataContext
  - supabase (clientul Supabase) din ../supabaseClient — verifică exact cum e exportat (default sau named) din supabaseClient.ts
  - ThemeConfig din ../types
  - PREDEFINED_THEMES, DEFAULT_THEME, applyTheme din ../lib/themes

  Interfața contextului:
  ```
  interface ThemeContextValue {
    currentTheme: ThemeConfig;
    setTheme: (theme: ThemeConfig) => void;
    saveTheme: (theme: ThemeConfig, scope: 'user' | 'club') => Promise<void>;
    predefinedThemes: ThemeConfig[];
  }
  ```

  ThemeProvider implementare:
  - const { activeRoleContext, currentUser, clubs } = useData()
  - useState<ThemeConfig>(DEFAULT_THEME) pentru currentTheme
  - useRef pentru a reține clubTheme (evită dependency loop)

  Effect 1 — pe mount și la schimbarea activeRoleContext.club_id sau clubs array:
  - Găsește clubul activ: clubs.find(c => c.id === activeRoleContext?.club_id)
  - Citește tema_config: activeClub?.tema_config as ThemeConfig | null
  - Salvează în ref pentru fallback
  - Dacă există, aplică temporar (va fi suprascrisă de effect-ul user dacă există override)

  Effect 2 — pe schimbarea currentUser?.id:
  - Dacă nu există currentUser.id, aplică clubTheme sau DEFAULT_THEME și returnează
  - Fetch: supabase.from('utilizatori').select('tema_config').eq('id', currentUser.id).single()
  - Prioritate: data?.tema_config (user) > clubThemeRef.current (club) > DEFAULT_THEME
  - Apelează applyTheme(resolvedTheme) și setCurrentTheme(resolvedTheme)

  setTheme: apelează applyTheme(theme) + setCurrentTheme(theme) — preview live fără save

  saveTheme:
  - scope 'user': supabase.from('utilizatori').update({ tema_config: theme }).eq('id', currentUser?.id)
  - scope 'club': supabase.from('cluburi').update({ tema_config: theme }).eq('id', activeRoleContext?.club_id)
  - Aruncă eroarea dacă există
  - Indiferent de scope, apelează applyTheme(theme) + setCurrentTheme(theme)

  Exporturi:
  - ThemeProvider (named export)
  - useTheme hook (named export) — aruncă eroare dacă e folosit în afara ThemeProvider

  ATENȚIE la ordinea effect-urilor: effect-ul pe club_id rulează la schimbarea clubs/activeRoleContext, effect-ul pe user.id rulează după și poate suprascrie cu user override. Folosește un ref `clubThemeRef` pentru a trece valoarea clubTheme de la effect 1 la effect 2.

  ATENȚIE: ThemeContext trebuie să aibă dependency pe activeRoleContext?.club_id în effect 1 (nu pe activeRoleContext obiectul întreg — evită re-render-uri infinite) și pe currentUser?.id în effect 2.
  </action>

  <verify>
    <automated>cd /mnt/c/Users/lungu/portal-phihau && npx tsc --noEmit 2>&1 | grep -E "ThemeContext|themes" | head -20</automated>
  </verify>

  <acceptance_criteria>
  - contexts/ThemeContext.tsx exportă ThemeProvider și useTheme
  - useTheme() returnează { currentTheme, setTheme, saveTheme, predefinedThemes }
  - predefinedThemes.length === 8 (vine din PREDEFINED_THEMES din lib/themes.ts)
  - ThemeContext.tsx importă useData din DataContext (verifică că nu există erori la tsc)
  - hooks/useDataProvider.ts linia cu SELECT cluburi conține tema_config
  - tsc --noEmit nu raportează erori în contexts/ThemeContext.tsx
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Button migration — primary/secondary la CSS vars cu hover state</name>
  <files>components/ui.tsx</files>

  <read_first>
    - components/ui.tsx (liniile 1-64): tot componenta Button — ambele branch-uri (as=label liniile 51-57, button liniile 59-63), variantClasses definit la liniile 30-37, finalClassName la linia 39
  </read_first>

  <action>
  Modifică NUMAI componenta Button din components/ui.tsx (liniile 1-64). Restul fișierului rămâne neatins.

  Schimbări necesare în interiorul funcției Button:

  1. Adaugă import useState dacă nu e deja importat la linia 1 (React, useState sunt deja importate — verifică).

  2. Adaugă state-ul hover ÎN INTERIORUL funcției Button (după destructurarea props, înainte de baseClasses):
     `const [isHovered, setIsHovered] = useState(false);`

  3. Modifică variantClasses — înlătură entry-urile primary și secondary care conțin bg/text Tailwind, înlocuiește-le cu clase FĂRĂ bg și FĂRĂ text, păstrând doar focus/shadow/active:
     - primary: "focus:ring-blue-500 shadow-md active:scale-95"
     - secondary: "focus:ring-slate-500 shadow-sm active:scale-95"
     - danger/success/info/warning: NESCHIMBATE

  4. Adaugă variantStyles după variantClasses:
     ```
     const variantStyles: Partial<Record<string, React.CSSProperties>> = {
       primary: {
         backgroundColor: isHovered ? 'var(--t-primary-hover)' : 'var(--t-primary)',
         color: 'var(--t-primary-fg)',
       },
       secondary: {
         backgroundColor: isHovered ? 'var(--t-secondary-hover)' : 'var(--t-secondary)',
         color: 'var(--t-secondary-fg)',
       },
     };
     ```

  5. Modifică AMBELE branch-uri de render:

  Branch `as === 'label'` (liniile 51-57) — adaugă style și handlers:
  ```tsx
  <label
    htmlFor={htmlFor}
    className={`${finalClassName} ${(disabled || isLoading) ? 'opacity-50 cursor-not-allowed' : ''}`}
    style={variantStyles[variant]}
    onMouseEnter={() => { if (variant === 'primary' || variant === 'secondary') setIsHovered(true); }}
    onMouseLeave={() => setIsHovered(false)}
    {...(props as any)}
  >
    {content}
  </label>
  ```

  Branch `<button>` (liniile 59-63) — adaugă style și handlers:
  ```tsx
  <button
    className={finalClassName}
    style={variantStyles[variant]}
    onMouseEnter={() => { if (variant === 'primary' || variant === 'secondary') setIsHovered(true); }}
    onMouseLeave={() => setIsHovered(false)}
    disabled={disabled || isLoading}
    {...props}
  >
    {content}
  </button>
  ```

  VERIFICARE POST-MODIFICARE: finalClassName rămâne definit ca înainte (nu pune style în ea). Linia cu finalClassName folosește variantClasses[variant] care acum nu mai conține bg-indigo-600 sau bg-slate-700 — verifică că nu mai există.

  IMPORTANT: Nu schimba nimic din restul ui.tsx (Card, Modal, Badge, Input, etc.) — asta e Sprint 2.
  </action>

  <verify>
    <automated>cd /mnt/c/Users/lungu/portal-phihau && grep -c "t-primary" components/ui.tsx</automated>
  </verify>

  <acceptance_criteria>
  - components/ui.tsx NU mai conține bg-indigo-600 sau bg-slate-700 în variantClasses (grep returneaza 0)
  - components/ui.tsx conține var(--t-primary) și var(--t-secondary) în variantStyles (grep returneaza >= 2 matches)
  - AMBELE branch-uri (label și button) au style={variantStyles[variant]} și onMouseEnter/Leave
  - isHovered state este declarat ÎNĂUNTRUL funcției Button (nu la nivel de modul)
  - danger/success/info/warning rămân cu clase Tailwind bg-* neschimbate
  - tsc --noEmit nu raportează erori noi în components/ui.tsx
  - Verificare vizuală: deschide app în dev server, un buton primary trebuie să fie albastru (#3b82f6 din --t-primary default)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThemeContext → supabase.from('utilizatori').update() | Self-update tema_config; RLS verifică auth.uid() = user_id |
| ThemeContext → supabase.from('cluburi').update() | ADMIN update tema_config club; RLS verifică rol |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | ThemeContext saveTheme('club') | mitigate | RLS policy cluburi_tema_config_admin_update (creat în Plan 01 SQL); UI ascunde butonul pentru non-ADMIN via usePermissions |
| T-05-05 | Tampering | ThemeContext saveTheme('user') | mitigate | RLS policy utilizatori_tema_config_self_update (creat în Plan 01 SQL) verifică auth.uid() = user_id |
| T-05-06 | Information Disclosure | useData().clubs expune tema_config | accept | clubs e vizibil deja pentru utilizatorii autentificați în contextul clubului lor; tema_config e date non-sensibile (culori hex) |
| T-05-07 | Denial of Service | applyTheme() setProperty în bucle | accept | Apelat max o dată la mount/role-switch; nu e în render loop |
</threat_model>

<verification>
Verificare automată:
- `grep -c "bg-indigo-600" components/ui.tsx` → 0 (nu mai există în variantClasses)
- `grep -c "t-primary" components/ui.tsx` → >= 2
- `grep "isHovered" components/ui.tsx` → linia cu useState(false) + 2 linii handlers
- `grep "tema_config" hooks/useDataProvider.ts` → apare în SELECT clubs
- tsc --noEmit fără erori noi

Verificare vizuală (dev server):
- Button primary albastru în aplicație (fallback din index.css --t-primary: #3b82f6)
- Button secondary are culoarea din --t-secondary (#1e293b — aproape identic cu vechea culoare bg-slate-800)
</verification>

<success_criteria>
- ThemeContext funcțional: la mount aplică DEFAULT_THEME (QwanKiDo Blue) pe :root — verificabil în Chrome DevTools: document.documentElement.style.getPropertyValue('--t-primary') returnează '#3b82f6'
- Button primary/secondary urmează --t-* CSS vars — vizibil în aplicație
- Hover pe Button aplică culoarea hover corect (--t-primary-hover)
- La schimbarea rolului (club diferit), ThemeContext re-fetchuiește și re-aplică tema clubului nou
</success_criteria>

<output>
Creează `.planning/phases/05-color-theme-system/05-02-SUMMARY.md` când termini.
</output>
