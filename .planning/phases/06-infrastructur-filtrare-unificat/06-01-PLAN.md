---
phase: 06-infrastructur-filtrare-unificat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useCompetitieFilters.ts
  - components/Competitii/CompetitieFilterBar.tsx
autonomous: true
requirements: [INFR-01, INFR-02, INFR-03]

must_haves:
  truths:
    - "hooks/useCompetitieFilters.ts exports useCompetitieFilters returning { filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive }"
    - "hooks/useCompetitieFilters.ts exports a pure aplicaFiltreCategorie(categorii, filtre) that returns CategorieCompetitie[]"
    - "hooks/useCompetitieFilters.ts exports the CompetitieFiltre interface"
    - "components/Competitii/CompetitieFilterBar.tsx exports a presentational CompetitieFilterBar with zero state except filtreVisible toggle"
    - "tsc --noEmit passes with both new files in the tree"
  artifacts:
    - path: "hooks/useCompetitieFilters.ts"
      provides: "Filter state hook + CompetitieFiltre interface + aplicaFiltreCategorie pure function"
      exports: ["useCompetitieFilters", "aplicaFiltreCategorie", "CompetitieFiltre"]
      min_lines: 60
    - path: "components/Competitii/CompetitieFilterBar.tsx"
      provides: "Collapsible presentational filter bar"
      exports: ["CompetitieFilterBar", "CompetitieFilterBarProps"]
      min_lines: 80
  key_links:
    - from: "components/Competitii/CompetitieFilterBar.tsx"
      to: "hooks/useCompetitieFilters.ts"
      via: "import type { CompetitieFiltre }"
      pattern: "import.*CompetitieFiltre.*useCompetitieFilters"
---

<objective>
Crează infrastructura de filtrare reutilizabilă pentru modulul Competiții: un hook de stare (`useCompetitieFilters`), o funcție pură de filtrare (`aplicaFiltreCategorie`) co-locată în același fișier, și o componentă prezentațională collapsibilă (`CompetitieFilterBar`) condusă 100% prin props. Aceasta este o extracție 1:1 din logica testată în producție din `InscrieriView.tsx`, adaptată la API-ul grupat decis în CONTEXT (D-01 → D-14).

Purpose: Phase 7 va monta această infrastructură identic pe 4 tab-uri (Categorii, Înscrieri, Raport, Template), eliminând duplicarea logicii de filtrare. Phase 6 NU modifică niciun tab existent — produce doar artefactele shared (INFR-01, INFR-02, INFR-03).

Output: `hooks/useCompetitieFilters.ts` (hook + interface + funcție pură) și `components/Competitii/CompetitieFilterBar.tsx` (componentă UI). Verificabile end-to-end prin `tsc --noEmit`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructur-filtrare-unificat/06-CONTEXT.md
@.planning/phases/06-infrastructur-filtrare-unificat/06-RESEARCH.md
@.planning/phases/06-infrastructur-filtrare-unificat/06-PATTERNS.md
@.planning/phases/06-infrastructur-filtrare-unificat/06-UI-SPEC.md
@components/Competitii/InscrieriView.tsx
@hooks/useFilteredData.ts
@types.ts
</context>

## Phase Goal

**As a** admin sau instructor al unui club QwanKiDo, **I want to** dispun de un hook și o componentă de filtrare unificate (gen, vârstă, grad, probă), **so that** orice tab din modulul Competiții poate filtra consistent fără cod duplicat.

## Artifacts this phase produces

Symbols created by Phase 6 (excluded from source-grounding drift verification):

- `useCompetitieFilters` — named function export in `hooks/useCompetitieFilters.ts`
- `aplicaFiltreCategorie` — named function export in `hooks/useCompetitieFilters.ts`
- `CompetitieFiltre` — exported interface in `hooks/useCompetitieFilters.ts`
- `FILTRE_INITIALE` — module-level const in `hooks/useCompetitieFilters.ts` (not exported)
- `CompetitieFilterBar` — named React component export in `components/Competitii/CompetitieFilterBar.tsx`
- `CompetitieFilterBarProps` — exported interface in `components/Competitii/CompetitieFilterBar.tsx`
- New files: `hooks/useCompetitieFilters.ts`, `components/Competitii/CompetitieFilterBar.tsx`

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define CompetitieFiltre contract + aplicaFiltreCategorie pure function</name>
  <files>hooks/useCompetitieFilters.ts</files>
  <read_first>
    - hooks/useCompetitieFilters.ts (does not exist yet — confirm absence, this task creates it)
    - hooks/useFilteredData.ts (file-structure analog: import style, named-export-only, no default export)
    - components/Competitii/InscrieriView.tsx lines 36-84 (source filter state) and lines 44-58 (source AND-filter logic to port)
    - types.ts lines 235-242 (Grad: ordine:number, nume:string), 611-630 (ProbaCompetitie, CategorieCompetitie with gen, varsta_min, varsta_max:number|null, proba_id:string|null, grad_min_ordine:number|null, grad_max_ordine:number|null)
    - .planning/phases/06-infrastructur-filtrare-unificat/06-PATTERNS.md (verified port of state + pure function with isNaN guards)
  </read_first>
  <behavior>
    - Interface CompetitieFiltre has exactly: gen:Set&lt;string&gt;, probaId:string, varstaMin:string, varstaMax:string, gradMin:string, gradMax:string (D-02)
    - aplicaFiltreCategorie returns the full categorii array unchanged when no filter is active (areFiltre false)
    - aplicaFiltreCategorie returns CategorieCompetitie[] (array), NOT Set&lt;string&gt; — intentional divergence from source per D-07
    - Categorie excluded when filtre.gen non-empty and !filtre.gen.has(cat.gen)
    - Categorie excluded when filtre.probaId set and cat.proba_id !== filtre.probaId
    - Categorie excluded when varstaMin set, numeric, and cat.varsta_min &lt; Number(varstaMin)
    - Categorie excluded when varstaMax set, numeric, and (cat.varsta_max === null OR cat.varsta_max &gt; Number(varstaMax))
    - Categorie excluded when gradMin set, numeric, and (cat.grad_min_ordine === null OR cat.grad_min_ordine &lt; Number(gradMin))
    - Categorie excluded when gradMax set, numeric, and (cat.grad_max_ordine === null OR cat.grad_max_ordine &gt; Number(gradMax))
  </behavior>
  <action>
    Create `hooks/useCompetitieFilters.ts`. Begin with `import { useState, useMemo } from 'react';` and `import type { CategorieCompetitie, ProbaCompetitie, Grad } from '../types';` (Grad/ProbaCompetitie are needed for the component props type but import them here only if used in this file's signatures — ProbaCompetitie and Grad are NOT used by the pure function, so import only CategorieCompetitie unless tsc reports unused; keep imports minimal). Export the `CompetitieFiltre` interface with the six fields named in &lt;behavior&gt; (do NOT add it to types.ts — it is UI state, not domain; per RESEARCH Anti-Patterns A1). Define a module-level const `FILTRE_INITIALE: CompetitieFiltre` with gen:new Set(), probaId:'', varstaMin:'', varstaMax:'', gradMin:'', gradMax:'' (D-05). Export `function aplicaFiltreCategorie(categorii: CategorieCompetitie[], filtre: CompetitieFiltre): CategorieCompetitie[]` (D-06, D-07) implementing the AND logic from InscrieriView.tsx:44-58 with the per-field exclusion rules in &lt;behavior&gt;. Add `!isNaN(Number(...))` guards on each numeric comparison (varstaMin, varstaMax, gradMin, gradMax) per RESEARCH §Security V5 / ASVS L1. Use `import type` for the domain types per project convention. Named exports only, no default export, matching hooks/useFilteredData.ts.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/useCompetitieFilters.ts` exists and `npm run lint` (tsc --noEmit) reports zero errors
    - Source contains `export interface CompetitieFiltre` with the six exact field names: gen, probaId, varstaMin, varstaMax, gradMin, gradMax
    - Source contains `export function aplicaFiltreCategorie` with return type annotation `: CategorieCompetitie[]`
    - Source contains a module-level `const FILTRE_INITIALE` defined outside any function
    - Source contains `!isNaN(Number(` on the four numeric range comparisons
    - File uses named exports only (no `export default`)
  </acceptance_criteria>
  <done>CompetitieFiltre interface, FILTRE_INITIALE const, and pure aplicaFiltreCategorie exist and type-check; INFR-03 satisfied at contract level.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement useCompetitieFilters hook (state + toggle/set/reset + nrFiltreActive)</name>
  <files>hooks/useCompetitieFilters.ts</files>
  <read_first>
    - hooks/useCompetitieFilters.ts (current state after Task 1 — interface + FILTRE_INITIALE + pure function already present)
    - components/Competitii/InscrieriView.tsx lines 60-67 (nrFiltreActive useMemo source) and lines 69-75 (toggleGen source)
    - .planning/phases/06-infrastructur-filtrare-unificat/06-RESEARCH.md §Pattern 1 (grouped-object hook shape) and §Pitfall 1 (FILTRE_INITIALE referential-stability)
  </read_first>
  <behavior>
    - Hook returns object: { filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive } (D-01)
    - toggleGen(gen): adds gen to filtre.gen if absent, removes if present, on an immutable new Set (D-03)
    - setFiltre(partial): merges partial into filtre via spread, leaving other fields untouched (D-04)
    - resetFiltre(): restores all fields to initial; gen becomes a fresh empty Set, not the shared FILTRE_INITIALE.gen reference (D-05, Pitfall 1)
    - nrFiltreActive: 1 if gen.size&gt;0; +1 if probaId truthy; +1 if varstaMin or varstaMax non-empty; +1 if gradMin or gradMax non-empty (max 4)
  </behavior>
  <action>
    In `hooks/useCompetitieFilters.ts`, add and export `function useCompetitieFilters()` (D-01) below the pure function. Use `const [filtre, setFiltreState] = useState&lt;CompetitieFiltre&gt;(FILTRE_INITIALE);`. Implement `toggleGen(gen: string)` updating via `setFiltreState(prev => { const next = new Set(prev.gen); if (next.has(gen)) next.delete(gen); else next.add(gen); return { ...prev, gen: next }; })` (ported from InscrieriView.tsx:69-75, adapted to grouped state). Implement `setFiltre(partial: Partial&lt;CompetitieFiltre&gt;)` as `setFiltreState(prev => ({ ...prev, ...partial }))` (D-04). Implement `resetFiltre()` as `setFiltreState({ ...FILTRE_INITIALE, gen: new Set() })` — the explicit `gen: new Set()` avoids reusing FILTRE_INITIALE's Set reference (RESEARCH Pitfall 1). Compute `nrFiltreActive` with `useMemo` over `[filtre]` using the counting rules in &lt;behavior&gt; (ported from InscrieriView.tsx:60-67). Return the grouped object `{ filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive }`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` (tsc --noEmit) reports zero errors
    - Source contains `export function useCompetitieFilters`
    - Hook return statement lists exactly: filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive
    - resetFiltre body constructs a fresh `new Set()` for gen (not bare FILTRE_INITIALE)
    - nrFiltreActive is computed inside a `useMemo` with dependency array `[filtre]`
    - toggleGen constructs `new Set(prev.gen)` (immutable update, not mutating prev.gen)
  </acceptance_criteria>
  <done>useCompetitieFilters returns the full grouped API and type-checks; INFR-01 satisfied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build CompetitieFilterBar presentational component</name>
  <files>components/Competitii/CompetitieFilterBar.tsx</files>
  <read_first>
    - components/Competitii/CompetitieFilterBar.tsx (does not exist yet — this task creates it)
    - hooks/useCompetitieFilters.ts (final state — import CompetitieFiltre from here, NOT from types.ts)
    - components/Competitii/InscrieriView.tsx lines 1-28 (named-export component + props interface style) and lines 134-236 (filter panel UI to port: toggle button, collapsible panel, gen chips, probă select, vârstă inputs)
    - components/Competitii/CategoriiTemplateManager.tsx line 462 (active-filters badge pattern)
    - .planning/phases/06-infrastructur-filtrare-unificat/06-UI-SPEC.md (full visual + interaction + copywriting contract — authoritative for classes, copy, group order)
    - .planning/phases/06-infrastructur-filtrare-unificat/06-PATTERNS.md (verified JSX for each control, incl. grad &lt;select&gt; with ordine-as-value)
  </read_first>
  <behavior>
    - Component is presentational: the ONLY internal state allowed is `const [filtreVisible, setFiltreVisible] = useState(false)` (D-12, A2 collapsed at mount)
    - All filter values and mutators come from props (D-11): filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive, probe, grade
    - Toggle button label is "Filtrează" when nrFiltreActive===0, "Filtrează (N)" when N&gt;0; chevron rotates 180° when filtreVisible
    - Reset link rendered only when nrFiltreActive &gt; 0, calls resetFiltre
    - Collapsible panel rendered only when filtreVisible === true
    - Gen chips: hardcoded ['Feminin','Masculin','Mixt'], each a label wrapping a controlled checkbox checked by filtre.gen.has(gen), onChange toggleGen(gen)
    - Probă: native select, value=filtre.probaId, first option value="" "Toate probele", maps probe[] to option value={p.id} label {p.denumire}
    - Vârstă: two type=number min=0 inputs, value filtre.varstaMin/varstaMax, onChange setFiltre({varstaMin:...})/{varstaMax:...}
    - Grad: two native selects, value=filtre.gradMin/gradMax, options from [...grade].sort by ordine, option value={g.ordine.toString()} label {g.nume}, first option value="" (D-10, Pitfall 5 — value is ordine string NOT id)
  </behavior>
  <action>
    Create `components/Competitii/CompetitieFilterBar.tsx` as a named-export React FC (consistent with InscrieriView named export; RESEARCH Open Q2). Imports: `import React, { useState } from 'react';`, `import type { ProbaCompetitie, Grad } from '../../types';`, `import type { CompetitieFiltre } from '../../hooks/useCompetitieFilters';` (import CompetitieFiltre from the hook, never types.ts — RESEARCH Pitfall 3 forbids the reverse import direction). Export interface `CompetitieFilterBarProps` with the seven props in &lt;behavior&gt; (D-11): filtre:CompetitieFiltre, toggleGen:(gen:string)=&gt;void, setFiltre:(partial:Partial&lt;CompetitieFiltre&gt;)=&gt;void, resetFiltre:()=&gt;void, nrFiltreActive:number, probe:ProbaCompetitie[], grade:Grad[]. Inside the component declare the single allowed state `const [filtreVisible, setFiltreVisible] = useState(false)`. Render the toggle button + reset link + collapsible 2-column grid panel (group order Gen → Probă → Vârstă → Grad per UI-SPEC §Visual Order). Use the exact Tailwind classes, SVG icons, copy strings, and JSX structure from 06-PATTERNS.md and 06-UI-SPEC.md: toggle uses `style={{ touchAction: 'manipulation' }}` and the `bg-brand-primary/20 border-brand-primary/50 text-brand-primary` active variant; badge follows the CategoriiTemplateManager.tsx:462 pattern; grad selects bind `value={g.ordine.toString()}` (NOT g.id) per Pitfall 5. Keep using the existing `bg-brand-primary/*` Tailwind classes verbatim — do NOT swap them for `var(--t-primary)` inline styles (UI-SPEC §Phase 5 Integration marks that as Phase 7 out-of-scope). No `components/ui.tsx` import is required; styling is 100% Tailwind inline as in InscrieriView.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `components/Competitii/CompetitieFilterBar.tsx` exists and `npm run lint` (tsc --noEmit) reports zero errors
    - Source contains `import type { CompetitieFiltre } from '../../hooks/useCompetitieFilters'`
    - Source contains `export const CompetitieFilterBar` (named export, no default)
    - Source contains `export interface CompetitieFilterBarProps` listing all 7 props
    - The only useState in the file is `useState(false)` bound to filtreVisible (no other useState/useReducer)
    - Grad option value uses `.ordine.toString()` and NOT `g.id`
    - Gen chips array literal is exactly `['Feminin', 'Masculin', 'Mixt']`
    - Toggle button contains `touchAction: 'manipulation'`
  </acceptance_criteria>
  <done>CompetitieFilterBar renders the full collapsible filter UI driven entirely by props, type-checks, and matches the UI-SPEC contract; INFR-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → filter logic | Numeric range inputs (vârstă, grad) are free-text-derived strings cast via Number() before comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | aplicaFiltreCategorie numeric casts | mitigate | Guard every Number() comparison with `!isNaN(Number(...))` so manipulated non-numeric input cannot produce NaN comparisons (Task 1, ASVS L1 V5) |
| T-06-02 | Information Disclosure | client-side filter only | accept | No new Supabase query, no RLS surface change; filtering operates on already-loaded in-memory data (RESEARCH §Security). Low risk. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH §Package Legitimacy — not applicable). No install tasks present. |
</threat_model>

<verification>
- `npm run lint` (tsc --noEmit) passes with both new files present.
- `hooks/useCompetitieFilters.ts` exports `useCompetitieFilters`, `aplicaFiltreCategorie`, and `CompetitieFiltre`.
- `components/Competitii/CompetitieFilterBar.tsx` exports `CompetitieFilterBar` and `CompetitieFilterBarProps`, importing `CompetitieFiltre` from the hook.
- No existing tab component (InscrieriView.tsx, index.tsx, CategoriiTemplateManager.tsx, RaportInscrieri.tsx) is modified.
- No new npm package added to package.json.

### End-of-phase human verification (human_verify_mode=end-of-phase)

<human-check>
Since Phase 6 produces shared artefacts not yet wired into any tab, there is no rendered UI to inspect in the running app. Verify instead:
1. Run `npm run lint` — must report zero errors.
2. Open `hooks/useCompetitieFilters.ts` and confirm the exported hook returns { filtre, toggleGen, setFiltre, resetFiltre, nrFiltreActive } and that `aplicaFiltreCategorie` returns an array.
3. Open `components/Competitii/CompetitieFilterBar.tsx` and confirm it is props-driven (only `filtreVisible` local state) and the grad selects bind `ordine`, not `id`.
Type "approved" or describe issues.
</human-check>
</verification>

<success_criteria>
- INFR-01: `useCompetitieFilters` hook exported with full grouped state + toggle/set/reset + nrFiltreActive — zero UI logic.
- INFR-02: `CompetitieFilterBar` component exported, props-driven, renders gen chips + probă select + vârstă inputs + grad selects + active-filters badge — only `filtreVisible` internal state.
- INFR-03: pure `aplicaFiltreCategorie(categorii, filtre)` exported from the hook file, returns filtered `CategorieCompetitie[]` via AND logic with NaN guards.
- `tsc --noEmit` clean; no existing tab touched; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/06-infrastructur-filtrare-unificat/06-01-SUMMARY.md` when done.
</output>
