---
phase: 18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migrations/consolidare_trigger_grad_actual_260906.sql
  - .planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-AUDIT-DATE-CORUPTE.md
autonomous: false
execution_requirements:
  supabase_mcp: required
  worktree_subagent: forbidden
requirements: [D-01, D-02, D-05, D-06, D-07, D-08, D-09, D-10]

must_haves:
  truths:
    - "Un singur trigger activ pe public.istoric_grade sincronizeaza sportivi.grad_actual_id (plus trigger_ajusteaza_debutant_la_import, nemodificat)"
    - "Inserarea unui grad INFERIOR cu data mai recenta NU retrogradeaza sportivul (regula MAX(ordine), D-01)"
    - "Stergerea unui rand din istoric_grade recalculeaza grad_actual_id la MAX(ordine) ramas (D-02)"
    - "metoda_selectie_grad ramane populata cu 'automat' dupa recalcul (folosita de components/UserProfile.tsx)"
    - "tr_sync_grad_history pe public.sportivi nu mai exista (sursa datei gresite CURRENT_DATE, D-07)"
    - "Utilizatorul primeste un raport cu randurile istoric_grade suspecte, fara nicio corectie automata (D-10)"
  artifacts:
    - path: "sql/migrations/consolidare_trigger_grad_actual_260906.sql"
      provides: "Migratia versionata in git a consolidarii trigger-elor (pana acum trigger-ele traiau doar live pe DB)"
      contains: "sync_grad_actual_canonical"
      min_lines: 40
    - path: ".planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-AUDIT-DATE-CORUPTE.md"
      provides: "Raport D-10: randuri istoric_grade cu data suspecta + sportivi cu grad_actual_id divergent de MAX(ordine)"
      min_lines: 20
  key_links:
    - from: "public.istoric_grade (INSERT/UPDATE/DELETE)"
      to: "public.sportivi.grad_actual_id"
      via: "trigger trg_sync_grad_actual_canonical -> functia sync_grad_actual_canonical()"
      pattern: "trg_sync_grad_actual_canonical"
    - from: "sync_grad_actual_canonical()"
      to: "public.sportivi.metoda_selectie_grad"
      via: "UPDATE ... SET metoda_selectie_grad = 'automat'"
      pattern: "metoda_selectie_grad"
---

<objective>
Consolideaza cele 4 trigger-e redundante/contradictorii de pe `public.istoric_grade` plus trigger-ul defect `tr_sync_grad_history` de pe `public.sportivi` intr-un SINGUR trigger canonic care deriva `sportivi.grad_actual_id` ca MAX(grade.ordine) din tot istoricul sportivului, pe INSERT, UPDATE si DELETE (D-01, D-02, D-05, D-06, D-07).

Purpose: `grad_actual_id` devine strict derivat, cu o singura cale de scriere. Elimina jumatatea de DB a race-condition-ului descris in 18-RESEARCH.md ("Live Verification" #2): azi `tr_sync_grad_history` insereaza in `istoric_grade` cu `CURRENT_DATE` in paralel cu insertul frontend-ului care are data reala a examenului, iar `ON CONFLICT ... DO NOTHING` pe `istoric_grade_sportiv_grad_unique` face ca oricare dintre cele doua scrieri ajunge prima la Postgres sa castige slotul — cealalta dispare fara eroare.

Output: fisier de migratie versionat in git + migratie aplicata live prin Supabase MCP + dovada functionala (smoke test SQL cu asertii) + raport de audit D-10.

**ATENTIE EXECUTIE:** acest plan NU poate fi rulat de un subagent gsd-executor in worktree — vezi `<execution_requirements>` de mai jos.
</objective>

<execution_requirements>
## Acest plan trebuie rulat INLINE, in sesiunea cu acces Supabase MCP

Trigger-ele si functiile acestui proiect traiesc EXCLUSIV pe proiectul Supabase `wuhidifzsutwgdfkwhmd` — nu exista in niciun fisier de migratie comis (confirmat de 18-RESEARCH.md si de memoria proiectului `feedback_audit_rls_verifica_live_nu_doar_migratii`). Singura cale de aplicare este `mcp__supabase__apply_migration` / `mcp__supabase__execute_sql`.

Per memoria proiectului `feedback_gsd_worktree_infra_esuata_execute_inline`, un subagent dispatch-uit in git worktree in acest mediu Windows NU are, tipic, acces la tool-urile `mcp__supabase__*`. Sesiunea orchestrator/inline are.

**Regula pentru /gsd-execute-phase:** ruleaza acest plan inline (fara `isolation=worktree`, fara subagent). Daca la Task 2 tool-urile `mcp__supabase__apply_migration` / `mcp__supabase__execute_sql` nu sunt disponibile, OPRESTE-TE si raporteaza — nu incerca fallback prin `supabase` CLI sau `psql` (ambele confirmate absente in acest mediu de 18-RESEARCH.md, sectiunea "Environment Availability").
</execution_requirements>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-CONTEXT.md
@.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-RESEARCH.md
@.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-PATTERNS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (simboluri noi, plan 01)

| Simbol | Tip | Locatie | Consumat de |
|--------|-----|---------|-------------|
| `public.sync_grad_actual_canonical()` | functie PL/pgSQL `RETURNS TRIGGER`, `SECURITY DEFINER SET search_path = public` | Supabase live + `sql/migrations/consolidare_trigger_grad_actual_260906.sql` | trigger-ul `trg_sync_grad_actual_canonical` |
| `trg_sync_grad_actual_canonical` | trigger `AFTER INSERT OR UPDATE OR DELETE ON public.istoric_grade FOR EACH ROW` | Supabase live + acelasi fisier de migratie | toate scrierile in `istoric_grade` (frontend, import, UserProfile) |
| `sql/migrations/consolidare_trigger_grad_actual_260906.sql` | fisier SQL versionat | repo | referinta viitoare / re-aplicare |
| `18-AUDIT-DATE-CORUPTE.md` | raport markdown | `.planning/phases/18-.../` | decizia manuala a utilizatorului (D-10) |

**Obiecte ELIMINATE (trigger-e, NU functii):** `trg_after_history_change`, `trg_sync_grad_actual_from_istoric`, `trg_sync_grade_on_history_change`, `trg_sync_grad_actual_manual` (toate pe `istoric_grade`), `tr_sync_grad_history` (pe `sportivi`).

**Obiect PASTRAT neatins (D-08):** `trigger_ajusteaza_debutant_la_import` -> `ajusteaza_debutant_la_import_examen` pe `istoric_grade`.

**Functii lasate definite fara DROP FUNCTION** (devin inerte fara trigger care le apeleaza — acelasi pattern ca Faza 25-04 cu `get_my_club_ids`/`get_my_clubs`): `fn_sync_sportiv_grad_from_history`, `sync_grad_actual_from_istoric_grade`, `fn_refresh_sportiv_grade`, `fn_sync_grad_to_history`, `sync_grad_actual_on_manual_grade`.
</artifacts_this_phase_produces>

<migration_sql>
## SQL canonic al migratiei (de copiat verbatim in fisier, Task 1)

```sql
-- =============================================================================
-- Faza 18 — Consolidare trigger-e grad_actual_id (sursa unica de adevar)
-- Data: 2026-09-06
-- Proiect Supabase: wuhidifzsutwgdfkwhmd
--
-- PROBLEMA (mecanism confirmat live, vezi 18-RESEARCH.md "Live Verification"):
--   Nu e o suprascriere literala, ci un RACE CONDITION intre doua scrieri
--   concurente pe aceeasi cheie unica (sportiv_id, grad_id), ambele cu
--   ON CONFLICT ... DO NOTHING:
--     (a) frontend-ul insereaza in istoric_grade cu data REALA a examenului;
--     (b) acelasi frontend face UPDATE sportivi.grad_actual_id, care declanseaza
--         tr_sync_grad_history -> fn_sync_grad_to_history, care insereaza in
--         istoric_grade cu data_obtinere = CURRENT_DATE.
--   Cine ajunge primul la Postgres castiga slotul unic; celalalt e no-op tacut.
--   In ManagementInscrieri.tsx cele doua scrieri sunt in acelasi Promise.all
--   => corupere reala de date (data examenului inlocuita cu data rularii).
--
-- SOLUTIA: grad_actual_id devine STRICT derivat dintr-un singur trigger pe
--   istoric_grade, cu regula MAX(grade.ordine) (D-01), activ si pe DELETE (D-02).
--   Frontend-ul nu mai scrie niciodata direct grad_actual_id (D-03/D-04, planurile
--   18-02 si 18-03) => scriitorul (b) dispare => race-ul dispare.
--
-- ORDINE: CREATE noul trigger INAINTE de DROP-urile vechi, ca sa nu existe
--   fereastra fara niciun mecanism de sincronizare (18-RESEARCH.md, Pitfall 4).
-- =============================================================================

BEGIN;

-- 1. Functia canonica unica (D-01, D-02, D-06).
--    SECURITY DEFINER obligatoriu: scrie pe public.sportivi indiferent de RLS-ul
--    apelantului, la fel ca functiile pe care le inlocuieste (vezi si bug-ul
--    tr_automatizeaza_roluri fixat in Faza 25-04: trigger fara SECURITY DEFINER
--    -> "permission denied for table users").
CREATE OR REPLACE FUNCTION public.sync_grad_actual_canonical()
RETURNS TRIGGER AS $$
DECLARE
  v_sportiv_id uuid;
  v_max_grad_id uuid;
BEGIN
  v_sportiv_id := COALESCE(NEW.sportiv_id, OLD.sportiv_id);

  IF v_sportiv_id IS NULL THEN
    RETURN COALESCE(NEW, OLD);
  END IF;

  -- Cel mai mare grad obtinut VREODATA (NU cel mai recent cronologic) — D-01.
  -- Tiebreak determinist daca doua grade ar avea acelasi "ordine".
  SELECT ig.grad_id
    INTO v_max_grad_id
  FROM public.istoric_grade ig
  JOIN public.grade g ON g.id = ig.grad_id
  WHERE ig.sportiv_id = v_sportiv_id
  ORDER BY g.ordine DESC, ig.data_obtinere DESC, ig.id DESC
  LIMIT 1;

  -- Daca istoricul a ramas gol (toate randurile sterse), grad_actual_id devine NULL
  -- — consecinta deliberata a regulii "strict derivat" (D-04).
  UPDATE public.sportivi
     SET grad_actual_id = v_max_grad_id,
         metoda_selectie_grad = 'automat'
   WHERE id = v_sportiv_id
     AND grad_actual_id IS DISTINCT FROM v_max_grad_id;

  RETURN COALESCE(NEW, OLD);
END;
$$ LANGUAGE plpgsql SECURITY DEFINER SET search_path = public;

-- 2. Trigger-ul canonic unic — creat INAINTE de DROP-uri.
DROP TRIGGER IF EXISTS trg_sync_grad_actual_canonical ON public.istoric_grade;
CREATE TRIGGER trg_sync_grad_actual_canonical
AFTER INSERT OR UPDATE OR DELETE ON public.istoric_grade
FOR EACH ROW EXECUTE FUNCTION public.sync_grad_actual_canonical();

-- 3. Elimina cele 4 trigger-e redundante de pe istoric_grade (D-05, D-06).
DROP TRIGGER IF EXISTS trg_after_history_change ON public.istoric_grade;
DROP TRIGGER IF EXISTS trg_sync_grad_actual_from_istoric ON public.istoric_grade;
DROP TRIGGER IF EXISTS trg_sync_grade_on_history_change ON public.istoric_grade;
DROP TRIGGER IF EXISTS trg_sync_grad_actual_manual ON public.istoric_grade;

-- 4. Elimina trigger-ul cu data gresita de pe sportivi (D-07).
DROP TRIGGER IF EXISTS tr_sync_grad_history ON public.sportivi;

-- 5. NU se atinge trigger_ajusteaza_debutant_la_import (D-08) si nu se sterge
--    constraint-ul istoric_grade_sportiv_grad_unique (D-09).
--    Functiile vechi raman definite, inerte (pattern Faza 25-04).

COMMIT;
```
</migration_sql>

<verification_sql>
## A. Inventar trigger-e (rulat dupa aplicare, Task 2)

```sql
SELECT tgname, tgrelid::regclass AS tabel, tgfoid::regproc AS functie
FROM pg_trigger
WHERE NOT tgisinternal
  AND tgrelid IN ('public.istoric_grade'::regclass, 'public.sportivi'::regclass)
ORDER BY tabel::text, tgname;
```

Rezultat asteptat pe `istoric_grade`: exact `trg_sync_grad_actual_canonical` + `trigger_ajusteaza_debutant_la_import`. Zero randuri cu numele celor 4 trigger-e sterse. Pe `sportivi`: `tr_sync_grad_history` ABSENT (alte trigger-e existente, ex. audit, raman si sunt in afara scope-ului).

## B. Smoke test functional cu asertii (Task 2)

Ruleaza ca un singur `execute_sql`. Blocul e o singura tranzactie: daca orice asertie pica, `RAISE EXCEPTION` face rollback complet, inclusiv sportivului de test. La succes, ultimele doua DELETE-uri fac curatenie explicita.

```sql
DO $$
DECLARE
  v_club uuid; v_low uuid; v_high uuid;
  v_sportiv uuid; v_actual uuid; v_expected uuid; v_metoda text;
BEGIN
  SELECT id INTO v_club FROM public.cluburi LIMIT 1;
  SELECT id INTO v_low  FROM public.grade ORDER BY ordine ASC  LIMIT 1;
  SELECT id INTO v_high FROM public.grade ORDER BY ordine DESC LIMIT 1;

  INSERT INTO public.sportivi (nume, prenume, club_id, status, data_inscrierii)
  VALUES ('ZZ_TEST_TRIGGER', 'Faza18', v_club, 'Inactiv', CURRENT_DATE)
  RETURNING id INTO v_sportiv;

  -- A) grad MARE cu data VECHE -> grad_actual_id = grad mare, metoda = 'automat'
  INSERT INTO public.istoric_grade (sportiv_id, grad_id, data_obtinere, observatii)
  VALUES (v_sportiv, v_high, DATE '2020-01-01', 'ZZ_TEST faza 18');
  SELECT grad_actual_id, metoda_selectie_grad INTO v_actual, v_metoda
    FROM public.sportivi WHERE id = v_sportiv;
  IF v_actual IS DISTINCT FROM v_high THEN
    RAISE EXCEPTION 'FAIL A: grad_actual_id=% asteptat=%', v_actual, v_high;
  END IF;
  IF v_metoda IS DISTINCT FROM 'automat' THEN
    RAISE EXCEPTION 'FAIL A2 (UserProfile.tsx): metoda_selectie_grad=%', v_metoda;
  END IF;

  -- B) grad MIC cu data RECENTA -> NU retrogradeaza (D-01, MAX(ordine))
  INSERT INTO public.istoric_grade (sportiv_id, grad_id, data_obtinere, observatii)
  VALUES (v_sportiv, v_low, CURRENT_DATE, 'ZZ_TEST faza 18');
  SELECT grad_actual_id INTO v_actual FROM public.sportivi WHERE id = v_sportiv;
  IF v_actual IS DISTINCT FROM v_high THEN
    RAISE EXCEPTION 'FAIL B (D-01 incalcat: latest-by-date in loc de MAX): grad_actual_id=%', v_actual;
  END IF;

  -- C) DELETE randul cu grad mare -> recalcul MAX din ce a ramas (D-02)
  DELETE FROM public.istoric_grade WHERE sportiv_id = v_sportiv AND grad_id = v_high;
  SELECT ig.grad_id INTO v_expected
    FROM public.istoric_grade ig JOIN public.grade g ON g.id = ig.grad_id
   WHERE ig.sportiv_id = v_sportiv ORDER BY g.ordine DESC LIMIT 1;
  SELECT grad_actual_id INTO v_actual FROM public.sportivi WHERE id = v_sportiv;
  IF v_actual IS DISTINCT FROM v_expected THEN
    RAISE EXCEPTION 'FAIL C (D-02 incalcat: DELETE nu recalculeaza): grad_actual_id=% asteptat=%', v_actual, v_expected;
  END IF;

  DELETE FROM public.istoric_grade WHERE sportiv_id = v_sportiv;
  DELETE FROM public.sportivi WHERE id = v_sportiv;
  RAISE NOTICE 'SMOKE TEST FAZA 18: PASS (A, B, C)';
END $$;
```

## C. Audit D-10 — randuri istoric_grade suspecte (Task 3)

```sql
SELECT
    ig.id AS istoric_id,
    s.nume, s.prenume, s.id AS sportiv_id,
    g.nume AS grad_nume, g.ordine,
    ig.data_obtinere,
    ig.observatii,
    ig.sesiune_examen_id,
    se.data AS data_sesiune_examen
FROM public.istoric_grade ig
JOIN public.sportivi s ON s.id = ig.sportiv_id
JOIN public.grade g ON g.id = ig.grad_id
LEFT JOIN public.sesiuni_examene se ON se.id = ig.sesiune_examen_id
WHERE
    ig.observatii ILIKE '%Schimbare automată grad%'
    OR ig.observatii ILIKE '%Update Profil%'
    OR (
        ig.sesiune_examen_id IS NOT NULL
        AND se.data IS NOT NULL
        AND ig.data_obtinere <> se.data
    )
ORDER BY ig.data_obtinere DESC;
```

## D. Audit conex — sportivi cu grad_actual_id divergent de MAX(ordine) (Task 3)

Camp DERIVAT, nu date istorice — se raporteaza, NU se corecteaza automat (spiritul D-10).

```sql
SELECT s.id AS sportiv_id, s.nume, s.prenume, s.club_id,
       gc.nume AS grad_actual_in_db, gc.ordine AS ordine_db,
       gm.nume AS grad_max_din_istoric, gm.ordine AS ordine_max
FROM public.sportivi s
LEFT JOIN public.grade gc ON gc.id = s.grad_actual_id
LEFT JOIN LATERAL (
    SELECT g.id, g.nume, g.ordine
    FROM public.istoric_grade ig JOIN public.grade g ON g.id = ig.grad_id
    WHERE ig.sportiv_id = s.id
    ORDER BY g.ordine DESC LIMIT 1
) gm ON TRUE
WHERE gm.id IS NOT NULL
  AND s.grad_actual_id IS DISTINCT FROM gm.id
ORDER BY s.club_id, s.nume;
```
</verification_sql>

<tasks>

<task type="auto">
  <name>Task 1: Scrie fisierul de migratie versionat</name>
  <files>sql/migrations/consolidare_trigger_grad_actual_260906.sql</files>
  <read_first>
    - `.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-RESEARCH.md` — sectiunile "Recommended Migration Structure", "Live Verification" (punctele 1-4, corectiile A1/A2/A3) si "Common Pitfalls" #3 si #4
    - `.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-PATTERNS.md` — sectiunea "Pattern: DB migration methodology (CREATE new trigger before DROP old ones)"
    - Sectiunea `<migration_sql>` din ACEST plan (sursa verbatim a continutului fisierului)
  </read_first>
  <action>
    Creeaza `sql/migrations/consolidare_trigger_grad_actual_260906.sql` cu exact continutul din sectiunea `<migration_sql>` a acestui plan, verbatim (inclusiv comentariul-antet care documenteaza mecanismul de race condition — corectia din 18-RESEARCH.md "Live Verification" #2; NU descrie bug-ul ca "suprascriere literala").

    Motivul pentru care fisierul EXISTA desi migratia se aplica prin MCP: pana acum toate trigger-ele acestui proiect au trait doar live pe Supabase, niciodata comise — exact reclamatia din memoria `feedback_audit_rls_verifica_live_nu_doar_migratii`. Fisierul e artefactul de audit al fazei, nu calea de aplicare.

    Elemente obligatorii care NU pot lipsi din fisier:
    - functia `public.sync_grad_actual_canonical()` cu `SECURITY DEFINER SET search_path = public` (per D-06 + ASVS V4; fara asta trigger-ul poate esua cu "permission denied", exact bug-ul `tr_automatizeaza_roluri` din Faza 25-04);
    - `metoda_selectie_grad = 'automat'` in `UPDATE public.sportivi` (D-06 / discretie confirmata: coloana e citita in `components/UserProfile.tsx` liniile 357 si 363 — daca dispare, ecranul de profil arata valoare stale);
    - `ORDER BY g.ordine DESC` (NU `data_obtinere DESC`) — este exact regula D-01;
    - trigger `AFTER INSERT OR UPDATE OR DELETE` (DELETE-ul e cerinta D-02, azi neacoperita de niciun trigger existent);
    - garda `AND grad_actual_id IS DISTINCT FROM v_max_grad_id` — evita UPDATE-uri no-op care ar declansa inutil trigger-ele de audit de pe `sportivi`;
    - ordinea CREATE-inainte-de-DROP (D-05/D-07, Pitfall 4) intr-un singur `BEGIN ... COMMIT`;
    - ZERO `DROP FUNCTION` (functiile vechi raman definite si inerte — pattern Faza 25-04);
    - ZERO atingere a `trigger_ajusteaza_debutant_la_import` (D-08) si a constraint-ului `istoric_grade_sportiv_grad_unique` (D-09).

    Nu inventa alte nume: `sync_grad_actual_canonical` / `trg_sync_grad_actual_canonical` sunt numele decise pentru aceasta faza (Claude's Discretion din D-05/D-06, fixata aici).
  </action>
  <acceptance_criteria>
    - Fisierul `sql/migrations/consolidare_trigger_grad_actual_260906.sql` exista si are peste 40 de linii.
    - `grep -c "sync_grad_actual_canonical" sql/migrations/consolidare_trigger_grad_actual_260906.sql` returneaza cel putin 3.
    - `grep -v '^--' sql/migrations/consolidare_trigger_grad_actual_260906.sql | grep -c "SECURITY DEFINER"` returneaza 1.
    - `grep -v '^--' sql/migrations/consolidare_trigger_grad_actual_260906.sql | grep -c "metoda_selectie_grad"` returneaza cel putin 1.
    - `grep -v '^--' sql/migrations/consolidare_trigger_grad_actual_260906.sql | grep -c "DROP FUNCTION"` returneaza 0.
    - `grep -v '^--' sql/migrations/consolidare_trigger_grad_actual_260906.sql | grep -c "trigger_ajusteaza_debutant_la_import"` returneaza 0.
    - Ordinea in fisier: linia `CREATE TRIGGER trg_sync_grad_actual_canonical` apare INAINTEA primului `DROP TRIGGER IF EXISTS trg_after_history_change`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c "f=sql/migrations/consolidare_trigger_grad_actual_260906.sql; test -f \$f && c=\$(grep -n 'CREATE TRIGGER trg_sync_grad_actual_canonical' \$f | head -1 | cut -d: -f1) && d=\$(grep -n 'DROP TRIGGER IF EXISTS trg_after_history_change' \$f | head -1 | cut -d: -f1) && test \$c -lt \$d && test \$(grep -v '^--' \$f | grep -c 'DROP FUNCTION') -eq 0 && test \$(grep -v '^--' \$f | grep -c 'SECURITY DEFINER') -eq 1 && echo OK"</automated>
  </verify>
  <done>Fisierul de migratie exista in git, contine functia + trigger-ul canonic in ordinea CREATE-inainte-de-DROP, cu SECURITY DEFINER si metoda_selectie_grad pastrate, fara niciun DROP FUNCTION.</done>
</task>

<task type="auto">
  <name>Task 2: Aplica migratia live prin Supabase MCP, dovedeste functional D-01/D-02, si ruleaza auditul D-10</name>
  <files>.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-AUDIT-DATE-CORUPTE.md (fara alte fisiere repo — restul e operatie live pe Supabase wuhidifzsutwgdfkwhmd)</files>
  <read_first>
    - `sql/migrations/consolidare_trigger_grad_actual_260906.sql` (creat la Task 1 — continutul exact de aplicat)
    - Sectiunile `<verification_sql>` A, B, C, D din ACEST plan
    - `<execution_requirements>` din acest plan (de ce nu se ruleaza in worktree/subagent)
    - `.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-CONTEXT.md` — D-10 (audit, NU fix automat)
  </read_first>
  <action>
    NOTA STRUCTURA: acest task combina ce erau anterior Task 2+3 intr-un singur task, ca sa incapa sub pragul de rutare inline al motorului de executie (`workflow.inline_plan_threshold`, implicit 2) — un plan cu <= 2 task-uri ruleaza mereu inline in sesiunea curenta, indiferent de checkpoint-uri, ceea ce garanteaza acces la tool-urile `mcp__supabase__*` (un subagent gsd-executor dispatch-uit separat NU are acces MCP in acest mediu — confirmat direct in aceasta faza: agentul de research `gsd-phase-researcher` nu a avut niciun tool `mcp__supabase__*` disponibil). Nu desparti acest task inapoi in doua fara sa adaugi un mecanism echivalent de rutare inline.

    **Parte A — migratie (D-01, D-02, D-05, D-06, D-07):**

    Pas 1 — inventar PRE-migratie: ruleaza query-ul din `<verification_sql>` sectiunea A prin `mcp__supabase__execute_sql` si salveaza rezultatul (il vei cita in SUMMARY). Confirma ca cele 5 trigger-e tinta exista inca sub numele documentate. Daca vreun nume difera (drift fata de 18-RESEARCH.md "Live Verification" #1, care raporta zero drift), OPRESTE-TE si raporteaza inainte de a aplica orice — nu ghici numele.

    Pas 2 — aplica migratia: `mcp__supabase__apply_migration` cu name `consolidare_trigger_grad_actual_260906` si continutul fisierului de la Task 1. Daca tool-ul refuza blocul `BEGIN;`/`COMMIT;` (unele implementari isi ruleaza propria tranzactie), trimite continutul fara cele doua linii — restul ramane neschimbat si ordinea CREATE-inainte-de-DROP e oricum respectata.

    Pas 3 — inventar POST-migratie: re-ruleaza query-ul A. Rezultat cerut: pe `istoric_grade` exact doua trigger-e non-interne — `trg_sync_grad_actual_canonical` si `trigger_ajusteaza_debutant_la_import` (D-08 intact); pe `sportivi` `tr_sync_grad_history` absent (D-07). Trigger-ele de audit existente pe `sportivi` (din quick task 260704-x9p) raman si sunt in afara scope-ului — nu le atinge.

    Pas 4 — smoke test: ruleaza blocul DO din `<verification_sql>` sectiunea B printr-un singur `mcp__supabase__execute_sql`. Asertia A verifica derivarea + `metoda_selectie_grad`, asertia B verifica D-01 (grad inferior cu data mai noua NU retrogradeaza — asta e diferenta fata de vechile trigger-e latest-by-date), asertia C verifica D-02 (recalcul pe DELETE). Daca blocul esueaza pe NOT NULL la INSERT-ul in `sportivi`, adauga in acel INSERT doar coloanele reclamate de eroare, cu valori minime; nu modifica asertiile.

    Pas 5: daca orice asertie de la Partea A pica, NU continua cu Partea B, NU continua cu planurile 18-02/18-03 — raporteaza esecul si SQL-ul exact al asertiei picate.

    **Parte B — audit D-10 (doar dupa ce Partea A trece complet):**

    Ruleaza prin `mcp__supabase__execute_sql` DOAR cele doua query-uri SELECT din `<verification_sql>` sectiunile C si D. Interdictie explicita D-10: zero `UPDATE`, `DELETE` sau `INSERT` pe date istorice reale in aceasta parte — raportul e pentru decizia manuala a utilizatorului.

    Sectiunea C = randuri `istoric_grade` cu data suspecta (observatii generate de vechiul `fn_sync_grad_to_history`, sau `data_obtinere` diferita de `data` sesiunii de examen asociate). Numele tabelului este `sesiuni_examene` (plural cu -e final; confirmat live in 18-RESEARCH.md "Live Verification" #3 — corectia assumption-ului A2, NU `sesiuni_examen`).

    Sectiunea D = audit conex, nemenționat explicit in D-10 dar in spiritul lui: sportivi al caror `grad_actual_id` din DB difera de MAX(ordine) din istoricul propriu. Acestia sunt inconsistentele mostenite de la vechile trigger-e latest-by-date. NU le corecta in aceasta faza; noul trigger le va repara automat la urmatoarea scriere in `istoric_grade` pentru acel sportiv, iar restul raman pentru decizia utilizatorului.

    Scrie `18-AUDIT-DATE-CORUPTE.md` cu: data rularii, cele doua query-uri exacte, numarul de randuri per sectiune, tabelele complete de rezultate (sau primele 50 de randuri + total, daca depasesc 50), si o sectiune finala "Recomandare" care explica: (1) pentru sectiunea C, corectia se face manual per rand prin editarea intrarii din `components/UserProfile.tsx` (ecran istoric grade, `handleEditGrade`), care acum declanseaza recalculul corect; (2) pentru sectiunea D, un `UPDATE` bulk de resincronizare este posibil dar NU a fost executat in aceasta faza (D-10), si include SQL-ul propus comentat, ca sa fie la indemana daca utilizatorul il cere.

    Daca vreun query returneaza 0 randuri, scrie asta explicit in raport ca rezultat valid (nu omite sectiunea).
  </action>
  <acceptance_criteria>
    - Query-ul A post-migratie returneaza pentru `public.istoric_grade` exact 2 randuri: `trg_sync_grad_actual_canonical` -> `sync_grad_actual_canonical` si `trigger_ajusteaza_debutant_la_import` -> `ajusteaza_debutant_la_import_examen`.
    - Query-ul A post-migratie NU returneaza niciun rand cu `tgname` in (`trg_after_history_change`, `trg_sync_grad_actual_from_istoric`, `trg_sync_grade_on_history_change`, `trg_sync_grad_actual_manual`, `tr_sync_grad_history`).
    - Blocul DO din sectiunea B ruleaza fara exceptie si emite NOTICE `SMOKE TEST FAZA 18: PASS (A, B, C)`.
    - Dupa smoke test, `SELECT count(*) FROM public.sportivi WHERE nume = 'ZZ_TEST_TRIGGER'` returneaza 0 (curatenie efectuata).
    - Rezultatele brute (PRE si POST) sunt citate in SUMMARY, nu doar rezumate.
    - `.planning/phases/18-.../18-AUDIT-DATE-CORUPTE.md` exista, peste 20 de linii, si contine ambele query-uri rulate (C si D).
    - Raportul contine numarul exact de randuri gasite pentru fiecare din cele doua audituri (inclusiv "0 randuri" daca e cazul).
    - Raportul contine o sectiune "Recomandare" cu SQL-ul de resincronizare bulk marcat explicit ca NEEXECUTAT.
    - Nicio comanda `UPDATE`/`DELETE`/`INSERT` pe `istoric_grade` sau `sportivi` cu date reale nu a fost rulata in Partea B (doar cele doua SELECT-uri).
  </acceptance_criteria>
  <verify>
    <automated>bash -c "f='.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-AUDIT-DATE-CORUPTE.md'; test -f \"\$f\" && test \$(wc -l < \"\$f\") -gt 20 && grep -q 'sesiuni_examene' \"\$f\" && grep -qi 'recomandare' \"\$f\" && echo OK"</automated>
    <human-check>Confirma in SUMMARY ca inventarul POST arata exact cele 2 trigger-e ramase pe istoric_grade, ca smoke testul a trecut toate cele 3 asertii, si ca raportul de audit D-10 exista fara nicio scriere pe date reale.</human-check>
  </verify>
  <done>Migratia e aplicata live; `istoric_grade` are un singur trigger de sincronizare plus cel de debutant; `tr_sync_grad_history` nu mai exista pe `sportivi`; smoke testul demonstreaza MAX(ordine) pe INSERT, non-retrogradare pe grad inferior cu data mai noua, si recalcul pe DELETE; raportul de audit D-10 exista fara nicio corectie automata pe date reale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser, rol ADMIN_CLUB/INSTRUCTOR) -> PostgREST -> `istoric_grade` | Intrare semi-increzatoare: RLS filtreaza pe club, dar continutul randului (grad_id, data_obtinere) vine din UI |
| `istoric_grade` (trigger) -> `public.sportivi` | Granita de privilegii: functia ruleaza `SECURITY DEFINER`, deci scrie pe `sportivi` ocolind RLS-ul apelantului |
| sesiune de executie -> Supabase MCP `apply_migration` | Granita DDL: modificari de schema pe proiectul de productie cu date reale (7 cluburi) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation of Privilege | `sync_grad_actual_canonical()` cu `SECURITY DEFINER` | mitigate | `SET search_path = public` fixat in definitia functiei (blocheaza search_path hijacking); functia nu accepta niciun input din afara randului declansator si nu executa SQL dinamic |
| T-18-02 | Tampering | Scrierea derivata pe `sportivi.grad_actual_id` | mitigate | Functia scrie EXCLUSIV pe randul `sportivi.id = COALESCE(NEW.sportiv_id, OLD.sportiv_id)` — nu poate atinge alt sportiv; RLS-ul de pe `istoric_grade` decide deja ce randuri poate crea apelantul |
| T-18-03 | Tampering | Migratie DDL partial aplicata (CREATE reuseste, DROP-uri esueaza sau invers) | mitigate | Totul intr-un singur `BEGIN...COMMIT`, ordine CREATE-inainte-de-DROP; verificare imediata post-aplicare prin query-ul A (inventar trigger-e) |
| T-18-04 | Denial of Service | Trigger row-level pe importuri in masa (ImportExamenModal proceseaza sute de randuri) | accept | Un `UPDATE` cu garda `IS DISTINCT FROM` per rand de istoric; cele 3 trigger-e vechi faceau deja aceeasi munca de 3 ori — noul trigger REDUCE incarcarea, nu o creste |
| T-18-05 | Repudiation | Pierderea trasabilitatii datei reale a examenului | mitigate | Exact bug-ul reparat de faza: dupa DROP `tr_sync_grad_history`, nicio scriere automata nu mai poate substitui `CURRENT_DATE` datei reale |
| T-18-06 | Information Disclosure | Raportul de audit D-10 contine nume/prenume sportivi (date personale, unii minori) | accept | Raportul ramane local in `.planning/` pe masina dezvoltatorului, in acelasi repo privat unde traiesc deja artefactele de faza; nu paraseste proiectul si nu e expus in UI |

**Package Legitimacy Gate:** nu se aplica — faza nu instaleaza niciun pachet npm/pip/cargo (confirmat in 18-RESEARCH.md, sectiunea "Standard Stack": zero dependinte noi).
</threat_model>

<verification>
1. Query-ul A (`pg_trigger`) post-migratie arata exact 2 trigger-e pe `istoric_grade` si niciun `tr_sync_grad_history` pe `sportivi`.
2. Blocul DO de smoke test trece toate cele 3 asertii (A, B, C).
3. Fisierul de migratie exista in repo si trece gate-urile de grep din Task 1.
4. Raportul de audit exista si nu a declansat nicio scriere pe date reale.
5. `SELECT count(*) FROM public.sportivi WHERE nume = 'ZZ_TEST_TRIGGER'` = 0.
</verification>

<success_criteria>
- Un singur trigger canonic sincronizeaza `grad_actual_id`, cu regula MAX(ordine), activ pe INSERT/UPDATE/DELETE.
- `metoda_selectie_grad` continua sa fie populata cu 'automat' (dovedit prin asertia A2).
- Trigger-ul sursa a datei gresite (`tr_sync_grad_history`) nu mai exista.
- `trigger_ajusteaza_debutant_la_import` si constraint-ul `istoric_grade_sportiv_grad_unique` sunt neatinse.
- Migratia e versionata in git (prima data cand un trigger al acestui proiect ajunge intr-un fisier comis).
- Utilizatorul are un raport de audit cu randurile suspecte, fara nicio corectie automata.
</success_criteria>

<output>
Create `.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-01-SUMMARY.md` when done.

SUMMARY-ul TREBUIE sa contina lipit: output-ul brut al query-ului A PRE si POST migratie, si NOTICE-ul de PASS al smoke testului. Fara ele, planul nu e verificabil ulterior (schema DB nu e in git).
</output>
