---
phase: 18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - hooks/useExamManager.ts
  - components/GestiuneExamene/RapoarteExamen.tsx
  - components/GestiuneExamene/ImportExamenModal.tsx
  - components/UserProfile.tsx
autonomous: true
requirements: [D-03, D-04, D-09]

must_haves:
  truths:
    - "Finalizarea unui examen din useExamManager scrie doar in istoric_grade, cu data reala a sesiunii"
    - "Finalizarea din RapoarteExamen scrie doar in istoric_grade, cu data reala a sesiunii"
    - "Importul CSV de examene scrie doar in istoric_grade cu data din fisier; crearea unui sportiv NOU pastreaza grad_actual_id in payload-ul de insert (exceptia D-04)"
    - "Comentariile din cod nu mai descriu regula veche 'latest by data_obtinere' si nu mai instruiesc dezvoltatorii sa scrie direct grad_actual_id"
  artifacts:
    - path: "hooks/useExamManager.ts"
      provides: "Flux de finalizare examen fara scriere directa pe sportivi"
      contains: "istoric_grade"
    - path: "components/GestiuneExamene/RapoarteExamen.tsx"
      provides: "Flux de finalizare din raport fara scriere directa pe sportivi"
      contains: "istoric_grade"
    - path: "components/GestiuneExamene/ImportExamenModal.tsx"
      provides: "Import CSV cu ordinea corectata (doar upsert istoric_grade)"
      contains: "istoric_grade"
  key_links:
    - from: "hooks/useExamManager.ts"
      to: "public.istoric_grade"
      via: "supabase.from('istoric_grade').insert(... data_obtinere: sesiuneData.data ...)"
      pattern: "from\\('istoric_grade'\\)"
    - from: "components/UserProfile.tsx"
      to: "public.sportivi.grad_actual_id"
      via: "refetchGradActual() dupa orice modificare in istoric_grade (citire, nu scriere)"
      pattern: "refetchGradActual"
---

<objective>
Elimina ultimele 3 scrieri directe pe `sportivi.grad_actual_id` (D-03, D-04) din `hooks/useExamManager.ts`, `components/GestiuneExamene/RapoarteExamen.tsx` si `components/GestiuneExamene/ImportExamenModal.tsx`, si corecteaza comentariile invechite din `components/UserProfile.tsx` care descriu regula veche de sincronizare.

Purpose: `useExamManager.ts` si `RapoarteExamen.tsx` asteapta secvential, deci nu corup date azi (18-RESEARCH.md, "Execution pattern per call site"), dar scrierea directa ramane redundanta arhitectural si — mai grav — comentariile lor inline instruiesc activ dezvoltatorii sa o pastreze ("Aceasta este pasul care lipsea si cauza bug-ul"). Codul asta a fost deja patch-uit ad-hoc de mai multe ori; daca nu se corecteaza si comentariile, scrierea va fi re-adaugata. `ImportExamenModal.tsx` are ordinea INVERSATA fata de celelalte (scrie pe `sportivi` INAINTE de upsert-ul in `istoric_grade`) si necesita tratament separat.

Output: cele 3 fisiere fara nicio scriere pe `sportivi.grad_actual_id`, cu exceptiile D-04 (payload de insert la creare sportiv nou) intacte.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-PATTERNS.md
@.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-CONTEXT.md
@services/sportivService.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 03)

Planul nu creeaza simboluri noi exportate. Modifica in loc:

| Simbol modificat | Fisier | Schimbare |
|------------------|--------|-----------|
| bucla de finalizare examen (`appliedGradeBySportiv`) | `hooks/useExamManager.ts` | Se elimina `supabase.from('sportivi').update(...)`; garda pe `ordine` ramane, dar guverneaza doar starea locala optimista |
| handler de finalizare (`sportiviGradMap`) | `components/GestiuneExamene/RapoarteExamen.tsx` | Idem |
| bucla de import per rand | `components/GestiuneExamene/ImportExamenModal.tsx` | Se elimina SELECT-ul de grad curent + update-ul conditionat; ramane doar upsert-ul in `istoric_grade` |
| `refetchGradActual` (comentariu) | `components/UserProfile.tsx` | Comentariu corectat: regula e MAX(ordine) via `trg_sync_grad_actual_canonical`, nu `data_obtinere DESC` |

Consuma (creat de planul 18-01): trigger-ul `trg_sync_grad_actual_canonical` pe `public.istoric_grade`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Elimina scrierile directe din useExamManager.ts si RapoarteExamen.tsx</name>
  <files>hooks/useExamManager.ts, components/GestiuneExamene/RapoarteExamen.tsx</files>
  <read_first>
    - `hooks/useExamManager.ts` liniile 120-200 — bucla de finalizare, insertul in `istoric_grade`, blocul de update direct (~158-173) si comentariul de stare locala (~178-190)
    - `components/GestiuneExamene/RapoarteExamen.tsx` liniile 220-300 — insertul in `istoric_grade`, blocul de update direct (~261-273) si blocul `setSportivi` care il urmeaza
    - `.planning/phases/18-.../18-PATTERNS.md` sectiunile pentru `hooks/useExamManager.ts` si `components/GestiuneExamene/RapoarteExamen.tsx` (contin blocurile exacte, cu instructiunea explicita de a PASTRA `appliedGradeBySportiv.set` / `sportiviGradMap.set`)
    - `services/sportivService.ts` liniile 100-137 — pattern-ul corect de referinta
  </read_first>
  <action>
    A. `hooks/useExamManager.ts` (~liniile 158-173): sterge apelul `await supabase.from('sportivi').update({ grad_actual_id: targetGradId }).eq('id', inscriere.sportiv_id)` impreuna cu verificarea `if (gradUpdateError) throw gradUpdateError`. PASTREAZA calculul `targetGrade` / `currentGrade` / `targetOrdine` / `currentOrdine` si garda `if (targetOrdine > currentOrdine) { ... }`, dar in interiorul ei ramane DOAR `appliedGradeBySportiv.set(inscriere.sportiv_id, targetGradId)` — motivul pentru care garda ramane e explicat in comentariul existent de la ~liniile 178-190 (bug fix 260709-m7m: previne downgrade optimist in UI). NU sterge acel comentariu de bug fix.

    Inlocuieste comentariul invechit de deasupra blocului ("Actualizeaza grad_actual_id direct în DB. Trigger-ul SQL sync_grad_actual_on_exam_result protejeaza contra downgrade, dar daca triggerul nu e activ, actualizam oricum...") — el numeste un trigger care NU exista pe DB (verificat live in 18-RESEARCH.md: trigger-ele reale erau `trg_after_history_change`, `trg_sync_grad_actual_from_istoric`, `trg_sync_grad_actual_manual`, `trg_sync_grade_on_history_change`). Noul comentariu spune: `grad_actual_id` este derivat exclusiv de `trg_sync_grad_actual_canonical` pe `istoric_grade` (regula MAX(ordine), D-01/D-04); garda de mai jos guverneaza doar starea locala de UI.

    Corecteaza si comentariul de la ~liniile 178-184 care spune "the trigger will handle the DB update, but we update local state for immediate feedback" — formularea "but" sugereaza redundanta optionala; noua formulare trebuie sa afirme ca trigger-ul e SINGURA sursa de adevar in DB.

    PASTREAZA neatins insertul in `istoric_grade` cu `data_obtinere: sesiuneData.data || sesiuneData.data_examen || new Date()...` si cu `observatii`.

    B. `components/GestiuneExamene/RapoarteExamen.tsx` (~liniile 261-273): sterge apelul `await supabase.from('sportivi').update({ grad_actual_id: targetGradId }).eq('id', inscriere.sportiv_id)` si `if (gradUpdateError) throw gradUpdateError`. PASTREAZA garda pe `ordine` si, in interiorul ei, DOAR `sportiviGradMap.set(inscriere.sportiv_id, targetGradId)` (folosita mai jos de `props.setSportivi`).

    Sterge complet comentariul "Aceasta este pasul care lipsea si cauza bug-ul: sportivi.grad_actual_id ramanea la valoarea veche in baza de date dupa finalizarea examenului." — documenteaza un patch ad-hoc anterior care e chiar cauza dual-write-ului; inlocuieste-l cu un comentariu care spune ca `grad_actual_id` e derivat de `trg_sync_grad_actual_canonical` si ca aici se pastreaza doar starea locala optimista.

    PASTREAZA neatins insertul in `istoric_grade` de deasupra (cu `sesiune_examen_id` si `observatii`).

    In ambele fisiere: nu redenumi variabile, nu schimba semnaturi, nu atinge alte apeluri Supabase.
  </action>
  <acceptance_criteria>
    - `hooks/useExamManager.ts` contine ZERO aparitii ale sirului `from('sportivi')`.
    - `components/GestiuneExamene/RapoarteExamen.tsx` contine ZERO aparitii ale sirului `from('sportivi')`.
    - `appliedGradeBySportiv.set(` exista in continuare in `hooks/useExamManager.ts`; `sportiviGradMap.set(` exista in continuare in `RapoarteExamen.tsx`.
    - Sirul `sync_grad_actual_on_exam_result` (trigger inexistent) nu mai apare in `hooks/useExamManager.ts`.
    - Sirul `Aceasta este pasul care lipsea` nu mai apare in `RapoarteExamen.tsx`.
    - Insertul in `istoric_grade` este intact in ambele fisiere (inclusiv `sesiune_examen_id` si `observatii`).
    - `npm run lint` trece fara erori noi.
  </acceptance_criteria>
  <verify>
    <automated>bash -c "test \$(grep -c \"from('sportivi')\" hooks/useExamManager.ts) -eq 0 && test \$(grep -c \"from('sportivi')\" components/GestiuneExamene/RapoarteExamen.tsx) -eq 0 && grep -q 'appliedGradeBySportiv.set(' hooks/useExamManager.ts && grep -q 'sportiviGradMap.set(' components/GestiuneExamene/RapoarteExamen.tsx && test \$(grep -c 'sync_grad_actual_on_exam_result' hooks/useExamManager.ts) -eq 0 && test \$(grep -c 'Aceasta este pasul care lipsea' components/GestiuneExamene/RapoarteExamen.tsx) -eq 0 && npm run lint"</automated>
  </verify>
  <done>Ambele fluxuri de finalizare examen scriu exclusiv in `istoric_grade`; starea locala optimista si gardile ei sunt pastrate; comentariile invechite/inselatoare sunt inlocuite.</done>
</task>

<task type="auto">
  <name>Task 2: Corecteaza ImportExamenModal.tsx (ordine inversata) pastrand exceptiile D-04</name>
  <files>components/GestiuneExamene/ImportExamenModal.tsx</files>
  <read_first>
    - `components/GestiuneExamene/ImportExamenModal.tsx` liniile 525-625 — bucla per rand: parsarea `gradOrdine` (~533), insertul de sportiv nou cu `grad_actual_id` in payload (~554), insertul in `inscrieri_examene` cu `grad_actual_id` in payload (~590), si blocul `if (row.Rezultat === 'Admis')` (~599-620)
    - `.planning/phases/18-.../18-PATTERNS.md` sectiunea `components/GestiuneExamene/ImportExamenModal.tsx` — semnaleaza explicit ordinea inversata si listeaza exceptiile de la liniile 554 si 590
    - `.planning/phases/18-.../18-CONTEXT.md` — D-04 (exceptia pentru insertul initial la creare sportiv nou)
  </read_first>
  <action>
    In blocul `if (row.Rezultat === 'Admis')` (~liniile 599-620) sterge INTEGRAL secventa care precede upsert-ul: interogarea `const { data: sportivCurent } = await supabase.from('sportivi').select('grad_actual_id, grade(ordine)').eq('id', finalSportivId).single()`, calculul `const currentOrdine = (sportivCurent?.grade as any)?.ordine ?? -1`, si blocul `if (gradOrdine > currentOrdine) { await supabase.from('sportivi').update({ grad_actual_id: gradId }).eq('id', finalSportivId); }`. Dupa stergere, blocul trebuie sa contina DOAR upsert-ul in `istoric_grade`.

    Acesta este singurul call site cu ordinea inversata (scrie pe `sportivi` INAINTE de `istoric_grade`) — deci nu copia mecanic pattern-ul din Task 1; aici se sterge un bloc care e mai sus, nu mai jos, de upsert. Rezultatul e o reducere de un round-trip pe rand de import (SELECT-ul dispare complet), relevant pentru importuri de sute de randuri.

    Variabila `gradOrdine` NU se sterge — ramane folosita la liniile ~533-539 (parsare + cautarea gradului dupa `ordine`). Verifica inainte de stergere ca `sportivCurent` si `currentOrdine` nu sunt referite in alta parte in fisier.

    PASTREAZA neatins upsert-ul `supabase.from('istoric_grade').upsert({ sportiv_id: finalSportivId, grad_id: gradId, data_obtinere: dataExamen, sesiune_examen_id: sessionId, club_id: sesiuneClubId }, { onConflict: 'sportiv_id,grad_id' })`. NU adauga `ignoreDuplicates` si NU schimba semantica de conflict aici: acest upsert este azi un `DO UPDATE` (default supabase-js), deci difera de celelalte call site-uri — schimbarea semanticii de upsert pe `istoric_grade` este scopul explicit al Fazei 19 ("Elimina ignoreDuplicates silentios pe upsert istoric_grade, 8 locuri"), nu al acestei faze (D-09: constraint-ul si comportamentul de conflict se pastreaza). Noteaza aceasta observatie in SUMMARY ca hand-off catre Faza 19.

    EXCEPTII D-04 — NU atinge: linia ~554 `grad_actual_id: row.Rezultat === 'Admis' ? gradId : null` din payload-ul `supabase.from('sportivi').insert({...})` pentru sportiv NOU (insert la creare rand, acelasi pattern ca `adaugaSportiv` din `services/sportivService.ts`), si linia ~590 `grad_actual_id: ...` din payload-ul `supabase.from('inscrieri_examene').insert({...})` (alt tabel, nu `sportivi`). De asemenea NU atinge `supabase.from('sportivi').update({ data_nasterii: row.birthdate })` de la ~linia 562 — alt camp, in afara scope-ului.

    Adauga un comentariu scurt in locul blocului sters: `grad_actual_id` e derivat de `trg_sync_grad_actual_canonical` din upsert-ul de mai jos (D-04).
  </action>
  <acceptance_criteria>
    - `components/GestiuneExamene/ImportExamenModal.tsx` contine ZERO aparitii ale sirului `update({ grad_actual_id`.
    - Sirurile `sportivCurent` si `currentOrdine` nu mai apar in fisier.
    - `grep -c "grad_actual_id: row.Rezultat === 'Admis' ? gradId : null"` returneaza 2 (ambele exceptii D-04 intacte: insert sportiv nou + insert inscriere).
    - Upsert-ul in `istoric_grade` este intact si contine in continuare `data_obtinere: dataExamen` si `onConflict: 'sportiv_id,grad_id'`.
    - `supabase.from('sportivi').update({ data_nasterii: row.birthdate })` este intact.
    - `npm run lint` trece fara erori noi.
  </acceptance_criteria>
  <verify>
    <automated>bash -c "f=components/GestiuneExamene/ImportExamenModal.tsx; test \$(grep -c 'update({ grad_actual_id' \$f) -eq 0 && test \$(grep -c 'sportivCurent' \$f) -eq 0 && test \$(grep -c 'currentOrdine' \$f) -eq 0 && test \$(grep -c \"grad_actual_id: row.Rezultat === 'Admis' ? gradId : null\" \$f) -eq 2 && grep -q 'data_obtinere: dataExamen' \$f && grep -q 'data_nasterii: row.birthdate' \$f && npm run lint"</automated>
  </verify>
  <done>Importul CSV scrie o singura data per rand (upsert in `istoric_grade`), SELECT-ul redundant a disparut, iar exceptiile D-04 pentru crearea sportivului nou si a inscrierii sunt neatinse.</done>
</task>

<task type="auto">
  <name>Task 3: Corecteaza comentariile invechite din UserProfile.tsx si valideaza fisierele planului</name>
  <files>components/UserProfile.tsx</files>
  <read_first>
    - `components/UserProfile.tsx` liniile 348-420 — `refetchGradActual` (cu comentariul invechit de deasupra), `handleAddGrade`, `handleEditGrade`, `handleDeleteGrade`
    - `.planning/phases/18-.../18-RESEARCH.md` sectiunea "Common Pitfalls" #3 (`metoda_selectie_grad` e citit aici — nu se elimina)
  </read_first>
  <action>
    `components/UserProfile.tsx` este consumator, nu scriitor, al lui `grad_actual_id` — nu are nicio scriere directa de eliminat. Singura modificare este de comentarii, dar e obligatorie: comentariul de deasupra lui `refetchGradActual` (~liniile 351-352) afirma "triggerul a recalculat deja pe baza data_obtinere DESC", ceea ce descrie regula VECHE si va induce in eroare orice dezvoltator care depaneaza gradele dupa aceasta faza.

    Inlocuieste-l cu o formulare care spune: dupa orice INSERT/UPDATE/DELETE in `istoric_grade`, trigger-ul `trg_sync_grad_actual_canonical` recalculeaza `grad_actual_id` ca MAX(grade.ordine) din tot istoricul sportivului (D-01), iar `metoda_selectie_grad` este setat pe 'automat'; de aceea ecranul re-citeste ambele campuri din DB.

    Adauga un comentariu scurt deasupra lui `handleDeleteGrade` care noteaza ca stergerea unei intrari din istoric declanseaza acum recalculul server-side si poate retrograda legitim sportivul (D-02) — aceasta este calea oficiala de corectie a unui grad acordat gresit, si pana la faza 18 nu functiona (niciunul dintre trigger-ele vechi nu asculta DELETE).

    NU modifica logica din `refetchGradActual`, `handleAddGrade`, `handleEditGrade` sau `handleDeleteGrade`. NU elimina `metoda_selectie_grad` din `.select(...)` (linia 357) sau din obiectul de stare (linia 363).

    Dupa modificarea comentariilor, ruleaza gate-ul de validare al acestui plan pe cele 4 fisiere ale sale (comanda din `<verify>`) si `npm run lint`.
  </action>
  <acceptance_criteria>
    - `components/UserProfile.tsx` nu mai contine sirul `data_obtinere DESC`.
    - `components/UserProfile.tsx` contine sirul `trg_sync_grad_actual_canonical` (in comentariul nou).
    - `.select('grad_actual_id, metoda_selectie_grad')` este intact in `refetchGradActual`.
    - Pe cele 4 fisiere ale planului (`hooks/useExamManager.ts`, `components/GestiuneExamene/RapoarteExamen.tsx`, `components/GestiuneExamene/ImportExamenModal.tsx`, `components/UserProfile.tsx`) numarul total de linii de cod (exclus comentarii `//`) care contin `update({ grad_actual_id` este 0.
    - `npm run lint` trece fara erori noi.
  </acceptance_criteria>
  <verify>
    <automated>bash -c "test \$(grep -c 'data_obtinere DESC' components/UserProfile.tsx) -eq 0 && grep -q 'trg_sync_grad_actual_canonical' components/UserProfile.tsx && grep -q \"select('grad_actual_id, metoda_selectie_grad')\" components/UserProfile.tsx && test \$(cat hooks/useExamManager.ts components/GestiuneExamene/RapoarteExamen.tsx components/GestiuneExamene/ImportExamenModal.tsx components/UserProfile.tsx | grep -v '^[[:space:]]*//' | grep -c 'update({ grad_actual_id') -eq 0 && npm run lint"</automated>
  </verify>
  <done>Comentariile din `UserProfile.tsx` descriu regula reala (MAX(ordine), recalcul si pe DELETE); cele 4 fisiere ale planului nu contin nicio scriere directa pe `sportivi.grad_actual_id`; tsc trece.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV incarcat de admin -> `ImportExamenModal` -> `istoric_grade` / `sportivi` | Intrare neincrezatoare (fisier extern): `Grad_Nou_Ordine`, `Data_Examen`, nume/prenume |
| UI examene -> PostgREST -> `istoric_grade` | Singura cale de scriere ramasa pentru grade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Tampering | Scriere directa pe `sportivi.grad_actual_id` din 3 call site-uri | mitigate | Eliminata complet (D-03/D-04); campul devine derivat de trigger-ul din 18-01 |
| T-18-12 | Spoofing / Tampering | CSV de import cu `Grad_Nou_Ordine` arbitrar poate promova un sportiv | accept | Comportament preexistent, neschimbat de aceasta faza: importul e accesibil doar rolurilor cu drept pe sesiunea de examen, iar `ordine` invalid arunca eroare la parsare (`isNaN`) si la cautarea gradului. Faza nu largeste aceasta suprafata; unificarea/hardening-ul importurilor e scopul Fazei 24 |
| T-18-13 | Tampering | Upsert `DO UPDATE` pe `istoric_grade` in `ImportExamenModal` poate suprascrie `data_obtinere` a unui rand corect existent | transfer | Documentat explicit ca hand-off catre Faza 19 ("Elimina ignoreDuplicates silentios pe upsert istoric_grade, 8 locuri"); D-09 cere pastrarea comportamentului de conflict actual in Faza 18 |
| T-18-14 | Repudiation | Comentarii care instruiesc dezvoltatorii sa re-adauge scrierea directa | mitigate | Comentariile inselatoare sunt sterse/inlocuite in toate cele 4 fisiere — masura anti-regresie, nu cosmetica (codul a fost deja re-patch-uit de doua ori pe aceasta cale) |

**Package Legitimacy Gate:** nu se aplica — zero pachete instalate in aceasta faza.
</threat_model>

<verification>
1. `grep -c "from('sportivi')"` returneaza 0 pentru `hooks/useExamManager.ts` si `components/GestiuneExamene/RapoarteExamen.tsx`.
2. `grep -c "update({ grad_actual_id"` returneaza 0 pentru `components/GestiuneExamene/ImportExamenModal.tsx`.
3. Exceptiile D-04 din `ImportExamenModal.tsx` (2 payload-uri de insert) sunt intacte.
4. `components/UserProfile.tsx` nu mai descrie regula `data_obtinere DESC`.
5. `npm run lint` trece.
6. Verificarea end-to-end live se face in planul 18-04.
</verification>

<success_criteria>
- Zero scrieri directe pe `sportivi.grad_actual_id` in cele 3 fisiere de scriere (D-03, D-04).
- Exceptiile D-04 (insert la creare sportiv nou, insert in `inscrieri_examene`) neatinse.
- Semantica de upsert pe `istoric_grade` neschimbata (D-09), cu observatia predata Fazei 19.
- Comentariile care documentau regula veche sau incurajau dual-write-ul sunt eliminate.
</success_criteria>

<output>
Create `.planning/phases/18-fix-suprascriere-silentioasa-grad-in-istoric-grade-sportivse/18-03-SUMMARY.md` when done.

SUMMARY-ul trebuie sa contina: (1) confirmarea ca ordinea inversata din `ImportExamenModal.tsx` a fost tratata ca atare (bloc sters DEASUPRA upsert-ului, nu dedesubt), si (2) hand-off-ul catre Faza 19 privind upsert-ul `DO UPDATE` ramas in acel fisier.
</output>
