---
quick_id: 260705-1js
slug: consolideaza-endpoint-uri-vercel-api-sub
date: 2026-07-05
mode: quick-full
status: ready
must_haves:
  truths:
    - "api/ are 14 fisiere handler top-level, Vercel Hobby permite max 12 Serverless Functions per deployment — deploy blocat cu eroarea exacta din prompt"
    - "api/sms-send.ts si api/sms-status.ts NU au niciun apel din frontend (verificat grep global) — doar api/sms-test-connection.ts e apelat, din components/SMS/SMSConfigurare.tsx:204"
    - "api/claude-proxy.ts si api/gemini-proxy.ts NU au niciun apel din frontend (verificat grep global) — doar api/groq-proxy.ts e apelat, din services/claudeService.ts:83 si services/agents/orchestrator.ts:121, ambele cu shape raspuns {text}"
    - "vite.config.ts are middleware dev DOAR pentru /api/claude-proxy (claudeProxyPlugin) — nu exista middleware dev pentru gemini/groq-proxy; acest middleware trebuie generalizat sa raspunda doar cand query provider=claude, altfel sa treaca mai departe (next())"
    - "api/schimba-email.ts si api/schimba-username.ts sunt AMBELE apelate din services/sportivService.ts (liniile 57 si 89)"
    - "Niciunul din cele 8 endpoint-uri consolidate nu e webhook extern (SMS_CALLBACK_SECRET nu apare in api/) — schimbarea URL-urilor e sigura, fara integrari externe de actualizat"
  artifacts:
    - "api/sms.ts (nou) — inlocuieste sms-send.ts, sms-status.ts, sms-test-connection.ts"
    - "api/llm-proxy.ts (nou) — inlocuieste claude-proxy.ts, gemini-proxy.ts, groq-proxy.ts"
    - "api/account.ts (nou) — inlocuieste schimba-email.ts, schimba-username.ts"
    - "api/ ajunge la 9 fisiere handler (14 - 8 sterse + 3 noi = 9), sub limita de 12"
  key_links:
    - "components/SMS/SMSConfigurare.tsx:204 (fetch /api/sms-test-connection -> /api/sms?action=test)"
    - "services/claudeService.ts:83 (fetch /api/groq-proxy -> /api/llm-proxy?provider=groq)"
    - "services/agents/orchestrator.ts:121 (fetch /api/groq-proxy -> /api/llm-proxy?provider=groq)"
    - "services/sportivService.ts:57,89 (fetch /api/schimba-email -> /api/account?action=email; /api/schimba-username -> /api/account?action=username)"
    - "vite.config.ts:7-66 (claudeProxyPlugin middleware — generalizat pentru path /api/llm-proxy + guard provider=claude)"
---

# Quick Task 260705-1js: Consolidare endpoint-uri Vercel API sub limita Hobby

## Task boundary

Deploy Vercel blocat: "No more than 12 Serverless Functions can be added to a Deployment on the Hobby plan." — api/ are 14 fisiere. Consolidam 8 fisiere in 3, ajungem la 9. Fara schimbare de comportament — doar routing intern prin query param in loc de fisiere separate.

**NU se ating:** api/creare-cont.ts, api/genereaza-magic-link.ts, api/health.ts, api/rag-index.ts, api/rag-search.ts, api/reset-parola-sportiv.ts, api/_rateLimit.ts (helper, nu conteaza ca functie) — raman neschimbate.

---

## Task 1 — Consolidare SMS: api/sms.ts

**Files:** `api/sms.ts` (nou), sterge `api/sms-send.ts`, `api/sms-status.ts`, `api/sms-test-connection.ts`, editeaza `components/SMS/SMSConfigurare.tsx`

**Action:**
Creeaza `api/sms.ts` care combina logica exacta din cele 3 fisiere:
- `GET` (fara action) → logica din `sms-status.ts` (query club_id/limit/status/tip pe `sms_queue`)
- `POST` cu `req.query.action === 'test'` → logica din `sms-test-connection.ts` (body club_id/test_phone)
- `POST` cu `req.query.action === 'send'` sau fara action → logica din `sms-send.ts` (body club_id/sportiv_id/tip/variabile)

Structura handler:
```typescript
import { VercelRequest, VercelResponse } from '@vercel/node';
import { createClient } from '@supabase/supabase-js';

export default async function handler(req: VercelRequest, res: VercelResponse) {
  if (req.method === 'GET') return handleStatus(req, res);
  if (req.method === 'POST') {
    const action = (req.query.action as string) || 'send';
    if (action === 'test') return handleTest(req, res);
    if (action === 'send') return handleSend(req, res);
    return res.status(400).json({ error: 'action invalid (send|test)' });
  }
  return res.status(405).json({ error: 'Method not allowed' });
}

async function handleStatus(req: VercelRequest, res: VercelResponse) { /* corpul exact din sms-status.ts, fara linia de export default si semnatura functiei */ }
async function handleSend(req: VercelRequest, res: VercelResponse) { /* corpul exact din sms-send.ts */ }
async function handleTest(req: VercelRequest, res: VercelResponse) { /* corpul exact din sms-test-connection.ts */ }
```
Fiecare functie interna isi creeaza propriul client Supabase (copiaza exact blocul de validare env + createClient din fisierul original — nu extrage helper nou, minimizeaza diff-ul si riscul).

In `components/SMS/SMSConfigurare.tsx:204`, schimba `fetch('/api/sms-test-connection', ...)` in `fetch('/api/sms?action=test', ...)`.

Sterge `api/sms-send.ts`, `api/sms-status.ts`, `api/sms-test-connection.ts`.

**Verify:** `grep -r "api/sms-" --include=*.ts*` in tot repo (exclus .claude/worktrees) → 0 rezultate. `npx tsc --noEmit` curat.

**Done:** `api/sms.ts` exista si acopera toate 3 comportamente, cele 3 fisiere vechi sterse, SMSConfigurare.tsx actualizat, tsc curat.

---

## Task 2 — Consolidare LLM proxy: api/llm-proxy.ts

**Files:** `api/llm-proxy.ts` (nou), sterge `api/claude-proxy.ts`, `api/gemini-proxy.ts`, `api/groq-proxy.ts`, editeaza `services/claudeService.ts`, `services/agents/orchestrator.ts`, `vite.config.ts`

**Action:**
Creeaza `api/llm-proxy.ts`:
```typescript
import { VercelRequest, VercelResponse } from '@vercel/node';
import { checkRateLimit, getClientIp } from './_rateLimit';

export default async function handler(req: VercelRequest, res: VercelResponse) {
  if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
  const provider = (req.query.provider as string) || 'groq';
  if (provider === 'claude') return handleClaude(req, res);
  if (provider === 'gemini') return handleGemini(req, res);
  if (provider === 'groq') return handleGroq(req, res);
  return res.status(400).json({ error: 'provider invalid (claude|gemini|groq)' });
}

async function handleClaude(req: VercelRequest, res: VercelResponse) { /* corp exact claude-proxy.ts, INCLUSIV rate limiting checkRateLimit/getClientIp, INCLUSIV raspuns raw (data completa, nu {text}) */ }
async function handleGemini(req: VercelRequest, res: VercelResponse) { /* corp exact gemini-proxy.ts, raspuns {text} */ }
async function handleGroq(req: VercelRequest, res: VercelResponse) { /* corp exact groq-proxy.ts, raspuns {text} */ }
```
IMPORTANT: pastreaza EXACT shape-ul de raspuns per provider (claude returneaza raspunsul Anthropic complet nemodificat, gemini/groq returneaza `{text}`) — clientii actuali (`claudeService.ts`, `orchestrator.ts`) asteapta `{text}` de la groq, nu schimba asta.

In `services/claudeService.ts:83`, schimba `fetch('/api/groq-proxy', ...)` in `fetch('/api/llm-proxy?provider=groq', ...)`.
In `services/agents/orchestrator.ts:121`, aceeasi schimbare.

In `vite.config.ts`, generalizeaza middleware-ul dev:
- Schimba path-ul interceptat din `/api/claude-proxy` in `/api/llm-proxy`.
- La inceputul callback-ului middleware, verifica daca `req.url` contine `provider=claude` (parseaza query string din `req.url`, ex. cu `new URL(req.url, 'http://localhost')`), altfel apeleaza `next()` (parametrul al treilea al callback-ului Connect middleware) ca cererile pentru gemini/groq sa treaca mai departe (fara emulare dev — comportament identic cu azi, cand nu exista deloc emulare pt ele).
- Restul corpului (fetch catre Anthropic) ramane neschimbat.

Sterge `api/claude-proxy.ts`, `api/gemini-proxy.ts`, `api/groq-proxy.ts`.

**Verify:** `grep -rE "api/(claude|gemini|groq)-proxy" --include=*.ts*` in tot repo (exclus .claude/worktrees) → 0 rezultate. `npx tsc --noEmit` curat. Manual: pornit `npm run dev`, verifica in consola ca AI widget (Asistent AI) raspunde (foloseste groq prin `/api/llm-proxy?provider=groq` — nu are emulare dev asa ca va da eroare in dev local daca GROQ_API_KEY lipseste, comportament identic cu inainte de schimbare, NU e regresie).

**Done:** `api/llm-proxy.ts` acopera toate 3 provider-e cu shape-uri raspuns identice cu originalul, cele 3 fisiere vechi sterse, callerii actualizati, vite.config.ts middleware generalizat corect (nu strica dev flow pt claude), tsc curat.

---

## Task 3 — Consolidare cont: api/account.ts

**Files:** `api/account.ts` (nou), sterge `api/schimba-email.ts`, `api/schimba-username.ts`, editeaza `services/sportivService.ts`

**Action:**
Creeaza `api/account.ts`:
```typescript
import type { VercelRequest, VercelResponse } from '@vercel/node';
import { createClient } from "@supabase/supabase-js";

export default async function handler(req: VercelRequest, res: VercelResponse) {
  if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });
  const action = req.query.action as string;
  if (action === 'email') return handleEmail(req, res);
  if (action === 'username') return handleUsername(req, res);
  return res.status(400).json({ error: 'action invalid (email|username)' });
}

async function handleEmail(req: VercelRequest, res: VercelResponse) { /* corp exact schimba-email.ts */ }
async function handleUsername(req: VercelRequest, res: VercelResponse) { /* corp exact schimba-username.ts */ }
```

In `services/sportivService.ts:57`, schimba `fetch('/api/schimba-email', ...)` in `fetch('/api/account?action=email', ...)`.
In `services/sportivService.ts:89`, schimba `fetch('/api/schimba-username', ...)` in `fetch('/api/account?action=username', ...)`.

Sterge `api/schimba-email.ts`, `api/schimba-username.ts`.

**Verify:** `grep -rE "api/schimba-(email|username)"` in tot repo (exclus .claude/worktrees) → 0 rezultate. `npx tsc --noEmit` curat.

**Done:** `api/account.ts` acopera ambele actiuni, fisierele vechi sterse, sportivService.ts actualizat, tsc curat.

---

## Task 4 — Verificare finala numar functii + doc hygiene

**Files:** `docs/RAG_IMPLEMENTARE.md` (referinte vechi, optional de curatat daca mentioneaza explicit denumirile de fisiere sterse)

**Action:** Numara fisierele handler ramase in `api/` (exclude `_*.ts`): trebuie sa fie 9 — `account.ts`, `creare-cont.ts`, `genereaza-magic-link.ts`, `health.ts`, `llm-proxy.ts`, `rag-index.ts`, `rag-search.ts`, `reset-parola-sportiv.ts`, `sms.ts`. Verifica rapid `docs/RAG_IMPLEMENTARE.md` daca mentioneaza `claude-proxy`/`gemini-proxy`/`groq-proxy` explicit ca nume de fisier — daca da, actualizeaza referinta la `llm-proxy.ts?provider=...`. Nu e critic, doar igiena documentatiei.

**Verify:** `ls api/*.ts | grep -v '^api/_'  | wc -l` → 9.

**Done:** 9 fisiere in api/, sub limita de 12 (marja de 3 pentru viitor), toate cele 3 endpoint-uri noi verificate tsc.

---

## Note pentru executor

- Copiaza corpul functiilor EXACT (nu refactoriza logica) — scopul e doar redistribuire in mai putine fisiere, nu schimbare de comportament.
- Fiecare `handleX` intern isi pastreaza propriul bloc de validare env-uri Supabase (nu extrage helper comun — minimizeaza riscul si diff-ul).
- Commite fiecare Task ca si commit separat (cod).
- Dupa toate cele 3 consolidari, ruleaza `npx tsc --noEmit` o singura data la final pentru confirmare completa.
